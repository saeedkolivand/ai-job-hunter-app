# AI Job Hunter — Aider System Prompt

**Read `AGENTS.md` in the repository root and follow it in full — it is the canonical, single source of truth for all coding rules.** It is also in this session's `read:` set, so it is already in your context.

The rules there are enforced by ESLint, TypeScript, and CI. Never duplicate them here — edit `AGENTS.md` instead. Claude Code's extra orchestration lives in `CLAUDE.md` and does not apply to you.
