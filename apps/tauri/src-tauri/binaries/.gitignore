# Built sidecar binaries — generated by `pnpm --filter @ajh/scraper-runtime build:binary`
# Tauri requires the binary to be present at build time; CI builds them before tauri-action.
*.exe
scraper-runtime-*
!.gitkeep
!.gitignore
