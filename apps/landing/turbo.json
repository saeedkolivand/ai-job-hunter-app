{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "src/**",
        "public/**",
        "data/**",
        "next.config.ts",
        "tsconfig.json",
        "package.json"
      ],
      "outputs": ["out/**", ".next/**"]
    }
  }
}
