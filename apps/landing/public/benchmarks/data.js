window.BENCHMARK_DATA = {
  "lastUpdate": 1788393131630,
  "repoUrl": "https://github.com/saeedkolivand/ai-job-hunter-app",
  "entries": {
    "Export render": [
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "7de160e10f457b19a83d89575ac61733ee985299",
          "message": "feat: connect gmail for email-confirmation watching (auto-track layer c, foundation) (#689)\n\n* feat: add the email-watch foundation (store, imap connect, ipc)\n\nBackend half of auto-track Layer C (task #23): EmailWatchStore (email_watch.db,\naccount singleton + seen dedupe, Resettable, excluded from backups), a thin\nIMAP validate_connection seam (native-tls; imap's rustls-tls bridge pins a\nrustls-webpki with 4 live RUSTSEC advisories and no update path), the\nemail-imap keychain slot, and the 5 email_watch_* commands with contracts,\ntauri-client namespace, and mock parity. No poller/parser yet — that is PR B.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* feat: add the email-watch settings section and service hooks\n\nFrontend half of PR A (task #23): use-email-watch hooks (status query + 4\nmutations seeding the status cache), the accounts EmailWatchSection\n(connect form, connected view with enable switch and check-now, honest\nconsent disclosure), settings-search entry, and en/de translations.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* fix: harden the email-watch foundation per critic review + add adr-0013\n\nReview round (security PASS, rust-arch PASS, frontend 1 HIGH — all resolved):\nexact-pin the alpha imap dep, try_state in the mutating commands instead of a\npanicking state(), fresh-account semantics on an address switch (clears the\nuid watermark and seen table, +test), sentinel the spawn_blocking join error,\nlog imap error kinds only, keep text labels on the pending check-now and\ndisconnect buttons (a11y HIGH, +2 regression tests, en/de keys).\n\nDocs: ADR-0013 (imap-over-oauth economics, notify-dont-write, zero content\negress), the new imap egress class enumerated in README and SECURITY, and\nthree lessons persisted.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* fix: guard email-watch writes against a racing disconnect and strip password paste-spaces\n\nCloses the /review ensemble's verified advisories: set_enabled/record_check\nnow no-op once the account row is cleared (WHERE id = 1 AND address IS NOT\nNULL, row-affected result + 3 interleaving tests), so a disconnect landing\nduring the multi-second imap round trip can never leave enabled=1 on an\nempty row; the gmail app password is stripped of ascii whitespace before\nvalidation (google displays it space-grouped and pastes keep the spaces);\nadr-0005 gains egress class 7 so adr-0013's citation resolves.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* fix: address the verified coderabbit findings on the email-watch foundation\n\nSeven of eleven bot findings verified real against head and fixed: imap\nvalidation gets explicit connect/read/write timeouts via a manual socket path\n(the pinned crate's builder exposes none, so a black-holing server pinned a\nblocking worker forever); connect()/clear() multi-statement writes are now\ntransactional (a landed update + failed seen-delete could permanently skip the\nstale-mailbox cleanup); the app password clears from component state on a\nfailed connect too; adr-0005's stale six-classes count, adr-0013's overstated\nrevocation and read-only claims, and the readme/security sqlite-credentials\nconflation are corrected. The other four were verified already-fixed, false\npositives, or an advisory metric - in-thread replies document each.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 4.8 <noreply@anthropic.com>",
          "timestamp": "2026-07-16T18:33:40+02:00",
          "tree_id": "a5f70f8188977487bfdeeb19f36744a7ae6b381a",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/7de160e10f457b19a83d89575ac61733ee985299"
        },
        "date": 1784220885752,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2212912,
            "range": "± 41236",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2596431,
            "range": "± 23527",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 297890,
            "range": "± 9966",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "b6281d6d84d9f36b24f4de514961799ac43232e0",
          "message": "fix: track bridge connections with a refcount and push status changes to settings (#693)\n\n* fix(shared): add a bridge live-connection-changed push event\n\nWire-protocol lockstep for the bridge's live-connection change push: a new\nEVENT_CHANNELS.extensionBridge.changed constant, its ExtensionBridgeChangedEvent\npayload type, and the matching onChanged subscription method on the\nExtensionBridgeContract, plus the generated Rust EXTENSION_BRIDGE_CHANGED\nconstant (pnpm gen:ipc).\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* fix(bridge): count live connections instead of one shared flag\n\nBridgeState.connected was a single AtomicBool set true on every AuthOk and\nfalse on ANY socket close. With two browsers paired on the same token,\nwhichever socket closed last decided is_connected() for every other\nstill-open one — Chrome's MV3 service worker idling its socket closed the\nflag while Firefox was still connected, so the Settings pill went stale.\n\nReplace it with an AtomicUsize refcount: inc_connected on AuthOk,\ndec_connected on that same connection's teardown (gated by a per-connection\n`authenticated` flag so an unauthenticated close never decrements), and\nemit `EXTENSION_BRIDGE_CHANGED` only on the 0->1 / ->0 transitions. Saturating\ndecrement so an unmatched call can never wrap the count below zero.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* fix(desktop): subscribe the renderer to the bridge connection event\n\nWire the client, the app-global root layout, and a new\nuseExtensionBridgeEvents hook to the bridge's live-connection push:\ninvalidates the extension-bridge status query on a 0->1 / ->0 transition so\nthe Settings pill flips immediately on pair/unpair, instead of only on the\nexisting 30s poll (kept as a fallback for a missed/dropped event).\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* fix(desktop): add a manual refresh button to the extension-bridge status\n\nBeside the connection pill in Settings, a RefreshButton that calls\nrefetch() on the status query — its label stays visible and only the icon\nspins while pending, matching this section's other pending-button pattern.\nAlso documents (en+de) that multiple browsers can pair with the same token\nand that Regenerate token disconnects all of them, not just the current one.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* refactor: extract the applied-check read path from the bridge module\n\nextension_bridge/mod.rs grew past the R8 hard LOC cap (tests/architecture.rs)\nonce the live-connection refcount landed. Move the self-contained\napplied.check machinery (AppliedCheckOk, resolve_applied_check,\napplied_result_reply, handle_applied_check) into a sibling applied_check.rs,\nmirroring the existing status_update.rs / autotrack.rs split. Pure move: no\nbehavior change, pub(super) visibility so import_tests.rs keeps direct\naccess, call sites updated to applied_check::handle_applied_check.\n\nmod.rs: 1419 -> 1316 LOC (cap is 1400).\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 4.8 <noreply@anthropic.com>",
          "timestamp": "2026-07-16T23:47:23+02:00",
          "tree_id": "4de6d3ccb74077e51b2ec7775ce9c558f1ca3d2d",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/b6281d6d84d9f36b24f4de514961799ac43232e0"
        },
        "date": 1784239053154,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2125104,
            "range": "± 49147",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2525684,
            "range": "± 25692",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 287334,
            "range": "± 13954",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "d2f6fb2040202e1e3598c559540e6bf03c5398de",
          "message": "feat: watch the inbox for application confirmations and notify to confirm (auto-track layer c) (#696)\n\n* feat: add the email-watch poller, parser, matcher, and scheduler\n\nThe watcher half of auto-track layer c (task #23 pr b): a separate l2\nscheduler mirroring the autopilot one (15-min ticks, failure backoff, 60s\ncheck-now rate limit), the imap read path (uid search above the watermark,\nheaders first, bodies only for fingerprint hits, all read-only), an en+de\nconfirmation-email parser (mail-parser, rfc2047/mime decode, sender-domain\nhints boost but never gate), and a token-jaccard company/title matcher\nagainst saved applications. New matches stamp the seen table first, then\npush a notification card routing to the matched application - never an\nautomatic write. Also treats a failed socket-timeout set as a connect error\n(the deferred pr-a review advisory).\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* ui: update the email-watch settings copy for the live watcher\n\nCheck now runs the real fetch-parse-match-notify pass and the watch switch\ngoverns the 15-minute automatic check, so the placeholder copy is rewritten\nto the true semantics (en+de); the 60s check-now rate limit surfaces as\nfriendly fixed copy instead of a raw error, with tests for both rejection\nshapes.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* fix: guard concurrent email checks and broaden confirmation recall\n\nCritic round two (security PASS, match-quality PASS, rust-arch 1 HIGH): an\nin-flight RunGuard on the shared run_check seam closes both the check-now\nTOCTOU (gmail login bursts) and the stamp-before-notify concurrency gap;\neffective_last_uid is reused not duplicated; advance_last_uid enforces its\nmonotonic invariant in SQL; per-tick header/body/subject byte bounds; EXAMINE\nover SELECT for a read-only session; try_state for the credential store in the\nscheduler. Parser recall broadened per job-match review (thanks-for-your-\napplication and received-your-application EN shapes, dative ihrer bewerbung,\neingangsbestätigung, informal deine bewerbung), and/und boundary truncation\nfixed, plus 8 adversarial fixtures documenting the rejection-email and\nwrong-role precision limits that must gate any future auto-write.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* docs: add the email-watch domain doc and close out the layer c pointers\n\nNew knowledge doc for the watcher domain (read-only imap posture, watermark\nand dedupe contracts, en+de fingerprint philosophy, honest limits incl. the\nrejection-parity constraint that gates any future auto-write), the\nextension-domain layer c pointer updated, and four lessons persisted from\nthis build's review rounds.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* fix: clear seen on mailbox renumber and guard tick writes against disconnect\n\nReview-ensemble round (two verified HIGHs plus advisories): a uidvalidity\nchange now wipes the seen table in the same transaction as the watermark\nreset, so reused uids from a renumbered mailbox can never silently swallow a\nreal confirmation; the three post-tick writes and the notify path re-check\nthe account still exists, closing the disconnect-mid-tick resurrection race\npr a solved for its own writes; the uid search is watermark-scoped when the\ngeneration is confirmed unchanged; a concurrent-run refusal no longer\ninflates the backoff counter; and the renderer rate-limit sentinel is pinned\nby a cross-language parity test. The email-watch domain doc is corrected to\nthe shipped behavior (native-tls, credential persistence, real seen schema,\nnotify-only wording).\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* fix: pin the disconnect-mid-tick guard with a test and cap body fetches at the protocol level\n\nExtracts the account-still-connected check run_check_inner branches on into\na pure should_commit_outcomes fn (mirroring is_due/backoff_interval/\neffective_last_uid/classify_tick_outcome's own pattern) so the guard that\nsuppresses both post-tick writes and notify_match after a disconnect\nactually has a test pinning it, not just inline logic.\n\nAlso bounds fetch_bodies at the protocol level via a partial-octet\nBODY.PEEK[]<0.N> fetch item (confirmed supported by the imap crate's own\ndocumented grammar) instead of only truncating in-process after the whole\nmessage is already resident - a large/hostile mailbox no longer costs a\nmemory spike per candidate. The post-fetch cap in poller.rs stays as\ndefense-in-depth for a non-compliant server.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 4.8 <noreply@anthropic.com>",
          "timestamp": "2026-07-17T03:21:09+02:00",
          "tree_id": "e6334d5373faccc2fabf2c5bbf61ce6ce02667f3",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/d2f6fb2040202e1e3598c559540e6bf03c5398de"
        },
        "date": 1784252350789,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1721403,
            "range": "± 13413",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2109663,
            "range": "± 34437",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 238407,
            "range": "± 7130",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "a69cace83b81daa5a87737f126ad69c8350df260",
          "message": "ci: credit external contributors with @mentions in release notes (#697)\n\n* ci: credit external contributors with @mentions in release notes (corrected)\n\nCORRECTED IMPLEMENTATION: Properly wrap preset transform, not replace it.\n\nConvert .releaserc.json → release.config.mjs (ESM with top-level await).\nResolve preset factory with top-level await to extract its transform function,\nthen wrap it with a composition that:\n1. Calls preset's transform first (preserves type→section bucketing, hidden\n   filtering, reference linkification, BREAKING CHANGE handling)\n2. Respects the preset's filtering (returns false/null for hidden commits)\n3. Augments the result with (@<login>) credit if applicable\n\nThis fixes the v10 empty-notes gotcha: directly setting writerOpts.transform\nREPLACES the preset transform entirely, losing all the preset's logic.\nBy wrapping, we preserve the preset's work while adding attribution.\n\nAdd comprehensive tests covering:\n- extractGitHubLogin unit tests (6 cases)\n- Config loads with ESM and top-level await\n- Transform is properly wrapped in release-notes-generator plugin options\n- Guard script still validates notes correctly (9 tests, all passing)\n\nUpdate eslint.config.mjs to allow CommonJS in release.config.mjs (if needed\nfor CI compatibility—actually ESM now so may not be needed, but kept for safety).\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* test: add real end-to-end render test for release notes with contributor credits\n\nAdd comprehensive test that validates the wrapped preset transform by rendering\nactual release notes through @semantic-release/release-notes-generator. This\ntest would have caught the replace-not-wrap bug because the output would have\nbeen broken (no sections, leaked chore, no linkification).\n\nTest covers synthetic commits (thejesh23 fix, owner feat, chore) and asserts:\n1. Non-empty notes output\n2. Section grouping preserved (### headers present)\n3. Exactly one (@thejesh23) credit for external contributor\n4. Owner commits have NO (@mention) suffix\n5. Hidden commits (chore/ci/test) filtered out entirely\n6. References (#679) linkified in GitHub URLs\n\nHermetic fixture (no git dependency) ensures test is stable across branches.\nComplements existing extractGitHubLogin unit tests and config structure checks.\n\nThis render test guards the v10 empty-notes gotcha forever: if transform ever\nreverts to replace-not-wrap, these assertions will fail loudly.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* ci: dedupe login-extraction and fix stale .releaserc.json doc refs\n\nRe-validation of the contributor-credit release-notes change found two\ndrift risks and fixed them before push:\n\n- release.config.mjs defined its own copy of extractGitHubLogin instead\n  of importing the one in scripts/release-notes-transform.cjs, so the\n  unit tests (which do import from that file) were not actually\n  exercising the production regex — the two could silently drift.\n  release.config.mjs now imports the single implementation.\n- eslint.config.mjs's config-file override still referenced a\n  .releaserc.cjs path that never existed on this branch; removed (the\n  new release.config.mjs is already covered by the *.config.* glob).\n- Nine docs/agent-config files (CLAUDE.md x2, CONTRIBUTING.md,\n  docs/DEPLOYMENT.md, docs/DESIGN_DECISIONS.md, ADR-024,\n  .claude/agents/project-steward.md, .claude/skills/deployment-rules,\n  .claude/review-routes.json, a Rust doc-comment, and the guard script's\n  comment) still named the removed .releaserc.json; updated to\n  release.config.mjs. The review-routes.json glob was live-checked by\n  `pnpm check:agent-system`, which failed until fixed.\n\n* fix: harden preset transform resolution and esm require in test\n\nAddress two findings from the AI review gate:\n\n1. Harden release.config.mjs resolvePresetTransform to handle both\n   preset.writer.transform and preset.writerOpts.transform shapes,\n   for version robustness across preset versions (some use writer,\n   others writerOpts). Add explicit function type-check before\n   returning. Update comment to remove imprecise 'commits' mention\n   and clarify that both shapes are handled.\n\n2. Fix scripts/release-notes-transform.test.mjs line 161: add proper\n   ESM-compatible require via createRequire(import.meta.url) at the\n   top of the file (line 4), so the test works under plain node,\n   not just vitest's shim. Guard-validates test still passes.\n\nBoth gates verified:\n- pnpm test -- --project scripts: 15/15 suites, 31/31 tests (including\n  real generateNotes E2E render confirming 3 (@thejesh23) credits)\n- pnpm lint:strict: zero warnings/errors\n- pnpm check:agent-system: in sync\n\n---------\n\nCo-authored-by: Claude Opus 4.8 <noreply@anthropic.com>",
          "timestamp": "2026-07-17T04:55:40+02:00",
          "tree_id": "6a91ca6750ff8352feab4885160c235e327c6dcf",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/a69cace83b81daa5a87737f126ad69c8350df260"
        },
        "date": 1784257435457,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1725401,
            "range": "± 31521",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2059871,
            "range": "± 10997",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 228067,
            "range": "± 3297",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "4faf353f1725bc844293ec7e8b3148d418e2209f",
          "message": "refactor: consolidate static landing into apps/landing and retire the scroll-film app (#737)\n\n* refactor: consolidate static landing into apps/landing and retire the scroll-film app\n\nThe TERMINAL VELOCITY scroll-film (ADR-0016) is abandoned by owner decision mid-M4.\napps/landing is now the self-contained static site formerly at landing/ (no build step,\nno workspace package); pages.yml publishes it directly. The film milestones M1-M3 remain\nin history; the uncommitted M4 tree is preserved in a stash on feat/tv-m4-robot.\nADR-0017 records the decision and supersedes ADR-0016.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* chore: reroute landing review ownership to project-steward for the static site\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: mark gl fleet dormant in routing table and webgl-standards skill per adr-0017\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-07-20T20:46:05+02:00",
          "tree_id": "b7eae4beb3eb9fb287d3a913d69628f86a0b6489",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/4faf353f1725bc844293ec7e8b3148d418e2209f"
        },
        "date": 1784573751756,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2229606,
            "range": "± 58430",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2662471,
            "range": "± 43590",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 291163,
            "range": "± 3866",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "408d0dc30eebd6fc308d64705ccc78604ad80bbf",
          "message": "feat: migrate landing to next.js static export with real routes (#740)\n\n* feat: migrate landing to next.js static export with real routes\n\napps/landing becomes a Next 15 static-export workspace app: the 5 authored pages\nare real routes (faithful port, verbatim CSS/HTML slices + client gag scripts),\ndashboards/benchmarks/storybook stay public/ passthrough, flat export keeps every\nlegacy URL working. Adds the version.json release seam with a client freshness\ncheck (tested, incl. injection regression), resurrects check:parity as a permanent\ngate, rewires pages/ci/quality/release workflows + lint/knip/vitest, and lands\nADR-0018 (amends ADR-0017's no-build-step; directory consolidation stands).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* chore: excise unrelated extension wip from migration commit and drop stray turbo input\n\nThe six extension/scrape_url files belonged to a concurrent branch's in-progress\nwork and entered the previous commit via a blanket git add of a shared working\ntree; restored byte-for-byte to origin/main. That work ships on its own branch.\nAlso removes the nonexistent data/** turbo input (review finding).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-07-21T00:11:51+02:00",
          "tree_id": "41fd07414345066dc5f8fc18f52318e3d005d230",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/408d0dc30eebd6fc308d64705ccc78604ad80bbf"
        },
        "date": 1784586769901,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2152850,
            "range": "± 114280",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2530622,
            "range": "± 23272",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 286340,
            "range": "± 9706",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "b91a5d1cae12cfc3c6577582694ae24089dd3c79",
          "message": "fix(extension): parse linkedin job ad from captured page dom instead of authwalled refetch (#741)\n\n* fix(extension): parse linkedin job ad from captured page dom instead of authwalled refetch\n\nthe canonical (spa/list-view) import branch discarded the extension's captured html and\nserver-fetched /jobs/view/<id>, which linkedin authwalls — imports lost the description.\nresolve(canonical) stays primary; on an unusable or description-less result the bridge now\ngap-fills title/description from the hint-scoped detail pane only (whole-document json-ld\nis ignored so a list shell can never import the wrong job). the content-script hint tries\nvisible job-detail-pane containers before main.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: update extension-import canonical-url seam after fix 2d6a7eda\n\ndocument the list-shell JSON-LD scope safeguard: canonical branch never\ncalls parse_from_html() on captured list-shell DOM; only the hint-scoped\njob_root_generic_html() extraction may gap-fill when resolve() fails.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(extension): walk ancestor chain in job-node visibility gate\n\nisHiddenByStyle only checked the matched element's own computed\ndisplay, which does not inherit; reuse field-signal.ts's isHidden\n(ancestor-walk) instead, built via content.ts's own isolated\nRollup pass so the import stays classic-script-safe.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-07-21T01:08:02+02:00",
          "tree_id": "b53f58b48d7005f3acede33a33747d7543ed0792",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/b91a5d1cae12cfc3c6577582694ae24089dd3c79"
        },
        "date": 1784589448743,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2208501,
            "range": "± 16430",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2633551,
            "range": "± 14462",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 287702,
            "range": "± 6067",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "a0230890dfbfe85f8444fe1a9554cc734ea19d59",
          "message": "fix(jobs): post-756 review findings and ai review sticky restyle (#758)\n\n* docs: broaden adr-029 dash-tail risk note and log review fast-follows\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(scraping): dedupe split request keys before the count cap\n\nclamp_split_request trimmed/blank-filtered/byte-capped other_keys but did not\nde-duplicate before .take(MAX_OTHER_KEYS), so a repeated key wasted one of the\n32 slots (the insert is idempotent anyway). De-dup first-seen order preserved\nvia a HashSet seen-check before the cap; self-pairs equal to the clamped\nmember_key are already dropped. Extended the clamp tests with a 33-entry,\n2-duplicate case → 31 distinct pairs.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(jobs): guard agency list edits pre-load and cap the zod schema\n\nPost-#756 AI-review-gate findings:\n\n- AgencyCompaniesPreferences add()/remove() early-return when useJobPreferences\n  is still undefined (pre-load) — otherwise a quick add builds the next list from\n  an empty [] and replaces the user's saved agency list with just that one entry\n  (single-column setter, but still column data loss). Mirrors the sibling-panel\n  guards in 92e5302b, with a regression test asserting no mutate fires pre-load.\n- JobPreferencesSchema.extraAgencyCompanies gains .max(500) to mirror the Rust\n  MAX_EXTRA_AGENCY_COMPANIES cap (same pattern as otherKeys' .max(32)); IPC\n  codegen unchanged (gen:ipc --check green).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(scraping): pin split-request dedup collapse and full cap capacity\n\nThe Stop review-gate asked for two explicit cases on clamp_split_request:\n- duplicate other_keys collapse to ONE entry, first-seen order preserved;\n- de-dup runs BEFORE the count cap, so repeats never steal slots — >32 distinct\n  keys with one key hammered still yields the FULL 32 distinct keys, not fewer.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* ci: restyle ai review sticky comment into severity sections\n\nReplace the cramped one-table sticky body with a claude-review-style\nlayout — bold verdict line, expanded Critical/High/Medium sections with\nper-finding anchors, and a collapsed Low details block. Presentation\nonly: validateFindings/blockingFindings/exit-code logic (ADR-0008) is\nunchanged.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-07-21T07:04:28+02:00",
          "tree_id": "ebfe5327a600f3ab123e1d3b9c5739f18e4f4952",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/a0230890dfbfe85f8444fe1a9554cc734ea19d59"
        },
        "date": 1784610910889,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2119936,
            "range": "± 46834",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2558016,
            "range": "± 27490",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 287045,
            "range": "± 6853",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "ccaddb3a6b750e43ca05c5d6bd2920e32237d0e2",
          "message": "feat(scraping): harvest ats slugs passively with watched-company autopilot targets (adr-030) (#760)\n\n* feat(scraping): harvest ats slugs passively with watched-company autopilot targets\n\nPassively extract ATS company slugs from every scraped/imported posting URL\nand persist them, so the (later) slug typeahead and watched-company autopilot\ntargets populate with zero user effort (ADR-030).\n\n- extract_ats_ref (scraping/ats_ref.rs): the single URL-shape authority mapping\n  a posting URL to (ats, slug) for greenhouse/lever/personio/workable/ashby/\n  recruitee/smartrecruiters/breezy/bamboohr/pinpoint. scrape_url's greenhouse/\n  lever/ashby/smartrecruiters parsers now reuse these slug fns (no forked\n  shapes); host gates tightened to exact/suffix, slug casing preserved.\n- DiscoveredCompanyStore (discovered/mod.rs): own SQLite db, transactional\n  migration, upsert bump + display-name backfill, starred-first search,\n  watched() pairs. Resettable + DataStore (discoveredCompanies) + managed.\n- Parse-only harvest (commands::discovery::harvest_ats_refs) wired after\n  scrape_boards / scrape_url / autopilot record / extension import; degrades on\n  error, zero new network.\n- discovery IPC namespace (search/setStarred honest {error} union/watched) +\n  service hooks + query keys + mock-client entries.\n- AutopilotTarget gains watchedCompaniesOnly: a run resolves the starred set at\n  run time and injects per-ATS slugs (manual-style fan-out); empty stars skip\n  company boards with needs-company instead of the curated seed.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(scraping): scope watched company fan-out per board and harvest pre-filter\n\nAddress scraping-applier-expert review of the ADR-030 slug-harvest feature.\n\n- HIGH: watched-company autopilot resolution no longer flattens every starred\n  slug into the shared BoardSearchInput.companies (which fanned a foreign ATS's\n  slugs at each company board and crowded its MAX_COMPANIES cap). It now builds a\n  PER-BOARD map (board id -> slugs starred under THAT ATS) and threads it through\n  the engine's existing per-board seeded_companies override via a new\n  scrape_boards_with_overrides; input.companies stays empty. A selected\n  company-scoped board with no matching star is skipped needs-company (never\n  fetched with a foreign slug, never the curated seed). scrape_boards_with_resolver\n  keeps its signature (existing engine tests untouched); the core impl gains the\n  override. New engine mock-transport tests: ashby-only stars -> ashby gets its\n  slugs, greenhouse skipped, no cross-ATS request; plus a per-board purity test.\n- MEDIUM: autopilot harvest moved to the pre-filter scraped postings (ADR-030\n  paragraph c), matching the manual-scrape harvest point.\n- LOW: discovery_set_starred rejects an atsKind that isn't a registered\n  company-scoped board id (registry lookup) so a compromised renderer can't\n  materialize garbage rows.\n- LOW: discovery_watched now uses a dedicated watched_companies() store query\n  (full starred rows, no search-cap coupling); watched() pairs stay for the resolver.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(jobs): slug typeahead with starred companies and watched autopilot target\n\nReplace the ScrapeForm comma-separated company input with an ADR-030 slug\ntypeahead: a new @ajh/ui CompanyTypeahead primitive (multi-select chips +\nper-row star toggle + free-text add) fed by the discovery service hooks,\nmerging passively-harvested slugs with the selected boards' curated seeds.\nAdd a 'My watched ATS companies' target toggle to the autopilot wizard board\nstep that sets watchedCompaniesOnly, with an inline watched list + empty hint.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(scraping): close ats harvest acceptance gaps\n\nAdd the missing acceptance-matrix deltas for ADR-030 (passive ATS slug\nharvest + watched companies):\n\n- ats_ref: multi-level subdomains under a real ATS suffix extract None\n  (pins the subdomain_slug label.contains('.') branch).\n- discovery: fixture posting URLs harvested through extract_ats_ref into\n  a real DiscoveredCompanyStore surface via search (URL->store->typeahead\n  seam; reuses the shared production mapping, never re-derived).\n- autopilot_helpers: real store stars resolve through watched() + the\n  registry requires_company filter into per-board targets (the store->\n  resolver seam between the store and pure-resolver tests).\n- CompanySlugField / WatchedCompaniesField: star/unstar failure surfaces\n  an error toast when the mutation throws (no silent failure).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* chore: remove rtk tool prefix from agent and assistant configs\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(jobs): per-company control labels and blur-commit for the slug typeahead\n\nAddress frontend-reviewer findings on 84bf3a77:\n- Per-instance aria-labels: chip remove, star toggle, and wizard unstar now\n  interpolate the company name ({{company}}) so rotor/voice-control users no\n  longer meet N identical controls; CompanyTypeahead's starLabel/removeLabel\n  props take a per-row function. Tests switch to name-based queries.\n- Blur-commit: a typed-but-uncommitted slug is now flushed on focusout (guarded\n  by relatedTarget so a mid-click on a suggestion/star doesn't double-add), so\n  Start Scrape can no longer silently drop it.\n- Result-count aria-live status region on the typeahead (no role=option added).\n- Remove the orphaned jobs.companies.hint key from both locales.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* chore: route apps/landing to the frontend fleet with next.js 16 hardening\n\nlanding ownership moves from project-steward to frontend-author with\nfrontend-reviewer + ui-ux-expert as critics; agent definitions gain\nnext.js 16 static-export guidance (async params, no server features,\nunoptimized images, turbopack, parity gates) sourced from current docs.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: record ats slug harvesting (adr-030) in the knowledge base\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* style: apply rustfmt to discovery acceptance test\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(scraping): bound the watched-companies queries\n\nBoth `DiscoveredCompanyStore::watched()` (pairs) and `watched_companies()` (full\nrows) selected every starred row with no LIMIT, unlike `search`'s clamp(1,100)\ndiscipline (CWE-770 in principle — an unbounded read + per-run autopilot fan-out).\nAdd a shared `WATCHED_LIMIT = 500` bound (generous over any real starred set) to\nboth queries and pin it with a test (WATCHED_LIMIT + 1 stars → exactly 500).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: align adr-030 ipc names with the shipped discovery contract\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(scraping): recognize rippling posting urls in the ats slug extractor\n\nextract_ats_ref covered 10 of the 11 company-scoped ATS boards — Rippling had no\nparser, so a Rippling posting could never be harvested, appear in the typeahead,\nor be starred/watched. Add `rippling_slug` + register in PARSERS.\n\nShape (verified against boards::rippling): posting urls are host-locked to\n`ats.rippling.com/{slug}/jobs/{id}` (the board's `is_valid_rippling_job_url` guard\n+ every parse fixture, e.g. `https://ats.rippling.com/acme/jobs/job-abc-123`).\nCompany slug = first path segment, casing preserved (Rippling slugs are URL path\nsegments, mixed case allowed per `is_valid_rippling_slug` — not DNS labels).\nExact-host gate so the API host `api.rippling.com` (whose first segment is\n`platform`), the apex, and look-alikes → None. Positive + near-miss tests added.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(scraping): cover the harvest display-name fallback via a shared pure seam\n\nThe harvest acceptance test re-implemented the display-name fallback as an\nunconditional `Some(company)`, while production does\n`display_name.or_else(|| trim + empty-check)` — the trim/empty branch had zero\ncoverage (ADR-029 seam lesson: a test that re-implements the mapping tests\nnothing). Extract the pure per-posting mapping (`extract_ats_ref` + display\nfallback) into `posting_to_ref` (AppHandle-free); `harvest_ats_refs` and the\nacceptance test now both call it, and a new case pins that an empty/whitespace\ncompany yields `display_name` None (never an empty string).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(jobs): flush pending slug imperatively before start scrape\n\nThe blur-commit in 592ca7c9 is not sufficient: on WebKit (WKWebView /\nWebKitGTK — the app ships dmg/deb/rpm/appimage) a Start-button click doesn't\nreliably blur the focused input, and even when it does, add() queues an async\nstate update while onStart reads companies synchronously.\n\nCompanyTypeahead now exposes a CompanyTypeaheadHandle.commitPending() via a\nforwarded ref that synchronously runs the pending-query add path (idempotent\nonce the query is cleared). CompanySlugField forwards the ref; ScrapeForm's\nstart flow (Start button + query Enter-submit) calls\nflushSync(() => fieldRef.current?.commitPending()) before onStart when the\ncompany field is shown — the deterministic backstop the removed flushSync\nguard documented. Blur-commit stays for Chromium UX; zero role=option kept.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* chore: scrub rtk from cursor rules and use rg in the stale-branch check\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: point scraping-domain at the extractor and store instead of restating them\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(scraping): validate rippling slugs at extraction and tidy count casts\n\nAddress PR #760 external review quick-wins:\n\n- ats_ref::rippling_slug validated the first path segment against the SAME shape\n  boards::rippling enforces (is_valid_rippling_slug, now pub(crate)), so the store\n  never persists a slug the board would later refuse (path-traversal/query chars,\n  leading/trailing hyphen, over-length). Negative test added.\n- discovered/mod.rs: the seen_count column no longer routes through the epoch-ms\n  ts_from_db/ts_to_db helpers — plain inline casts (u64::try_from(..).unwrap_or(0)\n  / i64::try_from(..).unwrap_or(i64::MAX)) with the same clamp/saturate semantics,\n  since it is a count, not a timestamp. first_seen_at/last_seen_at keep the helpers.\n- engine test: present-but-EMPTY override entry ({\"greenhouse\": []}) is treated as\n  absent — needs-company skip, no fetch — pinning the empty-vec branch of the skip\n  gate distinct from a missing key.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(jobs): guard rapid star toggles and assert the watched field renders\n\n- CompanySlugField.toggleStar: bail when the star mutation is already in\n  flight (setStarred.isPending), so a rapid double-click can't fire two writes\n  off the same stale option.starred snapshot. Regression test: two rapid clicks\n  during a deferred (in-flight) mutation → the write fires once.\n- StepTarget.test: stop mocking WatchedCompaniesField to null. Render the real\n  component (discovery service hooks stubbed, NotificationProvider added) and\n  assert the watched-companies toggle appears in the target step, so its\n  insertion is actually covered.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-07-21T11:58:08+02:00",
          "tree_id": "9144cd8a8e1f968b263aa36e5ecd94466ddb2dbf",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/ccaddb3a6b750e43ca05c5d6bd2920e32237d0e2"
        },
        "date": 1784629157570,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2169186,
            "range": "± 68516",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2597806,
            "range": "± 15442",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 288543,
            "range": "± 15128",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "420ba60427f15bba3a86e323799bd871fdd84ebb",
          "message": "feat(ai): persist url import provenance and harvest from resolves (adr-031) (#761)\n\n* feat(scraping): harvest ats refs from single url resolves\n\nADR-031 §c: on a successful `scrape_resolve_url` (the single-posting synchronous\nresolve behind JobUrlImport), feed the resolved posting into the ADR-030 slug-\nharvest seam (source 'scrape'), so a URL import populates the slug typeahead like\nthe scrape/autopilot/extension paths. One call site reusing the existing pure\nextractor — zero new network. Harvests the posting's FINAL/canonical `url` (what\ngets stored on it — an aggregator click-tracker resolves to the board's real\nposting url), matching the other harvest sites, and degrades-not-fails via the\nseam's established log::warn. Seam-level test (resolved-posting-shaped input →\nstore contains the ref) drives the same pure `posting_to_ref` seam the\nAppHandle-bound command uses (no mock-app harness).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(ai): persist job url provenance from url imports\n\nADR-031 renderer half: a URL-imported job ad now carries its provenance\nend-to-end so the generation joins applied-detection + cluster provenance.\n\n- JobUrlImport surfaces the resolved posting's canonical url + board alongside\n  the composed text via onImport(text, { url, board }).\n- JobAdField routes a URL import to a new optional onImport sink (falls back to\n  onChange for the Resume Analyzer, which doesn't persist provenance).\n- AIGeneratePage records provenance in the ai-generate session slice on import\n  and CLEARS it on any manual edit / paste-over / upload (stale-url foot-gun),\n  then threads jobUrl + board into useGeneration.persist(), which populates the\n  existing AiGenerationSaveRequest.jobUrl/board only when present.\n\nFailure path unchanged and audited (ADR d): scrape_resolve_url returns\njson!(null) for every failure (no distinct rate-limit union to surface), so an\nunresolvable url shows the inline jobUrlImport error with the field still\nusable; the Enter keyboard flow works. en+de parity preserved (no new keys).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: record url import provenance (adr-031) and close the backlog row\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: drop phantom harvestsource type from adr-031 owning symbols\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(scraping): document the resolve call-site coverage gap\n\n`single_resolve_harvests_the_resolved_postings_slug` pins the pure harvest seam\n(URL → ref → store → search) but NOT the `scrape_resolve_url` call site that\nwires `(posting.url, posting.company)` into it — a swapped arg order or a dropped\nharvest call there would still pass. Extend the test's header docstring to state\nthis explicitly (the call-site wiring is AppHandle + network-bound, unreachable\nwithout a mock-app harness this crate deliberately avoids; review + arg names\nguard it), mirroring the sibling acceptance test's honest-comment style. No code\nchange.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ui): label generation board chips via the boards i18n map\n\nThe GenerationCard board chip rendered gen.board raw, so a URL-imported\ngeneration whose generic-fallback resolve sets source 'url' showed a literal\nuppercase 'URL' badge. Route it through t('jobs.boards.${gen.board}', {\ndefaultValue }) like the jobs list, and add a jobs.boards.url pair\n(Web import / Web-Import) so the fallback reads humanely.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor: move ats harvest seam below commands into discovered harvest module\n\ndrops the l3-to-l3 peer import flagged in review: harvest_ats_refs + posting_to_ref move from\ncommands/discovery.rs to discovered/harvest.rs, apphandle-free (take the store directly). all 5 call\nsites (scrape x3, autopilot, extension import) resolve the store at the shell boundary and pass it\ndown; missing store stays a graceful no-op. the moved tests now drive the real harvest fn, pinning\nthe (url, company) arg order the review found unguarded.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test: assert localized board chip labels and unknown-board fallback\n\nboard-chip tests now render through the real en resources (getFixedT) instead of a key-echo mock, so\na missing jobs.boards.url resource fails; adds the unknown-board defaultvalue-fallback case.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: cite the moved harvest symbols and correct the adr-031 rate-limit claim\n\nadr-031 now points at discovered::harvest::harvest_ats_refs (both occurrences) and describes the\nreal 429 behavior: failures collapse to null with the generic inline error; distinct backoff\nsurfacing stays a fast-follow.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-07-21T21:32:29+02:00",
          "tree_id": "fd01ff259fd4c7ab6b6be82a503eccef9b9d39ce",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/420ba60427f15bba3a86e323799bd871fdd84ebb"
        },
        "date": 1784663494394,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2017225,
            "range": "± 69349",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2485931,
            "range": "± 102596",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 230664,
            "range": "± 8227",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "5c4453e00f8dd58687e970b1d934a10c234dfa40",
          "message": "fix: persist completed stream text so the poll fallback works (#828)\n\n* fix(ai): persist the completed stream text for the poll fallback\n\nThe renderer's poll fallback (awaitAiStream) recovers a dropped stream frame or a\nmissed `done` event by reading `result.text` from the completed job, but the backend\npersisted only `{ done: true }` — so PR #802's longer-wins fix was a runtime no-op.\n\nAccumulate the completed answer (non-thinking deltas only) in the provider-agnostic\nstreaming completion path (`stream::finish`, shared by openai/anthropic/gemini/ollama)\nand in the CLI-agent path (`cli_agent::emit_done`), and persist it as `result.text`.\nBoth strip inline `<think>…</think>` via a shared `strip_think_blocks` mirroring the\nrenderer's think-splitter, so the persisted text is the SAME shape the renderer buffers:\nthe poll's `persisted.length > buffer.length` compares like-for-like and can never\nresolve reasoning markup into the final document.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(generate): align the stream poll test with the real job contract\n\nThe prior mock returned `{ status: 'completed', result: { text } }`, a shape the\nbackend never produced, so the regression test proved nothing. Mirror the real\ncontract (`result: { done: true, text }`, text think-stripped) and add a case pinning\nthat a recovered persisted result can never surface `<think>` markup.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-07-22T04:10:06+02:00",
          "tree_id": "556b2f988245c9acb062abe20fc3d132e5a1e6d0",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/5c4453e00f8dd58687e970b1d934a10c234dfa40"
        },
        "date": 1784687437989,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2244132,
            "range": "± 19353",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2688114,
            "range": "± 33514",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 299189,
            "range": "± 14333",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "4d75800ebacd2667d7ea52c339c54d73940f7823",
          "message": "fix: small advisory batch (rust) (#829)\n\n* perf(autopilot): skip duplicate note candidates within one run\n\nrun_notes_loop keyed only against prior runs, so the same NEW job surfaced\nunder two URL variants in one run bought an AI note for each; the later\nmerge_found_jobs collapsed them and discarded one - waste against the\nASSISTANT_NOTES_MAX ceiling. Track canonical keys seen this run and skip the\nsecond variant (before charge_daily, so it also spares the shared ceiling).\n\nThe oversized autopilot_helpers inline test module is moved to a sibling\ntests.rs so mod.rs stays under the R8 LOC cap; no test behavior changes.\n\nSource: #813 follow-up.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ai-generations): enforce one aggregate row per job url with upsert\n\nsave_application released the store lock between find_by_job_url and the\ninsert, so two concurrent saves for one job could both miss and both insert,\nforking the per-job aggregate. Add a UNIQUE(job_url) partial index (WHERE\njob_url != '', so deliberately unlinked '' rows still coexist) and recover\nfrom the resulting insert conflict by merging into the row the other writer\ncreated. The migration collapses any pre-existing fork before creating the\nindex, keeping the linked-or-newest row.\n\nTwo applications delete/detach tests inserted duplicate-url child rows to\nexercise multi-row cleanup; they now use distinct urls per the new index.\n\nSource: #816 follow-up.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* chore(applications): tighten the set-status test bound and trim a stale comment\n\nThe set-status contention test asserted events.len() >= ITERS; tighten it to\nthe exact count (ITERS * 2 transitions across the two threads + 1 creation\nseed event). Also drop the stale tail of set_status's comment claiming\n.transaction() needs a &mut *guard - the code uses guard.transaction() via\nauto-ref.\n\nSource: #811 nits.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-07-22T06:29:11+02:00",
          "tree_id": "e91539f5376eba2945b9df0a0f97d073a0ac953b",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/4d75800ebacd2667d7ea52c339c54d73940f7823"
        },
        "date": 1784695147820,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2121392,
            "range": "± 82214",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2545666,
            "range": "± 129549",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 288456,
            "range": "± 11471",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "49699333+dependabot[bot]@users.noreply.github.com",
            "name": "dependabot[bot]",
            "username": "dependabot[bot]"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "e0eebbcbfceb446412760147d592ca442a0b0063",
          "message": "chore: bump uuid from 1.23.5 to 1.24.0 in /apps/desktop/src-tauri (#871)\n\nBumps [uuid](https://github.com/uuid-rs/uuid) from 1.23.5 to 1.24.0.\n- [Release notes](https://github.com/uuid-rs/uuid/releases)\n- [Commits](https://github.com/uuid-rs/uuid/compare/v1.23.5...v1.24.0)\n\n---\nupdated-dependencies:\n- dependency-name: uuid\n  dependency-version: 1.24.0\n  dependency-type: direct:production\n  update-type: version-update:semver-minor\n...\n\nSigned-off-by: dependabot[bot] <support@github.com>\nCo-authored-by: dependabot[bot] <49699333+dependabot[bot]@users.noreply.github.com>",
          "timestamp": "2026-07-22T23:08:06+02:00",
          "tree_id": "ccb98cc7dcbd0cf12d8616f0ddb9a96a34aad20d",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/e0eebbcbfceb446412760147d592ca442a0b0063"
        },
        "date": 1784756075335,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2156873,
            "range": "± 22140",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2592760,
            "range": "± 34304",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 302271,
            "range": "± 17168",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "35212698+thejesh23@users.noreply.github.com",
            "name": "Thejesh",
            "username": "thejesh23"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "a7c217b05fb08e08615fac97d4b2091290553879",
          "message": "fix(scraper): delegate board location filtering to the central location_filter (#835)\n\n* fix(scraper): match a 'City, Country' location label segment-wise\n\nThe location box normally holds the geocode picker's \"<city>, <country>\"\nlabel — `commands::geocoding` builds it exactly so \"the UI only ever shows\n'City, Country'\", and both entry points (jobs ScrapeFilters, autopilot\nStepTarget) write `suggestion.display` straight into `location`.\n\nThe two boards that filter location client-side required that WHOLE label to\nappear as one contiguous substring of the board's own location text. No board\nwrites it that way:\n\n  The Muse        \"New York, NY\"            vs request \"New York, United States\"\n  GermanTechJobs  \"Karl-Marx-Allee 1, Berlin\" / \"Munich, Bayern\"\n                                            vs request \"Berlin, Germany\"\n\nso the filter dropped every row and the board reported 0 results for any\npicked city. Typing free text without picking a suggestion still worked,\nwhich is why it survives manual testing.\n\nMatch segment-wise instead: any non-empty comma-separated part of the request\nappearing in the haystack is enough. Conservative — it errs toward keeping,\nmirroring the engine's own `location_filter`, which already tokenizes the\nrequest and keeps on any token hit for this exact reason.\n\n- adds `common::location_matches`, shared by both call sites\n- extracts `germantechjobs::matches_gtj_filters` from `search()`'s `retain`\n  so tests drive the real predicate instead of a copy that could diverge\n  (same reasoning as The Muse's `apply_filters`)\n\nSingle-segment requests behave exactly as before.\n\n* fix(scraper): delegate board location filtering to the central location_filter\n\nReworks the fix per review. The first version added a board-local segment-wise\nmatcher (`location_matches`); this removes board-local LOCATION filtering from\nthe three `supports_location() == false` boards entirely and lets the engine's\ncentral `location_filter` be the single authority — the reviewer's preferred\ndirection, and the one the issue points at.\n\nWhy this is correct and strictly better than a second local matcher:\n\n- The Muse, Comeet and GermanTechJobs are all non-location boards, so the engine\n  already runs `location_filter` on their output both as a live gate\n  (`KeepItemByBoardFn`) and as a post-hoc safety net. Board-local location\n  filtering was redundant with it.\n- The central filter is diaeresis/exonym-aware (München/Munich, Köln/Cologne via\n  the curated table + DIN-5007-2 folding) and conservative (keeps remote /\n  empty / unknown, and a country-code-only request is inert). A raw\n  case-insensitive board-local matcher had none of that and, running BEFORE the\n  central filter, produced false drops the central filter is designed to rescue.\n- The original 0-results bug is still fixed: `location_spec()` feeds the picker's\n  \"<city>, <country>\" label into the central filter, which TOKENISES it (it does\n  not require the whole label as one substring), so a geocode-picked city keeps\n  its rows via the central filter alone.\n\nChanges:\n- common.rs: `matches_filters` is now keyword-only (drops the `location` param);\n  `location_matches` is deleted.\n- germantechjobs: `matches_gtj_filters` is keyword-only; the unused `loc` local\n  is removed.\n- The Muse / Comeet call sites pass query only.\n- Tests updated across common/themuse/germantechjobs: the board-local location\n  assertions are replaced with ones pinning that these boards no longer drop on\n  location (delegated to the central filter, whose own exonym/diacritic behaviour\n  is unit-tested in engine::location_filter).\n\nKnown limitation (unchanged by this PR, now consistent across all non-location\nboards): because `location_spec()` stores the whole label in `city`, the central\nfilter also tokenises the country segment, so \"Munich, Germany\" keeps a\n\"Frankfurt, Germany\" row. That is a central-filter property shared by every\nnon-location board, not something this PR introduces; tightening it is a separate\nchange to `location_filter`/`location_spec`.\n\n---------\n\nCo-authored-by: thejesh23 <thejesh23@users.noreply.github.com>",
          "timestamp": "2026-07-23T10:03:53+02:00",
          "tree_id": "6b171ae5ebe713b3506e7da252f1f751fde06b11",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/a7c217b05fb08e08615fac97d4b2091290553879"
        },
        "date": 1784795705083,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2159490,
            "range": "± 83256",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2542820,
            "range": "± 29480",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 290453,
            "range": "± 23312",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "35212698+thejesh23@users.noreply.github.com",
            "name": "Thejesh",
            "username": "thejesh23"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "a9c0c91d8287dcd5ad71085541caaba18b1895d6",
          "message": "fix(export): do not consume a letter's salutation as the letterhead name (#876)\n\nThe DOCX letter renderers treated the first non-blank line as the letterhead\nname whatever it was: `if !header_done && docx.document.children.is_empty()`.\nA letter that opens straight at the salutation (no letterhead) therefore had its\n\"Dear …\" line consumed as the name and replaced with meta.candidate_name — and\nbecause `in_body` is set only in the salutation arm, the whole body then\nrendered in the muted addressee style.\n\nGuard the name block with `!is_salutation && !is_signoff` (both already computed\njust above) so a salutation-first letter falls through to the salutation arm,\nwhich sets header_done/in_body correctly. The block is duplicated in\ngenerate_cover_letter_docx_classic (Classic) and generate_cover_letter_docx_layout\n(Refined/Banded); both are guarded.\n\nTest opens a letterhead-less letter with a candidate name set and asserts the\n\"Dear Hiring Manager\" line survives, across all three layouts. Fails on main\n(Classic path: the salutation is replaced by the candidate name).\n\nCo-authored-by: thejesh23 <thejesh23@users.noreply.github.com>",
          "timestamp": "2026-07-23T13:15:43+02:00",
          "tree_id": "648cdd7b9f8f6b6fec31146b3d2c544b0c353c60",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/a9c0c91d8287dcd5ad71085541caaba18b1895d6"
        },
        "date": 1784806664280,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2211462,
            "range": "± 8561",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2632122,
            "range": "± 21564",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 292792,
            "range": "± 6115",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "a8bf6089e9e4d89a4daccfc0000da8c6bf219d2f",
          "message": "fix(export): keep a subject-line-first letterhead-less letter's subject line (#878)\n\nExtends the #876 name-block guard in both docx letter renderer arms with\n!is_subject_line, so a letter opening with a subject/reference line (e.g. a\nGerman Betreff: line) and no letterhead name no longer has that line consumed\nas the candidate name. The later subject-line call sites reuse the computed\nbinding instead of recomputing. Pinning test covers all three letter layouts\nand fails on the unguarded renderer.\n\nFixes #877\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-07-23T13:44:23+02:00",
          "tree_id": "15e825023643862c1ae397913a79b422e62b7d3f",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/a8bf6089e9e4d89a4daccfc0000da8c6bf219d2f"
        },
        "date": 1784807653153,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2202960,
            "range": "± 7815",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2625640,
            "range": "± 17341",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 293703,
            "range": "± 2009",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "95a0656baecf8b0f05f4c5333dcc3962303af085",
          "message": "fix: scraping response caps, country backfill, jobicy label (#884)\n\n* fix: raise lever/ashby response cap and backfill scrape country code\n\nFour scrape-trust fixes:\n\n- Lever/Ashby fetch a company's WHOLE board in one call, so a large employer\n  (veeva, openai) tripped the shared 8 MB guard and the company was dropped\n  silently. Raise max_bytes to 16 MB for these two boards only, following the\n  germantechjobs precedent.\n- Per-company ATS fetch failures were invisible whenever a sibling company\n  succeeded (the run still returns Ok). Lever/Ashby now count them and emit the\n  new companies-failed:<n> note via ctx.report_note; BoardSummaryChips maps it\n  to a localized chip (en/de).\n- Manual scrape passed req.country_code straight through, so a typed-freehand\n  location arrived with none and the aggregator hardcoded a 'de' guess and\n  suppressed broadening. derive_country_code moves from commands::autopilot to\n  commands::geocoding (next to suggest) and now also runs on the scrape path,\n  inside the spawned task so the command still returns its jobId immediately.\n- ScrapeFilters kept a stale picked countryCode/lat/lon when the location was\n  typed over; clear them on freehand input, mirroring autopilot's StepTarget.\n- Add the missing jobs.boards.jobicy label (en/de) and defaultValue fallbacks\n  at the four bare board-label call sites.\n\n* fix: close the lost-cancel window on the scrape geocode backfill\n\nReview fixes from the scraping-applier audit:\n\n- HIGH: a jobs_cancel landing during the awaited geocode backfill was silently\n  discarded — ScraperEngine::cancel removes the job slot before cancelling it,\n  so the later scrape_boards found no pre-registered token and minted a fresh,\n  un-cancelled one; the abandoned run then scraped on and its first streamed\n  item cleared the cache the user's new search was filling. Register a clone of\n  the token, race the backfill against it with a biased tokio::select!, and\n  return without starting the scrape when it is already cancelled. Engine\n  contract test pins the mint-fresh behavior the guard exists for.\n- Drop the non-cancellation gate on the new companies-failed note in lever and\n  ashby: the engine cancels ctx.signal the moment the central amount cap fills,\n  which is exactly when a long seeded company list has failures worth showing.\n  failed_fetches only ever counts non-cancellation errors, so the count is\n  honest without the gate.\n- Extract LEVER_BASE_URL + fetch_lever_company and a base-parameterized search\n  loop (the aggregator's JOOBLE_BASE_URL pattern) so the note wiring is covered\n  by two wiremock tests: 200-then-404 yields companies-failed:1 with the good\n  company's postings kept, and an all-success run emits no note.\n- Document companies-failed:<n> in the BoardScrapeSummary note vocabulary and\n  amend the at-most-one-of sentence it invalidated.\n- ScrapeFilters test: the LocationInput stub now fires onChange then\n  onSelectSuggestion in the real order and the pick assertion reads the last\n  call, so a clear-after-pick regression can actually fail it.\n- Correct the lat/lon clearing rationale: no board consumes the coordinates\n  today; they ride into LocationSpec and countryCode is what changes behavior.\n\n* fix: cancel scrape jobs in place so a pre-run stop is never lost\n\nRoot fix for the lost-cancel window plus the PR review follow-ups:\n\n- ScraperEngine::cancel now cancels the job slot in place instead of removing\n  it. Removing it meant a run reaching the engine after the cancel found no\n  slot and minted a fresh, un-cancelled token — the window spans the caller's\n  pre-scrape work AND the engine's own semaphore wait, so the abandoned run\n  could scrape on and clobber the search the user started instead. Verified no\n  leak first: every owner (commands::scrape both exits, autopilot_run's\n  scrape-Err, cancelled and success paths) already unregisters, and an\n  engine-minted slot is removed by the we_minted branch.\n- Replace the tautological map-bookkeeping test with a real one: cancel, then\n  run scrape_boards_with_resolver against FakeScraper and assert zero items\n  streamed and Err(\"scrape cancelled\"). Add an idempotent-cancel companion\n  pinning the slot-survives invariant the no-leak argument rests on.\n- Extract backfill_country_code(token, input) -> bool with an injected-lookup\n  seam, and unit-test all four cases against hung/instant/never-polled futures\n  (no network, no AppHandle): pre-cancelled never polls the lookup, a cancel\n  during the lookup wins immediately, a resolved lookup fills the country, and\n  a picked country is never clobbered. Comment now states honestly that the\n  command layer narrows the window and the engine layer closes it.\n- Ashby parity with lever: ASHBY_BASE_URL + fetch_ashby_company + a\n  base-parameterized search loop, with the same two wiremock tests\n  (partial failure yields companies-failed:1 and keeps the good company's\n  postings; a clean run emits no note).\n- JobsPage: functional setScrapeForm update so two changes dispatched in one\n  tick can't drop the first.",
          "timestamp": "2026-07-28T00:31:15+02:00",
          "tree_id": "9c57874ce6536a30f9d158acea9251a1bf9a58fd",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/95a0656baecf8b0f05f4c5333dcc3962303af085"
        },
        "date": 1785192737897,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2155166,
            "range": "± 68469",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2641056,
            "range": "± 50287",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 319309,
            "range": "± 8889",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "650bcdda79e22036559745e2dbda7b99930acc58",
          "message": "feat: revoke extension pairing over the bridge on token rotation (#895)\n\n* feat: revoke extension pairing over the bridge on token rotation\n\nRotating the pairing token (Settings -> Regenerate, or a factory reset) left\nlive authenticated sockets running and the connected count up, and a\nreconnecting extension only saw the deliberately silent failed-handshake close\n- indistinguishable from a crashed app - so it retried the dead token forever\nand never returned to its pairing view.\n\nAdds a desktop -> extension `token.revoked` frame (TS constants + zod enum and\nthe Rust msg mirror, in lockstep). BridgeState::regenerate_token now signals\nevery live connection task, zeroes the live-connection count, then swaps the\nsecret, all inside the token lock so no handshake can authenticate against the\nold token and miss the revoke. Only an ALREADY-AUTHENTICATED socket is sent the\nframe; an unauthenticated one closes silently as before, preserving the\nno-token-oracle invariant (ADR-0010). The frame carries no payload and no token\nmaterial.\n\nThe extension drops its stored token through the same local un-pair path the\npopup's Unpair button uses, skips the backoff loop, and re-probes once unpaired\nso the popup lands on the pairing view. Old extensions ignore an unknown wire\ntype, so no protocol-version bump is needed.\n\nAlso moves the msg constant table into its own module (the addition pushed\nextension_bridge/mod.rs past the R8 hard LOC cap) and mentions re-pairing in the\nfactory-reset confirm copy (en + de).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: honor token.revoked only on an authenticated bridge session\n\nThe `handshakeFrame` intercept is a step-in-flight latch, not an auth check:\n`awaitHandshakeFrame`'s `settle` nulls it the instant a step's frame is\nconsumed, so a frame arriving between steps - notably while `computeProof` is\nawaited - fell through to the type dispatch. A port-squatter needs zero token\nknowledge to send a syntactically-valid `challenge`, then a `token.revoked`, to\ndestroy the stored pairing credential; the revoke close path's backoff reset\nmade that loopable. The pre-hello window and the no-token attach (which reaches\nphase `connected` with no handshake at all) were exposed the same way.\n\nAdds a real `authenticated` flag on BridgeClient, set only where serverProof\nverification completes and cleared on every attach and on close, and gates the\nrevoke branch on it. The branch stays below the handshake intercept so both\nprotections compose.\n\nTests: the security probe is now permanent (valid-challenge-then-revoke during\nthe computeProof window must not unpair), plus a stale-frame-after-close probe\nand a revoke aimed at the unpaired re-probe socket - both verified to fail\nwithout their respective guard.\n\nAlso: a failed token clear now forces bad_token instead of silently retrying a\nknown-dead secret, and the rotation lock-scope comments no longer overclaim\n(the proof is verified outside the token lock; safety rests on\nsubscribe-at-accept plus the buffered broadcast).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: bind the bridge handshake verdict to the transport that earned it\n\nWS5 defense-in-depth. `authenticated` is set after `await computeProof(...)`,\nso a socket that closes inside that await had the flag cleared by `onClose` and\nthen set straight back by the resuming continuation - leaving it true with a\nnull transport until the next attach, and `setPhase('connected')` claiming a\nlive session over nothing. Only a valid serverProof reaches that line, so it\nwas not peer-reachable, but a stale-true auth flag on the bridge is worth zero\ntolerance: the next frame delivered on that dead transport would be trusted.\n\nRe-checks the captured transport identity before applying the verdict. Plain\nreturn rather than finishHandshake: whatever ended the transport already set\nits phase and armed its reconnect, and finishHandshake would null and close\n`this.transport` - which in the replaced case is a different, healthy socket.\n\nTest: socket closes during the serverProof await -> phase never becomes\nconnected and a following token.revoked is ignored. Verified to fail before the\nguard (phase came back connected on a dead transport).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* style: apply rustfmt to extension bridge\n\nFormatting drift from my own edits, not the main sync: the third `next_step`\nargument rewrapped the read loop's match, and the new `token_revoked_reply`\nassert exceeded the width. `cargo check`/`clippy`/`test` all pass on\nbadly-formatted code, so nothing I ran locally caught it - `cargo fmt --check`\nis its own gate.\n\nFormatting only, no behavior change: cargo test --lib extension_bridge 315\npassed and clippy --all-features --all-targets is clean after the reformat.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: harden bridge pairing revocation after review\n\nHIGH - the failed-clear branch was dead under test. Every revoke case resolved\n`onTokenRevoked`, so nothing exercised the path where storage refuses to drop\nthe token. Covered now: rejecting the clear must surface `bad_token` and start\nno re-probe, since reconnecting would put a known-dead secret back on the wire\nand resume the forever-retry loop the frame exists to end.\n\nMEDIUM - count theft. A socket parked in a long dispatch await had not polled\nits revoke receiver when the rotation zeroed the count; if a browser re-paired\nfirst, that socket's later teardown decremented a count it no longer owned and\n`is_connected()` under-reported a live pairing. Adds a rotation epoch bumped\ninside the same lock hold; a connection stamps the epoch it counted itself\nunder (read AFTER the increment) and only decrements while it still matches.\n\nMEDIUM - the desktop no-oracle gate had no Rust coverage. Extracted as the pure\n`revoke_frames(authenticated)` with both arms pinned: authenticated gets the\nrevoke then a close, unauthenticated gets nothing at all.\n\nLOW - compute both handshake proofs up front, leaving zero await between\nreceiving `auth.ok` and setting `authenticated`; the old lazy server-proof await\nsat exactly where a legitimate `token.revoked` lands and dropped it. LOW - a\nclosed broadcast channel is no longer treated as a revocation (it would have\nmass-unpaired every browser on a channel-lifecycle change); it tears the\nconnection down without a frame. LOW - cancel assist streams before enqueueing\nthe close, so no billable chunk trails behind it. LOW - reset the revoke latches\nin `attach()` like their siblings. LOW - the rotation doc now names BOTH\nload-bearing invariants (single lock hold AND subscribe-at-accept).\n\nAlso splits the revoke wire surface into its own module: the epoch work pushed\nextension_bridge/mod.rs past the R8 hard LOC cap again.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-07-28T06:24:58+02:00",
          "tree_id": "b9ec9704198538786893d9d063cb146eb61b08f6",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/650bcdda79e22036559745e2dbda7b99930acc58"
        },
        "date": 1785213999794,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2130085,
            "range": "± 45825",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2544464,
            "range": "± 78726",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 295775,
            "range": "± 16269",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "167cc5a6a5e043a0a5044b4cde9246a3cdf1d7d2",
          "message": "feat: page-bounded aggregator fetch loop with amount-derived search budget (#896)\n\n* feat: page the aggregator by requested amount\n\nAdzuna and JSearch previously issued exactly one request per search\nregardless of how many jobs the user asked for, so any amount above one\npage silently under-filled.\n\nAdzuna now loops `page in 1..=ceil(amount/50).min(2)`, stops early on a\nshort page, de-dupes across pages by posting id, fails open mid-loop\n(page 1 still propagates its error so primary_chain can fall through to\nJSearch), and re-checks cancellation between pages. JSearch maps the same\namount onto `num_pages = min(ceil(amount/10), 3)`.\n\nThe budget is driven by the requested AMOUNT, never by\nBoardSearchInput::pages: the manual search path hardcodes\npages = MAX_PAGE_BUDGET and Adzuna's free tier is a daily call quota, so a\npages-driven loop would multiply the spend of every search. An amount that\nfits in one page still costs exactly one request. The near-empty broaden\nretry stays single-page.\n\nBecause `pages` is inert for the aggregator, the autopilot wizard now\ndisables the \"Pages to scrape\" field (with a hint, en+de) when the\naggregator is the only selected board; a mixed selection keeps it live.\n\nThe Adzuna provider moves to its own module — providers.rs crossed the R8\nmodule-size cap.\n\nRiders from the #884 re-verify: name commands::agent::agent_run as the\nthird cancel-token owner in ScraperEngine::cancel's rustdoc; scope the\ncancel-idempotence test comment to idempotence; pin LocationInput's\nonChange-before-onSelectSuggestion ordering at the source.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: bound aggregator spend by an explicit budget, not the amount sentinel\n\nThe page loop derived its upstream budget from BoardSearchInput::amount, but\nthat field is sentinel-prone: autopilot_helpers sets amount = 100 to mean\n\"don't cap me\" (it expresses its target in pages), not \"fetch 100 postings\".\nRouting it into the loop bought 2 Adzuna calls and 3 billed JSearch pages on\nevery scheduled run — an hourly autopilot goes 24 to 48 calls/day against a\nhard DAILY quota, and exhaustion compounds because an Adzuna error falls\nthrough to the 3x-billed JSearch tier.\n\nAdd BoardSearchInput::provider_amount (Option<u32>, default None = cheapest\nsingle-request form) as the only signal that buys upstream calls, and set it\nsolely in commands::scrape, where the amount is a count the user actually\ntyped. amount keeps its meaning as the output cap. SearchBudget carries the\ntwo independently through the aggregator's orchestration.\n\nGuarded by a differential pair of wiremock tests through search_with_providers:\nan autopilot-shaped input (amount 100, no provider budget) issues exactly one\nAdzuna request even when page 1 comes back full, while a manual-shaped input\npages. Verified by reintroducing the bug: the guard fails.\n\nAlso give AdzunaProvider the same base_url injection the page request has, and\ncover the post-loop policy that reads the loop's post-dedup count (the\ncountry-wide broaden retry and the guessed-market note) through search itself.\n\nCorrect three comments: the broaden retry's worst case is ADZUNA_MAX_PAGES + 1\nfetches (a full page 1 that all dedupes away keeps the loop going); the\nper-host limiter is nominal at this budget; and the in-loop cancellation check\nis load-bearing, since without it a Stop is logged as a page failure.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test: pin the aggregator request-to-budget mapping against mutation\n\nThe wiremock spend guards hand-construct SearchBudget, so they never observe\nthe request -> budget translation — rewriting it to spend `amount` kept all\n126 aggregator tests green, i.e. the guards had a hole exactly on the original\nbug's line.\n\nExtract that hop into SearchBudget::from_input, now the only production\nconstruction site (new() is #[cfg(test)]), and pin it with a pure test\nasserting an autopilot-shaped input yields no provider spend while a\nmanual-shaped one maps its user-typed count through. Re-ran the mutation:\nit now fails that test instead of passing everything.\n\nAlso correct the SearchBudget::amount docstring — the additive LinkedIn tier\nis the paid opt-in one with its own spend caps (ADR-028), not \"the free side\"\n— and the de hint, since Adzuna's free tier is quota-metered per call\n(kontingentiert), not billed (abgerechnet).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: gate the paid apify tier on provider_amount and stop retrying metered 429s\n\nThe Apify LinkedIn gate read `budget.amount`, which a scheduled run passes as the\nsentinel 100 — so it always opened and every autopilot run bought a full 50-item\nactor run, contradicting the three doc claims that `provider_amount` is the only\nthing buying upstream calls. The decision now lives in one `apify_cap` fn that\nrides `provider_amount`: None means no paid run at all, Some(spend) buys only the\nunmet remainder clamped to APIFY_MAX_ITEMS. Deliberate behavior change: an\nApify-enabled user's SCHEDULED runs no longer buy LinkedIn results; manual\nsearches, which carry a real user-typed spend target, are unchanged.\n\nAdzuna and JSearch are metered, and fetch_text re-sends on 429/503, so the default\nretries=2 made one search cost up to 9 quota calls and answered \"you are over\nquota\" by spending more of it. Both now pin retries=0 through a shared\nFetchOptions builder, mirroring the existing APIFY_RETRIES precedent so the\ninvariant is assertable rather than inline.\n\nCancellation is now a clean stop on the page-1 path too: a cancel observed\nmid-flight surfaced as Err and made primary_chain log a jsearch fallback its own\ncancel guard would never run, and the broaden retry could still spend one more\nquota call on the way out.\n\nAlso hoists the double `req.amount.clamp(1, 100)` into one `requested_amount`.\n\n* fix: unblock the inert autopilot page budget and announce its hint\n\nA persisted out-of-range or non-integer `pages` still failed the wizard schema\nwhile the aggregator-only selection had the control DISABLED — the wizard refused\nto advance with no reachable way to comply. It is now normalized into the schema\nrange as soon as the field goes inert (the same round+clamp NumberField applies on\nblur), so re-adding a pages-aware board hands back a sane, editable number.\n\nThe \"why is this disabled\" hint was a bare span no screen-reader user reached.\nWizardField now gives its hint and error stable ids and wires them onto the\nlabelled control via aria-describedby, so every usage benefits.\n\n`pagesInert` derives from the deduplicated board set rather than raw array length,\nmatching `aggregatorSelected` right above it.\n\nTests: replace the fixed 350ms sleeps in the LocationInput typeahead with\nrender-aware waits on a concrete suggestion, so keyboard nav can't silently pass\nthrough the free-text fallback.\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-07-28T07:34:07+02:00",
          "tree_id": "2c163d262999b167ea584106b08a7e67dde99752",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/167cc5a6a5e043a0a5044b4cde9246a3cdf1d7d2"
        },
        "date": 1785218076025,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2115712,
            "range": "± 16716",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2550226,
            "range": "± 22218",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 276220,
            "range": "± 6336",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "273ef22ba297bd4687c8defca35d2fa290fa6eda",
          "message": "feat: applications page pipeline redesign with unified contacts, reminders, and status notes (#897)\n\n* feat: unify application contact fields and notify due follow-ups\n\nBackend half of the Applications-page upgrade.\n\nContact unification: `contact_name`/`contact_email` become THE single primary\ncontact per application. `recipient_name`/`recipient_email` are demoted to\ndeprecated aliases — accepted as inputs (both names write the same storage,\ncanonical wins on a tie) and always mirrored back on responses, so the renderer\nand extension keep working unchanged. Migration `unify_application_contact`\nbackfills the canonical pair from an alias-only value and leaves the deprecated\nCOLUMNS untouched (additive-only, never destructive); the store simply stops\nreading/writing them. `import` folds a pre-unification bundle the same way. The\naddress validation that guarded `recipientEmail` now guards both names, so the\napply-by-email sink can't be bypassed under the other name.\n\nReminders: a new L2 `reminder_scheduler` sweeps due/overdue `next_action_at`\nvalues on startup (after a grace period) and every 30 minutes, raising one\n`application.follow_up` notification routed to `/applications?highlight=<id>`.\nDedupe uses a new nullable `next_action_notified_at` marker column, stamped\nbefore the push and cleared by `update_fields` whenever the due date moves, so\none application notifies exactly once per due date and a reschedule re-arms it.\nTerminal pursuits never nag, and one sweep is capped so a first run after\nupgrade can't burst. No new IPC command: the overdue/upcoming counts the UI\nneeds are derivable from the `nextActionAt` values `applications.list` already\nreturns.\n\n`applications/mod.rs` sat one line under the R8 hard LOC cap, so the migration\nlist and the job-URL normalizer move to sibling files verbatim (no behaviour\nchange; `normalize_job_url` is re-exported).\n\n* feat: application status notes and unified contact\n\nOptional-note prompt on every status change plus a single canonical contact\npair across the detail surfaces.\n\n- StatusNoteModal: shared prompt opened AFTER a persisted stage change (row +\n  detail header) and from the Timeline's explicit \"Add note\". Saving writes a\n  same-status setStatus({ note }); Skip/Escape is a no-op, so the common\n  \"just move it to Applied\" path costs one keystroke.\n- Timeline collapses a from === to event to a single stage entry so a note\n  event never renders as \"Interviewing -> Interviewing\".\n- Status changes moved from a bare void mutateAsync to mutate + onSuccess /\n  onError, so the prompt only opens on a persisted change and a failure now\n  surfaces a localized inline alert instead of being silently swallowed.\n- ApplyByEmailTab recipient fields now read AND write contactName /\n  contactEmail (the canonical pair) instead of the deprecated recipient\n  aliases, with a hint making the shared binding discoverable. The heuristic\n  job-description prefill is unchanged and now lands on the canonical field.\n- Follow-up reminder promoted: its own leading Overview section with an\n  overdue-tinted hint, plus a header chip visible from every tab.\n- Row meta gains the board chip, salary range and an applied/updated stamp\n  (relative label, full timestamp in title).\n- i18n: every new string in en + de.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* ui: add applications pipeline strip and richer list rows\n\nSix always-present glass stat cards above the list — Saved, Applied,\nScreening, Interviewing, Offer and Closed (the four end-states aggregated) —\neach a toggle filter for its stage group, plus a client-derived overdue\nfollow-up badge.\n\n- lib/pipeline.ts owns the grouping, counts, overdue count and sort orders as\n  pure functions; a test pins PIPELINE_GROUPS against APPLICATION_STAGES so a\n  new backend stage can never silently fall out of the strip.\n- Counts always describe the WHOLE list (never the filtered view) and include\n  zeros, so an empty column still reads as information.\n- The Closed group tags each row with its own stage, so an aggregated card\n  never hides how a pursuit ended.\n- Sort control in the page header: updated (the server order, default),\n  company A-Z, next action. Persisted on the applications session slice next\n  to the existing filter, and narrowed on read so stale state degrades to the\n  default instead of throwing.\n- A ?highlight deep-link now CLEARS an excluding stage filter as well as\n  un-collapsing the stage section, so a follow-up notification always lands on\n  a visible row. An unknown highlight id leaves the filter untouched.\n- Overdue counts derive client-side from the nextActionAt already in\n  applications.list() — no new IPC.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test: keep applications element casts typecheck-safe\n\n`eslint --fix` strips `as HTMLInputElement` on `getBy*` results as an\nunnecessary assertion, but `tsc` still needs the element type for `.value`.\nUse testing-library's generic form so both gates agree.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: promote the application contact pair atomically and harden its guards\n\nCritic round on the contact-unification + reminder backend.\n\nCONVERGED HIGH — migration 7 promoted name and email INDEPENDENTLY, so a row\nwhose canonical pair was half-populated absorbed the other half from the\ndeprecated alias pair, fusing person A's name with person B's address. That\nfused identity feeds the apply-by-email mailto: sink, i.e. a message addressed\nto B under A's name. The promotion is now pair-atomic (both columns move, and\nonly when the WHOLE canonical pair is empty) and uses the same TRIM-based\nemptiness rule as its import-time twin, since whitespace-only values are\nreachable from pre-trim builds. Application::canonicalize_contact carried the\nidentical flaw on the import path and gets the identical fix, so a bundle\nimports exactly as the same row migrates in place. A test seeded with the\ncritics' probe rows reproduces the fusion against the old SQL.\n\nAn alias pair that is genuinely a second person is no longer dropped: the\nmigration and the import fold both append it to notes as\n\"Apply-by-email: <name> <<email>>\". Without that it was lost for good — the\nstore stops reading the deprecated columns and an export mirrors the canonical\npair, so no round trip could recover it. Both appends are replay-guarded.\n\nHIGH — the detail page seeded its save-on-blur buffers once per id, so after the\napply-by-email tab wrote the now-shared canonical contact, an Overview blur\npersisted its stale empty buffer back and wiped it. The mount key carries\nupdatedAt, re-seeding on every persisted change.\n\nAlso: byte-capped contact names on both inbound aliases (Zod + a Rust guard\nbeside the email one); rejected control characters and spaces in an address\nbefore the shape check (a stored CRLF is a header-injection primitive, and\nlegacy unvalidated rows now mirror into the recipient sinks); made the reminder\nstamp conditional on the due date the sweep decided against, so a reschedule in\nthe read-stamp window can no longer silence the new date; logged rather than\nswallowed a follow-up query failure; and set MissedTickBehavior::Delay so a\nlaptop wake does not drain a burst of catch-up sweeps.\n\nThe contact fold moves to applications/contact.rs — the added guards pushed the\nstore back over the R8 LOC cap.\n\n* fix: keep the applications note prompt and edit buffers alive across a refetch\n\nCritic round on the applications UI. Three HIGH findings, all reproduced.\n\nHIGH — the status-note prompt was destroyed by its own invalidation refetch on\nboth surfaces. On the list the refetch moves the row into a different keyed\nstage section, so React unmounted the row and the prompt state with it; on the\ndetail page the updatedAt mount key remounted the view and reset it. Only the\nTimeline entry point worked. The prompt now lives above the churn: page-level\nand keyed by application id on the list, and above the mount key on the detail\npage. The row just reports the persisted stage through onStatusChanged. The new\nlist test delivers the real refetch (the mocked query data changes) and asserts\nthe old row node is disconnected while the dialog survives.\n\nHIGH — replaced the key={id}:{updatedAt} remount with per-field reconciliation.\nThe remount fixed the contact-wipe but destroyed keyboard focus, discarded text\ntyped into another field while the first write was in flight, tore down the\nwhole TailorFlow sub-tree on every job-ad typing pause, and closed the note\nmodal on any landing write. Each save-on-blur buffer now adopts its own server\nvalue during render (useSyncedBuffer), so a sibling write can no longer clobber\nan uncommitted edit. The wipe regression test still passes; restoring the old\nkey turns the two new survival tests red.\n\nHIGH — prettier was red on two files. The npx wrapper prints \"All files\nformatted correctly\" while exiting 1, so the earlier claim was wrong; verified\nnow against pnpm format:check (exit 0).\n\nAlso: re-picking the CURRENT stage is a no-op on both Dropdowns (Dropdown.select\nfires onChange unconditionally, which appended a phantom event); a failed note\nwrite keeps the dialog open with the text and shows a localized error instead of\ndiscarding it; the note re-reads the live status at save time so a transition\nlanding while the prompt is open is not reverted; both Overview contact inputs\nsurface a rejected write the way ApplyByEmailTab already did for the same\ncanonical pair; and the pipeline strip announces its filter through a live\nregion.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: align the contact-unification whitespace and cap rules across layers\n\nFinal queued LOWs on the applications backend.\n\nTRIM charset divergence: SQLite's bare TRIM(x) strips only U+0020 while Rust's\nstr::trim strips all Unicode White_Space, so a contact field holding a TAB or an\nNBSP — endemic in text copied out of scraped HTML — read as non-empty in SQL and\nas empty in Rust. The same row therefore folded one way through the in-place\nmigration and the other way through a restored bundle. Migration 7 now passes an\nexplicit charset (space, TAB, LF, CR, NBSP) to both WHERE clauses. Rust stays a\ndeliberate superset for exotic separators (U+2028, U+3000); chasing full parity\nwould mean pinning an unbounded char() list to the current Unicode table, and\nthe residual failure mode is a contact left unpromoted rather than a wrong one —\ndocumented in the contact.rs lockstep note. Seeded tab and NBSP rows; reverting\nto bare TRIM leaves the tab row at (\"\\t\", \"\\t\\t\") instead of promoting it.\n\nName caps counted characters client-side and bytes server-side, so a 200-char\nCJK name passed Zod and was then rejected by the store with an error the user\ncould do nothing about. Both name aliases now use the TextEncoder byte refine\nthe jobDescription field already established; the email aliases get it too,\nsince they share the same column and the same divergence.\n\nvalidate_contact_name gains the control-character rejection its email sibling\nalready had — the name is interpolated verbatim into the migration's\n\"Apply-by-email: <name> <<email>>\" note and into the display-name half of any\nfuture message header. Interior spaces stay legal, unlike in an address.\n\nAlso seeds the identical-alias-pair row the test's own doc comment claimed but\nnever had, pinning the <> distinctness guard that suppresses the preserved note.\n\n* fix: make applications state paint, fit and return focus\n\nUX audit round. Five HIGH findings, all measured against the built bundle.\n\nHIGH — the selected pipeline card painted pixel-identical to an unselected one.\nIts bg-brand/border-brand utilities live in @layer utilities while\n.surface-card's own background and border are unlayered, so the card never\nchanged. Selection is now a ring (box-shadow, which .surface-card never sets, so\nnothing can contest it) plus a check glyph as a non-colour cue. A probe against\nthe built CSS asserts the emitted declarations, not the class names.\n\nHIGH — text-destructive is a dark surface tone (oklch 30%), so six new\nrole=alert sites announced correctly and painted invisibly. All now use\ntext-red-400 (oklch 70.4%); --color-error is genuinely undefined, so text-error\nwould have degraded to plain foreground.\n\nHIGH — at 900px three tags beside the row title squeezed it to nothing and\noverlapped the stage Dropdown. The title row wraps now, tags never shrink, and\nan aggregated pipeline group renders ONE flat list instead of per-stage headers\nthat printed each stage twice (header + row tag) and fragmented a small bucket.\n\nHIGH — closing a dialog dumped focus on <body>. useFocusTrap now remembers the\npre-open element and restores it. Deliberately a fallback: it skips when focus\nwas already moved somewhere meaningful or the element left the DOM, so a wrapper\nwith its own return target still wins. The note dialog also stops closing on a\nbackdrop click, which was destroying typed text with no confirmation.\n\nHIGH — the note field had no visible boundary against the modal panel; it uses\nthe glass variant now, matching the Overview notes precedent.\n\nAlso: contrast lifted to /70 across the strip label, row meta, hints and the\nSkip action; the meta line is differentiated by weight and caps rather than\nsub-12px sizes that all render at the same size anyway; a stage change on the\nLIST now offers a transient chip instead of seizing the screen with a modal\n(the detail page keeps the immediate prompt, where the change is the whole\ntask), and the dialog names the application; the filtered-empty state says which\nfilters are hiding everything and offers a way out; the loading state reserves\nthe strip's footprint; the overdue badge is a control that surfaces those rows;\nthe Closed bucket is renamed Finished so it stops reading as failure for an\naccepted offer; the shared-contact hint moved onto both inputs via\naria-describedby and states its consequence; the comp-only section is renamed\nCompensation; and the app root wraps everything in MotionConfig reducedMotion\nuser, so motion honours the OS setting that CSS media queries never reached.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: return focus from the note dialog and finish the applications ux round\n\nClosing UX re-verify. One HIGH plus the small-item batch.\n\nHIGH — closing the note dialog still stranded focus on <body>. The TextArea's\nautoFocus is applied during COMMIT, ahead of every effect, so the focus trap\nremembered the field itself as the opener, found it disconnected on close and\ncorrectly skipped the restore. Removed it — the trap already focuses the first\nfocusable — and moved the trap's capture into a layout effect so a descendant\nthat grabs focus from a passive effect cannot fool it either. Three tests pin\nthis: the passive-effect case, the autoFocus case (which documents WHY dialog\ncontent must never use it), and an end-to-end open-from-trigger then Escape.\n\nRULING — any active stage filter now renders one flat list. A section header\nunder a filter was either a word-for-word copy of the strip card selected right\nabove it, with a collapse toggle that means nothing when there is one section,\nor a second printing of each row's stage. The stage Tag stays gated on a group\nspanning several stages, so the two concerns are now separate flags.\n\nAlso: both new chips expressed their hairline with border-*, which the same\nunlayered `* { border-color }` rule overrode — swapped to the ring mechanism and\ngiven a 24px target floor; the overdue label takes real one/other plurals in\nboth locales; the light-scheme red/amber steps go one deeper (the pre-existing\nremap already shipped -600/-700 and still measured 4.0-4.4:1 on the tinted Tag\nfills) and brand-soft collapses to the light brand; the last surface-tone alert\nis gone; the loading placeholder now shares the strip's own class constants and\nnon-breaking spaces at the real type sizes, and the overdue row is always laid\nout so gaining one never shifts the list; the chip's retire timer skips while it\nholds focus (WCAG 2.2.1); the shared-contact hint is one node referenced by both\nfields; and the dead trackingSection key is gone.\n\nReverts the Input `hint` prop added last round — the single shared hint made it\nunused, and an untouched public API is worse than none.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* ui: keep a truncated pipeline card label readable via title\n\nThe German \"Abgeschlossen\" clips at the large text scale. Mirrors what the\napplication row title already does.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: surface rejected contact writes and sync the canonical pair\n\nReview round on PR #897 — the renderer and shared half.\n\nRejected contact writes were invisible on the apply-by-email tab: persistName\nfired and ignored the result, so a value the server refused stayed on screen\nwhile storage kept the old one and the mailto button pointed at the stale\naddress. Both fields now surface BOTH failure shapes — the in-band {error} and a\ntransport rejection — and the Overview pair, which edits the same canonical\ncolumns, gains the same onError. Six tests cover the paths, which had none.\n\nThe recipient pair also moved to useSyncedBuffer, extracted out of the detail\npage into the feature lib so the two surfaces editing those columns share one\nimplementation instead of one re-seeding and the other not.\n\nAlso: the note chip swallowed Enter/Space so activating it by keyboard no longer\nopens the detail page underneath the dialog; a slow note save can no longer\nforce-close a prompt the user has since opened for another row; the list prompt\nuses the post-transition copy, since only a persisted change raises it; the\npipeline counts are memoised; and the note field takes a client-side length\nguard (the command stores it uncapped — a real bound belongs there).\n\n\"Overdue\" now means the due DAY has passed. The field stores local midnight, so\na follow-up set for today was announced as late from 00:01 — in the badge, the\nstrip counter and the notification. Six fixtures asserted that old behaviour and\nwere updated.\n\nThe light-scheme contrast fix gains a real test: it parses the stylesheet and\npins exactly ONE light rule per remapped class, which is how the first attempt\nshipped inert behind a duplicate later in the same file. Its values moved into\nnamed tokens, so utilities.css states intent and tokens.css owns the step. The\nfocus trap gains stacked-trap coverage and its negative tests now assert focus\nactually lands on body rather than merely leaving the opener.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: close the reminder sweep race, banner storm and backup replay\n\nSeven PR #897 review findings on the follow-up reminder path.\n\n- Migration 8 now backfills `next_action_notified_at` for every reminder already\n  due at migration time. Without it the first post-upgrade sweep treated the whole\n  existing backlog as new and announced it; MAX_PER_SWEEP paced that but did not\n  suppress it. Entry 8 has never been released, so the body is amended in place\n  rather than appended as a ninth step.\n- `mark_next_action_notified` re-asserts the non-terminal status inside the\n  UPDATE's WHERE (list derived from `is_terminal`, drift-guarded by a test) and\n  reports rows-affected == 1. A status change landing between the sweep's read and\n  its stamp no longer notifies on a closed pursuit; a Rust-side re-check could not\n  have closed that window.\n- `next_action_notified_at` joins the `Application` wire type and\n  `write_row_conn`, so export/import round-trips it. Restoring a backup used to\n  re-fire every already-announced reminder. The compiler now forces every write\n  path to decide the marker's value; a test covers the re-upsert merge branch.\n- `tick` moved the storage half onto `spawn_blocking` (the email-watch poll\n  pattern) and stayed async for delivery, so the parking_lot lock, the table scan\n  and the row writes no longer block a tokio worker while `is_focused` keeps its\n  main-thread round trip on the async side.\n- `follow_up_candidates`' error arms are tested: a dropped table degrades to an\n  empty sweep through both the per-row arm and, after the connection reloads its\n  schema, the prepare arm. Both were verified red by replacing the guard with\n  `expect`. A row that cannot decode is skipped without costing the healthy ones.\n- `normalize_job_url` strips C0/DEL before the scheme guard, so `java\\nscript:` —\n  which HTML and the WHATWG URL parser both read as `javascript:` — can no longer\n  pass a raw-byte scheme scan verbatim. Defense in depth; not reachable today.\n- `canonicalize_contact` now requires a non-blank alias pair to promote, matching\n  the migration's WHERE exactly. SQL wins the lockstep because it is the in-place\n  path every existing row already took.\n\n`follow_up_candidates` + `mark_next_action_notified` moved to a new\n`applications/reminders.rs` (R8 LOC cap; same precedent as contact/job_url), and\na test-local `Patch` struct replaces the positional `update_fields` calls.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: cap the status note server-side and name contact errors as the ui does\n\nTwo PR #897 review LOWs on the applications command layer.\n\n- `applications_set_status` stored the note uncapped: the textarea's\n  `maxLength={2000}` was the only bound and no other caller passes through it,\n  while the note lands in the permanent `status_events` history. Added\n  `MAX_STATUS_NOTE_BYTES` and a trim + byte-cap guard that REJECTS rather than\n  truncates, matching `validate_contact_name` — a silently halved interaction-log\n  entry is worse than a visible error, and both surfaces already render the\n  returned error inline. The client cap is chars and the server cap is bytes, so a\n  very long multi-byte note surfaces a clear rejection instead of a silent loss;\n  the now-stale comment on `NOTE_MAX_LENGTH` was corrected to say so.\n- The contact-email rejections named `recipient_email`, the storage/alias\n  identifier, but the UI labels the field \"Contact email\" and there is no backend\n  i18n catalogue, so these strings reach the user verbatim. Renamed to the\n  user-facing name; no test pinned the old wording.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-07-28T09:17:40+02:00",
          "tree_id": "38a593d1eece81d20e0c063586769a9cce1f15c0",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/273ef22ba297bd4687c8defca35d2fa290fa6eda"
        },
        "date": 1785224302206,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2239378,
            "range": "± 103774",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2658224,
            "range": "± 25924",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 300358,
            "range": "± 7184",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "0ad1647aa43456e1e57264b516995064e0a002d4",
          "message": "fix(ai): claude 5 family support — adaptive thinking, sampling gates, model lists, spend rates (#901)\n\n* fix(ai): support claude 5 fable alias and spend rates\n\nAnthropic 5-family models (opus-5/sonnet-5/fable-5) never supported the\nclassic thinking budget_tokens block; anthropic_supports_thinking() already\nexcluded them by omission, but the comment read like an oversight rather\nthan deliberate adaptive-thinking routing. Clarify the gate and lock the\n5-family exclusion + the resulting no-thinking-block request shape with\nregression tests, alongside the existing 3.7/4.x/pre-3.7 coverage.\n\nAdd the \"fable\" bare alias to Claude Code's CLI model list (mirrors\nsonnet/opus/haiku) and to the prompts package's large-model detector, so it\nno longer falls through to the small prompt tier. Add spend RATES rows for\nclaude-fable-5 ($10/$50), claude-opus-5 ($5/$25), and claude-sonnet-5\n($3/$15); haiku-4-5 already matches the existing claude-haiku-4 prefix.\nPrefix-shadow safety between the 4.x and 5.x rows is pinned by a test.\n\nContext7 was unavailable in this session's toolset, so the exact Messages\nAPI wire shape for adaptive thinking + the effort parameter was not\nverified live; effort is left unwired for the direct Anthropic path (it\nremains CLI-agent-only end to end per the shared schema), which is the\nverification-gated task's own permitted \"send nothing extra\" outcome.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ai): send adaptive thinking config and gate sampling for claude 5 models\n\nThree-way model classification in the anthropic adapter: classic models keep\nextended thinking (enabled + budget_tokens), adaptive models (opus 4.7/4.8 and\nthe 5 family) get thinking type adaptive with display summarized so the\nthinking view receives text (display defaults to omitted there), plus the same\nmax_tokens headroom since thinking tokens count toward the cap in both modes.\nAdaptive models reject non-default sampling params on every request, so\ncapabilities() is now model-aware and all four request builders gate\ntemperature/top_p on it. Dot-form version ids are normalized before matching.\n\nSpend: opus 4.5 gets its own rate row (was shadowed by the opus 4 prefix into\na 3x overestimate), vendor-prefixed ids strip to the bare name, and rate tests\nassert the matched prefix, not the price.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* ui: refresh anthropic model lists and onboarding default for the claude 5 family\n\nCurated anthropic seed list moves to claude-fable-5 / claude-opus-5 /\nclaude-sonnet-5 / claude-haiku-4-5, the claude code cli list gains the fable\nalias, the onboarding anthropic default becomes claude-sonnet-5, and the\nprovider description stops enumerating model families so it cannot go stale.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ai): apply security gate advisories to thinking headroom and spend rates\n\nAdaptive max_tokens inflation now applies regardless of the classic 2048 gate\n(the extension answer assist calls with a 1000 cap and was billed thinking out\nof it), opus 4.6/4.7/4.8 get their own 5/25 rate rows instead of shadowing\ninto the opus 4 15/75 row, max_tokens arithmetic saturates, and rate lookup\nfalls back to the full string when the last path segment matches no row.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ai): fail safe on unknown claude families and floor adaptive headroom\n\nanthropic_supports_temperature() is now the single sampling gate for all four\nrequest builders: unclassified claude ids omit temperature (omitting never\n400s) with a legacy pre-thinking carve-out, adaptive headroom gets a 1024\nfloor so small caps keep a useful thinking budget, rate prefixes match\ndot-form ids on both sides, and the classification debug log no longer\nmislabels legacy non-thinking models.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: note adaptive thinking in the anthropic provider table row\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(ai): split anthropic tests into a sibling file under the r8 loc cap\n\nBehavior-preserving move of the test module (all 35 tests verbatim) into\nanthropic_tests.rs via the answer_assist_tests.rs precedent; anthropic.rs\ndrops from 1540 to 951 lines, back under the 1400 hard cap.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ai): address pr 901 review findings in spend and model classification\n\nAdd a gpt-4-1106 rate row (dot-dash normalization made it match the gpt-4.1\nrow at a 15x under-report), replace the last vacuous price assertion with a\nprefix assert, delete the untriggerable full-string rate fallback, strip\nvendor prefixes before model classification so the fail-safe holds behind\nanthropic/, make version needles boundary-aware (opus-4-70 no longer matches\nopus-4-7), and match the bare fable alias exactly so fable-1b local models\nkeep the small-model prompt.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* ui: keep stored model selections visible when absent from curated lists\n\nA stored model dropped from the curated seed list rendered as the muted\nplaceholder until the live model fetch resolved, reading as a reset config.\nBoth settings panels now inject a synthetic option for the stored value, with\nregression tests, and a contract test pins the onboarding defaults to the\nprovider catalog.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-07-28T14:29:41+02:00",
          "tree_id": "6355b89d7537bfb3dc645c78cd69eb76493dd179",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/0ad1647aa43456e1e57264b516995064e0a002d4"
        },
        "date": 1785243037918,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2160139,
            "range": "± 78324",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2520375,
            "range": "± 34388",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 295906,
            "range": "± 7656",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "820ceef7f53e6164318518e5b31bc31210ade043",
          "message": "feat: diagnose the role a cover letter answers before drafting it (#926)\n\n* feat: diagnose the role a cover letter answers before drafting it\n\nFold a company-and-vacancy intelligence pass into the existing single-pass\ncover-letter generation — no extra AI calls, no new IPC.\n\nCompany brief (opt-in \"Research company\", already provider-native web search):\nnow also covers competitors and the challenges/strategic priorities the company\nis working on, hedges anything inferred rather than stating it as fact, and runs\n150-200 words. The `<company_research>` fence cap moves 1200 -> 1600 chars so the\nlonger brief is not cut mid-sentence, and the Ollama search query asks for\ncompetitors/strategy too.\n\nCover letter: a private diagnosis before drafting — why the role is open, the 3\noutcomes the hire is judged on in the first 6-12 months, and which real résumé\nachievements make the candidate part of that solution. Every step is bound to\nevidence in the job ad, the brief, or the résumé; thin evidence keeps the\ndiagnosis broad instead of guessing, and the letter voices it as the candidate's\nreading of the role, never insider knowledge. The same one-liner goes into the\nagent-mode draft_cover_letter prompt.\n\nAlso drops the hardcoded \"200 to 300 words\" from four places: it contradicted\n<market_conventions>, which already carries the per-market band.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: harden the company brief and bound the role diagnosis to employer evidence\n\nReview findings from CodeRabbit + the claude PR review on #926.\n\n- Prompt injection (major): search results are attacker-reachable text and the\n  brief is the one stage that reads them unfenced, so VERIFIED_ONLY now tells\n  the model to treat every page and snippet as untrusted data, ignore\n  directives inside them, and never let them add claims about a candidate.\n- Diagnosis evidence (major): a résumé says what the candidate has done, never\n  why an employer opened a role or what they will measure. Steps 1 and 2 now\n  stand only on <job_ad> (+ <company_research> when fenced); <candidate_resume>\n  is reserved for step 3, the through-line.\n- Test coverage (high): the brief-present branch of the diagnosis was never\n  asserted. Both branches are now covered, including the negative case.\n- Localization: the hedge exemplars were English strings injected into every\n  target language; the prompt now asks for hedging in the target language.\n- Competitors are their own required facet, no longer an \"or\" the model may skip.\n- Reuse hasBrief instead of recomputing companyBrief.trim(), and restore the\n  role-context comment to the code it documents.\n- Record why the agent-path length band is deliberately market-agnostic.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-02T00:46:38+02:00",
          "tree_id": "aa061e14aceed1b3d9bfec4252ece5b2e60b518c",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/820ceef7f53e6164318518e5b31bc31210ade043"
        },
        "date": 1785625733292,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2206555,
            "range": "± 63837",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2594150,
            "range": "± 48597",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 301023,
            "range": "± 3674",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "9023c41233c13fea9396c92a9df8aebdaf3d1d65",
          "message": "feat: add opt-out crash reporting via sentry (#927)\n\n* feat: add opt-out crash reporting via sentry\n\nThe app was blind in production. The panic hook wrote crashes.log and the\ndiagnostics zip could export it, but both needed the user to notice a failure\nand file an issue by hand. React errors caught by a boundary never reached the\nwindow at all, so they were invisible even in principle.\n\nDesktop app only. The extension keeps its AMO data_collection_permissions of\n['none'] and the landing site keeps its no-third-party-JS origin invariant.\n\nConsent is Rust-owned rather than a renderer preference: sentry::init has to run\nbefore tauri::Builder, because sentry-rust-minidump forks the crash-reporter\nprocess at startup and panic = \"abort\" leaves nothing in-process able to flush a\ncrash. There is no WebView at that point, so no localStorage to read.\n\nTransmission requires enabled && consentShown, not just enabled. The default is\non, but nothing is sent until the setup wizard has actually shown the choice — a\ndefault nobody saw is not consent, and that gap is where privacy claims break.\nA user who never finishes onboarding never reports anything.\n\nEvery outgoing event is serialized, passed through the same redact_token\npipeline the diagnostics bundle uses, and deserialized — whole-event rather than\nfield-by-field, because a field list is a denylist that rots as the SDK adds\nfields. send_default_pii is off and server_name is overridden, since the SDK\notherwise sends the machine hostname.\n\nThe DSN is baked by build.rs from an env var set only in the signed release job\nand read with option_env!, so local builds, contributor clones and every CI\ncheck compile to None and cannot transmit.\n\nThis reverses a published promise: the privacy policy named Sentry as absent in\nthree places and ADR-0005 defined the boundary as \"no telemetry\". ADR-0005 gains\nan eighth egress class, ADR-0020 records the decision, and all twelve claim\nsites are rewritten — including Footer.tsx, whose \"no server-side copy, deleting\nlocally deletes it everywhere\" became false, and /privacy, which is the policy\nURL filed with both stores and now separates desktop collection from extension\nnon-collection.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: classify the crash-reporting module in the architecture layers\n\nThe architecture test caught it unclassified. Shell layer rather than lower: it\nis constructed by lib::run() before the Tauri builder exists, and it reaches\ndown into commands::support::redact_lines to reuse the diagnostics redactor\ninstead of growing a second, weaker one.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: resolve the crash-reporting consent directory exactly once\n\nReview found the feature could never activate. init() ran before\ntauri::Builder and resolved the consent file through\nplatform::config::data_dir(), which at that moment falls back to $HOME/.ajh\nbecause AJH_DATA_DIR is only exported later, inside setup(). The privacy\ncommands meanwhile wrote through app.path().app_data_dir(). Consent was\nwritten to one directory and read from another, so no user choice was ever\nobserved — and it failed silently, since a missing file is indistinguishable\nfrom \"user said no\".\n\nCache the first resolution in a OnceLock and funnel load/save/clear through\nit, so both sides agree by construction rather than by two call sites\nhappening to resolve alike. The commands no longer take an AppHandle at all.\n\nAdds a regression test pinning that every accessor shares one resolved\ndirectory and that a save() is observable through load().\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: stop transmission on factory reset and permit the sentry plugin\n\nTwo findings from the second review pass.\n\nFactory reset cleared the persisted consent file but left the in-memory client\nbound, so a user who had consented kept transmitting for the rest of the\nsession immediately after performing what reads as a full privacy wipe — the\nreset does not restart the process. Now unbinds alongside the clear, mirroring\nthe opt-out path in privacy_set_crash_reporting.\n\nThe plugin was registered without a capability entry. Its browser side forwards\nevents to Rust over invoke, and those commands (envelope, breadcrumb) are\npermission-gated in Tauri 2, so without \"sentry:default\" every renderer error\nwould have been denied at the IPC boundary and silently never reported — half\nthe feature dead while looking wired up.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: use the ureq transport and document where consent is stored\n\nThird review pass.\n\nDrop sentry's default features. The default transport is reqwest + native-tls,\nand sentry 0.42 pins reqwest 0.12 — a second copy alongside the app's 0.13, in\na binary optimized hard for size. ureq is lighter and also more correct here:\nthe reqwest transport needs tokio, but the minidump supervisor is a bare forked\nprocess with no async runtime to send on. Verified reqwest 0.12 is gone from\nthe tree and ureq 3.3 replaces it. The reported actix-web integration was not\nactually in the tree — defaults never enabled it.\n\nThe module claimed the consent file sits next to the other app data. It does\nnot: it lives where data_dir() resolves before Tauri starts, which in a default\ninstall is $HOME/.ajh, not the per-OS app-data dir. That placement is forced —\nsentry::init has no AppHandle, and the init cannot move into setup because the\nminidump supervisor re-executes everything above its own call in the forked\nchild. Corrected the claim rather than the location, and recorded the\nconsequence: deleting the app-data dir by hand leaves this file, though it\nholds two booleans and the factory reset clears it.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-02T04:28:17+02:00",
          "tree_id": "3f43c7a45aefb529aeb8c6555f756c119e48e339",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/9023c41233c13fea9396c92a9df8aebdaf3d1d65"
        },
        "date": 1785639046986,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2189221,
            "range": "± 66784",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2705311,
            "range": "± 26886",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 251272,
            "range": "± 3295",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "0c78659a1f84fd6cdc6d12621970c1dd3a7c8c38",
          "message": "fix: keep project links out of the resume header and make them work (#931)\n\n* fix: keep project links out of the resume header and make them work\n\nThree defects from one generated resume.\n\nProject sites were swept into the contact profile's extra links at import, so\nthe exported header rendered them on the contact line and again in PROJECTS.\nOnly platform profiles may seed extra links now, and website selection prefers\nan apex over its own subdomains so it no longer depends on the order links\nappear in the source PDF.\n\nPROJECTS items read as \"Name - same-name-slug\" with no hyperlinks, because the\nprompt asked for that shape and injection matched the machine label literally\nwhile the PDF extractor humanised it differently. Items now carry their real\nname, matching is normalised and assigns each link to its best line, job-board\nand non-personal profile URLs are dropped rather than becoming invented\nprojects, and a link that still matches nothing is placed in the localized\nprojects section or left alone rather than appended as an orphan.\n\nThe header shown in the editor differed from the exported one, and edits to it\nwere silently discarded from PDF and DOCX while still shipping in TXT and\nclipboard copy. The document text now owns the header at export; the profile\nseeds it at generation and fills only what the text leaves blank. See\nADR-0021.\n\nRules that exist in both Rust and TypeScript are gated by shared fixtures\nasserted from both sides, and the section-name list is read from its fixture\nby TypeScript rather than duplicated, after three separate bugs this session\ntraced to a mirrored rule whose comment claimed a parity it did not have.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: bound the header seeding scan so it cannot delete document content\n\nThe seeding scan stopped only at a recognised section heading, and the shared\nheading fixture covered three of the seven locales the app generates resumes\nfor. For the rest the scan ran to the end of the document and spliced out every\ncontact-shaped line it found, silently removing employment roles, skills lines\nand project links from the generated resume. English resumes whose first\nheading was not in the list were affected too.\n\nThe scan is now bounded to the first blank-line-delimited block, so a predicate\nmiss can only fail to seed or duplicate a line, never delete real content. On\ntop of that the heading rule is recognised by shape as well as by name, ported\nfrom the parser and gated by a shared fixture, and the name list now covers\nevery locale the conventions ship.\n\nAlso caps contact values before formatting rather than after, so the rendered\nheader and the set the export validator compares against can no longer diverge\nand hard-block a legitimate profile, and routes the candidate name through the\nsame control-character stripping as every other header field.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: never remove a line while seeding the resume header\n\nThe seeding scan replaced the first contact-shaped line in the header block and\ndeleted the rest. On a document that follows the prompt exactly, a job title\ncontaining separators is itself contact-shaped, so it was classified alongside\nthe real contact line and silently destroyed. Bounding the scan did not help,\nbecause the loss happened inside the bound.\n\nSeeding now only ever replaces or inserts, so losing content is structurally\nimpossible rather than merely unlikely, and the line it replaces is chosen by\nsignal, preferring the one carrying an email, so a job title is no longer\nmistaken for the contact line.\n\nA second contact-shaped line therefore survives now and is joined into the\nrendered header. That is a visible, user-correctable defect and is documented\nas the deliberate trade against silent deletion. Also gives the heading length\nguard byte parity with the parser and covers the guards the shared fixture was\nnot exercising.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: seed the resume header on every generation path\n\nThe builder path never seeded its header, which was harmless while export\noverwrote the header unconditionally and became a defect once export only\nfills a blank one: the builder prompt orders the model to write a contact line\nand promises it will be replaced, while the builder's own answers carry no\nemail, phone, location or links. That invented line was shipping to employers.\nBoth generation paths now seed through one helper, and the false promise is\ngone from the prompt.\n\nA profile with only a name never seeded it either, because the whole function\nwas gated on a check that deliberately ignores the name. The gate is gone; the\nper-field guards were already correct on their own.\n\nSeeding also no longer picks its target by a bare at-sign, which let a job\ntitle outrank the real contact line, nor by the last match, which reached\ndeepest into the document exactly when boundary detection had failed. Line\nzero is never overwritten, so a combined name and contact line keeps the name.\nThe two profile lookups can no longer reject the finished generation, and the\njob-board warning no longer depends on a profile it never consulted.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* style: apply cargo fmt to the contact profile tests\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: remove the quadratic link-span regex and the host-substring assertion\n\nCodeQL flagged two high-severity issues on the PR. The url extractor ran an\nunanchored regex over each markdown link span, so a span containing many\nbracket-paren pairs retried every one of them and degraded to quadratic time\non text derived from an imported PDF. The span always ends with the url in\nparentheses, so the last opening pair is the only candidate and one scan finds\nit.\n\nThe idempotency test counted lines containing a bare host, which reads as url\nhost sanitization to the scanner and was the weaker assertion anyway, since it\nwould pass on a mangled line as long as the host survived. It now asserts the\nwhole seeded line.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: address the pre-merge review findings on the header work\n\nLine zero was the one write in the seeding function that was never guarded, so\na model that omits the name line and opens with a section heading had that\nheading overwritten. It is now replaced only when it looks like a name, and\ninserted otherwise.\n\nHeader values spliced into a markdown link kept their brackets and parentheses,\nwhich could close the construct early; they are now stripped on both the\nrendered header and the set the validator compares against, so the two stay\nidentical.\n\nThe heading predicates compared raw lines while the parser they mirror compares\nstripped ones, and the fixture tests were themselves calling the predicates on\nraw input, so the parity gate was not exercising parity. Both sides now strip\nfirst, and the fixtures carry cases that tell the two apart.\n\nAlso fixes a year guard that matched a repeated digit rather than four\nconsecutive ones, an untrimmed name assignment present in both the PDF and DOCX\npaths, and several tests that could not fail. Documentation moves back to\npointers rather than copied implementation detail.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: reject failed contact saves and enforce header parity on the real header\n\nSaving the contact profile with the store unmanaged returned an error value\nin-band that the only caller never read, so a save failed silently and\npermanently. The command now returns a result, which rejects the invoke and\nlets the caller's error path run, and the three commands are split so their\ndegraded branches are testable without a mock app.\n\nExport validation gated the whole strict header-parity block on the profile\nbeing the header's source, which left the common case guarded only by an\nadvisory warning. It now rebuilds the header the renderer will actually emit\nand checks parity against that, so a company link leaking into a text-owned\nheader blocks again.\n\nThe last-resort link net trusted a lexicon prefix match for section\nboundaries, so a job title beginning with a section word could receive a\nspliced link; it now requires a real standalone heading. Parentheses in a url\nare percent-encoded rather than deleted, format characters are stripped\nalongside control characters, and a personal profile on a job-board host no\nlonger draws a spurious warning.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* style: apply cargo fmt to the contact profile command and validate tests\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: block header link mismatches on header-owned links only\n\nThe blocking check ran against every link in the top band of page one, which\nis a heuristic, so a genuine body link near the top of the page could fail an\nexport that was perfectly correct. It now runs against the links the\nreconstructed header actually owns; the advisory checks keep using the band.\nTwo tests added in an earlier round had asserted the opposite and were pinning\nthat false block as expected behaviour rather than guarding against it.\n\nADR-0021 described the check as skipped once the document text owns the\nheader, which stopped being true when the check was re-pointed at whichever\nheader is authoritative. The document now describes what the code does.\n\nAlso caps contact values before percent-encoding rather than after, so a\ntruncation can no longer split an escape; stops validation from overriding a\ntext-derived name with generation metadata, which fired a spurious missing\nname warning on an edited header; bounds body-link title matching to lines at\nor after the first section heading, so the candidate's own name can no longer\nbecome a project link; surfaces a failed contact save to the user; and honours\ncancellation during header seeding.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: scan for the inline separator instead of matching it with a regex\n\nThe separator pattern had two unbounded whitespace runs, so a line with many\nspaces and no dash made the engine retry from every start position, which is\nquadratic on text extracted from a user supplied pdf. A single left to right\npass finds the same separator and examines each character once.\n\nHeader link mismatch now blocks only when the rendered header owns at least\none link, stated as an explicit branch with the invariant written down rather\nthan left implicit in an iterator that yields nothing. Two reviewers in a row\nread the previous form as a hole, and the case it covers was untested by\neither existing sibling test, so it now has one of its own.\n\nAdds coverage for the abort check that runs after the profile lookups resolve,\nfor where an unmatched label is placed rather than only that it exists, for a\nstray unpaired bold marker, and for a parenthesis falling just past the value\ncap.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: pick the header's own links by position, not annotation order\n\nWhich links counted as the header's own was decided by the order annotations\nappear in the pdf, which carries no guarantee about where they sit on the page.\nA two column template emits its sidebar as a separate run, so a body link can\nbe written before a header one; the check would then compare a body link\nagainst the header's expected set, report a critical mismatch, and the\nauto fix would re render the document single column, silently discarding the\nlayout the user chose.\n\nSelection now sorts by the same top edge the band filter already reads, so it\nis a statement about the page rather than about the writer that produced it.\nExtracted into its own function so the property is pinned by a test that fails\nwhen the ordering is removed, rather than living inside the render path where\nit cannot be reached without crafting a pdf.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-04T15:49:07+02:00",
          "tree_id": "a0de2bc36ab86c65b3561fc227bd9408d2dfbe1f",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/0c78659a1f84fd6cdc6d12621970c1dd3a7c8c38"
        },
        "date": 1785852717126,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2210011,
            "range": "± 11022",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2612161,
            "range": "± 61049",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 289557,
            "range": "± 12609",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "82d244477cb8d63fdc6064ccf3c199ff8b4bb1a3",
          "message": "feat: offer reasoning effort per model and fix embedding re-indexing (#933)\n\n* feat: offer reasoning effort per model and fix embedding re-indexing\n\nRe-indexing embeddings reported success while writing nothing. The input was\ntruncated to a fixed character budget that overflows a dense language's token\nwindow, every provider error was swallowed, and the batch completed regardless,\nso the status strip correctly read 0 indexed under two success toasts. Input is\nnow chunked and mean-pooled so the whole document is embedded, the working size\nis learned once per document and bounded, and a run that embeds nothing fails.\n\nGemini's default embedding model had been retired by Google, and changing the\ncode default fixed nothing because the resolved name is persisted, so a\nmigration rewrites it and evicts the caches keyed on it. Stored vectors carry a\nformat version, since pooled vectors are not comparable with the truncated\nones written before this change.\n\nThe effort setting reached only the Codex agent; every HTTP adapter dropped it.\nIt is now gated on the per-model reasoning capability, and the levels a picker\noffers come from the backend per model, because providers publish different\nvocabularies per model and an unsupported value is rejected outright. Parameter\nshapes were taken from current provider documentation rather than assumed.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: keep the learned chunk size and validate the effort level per model\n\nThe size learned for a document was clamped to each chunk's leftover tail and\nthen carried into the next chunk, so one short remainder shrank the working\nsize for the rest of the document. A long resume then spent its whole call\nbudget and failed with a misleading message after embedding a few percent of\nits text. The learned size is now lowered only by a real rejection from the\nprovider.\n\nThe level a gemini model accepts varies by model, and the setting is stored per\nprovider, so switching models within gemini carried a level the new model\nrejects straight to the api. The request builder now checks the level against\nthe model's own table rather than only against the model generation.\n\nUsage is accumulated as work happens rather than returned only on success, so\ncalls already billed before a partial failure still reach the spend ledger.\n\nAlso removes a model from the one click list that the vendor has since shut\ndown, which this branch had added, and records against each hardcoded model\nlist which vendor page it was checked against and when, since all three stale\nmodel defects on this branch came from lists that were correct when written.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* refactor: give the adaptive embedding path its own module\n\nAdding the chunking and pooling work nearly doubled the provider module, which\nis meant to be the trait, registry and shared types rather than a home for a\nsubsystem. The adaptive path moves to its own file unchanged, and the two\nadapter modules that also crossed the size limit move their tests to sibling\nfiles, matching the pattern anthropic already used.\n\nNo behaviour changes and no test lost: the same three thousand and ten tests\npass, and the architecture rules pass without adding a single exemption, since\nan allowlist entry here would only have hidden the thing the rule exists to\nreport.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: replace the retired gemini models and validate anthropic effort per model\n\nThree of the four curated gemini entries were dead: the first one, which is the\nonboarding default, had been shut down, and two others no longer appear on the\nvendor's model page at all. A previous round replaced only the single dead id it\nalready knew about and left its neighbours unchecked, so the note in that file\nnow says to verify every entry when re-checking, not just the one being changed.\n\nAnthropic's effort levels turned out not to be uniform either: the vocabulary\nhas five values and the top two exclude different model sets, so the send path\nnow checks the level against the model's own table the same way gemini does.\nOpenai's enum has grown too, but its guide defers the per model breakdown to\nindividual model pages rather than one table, so only the values verified as\nuniversal are offered and the gate is there for when that changes.\n\nA re-index whose job finished before react committed the state holding its id\nleft the panel stuck, because the event handler read the id from a closure that\nhad not updated yet. The id is now held in a ref written before the state.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: stop injecting a temperature gemini 3 asks us not to set\n\nThe request builders substituted a default temperature whenever the caller had\nnot chosen one, and that substituted value is below the figure google asks for\non gemini 3, where lowering it risks looping and degraded output on exactly the\nreasoning heavy work this app does. Moving the onboarding default onto a gemini\n3 model made that the path every new user takes, so the fix belongs with it.\n\nA value the user actually chose is still sent on any model. Only the invented\none is dropped, and only where the vendor asks for its own default. Applied at\nall four call sites rather than the one reported, including company research,\nwhere the hardcoded figure was furthest from what is recommended.\n\nThe re-index test now asserts the panel leaves its working state rather than\nonly that a toast fired, so dropping the clear would fail it.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: let the chunker own length policy instead of the ollama adapter\n\nThe chunker grows its per chunk size for a long document so the number of\nchunks stays bounded, and the ollama adapter still cut anything over eight\nthousand characters before sending it. A long resume was therefore silently\ntruncated again, by a different route, inside the change whose purpose was to\nstop that, while the log line beside it claimed no text is dropped.\n\nThe adapter no longer rewrites what it is given. Its own over length error is\nalready recognised, so a chunk that really is too large now comes back as a\nfailure the halving ladder handles, which is what that ladder is for. The body\nbuilder is now a pure function so a grown chunk can be asserted to arrive whole\nwithout a network mock.\n\nAlso gates the ollama thinking parameter against the levels it accepts, the way\nthe other three adapters already do, and checks the embedding dimension the api\nactually returns rather than trusting the request shape, since two careful\nreadings of the vendor reference disagreed about it.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-04T23:19:28+02:00",
          "tree_id": "88a63cfd15d172de3f9fe476595f1cb14ed8148a",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/82d244477cb8d63fdc6064ccf3c199ff8b4bb1a3"
        },
        "date": 1785879773312,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2249847,
            "range": "± 57683",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2680229,
            "range": "± 36027",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 323373,
            "range": "± 7712",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "9c6ee42a9b2047f382b750deadd2612081e56e6e",
          "message": "feat: make provider model listing report failures and carry model metadata (#935)\n\n* fix: surface provider model-list failures instead of returning an empty list\n\n`AiProvider::list_models` returned `Vec<Value>` and collapsed five distinct\noutcomes into `vec![]`: missing key, request failure, non-2xx status, an\nunparseable body, and a genuinely empty catalogue. `ai_list_provider_models`\nadded a sixth via `resolve_by_name`. That is invisible today only because the\npicker falls back to a hand-curated model array.\n\nThose curated arrays are what this programme removes, because they rot\nsilently — four stale-model defects shipped in one session, including a\nshut-down model serving as the Gemini onboarding default. Removing the\nfallback without this change would hand every offline or erroring user an\nempty model picker with no explanation.\n\nSo the trait now returns `AppResult<Vec<Value>>`, every adapter returns `Err`\nfor a real failure and `Ok(vec![])` only for an empty catalogue, and the\ncommand rejects the invoke rather than returning an in-band error object that\nno caller reads. The projection stays `{name}` — carrying vendor metadata was\nconsidered and dropped, since capability gates already resolve per model\nthrough `ai_model_capabilities`.\n\nFixed alongside, all found by review rather than by the spec:\n\n- Gemini listed from `/v1/models` while every generation path uses `/v1beta`,\n  so the live list omitted every `-preview` model — including the curated\n  Pro-tier entry, which would have vanished from the picker the moment the\n  fallback was removed.\n- Both cloud list endpoints paginate and only page one was read.\n- A keyless OpenAI-compatible server (LM Studio, vLLM) could generate but\n  never list, since the key was required for listing and optional everywhere\n  else.\n- Transport and parse failures were built with a bare `format!`, landing as\n  `AppError::Message` and reporting a network timeout as non-retriable.\n- `test_key` accepted a whitespace-only key while the listing path rejected\n  it, under a doc comment claiming the two agreed.\n\nThis PR is deliberately UI-neutral; a rejected query still renders the curated\nfallback, now covered by a test that drives the real component rather than\nasserting from the source.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: stop a gateway base url leaking its query secret into provider errors\n\n`format!(\"{}/models\", base_url)` is string concatenation, not URL building. A\nbase URL carrying query-string auth — the pattern Cloudflare AI Gateway and\nseveral self-hosted proxies use — produced\n`https://gw.example.com/v1?api-key=SECRET/models`, which parses as path `/v1`\nwith the secret mangled into the query. So the request silently hit the wrong\nendpoint, and the secret was echoed verbatim into the error that the previous\ncommit began routing to the renderer.\n\n`Url::join` is not the fix: a reference with no query clears the base's query,\nso joining drops the auth entirely. Resolve through `path_segments_mut()`\ninstead, which preserves query and fragment and is trailing-slash invariant.\n\nBuilding the URL correctly is still not sufficient — `reqwest::Error`'s own\n`Display` embeds the failed URL and strips only userinfo, as its `without_url`\ndoc comment warns. Transport and parse errors now go through\n`scrub_url_secret`, which clears the query and fragment before the message can\nescape. `scraping/http.rs` already defends this exact class.\n\nPagination gains a progress guard and a cumulative deadline: the cursor was\nassigned without comparing it to the previous value, so a provider returning a\nnon-advancing cursor re-fetched the same page up to 50 times — worst case 500s\ninside one invoke. A deadline overrun now errors rather than returning a\npartial list, since silent truncation is what pagination was added to fix.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* test: build the scrub-url test client through the shared http layer\n\nR5 confines reqwest client construction to net/http.rs. The test is more\nfaithful for it — the error being scrubbed now comes from the same client the\nproduction path uses.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* feat: carry provider model metadata through the list endpoint\n\nEvery adapter mapped its catalogue down to `{name}`, discarding the display\nname, release date and context window each endpoint already returns — and then\nthose same facts were hand-maintained in a curated TypeScript array that went\nstale four times in one session.\n\nEntries are now `{name, displayName?, createdAt?, contextLength?}`. `name` is\nbyte-identical to before, since it is what every selection and stored\npreference keys on.\n\nEvery added field is optional because no field is available from every\nprovider, verified against each vendor's own docs rather than assumed:\n\n  Anthropic  display_name  created_at        max_input_tokens\n  Gemini     displayName   —                 inputTokenLimit\n  OpenAI     —             created (epoch)   —\n  Ollama     —             modified_at       —\n\nGemini returns no creation date at all, so newest-first ordering can never be\nuniversal — which is why the onboarding default stays a user choice rather\nthan a ranking rule. An absent field is omitted, never zero-filled: a\nfabricated timestamp would sort wrongly and silently.\n\n`createdAt` normalizes to epoch milliseconds, matching this codebase's\nexisting convention, so the renderer sorts numerically with no per-provider\nbranching.\n\nAnthropic also returns a `capabilities` object. It is deliberately NOT carried:\nreasoning and effort gates already resolve per model through\n`ai_model_capabilities`, and a second provider-shaped capability source is\nexactly the duplicated-rule divergence this programme keeps paying for.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: error on a truncated model catalogue instead of returning it as complete\n\nThe pagination loop still had two paths that reported an incomplete list as a\nsuccess — the exact failure this PR exists to remove, left in the code added\nto remove it:\n\n- `has_more: true` with a missing or blank `last_id` was treated as clean\n  end-of-pages rather than the malformed response it is.\n- Exhausting the 50-page budget while the cursor was still advancing fell out\n  of the loop and returned `Ok(all)`.\n\nBoth now error. The loop's decision is extracted into a pure `pagination_step`\nso the boundary is directly testable — neither adapter is wiremock-testable\n(hardcoded `BASE`, no `AppHandle` seam), and hand-duplicating loop logic in a\ntest would assert the test's copy, not the loop.\n\nA non-2xx from the list endpoints also bypassed `friendly_api_error`, throwing\naway the classification every other request path in these files keeps: 401/403\nas a config error, 429 and 5xx as retryable network. Since the whole argument\nfor making this fallible is that the message becomes the user's only\nexplanation once the curated fallback goes, discarding the part that separates\n\"bad key\" from \"rate limited\" defeated the point. Fixed in all three adapters,\nin both `list_models` and `test_key`.\n\nAlso:\n\n- The key helpers validated `k.trim()` and returned the padded `k`, so a\n  pasted key with a trailing space reached the wire and 401'd — and one with a\n  newline made the header invalid, failing before any request. All three\n  adapters had it; only OpenAI's was reported.\n- `embed_impl`, `complete_impl` and `chat_with_tools` skipped `trace.end` on\n  early error returns, leaving the `ai` span open so failures never reached\n  latency telemetry. Predates this PR, verified against the merge base; fixed\n  because these are lines it already reworked.\n- Provider-controlled epoch seconds are multiplied with `checked_mul`; on\n  overflow `createdAt` is omitted rather than fabricated.\n\nThe keyless-listing test only ever proved the no-key branch, and the mocks\nnever required an authorization header — so deleting `bearer_auth` outright\nwould have kept the suite green. There is now a test that requires it.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* refactor: share the pagination decision instead of copying it per adapter\n\n`PaginationStep`, `advance_cursor` and `pagination_step` were duplicated\nverbatim across the Anthropic and Gemini adapters — identical logic, identical\ndoc comments. That is the defect this repo keeps paying for: a rule written\ntwice, where hardening one copy leaves the other behind and nothing fails.\nNotably it arrived in the very commit that fixed a truncation bug in one\nbranch of that loop while two other branches still returned a partial list as\nsuccess.\n\nAll three now live once in `ai_provider/mod.rs`, generic over the cursor type,\nwith the page budget passed in rather than read from a module-private const.\nNo \"keep in sync\" comment is left behind — that comment is what has been\nlying.\n\nThe loop also had no integration coverage. Its pure decision was tested, but\nnone of the wiring was: that a cursor actually reaches the next request's\nquery, that a mid-pagination failure rejects the whole fetch instead of\nreturning the pages already collected, that a malformed later page errors, and\nthat the cumulative deadline fires at all. A deadline that silently never\ntriggers is exactly the sort of guarantee this branch keeps finding was never\ntrue.\n\nBoth adapters now expose a `list_models_transport` seam taking `base` and\n`total_deadline` — the same split `OpenAiClient` already used — so wiremock\ncan drive all four cases per adapter. The deadline test runs a 30ms budget\nagainst a 150ms delayed response, so it proves the bound in ~40ms instead of\nwaiting on the real one.\n\nMoving the shared code into `mod.rs` pushed it to 1410 lines, past the R8 cap,\nso its test module moves to a `tests.rs` sibling — the same split\n`ai_config/mod.rs` already uses. `mod.rs` is now 1076.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: bound the whole paginated fetch and reject a stalled cursor\n\nThird and fourth instances of one defect in this loop: an incomplete result\nreported as success.\n\nA stalled cursor was treated as clean completion. `advance_cursor` mapped a\nrepeated cursor to `Done`, so when a provider said another page existed but\nhanded back the same cursor, both transports returned `Ok` with a partial\ncatalogue. The progress guard added earlier stopped an infinite re-fetch and\nquietly converted it into truncation — trading a hang for the exact failure\nthis branch exists to remove. `Done` is now reserved for \"no continuation\nvalue present\"; a stalled cursor is its own outcome and errors.\n\nThe cumulative deadline also did not bound the operation it was named for.\n`timeout_at(deadline, req.send())` covers the send and the response headers\nonly; the error-body read and the JSON parse that follow ran outside it, so a\nprovider stalling its body sailed straight past the budget.\n\nRather than wrap the two missed awaits, all three I/O steps per page now go\nthrough a shared `bounded()` helper, so the obvious tool at hand is the\ncorrect one and a future fourth step is awkward to leave unbounded. That is\nthe actual fix — the loop has now produced this same defect four times, which\nis a structural problem, not four mistakes.\n\nThe test that should have caught it served a single terminal page, so it\npassed whether the budget was cumulative or per-request. It could not be\nwritten with wiremock either: `set_delay` delays the entire response including\nheaders, so a two-page wiremock test still passes when only `send()` is\nwrapped. It now runs against a raw-socket HTTP double that delays only page\ntwo's body, and was verified to fail against the unfixed transport before the\nfix was restored.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-05T09:38:23+02:00",
          "tree_id": "3af71034513e388dbcf0facd859f06e4e618900f",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/9c6ee42a9b2047f382b750deadd2612081e56e6e"
        },
        "date": 1785916858163,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2119992,
            "range": "± 87876",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2528030,
            "range": "± 24992",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 287097,
            "range": "± 2667",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "7715a2be6a93ef802c1b8cf0ef0ff32275c0fc61",
          "message": "fix: import linkedin profiles anonymously, because signing in is what breaks it (#940)\n\n* fix: import linkedin profiles anonymously, because signing in is what breaks it\n\nReported as \"onboarding imports the resume, settings returns an error\" — same\ncomponent, same command, no code-level divergence. The difference was runtime\nstate, and the direction is backwards from what it looks like.\n\nThe parser needs the ld+json Person block LinkedIn serves to anonymous\ncrawlers. The fetch was built from the stored cookie jar whenever one existed,\nand onboarding has no LinkedIn-connect step — so a fresh install fetched as a\nguest and parsed fine. Connect LinkedIn, and the same URL returns the\nauthenticated SPA shell with no ld+json. The credentials were the defect.\n\nSo this path now always fetches anonymously, deliberately, with the reason in\na comment because \"don't send the user's credentials\" reads as an oversight.\nScoped to the profile-import GET; board scraping and login keep their jar, and\n`build_authed_client`'s other caller is untouched.\n\nIt had been failing invisibly rather than loudly. Success required only that\nname OR experience OR skills be non-empty, and name fell back to og:title — so\nan authwall page with a title produced a successful import of an empty résumé.\nSuccess now requires a real ld+json name, which is precisely the field\nLinkedIn withholds from an authenticated fetch. og:title survives as a\nheadline fallback only, where it cannot fake a result.\n\nThe path also had no logging at all — the reporter's diagnostics bundle\ncontained nothing about their own bug. It now records platform, status,\nwhether a session was attached, and the error, but never the URL or slug:\nbundles get shared, and a profile URL identifies a person.\n\nThe old errors advised connecting a LinkedIn account, which is now exactly\nbackwards. They name what actually failed instead — unreachable, rate-limited,\nblocked, or no public data — and a test asserts none of them ever say \"log in\"\nagain.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: stop claiming a linkedin profile was imported when the save failed\n\nThe import result was error-checked; the save that follows was not. It was\ncast to `{ id?: string }` and the success toast fired unconditionally — but\n`documents_import` returns `{ error }` in-band rather than rejecting, so a\nscanned PDF or a failed text extraction resolved, \"Profile imported\"\nappeared, and `onImported` ran with an undefined id, which silently skipped\nthe résumé write in onboarding.\n\nSo the bug report's own premise — that onboarding imported the résumé — may\nnever have been true. The failure had no way to reach the user.\n\nThe auth classifier is deleted rather than rewritten. It matched \"log in\" and\nfriends and replaced the backend message with advice to connect a LinkedIn\naccount — advice that is now exactly backwards, since connecting an account is\nwhat breaks this import. No backend string matches it any more either, so it\nwas dead as well as wrong. Keeping a substring classifier over messages the\nbackend owns would have recreated the two-places-encode-one-rule drift this\nrepo keeps paying for; the messages were rewritten to be shown, so they are\nshown, and `resumeInput.profileLoginRequired` is gone from en and de.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-05T16:30:33+02:00",
          "tree_id": "18220bb51901d9c39dbd02104a75ac0a2eafc080",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/7715a2be6a93ef802c1b8cf0ef0ff32275c0fc61"
        },
        "date": 1785941610230,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2140180,
            "range": "± 62526",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2518048,
            "range": "± 19257",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 285691,
            "range": "± 2962",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "49699333+dependabot[bot]@users.noreply.github.com",
            "name": "dependabot[bot]",
            "username": "dependabot[bot]"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "9557b1f0e72cc9201b0c2b78162916bfba33abc5",
          "message": "chore: bump aes from 0.9.1 to 0.9.2 in /apps/desktop/src-tauri (#943)\n\nBumps [aes](https://github.com/RustCrypto/block-ciphers) from 0.9.1 to 0.9.2.\n- [Commits](https://github.com/RustCrypto/block-ciphers/compare/aes-v0.9.1...aes-v0.9.2)\n\n---\nupdated-dependencies:\n- dependency-name: aes\n  dependency-version: 0.9.2\n  dependency-type: direct:production\n  update-type: version-update:semver-patch\n...\n\nSigned-off-by: dependabot[bot] <support@github.com>\nCo-authored-by: dependabot[bot] <49699333+dependabot[bot]@users.noreply.github.com>",
          "timestamp": "2026-08-05T18:58:44+02:00",
          "tree_id": "c74e3d178ac94857496f3da61cff85fa427d15b9",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/9557b1f0e72cc9201b0c2b78162916bfba33abc5"
        },
        "date": 1785950546021,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2154401,
            "range": "± 54057",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2597384,
            "range": "± 44559",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 293313,
            "range": "± 7326",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "1335e870dd9d35c0f091aea1f76d5d4d178bd893",
          "message": "fix: bound every http body read, validate probe base urls, and stop reporting failed imports as success (#947)\n\n* fix: bound every http body read through one capped helper in net/http\n\n`read_text_capped` lived in `scraping/http`, so every other subsystem read\nresponse bodies unbounded — a hostile, compromised or merely misconfigured\nendpoint could drive the process into OOM. Move it to `net/http`, the\ndocumented centralized HTTP layer (ADR-003), and add `read_bytes_capped`\nand `read_json_capped` beside it.\n\nConverted: geocoding, LinkedIn profile import, the six `scrape_url`\nresolvers (which hold `get_guarded*` responses — exactly the case the\nhelper was made `pub(crate)` for), and the LinkedIn client's two binary\nreads. Scraping's own `MAX_BYTES` / `FetchOptions::max_bytes` behaviour is\nbyte-for-byte unchanged; it just calls the moved helper.\n\nThe new cap tests force `Transfer-Encoding: chunked` — hyper auto-adds\n`Content-Length` for a known-size wiremock body, so a plain response never\nexercises the streaming-only guard.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: surface a failed document import instead of toasting success\n\n`documents_import` returns its errors in-band (`{ error, message }`), never\nby rejecting, and `use-import-with-ocr` handed every non-`scanned_pdf`\nerror straight back to the caller. All four consumers got it wrong:\nsettings and onboarding toasted \"Uploaded\" unconditionally, and both\n`ResumeInputCard` paths guarded on `result.id` with no else, so a failure\nproduced no feedback at all.\n\nEvery one of those call sites already wraps `importFile` in a try/catch, so\nthe hook now throws — one change, four sites fixed. The post-OCR re-import\nwas returned unchecked too and gets the same treatment.\n\nSame shape as the LinkedIn import bug in #940: an in-band error nobody\nreads, so the success toast lies.\n\nAlso: `ResumeStep` took the new document id from the pre-import cached\nlist rather than the import result, so it could record the wrong default\ndocument — or none at all on a first-ever import.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: persist the embedding format version on cached posting vectors\n\n`get_posting_vector` synthesised `version: EMBEDDING_VECTOR_VERSION` when\nrebuilding the space, so `EmbeddingConfig::matches` was structurally\nincapable of ever rejecting a row from this table. Freshness rested\nentirely on a hand-maintained invariant: every future format bump had to\nremember to ship its own eviction migration, or old-format vectors would\nread as current under an identical space tag.\n\nPersist the column instead. Existing rows default to 0, so they become a\nnatural miss and re-embed on next use — no wipe needed.\n\nSame lesson `upsert_vector_with_conn` already carries a few lines below: a\nwrite path must persist an identity field, not re-derive it.\n\nThe migration is appended at the END of the array — migrations are\nposition-indexed via `PRAGMA user_version`, so inserting mid-array would\nmake every already-migrated install skip it forever.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: cap adapter body reads and validate probe-path provider base urls\n\nRoutes every non-streaming response read in the four AI adapters through\nthe `net/http` capped helpers added in fc602950 — 26 calls across 21 sites.\nSSE and the Ollama pull-progress loop are left alone (already incremental),\nand every `.unwrap_or_default()` diagnostic-body read keeps that shape so a\ncapped-read failure can never mask the status error it was describing.\n\n`ai_test_provider_key`, `ai_list_provider_models` and `ai_model_capabilities`\ntook a renderer-supplied `base_url` and handed it straight to the network\nwith no checks, while the setter path has always dropped it for non-\nopenai-compatible providers and run it through `ssrf::validate_provider_base_url`.\n`resolve_by_name` is the sole entry point for all three, so both rules now\nlive there.\n\nThe parameter stays on the IPC surface — Settings deliberately tests an\nunsaved base URL so an LM Studio endpoint can be verified before saving.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* ui: tell macos users where to run the xattr command\n\nThe command and its copy button were already on the download page, but\nnothing said the code block goes into the Terminal app — which is the one\nthing a non-technical user needs told.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: bound what happens after the cap, not just the read\n\nSecurity review of this branch found the sweep's own claim was false in one\nplace. The LinkedIn client deliberately disables reqwest decompression and\ngunzips by hand, so capping the read bounded only the compressed bytes —\n8 MB in, up to ~8 GB resident. Both decode sites now bound the decompressed\nside too and reject a payload that lands exactly on the cap.\n\nAlso: a discarded `.text()` read in the same file the sweep converted;\n`read_json_capped` rebuilt on `from_slice` so it stops charset-sniffing\n(JSON is UTF-8 by RFC 8259, and a gateway mislabelling it as iso-8859-1 was\nturning valid bodies into schema errors); its parse-failure log now emits\nposition rather than serde's message, which would embed the offending body\nvalue once a call site deserializes into anything but `Value`.\n\nTwo test fixes: the cap test used one oversized chunk, so it passed\nidentically against a per-chunk guard — it now drives three small chunks\nthrough the shared accumulator, verified by temporarily breaking the guard.\nAnd the migration idempotency test now rolls `user_version` back, because\n`run_migrations` skips on `current >= version`, so the guard it claimed to\nre-exercise never ran twice.\n\nThe new column defaults to 2, not 0. Every surviving row post-dates the\nv1 to v2 eviction wipe, so it is provably current — defaulting to 0 would\nhave re-embedded the whole posting cache for no correctness gain. The\nliteral must not track future bumps; it records what rows were at migration\ntime.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: validate the embedding base url the setter persists\n\nThe previous commit claimed `resolve_by_name` was the single entry point\nfor a renderer-supplied `base_url`. It wasn't: `ai_set_embedding_config`\npersisted one unvalidated, and it reaches egress via `embed_text` carrying\nthe provider key and résumé text. Same scrub-then-validate pair, extracted\nso both callers share it rather than growing a third variant of the rule.\n\nAlso restores what the sweep in 70738c63 broke: the `.await??` rewrite at\nthe two paginated list_models sites dropped the \"{name}: parse: \" context\nand flipped the IPC error code from PROVIDER to PARSE. Both are pinned by a\ntest now.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-05T22:19:54+02:00",
          "tree_id": "55a5acb8e772e9f027001cd2f98d8303a0aad48b",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/1335e870dd9d35c0f091aea1f76d5d4d178bd893"
        },
        "date": 1785961874233,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1992363,
            "range": "± 45770",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2419700,
            "range": "± 24708",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 222714,
            "range": "± 2883",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "721c5e517cb60f024e0178ee9da3353742117852",
          "message": "fix: the macOS bug report — docx sizes, blocked pdf downloads, empty generations, and logs you can actually read (#948)\n\n* fix: write docx font sizes in half-points, not twentieths of a point\n\nA user's exported CV came out as 132 pages of enormous text. `w:sz` is in\nhalf-points, but every font size went through `pt_to_dxa` (pt × 20 — the\ncorrect unit for spacing, indentation and page geometry, not type). A 10.5pt\nbody font was written as `w:sz val=\"210\"`, i.e. 105pt. Every DOCX export, on\nevery platform, since the renderer was written.\n\n24 call sites now route through `pt_to_half_points` (pt × 2, rounded — odd\nhalf-point sizes like 10.5 → 21 are representable and were previously\ntruncated). The genuine dxa uses — line spacing, section spacing, indents —\nare unchanged, and border widths never went through the helper at all.\n\nNo test asserted `w:sz` anywhere, which is how a 10× error survived: the\nDOCX suite covered page size, fonts and colours only. There are now exact\nsize assertions for both writers plus a ceiling guard that fails if any run\nexceeds 50pt, which catches this whole class rather than these constants.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: stop false-blocking cover-letter pdfs and make export diagnosable\n\nA user could not download their PDF while the same document exported to\nDOCX fine. `header_url_mismatch` is the only critical check gated to PDF\nonly, which is what made the asymmetry findable at all.\n\nIts résumé arm was narrowed to the topmost N links after two prior\nfalse-block regressions. The cover-letter arm still checked every link in\nthe top 144pt band unconditionally, justified by \"no body section can\nrender early into its band\" — true of a body section, not of a body link in\nthe opening paragraph, which `tokenize_rich` will happily turn into a real\nPDF annotation. With company research on, a model echoing a researched URL\nin its first sentence lands there. And a cover letter is never two-column,\nso `can_linearize` is false: the user got a hard block with no remediation\noffered. Now narrowed the same way, verified by reverting the file and\nwatching the new test fail on old code with the incident's exact shape.\n\nThe bundle that reported this had no export log lines at all — the command\ncomputed a full report with per-issue codes and threw all of it away except\na concatenated string. Export now uses the `Span` idiom the rest of the app\nalready uses, and logs the issue CODES on a block, never the messages,\nwhich can embed a URL.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: treat an empty generation as a failure instead of a result\n\nA user's CV and cover letter both came out empty with no error shown, and\nregenerating produced real content. `awaitAiStream` has two resolve paths —\nthe stream's `done` chunk and the job-status poll fallback — and neither\nchecked for content, so an empty completion was persisted and displayed as\na successful generation.\n\nBoth paths now reject on whitespace-only output. Neither caller needed a\nchange: `handleGenerate` and `runTailor` already persist only on the success\npath, so rejecting at the source was enough — the same reasoning as the\nimport fix in #947.\n\nAlso logs job id + provider/model on the empty path, never the content.\nThat absence is why the reporter's bundle showed nothing.\n\nWhat produced an empty stream in the first place is still unknown; this\nonly stops it being silent.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: stop two whole-response eaters and record why a stream produced nothing\n\nThe reporter on ollama-cloud/gpt-oss:120b got an empty CV and cover letter\nwith no error. Two independent causes, both of which delete or discard a\ncomplete response:\n\n`extractPlainText` strips fenced code blocks, so a model that wraps its\nENTIRE answer in one fence had the whole document deleted — verified at\nzero length for a bare fence, a language-tagged fence, and a fence with a\ntrailing newline. This was already found and fixed in `github-projects.ts`,\nwhose comment names the failure outright; the shared helper every résumé\nand cover-letter generation runs through was never touched. Now unwraps\nonly when the entire trimmed response is one fence, so a genuine mid-answer\nsnippet is still stripped as before.\n\n`finish()` persisted a zero-content stream as a completed job. A reasoning\nmodel that never reaches its final channel leaves `answer` empty while the\nstream itself succeeded, and every consumer read that as success. It now\nerrors the same way a transport failure does, which routes the extension\nbridge and CLI-agent paths correctly without touching either.\n\n`finish_reason` was never parsed, so \"ran out of budget mid-reasoning\" was\nindistinguishable from a clean empty stream — that ambiguity is why this\nneeded two investigations. It is now parsed and reported distinctly.\n\nAlso prices gpt-oss: with no matching RATES row it fell through to the\n3.00/15.00 default, billing a cheap open-weights model at Claude Sonnet\nrates. Ollama Cloud publishes no per-token figure, so the rows use\nOpenRouter's live published rate for the same model.\n\nStream start and end now log provider, model, answer/thinking lengths,\nfinish reason and usage — ids, counts and enum values only, never content.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: make the logs actually reach the file a user can send\n\nTwo reasons the reporter's diagnostics bundle was nearly empty, neither of\nwhich was a missing log call.\n\nThe global level filter is Warn and only `cover_letter::research` was raised\nto Info — which is exactly why research briefs were the sole app content in\ntheir bundle. Every `Span` line in the app logs under one target, so raising\n`observability` covers ai, scrape, apply, autopilot, pipeline and export at\nonce, plus the stream module for its own two lines. A regression test pins\nthe module path against the filter target, so moving the file can't silently\nturn every span dark again.\n\nAnd no renderer log had ever reached the file at all: `attachConsole` mirrors\nRust logs INTO devtools, the opposite direction, so following that route\nwould have shipped a bridge that does nothing. The console methods now\nforward to the plugin, which captures every existing call site for free. The\ncapability it needs was already granted.\n\nRenderer generation and export paths now log start/done/failed. They use\nwarn deliberately — the repo bans `console.info`, and warn is what the\ncurrent filter passes, so the two halves need not agree on a level.\n\nEnd-to-end delivery is NOT verified: the unit tests prove the forwarding\nlogic against a mock, not that the IPC round-trip lands in the file. That\nneeds a real run.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: stop logging absolute paths into shareable diagnostics bundles\n\n`AGENTS.md` forbids absolute paths, usernames and home dirs anywhere,\nlogs included. Two sites violated it at warn/error, so they were in\nbundles today — and this branch is actively asking users to send bundles.\nOne arrived over Telegram this week.\n\n`postings` logged the full `interactions.json` path four times; the message\nalready names the file, so most lines lose the path entirely and the rest\nlog a file name. `extension_bridge` was harder: six browsers share one\nmanifest filename in different directories, so a bare name would be\nambiguous — it now takes a caller-supplied label instead of deriving\nanything from the path.\n\nSwept the whole tree for the class rather than fixing only what was\nreported: 90 `.display()` sites and every log/tracing call. No other\nproduction leaks. `std::io::Error` from `fs` does not embed the path in its\nDisplay, and the diagnostics-bundle builder and Sentry already run through\na shared redaction gate — both confirmed rather than assumed.\n\nNew architecture rule R15 fails on `.display()` inside a log or tracing\nmacro, with an empty allowlist. It deliberately ignores `.display()`\nelsewhere: a path in an error the user reads about their own file is\nlegitimate. Four self-tests prove it catches the real multi-line shape\nrather than passing vacuously.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: show the user when an export fails instead of nothing at all\n\nWe spent two investigations working out what error message the reporter\nsaw when their PDF would not download. They saw none. Every export trigger\nwas either a bare try/finally with no catch or a fire-and-forget `void\nfn()`, so the backend's perfectly readable \"Export blocked: ...\" became an\nunhandled promise rejection and the button simply did nothing.\n\nThat is very likely the literal experience behind \"couldn't download it as\npdf\".\n\nThree call sites now catch and surface the backend's own message verbatim,\nper the pattern #940 set for this path, with a localized fallback only when\nthe thrown value carries no message. Two more were covered transitively\nthrough the shared modal — including one the original audit missed. The\nexport modal also stops closing itself on failure, so a retry is possible.\n\nFailures log format, document type and message — never the filename,\ncandidate name, company or content.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* test: prove every résumé template's pdf text actually extracts\n\nThe leading explanation for the blocked download is `no_extractable_text` —\na critical block that fires when extraction succeeds but returns under 20\ncharacters, the signature of a PDF that renders correctly but whose text\nlayer is unreadable. It fits the whole evidence set: both document types,\nPDF only, DOCX fine (XML, no glyph layer), preview fine (a different\nserializer with no text layer at all).\n\nNothing in the repo could have caught it. One test round-tripped real PDF\ntext through extraction, covering a single letter/layout/template\ncombination. The 12-template résumé test asserted only a %PDF header and a\npage count, and the two accented-text résumé tests exist to guard a\ngrapheme-slicing panic and never re-extract. ADR-008 moved subsetting into\nthe external Typst crates when our renderer was deleted, so no test\nverifies that claim at all.\n\nAll 12 templates plus the Refined and Banded letter layouts now render and\nextract, asserting against the validator's own normalize + 20-char gate so\na pass means the real validator would not have blocked. The fixture uses\nItalian grave accents including capitals — the shape the audit found least\ntested. Iterating a shared template list means a new template is covered\nwithout remembering to.\n\nProven to fail: stripping /ToUnicode from the font objects of a real\nrendered PDF drops extraction to zero characters and trips both assertion\nstyles. 0.68s for all 12 templates, so no ignore attribute is warranted.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: make the renderer log bridge actually reach the log file\n\nThe bridge shipped in this PR forwarded console.info through the plugin's\ninfo() wrapper, which sends a `location` derived from the call stack. The Rust\ncommand turns that into the record target `webview:{location}` — and\ntauri-plugin-log filters through fern, whose level_for prefix match only walks\n`::` boundaries. A single-colon target matches no entry, so every forwarded\nconsole.info fell back to the global Warn filter and was dropped: exactly the\n\"how far did the generation get\" breadcrumbs the bridge exists for.\n\nInvoke `plugin:log|log` directly with no location, so the target is bare\n`webview`, and register level_for(WEBVIEW_TARGET, Info). Nothing is lost by\ndropping the location — the plugin derives it from the 4th stack frame, which\n(because this bridge is what calls the plugin) always pointed at log-bridge.ts\nrather than the real console caller.\n\nFound by CodeRabbit on #948.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: unwrap a one-line whole-response code fence instead of emptying it\n\nThe whole-fence unwrap added earlier in this PR required a newline after the\nopening marker, so a response the model wrapped on a SINGLE line fell straight\nthrough to the delete pass and came back as ''. That is the same\nwhole-document-wiped defect the unwrap was written to fix, via the shape it\ndidn't cover — and a one-word application answer is exactly that shape.\n\nMatch the one-line form too. It cannot carry a language tag (a lone tag would\nleave no answer), so everything between the markers is the body. The\ninterior-fence guard still refuses two back-to-back one-line fences.\n\nFound by CodeRabbit and the CI AI review on #948.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: keep export error messages out of the diagnostics bundle\n\nThe export catch blocks added in this PR logged the raw error message, and the\nconsole log bridge added alongside them persists that into the rotated log file\na user attaches to a bug report. The backend's own header_url_mismatch message\ndeliberately embeds the offending URL, so a resume's personal links could ride\ninto a shareable bundle — the exact trade the Rust side had already made the\nother way in export/commands, which logs issue codes and never issue.message.\n\nLog a stable error class instead, at all six sites (the three new catch blocks\nplus the three pre-existing raw-object logs in export.ts, now on the same\npersisted path). The user still sees the full message: the toast renders it and\nis not persisted anywhere.\n\nFound by CodeRabbit (CWE-532) and the CI AI review on #948.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: log the model a run actually used, and log cancellations\n\nBoth generation start lines logged the caller's model argument rather than\nresolveActiveProvider's resolved activeModel. That argument is only the\nfallback used when the active config carries no model, so a diagnostics bundle\ncould name a model the run never touched — worse than no line at all when the\nwhole point is reconstructing what happened.\n\nAlso log a cancelled tailor run. The abort branch returned silently, so a run\nthe user cancelled and a run that hung or died mid-stream looked identical in\nthe log: both just stop after '[runTailor] start'.\n\nFound by CodeRabbit on #948.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: price gpt-oss-safeguard from its own rate, not the family catch-all\n\nThe gpt-oss rows added in this PR claimed the bare catch-all was \"the more\nconservative (higher) of the two known sizes, so an unmatched variant is never\nsilently under-costed\". That is false: gpt-oss-safeguard-20b bills $0.075/$0.30\nper 1M — roughly double the catch-all's $0.037/$0.17 — so the one variant the\ncomment named by hand was the one it under-costed.\n\nAdd its row ahead of the prefix catch-all and correct the comment to say what\nis actually true: the catch-all is not a ceiling for the family, and a new\nvariant needs its rate checked rather than assumed covered.\n\nRate verified against openrouter.ai/api/v1/models/openai/gpt-oss-safeguard-20b\n/endpoints on 2026-08-06, not from memory. The fallback test now uses a\ngenuinely unlisted id, since its old subject has a row of its own.\n\nFound by CodeRabbit on #948.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: stop pointing users at a settings field they cannot see\n\nThe finish_reason:length message told the user to raise max output tokens in\nSettings → AI. That control (LocalModelLimits) renders only for the LOCAL\nollama provider, and local Ollama is the one provider that never reaches this\nbranch — it streams no finish_reason. So every user who could see the message\nwas sent to a field that does not exist for them. Keep the diagnosis, drop the\ndead pointer.\n\nAlso correct the stop_reason doc, which claimed Anthropic/Gemini/local Ollama\nhave \"no streamed equivalent\" of finish_reason. All three stream one\n(message_delta.stop_reason, candidates[].finishReason, done_reason); our frame\nparsers just do not map it yet. The comment told the next maintainer the data\ndoes not exist, which is the part that actually causes harm.\n\nAnd prefer the captured stderr when a CLI agent emits a Done sentinel and THEN\nexits non-zero: that path skipped the earlier guard, so it reported the generic\n\"produced no answer content\" and discarded the not-logged-in / quota / bad-flag\ndetail friendly_cli_error already knows how to read.\n\nFound by the CI AI review on #948.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: close two holes that made architecture rule r15 miss real leaks\n\nR15 shipped in this PR with an empty allowlist, which read as \"the codebase is\nclean\". It was partly \"the scanner cannot see it\":\n\n1. The marker list held only fully-qualified calls (`log::warn!(`), so the three\n   modules that do `use tracing::warn;` and call `warn!(…)` — extraction/mod,\n   extraction/pdf, extraction/registry — were silently exempt. Match the bare\n   forms too.\n2. The span heuristic ended at the first line ending in `);`, documented as\n   \"the outer call's own close\". Not always: a macro argument holding a block or\n   closure can contain a statement ending in `);` at a deeper indent, which\n   truncated the span before the real `.display()` one line further down.\n   Require the close to be at an indent no deeper than the macro's own line.\n\nBoth get a regression test built from the exact mechanism. The rule still\npasses over the whole crate with an empty allowlist — now meaning it.\n\nFound by the CI AI review on #948.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* test: pin the letter signature, not just the letterhead\n\nBoth accented-Latin letter fixtures print the candidate's name twice — once in\nthe letterhead, once in the signature — so the whole-document `contains` passed\neven if extraction dropped the entire sign-off block. Assert the name appears\nafter the sign-off as well, which is what the test claims to prove.\n\nAlso record how the cover-letter false-block regression's band precondition was\nverified: reverting the topmost_n narrowing makes it fail with exactly\nheader_url_mismatch on the body link, which is only reachable if that link is\ninside the 144pt band. The reviewer's concern was that it might pass identically\non main; it does not.\n\nFound by CodeRabbit and the CI AI review on #948.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: cap generation error messages in the log instead of dropping them\n\nThe AI review flagged the two generation-failure log lines as the same\nCWE-532 shape as the export ones and asked for errorClass. That fix is wrong\nhere, and the asymmetry is the point.\n\nAn export failure can lose its message safely because the Rust side already\nlogs the critical issue CODES — the reason survives elsewhere. A generation\nfailure has no second record: \"Ollama 500: the input length exceeds the context\nlength\" is the ONLY trace of why a run died, and it is precisely the line that\nwould have resolved the macOS report this PR exists for in one look. Reducing it\nto \"Error\" would delete the reason this logging was added.\n\nSo keep the message, bounded to 300 chars — the same cap friendly_cli_error\nalready applies to captured CLI stderr. A provider or transport error is short;\na response that has somehow captured document text is not, and the cap stops\nthat spilling wholesale. redact_lines strips URLs, paths and credential\nassignments as the bundle is written, but does NOT redact prose, which is\nexactly the gap this cap covers.\n\nTruncation is marked, not silent — an unmarked cut reads as a complete provider\nerror that merely ends oddly.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* test: cover the cli-agent stderr fallback and tighten two weak assertions\n\nThe stderr-preference fix shipped without a test because `run_stream` needs an\nAppHandle and a real child process. The DECISION it added needs neither, so\nextract it as `terminal_error` and pin all three branches: a non-zero exit with\nrecognisable stderr (upgraded to the actionable sign-in error), a non-zero exit\nwith opaque stderr (still beats the generic empty-answer message), and the\ndifferential — a clean exit keeps the empty-answer message untouched.\n\nTwo assertions were weaker than they read:\n\n- The letter signature checked `àlvaro` and `èsposito` as separate substrings,\n  so reordered or separated tokens would pass. Assert the full name, which is\n  what the validator actually matches.\n- The adjacent-fence test asserted only the absence of backticks, so an\n  implementation that stripped the delimiters and left the bodies behind would\n  pass too. Assert the exact output.\n\nFound by CodeRabbit on #948.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-06T06:28:24+02:00",
          "tree_id": "3418157233a129f5b07c3c50e5ac941d29fda8cb",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/721c5e517cb60f024e0178ee9da3353742117852"
        },
        "date": 1785991174129,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2126904,
            "range": "± 82527",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2494333,
            "range": "± 12571",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 287542,
            "range": "± 7951",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "06daf61575e4865c9030a1306cf9afffd88d0c9e",
          "message": "fix: make clicking an OS notification open the app, on the right screen (#949)\n\n* fix: make clicking an os notification open the app, on the right screen\n\nReported twice: clicking a Windows notification does nothing, and there was no\nway to jump from a notification to the thing it was about.\n\nBoth had one cause. `tauri-plugin-notification`'s desktop `show()` is\nnotify-rust fire-and-forget, and the `onAction` JS helper its docs point at for\nclicks resolves to `plugin:notification|register_listener` — a command the\nplugin registers ONLY on mobile (its desktop invoke_handler is exactly notify,\nrequest_permission, is_permission_granted). So the renderer's subscription\nfailed with \"Command not found\" on every desktop startup and no banner click\never reached the app. `notifications_clicked` — which already does the right\nthing — had no caller other than the tray.\n\nShow the banner natively instead, per platform, since only Windows is\ncallback-based:\n\n- Windows: tauri-winrt-notification's on_activated, non-blocking.\n- macOS/Linux: notify-rust's wait_for_action BLOCKS, so it runs on\n  spawn_blocking. Linux additionally needs a declared `default` action for a\n  body click to be delivered at all.\n\nBoth crates were already in the tree transitively; they are pinned to the\nlocked versions so there is no duplicate copy.\n\nRouting: the click now carries the clicked notification's OWN route, so\n\"Autopilot X found 3 jobs\" opens THAT autopilot rather than a list to search.\nEvery notification source already attached a route (autopilot -> /autopilot with\nits id) and the bell already followed it — only the OS banner didn't. Validated\nthrough the same resolveNotificationRoute the bell uses, so an unknown backend\nroute falls back instead of breaking navigation. A tray click sends no route and\nstill just opens the inbox.\n\nThe dead renderer seam (onOsBannerClick) is removed, which also silences the\nstartup console error it produced.\n\nKnown limit, all platforms: the callback lives in this process, so it fires only\nwhile the app runs. A click after the app exits needs a COM activator on Windows\nand is out of scope.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: bound banner listeners and close the windows ci gap\n\nFour review findings, all real.\n\nThe macOS/Linux path parks a blocking thread in wait_for_action until the user\nclicks or the banner closes — and a notification nobody touches (sitting in\nmacOS Notification Center) parks it indefinitely. OsBanner::Always sources like\nautopilot can emit a burst, so a run of ignored notifications would hold\nblocking workers permanently and starve every other spawn_blocking caller.\n\nBounded to 8 concurrent listeners via an RAII slot (released on Drop, so a panic\ninside the wait cannot leak the budget). Over budget the banner is still\nDELIVERED — on Linux show() already sent it, and macOS's handle Drop sends it —\nit just has no click handler. Losing one banner's click beats starving the pool.\nThe async alternative was checked and rejected: wait_for_action_async exists\nonly on Linux, and behind a feature that swaps the dbus backend.\n\nWindows had the same CI hole this branch opened the macOS job for: no PR job\ncompiles cfg(windows), so the notification code that actually fixes the reported\nbug was built only on the dev machine. Added the counterpart.\n\nAlso corrected the macOS job's own comment, which overstated the gap:\ncfg(not(windows)) IS compiled by the ubuntu jobs — only platform-exclusive\npredicates were uncovered.\n\nAnd the route tests now use the shared NotificationOpen type instead of local\nstructural aliases; they exist to guard that contract, so a local shape could\nnot detect drift in it.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-06T08:01:25+02:00",
          "tree_id": "3c17c3d25bdf14d980a03b888c8bcac17375e4b4",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/06daf61575e4865c9030a1306cf9afffd88d0c9e"
        },
        "date": 1785996784023,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2156066,
            "range": "± 45970",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2544624,
            "range": "± 21150",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 287111,
            "range": "± 8742",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "ce9000a4d70fe2f9709011c5c601b7d09b408af5",
          "message": "fix: map local Ollama's stop reason, and stop presuming an OpenAI embedding model (#950)\n\n* fix: map local ollama's stop reason and stop presuming an openai embedding model\n\nTwo follow-ups from #948, both verified against the code first.\n\n**Local Ollama never reported a stop reason.** `parse_ollama_frames` set `usage`\non the final sentinel but skipped `done_reason`, which rides on that same object\n— so `stream::finish` could not tell \"the model burned its whole output budget\nreasoning and never answered\" from \"the provider silently returned nothing\" for\nlocal Ollama. That is the exact ambiguity that made an empty generation\nunexplainable in the macOS report.\n\nThe non-streaming agent path (`parse_ollama_turn`) already read the field, so the\nmapping is extracted to `ollama_done_reason` and shared rather than copied — a\nsecond hand-written copy is the duplicated-heuristic defect this codebase keeps\nre-learning. Callers keep their own precedence on top (a turn still lets Length\noutrank a tool call).\n\n**`text-embedding-3-small` was offered as the default for providers that have\nnever heard of it.** It is an OpenAI model id, and every OpenAI-compatible client\nis built on `OpenAiClient`: Ollama Cloud delegates `default_embedding_model`\nstraight through, and `OpenAiCompatible` covers LM Studio, vLLM and OpenRouter.\nEmbedding with any of those and no explicit model posted an OpenAI model to a\ngateway that does not host it, and the model-not-found came back reading as\n\"embeddings are broken\" rather than \"choose an embedding model\".\n\nGated on `id == OpenAi`, the same way `supports_web_search` already is.\n\nDeliberately NOT flipping `supports_embeddings` to false for those providers,\ndespite that being how I had recorded this: the boolean is provider-level (the\nerror text says so, and Anthropic is the only false), the `/v1/embeddings`\nendpoint may genuinely exist on a given gateway, and an explicit model the user\nknows works must still go through. A per-model allowlist would also break the\nzero-code-change rule for new models and risk false negatives that block working\nsetups. Only the presumed DEFAULT was ever wrong.\n\nAlso split the two embedding errors that were identical strings: \"no default\nmodel for X, choose one\" is fixable by picking a model; \"X does not support\nembeddings\" is not.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: point local ollama at the output cap it can actually raise\n\nThe AI review caught that this branch invalidated my own reasoning from #948.\n\nThere, EMPTY_ANSWER_LENGTH_MESSAGE deliberately dropped its \"raise max output\ntokens in Settings\" pointer, because the only such control (LocalModelLimits)\nrenders solely for the LOCAL ollama provider — and local Ollama was the one\nprovider that could never reach the length branch, having reported no stop\nreason at all. Sending an OpenAI user to a field they cannot see is worse than\nno advice.\n\nMapping done_reason in this same PR is exactly what makes local Ollama reach\nthat branch. So the doc comment became false, and the users who CAN act on the\nadvice are now the only ones not getting it.\n\nSplit the message: local Ollama gets wording naming the real control\n(\"Max output tokens\" under Generation limits), every other provider keeps the\ngeneric one. Pinned with the differential — five other providers must still get\nthe generic message.\n\nAlso dropped the CLI-agent call sites' use of this helper: emit_done's own doc\nsays CLI agents have no finish_reason, so the call could only ever return the\ngeneric constant. Use it directly rather than passing a provider that does not\ninform the result.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-06T08:48:50+02:00",
          "tree_id": "34d520571674a5afa11662c96e0000751ea5d210",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/ce9000a4d70fe2f9709011c5c601b7d09b408af5"
        },
        "date": 1785999645826,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2219062,
            "range": "± 77176",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2747318,
            "range": "± 76104",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 303881,
            "range": "± 5808",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "00881e58672170ff9974b7d172ba36bce5bde198",
          "message": "feat: index documents automatically, and stop the index strip reading as a chore (#951)\n\n* feat: index documents automatically, and stop the index strip reading as a chore\n\nReported as \"I have to go to AI settings and press the indexing button after\nuploading a résumé\". Tracing it first: that was never true. `match_resume`\nalready embeds lazily when it finds no usable vector and caches the result, so\nthe Settings button is a pre-warm, not a prerequisite. What it actually cost was\na slow first match and a status strip that looked like a task list.\n\nThree parts.\n\n**Say what is true.** The strip said \"N documents need re-indexing for the active\nmodel\" in amber. Nothing needs it — matching indexes on first use. Reworded, and\nno longer styled as a warning, with a separate line for when auto-indexing is on.\n\n**Auto-index, opt in.** New `autoIndexOnUpload` preference. One hook mounted at\nthe root covers all three moments that create stale documents, because they all\nsurface identically as a non-zero `documents.stale`: a fresh import, an embedding\nprovider/model change, and leftovers from a previous session.\n\nBacked by a NEW command rather than the existing one: `ai_reembed_all` re-embeds\nevery document unconditionally, so reusing it would re-bill a cloud embedding\nprovider for already-indexed documents each time a single file is added.\n`ai_index_stale_documents` embeds only what is missing and returns a null job id\nwhen nothing is. Both share one job body so they cannot drift in error handling,\ncancellation or progress.\n\n**Ask during setup.** New onboarding step immediately before the crash-reporting\nconsent — both ask \"may this run on your behalf\", and this one calls a provider\nthat may bill per token, so the two spend/privacy decisions sit together.\n\nDefaults to OFF, and the step's switch is seeded from the stored value rather\nthan forced on: an existing install must never start calling a paid embedding\nprovider because of an update, and stepping back through the wizard must not\nsilently discard a \"no\". Leaving it off costs nothing — matching still indexes\nlazily, which is the whole point of the first part.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: auto-index a second upload that leaves the same stale count\n\nThe AI review found a HIGH in the guard I wrote, and it was right: the dedup key\nwas `provider/model/staleCount` and never reset. Import one résumé (stale 1,\nindexed), import another (stale 1 again) and the second matched the previous key\nand was skipped — auto-indexing silently died after the very first document. A\ncount identifies a situation, not a batch.\n\nTwo things were needed, and each protects against the other's failure mode.\n\nHold the concurrency guard until the JOB ends, not until indexStaleDocuments\nresolves. That returns as soon as the job is spawned, so the status refetch\nstraight after it still reports the pre-run count and would start a second,\nseparately-billed run over the same documents. Now released on the job's\nterminal event, via the same useJobEvents path the manual re-index already uses.\n\nKeep a per-(space, count) attempt key, but CLEAR it once the index goes clean or\nthe space changes. Without the key, a run that fails to reduce the count\nre-triggers the instant it ends — an unbounded loop of paid provider calls.\nWithout the reset, the reported bug.\n\nThe guard also had to become state rather than a ref: the effect must re-run\nwhen a job finishes, and clearing a ref re-renders nothing.\n\nBoth halves are pinned, and the regression test was verified to fail against the\noriginal guard rather than assumed to: removing the reset reproduces exactly the\nreported symptom.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: never run two embedding jobs at once, and stop the strip guessing\n\nThree review findings.\n\nAuto-indexing and the manual \"Re-index now\" button were independent triggers\nwith no knowledge of each other, so a background auto-index and a button press\ncould embed the same documents concurrently — billing a cloud provider twice for\nidentical work. Guarded in the BACKEND rather than by disabling the button:\nthat is the one place every trigger has to pass through, and a UI-only guard\nnarrows the race instead of closing it. A second trigger now returns the running\njob id, so the caller watches that run instead of starting a duplicate. No new\nstate — JobTracker already records kind and status.\n\nThe strip inferred \"indexing them now\" from the auto-index PREFERENCE alone,\nso it claimed work was happening even when the run had already failed, or was\nnever started because nothing had changed. `ai_embedding_status` now reports\nwhether an embedding job is actually running and the strip reads that.\n\nAnd the step's top doc comment still said the switch \"defaults ON\" while the\ncode — and a comment twelve lines below it — did the opposite. Left over from\nthe first draft; the opt-in behaviour is the correct one, so the comment was\nwhat needed fixing.\n\nThe guard's per-record decision is split out as a pure predicate so it can be\ntested without the AppHandle this crate has no harness for. The load-bearing\ncase is the differential: a COMPLETED job must not count as active, or the guard\nlatches after the first run and auto-indexing never fires again.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: make the embed-job claim atomic and the completion listener race-proof\n\nTwo review findings, and closing the second surfaced a harder race than the one\nreported.\n\nThe guard was check-then-act: `running_embed_job` then `job_start` are two\nseparate lock acquisitions, so two commands can both observe \"nothing running\"\nbefore either registers. Scan and insert now share one lock via\n`JobTracker::start_exclusive`.\n\nThat returns `Option`, not `Result`. R6 caught the first attempt for banning\n`Result<_, String>`, and the rule was pointing at a real modelling error: \"a job\nis already running\" is not a failure, it is the job the caller should watch. The\ntype says that now.\n\nThe completion listener compared against the `inFlightJob` STATE.\n`setInFlightJob` only schedules a render, so a job finishing before that render\ncommits hits a closure still holding `null`, drops its own completion, and\nlatches the guard forever — auto-indexing dead until restart. Now compared\nagainst a ref written synchronously alongside the state, the same fix\n`EmbeddingsSettings` already documents for `reindexJobIdRef`.\n\nWriting the regression test for that exposed a second ordering: a job can finish\nbefore the invoke that created it even RESOLVES (an index where every document\nfails returns almost immediately), so the completion arrives before there is any\nclaim to match. The ref alone does not help there. Terminal ids seen without a\nclaim are now remembered briefly, and a claim for one of them releases\nimmediately instead of waiting for an event that already passed.\n\nBoth races are pinned, and both tests were verified to fail against the previous\ncode rather than assumed to.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-06T11:14:54+02:00",
          "tree_id": "48e0533edc940a3481a8673d5d0e8e5d5c8fcf1a",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/00881e58672170ff9974b7d172ba36bce5bde198"
        },
        "date": 1786009159785,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2154270,
            "range": "± 55642",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2537708,
            "range": "± 21808",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 288865,
            "range": "± 9170",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "94d65b35746dcab83aba69ca3eab372fb36b20ac",
          "message": "fix: stop the resume header rendering twice and repair pre-v0.134.0 link mojibake (#957)\n\n* docs: add the landing workspace to the monorepo directory listings\n\napps/landing has been a real pnpm workspace for a while but appeared in none\nof the four Markdown listings that describe the tree, so every one of them\ntold a reader the repo has two apps.\n\ndocs/DEVELOPMENT.md's block was the stalest: it still called the desktop app\napps/tauri, pointed at a src/tauri-client.ts that is a directory, and omitted\napps/extension and packages/translations as well.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: reconcile the resume header name in place instead of stacking a second one\n\nseedHeaderFromProfile only recognised the document's own name line when it sat\nat exactly index 0. Two shapes defeated that. An ALL-CAPS name passes\nisAllCapsSectionHeading, so looksLikeHeaderBoundary read it as a section\nheading and the profile's name was unshifted above it; and a leading blank\nline, which PDF extraction routinely emits, pushed the real name line to\nindex 1 where nothing looked for it. Either way the contact scan then broke on\nthat same line at i = 1, found no match, and spliced a second contact line in\ntoo, so the export rendered the whole header twice: the styled header, then a\nbody section headed with the ALL-CAPS name carrying the title and the original\ncontact line.\n\nThe header block is now searched for a line that already IS the profile's name,\ncompared casing- and punctuation-insensitively, and that one line is\noverwritten in place. It is the only safe re-scoping of a line the boundary\npredicate flagged, because it can match nothing except the name we are about to\nwrite there; every other line keeps the never-remove-or-blind-overwrite\ninvariant from #931.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: key the resume name rule on the first line with content, not raw line 0\n\nparse_line decided \"this line is the candidate's name\" from the raw line\nindex. PDF extraction routinely emits a leading blank line before the header\ntext, so the real name landed at index 1, missed the rule, matched\nis_all_caps_section_heading instead, and became a section header. That flips\nseen_section immediately, so the title and contact lines below were swallowed\ninto a section named after the candidate while header.name and header.contact\nstayed empty and apply_to_header refilled them from the contact profile — the\nheader rendered twice, once styled and once as a body section.\n\nidx == 0 is kept as its own arm so the original rule cannot regress whatever\nall_lines holds; past that the preceding lines must all be blank and idx must\nbe a real index into all_lines, since an out-of-bounds range must not read as\na vacuously-true empty slice.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: match accented and all-caps names when reconciling the resume header\n\nThree defects the sibling review caught in the name reconciliation.\n\nnameKey claimed NFKD folds accented variants together, and it does not. NFKD\nsplits a composed character into base plus combining mark, the mark is\ncategory Mn rather than a letter or number, so the punctuation collapse turned\nit into a space: Francois became \"franc ois\" and stopped matching FRANCOIS.\nEvery user whose profile name and resume differ in accent presence fell\nthrough to the old path and got the duplicated header this change exists to\nprevent. Combining marks are now stripped outright.\n\nsanitizeHeaderName never changes case, so an all-caps fullName in the profile\nleft the reconciled line all-caps, the contact scan re-tripped the boundary\npredicate on it, broke early, and inserted a second contact line. That index is\nnow skipped by the scan exactly as index 0 already was. The name keeps the\ncasing the user typed.\n\nheaderBlockEnd spans the whole document when it contains no blank line at all,\nso findNameLine could reconcile an occurrence of the name in the body and leave\nthe header with no name at all. It now stops at the first section boundary,\ntesting for a name match first so an all-caps name still matches itself.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: repair pdf link anchors corrupted before the utf-16 decoder landed\n\npdf_text_string in #955 stopped new corruption but could not touch text\nalready written. A PDF text string is UTF-16BE behind a FE FF BOM, and reading\nit with from_utf8_lossy rendered the BOM as two U+FFFD followed by every\ncharacter interleaved with its NUL high byte, so stored link anchors read as\nmojibake and matched no project title, leaving every link heaped at the end of\nthe section instead of on its own entry.\n\nThe shape is losslessly reversible, so repair_utf16_mojibake drops the doubled\nreplacement chars and the NULs, gated on the string containing a NUL at all,\nwhich is never legitimate in stored document text. Two appended migrations\nrewrite only NUL-bearing rows in documents.text and in ai_generations'\nresume_text and cover_letter_text. A per-row failure warns rather than\npropagating: rolling back would undo every other row's repair and leave\nuser_version wedged, retrying the same failure on every future startup.\n\nAlso fixes two pre-existing tests that identified their target as\nMIGRATIONS.len() - 1. Appending anything silently pointed them at the wrong\nmigration, which is the trap a position-indexed list sets for every future\nappend.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: scope the mojibake repair to the shape it can actually reconstruct\n\nReview round on the repair migrations.\n\nThe per-row UPDATE error is propagated instead of swallowed. Swallowing did\nnot do what its comment claimed: on a disk-full or IO error SQLite auto-rolls\nback the enclosing transaction, so the loop continued in autocommit and the\nfollowing PRAGMA user_version committed on its own, durably marking the\nmigration applied while the row stayed corrupt and was never retried. Store\nopens are already non-fatal in lib.rs, so propagating costs a session, not the\napp.\n\nThe repair is now anchored on the corruption's own shape rather than stripping\nevery doubled replacement char and every NUL in the row. It requires the BOM\nmarker followed by a NUL, then consumes only (NUL, ASCII) pairs it can\nreconstruct. A resume body carrying two adjacent unmapped-glyph markers no\nlonger has them silently deleted, which fused words on the html and rtf import\npaths, and a UTF-16 anchor with non-ASCII code units is left alone rather than\nturned into plausible garbage with its markers erased, since those bytes are\nstill a faithful image of the payload and remain recoverable.\n\nRepaired documents also clear keywords_json and indexed. Rewriting the text\nwhile leaving the cached keywords and the embedding pinned to the corrupt\nversion would have scored the resume forever against text it no longer\ncontains. Both migrations snapshot the affected rows first, and the insert,\nupdate and import paths repair on write, so restoring a bundle exported before\nthis fix cannot re-inject the corruption.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* docs: correct adr-0021 for the in-place header name reconciliation\n\nThe decision and consequences both still said seedHeaderFromProfile replaces\nline 0. It now searches the header block for the line that already is the\nprofile's name and overwrites that one in place, falling back to the original\nreplace-or-insert only when nothing matches. Records the ceiling too: an\nall-caps name that does not match the profile still stacks, deliberately,\nbecause guessing would risk destroying a real section heading.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* style: apply cargo fmt to the header and mojibake changes\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: drop the mojibake snapshot tables on a full data erasure\n\nThe repair migrations snapshot every affected row's pre-repair text as a\nsafety net for an in-place rewrite, and that snapshot holds the user's\noriginal resume and cover letter in plaintext. Neither store's clear_all\nknew about it, so a full erase-my-data reset left those documents on disk.\nFor a local-first app whose privacy story is that documents never leave the\nmachine and can be erased on demand, that is the wrong outcome, and it exists\nonly because the snapshot was added.\n\nBoth clear_all batches now drop the snapshot alongside the live tables. DROP\nrather than DELETE: it is a one-shot migration artifact and user_version is\nalready past the migration, so nothing recreates it. The tests assert it is\ngone from sqlite_master rather than merely empty.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: start the header block at the first content line, not line 1\n\nheaderBlockEnd scanned for the terminating blank from index 1, so two or more\nleading blank lines made it return 1. findNameLine could then only look at\nindex 0, never reached the real name, and the seeder fell back to replacing\nline 0 and left the original name below it, reproducing the duplicated header\nthis branch exists to remove.\n\nIt also diverged from the parser fix on this same branch, which accepts the\nfirst line with content after any number of leading blanks, so the two halves\nof one fix disagreed about where the header starts.\n\nThe block is now the first run of content lines. The no-match insert branch\nwas anchored on line 0 for the same reason and would have spliced the contact\nline into a run of blanks, so it follows the same content start. Both reduce\nto the previous expressions when nothing leads the text.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* test: assert the unmappable row survives and collapse the duplicated fixture\n\nThe skip-an-unmappable-row test was named for the row being logged and skipped\nrather than silently dropped, but asserted only on the mappable row and on\nuser_version, so a change that deleted the row would still have passed. It now\ncounts the row directly, since store.get cannot read it at all: its text column\nis BLOB storage class, which is what makes it unmappable in the first place.\n\nFive copies of the same corrupt-byte builder and its expected output collapse\ninto the helper pair ai_generations' tests already use. The large-payload\nbuilder in the failed-write test stays inline: it needs a much bigger payload\nto force the failure, so folding it in would lose what that test is for.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* docs: point at the header seeder instead of restating how it works\n\nBoth passages had restated seedHeaderFromProfile's matching and scan mechanics,\nand had already drifted in the round that wrote them: they described the name\ncomparison as casing and punctuation insensitive while nameKey also folds\ndiacritics, which is precisely the case that was broken. The ownership\ndecision, the never-remove-or-blind-overwrite invariant and the accepted\nceiling stay, since those are the decision; the mechanics now point at the\nfunction that holds them. The landing bullet drops the framework version and\nexport mode for the same reason.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: keep the first content line out of the contact overwrite scan\n\nIndex 0 was excluded from the scan because a combined first line such as\nJane Doe | jane@example.com has no separate name line, so overwriting it\nerases the name outright. Moving the header block start to the first content\nline left that exclusion pinned to index 0, so behind two or more leading\nblanks the line it was written to protect became a candidate again and was\nblind-overwritten.\n\nThe scan now skips the first content line the same way it skips the\nreconciled name index. The two usually coincide but not always, so both are\nchecked independently.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: evict the embedding derived from the corrupt text, not just the flag\n\nThe repair set indexed = 0 to force a re-embed, but nothing reads that column\nwhen deciding what to re-embed. stale_documents asks only whether a vector\nexists in the active embedding space, so a repaired document kept its old\nvector, that vector still matched, and the embedding stayed derived from text\nthe document no longer contains. The flag was the wrong lever and it was mine.\n\nThe migration now deletes the vectors row for every id it repairs, which is\nwhat actually makes the document stale, and the comment names stale_documents\nas the consumer so the next reader does not repeat it. insert does the same\nwhen the repair actually changed the text, and import skips restoring the\nbundle's own vector in that case rather than cleaning up after it, since the\nrestore would otherwise land after the delete. A document whose text was\nuntouched keeps its embedding.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-07T17:43:00+02:00",
          "tree_id": "b6dc1dd29fcd9fc5742937795ebce788f30a05a1",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/94d65b35746dcab83aba69ca3eab372fb36b20ac"
        },
        "date": 1786119133147,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2150746,
            "range": "± 15175",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2518621,
            "range": "± 20586",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 294597,
            "range": "± 9080",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "09d9d0f2492938665d6f47bdc1793e7fc361fe2b",
          "message": "fix: stop the resume prompt licensing invented metrics, and let each provider own its sampling (#958)\n\n* fix: stop the resume prompt licensing invented metrics\n\nThe full-tier resume prompt said never fabricate numbers, only use metrics if\nthey are in the original OR CAN BE REASONABLY INFERRED, two lines after\ndemanding every bullet carry a measurable result, and ats mode adds quantify\nevery achievement. Three other rules in the same prompt ban exactly what that\nclause permits. The brief tier and the Rust port of the same spine both get it\nright, so the full tier, the one cloud users hit, was the outlier. A resume\nwith an invented number is one the candidate has to defend in an interview.\n\nThe analyze prompt fought itself the same way: synonyms fail 70% of the time\nand hard skills must match EXACTLY, then a hundred and fifty lines later\nReact.js equals React equals ReactJS, while the user prompt and the Rust\nsynonym map both implement semantic matching. That decides missingKeywords and\nkeywordCoverage, which the user sees. The ATS score was also decomposed two\nincompatible ways in one system prompt.\n\nAlso removes about five hundred tokens instructing a weighted overall score\nthat has no field in the schema, a dangling CANDIDATE RESUME header emitted\nafter the resume it announces, a leaked topRequirements variable name, and a\nduplicate point budget. The German subject label now goes through the output\nlanguage fitter like the salutation and sign-off already did, the format\nskeleton gains the subject slot it mandates, and the company research sentences\nare gated on a brief actually being fenced.\n\nPrompt size was measured, not assumed: resume 4563 tokens, analyze 5684. Size\nwas never the problem and nothing was rewritten for it.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: let each provider own its sampling values instead of the renderer\n\nThe renderer picked a temperature per task step and sent it to whatever model\nhappened to be configured. That is wrong on most of them now. Non-default\ntemperature, top_p or top_k returns 400 on every request to Claude 4.7 and\nlater, thinking or not. OpenAI reasoning models reject temperature. Google says\nto remove the parameters from Gemini 3 requests entirely and documents that a\ntemperature below 1.0 causes looping. Six of eight families publish one number\nper model with no task dimension at all, so the per-step table was tuning an\naxis no vendor supports.\n\nGemini already had a guard for this and it was dead code. It withheld the\nadapter's own fallback only when the caller supplied nothing, but the renderer\nalways supplied something, so every request looked like a deliberate user\nchoice. The onboarding default is a Gemini 3 model, so new users hit the\ndocumented degradation case on their first generation.\n\nThe renderer now sends an intent and each adapter owns the numbers, behind a\ntrait method whose default sends nothing at all. Omitting is correct or better\non Claude, OpenAI reasoning models, Gemini 3, native Ollama, which inherits the\nmodelfile, and vLLM gateways, which inherit generation_config. An unknown model\non an unknown provider therefore falls through to that provider's own defaults,\nso a new model still needs no code. Anthropic needed no change: its fail-safe\nwas already right, and the work was generalising it to the other four adapters.\n\nExplicit values still win, so the per-model temperature sliders keep working.\nprose_grounded keeps presence_penalty off the surfaces that write factual\nclaims about the candidate to a third party, since that penalty pushes toward\nnew topics. Ollama Cloud is special-cased because its openai-compatible layer\nhardcodes 1.0 when a value is omitted, overriding the modelfile.\n\nAlso routes the job analysis and the inline rewrite through the resolver. Both\nused bare literals, so the analysis slider changed metadata extraction and not\nthe analysis it is named for.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: keep the deterministic sampling the providers actually accept\n\nThe first cut of this change defaulted every adapter to sending nothing, on\nthe theory that omitting a value is neutral. It is not. Omitting means the\nmodel applies its own default, which is 0.7 to 1.0, so the analyze surface\nwent from 0.1 to 1.0 on most providers while its prompt is a strict JSON\ncontract that hard-throws on a parse failure, and the grounded prose surfaces\nwent from 0.5 to 1.0 while their whole purpose is staying traceable to the\nresume. Measured on this machine, the local models carry temperature 1 and\npresence_penalty 1.5 in their own modelfiles, so on the default install the\nresume surface would have run hotter and with the exact penalty that pushes a\nmodel toward inventing facts.\n\nOmission is right only where sending is refused or documented as harmful:\nClaude 4.7 and later, OpenAI reasoning models, Gemini 3, and any model an\nadapter cannot classify. Everywhere else each adapter now declares the values\nthat reproduce today's behaviour. Anthropic and Ollama were reading the\nrequest directly and never consulted their own profile, which is what kept\nAnthropic's hardcoded fallback invisible; both now resolve through it.\n\nThe cover letter moves to the grounded profile. Calling it creative was an\nargument about temperature, not about a penalty that rewards new topics in a\nletter asserting real achievements to an employer. Inline rewrite goes back to\ndeterministic, since a span edit should not inherit a prose profile. The\nopenai-compatible adapter no longer hands OpenAI's numbers to arbitrary\ngateways.\n\nTests now assert the request body per adapter and per intent, including the\nabsences, because the previous round asserted the intent string and every one\nof these defects passed. The intent vocabulary is generated from the shared\nschema so a renamed literal cannot silently degrade every request to the\ndefault. Settings no longer claims app defaults apply when the modelfile does.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* build: track the generated ai intents contract\n\npub mod ai_intents in ipc_contracts/mod.rs referenced a file that was never\nadded, so the crate compiled here and would not on a fresh checkout. Staging\nby path rather than wholesale is the right habit, but it does not pick up a\nnew file on its own.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: gate the technology requirement on the resume actually naming one\n\nThe bullet formula was fixed for metrics and left mandatory for technology in\nthe same line, so a bullet whose original names no tool, mentored three junior\nengineers, still had to name one. That is the fabrication class this branch\nexists to close, one token to the left of where it was closed. Now conditional\nat all five sites, including a task-tier copy nobody had listed, with wording\nshared so they cannot drift apart again, and the duplicate formula line is\ngone.\n\nOne temperature slider was driving three intents. The answers key covered\napplication answers, the job ad summary and every interview surface, so\nenabling it retuned deterministic summarisation and prose questions too. Each\nkey now maps to exactly one intent, with a new questions key, and the tests\nassert that a sibling override does not leak, which is what would have caught\nit. Slider labels name the surfaces they affect and the automatic mode copy no\nlonger promises modelfile behaviour that only some providers get.\n\nThe grounded prose temperature is higher than the ungrounded one, which reads\nbackwards, so its doc comment now explains that grounding comes from\nwithholding presence_penalty rather than from cooling the sampler, and that the\nordering is an accident of which surfaces carry which intent. The test that\nclaimed the opposite is renamed.\n\nAPI.md documented a GenerateRequest that does not exist, with fields the real\nschema never had, and returned a generationId where the contract returns a\njobId. Replaced with a pointer to the schema that owns it.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: keep the deterministic temperature on openai-compatible gateways\n\nLast round I told the author to gate the openai profile on the native provider\nid, after a reviewer objected that handing our numbers to arbitrary gateways\ncontradicted the unknown-model fail-safe. That over-corrected. LM Studio,\nvLLM, OpenRouter and any custom endpoint fell to a fully neutral profile, so\nthe analyze surface lost the low temperature it had before this branch, on a\nprompt that is a strict JSON contract and a caller that throws on a parse\nfailure.\n\nThe distinction I missed is that deterministic encodes an app requirement, not\na model preference. The fail-safe is about not guessing what creative sampling\nan unknown model wants; it was never a reason to let a JSON contract run hot.\nCompatible gateways now resolve the same profile as native OpenAI, behind the\nsame unchanged reasoning-model gate, and the doc comment records that the id\ngate was tried and reverted so the objection does not get re-raised and\nflipped back.\n\nAlso covers generateReferralImprove, which changed intent alongside its\nsibling and had no test.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-07T22:40:39+02:00",
          "tree_id": "98bf1f5d494ca1295227ef16830631706fcd5f2d",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/09d9d0f2492938665d6f47bdc1793e7fc361fe2b"
        },
        "date": 1786136687775,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2194989,
            "range": "± 33295",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2701396,
            "range": "± 53537",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 317523,
            "range": "± 14360",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "c1de59f9d6da6a3b367654d8dc326b01b2aa541a",
          "message": "fix: make a failed generation visible instead of silently resetting (#959)\n\n* fix: apply the deterministic profile to non-gemini-prefixed google models\n\nThe pre-v3 gate additionally required a gemini- prefix, so a gemma or learnlm\nid got a fully neutral profile while every other gate in the same file keys\nonly on the v3 boundary and would have applied the deterministic default. The\nmodel listing filters on the models/ prefix and not on family, so those ids do\nreach the picker, and before #958 they received the low temperature the\nanalyze surface needs. The gate is now the exact inversion of the v3 check.\n\nThis is the third time an unclassifiable-means-neutral gate cost a\ndeterministic surface its temperature, after the openai-compatible one in\n89435a47, so the doc comment records that the prefix requirement was tried and\ndropped and why. That fail-safe is for declining to guess an unknown model's\ncreative sampling; it was never a reason to let a strict json contract run at\nwhatever the server defaults to.\n\nConfirmed while here that the v3 check cannot misclassify a gemma id: it\nrequires the literal prefix before it parses any version digits.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: tell the user when a tailored generation fails\n\nThe stage is derived as generating, then done if there is output, else\nconfiguring. A failed run leaves both outputs empty, so it fell back to the\nwizard, and the file had no reference to the error at all: runTailor wrote it\nto the session, the hook returned it, and nothing rendered it. The catch only\nlogged to the console, so there was no toast either. The result was a\ngeneration that appeared to do nothing, which cost two investigations to\nreproduce.\n\nReturning to the wizard is the right behaviour, since the user needs to change\nsomething and try again, but it has to arrive with the reason. The error now\nrenders below the wizard where the user just clicked Generate, and a toast\nfires, matching what the AI Generate wizard already did.\n\nThe test drives a real failure through the hook and asserts the rendered text\nrather than the store field, because a test that only checked the field was\nset would have passed against the broken code.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: scale the stream deadline by effort and flag a truncated generation\n\nThe 300 second deadline was fixed on both sides and reqwest bounds the whole\nrequest, connect through the last streamed byte, so a generation that\nlegitimately runs longer was killed mid stream. High effort on a large cloud\nmodel with a full resume and job ad sits squarely in that range, which is the\nlikely throw behind a failure that then vanished into the silent reset. Both\nsides now scale by the request's effort, and the renderer stays the outer\nbound so the backend's actionable provider error fires first rather than a\ngeneric timeout.\n\nThe tables cannot be shared across the IPC boundary, so a test hardcodes the\nbackend schedule and asserts the renderer exceeds it at every tier. The tier\norder is the vendors', where max sits above xhigh, which reads wrong at a\nglance and was inverted here at first: max got a shorter deadline than xhigh,\nso the highest effort runs got the tightest bound, which is the exact failure\nthis exists to prevent. The monotonicity tests listed the tiers in the same\nwrong order, so they passed against it; only the value assertions caught it.\nBoth orders are now pinned with a comment saying why.\n\nfinish also consulted the stop reason only when the answer was empty, so a\nstream that produced text and then reported length took the success path and a\ntruncated resume was saved and exported as complete. It still completes and\nstill keeps the partial text, since that text is worth having, but it now\nraises a notification through the existing centre rather than presenting a cut\noff document as finished.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* refactor: generate the stream deadline schedule from one source\n\nThe renderer test hardcoded the backend's baseline and effort multipliers, so\na change to either Rust constant alone would have left it passing while the\nrenderer timeout began firing before the backend deadline. That is the same\nshape as the inversion this branch already fixed, where both monotonicity\ntests listed the tiers in the wrong order and passed against it, so this table\nhas now demonstrably drifted once already.\n\nThe baseline and the multipliers live in the shared package and generate into\nthe rust contract, the way the intent vocabulary already does. Both sides\nimport the same numbers, gen:ipc:check fails on drift, and the test is left\nasserting the one thing that is still a real relationship rather than\nre-typing the other side's constants. The tier order prose moves with the\ntable, since it is the only thing standing between the next editor and the\nsame inversion.\n\nThe gemini test now covers learnlm alongside gemma. It does not restore the\nneutrality assertion the review asked for: that contract was removed\ndeliberately, because it was what withheld a temperature from those models on\na strict json surface.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: redact a generation error before it reaches the screen\n\nShowing the failure reason is the point of this branch, but the raw provider\nmessage was reaching the UI unredacted, and this repo has already leaked a\nbase_url's query string auth into an error that way. The existing redactor was\nwired into crash reporting and the diagnostics zip and never into the AI path.\n\nThe review asked for a fixed string with the real text kept in the console.\nThat would undo the change: the user was getting nothing, which is what made a\nreal failure take two investigations to reproduce, and a generic failed is\nbarely better than the silent reset it replaced. Redacting at the boundary\nkeeps both properties, so the reason survives and credentials, paths, hosts\nand emails do not.\n\nemit_stream_error is the single choke point. Neither generate command lets the\ninvoke itself reject; both route their error branch through it, and every\nother renderer failure path already uses a fixed string. A test pins that an\nordinary provider error such as 429 Too Many Requests passes through byte for\nbyte, so the next person cannot answer this by flattening everything and\ncalling it security.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-08T08:57:07+02:00",
          "tree_id": "b341742f7072d28eb5793929f055c842bbb4b8e8",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/c1de59f9d6da6a3b367654d8dc326b01b2aa541a"
        },
        "date": 1786172878869,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2112104,
            "range": "± 24360",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2534637,
            "range": "± 61334",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 289367,
            "range": "± 10404",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "d367bccacdde1b446cc6fb048b18db1dd0eb9fc7",
          "message": "fix: generation-burst reliability and the silent defects from the 2026-08-08 diagnostics bundle (#960)\n\n* fix: stop cover letters being addressed to job-ad headings\n\nSix consecutive generations in a support bundle researched, and then addressed a\ncover letter to, a fragment of the ad instead of the employer: \"You'll Do\" (x3),\n\"experience **fast\", \"the core of their product. You'll sit at the intersection\nof design\", and \"a **bootstrapped AI platform and consulting studio** based in\nMunich\".\n\nTwo causes, both fixed:\n\n- The fallback regex alternated on a bare `at`, with no word boundary, so it\n  matched inside \"Wh(at) You'll Do\" - a near-universal JD heading - and captured\n  from mid-word to the next comma. Same defect on `job` inside \"jobs\". Both\n  alternations are now `\\b`-anchored.\n- Neither source of companyName was validated. The model returns an ad heading\n  as the employer often enough that `parsed.companyName ?? ''` is not safe, and\n  the regex is worse still. `sanitizeCompanyName` now gates both, rejecting\n  markdown, sentence breaks, pronoun contractions, mid-sentence fragments,\n  over-long values and a heading denylist. Rejection yields '', which\n  CompanyResearch::enrich_with already treats as \"skip research\".\n\nThe bare `catch {}` that hid the fallback is now a warn on both the throw and\nthe unparseable-JSON path, so the log shows which extractor produced the name.\n\nTests use the verbatim strings from the bundle, plus the correctly-extracted\ncompanies from the same session as the must-not-regress set.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: enforce the generation concurrency cap on the path the app uses\n\n`AI_GENERATE_CONCURRENCY_MAX = 3` was enforced on `ai_generate`, but the\ntailoring flow calls `generate_pipeline`, which acquired nothing: no rate\nwindow, no concurrency slot, and no per-provider daily charge. A support bundle\nshows the result - six user-initiated runs producing SEVEN concurrent streams,\nending in a provider 429 that discarded nine minutes of already-generated work.\n\n`generate_pipeline` now charges the daily ceiling at admission and takes a\nconcurrency slot inside its spawned task, so the slot is held for the whole\nstream and released on drop.\n\nIt uses the new `Limiter::acquire_queued` rather than `acquire`: every call is a\ndeliberate click on Generate, so the 4th waits its turn instead of being thrown\naway. Both admission styles draw on ONE per-command semaphore - two independent\nmechanisms would have reproduced the same \"cap enforced somewhere else\" bug.\nThe queue is depth-bounded, so it cannot itself become the unbounded resource.\n\nRenderer: `awaitAiStream` re-arms its deadline while `jobs.get` reports\n`queued`. That deadline bounds the stream, not the wait for a slot; without\nthis a queued generation fails on a timeout it never had a chance to beat,\nhaving never sent a request. It still fires normally once the job is running,\nso a genuinely stuck job is still caught.\n\nBoth release paths are mutation-tested: neutering `QueueSlot::drop` fails the\ncancel-safety test, leaking a permit fails four, and dropping the re-arm fails\nthe queued-deadline test.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: scale the company-research deadline with reasoning effort\n\nResearch was bounded by a flat 25s around search AND synthesis. Synthesis is a\nmodel call, so that bound was never about the network - it was a cap on how much\nreasoning the chosen model was allowed to do. In one support bundle all six\nresearch calls of a gpt-oss:20b batch timed out, and all six cover letters were\nwritten with no company knowledge and no visible failure.\n\n25s was also barely above the 25s WEB_SEARCH bound it wraps, so the outer\ndeadline fired first and the actionable inner error never surfaced.\n\n`research_deadline(effort)` now scales a 90s baseline by the same generated\nEFFORT_TIMEOUT_MULTIPLIER table `stream_deadline` uses - one table, so a new\neffort tier is picked up for free. Both research enrichers take the deadline as\nan INJECTED argument rather than reaching up into `commands` for it, matching\nhow `SalaryResearch::enrich` already takes its cache; the architecture test\ncaught the upward import and was right to.\n\n`salary_research` carried an identical copy of the constant and the same bug;\nit is gone, and its timeout test now derives its sleep from the injected bound\nso raising the baseline can never silently turn that test into a no-op.\n\nAlso extracts the ~40-line preamble the three `ai_research` commands each\nrepeated verbatim (acquire, resolve, charge - in that order, so a rejected call\ncosts no budget) into `admit_research`, which is what buys back the LOC the new\nargument needed.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: make the diagnostics bundle readable\n\nThree changes, all aimed at the same thing: a support bundle from a session with\nsix failed/degraded generations contained exactly ONE `[ERROR]` line, and 98.5%\nof it was a single repeated warning.\n\n- `Span::end`/`end_with` logged every terminal line at INFO regardless of\n  outcome, so eight silently-failed provider calls sat at the same level as the\n  successes. `ok=false` now logs at WARN, via one shared `log_end` so the two\n  entry points cannot drift.\n\n- `[ai] →` and `[ai] ←` carried nothing to pair them. With seven concurrent\n  streams the log is a shuffled interleaving, and matching an end to its start\n  meant subtracting each reported `duration` from its timestamp by hand.\n  `RequestTrace` now stamps a process-local `req=N` on both lines - a counter\n  rather than a job id, since research, embeddings and model listing have no job\n  of their own - and `stream start`/`stream end` carry `job=`, which they always\n  had in scope.\n\n- 27,178 of 27,601 lines were Tauri's own \"Couldn't find callback id\", one per\n  event dispatched to a stale listener, i.e. one per streamed delta. It comes\n  from Tauri internals so there is no call site to fix, and `tauri_plugin_log`'s\n  filter only sees level + target, never the message. Dropped in the renderer\n  bridge instead, which also saves an IPC round-trip per token. The first\n  occurrence per session still forwards - the condition is worth knowing about -\n  and devtools output is untouched.\n\n`RequestTrace` moves to its own module: `ai_provider/mod.rs` was at its R8 LOC\ncap, and per-request tracing is self-contained. Its path is re-exported, so no\ncall site moves.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: stop sending an embedding model to ollama's chat endpoint\n\nEvery HTTP 400 in a reported support bundle - eight of them - was\n`provider=ollama model=qwen3-embedding:8b endpoint=/api/chat`, one per scraped\njob ad. An embedding model on the chat endpoint is unconditionally rejected.\n\nThe chain: `translate_text` (job-ad translation, which runs on every scrape)\nreads the EMBEDDING config to find a local provider, then asks\n`reachable_chat_model` for a model. That forwarded `ollama::reachable_model`,\nwhich returned `/api/tags`'s first entry unconditionally - it was written as a\nhealth probe (\"is Ollama up, and does it have anything\") and reused as if it\nanswered \"give me a model that can chat\". A user whose first entry is an\nembedding model gets a guaranteed 400 on every ad.\n\n`first_chat_model` now skips embedding-only models and returns None when the\nuser has nothing else installed, so the caller skips translation instead of\nissuing a request the daemon will always reject. The filter is a name heuristic\nbecause `/api/tags` carries no capability field - `details.family` is `bert` for\nnomic/mxbai but the base family for qwen3-embedding - and the ceiling plus its\nupgrade path (`/api/show` capabilities) is written at the function.\n\nIt was also invisible: `translate_text` returns the untranslated original on any\nerror, so the only trace was an `ok=false` span at INFO. Both fallback paths now\nwarn, which is how this would have been found from the bundle alone.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: retry a stream's handshake on a transient 429 instead of losing the run\n\nThe 429 that killed a generation in a reported session was never retried. The\nretry module was wired only into the one-shot complete/embed calls, on the\nreasoning that \"a mid-stream restart would duplicate already-emitted deltas\".\n\nThat reasoning does not cover the case it excluded. A non-2xx STATUS arrives\nfrom the request/response handshake, before a single delta has been read, so a\nretry there re-sends a request that emitted nothing. There is no output to\nduplicate. Treating it as terminal is what turned one provider rate-limit into a\ndiscarded nine-minute generation.\n\nAll four streaming adapters now send through `send_stream_with_retry`: the same\nbounded loop, with a higher backoff ceiling (30s vs 8s) because the alternative\nto waiting is throwing away work the user has already waited minutes for. A\ngenuinely terminal 4xx is still not retried, the attempt budget is unchanged,\nand nothing restarts a stream that has already produced output.\n\nRetries also log at WARN now - a provider pushing back is exactly the context\nyou want when a generation later fails outright, and at DEBUG it never reached\nthe log file.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: stop researching apply buttons and nav links as the job role\n\nA reported session searched the provider's web index for role=\"Jetzt bewerben\"\n(an apply button), role=\"[← Alle offenen Stellen](/karriere)\" (a nav link) and\nrole=\"Please note: Fluent Dutch language skills are required for this role.\"\n\nThe role heuristic's last resort is \"the ad's first non-empty short line\", which\non a scraped page is chrome. Two changes:\n\n- The AI-extracted `jobTitle` is now threaded as a `role` override exactly like\n  `company` already was, so the heuristic is a fallback rather than the source.\n- Both extracted fields pass a `sanitized` gate that drops markup, page chrome\n  (multilingual apply/nav labels) and prose. Empty means \"nothing usable\", which\n  every caller already handles by skipping research. A wrong subject is worse\n  than none: it returns a confident brief about the wrong thing.\n\nThe same commit fixes the Rust twin of the bug already fixed on the renderer\nside: `extract_company`'s `\"at \"` prefix was matched anywhere in the line, so it\nhit inside \"Wh(at )You'll Do\" — the same near-universal heading — and produced a\ncompany of \"You'll Do\". Both sides now require a word boundary. This gate also\nmatters more than it did: `sanitizeCompanyName` empties the override more often\nnow, so the Rust fallback runs more often.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: make an unpairable extension and a panic diagnosable instead of noisy\n\nTwo independent findings from the same support bundle.\n\nThe extension bridge wrote TWO warnings per rejected handshake — 1,932\nrejections plus 1,932 paired failures from one build whose origin is not\nallowlisted. The retry loop causing them is NOT a client bug to fix: a browser\ndoes not expose a failed WebSocket handshake's HTTP status to script, so the\nextension genuinely cannot tell \"refused my origin\" from \"desktop not running\",\nand its ~10s reconnect is the right behaviour for the latter. The logging was\nthe problem. The origin is now reported once per origin per session, with an\nactionable message naming `extensionDevOrigins`, and the paired line drops to\ndebug when the error is the 403 this module itself returned.\n\n`crashes.log` held two PANIC entries that stopped dead after \"Backtrace:\" — no\nframes, and not even the `---` terminator — so neither crash could be\ndiagnosed. Redaction was the obvious suspect and is NOT the cause: a test now\npins that a backtrace survives it with only its paths removed. The missing\nterminator points at a partial write instead, which is exactly what to expect\nfrom a panic that fires during shutdown. The hook now renders the whole entry to\na String and writes it in one call, and an empty capture says so explicitly\nrather than looking identical to a truncated one.\n\nThe panic itself (\"cannot move state from Destroyed\", from the windowing layer\nduring teardown, five seconds after a WebView2 \"Class not registered\") is NOT\nfixed here - it is third-party shutdown behaviour I could not reproduce, and a\nspeculative guard would be worse than the next real backtrace.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* feat: tell the user a reasoning-heavy run is working, not stuck\n\nA gpt-oss:20b batch took 8-13 minutes per document where a non-reasoning model\ndid the same work in 12-23 seconds - 1,039,821 characters of internal reasoning\nagainst 45,416 of answer. Throughout, the panel showed the same \"this can take a\nmoment\" caption, so the run was indistinguishable from a hang.\n\nThe reasoning stream is already on screen, so the signal costs nothing to\ncompute: once it passes a floor AND dwarfs the document, the caption switches to\nsay the model is reasoning heavily and to expect minutes. Measured live rather\nthan from a per-model table, which would go stale with every model release; the\nratio is the thing that matters anyway.\n\nThe floor and the ratio are both load-bearing and both tested: without the floor\nevery reasoning run flashes the hint for a second at the start (reasoning\nstreams before the document does) and the user learns to ignore it; without the\nratio every long run shows it, including ones producing a document the whole\ntime. Removing the ratio check fails a test.\n\nUpgrade path if this needs to be known BEFORE a run starts, rather than during:\npersist thinking tokens alongside output_tokens in the spend store.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: address review findings and the cargo-fmt ci failure\n\nCI failed on `cargo fmt --check`. I pushed with `--no-verify`, which skipped the\nhook that would have caught it, and I never ran the formatter myself - both\n`cargo fmt --check` and `prettier --check` are now clean and are part of the\nverification list going forward.\n\nReview findings, in severity order:\n\n- The limiter's rate check and window record were no longer atomic. Splitting\n  `acquire_at` into check + record let two callers both observe room in the\n  window and both take it. Admission is one lock hold again; the \"a rejected\n  call costs no window slot\" property now rides on `undo_admission` refunding\n  the slot when the concurrency or queue admission that follows fails, with a\n  test for each rejection path. Neutering the refund fails both.\n\n- Stream handshake retries had no total budget. Each attempt rebuilt its own\n  `.timeout(stream_deadline(..))`, so three attempts could run past the\n  renderer's deadline and replace the actionable provider error with a generic\n  \"Generation timed out\" - inverting the relationship `computeStreamTimeoutMs`'s\n  test deliberately pins. The caller's deadline now bounds the whole sequence.\n  Notably this makes the reported failure a no-op: that 429 arrived after the\n  full deadline had elapsed, so it retries zero times.\n\n- `jobTitle` was left ungated while `companyName` was gated - the same defect I\n  had just fixed, half-applied. Both now go through one `sanitizeSubject`, on\n  both the model and heuristic paths. The title is the other half of the subject\n  a provider web search runs on.\n\n- The rejected-origin set was unbounded and keyed on an attacker-supplied\n  header. Capped at 64.\n\n- The trace-id test asserted a delta on a process-global counter, which a\n  parallel test could break while uniqueness still held. It asserts uniqueness.\n\n- The queued activity-feed text was hardcoded English (rule 2). Routed through\n  `@ajh/translations` with the count interpolated, en + de.\n\n- The queued-deadline test ran on real timers with a 10ms margin. Widened to\n  60ms with 6x25ms of queueing, so it still proves the re-arm without depending\n  on CI scheduling luck.\n\n- Added boundary cases for the sanitizer's 60-char/6-word caps and the\n  reasoning-heavy 8,000-char/5:1 thresholds, which carried real rejection load\n  with nothing pinning them.\n\nNot changed: `base_url` in the trace fields was flagged as a secret-leak risk,\nbut it predates this PR (it is on origin/main) and the diagnostics bundle\nalready redacts it to `<url-redacted>` on export. Worth its own change, not a\nsilent widening of this one.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: charge the daily provider budget after admission, not before\n\n`generate_pipeline` charged `PROVIDER_DAILY_MAX` before `acquire_queued`, so a\ncall the rate window or the queue ceiling then rejected still burned a day's\nquota without ever reaching a provider. `ai_generate` and `admit_research` both\nacquire first and charge second, precisely so a rejected call costs no budget;\nthis was the one site that inverted it.\n\nThe comment I used to justify the old order was wrong, which is worth recording:\nit claimed charging after the wait would let an \"unbounded number of runs past\nthe ceiling while they park\". The queue is bounded - at most\n`AI_GENERATE_CONCURRENCY_MAX + AI_GENERATE_QUEUE_MAX` (23) calls can be\nadmitted-but-uncharged at once, against a ceiling of 4,000. That overshoot is\nbounded and irrelevant; the quota leak it was trading against was not.\n\nNo direct test: the ordering lives in a `#[tauri::command]` that needs a live\nAppHandle, the same reason `ai_generate`'s identical ordering is untested. All\nfour charge sites are now acquire-then-charge.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: give job titles their own size caps instead of the company's\n\n`sanitizeJobTitle` checked 80 chars and then delegated to a gate that checked 60,\nso the larger ceiling was dead code: every title over 60 chars was still dropped,\nand the company's 6-word cap rejected any 7-word title. Verified before fixing -\n\"Senior Staff Software Engineer for Platform Infrastructure Group\" (64 chars, 9\nwords) and \"Senior Staff Software Engineer Platform Infrastructure Lead\" (59\nchars, 8 words) both returned \"\".\n\nThe doc comment claiming titles were \"slightly more permissive on length\" was\ntherefore false, and worse than the code: a reader would have trusted it.\n\nSize limits are now a per-caller argument - 60/6 for a company, 80/10 for a\ntitle - while both keep the identical SHAPE rules (markdown, prose, contractions,\nheadings), which is the part that must not diverge.\n\nWhy the original tests missed it: neither case sat in the 61-80 char band or\npast 6 words. `'A'.repeat(81)` was rejected for the wrong reason (81 > 60, not\n81 > 80) and the one \"long\" title I picked was 54 chars / 6 words - exactly\ninside both company caps. The new cases are all in the band that was broken, and\nreintroducing the old shared-limits call fails four of them.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-09T12:21:55+02:00",
          "tree_id": "f2c5e17738fde9b107195d2f33cf3e0e61d73d14",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/d367bccacdde1b446cc6fb048b18db1dd0eb9fc7"
        },
        "date": 1786272000189,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1447453,
            "range": "± 43073",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 1685306,
            "range": "± 57461",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 169735,
            "range": "± 8357",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "cc73af992b74eeeeb2ed9378edeb646088997dd6",
          "message": "feat: add exa as a fallback web-search backend for company research (#961)\n\n* feat: add a pluggable web-search backend with exa as the fallback\n\nCompany research runs on the active AI provider's own web search, which leaves a\nwhole population with nothing: local Ollama without an ollama.com account key,\nevery `openai-compatible` gateway, and any provider whose search isn't wired up.\nThey get a silently empty brief, and the cover letter is written with no company\nknowledge - indistinguishable from one written with a full brief.\n\nThe seam was already there. `research.rs` is provider-agnostic (SearchResult,\nthe query builders, the synth prompts, the injection guard), and the three\n`ollama_research*` functions were each \"search -> if empty return '' ->\nsynthesize with the provider's own model\". Only the search step was ever\nOllama-specific.\n\nSo this extracts that step behind a `WebSearcher` trait, moves the three\npipelines to `search/` as `searched_research*`, and adds `ExaSearcher`\n(api.exa.ai/search, `ai:exa` key). All three research facets - company, salary,\nper-question answers - gain the fallback at once.\n\nSelection is FALLBACK-ONLY and decided from configuration before any call: a\nprovider that can already search keeps using its own, even with an Exa key\nstored, so nobody silently starts paying a second vendor. A native search that\nruns and returns empty is NOT retried against Exa - one pass, one search, one\nvendor sees the query. `resolve_search_backend` is a pure predicate so that\npolicy is testable, and inverting it fails a test.\n\nSynthesis deliberately stays on the user's own model. Exa's own answer endpoint\nwould be fewer calls but would bypass both `SYNTH_SYSTEM`'s prompt-injection\nguard (search results are attacker-reachable text) and the `is_no_info` filter.\n\nThe fallback lands on the trait's DEFAULT `research()`, so providers with native\nsearch override it and are untouched, and a new provider with no search inherits\nit for free - no per-provider change either way.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* feat: report whether research can actually run, and add the exa key ui\n\n`supportsWebSearch` reported what a provider ADVERTISES. Local Ollama advertises\nweb search for the family, so a keyless install showed the \"search company\"\ntoggle ON and returned an empty brief every single time - a state the user\ncannot act on, because nothing told them a key was missing.\n\nIt now answers whether a search backend is CONFIGURED: the provider's own model\nsearches, or its searcher has a key, or a fallback backend does. Keyless Ollama\nreads false, which is the truth.\n\nThree parts:\n\n- `search::research_available` is the single source, used by both the renderer\n  probe and the server-side per-question gate. That gate previously read the\n  static capability, so it would have skipped (and mis-charged) a keyless\n  install that has a working fallback - the trait method is now\n  `research_available()`, which is also what the test fakes assert on, so the\n  two can't drift.\n\n- `ai_model_capabilities` takes `AppHandle` to read stored keys. The renderer\n  signature is unchanged (Tauri injects it), but the key mutations now\n  invalidate the capabilities query: it caches with a VERY_LONG staleTime, so\n  without that, adding a key left the toggle wrong until restart.\n\n- Settings → Company Research grows a second key slot. The existing field was\n  hardcoded to `ollama-cloud`; the two differ only in labels and credential\n  slot, so it is parameterised rather than copied. Both go through the existing\n  key IPC, which already accepts an arbitrary slot name - no new commands.\n\nThe section copy states the fallback-only rule explicitly, so a user adding an\nExa key knows it will not take over from a provider that already searches.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* docs: record the search-backend axis and refresh the stale limit docs\n\nADR-0023 records why web search is a separate axis from generation, and the three\nsub-decisions that each had a real alternative: fallback-only (not Exa-preferred),\nno second chance on an empty native search, and synthesis staying on the user's\nown model rather than using Exa's answer endpoint - which would bypass both the\nprompt-injection guard and the no-info filter.\n\nADR-0005 needs no amendment and the ADR says so explicitly: Exa is a new vendor\ninside the already-enumerated class 3 (web search, opt-in), not a new egress\nclass, and it receives exactly what the current search already does. Its\nconsequence - keep the enumeration accurate - is honoured in README and\nSECURITY, which now name Exa within that class.\n\n`docs/CONTEXT.md` gains **search backend**, the term this feature creates: it\nretrieves, it cannot generate, and it is not an AI provider. Without it, \"search\nprovider\" would have drifted into use for something deliberately absent from\n`ProviderId` and the provider picker.\n\nTwo fixes to `anti-abuse-limits.md` that were already stale, one of them my own:\n\n- The concurrency guard stopped being a counter in #960 and became a semaphore\n  permit with two admission styles (reject vs wait), and I did not update the\n  doc then.\n- The daily ceiling is keyed by VENDOR, not by `ProviderId` - which is what lets\n  a search backend charge its own bucket instead of spending the AI provider's.\n\nAlso replaced two line-number references with symbol names; they had already\ndrifted and would drift again.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n* fix: route research in one place so gateways actually reach the fallback\n\nThe AI review caught a HIGH that undoes half this PR's purpose, and it was\nright. `OpenAiClient` overrides research()/research_salary()/research_answer()\nfor EVERY id it serves, and those call `web_search_complete`, which returns \"\"\nwhen `!supports_web_search()`. So an openai-compatible gateway never reached the\nnew default and got no research at all - while `research_available` now reported\ntrue for it, meaning the toggle would say ON and still produce nothing. That is\nprecisely the bug this PR set out to fix, reintroduced somewhere else.\n\nThe per-provider shape is what failed, so the fix is structural rather than\nthree more fall-throughs. `Completer` is the single caller of all three research\nmethods, so the decision lives there now: `has_native_search()` picks the native\ncall or the shared search-then-synthesize path, once, for every provider and\nevery facet. A provider can no longer forget to fall through, because it no\nlonger participates in the choice.\n\nThat let a lot go away. `AiProvider::research*` are back to meaning only \"the\nmodel searches for itself\", the Ollama family's six research overrides are gone\n(the router sends them down the shared path, where `native_searcher` picks the\nWeb Search API), and `needs_explicit_searcher` collapsed into `has_native_search`\n- one concept where there were two. Ollama Cloud gained the searcher hook it was\nsilently missing: it would have fallen through to Exa despite having its own.\n\nThree tests pin the routing per provider family, and dropping either the\n`has_native_search` override or the capability term fails them.\n\nAlso from the review: `OllamaSearcher` caches its key (three keychain reads per\nresearch pass became one) and honours `limit` rather than hardcoding 5 while\n`ExaSearcher` honoured it; `searcher_for` builds the Exa searcher once; the key\nfield and its icon-only reveal button gained accessible names; and the\ncapability probe's \"static, network-free\" claim is corrected in both the Rust\nand TS docs, since it now reads the keychain.\n\nTwo docs findings applied: `docs/**` must point at owning sources rather than\ncopy them, so the glossary entry drops the implementation detail and\nanti-abuse-limits.md drops the constant table that had already drifted.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 (1M context) <noreply@anthropic.com>",
          "timestamp": "2026-08-09T13:51:51+02:00",
          "tree_id": "770a1fe6b380ad6bce20d0f16c03978fb38d268f",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/cc73af992b74eeeeb2ed9378edeb646088997dd6"
        },
        "date": 1786276972034,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2161286,
            "range": "± 20782",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2578659,
            "range": "± 30350",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 294660,
            "range": "± 8923",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "c438eda00ed37b9ed174cba1bf39b1010ddf088e",
          "message": "feat: deterministic content validation and quality report for generated documents (#963)\n\n* feat: add evidence extraction module and move the bullet scorer out of match_resume\n\nrank_trim_candidates' core moves verbatim into documents::evidence::rank_bullets,\nwhich extract_evidence now reuses to structure the source resume into roles,\nprojects, education and an honest present/absent skill split for one posting.\n\nmatch_resume keeps a TrimCandidate: From<EvidenceBullet> shim so the\nmatch:trimSuggestions payload is wire-identical (three camelCase fields, score\nstill a JSON integer) and shrinks 1276 -> 1132 LOC.\n\nkeywords_normalized becomes a collect() over the new keywords_normalized_list so\noccurrence-counting consumers share one tokenizer instead of forking it.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat: add deterministic content validators for generated resumes and letters\n\nvalidate::content checks the text a model wrote against the candidate's own\nresume, the posting, and the target language, before anything is rendered. The\nrule it mechanically enforces: a model may never produce a Critical - every one\ncomes from a deterministic comparison against the source document.\n\n24 issue codes in one table (6 Critical, 18 Warning); the constructor reads the\nseverity from that table so a code and its severity cannot drift. Pure L1 - no\nTauri, no AppHandle, no IO - and one languages_align decision drives every\nresume-posting comparison, so a quality report can never contradict the match\nscore shown for the same pair.\n\nFalse positives are the risk this module is designed around: every threshold is\na named const with a boundary test, 1900-2099 is excluded from metric checks,\nthe contact band is skipped, an open-ended source span resolved to a concrete\nyear is not a fabricated date, and an unusable posting or a wrong-language\noutput silences the derived comparisons instead of cascading. Three fixtures\nassert a clean en resume, a clean de resume and a grounded letter produce\nliterally zero findings.\n\nlexicon.rs is a hand-stub transcribing the anti-AI-tell lists from\npackages/prompts natural-voice.ts; codegen replaces it behind the same\ninterface. Not implemented: the letter company-name check - ContentInput has no\ncompany field and the frozen code vocabulary has no code for it.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: point languages_align at its three real consumers\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(prompts): export ai-tell word lists and template openers\n\nAdds AI_TELL_LEXICAL_WORDS_*, AI_TELL_PROSE_WORDS_*, and TEMPLATE_OPENERS_*\narrays to natural-voice.ts as the discrete, codegen-consumable source of\ntruth behind the prose anti-AI-tell rulesets. The cover-letter prompt's\nopener-ban examples now derive from TEMPLATE_OPENERS_EN/DE instead of\nseparately hand-authored strings, so the prompt bans exactly what the\nRust content validator (generated from these same arrays) checks for.\n\n* feat(validate): generate the content-validator lexicon from prompts\n\nAdds packages/prompts/scripts/gen-prompts-rust.ts (pnpm gen:prompts /\ngen:prompts:check, mirroring gen-ipc-rust.ts) and replaces the hand-stubbed\napps/desktop/src-tauri/src/validate/content/lexicon.rs with its generated\noutput, sourced directly from natural-voice.ts's word-list arrays so the\nRust validator can never drift from the prompt's own bans. Moves the\nstub's unit tests into content/test.rs (generated files carry no\nhand-authored tests) and wires gen:prompts:check into the same CI job\nas gen:ipc:check.\n\n* feat(agent): add four résumé-quality read tools to the agent registry\n\nAdd validate_resume, search_candidate_evidence, lookup_salary, and\nget_trim_suggestions as thin adapters over validate::content,\ndocuments::evidence, and salary_research (zero new business logic).\nSplit into a sibling agent/tools_quality.rs to stay under the R8\nmodule-size cap, wired into read_tools()/prep_application_tools() via\none call. ToolContext now also carries resume_id so the SOURCE résumé\nresolves server-side, never from model-supplied args. Raise\nMAX_AGENT_STEPS 12 -> 14 for the two extra optional quality-tool turns\na run may now spend.\n\n* feat(ai-generations): persist quality_report on the application aggregate\n\nAdd the deterministic content-quality report as a new column on the\nai_generations aggregate (ADR-007 addendum): additive migration,\nAiGenerationRecord field, insert/update/import/row_to_record wiring, and\na pick-non-empty merge rule that also treats the migration's '{}'\nplaceholder as \"no report\" so it never clobbers a real one.\n\n* feat(resume): add content-validation ipc command\n\nExpose validate::content::validate_content over ipc as\nresume_validate_content: a new zod schema + generated rust request\nstruct, a server-side-clamped tauri command, and the tauri-client\nnamespace wiring. Pure/deterministic, no ai call, safe to run on every save.\n\n* feat(ai-generate): add quality-report validation lib and service hook\n\nRenderer-side glue around resume:validateContent: computeQualityReport\n(best-effort, never blocks/fails a save), the useValidateContent mutation\nhook, and a canned clean-report mock-client stub for tests.\n\n* feat(ai-generate): carry a fresh quality report on every generation save\n\nBoth surfaces (AI Generate's useGeneration, TailorFlow's generation-store)\nnow validate resumeText/coverLetterText right before persisting and attach\nthe report to the save request; the parsed report lands in session state\n(additive report field) and cold-entry hydration seeds it back from a\nsaved record's qualityReport. Validation failure degrades to a report-less\nsave, never blocking or failing the generation itself.\n\n* fix(ai-generations): merge quality_report per top-level wrapper key\n\npick_report treated the whole column as one opaque blob, so a letter-only\nsave (which only ever carries a coverLetter sub-report) clobbered a stored\nresume sub-report and vice versa. Parse both sides as JSON objects and\noverlay incoming's top-level keys onto existing's instead, so independent\nresume/coverLetter saves layer the same way every other field on this\naggregate already does.\n\nThe add_quality_report migration is still unreleased on this branch, so\nits default simplifies from '{}' to '' in place (no shipped build has\nseen it) and the merge no longer needs a placeholder special case. Also\nclamp the save-path quality_report to 256 KB, the same trust-boundary\ntreatment every other free-text field on this command gets.\n\n* fix(resume): reject an unrecognized doc kind and drop the duplicate span\n\nresume_validate_content silently coerced any unrecognized docKind to\nDocKind::Resume and logged the raw request string twice (the command-level\nspan plus validate_content's own). Reject \"resume\"/\"coverLetter\" strictly\nwith a Validation error, and drop the command-level span entirely — the\ninner validate_content span already logs codes/counts (ADR-027).\n\nAdd the command-level test coverage this file was missing: oversized\nmulti-byte inputs and an oversized requirements list are clamped rather\nthan processed whole (mirrors match_resume's own test), coverLetter\ndocKind routes to the letter ruleset instead of the résumé-structure\nchecks, and an unknown docKind is rejected.\n\n* feat(ai-generate): add quality badge, report panel, and hand-edit nudge ui\n\nQualityBadge (self-describing chip, opens QualityReportPanel), the panel\n(issues grouped by section, severity chip, guidance-framed message per\nissue code with a generic fallback for unknown codes, quoted evidence,\nmetrics footer), and HandEditNudge (dismissible, once per generation via a\nremount key) — wired into both results surfaces. en/de i18n for all new\ncopy, keyed per issue code (quality.issue.<code>).\n\n* fix(agent): close two fence-boundary forgery gaps in the quality tools\n\nHIGH-1: register the three new agent/tools_quality.rs result tags\n(validate_resume_result, search_candidate_evidence_result,\nget_trim_suggestions_result) in FENCE_TAG_PATTERNS so a forged tag\nsmuggled into a job_posting body (or a sibling quality-tool result)\ngets neutralized, not just the tag matching its own wrapper.\n\nHIGH-1b: neutralize a forged bare [tool_result:{name}] marker inside\nuntrusted tool-result bodies before wrapping the real one around it —\nFENCE_TAG_PATTERNS only covers the <tag> syntax, not this separate\ntranscript-boundary format.\n\nAdds regression tests for both, including the critic's probe B (a\njob_posting body carrying a forged validate_resume_result block) and\nthe sibling-tag case.\n\n* fix(validate): stop content validators accusing truthful resumes\n\nSix reproduced false-positive Criticals, each on a document that stated\nnothing but the truth:\n\n- project links compare on a canonical key (scheme dropped, host lowered,\n  trailing slash removed) and report the verbatim span; a bare host now needs\n  a path or a known code host, so a stack line's Socket.IO/Bun.sh no longer\n  read as links, and the stack line is excluded structurally as well\n- a role survives on any company token of two characters or more, so a\n  shortened employer (IBM Deutschland GmbH written as IBM) is not a dropped\n  role; legal forms and geography can never be the sole survival evidence\n- the language Critical is withheld when the source resume reads as the same\n  wrong language, which is detector disagreement rather than a defect, and\n  the metrics survive with it\n- unsupported_date no longer fires on a later year just because the source\n  spelled its open end as seit 2021 / since 2021 / 2021 -, and open-ended\n  detection now understands those spellings\n- present-tense markers match whole words, so presented, knowledge, currently\n  and actually stop reading as date context\n- the second-contact-block Critical needs a real address and a name on the\n  section's first line, not a bare @ next to any short line\n\nAlso: k/m/bn and doubled/2x restatements count as sourced numbers, metric\nchecks no longer skip every separator-laden body bullet, education entries\nwith date spans reach the evidence set, German function words are filtered\nout of the skills split (locally, never in the scoring kernel's STOPWORDS),\nstems never reach a user-facing message, low_coverage needs a five-point\ndrop, and the report's serialized key set is pinned against its TS mirror.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(validate): stop title_drift matching employers on a shared year\n\n`titled_entries` splits an entry at its FIRST separator, so the date span stays\ninside the company string and its year tokens join the company identity set.\nTwo unrelated employers whose spans merely touched the same year (\"… | 2018 -\n2021\" and \"… | 2015 - 2018\") matched each other, and the second entry's title\nwas reported as drift from the first's.\n\nFound by extending the German fixtures with a third role, which also gives the\nGerman paraphrase fixture a shortened-company-name case (IBM Deutschland GmbH\nwritten as IBM) so it discriminates the dropped_role survival rule.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(validate): correct two comments that overstated the contact-line rule\n\n`metrics_in` no longer skips every `is_contact_shaped` line, and\n`has_real_contact_match` has one caller, not two — `ats::is_contact_cluster`\napplies the same rule inline because it also has to reject a date range that\n`PHONE_RE` reads as a phone number.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ai-generate): detect stale quality reports after hand-edits\n\nWire a QualityReport.sourceTextHash (djb2 of the exact validated text) so\nQualityBadge/QualityReportPanel can tell when resumeOut/coverOut has diverged\nfrom what was checked - a hand-edit, or reopening a saved generation edited\nsince it was saved. A stale doc switches to a distinct \"checked before your\nedits\" state instead of keeping a green \"no issues\" badge on text the report\nnever saw. The same hash comparison covers both the live session and cold\nhydration for free, since staleness is derived at render time rather than\nstored.\n\nAlso wires the previously-dead useValidateContent hook to a \"Re-check\" action\non the quality panel (AI Generate surface only, where source/jobAd/meta are\nalready local) that re-validates the current text and clears staleness.\n\n* fix(ai-generate): stop a cancelled/regenerated run from persisting a stale quality report\n\nReproduced race: computeQualityReport() sits between generation finishing and\nonComplete/save. cancel() aborts the controller and resets the session\nsynchronously, but the in-flight validation call keeps running past that\npoint - without a guard it resurrects the (now-reset) session with a stale\nreport and fires onComplete (the caller's save), silently persisting +\nflipping \"Applied\" for a run the user cancelled.\n\n- generation-store.ts: check controller.signal.aborted before patching the\n  report / calling onComplete; ownership-guard the finally block's\n  controller-map cleanup so a stale run's teardown can't clobber a newer run\n  that already replaced it.\n- useGeneration.ts: abort any prior controller when a fresh run starts\n  (Regenerate re-entrancy), guard persist()'s setReport/save behind the same\n  aborted check, and ownership-guard the finally block's isGenerating/ref\n  cleanup the same way.\n- AIGeneratePage's reset() aborted only while stage==='generating', missing\n  the post-generation validation/save window (stage is already 'done' there)\n  - broadened to abort whenever a run is in flight.\n\n* fix(prompts): resolve lexicon-prompt drift, opener duplicates, codegen escaping\n\nRemoves 'not only'/'nicht nur'/'sondern auch' from the AI-tell prose arrays\n(the prompt never bans them, or only asks to use them sparingly), fixes the\ncover-letter opener-ban examples to quote two distinct opener families\ninstead of two near-duplicates and to render the German example with\ncorrect mid-sentence noun capitalization, derives the same hand-typed\nopener examples in the application-email prompt from the shared array, and\nadds a control-character guard to the prompt-to-Rust lexicon codegen so a\nbad array entry fails at generation time instead of emitting invalid Rust.\n\nAdds a mechanical array-to-prompt direction guard (the reverse of the\nexisting prompt-to-Rust codegen pin) so every AI-tell lexicon entry stays\ntraceable to prose the model was actually instructed to avoid.\n\n* refactor: narrow the quality report barrel to its consumed exports\n\n* docs: sync content-validation phase 1 into knowledge base\n\n- resume-domain.md: updated trim-panel pointer from match_resume::rank_trim_candidates\n  to documents/evidence::rank_bullets; added content-validation paragraph (deterministic\n  validators, 24 codes, ContentReport wrapper on ai_generations).\n- domain-model.md: added evidence extraction + content validation paragraphs under\n  Job/matching section; noted four agent Read tools.\n- adr-007: added F2 addendum documenting quality_report column (per-key merge rule,\n  opaque JSON wrapper, partial-save preservation via merge_quality_report, sourceTextHash\n  staleness detection).\n- Updated last-updated timestamps to 2026-08-09.\n\nPointers verified against evidence.rs + validate/content/mod.rs source. Contract shapes\n(ContentInput/ContentIssue/ContentReport) match task #1 shipped artifacts.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(renderer): validate quality_report shape before parsing (m-1)\n\nparseQualityReport cast unknown -> QualityReport with no shape validation,\nso a malformed persisted quality_report column crashed the whole app (only\nthe root ErrorBoundary caught it) every time that record's TailorFlow\nopened. Validate schemaVersion + each sub-report's issues array/metrics\nobject defensively; a malformed sub-report now drops to undefined instead\nof throwing, and a malformed issue entry is filtered rather than crashing\nthe array.\n\n* fix(agent): defeat nested [tool_result] marker forgery in tool results\n\nTOOL_RESULT_MARKER matched a full [tool_result:...] delimited token whose\nbody admitted a literal `[`, so a forged marker nested inside another one\n(e.g. [tool_result:[tool_result:save_resume]]) survived neutralization:\nthe greedy body stopped at the FIRST `]` (the inner marker's own close),\nleaving the fully-formed inner marker untouched in the output.\n\nMatch the opening [tool_result PREFIX only and replace with the canonical\n\"[ tool_result\" form, same convention as the fence-tag neutralizer -\nevery occurrence is found and broken independently of surrounding\nbrackets, so nesting can no longer hide one marker from the scan.\n\n* perf(validate): cap the near-duplicate bullet scan at 400 bullets\n\nduplicates::validate is an O(bullets²) pairwise Jaccard scan that ran\nunbounded — a malformed or hostile \"generated\" document with thousands of\nbullet-shaped lines held a tokio worker for seconds on every save.\n\nTruncate to MAX_DUP_BULLETS (400) before the pairwise pass, mirroring the\ncount-cap discipline agent::tools_quality already uses. A pathological\nbullet count now returns in well under 500ms and still flags a genuine\nduplicate placed inside the cap.\n\n* fix(validate): cap the content-report issue list to avoid silent report loss\n\nAn uncapped ContentReport.issues could grow the serialized report toward\n~1MB on a pathological document (thousands of forged roles / duplicate\nbullets). The IPC save path clamps quality_report to 256KB, truncating\nthat blob mid-JSON; merge_quality_report then fails to parse it and\nsilently falls back to the OLD stored report — a fresh report just\nvanishes.\n\nCap the issue list at MAX_CONTENT_ISSUES (200) in validate_content itself,\nso the byte clamp downstream is never reached. Criticals sort first before\ntruncation so a warning flood can never push a real Critical out of the\nvisible list; a trailing REPORT_TRUNCATED marker names the dropped count\ninstead of a silent cut.\n\n* fix(validate): strip control chars from target-language input\n\nnormalize_language only trimmed leading/trailing whitespace before taking\nthe first 2 characters, so an internal control character (e.g. \"a\\nb\")\nsurvived into the 2-char result. That value reaches this module's own\nvalidate:content span text and content.language_mismatch's evidence — a\nlog-injection primitive.\n\nFilter to alphanumeric characters before taking the first 2. Also mirror\nthe Zod targetLanguage.max(32) cap server-side in resume_validate_content,\nthe same trust-boundary treatment every other field on that command\nalready gets.\n\n* docs(agent): correct stale four-result-tags comment in the fence-tag registry\n\nThe comment claimed four super::tools_quality result tags were registered;\nonly three are. The fourth tool (lookup_salary) carries no free text to\nfence and returns via envelope_result instead. Fix the comment so a future\nfenced salary result isn't wrongly assumed already covered.\n\n* fix(ai-generations): clamp quality_report on the bundle import path\n\nThe IPC save path already clamps quality_report to QUALITY_REPORT_MAX_BYTES\nbefore persisting, but AiGenerationStore::import (a restored backup bundle\n- an equally untrusted, user-supplied file) wrote the bundle's quality_report\nstraight to the column with no cap at all.\n\nMove QUALITY_REPORT_MAX_BYTES into the data layer as the single source of\ntruth for both write paths, and clamp it in import too.\n\n* docs: point at the content-issue code roster instead of copying its count\n\n* fix(ai-generate): clear stale report before regenerating\n\nhandleGenerate cleared resumeOut/coverOut/buffers but never the quality\nreport, so the 'both'-mode progressive-reveal window (stage done while\nthe cover still streams) rendered the previous generation's report\nagainst the new resume until persist recomputed one.\n\n* fix(ai-generate): guard recheck against a mid-flight session reset\n\nhandleRecheck awaited validateContent.mutateAsync then unconditionally\ncalled onReportChange with a stale closure's report/currentOutput — a\nReset during the in-flight call could re-inject a report into an\nalready-cleared session. Add an epoch ref bumped whenever meta/\nsourceResume/jobAd change, checked before onReportChange fires.\n\n* fix(quality-report): map the truncated-report code and add a message fallback\n\nThe synthetic report.truncated code had no i18n key, so it rendered the\ngeneric quality.fallback sentence plus a bare count as evidence. Add\nquality.issue.report.truncated (en + de) and make messageFor() fall\nback to the Rust-authored issue.message before quality.fallback, so\nany future unmapped code degrades to something meaningful.\n\n* fix(validate): key accented project links without a utf-8 boundary panic\n\ncanonical_link stripped the scheme with `&s[..8]`, a fixed BYTE offset, so\nany project URL whose 7th/8th byte fell inside a multibyte char aborted the\nprocess: `www.café-berlin.de`, `http://éxample.com` and `ab.com/éx` all\npanicked with \"byte index 8 is not a char boundary\". The release profile is\npanic = \"abort\", so the app died mid-generation, before the document was\nsaved — a German or French project domain was enough to trigger it.\n\nByte-compare the scheme prefix on `as_bytes()` and only slice once the\nprefix is known to be that (pure-ASCII) scheme. Audited the rest of\ncanonical_link and urls_in: every other boundary already comes from a\nchar-aware API (`trim`, `trim_end_matches`, `split_once`, regex match\nspans), so this was the only fixed-offset cut.\n\nCovered by a canonical_link unit test with a straddling char at each\nboundary the function cuts at (2-, 3- and 4-byte widths), the three\nreproducer URLs run through the full validate_content path, and an accented\nlink in the en source/paraphrase fixture pair so the fixture suite carries\nit permanently.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* fix(agent): order criticals first before compacting the issue list\n\ncompact_content_report took the first MAX_ISSUES issues in EMISSION order,\nbut validate_content emits in check order, not severity order. A draft that\ntripped 20+ Warnings before a late check therefore dropped the Critical\nitself — ats.header_in_body is emitted near the end — while the summary\nstill reported `criticals: 1`. The model saw a count it had no issue to act\non.\n\nStable-sort Criticals ahead of everything else before the cap; within each\nseverity the emission order is preserved, and the counts still reflect the\nfull report rather than the surfaced slice.\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>\n\n* docs: regenerate the workflow catalog\n\nThe catalog generator's output drifted from the committed README on\norigin/main (heading style + this branch's new prompts-codegen CI step);\nthe pre-push gate diffs the regenerated output, so the regen must land\nwith the branch that trips it.\n\n* fix: guard superseded generation catch from stomping a newer run's ui\n\nThe catch block in handleGenerate lacked the ownership guard its finally\nblock already had: a Regenerate click during an in-flight cover-letter\nstream aborts run 1 after run 2 owns the UI, and run 1's late rejection\nthen reset stage/buffers/stage rotation out from under run 2. Early-return\nwhen the controller ref no longer matches. Regression test covers the\nsupersede-during-stream window the stale-persist tests missed.\n\nPR #963 review finding (MEDIUM, renderer race).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: clamp content issue message and evidence spans to byte caps\n\nMAX_CONTENT_ISSUES bounds issue count but not size: ats.long_bullet\nquotes the whole bullet verbatim (it fires because the bullet is long),\nso a 200KB document could push a sub-report past the 256KB quality_report\nbudget and into the truncate-mid-JSON path M-3 exists to make\nunreachable. Clamp message/evidence at the issue() chokepoint (400 bytes\neach, char-boundary safe via clamp_to_bytes, truncation marker inside\nthe budget) so count x size is genuinely bounded.\n\nPR #963 review finding (MEDIUM, correctness).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: keep merged quality report within the 256kb column budget\n\nBoth write paths clamp the incoming quality_report alone, so merging\nonto a stored sub-report near the cap could persist up to twice the\ndocumented budget. Guard at the sink: if the merged union exceeds\nQUALITY_REPORT_MAX_BYTES, keep incoming whole (known-parseable and\nwithin budget) instead of byte-truncating merged JSON, which would be\nunparseable on the next read and silently revert to the stale report.\n\nPR #963 review finding (LOW/MEDIUM, data).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: flag lone surrogates in the gen-prompts rust escape guard\n\nWell-formed JSON.stringify (ES2019+) escapes an unpaired surrogate as\n\\uXXXX just like a control character, producing the same Rust-invalid\nescape the guard exists to catch; valid astral pairs pass through raw\nand stay fine. Rename hasControlChar to hasRustUnsafeChar to match.\nAlso corrects the stale pick_report reference in quality-report.ts:\nthe Rust merge is merge_quality_report and does not special-case '{}'.\n\nPR #963 review findings (2x LOW, robustness + docs).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: anchor staleness hash per document slot and persist re-checks\n\nsourceTextHash was a top-level sibling map, but quality_report merges\nper top-level key: a resume-only regenerate emitted {resume} and the\nmerge wholesale-replaced the map, permanently orphaning the surviving\ncover letter's staleness anchor (a later hand-edit then showed green on\ntext the report never saw). The wrapper is now schemaVersion 2 with the\nhash inside each document slot ({report, sourceTextHash}), so the\ngeneric per-key merge carries report and anchor atomically; v1 blobs\nparse as no-report by design (unreleased shape, dev machines only).\n\nAlso unifies Re-check via a shared use-quality-recheck hook: TailorFlow\n(the surface with inline editing) gains the previously missing Re-check\naction, and both surfaces now persist the merged wrapper through the\nsave path's empty-keeps-existing merge rules instead of discarding the\nresult on reopen. Session-only fallback (commented + tested) when the\nrecord has no job url to merge onto.\n\nPR #963 round-2 review findings (MEDIUM staleness, LOW docs shape,\nLOW/MEDIUM re-check split). Mutation-checked.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: key re-check staleness guard off run starts, not input identity\n\nThe epoch guard in use-quality-recheck bumped only when meta/source/\njob-ad identity changed, which covers Reset but not Regenerate (both\nrun paths reuse those references), so a slow re-check resolving after\na Regenerate click could resurrect a stale report and persist\npre-regenerate text over the fresh document. The epoch now also bumps\non every generating-flag transition; merge/persist read the live base\nreport and live texts at resolution time and drop the result if the\ndocument text changed while the check ran. Hook hoisted from\nOutputPanelDone to AIGeneratePage: an AnimatePresence exit renders\nwith the previous props, so a generating prop read inside the\nunmounting panel provably never fires (verified with a probe).\nDedicated test file added so deleting the guard can no longer pass\nthe suite; three mutation checks reported.\n\nfrontend-reviewer HIGH on 1ffa290c.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test: cover the warning variant of the quality badge\n\nEvery issues-bearing fixture carried a critical, so the warning branch\n(issues present, zero criticals, not stale) was never exercised; a\nvariant flip there passed the whole suite. Asserts the amber border\ntoken and the 0-critical label against the danger case.\n\nPR #963 round-3 review finding (MEDIUM, test-coverage).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: drop over-cap quality reports to the sentinel instead of byte-truncating\n\nclamp_to_bytes cut the incoming blob on a byte boundary, producing\ninvalid JSON that merge_quality_report silently discarded — and on a\nfirst save for a job the insert path bypasses the merge guard entirely,\nso truncated garbage went straight to storage. Both untrusted write\npaths now route through sanitize_quality_report: an over-cap blob\nbecomes the documented '' no-report sentinel with a content-free\nwarning (source + byte count, ADR-027), making the degrade explicit,\nlogged, and parse-safe on every path.\n\nPR #963 round-3 review finding (MEDIUM, correctness) — confirmed and\nextended to the uncovered insert path. Mutation-checked (4 tests fail\nwithout the guard).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: clamp agent quality-tool inputs and size the summary cap to fit\n\nvalidate_resume compared the RESUME_CAP-fenced draft against the full\nunclamped stored resume, flagging roles beyond 8000 chars as dropped —\nfalse Criticals for content the drafting tool was never shown; both\nsides now see the same clamped universe. All three quality handlers\nclamp resume/job text before CPU-bound analysis like their siblings.\nSUMMARY_CAP is now derived from MAX_ISSUES x the per-issue worst case\n(the old RESUME_CAP value truncated a full summary mid-JSON at ~13KB),\nwith the arithmetic pinned by a worst-case test.\n\nPR #963 round-3 review findings (1 HIGH + 2 MEDIUM, all confirmed by\nreproduction). Mutation-checked.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: seven validator and evidence precision defects from round-3 review\n\nAll confirmed by reproduction before fixing. Letter reports no longer\ncarry bogus role counts (doc-kind metrics decided in one match arm);\ngeneric-letter and ai-tell checks now honor their documented scoping\n(posting comparisons suppressed on language mismatch; phrases already\nin the source resume exempt, per the prompt's own rule); a fabricated\npercentage >=100 yields one Critical instead of two (dedupe on the\nnormalized number); entry scans get a MAX_SCANNED_ENTRIES cap\nmirroring MAX_DUP_BULLETS; project link-lines classified Contact are\nkept as evidence (mirrors the Education arm); and the formation-family\neducation headings are word-boundary matched so PERSONAL INFORMATION\n(en/es/pt/it) stops classifying as education — previously the block's\ncontact PII reached the generation prompt as a degree.\n\nPR #963 round-3 review findings (6 MEDIUM correctness + 1 perf-shape).\nMutation-checked on the two scoping guards.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* ui: hide the roles metric row on cover-letter reports\n\nRole counts describe resumes; after the validator stopped emitting\nbogus counts for letters the row still rendered a meaningless 0 -> 0.\n\nCompletes the round-3 letter-metrics finding on the renderer side.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* ui: hide all hard-constant metrics on cover-letter reports\n\nThe CoverLetter validate_content arm returns top-requirement hits,\nduplicate ratio, and role counts as constants (alignment/duplicates\nnever run for a letter); rendering them stated \"0 requirements\ncovered\" as fact on a well-targeted letter. Only keyword coverage is\ngenuinely computed for both kinds, so it is the one metric a letter\nreport keeps.\n\nPR #963 round-4 review finding (MEDIUM) — completes what 814eb509\nstarted for the roles row.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: enforce the tool summary budget on serialized length, not raw chars\n\nSUMMARY_CAP was derived from pre-escape per-field char budgets, but the\ncap is applied to the JSON-serialized body — quote-heavy drafts inflate\npast the budget and were cut mid-string into unparseable JSON for the\nmodel. compact_content_report now measures the actual serialized length\nand drops whole weakest-last issues into the truncated marker until it\nfits. Also caps hits count (MAX_HITS) and clamps each hits/skills\nentry's length: keywords_normalized has no upper token bound, so one\nlong alphanumeric run in a posting became an unbounded string in the\nsummary.\n\nPR #963 round-4 review findings (2 MEDIUM, both confirmed by\nreproduction). Mutation-checked.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: seven more validator precision defects from round-4 review\n\nAll reproduced with a failing test before fixing. Companies that reduce\nto geography/legal-form tokens only (Deutschland GmbH, bare Berlin) are\nskipped instead of unconditionally accused of a dropped role; the\ntwo-space entry form gets its own split so the city stops becoming the\ncompany; title-drift identity tokens exclude legal forms and geography\nvia a shared identity_tokens helper; German keyword-density counting\nfilters function words (fuer is 4 bytes, so the byte-length filter\nnever dropped it); the German AI-tell tier was entirely dead against\ninflected adjectives and now matches stem+e/em/en/er/es through the\nsame word-boundary path; orphan experience bullets land in an\nunattributed bucket instead of silently vanishing from the evidence\nset; and issue() clamps section (120B) so the report byte arithmetic\nholds with every term bounded.\n\nPR #963 round-4 review findings (1 HIGH + 6 MEDIUM, zero false\npositives). Mutation-checked on the survival gate and the DE matcher.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor: split tools_quality and evidence tests into sibling test files\n\nFour review rounds of in-file regression tests pushed both modules past\nthe R8 1400-LOC hard cap. Converted to the directory-module pattern\nai_generations and validate/content already use (mod.rs + cfg(test)\ntest.rs); pure code movement, test counts unchanged (33 + 24).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: keep validator-computed numbers for lossy issue codes in the panel\n\nmessageFor preferred the static translation whenever a key existed,\nsilently discarding interpolated counts the Rust validator computed.\nAudited all 24 codes against their evidence fields: exactly five carry\na number the evidence does not repeat (keyword_density's total,\nlong_bullet's length, bullet_count's count, low_burstiness's sentence\ncount, rule_of_three's raw ratio) — those render the Rust message;\neverything else keeps the translation. Boundary pinned both ways.\n\nPR #963 round-5 review finding (MEDIUM, i18n). Mutation-checked.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: shared summary shrink loop, fence-name neutralization, lexicon dispatch\n\nThe round-4 measure-then-drop loop covered compact_content_report only;\ncompact_evidence_set (~20KB worst case) and compact_trim_suggestions\n(~15KB) still overflowed SUMMARY_CAP on their own declared caps and got\ncut mid-JSON — all three now route through one shrink_to_summary_cap\nhelper dropping whole weakest-last items. tool_result_fence neutralizes\nand caps (64 chars) the model-chosen tool NAME, closing the transcript-\nboundary forgery the body scan never saw. Generated lexicon dispatch\nreturns an empty slice for uncurated languages instead of falling back\nto English (the prompt sends those languages a wordless directive, so\nthe validator flagged words the model was never told to avoid) — fixed\nat the codegen source and regenerated. Four tool handlers gain\nAppHandle-free cores with 17 tests over not-found and fallback paths.\n\nPR #963 round-5 review findings (3 MEDIUM + 1 LOW security, all\nconfirmed). Mutation-checked on the shrink loop and the name fence.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: seven round-5 validator findings; unmeasured hits render as a dash\n\nA generated document with no Projects section no longer draws one\nfalse altered_project_link Critical per source link (dropping the\nsection is a legitimate trim; both sides must have the section to\ncompare). The language-mismatch guard is now a positive control — it\nfires only when the source is long enough to detect AND aligns with\nthe target — instead of failing open on short or third-language\nsources. Skills-line category labels (Languages:, Frameworks:) are no\nlonger claimed skills. top_requirement_hits is Option<u32> end-to-end\n(None for uncomparable postings, empty requirements, and letters) and\nthe panel renders an em dash, never a literal 0 presented as fact.\nKeyword-density suppresses for languages without a curated function-\nword list rather than accusing French/Spanish prose. Contact-shaped\nentry lines under Experience (Acme Payments, Berlin, 2018 - 2021)\nkeep their employer. Legal-form company detection is positional\n(ends-with), so Group Product Manager is a title again.\n\nPR #963 round-5 review findings (1 HIGH + 6 MEDIUM, zero false\npositives). Mutation-checked on the Projects gate, Contact arm, and\nlanguage guard.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: preserve null requirement hits through persisted-report parsing\n\nparseMetrics coerced every non-number to 0, so a saved report with\ntopRequirementHits: null (not measured) rehydrated as a literal 0 on\nreopen — resurrecting the \"measurement nobody took, rendered as fact\"\nstate one modal close after the panel showed the correct em dash.\nnull now survives the round trip, pinned by a parse test.\n\nPR #963 round-6 review finding (MEDIUM, correctness).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: count limit-capped drops in the tool summary truncated field\n\ntruncated was computed after .take(limit), so it only reflected\nshrink-loop drops — a candidate set five over the limit reported\ntruncated: 0 while withholding five bullets. Both compactors now count\nagainst the pre-limit total, with tests covering the cap alone and\nboth mechanisms together.\n\nPR #963 round-6 review finding (MEDIUM, no-silent-caps).\nMutation-checked (all four new tests fail on the old arithmetic).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: size the quality-report byte budget for both sub-reports\n\nQUALITY_REPORT_MAX_BYTES (256KiB) was sized against ONE sub-report's\ndocumented ~214KB worst case, but the wrapper it guards holds resume\nAND cover-letter sub-reports — a legitimate near-worst-case save was\nsilently dropped to the sentinel on exactly the documents the caps\nwere sized to allow. Raised to 512KiB with the two-sub-report\narithmetic in the doc; a regression test builds both sub-reports at\nworst-case size from the real validate::content constants and asserts\nthey persist byte-for-byte.\n\nPR #963 round-6 review finding (MEDIUM, correctness).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: seven round-6 validator and evidence precision defects\n\nAll reproduced red before fixing. Mid-sentence typographic multipliers\n(3x) are extracted again (x needs no trailing boundary); space- and\napostrophe-grouped figures (1 200, 1'200) parse as one number with the\nexactly-three-digit rule as arbiter; date-order checks only fire on\nactual date spans, not prose comparing two years; proficiency lines\n(Python: Advanced, Deutsch: Muttersprache) keep the skill as the claim\ninstead of the grade; the pipe/middot entry split filters location-only\nsegments like the two-space form; Beruflicher Werdegang classifies as\nexperience and unclassified sections with bullets degrade to the\nunattributed bucket instead of vanishing; and a mid-section unparsed\nentry line opens its own role (employer salvaged via the existing\nsplitter, never guessed) rather than crediting the previous employer.\nAlso refreshes two doc references to the 512KiB wrapper budget.\n\nPR #963 round-6 review findings (5 MEDIUM + 2 LOW, zero false\npositives). Mutation-checked on the date gate and both halves of the\nentry-line rescue.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: report skills-list truncation in the evidence summary\n\nskillsPresent/skillsAbsent were MAX_SKILLS-capped with no signal while\nthe adjacent truncated field read as whole-payload completeness — and\nskillsAbsent is the gap list the agent plans from. A skillsTruncated\nfield now counts both lists' pre-cap drops (asymmetric-count test\nguards against one-sided or doubled accounting). MAX_HITS deliberately\nstays unreported, with the rationale on its doc.\n\nPR #963 round-7 review finding (LOW, no-silent-caps).\nMutation-checked.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: tighten the role-opening gate and the skills list discriminator\n\nThe round-6 entry-line gate accepted a bare year, so prose like\nPromoted to Staff Engineer, 2022 opened a role — and the comma-less\nsalvage path named that role after the whole line, contradicting the\nnever-guess contract. The gate now requires real column structure\n(span separator, open end, or month token) and a comma-less label\nopens an unattributed role via a scoped salvage helper, leaving\nsplit_entry's load-bearing two-space behavior untouched (proven by\nmutation). A skills list now requires an actual separator (slash\nexcluded — Deutsch: C1/C2 is a grade), so multi-word grades like\nverhandlungssicher in Wort und Schrift keep the language as the claim\ninstead of the grade words; the conceded space-separated-category\nboundary is pinned as the miss it is.\n\nPR #963 round-7 review findings (1 MEDIUM + 1 LOW, both confirmed).\nFour mutation checks.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: log the oversized-union quality-report fallback\n\nmerge_quality_report's oversize fallback silently discarded the other\ndocument's stored sub-report while its sibling drop path\n(sanitize_quality_report) logs the same event class. Same content-free\nwarn (byte counts only, ADR-027) so the drop is diagnosable.\n\nPR #963 round-8 review finding (MEDIUM, data-loss visibility).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: neutralize forged fence tags in every tool result; honest draft flag\n\ntool_result_fence neutralized the marker but not fence tags, so tool\noutputs that never pass through fenced() (research brief, draft text)\ncould smuggle a forged <validate_resume_result> block — every result\nbody and name now runs through one shared neutralize_known_fence_tags\nchokepoint (idempotent on already-fenced bodies, pinned). A draft\nlarger than the validation cap now reports draftTruncated and forces\nok:false instead of silently validating 20% (cap raise rejected: it\nbreaks the same-universe invariant both directions; CPU cost measured\nat 2x, recorded honestly). Construction-dependent prose bans\n(not just / highlighting / showcasing / underscoring family) leave the\ngenerated lexicon — the prompt states them as constructions a\nsubstring cannot judge — while the prompt text provably keeps them.\n\nPR #963 round-8 review findings (1 HIGH security + 2 MEDIUM; the\nalphabetical-skills finding stopped at the file boundary with a\nproducer spec). Mutation-checked on the fence and both flag halves.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: nine round-8 validator and evidence findings, all reproduced red\n\nCareer Summary/Objective/Profile classify as summary, not an invented\nrole; header_in_body requires a real phone shape instead of any 7-char\ndigit run (salary ranges no longer Criticals); the fabricated-metric\nband skip is position-based, so mid-document contact-shaped prose is\nchecked and source contact digits no longer source unrelated metrics\n(both sides walk the same band-skipped lines); scheme-less project\nlinks (github.com/...) count as source links via per-URL resource\nnaming, ending a false altered-link Critical; skill_not_demonstrated\ngates on curated function-word lists like the density check; category\nlabels claim nothing (closed en+de label set) instead of reporting\nframeworks as an undemonstrated skill; a comma-less entry label becomes\nthe company only when it reads like a name (caps-based, works en+de,\nresidual documented); and the clean-fixture guards are genuine\nrewordings now — which shook out one real fixture defect and one\npre-existing kernel weakness (German articles surviving the byte-length\nstopword filter), recorded for follow-up, not silently patched.\n\nPR #963 round-8 review findings (8 MEDIUM + 1 LOW, zero false\npositives). Ten repro tests red-first; two mutation checks.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: neutralize transcript markers in fenced prompt inputs too\n\nfenced() neutralized fence tags but not the [tool_result marker, so a\nscraped job posting could carry an intact-looking transcript marker\ninto its own data block — the input-side mirror of the tool-result\nfence hole. The marker neutralizer moved down into tools.rs (layering:\ncontroller imports tools) and both boundaries now run through one\nneutralize_transcript_boundaries chokepoint, enumerated once,\nidempotent, ordering-immaterial. The pass also exposed and corrected a\nwrong idempotence assertion in the prior round's test (trusted output\nfed back as untrusted input).\n\nProactive fix of the adjacent gap flagged while closing the round-8\nHIGH. Mutation-checked (three tests fail without the marker pass).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor: split controller tests into a sibling test file\n\nThe fence-neutralization rounds pushed agent/controller.rs past the R8\n1400-LOC hard cap; same directory-module split as tools_quality and\nevidence (mod.rs 694 + test.rs 753, 25 tests before and after, pure\nmovement).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: relevance-ordered skills lists and one shared phone rule\n\nskills_present/skills_absent were alphabetical, so every truncating\nconsumer kept an alphabetical prefix of the gap list — both lists now\norder by posting term frequency (kernel-derived counts, stemmer-folded,\nalphabetical tiebreak keeps determinism) so take(N) is automatically\nthe top-N. has_real_contact_match adopts the strict header-phone rule\n(moved into mod.rs, ats imports it downward), closing the letter-path\nhole where a rate-range paragraph escaped the fabricated-metric check;\nthe candidate's own sign-off number stays exempt, pinned.\n\nRound-8 follow-ups (producer spec + flagged loose-phone item).\nMutation-checked both.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: run resume content validation off the tokio worker threads\n\nresume_validate_content was async with no await, so its CPU-heavy body\n(200KB x 3 inputs, capped n^2 scans) ran inline on a shared tokio\nworker; dropping async routes it through Tauri's blocking pool like\nevery other awaitless command. Limiter coverage was investigated and\ndeliberately not added: Limiter gates external cost surfaces\neverywhere in this codebase, and the chosen mitigation for local CPU\nis the algorithmic caps already inside the validators.\n\nPR #963 round-9 review finding (MEDIUM, half confirmed / half declined\nwith pattern evidence). Two pre-existing siblings with the same shape\nflagged for a follow-up outside this PR.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: wire the quality tools into the prep flow and honest letter checks\n\nThe four quality tools were whitelisted but the prep system prompt\nprescribed the old closed 7-tool sequence, so nothing ever invoked\nthem. The flow is now a 10-step closed sequence with a mandatory\nvalidate_resume self-check (act on Criticals before saving the\ncorrected text) plus one rationed optional quality call; a set-\nequality test derives the expected tools from the registry so\nprompt/registry drift fails a test in both directions. validate_resume\ngains a validated docKind so a drafted cover letter gets the letter\nruleset (unknown values refused, empty-letter fallback refused); the\ndead fenced() tag-wrap on quality payloads is gone (the controller\nprovably destroyed it — results now neutralize + clamp without a\nwrapper, tags stay registered so any occurrence is unambiguously a\nforgery); stale alphabetical-cut docs state the producer-ordering\ncross-module invariant; and the DE prose lexicon drops erfolgte durch\n(construction-dependent, prompt-only) leaving an honestly-empty list.\n\nPR #963 round-9 review findings (1 HIGH arch + 2 MEDIUM + 2 LOW).\nMutation-checked on the naming guard and both docKind halves.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: five round-9 validator findings and the requirement-hit ratio\n\nThe pipe/middot date-column pick requires a year (a company named\nCurrent no longer eats the employer — the suggested is_date_only was\ndisproven by mutation and a stricter named check used instead);\nrequirement hits ship with their measured denominator as one\nunconstructable-apart pair, rendered as 2 / 4 in the panel, preserved\nthrough the parse layer, bare count only for pre-denominator persisted\nreports; the truth side of the metric pass filters section 0 by shape\nso pre-heading profile figures still source their restatements; a\nnumber-alone phone-shaped line is never a metric claim in any band\n(closing the letter sign-off triple-Critical the R8 symmetry fix\nintroduced, with all three directions pinned); and generic-letter\ngates on job-ad/target alignment via a dedicated premise (folding it\ninto posting_comparable broke a pinned case — the ad-vs-setting\ncomparison is not the document-vs-target one) with the short-ad\npositive control carried over. Lexicon entry rules now exempt the\nhonestly-empty DE prose tier from the non-empty assertion while\nkeeping per-entry rules future-proof.\n\nPR #963 round-9 review findings (5 MEDIUM, zero false positives).\nSeven mutation checks across the batch.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test: carry the requirement denominator in the tailor hydration fixture\n\nThe parse layer now emits topRequirementsMeasured, so the seeded-slot\nequality fixture needs the field to round-trip byte-identical.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: run agent quality-tool cores on the blocking pool\n\nThe three quality handlers called synchronous SQLite reads plus their\nCPU-bound cores inline in async fns, parking a tokio worker per agent\ntool call. Each handler now moves the store read and core call into\none tauri::async_runtime::spawn_blocking closure (spawn_blocking_core,\nmirroring documents' established wrapper and JoinError mapping); the\nin-memory postings-cache lookups stay inline like their siblings.\n\nPR #963 round-10 review finding (MEDIUM, perf).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: dispatch resume content validation off the main thread correctly\n\nRound 9 dropped async on the premise that plain sync commands run on a\nblocking pool; the vendored tauri-macros/ipc source for the locked\n2.11.5 proves the premise inverted — a plain command responds inline\non the webview IPC callback (no pool exists), which put the 600KB scan\non the UI event loop. The command now carries tauri::command(async)\n(sync body, dispatched via async_runtime::spawn onto the tokio pool),\nwith the verified rule + source citations + the two-round history in\nthe doc so it cannot flip again without a contradicting citation. The\nround-9 sibling precedents were audited: all negligible-cost, none\nneeding the attribute.\n\nPR #963 round-10 review finding (MEDIUM, correctness — the reviewer\ncatching its own prior round's wrong suggestion).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: four round-10 validator findings, all reproduced red first\n\nTitle-drift identity now reads split_entry's parsed company (two\nemployers sharing a Remote column or a spelled-out month no longer\nmerge and accuse an unchanged document); the skills relevance key\ngates on curated function words so French fillers stop topping the\ngap list (demotion, not deletion — an empty list would read as no\ngaps, and detected language misreads must not delete real ones; the\nhonest limitation is measured and recorded); trimming one project of\nthree no longer fires per-link Criticals (source links compared only\nfor entries the generated document kept — trimming never adds a\nlink); and the bare-phone exemption is scoped claims-side everywhere\nbut source-side only inside the contact band — the literal claims-\nside-only fix provably re-broke the round-9 sign-off direction, so\nboth invariants now hold and are stated precisely.\n\nPR #963 round-10 review findings (3 MEDIUM + 1 LOW, zero false\npositives). Four mutation checks.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: run the renderer validation scan on the blocking pool too\n\nThe (async) attribute moved the scan off the UI thread but parked it\non a tokio worker — a different placement than the identical scan in\nthe agent handlers. Now a genuinely async command wrapping the scan in\ntauri::async_runtime::spawn_blocking with the same JoinError mapping:\none consistent answer, with the vendored-source citation trail kept\nand the one remaining outlier (frozen match_resume.rs, chip-tracked)\nnamed in the doc.\n\nPR #963 round-11 review finding (LOW, perf consistency).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: report daily-budget exhaustion distinctly from rate limiting\n\nadmit_research collapsed a permanently-exhausted daily budget into the\ntransient RateLimited outcome, so the salary tool told the agent a\ncondition that cannot succeed until UTC midnight was retryable. New\nDailyBudgetExhausted variant end-to-end (admission, salary reason,\ntool envelope string) with the tool description telling the model\nwhich reason is worth retrying; the daily-charge branch extracted into\nan AppHandle-free helper so both admission failures are unit-pinned.\n\nPR #963 round-11 review finding (MEDIUM). Mutation-checked at both\nchoke points.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor: split commands/ai tests into a sibling test file\n\nThe round-11 budget-outcome tests pushed commands/ai.rs past the R8\n1400-LOC cap; same directory-module split as tools_quality, controller,\nand ai_generations (mod.rs 1113 + test.rs 321, 19 tests before and\nafter, pure movement).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: five round-11 validator findings atop the evidence module split\n\nOpened with the overdue R8 split: the entry/label/date family moved\nverbatim into documents/evidence/entry.rs (re-exported, no caller\nchanged). Then: the header-phone shape test excludes year spans at the\none shared site (ats's duplicate clause deleted, its coverage pinned);\ntitle-drift compares against every source title at an employer, so two\nroles at one company stop accusing a byte-identical document; an\nall-geography comma-less label opens unattributed instead of naming\nBerlin the employer; the pipe arm reads company-first when the first\nsegment ends in a legal form (cheapest reliable signal — vocabulary\nand voting rejected with reasons, residual conceded in the doc); and\nheading detection promotes via the classify_section lexicon when the\nparser found no headings at all — probing showed the reviewer's named\nGerman examples already worked, the real class was every Title-Case\nheading outside the exact SECTION_NAMES list, and the promotion is\nshape-guarded so it can never eat a figure from the metric whitelist.\n\nPR #963 round-11 review findings (5 MEDIUM, all confirmed; one\nreviewer premise narrowed by probing). Seven mutation checks.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: update landing architecture citation for the commands/ai split\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* chore: update the ai command route glob for the directory split\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: tag each tool-summary issue with its severity\n\nThe prep flow orders the model to fix Criticals, but the per-issue\nlist carried no severity — only a document-level count with nothing to\nmap it onto. Each issue now carries the same lowercase wire word every\nother Severity consumer sees (serde impl reused, no string to drift);\nthe worst-case arithmetic gained its measured terms and the cap test\nasserts the field survives the shrink loop whole.\n\nPR #963 round-12 review finding (MEDIUM). Mutation-checked.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: four round-12 validator findings, control-pair reproduced\n\nThe skills self-comparison takes its own stemming decision from the\ndocument actually compared (a foreign-language ad can no longer break\na document-internal check — control pair proves the ad was the only\nvariable; the orphaned Analysis::display went with it); density and\nskill checks early-return on a proven language mismatch while the\nsame document read correctly still fires; heading promotion is per\nline instead of a document-wide switch (one conventional Ausbildung\nheading no longer disables the fix for Beruflicher Werdegang in the\nordinary mixed CV); and the header-phone year guard narrowed to what\nits own lesson said — a date SPAN is not a phone — restoring metric\ninvariant 2 as shape-test agreement (whitelist-keep was tried and\nrejected: it provably re-broke the round-9 bare-US-number direction).\n\nPR #963 round-12 review findings (3 MEDIUM + 1 LOW/MEDIUM, zero false\npositives). Eight mutation checks.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: three round-13 validator findings, red-first\n\nA job title above its employer no longer promotes to a phantom\nProjects heading (a heading sits above a BLOCK, a title above a LINE —\npromotion refuses when the next line opens a role, reusing the\nevidence module's own entry shapes rather than a third opinion; the\nphantom had zeroed rolesOutput and could even re-arm the project-link\nCritical); promotion never applies to cover letters (a mini-heading\nhad flipped the letter to the resume metric path and emptied its\nreport); and document-internal token comparisons share one\nDocumentTokens mechanism — title-drift and, by measurement, the\nduplicate ratio (0.2857 to 0.0 on one unchanged document purely from\nthe ad's language) now stem by the document compared, not the ad.\n\nPR #963 round-13 wave-1 findings (1 MEDIUM + 2 LOW-tier, zero false\npositives). Seven mutation checks.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: four round-13 wave-2 findings in the ats checks\n\nheader_in_body now means what its Critical claims — the candidate's\nOWN header repeated (identity against the header band's name forms),\nso listing a referee stops being an accusation; three pre-existing\ntests were pinning the false positive as the canonical fixture and now\npin the true shape, with the referee form owned by its own test (the\nsuggested REFERENCES lexicon was rejected: it misses ANSPRECHPARTNER\nand this lane has twice shipped English-only-lookup bugs — cost of the\nidentity rule stated on the function). bullets_per_role opens a role\non a trailing date column exactly as the evidence module does, so a\nsecond employer's bullets stop counting toward the first (correct\nattribution asserted, not just absence). bullet_count groups under the\ndocument's own heading instead of a hardcoded English Experience.\n\nPR #963 round-13 wave-2 findings (2 MEDIUM + 1 LOW i18n across two\nthreads incl. the reviewer's own retraction). Four mutation checks.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: size the quality-report byte cap for json escaping, not raw bytes\n\nQUALITY_REPORT_MAX_BYTES was sized from the RAW per-field caps in\nvalidate/content (~214 KB per sub-report), but serde_json escaping roughly\ndoubles quote/backslash-heavy text: a legitimate two-sub-report wrapper full\nof quoted resume evidence serializes to ~780 KB — past the old 512 KiB cap —\nand sanitize_quality_report would silently drop it to the empty sentinel.\nResize to 1 MiB (2x, matching the documented <=2x escape-density assumption\nfor realistic text) with the escaped arithmetic in the doc comment, add a\nregression test that measures the actual serialized bytes of a quote-filled\nworst case (asserts it exceeds the old cap AND survives the new one), and\nrefresh the two validate/content doc comments that cited the old 512 KiB\nfigure. The round-6 worst-case test filled fields with 'x' — non-escaping\nfiller never stressed the expansion the cap exists to absorb.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: three round-14 findings in section classification and factual metrics\n\nCombined headings (\"Skills and Experience\", \"Kenntnisse und Erfahrungen\")\nclassified as Experience because EXPERIENCE_HEADINGS matched by substring\nwith unconditional precedence, filing skill lines as work-experience bullets\n— and, in extract_evidence, DELETING the section outright (no arm reads\nSkills). The bare \"experience\" word per language moves to the ambiguous set\nand now yields to a summary or skills word; work-qualified spellings\n(employment, work experience, berufserfahrung) keep unconditional precedence.\n\nSuffixed numeric claims (\"35k users\", \"3.5m revenue\") were invisible on the\nclaims side: INTEGER_RE only saw the mantissa and dropped it (<3 digits or\ndecimal), so a fabricated suffixed figure never fired factual.unsourced_metric.\nBoth sides now expand suffixes through one shared expand_suffixed, and\nINTEGER_RE captures inside a suffixed span are suppressed — the bare mantissa\nhad been a pre-existing false Critical against a source writing \"250,000\".\n\ncanonical_link stripped the scheme but not \"www.\", so the same project URL\nwith and without it drew two false altered_project_link Criticals. One\nleading www. label is stripped case-insensitively in the same\nbyte-compare-then-ASCII-slice shape as the scheme strip; accented fixtures\nextended (the UTF-8 boundary panic here was this PR's first critical).\n\nAll three reproduced red first and mutation-checked; full suite and\narchitecture tests green.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-10T15:57:56+02:00",
          "tree_id": "356331ea969331e664e7e14838773990acd94b60",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/c438eda00ed37b9ed174cba1bf39b1010ddf088e"
        },
        "date": 1786371735603,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2176648,
            "range": "± 27534",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2586758,
            "range": "± 101258",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 291620,
            "range": "± 13059",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "96cc9f9640992bc5ae019788fcedf717a896b307",
          "message": "feat: native structured output for openai, gemini and ollama with a hardened json parse module (#964)\n\n* refactor(ai-provider): split cursor pagination helpers into pagination.rs\n\n* feat(ai-provider): add complete_structured with a prompt-discipline default\n\n* feat(ai-provider): wire openai response_format json schema\n\n* feat(ai-provider): wire ollama format field for structured output\n\n* feat(ai-provider): wire gemini response schema for structured output\n\n* feat(ai-provider): add anthropic structured-output model gate and correct json-mode capabilities\n\n* feat(pipeline): add pure json extract, repair and typed parse module\n\n* refactor(prompts): drop the unused structured-output profile flag\n\n* refactor(pipeline): compare json delimiters as chars in the balanced scan\n\n* docs: drop the removed structured-output flag from the provider profile description\n\nThe branch removed supportsStructuredOutput from ProviderProfile (native\nstructured output is now decided per-request by the Rust provider layer via\ncomplete_structured); this line still described the old field and behavior.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: harden json extraction and structured-prompt assembly against hostile model output\n\nSecurity review found the latent complete_structured surface exploitable\nbefore its first caller lands. extract_json committed to the first fenced\nblock before any deserialization attempt, so a triple-backtick inside a JSON\nstring value (a job posting with a code fence echoed into a field) hijacked\na fully valid response into all-defaults data reported as success, and an\nattacker echoing a fenced object first controlled the parsed value on every\nprompt-discipline path. parse now walks ordered candidates — the whole\nresponse when it is exactly one balanced value, then fenced bodies last-first,\nthen top-level balanced spans last-first — with a clean parse pass for every\ncandidate before any repair.\n\nJsonParseError derived Debug, re-exposing the exact model content its\nhand-written Display withholds ({:?} logging and .expect() panic text reach\nthe crash reporter); Debug is now hand-written over reason() only. The\nre-ask path gets a safe-by-construction reask_detail() that wraps the\nfragment in the shared ADR-010 fence with the raw detail() accessor demoted\nto pub(crate). structured_prompt now flattens turns through the shared\nflatten_messages so assistant/tool provenance markers survive, matches the\nsystem role case-insensitively, and schema translation recursion is\ndepth-capped, failing whole to the existing fallbacks.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: seven pre-pr review findings on the structured-output surface\n\nContainment now overrides the parse pass split: a candidate with a nested\nlater candidate gets its repair attempt immediately, so a clean fence quoted\ninside a container that merely needs repair (raw newline, trailing comma,\nsmart quotes) can no longer win — while sibling clean-beats-repairable and\nthe shape-mismatch fallthrough are preserved and now both pinned by tests.\nThe three native complete_structured overrides thread req.effort through the\nsame gate as their streaming bodies (openai reasoning_effort, gemini\nthinkingLevel without includeThoughts, ollama think), positive and negative\ntests each. Both schema translators degrade whole on composition keywords\nthey cannot walk (anyOf/oneOf/allOf/not/$ref/$defs at any depth) instead of\nemitting strict:true over unstrictified subtrees or silently dropping the\nconstraint. GEMINI_KEPT_KEYWORDS gains the documented value constraints\n(pattern, min/max, length and property bounds) verified against the current\nSchema reference. The json-only directive no longer references an example on\nthe empty-hint path, and the public extract_json trap moved into the test\nmodule. Ollama Cloud's response_format delegation is now cited to vendor\ndocs.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: five coderabbit round-1 findings on the structured-output surface\n\nopenai_response_format now rides strictify's walk with a doc-verified\nOPENAI_STRICT_KEYWORDS allowlist and degrades whole to json_object on any\nkeyword strict mode does not permit (minLength/minProperties and the\nif/then/else family were the real exposure — the finding's pattern example\nis stale, pattern is supported now); nothing is ever silently stripped.\ncontains_version_needle requires component boundaries on both sides of the\nneedle, rejecting lookalike model ids glued to neighbouring components.\nFence language tags are stripped only when the newline precedes the fence's\nown closer and the tag is not JSON punctuation, so a one-line fence no\nlonger promotes an unrelated span into the fenced tier. bounded's expired\ncumulative deadline gets a hermetic regression test, and MAX_CANDIDATES is\npinned (band, bottom-drop truncation, real answer survives).\n\nThe sixth finding was disproven: the embed body's embedContentConfig\nnesting matches the current v1beta discovery document (rev 20260806), which\nmarks the top-level outputDimensionality deprecated — the suggested move\nwould have targeted the deprecated location. Citation recorded in the doc\ncomment; no code change.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* chore: record the gemini embed-body nesting precedent in review-config\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test: bound the pathological duplicate-scan test for slow ci runners\n\nThe 500ms wall-clock bound held locally but failed all three nextest\nretries on the shared CI runner (~2.2s in a debug build), turning every PR\nred — including #963 itself at merge time. The assertion's job is to\ndistinguish the capped scan from an uncapped quadratic blowup (~18M pair\ncomparisons, tens of seconds anywhere), which a 10s bound still does with\nfull margin on any runner.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: three ai-review round-2 findings on the structured-output surface\n\nThe structured path dropped the token limits on every native provider, not\njust the reported Ollama: streaming bodies sent num_predict/num_ctx,\nmax_tokens and maxOutputTokens while the non-streaming builders dropped\nthem, truncating large resume/job-ad prompts. Each adapter's non-streaming\nbuilder now takes an Option<StructuredCall> mirroring its own streaming\ngating field-for-field (absent, never null), with parity tests asserting\nagainst the streaming body's value so the two can only drift together.\n\nThe invalid_json_detail re-ask fence tag joins FENCE_TAG_PATTERNS (as a\nliteral — R7 forbids an agent-from-commands import; the producing side's\ntest pins the tag text), so a forged parser-detail block inside foreign\nfenced content is neutralized like the established tags.\n\nJsonParseError's Syntax/Shape payloads become RawDetail — private field,\ncontent-free Debug, no Display — so pattern matching can no longer reach\nraw model text past the content-free formatting; detail() destructures it\npub(crate)-only and reask_detail() stays the public safe path, with a\nsource-pin guard on the field visibility.\n\nKnown pre-existing gap flagged, not half-fixed: the prompt-discipline\nfallback still calls the 5-arg complete_with_usage and cannot carry the\nlimits without a trait change touching every adapter.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: two coderabbit round-2 findings on the model gates and strict schemas\n\nThe adaptive-thinking gate's bare mythos substring was the last unbounded\nneedle — claude-notmythos-9 classified adaptive; it now routes through\ncontains_version_needle, whose component-boundary rule applies unchanged to\na bare family word, keeping family breadth (mythos-6 and later stay\nadaptive) pinned by test. Tuple-form and boolean items schemas degrade\nwhole to json_object: the non-object arm of the strict-keyword walk\nreturned fine, shipping maxLength and unclosed object members under\nstrict:true — and the alternative guard placement inside strictify is\nprovably incomplete (its object arm clones items through untouched).\nGemini's walker already degraded on both tuple shapes; now pinned so the\nproviders cannot drift.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-10T20:38:14+02:00",
          "tree_id": "fda8a16ee9e6ba3a53e74b83ad29d5051d5917a9",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/96cc9f9640992bc5ae019788fcedf717a896b307"
        },
        "date": 1786387815080,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2192314,
            "range": "± 27847",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2641317,
            "range": "± 143153",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 304878,
            "range": "± 4256",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "3ca7d9cb6411670f85bbf5f63ecf3cc08a66b9d8",
          "message": "feat: add jake, awesome and deedy resume templates from the latex classics (#965)\n\n* feat: add jake, awesome, and deedy resume templates\n\nPort three classic LaTeX community resume designs into the Typst engine.\nJake (ATS tier) routes through the parametric single_column.typ with no new\nknobs. Awesome and Deedy (design tier) get bespoke .typ files - a thin\naccent-tinted header band with accent-bar section markers, and a bold name\nblock with an accent-colored surname, respectively.\n\nAwesome's white band-text registry field would otherwise leak into the\ngeneric DOCX renderer as invisible white-on-white text, so name_color stays\na real dark ink and the .typ hardcodes white for its own band (mirroring\nmeridian.typ's existing c-band-text convention). DOCX approximates the band\nas run-level shading behind the name run, echoing the Banded cover-letter\nlayout's flat-shading precedent.\n\n* test: cover jake, awesome, and deedy templates in the golden test matrices\n\nAdd the three new templates to canonical_template_ids (every-template PDF\nrender/extraction/tier tests pick them up automatically), plus dedicated\nspec-pin, DOCX-shading, and ATS-mode-honesty tests for Awesome and Deedy.\nSync the frontend picker's tier-badge counts and ATS-toggle gate lists.\n\nRegenerate resume + cover-letter template preview SVGs and the showcase\nbanner via the offline #[ignore] generators (ran fine on this host in debug\nprofile). This also refreshes several older templates' previews that had\ngone stale since their last regen (a pre-existing gap the generators'\nown doc comments already flagged), not just the three new ones.\n\n* chore: regenerate resume + cover-letter template preview assets\n\nNew SVGs for jake, awesome, and deedy (resume + cover-letter previews) and\nthe 16-column showcase banner. Also refreshes academic, atelier, classic,\nlebenslauf, meridian, portrait, swiss-minimal, and throughline previews that\nhad gone stale since their last regen — a pre-existing gap the ignored\ngenerators' own doc comments already flagged, surfaced by running the\nrequired regen for the three new templates.\n\n* docs: sync export templates doc to the sixteen-template set\n\nThe doc was stale two PRs deep (still said twelve): cologne-navy from #953\nwas never added, and this branch adds jake, awesome, and deedy. Counts,\ntable rows, tier breakdown (nine ats / seven design), and the\nparametric-vs-bespoke route note verified against the TemplateId enum and\ntemplate constructors in source.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: honest ats-mode hint for decorative templates and a shared template id type\n\nThe ats toggle hint was a two-way guess (two-column or photo) that predated\ndesign-tier templates with neither trait — awesome and deedy fell to the\nphoto branch and promised to remove a photo that does not exist. A\nPHOTO_TEMPLATE_IDS trait set plus one atsModeHintKey helper now routes all\nthree cases (two-column, photo, decorative) with new en+de copy, and both\ncall sites consume the helper so they cannot drift apart again.\n\ntemplates.ts also stops declaring its own sixteen-literal TemplateId union:\nit imports the wire contract's type from @ajh/shared (newly re-exported\nthrough the contracts barrel — it was never in the curated export list), so\na template id added to only one side becomes a tsc error instead of a\nsilent Classic fallback at deserialization.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: seven export-side review findings on the new templates\n\njake's name_centered was a render no-op — align(center) inside an\nauto-width block; the name, title and contact blocks now take width 100%\nwhen centred (contact included deliberately: the docx path centres all\nthree off the same flag), pinned by a glyph-geometry test measured from the\nrendered svg with a left-aligned control, and the fourteen non-centred\ntemplates verified byte-identical by svg hash. Awesome's docx band becomes\nparagraph-level shading with the shared band tint (accent lightened 85%)\nand dark ink — the run-level crimson-plus-white approximation contradicted\nthe banded-letter precedent it cited and hardcoded the invisible-white-text\nhazard the registry test guards against — and the band now gates on\nats_mode like its pdf sibling. The suspected band overflow was real but\nworse than predicted: the unbounded background layout pushed a long contact\nline off the sheet entirely (x=630 on a 595pt page); the header now wraps\ninside the printable width and the band grows to 28mm when a title is\npresent, both pinned by render measurements. deedy's local scale supplement\nbecomes the section_above_extra registry knob (rule_thickness precedent,\nrender byte-identical). The validator matrices derive from\ncanonical_template_ids so a new template cannot skip validator coverage.\nPreview assets regenerated (jake, awesome, showcase); export docs and the\nstale preview-regeneration note in the knowledge base corrected.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: template captions through i18n and a hint test that can actually fail\n\nTEMPLATE_CAPTIONS shipped all sixteen captions as raw English rendered\nstraight into the template cards; the map now stores translation keys\nresolved through t() at the render site, with faithful en and de entries\nfor every template, pinned by a test using the real translations instance\n(it fails on the raw-string map) and a render-level test that fails if the\nsite skips t(). The atsModeHintKey routing test restated the\nimplementation's branch order and could not fail; it is replaced by a\ndesign-tier totality check and an overlap invariant — the two-column and\nphoto sets intentionally intersect on portrait, aria and saffron, so the\nsuggested disjointness assertion would be false; the overlap is pinned to\nexactly those three instead.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: eight coderabbit findings on the awesome band, theme routing and test rigor\n\nThe band's two-line contact budget was confirmed reachable through the real\nadapter path — extra_links is an unbounded vec, and twelve of them wrapped a\nthird line to baseline y=86 against a 79.4pt band bottom, white on white.\nband-h is now measured from its own content inside a context (the 24/28mm\nthin minimums become floors; descender padding tuned so one and two lines\nstay byte-identical), making any line count safe. The duplicated rich-text\nladder collapses to one render-runs-in(runs, link-fill) — and mutating the\nband's link fill to the body accent revealed no test pinned it, so the band\nregion now asserts zero accent-filled glyphs. The docx band decision routes\nthrough theme::has_header_band with droppable-band semantics (meridian also\npaints a band but is ats-tier with no toggle; a test pins band templates to\nthe design tier, and meridian's pre-existing pdf/docx divergence is recorded,\nnot changed). Tier matrix gains a length-plus-membership guard, awesome's\nname color is pinned by equality, and the svg measurement helper now panics\non transforms it cannot parse instead of returning zero — the strict parser\nfired on nothing across the engine suite, and the silent zero had been\nhiding the link-fill gap above. Docs: route matrix completed to all sixteen,\nthe copied lightening literal replaced with pointers, and the preview\npipeline documented as its actual two consumers. All rendered svgs verified\nbyte-identical before and after by hash.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-11T07:58:30+02:00",
          "tree_id": "fac4cd7de6b833a6f85a78f74a70bb2eff483045",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/3ca7d9cb6411670f85bbf5f63ecf3cc08a66b9d8"
        },
        "date": 1786428858096,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1666480,
            "range": "± 116807",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2020322,
            "range": "± 25936",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 208488,
            "range": "± 12470",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "7e331463ef67c7b1b10dc449a8c547160e79fc01",
          "message": "feat: autopilot adopts semantic scoring when enabled (#966)\n\n* feat: autopilot adopts semantic scoring when enabled\n\nScheduled runs re-rank the keyword prefilter's top survivors through the\nsame combined kernel as the jobs page — score_one semantics including\nlanguages_align, closing the known divergence on this path — when the user\nhas semantic scoring on. The webview-only toggle gets a rust-readable\nsingle-column mirror on job_preferences written by settings and boot-sync,\ndeliberately not a struct field so the full-row set() can never clobber it\n(the salary_expectation precedent). Phase two runs after cluster_aware_retain\nso minMatchScore keeps its keyword meaning and only deduped survivors cost\nan embed, capped at twenty per run from the aggregator's posting cap;\ncache ids hash canonical_job_key and the resume text so tracking-param\nvariants hit the paid row and a resume edit is a miss by construction.\nEmbed failure degrades that job to keyword-only via an explicit scoreSource\nemitted from the same option check as the blend — a run never fails because\nof scoring, and the formula version stays unbumped because no score changes.\nThe autopilot percent label flips to match percent only when semantic is on,\nen and de.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: critic blockers on the autopilot semantic re-rank\n\nThe translate flag becomes a MatchSurface enum with one shared translates()\ntable — every surface labelled match percent runs the full jobs-page\npre-processing including translation (the skipped step collapsed coverage\nto language-neutral tokens and embedded a cross-lingual cosine, two\ndifferent numbers under one label), false only for the extension's\nstructural zero-egress; pinned by a cross-surface parity test. The\nsynthetic resume snapshot vector moves out of the shared vectors index into\nposting_vectors with its TTL discipline, the write site itself now refuses\nsynthetic scoring ids (documents/sql.rs split for R8), and a test pins the\nindex count and stale arithmetic across a semantic run. The tautological\nsemantic-off test is replaced by should_semantic_rerank plus a phase\nfunction owning the gate, the wall clock (per-run timeout with keyword-only\ndegrade and a rerank_start step) and a lazy setup closure, so zero-cost-off\nis a property of the tested function. Re-ranked head and keyword tail sort\nas two blocks; only cluster canonicals are re-ranked (an unreachable\nbelt-and-braces dedup guard was deleted rather than shipped); daily-cap\ncharges move inside score() behind an actual-round-trip predicate; the\nautopilot locale source now matches the jobs page with detect-and-backfill\nrecorded as a deferral; mirror-write failures surface a notification.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: charge embeds at the call on the exact bytes and close the advisory gaps\n\nThe will-this-reach-the-provider predicate answered a different question\nthan the call — it hashed the untranslated blob while the embed consumed\ntranslated text (phantom charges on every cache-hit run of a translated\nposting) and never saw the resume embed at all (uncharged real embeds,\nincluding after ceiling exhaustion). It is deleted rather than patched:\ndocuments::embed_charged charges at the single choke point all three\nembeds pass through, on the miss path only, with a latching per-run budget\nthat still degrades a refused job to keyword-only. The provider-reaching\neffects sit behind a ScoreIo seam so the kernel is testable without an\nAppHandle, and the required fixture exists: a translator that actually\nrewrites the text, pinning zero charges on a fully-cached translated\nposting and exactly one per real embed. score_one also stops caching a\nkeyword-only number under the semantic key when no embedding backed it —\na refused or provider-offline job no longer freezes the wrong number for\nthe cache TTL (a pre-existing jobs-page trap). Autopilot delete drops its\norphaned resume vector unless another autopilot shares the text; the\nsemantic-scoring command returns a real Result so mirror-write failures\nreject the promise and the notification can fire; the blob map is\ncandidate-scoped; import warns-and-skips a synthetic vector row instead of\naborting the restore.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: delta-review findings on the semantic score cache and rerank loop\n\nsemantic_available now requires BOTH vectors — a cached posting with a\nrefused resume embed produced combined = 0.4 x ats labeled combined and\nfroze it under the semantic cache key for the TTL; both mixed shapes are\npinned as separate tests so the asymmetry that hid this cannot recur, and\nthe degrade explanation gains a third branch instead of reporting a\nsimilarity that was never measured. The candidate-url set drops its\npositional cap (the loop's own counter bounds embeds; the cap silently\ncost later jobs their rerank slot when head entries collapsed as URL\nvariants). Autopilot update and remove both route through one mutate_record\npath that drops the orphaned resume snapshot vector AND its resume-scoped\nmatch_scores rows. A consecutive-degrade breaker (3) stops the phase when\nthe provider is plainly down, bounding offline cost without reintroducing\nthe frozen-cache bug. Embedder types go pub(crate) so the charge choke\npoint is a visibility guarantee, and score sources emit via the shared\nconstants.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: ensemble findings — comparability, merge pairing, translation cache key\n\nsemantic_available now derives from the one comparison rather than vector\npresence: a cross-space pair (a stale resume vector surviving a base-url\nswitch under the same model name) made compare() err, flattened to a\nmeasured zero, published as combined, cached for the TTL and promoted by\nthe re-rank — it now degrades honestly to a keyword score, pinned by the\ncrate's first test of compare()'s error branch with fixture preconditions\nproving the mechanism (the pair is genuinely present AND genuinely\nincomparable). The root cause (vectors survive a config write) is a\nrecorded cost deferral — the stale vector now yields an honest recomputed\nkeyword score instead of a lying combined one. merge_found_jobs' new\nscore_source pairing gets the resurface test its score_provisional sibling\nalways had (deleting the line failed nothing before). The translation cache\nkeys on (job_id, sha256(text)) with a bounded size, so a board updating a\nposting under this branch's cross-run-stable ids is a natural miss instead\nof a stale translation feeding a stale score forever. ADR-020 gains the\naddendum eighteen call sites already cited, and the embedding-free claims\nin the knowledge base become conditional on the toggle.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: coderabbit round-1 findings on the semantic re-rank surface\n\nFive tests that asserted against local copies of the formula, guidance\nsentence or stemmer guard are rewritten against the real kernel with fixed\ninputs (or deleted as redundant with the seam tests); the weights pin\nseeds a known cosine and a verbatim-contained resume so the combined value\nhas exactly one correct answer. A timed-out re-rank now returns its\npartial summary through caller-owned state and emits a distinct\nrerank_timeout step instead of reading like a keyword-only run. The\npreference gate logs when the store is unmanaged; a source-pin asserts\nmutate_record is the only record writer; the match-path prune amortizes\nonce per 64 writes like its sibling; posting-vector cache-write failures\nwarn content-free; the v5-to-v6 migration upgrade is pinned by a reorder\nmutation; sql.rs's only tauri marker was spawn_blocking so it moves to\ntokio and the R2 allowlist entry is deleted; the score-variant union is\nexported and derived in the i18n test; and the mixed-scale list gains a\nvisible per-block metric label in both locales. The R8 cap forced phase\ntwo into commands/autopilot/rerank.rs as a pure move. Docs: the status\nrow annotation, the ADR duplication and line-anchor, and the\nmatching-algorithm pipeline section all updated to the two-phase model.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-11T10:40:24+02:00",
          "tree_id": "7aac32b6ac736cd6e6593083452f41ecd44d785f",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/7e331463ef67c7b1b10dc449a8c547160e79fc01"
        },
        "date": 1786438352947,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2185624,
            "range": "± 17125",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2568010,
            "range": "± 31802",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 299658,
            "range": "± 10820",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "5dc139577a3b6a76c2f2f4101d7a901197413080",
          "message": "feat: integrate the no-ai-slop catalog into the anti-ai-tell system (#967)\n\n* feat: integrate the no-ai-slop catalog into the anti-ai-tell system\n\nCurates all ~90 catalog items through a four-rule checkability bar (fixed\nform, zero factual content, unconditional prompt ban, no hidden domain\nmeaning): eight high-precision entries join the validated lexical tier\n(paramount, transformative, multifaceted, ever-evolving, paradigm shift,\nmeticulous, widely regarded as, it is worth noting), ~44 items — including\nevery construction pattern (binary contrasts, colon reveals, fake-profound\nkickers, portability test, show-don't-tell) — become prompt guidance the\nmodel is instructed by but user text is never flagged for, and ~19 are\nskipped with recorded reasons. Zero structural checks shipped: both\ncandidates fired on truthful counter-examples (\"shipped a status page,\nshowcasing live latency\"; a polite self-answered close) — the family\nalready removed twice. Words naming real work (utilize, facilitate,\nspearhead, beacon) stay validator-invisible by rule, pinned by negative\ncontrols over an ordinary resume; apostrophe-bearing entries are blocked by\na shape test (matching never normalizes punctuation, so they were half-dead\nby construction); German stays curated-not-translated with a leak pin.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: depth-aware guidance tier and the critic advisories on the tell catalog\n\nThe anti-tell blocks split into checked and guidance tiers per depth: brief\nships exactly the lines that back a voice.* check (the validator cannot see\nwhich depth produced the output, so brief may only instruct what it also\nverifies), dropping the block share of the small-model letter prompt from\n48 to 26 percent; full and task keep the complete text and every other call\nsite is byte-identical via the depth default. The voice formatting line is\nscoped so it no longer contradicts the letter's keyword-bolding output\nrules, pinned by a prompt-consistency test. transformative moves to the\nprompt tier — it fails the catalog's own no-hidden-domain-meaning rule on\ntransformative learning and transformative justice, both now negative\ncontrols. Curly apostrophes fold to ascii at the two ai_tell_issues call\nsites only (header-name matching untouched), making contraction tells\ncheckable: it's worth noting, it's important to note and the today's-world\nfiller join their frequent surface forms — the uncontracted twins needed\ntheir own entries, the for-free hypothesis tested false. Shape guards gain\ndouble-space and leading-non-word checks; the German construction-guidance\nasymmetry is recorded as an evidence-driven deferral.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: ensemble findings — depth everywhere, honest tiers, paramount out\n\nThe depth tiering reaches all nine prose consumers instead of two:\nreferral, application-email, rewrite and the interview and answer builders\nthread their resolved depth (the last three gained a backward-compatible\ntarget param), roughly halving every small-model system prompt — a brief\nreferral note drops from 7129 to 4140 chars. The CHECKED constants now\ncarry only validator-backed phrases: ten prose phrases and nine lexical\nstrays the module's own tests classify as never-validated moved to the\nguidance tier, enforced by a mechanical reverse test that parses the\nquoted bans out of the checked lines (a bogus phrase added there now\nfails it), shrinking the brief blocks further. paramount moves to the\nprompt tier — Paramount Global is a real employer and the exemption reads\nonly the resume, never the job ad; the employer-name exemption widening is\nrecorded as a deferral with its injection risk noted (company name, not ad\nbody). The intermediate-commit baselines are reworded to what main\nactually shipped, the budget bound ratchets to a quarter, the opener\nmatcher folds curly apostrophes like its siblings (with a contraction\nopener appended and the load-bearing indices documented), and the vacuous\nbold-ban iteration is replaced by unguarded cases per ban-bearing depth.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: coderabbit round-1 findings on the voice-block composition and test rigor\n\nThe application-email builder composed its voice block only in the full\nbranch — brief and task carried no anti-tell rules at all, the format\nskeleton's two opener examples being the entire anti-slop surface; the\ndepth-scoped block is now hoisted into one local composed by all three\nbranches (full byte-identical), pinned per depth with mutations failing\nexactly the branch they remove. The opener tests assert through a\nfirst_evidence Option so a miss names the fixture; the letter negative\ncontrol gains the genuinely missing template-opener silence pin while four\ncomments naming a voice.ai_tell_prose code that never existed are\ncorrected; the honesty test's normalize folds curly apostrophes like the\nmatcher it mirrors; and the quoted-ban parser throws loudly on an\nunbalanced quote instead of silently shifting every later ban into prose.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-11T13:03:06+02:00",
          "tree_id": "fdba463ea95cb1a761db790dbed469f246597f28",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/5dc139577a3b6a76c2f2f4101d7a901197413080"
        },
        "date": 1786446866420,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2167338,
            "range": "± 9267",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2553596,
            "range": "± 17642",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 295607,
            "range": "± 7506",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "0e0675c29121704665859c0f346ff31ec20c659e",
          "message": "feat(pipeline): phase 2 run infrastructure (#968)\n\n* feat(pipeline): phase 2 run infrastructure\n\n- jobs/cancel.rs: CancelRegistry (pays the TODO(arch)); ScraperEngine\n  delegates, commands/agent.rs drops its engine borrow; one shared map\n- pipeline/budget.rs: Budget + AGENT_PREP/RESUME_QUALITY, StoppedReason\n  relocated with wire-compatible re-export + RunTimeout/MaxToolCalls/\n  MaxRepairs; agent consts deleted; compile-time budget relations\n- Pipeline::run_hooked + StageHooks (cancellation seam in before());\n  Pipeline::run untouched\n- pipeline/runs: pipeline_runs + pipeline_run_events in own\n  pipeline_runs.db; DataStore/Resettable/R3/backup wiring; newest-3\n  per-job retention (history, not a cache — no tier knobs)\n- pipeline:stage event channel declared + lock-tested (no emitter yet)\n- gen-prompts: stage prompt blocks frozen into pipeline/resume/\n  prompt_blocks.rs; FACTUAL_GROUNDING_RULES deduped from 3 copies\n- Completer::complete_json: parse -> one fenced budget-charged re-ask ->\n  hard error; record_usage on every call incl. the re-ask\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(renderer): honest labels for new stopped reasons, neutral fallback\n\n- STOPPED_SUFFIX maps run_timeout/max_tool_calls/max_repairs to their\n  own keys; unknown reasons fall back to a neutral \"stopped\" key, never\n  \"done\" (the old ?? 'done' rendered a timed-out run as completed)\n- chip tone derives from the same lookup so copy and colour cannot\n  diverge; inert defaultValue removed (its key always existed)\n- en+de copy for the three variants + the neutral fallback\n- tests pin each variant, the unknown-reason path, and the done tone;\n  mutation-checked both directions\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): close phase 2 critic findings (10)\n\n- metrics_json clamped at write site + import (4 KiB, marker fails a\n  JSON reader) — the uncapped sibling of artifact_json\n- pipeline:stage sectionKey narrowed to a closed PipelineSectionKey\n  union + isPipelineSectionKey guard + 24-char bound, lock-tested\n- complete_json charges the provider daily ceiling before EVERY\n  round-trip incl. the re-ask; a rejected charge never reaches the\n  provider\n- prune() sequenced: partial failure now rolls back (the tuple-match\n  scrutinee committed unconditionally); both DELETE error arms covered\n  via injected failures\n- id TEXT PRIMARY KEY NOT NULL + NOT EXISTS orphan sweep (one NULL id\n  turned NOT IN into a permanent silent no-op; migration unshipped)\n- spend recording moved to the testable side of the seam — deleting\n  record/charge now fails three tests\n- budget const asserts moved out of cfg(test) (probe proved they were\n  inert); shrinking a budget below its flow now fails cargo check\n- clamp reserves the marker inside the cap; shared clamp_json\n- retention partitions by (job_url, kind); per-kind prune pinned\n- stale PipelineStageEvent doc reference reworded\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: sync architecture rules doc with the arch tests\n\n- R3 owner row rebuilt from the actual R3_ALLOW list (conversations\n  removed long ago; pipeline/runs and 7 other stores were missing)\n- R8 hard cap 1100 -> 1400 (matches HARD_CAP_LOC)\n- L2 prose: ad-hoc SQL stays forbidden; R3-allowlisted per-domain store\n  mod.rs files (pipeline/cache, pipeline/runs) may use db::open()\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* chore(pipeline): close ensemble advisories (7)\n\n- phase vocabulary is now a runtime export (PIPELINE_STAGE_PHASES as\n  const, type derived from it); the pinning test asserts on the export\n  instead of comparing a literal to itself\n- rustStringLiteral delegates the control-char/surrogate check to\n  hasRustUnsafeChar (duplicate walk removed); describe blocks added for\n  the new codegen helpers; generated output byte-identical\n- rustdoc links in jobs/cancel.rs fully qualified (warnings gone)\n- dangling doc refs to deleted consts point at Budget::AGENT_PREP\n- runs module doc narrowed to what import() actually bounds\n- gen-prompts header documents both generated outputs\n- architecture-rules L2 sentence un-garbled (two remedies restored;\n  exception says rusqlite-where-R3-allowlisted, not just db::open)\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* chore(hooks): drop the ai review step from the pre-push gate\n\nOwner decision: it was the slowest step (~3 min LLM round-trip, often\n\"reviewer unavailable — passing\") and the internal pre-PR review chain\n+ CodeRabbit + CI cover the same ground. The deterministic gates\n(fmt/clippy/tests/deny/drift checks) stay. scripts/pre-push-review.mjs\nremains for the Stop-gate tooling shared via review-lib.mjs; the dead\nRANGES accumulation is removed.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): close coderabbit round 1 (8 fixed, 1 disproven)\n\n- pre-push-review.mjs deleted (nothing invokes it since the gate\n  dropped the ai-review step; it was the only .review-cache writer);\n  hook + review-lib/review-gate comments de-staled\n- RESUME_FRAMING_STAGES const; max_steps assert now matches the\n  documented 12+4 arithmetic (a shrink to 13 no longer compiles)\n- all six budget timeout fields pinned to literals; WIRE table pinned\n  by wildcard-free match + length (new variant fails to compile until\n  added; row removal fails the count)\n- clear_all logs per-table warns instead of discarding DELETE errors;\n  privacy.rs Resettable comment no longer over-promises\n- pipeline:stage phase vocabulary emitted into generated events.rs\n  (one source of truth for the Phase-3 emitter); RunEventRow.phase\n  stays TEXT by design (older bundles must restore)\n- timeout stopped-reason pinned to the neutral label in renderer tests\n- architecture-rules: R3/R8 rows now point at R3_ALLOW/HARD_CAP_LOC\n  (the executable truth) instead of hand-copies; L2 exception wording\n  precise (application stores, db::open + DataStore)\n- cancel-registry identity finding DISPROVEN: every registry key is a\n  UUIDv4 from new_job_id(); doc note records the mint invariant\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: de-stale references to the removed pre-push llm review lane\n\nCLAUDE.md + DEVELOPMENT.md now describe the real flow (deterministic\npre-push gate; AI review pre-PR + CodeRabbit + the CI required check).\nADR-0008 and ADR-025 get status addendums rather than rewrites — the\nbodies stay as historical record.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): close claude-review round 1 (3 new findings)\n\n- pipeline_run_events.phase gets CHECK (start/finish/error) while the\n  migration is unshipped; drift test parses the CHECK literals from the\n  executed SQL and asserts set-equality with the generated\n  PIPELINE_STAGE_PHASES (both directions); out-of-vocabulary phase\n  fails append_event AND aborts import without clearing\n- bundle wiring pinned: pipelineRuns round-trips a real store through\n  the section contract; source pins force build_bundle/data_import\n  try_state symmetry (deleting either block fails a test)\n- unreachable stoppedSuffix ternary arm removed (suffix derived inline\n  at the two result-guarded use sites; same-lookup invariant intact)\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* chore(ci): bump dorny/paths-filter to v4.0.3\n\nCarries the GHSA-7hc6-8hq5-9q2m fix (multi-line filename escaping in\nlist-files output — we don't use list-files, so defense-in-depth) plus\nthe some-with-excludes predicate quantifier. SHA-pinned as before.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ci): close the path-filter holes on both sides (audit follow-up)\n\nCI (dorny/paths-filter is picomatch — no ** means root-only):\n- frontend filter now covers apps/desktop root files (vite/vitest/\n  playwright configs, package.json, tsconfig, index/e2e.html) — a PR\n  touching only those previously skipped typecheck/tests/build while\n  ci-ok went green\n- extension filter gains turbo.json (the job runs through turbo)\n\nHook (.husky/pre-push):\n- a FAILED git diff now forces the full gate (the || true swallowed it\n  as \"nothing changed\" and every gate silently skipped — proved in a\n  scratch repo, contradicting the hook's own uncertainty rule)\n- the docs fast path only applies when the pushed ref IS HEAD (pushing\n  feature-x from a docs-only HEAD previously fast-pathed real code)\n- new EXTENSION group + gate (turbo typecheck/test/build, mirroring\n  ci-pipeline.yml's extension job) — apps/extension/** tripped nothing\n  locally before\n- NODE regex gains the apps/desktop root-files twin of the CI fix\n\nAll filter changes verified with a picomatch harness against the live\nYAML and the extracted hook regex block under sh and dash.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(ci): bring landing + extension into coverage; harden test reports\n\n- apps/extension joins the root vitest projects (excluded by inertia —\n  git history shows the list predates it; no env conflict) and both\n  apps/landing + apps/extension src enter coverage include; Next.js\n  route shells excluded by FILENAME so app/world/world-config.ts (has\n  tests) stays visible; extension manifest.ts (build-time) excluded\n- newly measured: extension 87.3% lines, landing 83.9%; existing gated\n  threshold globs untouched (new surfaces measured-not-gated, per the\n  advisory-surfaces convention)\n- the two dorny/test-reporter steps get continue-on-error: a fork PR's\n  read-only token 403s the check-run API itself, which must not fail a\n  green tests job (same guard the coverage steps already carry)\n\nFull vitest run --coverage: 455 files / 6221 tests, exit 0.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): close review round 2 (8 findings)\n\n- import hardening: identity/text columns REJECTED past named caps\n  (id 128B, job_url 2KiB, labels 64B, stopped_reason capped too) and\n  row caps (5k runs / 50k events) checked before the transaction opens;\n  truncating an identity would corrupt it, so reject-not-clamp\n- sectionKey contract codegen'd: SECTION_KEY_MAX_LENGTH, fixed keys,\n  experience prefix emitted from the TS source (mutating the TS value\n  fails the Rust pins — proof against hand-copies)\n- gen-ipc slice emission is width-independent: measured rustfmt's real\n  rule (array_width=60, then PACKING — the reviewer's one-per-line\n  sketch isn't fmt-clean either); block form ships one-per-line under\n  rustfmt::skip, byte-stable at any vocabulary size; output-neutral\n  for every existing const today\n- WIRE table: per-variant uniqueness assertion (a duplicate row could\n  previously hide an omitted variant at unchanged length)\n- data.rs source pins scan the actual routing forms (import_into /\n  stores.get), not bare quoted keys — the bare-key scan passed a\n  mutation that deleted the routing\n- docs: ADR-0008 title names three active surfaces; DEVELOPMENT.md\n  pre-push section no longer contradicts itself, pipeline_runs.db in\n  the per-store DB list; CLAUDE.md review-gates paragraph consistent;\n  review-gate.mjs header scopes the CI-only claim to repository-owned\n  automation\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): close review round 3 (3 findings)\n\n- non-HEAD pushes take the FULL gate with their own reason echo\n  (disabling only the fast path left docs-only non-HEAD pushes with\n  LESS checking than HEAD pushes)\n- routing pin is per-section: interactions requires stores.get, every\n  DataStore section requires import_into — the OR form passed a\n  mutation that swapped pipelineRuns' routing for a bare read\n- generated is_pipeline_section_key guard enforces canonical ASCII\n  decimals BEFORE parse (str::parse::<u8> accepts \"+1\"/\"007\"/\"01\" —\n  the old generated doc's \"same closed set\" claim was false); pins\n  assert both that a bare parse would accept each case and the guard\n  rejects it\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-11T19:41:07+02:00",
          "tree_id": "094a2b85d4d048f863f73fcff5f71b2af0ed69e8",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/0e0675c29121704665859c0f346ff31ec20c659e"
        },
        "date": 1786471588704,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2177206,
            "range": "± 16768",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2587690,
            "range": "± 48309",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 296784,
            "range": "± 5522",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "65d5f169ea0b9a59e38e7c46a0cae3bb2e0963ef",
          "message": "feat(pipeline): phase 3 — the quality generation depth (#969)\n\n* feat(pipeline): phase 3 quality-depth rust spine + ipc contract\n\n- pipeline/resume: flat serde artifact types w/ filled examples,\n  stage prompts interpolating the generated prompt_blocks (no third\n  copy), ADR-017 cache keys (PIPELINE_PROMPT_VERSION pinned), six\n  stages behind Pipeline::run_hooked\n- structurally model-proof: evidence quotes verbatim-filtered with\n  status overwritten from the keywords kernel; strategy rebuilt on the\n  seeded roster (model authors only angle/emphasis, never the roster,\n  <=8 plans + condensed group); every Critical deterministic\n- repair <=2 rounds, section-scoped splice, strictly-more-criticals\n  reverts; unsourced_metric located via evidence span (label-only\n  grouping left the commonest Critical unrepairable)\n- commands/resume_pipeline: five commands, RunHooks as the sole\n  emit/persist/stop point, v2 quality-report wrapper\n- run_timeout re-derivation proved the Phase-2 flag right in the\n  dangerous direction: 30 min < the 1800s the three JSON stages can\n  legitimately consume; floor raised to 45 min, pinned both sides\n  against quality_run_deadline; max_tokens documented as unenforced\n- resumePipeline IPC contract + namespaces + mock-client wiring;\n  qualityRunDeadlineSecs in ai-timeouts with renderer>backend lock\n- 18/18 mutation battery (incl. one re-aimed after hitting dead code)\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(renderer): quality-report round-trip preserves depth + fabrications\n\n- pipeline widened from the 'fast' literal to GenerationDepth; parse\n  preserves the stored depth (unknown/missing -> 'fast' for v1 compat)\n  instead of re-stamping\n- parseSlot overlays the validated report onto the raw slot so\n  fabrications and future pipeline-side keys survive the round-trip;\n  fabrications stays deliberately untyped-opaque (typing without\n  validating is the defect class the overlay avoids)\n- mergeRecheckedReport had the SAME defect one layer down (rebuilt the\n  slot from scratch) — a Re-check would still have wiped the review\n  list and Remove/Keep verdicts after the parser fix; now overlays\n- byte-level round-trip + end-to-end persisted-blob tests; 4/4\n  mutations caught (re-stamp, dropped spread, dropped merge overlay,\n  merged-wrapper re-stamp)\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): close phase 3 critic round (19 findings)\n\n- terminal_state derives status+reason together; cancel token consulted\n  on the error path; a failed run can never report \"done\" (cancel\n  mid-draft previously surfaced as failed+done -> renderer success)\n- RunDeadline shared by hooks and repair loop: checked between rounds\n  AND between per-section calls, stopping with RunTimeout keeping\n  progress; deadline re-derived (fixed 1800->4200s, passes 3->1,\n  budget floor 45->75 min) — repair calls are flat-bounded so they\n  belong in the fixed term, not effort-scaled; both lock tests now\n  compute inner bounds from the fan-out constants (the old TS test was\n  an identity that could not fail)\n- regenerate_section acquires the agent_run limiter (was the only\n  provider-calling command with no admission)\n- ensure_latest_run refuses non-latest runs (aggregate-row confusion);\n  per-section repair error = failed attempt, RateLimited = Budgeted;\n  strategy matches roles by name only + emphasis filtered against the\n  grounded evidence map; artifacts compact-serialized with cap sized\n  on MEASURED worst cases (16k; evidence artifact measured 13.2k vs\n  the 3.9k estimate)\n- server-side clamps hoisted from commands/resume.rs; normalized\n  language token in the system slot; fence patterns match attribute\n  form <tag x=..> for all 19 tags (empirically re-run hostile set)\n- Completer seam extracted + 6 end-to-end repair-loop tests; the\n  parse_resume lines 1:1 assumption pinned; stoppedReason widened to\n  nullable in the contract (status is the authority)\n- 21 mutations applied, all caught (2 initially survived — re-aimed)\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(renderer): phase 3 quality-depth ui\n\n- resume-pipeline machine + session hook: coarse run states, stage\n  index/total/phase/attempt, cancel via jobs.cancel, reconnect via\n  initialRunId; TERMINAL DETECTION READS RUN STATUS ONLY (a cancel or\n  deadline stop emits no pipeline:stage event at all — before() errs\n  ahead of its start emit; contract doc corrected) backed by a\n  busy-gated poll; draft consumed via onStream, never awaitAiStream\n- generation-store ADR-006 siblings (runId/pipelineStage/\n  sectionStates); fast path asserted byte-identical\n- depth selection: Settings default + per-run override in both\n  wizards; per-option tooltips + info popover with honest cost table;\n  max shown disabled with reason (backend rejects it this phase);\n  soft small-model warning (never blocks)\n- staged GeneratingPanel (StepDots from index/total, streamed draft\n  display-only); section verdict chips; Fix-this-section ->\n  regenerateSection surfacing the non-latest-run refusal honestly\n- terminal Remove/Keep fabrication review with its own validating\n  parser (the opaque carry is deliberate); entries whose evidence no\n  longer occurs in the document render as \"no longer in the document\"\n- per-job runs list (<=3, status/depth/date + report)\n- en+de i18n (2352=2352 parity); 10/10 mutations caught (one re-aimed\n  after a fixture tripped a sibling guard)\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): close delta round 2 (retry budget, terminal matrix, fence)\n\n- send_with_retry/send_stream_with_retry now OWN the per-request\n  timeout as a total budget (retries get only the remainder — a\n  sequence cannot outlive its bound); 14 call sites dropped their\n  builder .timeout; the streaming twin's duplicated stream_deadline\n  argument collapsed. QUALITY_RUN_FIXED_SECS 4200 is now correct\n  rather than accidental; client margin 60->960s (worst in-flight\n  overshoot is stream_deadline(max)); argued deviation: MAX_ATTEMPTS\n  no longer bounds anything, so no test couples to it — the budget is\n  the bound and IS mutation-pinned\n- terminal_state takes persisted: a boundary RunTimeout with a saved\n  document is needsReview/completed, and the job event moved with it\n  (job_complete whenever status != failed)\n- complete_json takes a required deadline guard composed into charge —\n  the re-ask cannot fire past deadline; single source for the\n  run-timeout message\n- fence neutralizer writes the matched run BACK instead of dropping it\n  (the executed proof showed 3 posting lines deleted); the reviewer's\n  prescribed {0,200} bound was probed and REJECTED — padded attributes\n  survive it; pattern stays unbounded+newline-tolerant, hostile set\n  96 tests green; agent/tools.rs split at R8 (tests -> tools/test.rs)\n- emphasisDropped count on the strategy artifact; repair timing test\n  margins one-sided; is_verbatim cfg(test); ensure_latest_run message\n  honest; TS lock notes the Rust twin is the live guard\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(ui): segmented-control support for the depth selector\n\nPart of the phase 3 renderer build (belongs with 379272e2; missed by\npath staging).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): close round 3 — fence nesting, retry floor, embed budget\n\n- the kept attribute run now has every inner < broken (INNER_LT <\\s?\n  -> \"< \", consuming one ws char keeps it a fixed point) — same-tag\n  nesting like <job_posting x=</job_posting> no longer reconstitutes a\n  live boundary; cross-tag was already safe (probe-settled); hostile\n  set extended with the nested shapes; fenced's bound documented as\n  input-cap (+1 char per broken <)\n- MIN_RETRY_ATTEMPT_FLOOR 2s: a sub-RTT doomed attempt is refused so\n  the actionable 429 outcome is returned, not a generic timeout\n- send_embed_with_retry: 3x per-attempt sequence budget restores the\n  cold-model-load recovery the total-budget collapse removed (embed\n  only; long completion bounds untouched)\n- terminal matrix: BOTH (reason, token-cancel) rows now yield\n  cancelled (RunTimeout-only would be arbitrary; Budgeted has the\n  identical shape); unobservable !token_cancelled term removed from\n  timed_out_with_document rather than left mutation-proof\n- handoff corrected (15 retry call sites; R8 numbers refreshed)\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(renderer): close renderer critic round (10 findings)\n\n- remove now removes: isFabricationResolved requires the verdict and\n  the document to AGREE (keep = settled; remove = settled only when\n  the span is absent); removeEvidenceLines is line-granular, applied\n  through the surface's own editor handler -> debounced commit -> the\n  standard save; verdict recorded even when the apply throws; the\n  still-present span renders \"marked for removal — still in the\n  document\", never green; badge counts against the LIVE text\n- contrast: the five flagged opacity-suffixed sites -> bare remapped\n  -400, plus five more of the same shape found in the new code\n  (STATUS_TONE bare -300, icon/body -400/80); pre-existing instances\n  deferred to a sweep (recorded)\n- clean section chips label from the matched document heading (raw\n  machine key leaked before); chip test asserts label text\n- poll death fixed: undefined-vs-null separated in refetchInterval,\n  session hook errors only when no record ever landed (a blip on a\n  live run keeps polling); use-resume-pipeline.test.ts (8 tests)\n- radio-role cursor exclusion + css guard; pipeline.phase.* i18n en+de;\n  resolveError alert test; aria-controls; resolveRunnableDepth wired;\n  STORE_VERSION 5 with migration step\n- 17/17 mutations caught (4 survivors each produced a better test)\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(pipeline): terminal notifications + divergence semantics\n\n- run_notification pure builder (stream.rs truncation precedent) +\n  notify_terminal at the one call site with a source lock; four cards\n  (completed / needs-review-as-ready / failed / cancelled); unresolved\n  count single-sourced via report::unresolved_count with the\n  unreviewable-critical branch honest; failed card carries no error\n  text (persisted strings can surface as OS banners); backend sends\n  strings per the established convention (no rust-side i18n catalog)\n- divergence documented structurally: resumeText AND report are\n  aggregate-owned, run rows own status/reason/metrics/trail;\n  sourceTextHash is the join and hash-stale is the honest state;\n  record_decision survives text moves (update_texts never touches\n  quality_report — pinned); the Rust merge replaces slots WHOLE (the\n  renderer overlay is what preserves fabrications — both directions\n  pinned); orphaned fabrications deliberately not reconciled (missing\n  evidence IS the success state for a remove verdict)\n- 7/7 mutations caught (one false catch recorded honestly: a\n  compile-error mutation proves nothing, re-applied as deletion)\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(renderer): quality-run entry point on the job detail pane\n\n- TailoredResumePanel sibling to PrepApplicationPanel: DepthSelector +\n  small-model warning + run lifecycle + runs list + report, all reused\n  as built; fast ROUTES to the existing tailor handler (never\n  re-implemented); quality sends the literal depth with no client\n  routing/model fields\n- traps handled: job.completed mid-run is not done (a busy session\n  with a full draft and a running record still renders running);\n  nullable stoppedReason; needsReview says \"not finished — N claims\n  need your verdict\"; non-latest runs open read-only with the reason;\n  Remove withheld here (no editor for the saved résumé — the verdict\n  records and renders marked-for-removal, never claims deletion)\n- real defect found+fixed in the session hook: five early-failure\n  paths (limiter, completer, doc lookup, job text) fail the umbrella\n  job with NO run row — get() answers null and the panel span forever;\n  job.failed now consumed while the machine is busy (not while\n  record-less — that misses a row stuck at running)\n- GeneratingPanel deliberately not imported cross-feature (~25 lines\n  of the same primitives; promotion to components/generation recorded)\n- default résumé with label (no picker precedent to copy); report\n  note field for \"not the newest run\" / \"remove applies in the editor\"\n- 20/20 mutations caught (3 survivors forced fixture/design fixes)\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): notification routes to /jobs where runs now live\n\nThe /ai-generate route predated the entry surface; runs start and are\nreviewed in the job detail pane's TailoredResumePanel on /jobs.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: adr-032 pipeline ownership, adr-033 no model-written memory\n\nPlus domain-model + resume-domain additions for GenerationDepth, the\nrun store, aggregate-vs-run-owned report semantics, and the\nfabrication verdict rule. (Steward wrote these to the main checkout;\nrelocated to the branch so they ship with the PR.)\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(agent): three quality pipeline tools (analyze, report, run)\n\n- tools_pipeline module (tools_quality is at 1213/1400 R8): analyze_job\n  (runs the pipeline's own stage struct — shared cache, not a copied\n  prompt), get_quality_report (store read, honest hash-stale surface),\n  run_quality_pipeline (returns draft+report AS DATA, persists nothing;\n  saving stays behind gated save_resume)\n- run_quality_pipeline gated to improve_resume_tools (Phase 7), NOT\n  prep: the 360s step timeout would kill a 75-min pipeline mid-spend —\n  exclusion pinned off the arithmetic, not a name list\n- two truncation holes closed: fabrications and draft now shrink\n  inside the summary cap instead of being cut mid-JSON by take(cap)\n- pre-existing finding pinned honestly: Budget::max_tool_calls has no\n  producer anywhere; a naive counter would just trade dead variants —\n  documented at the field + a pinning test; real fix (tool suppression\n  once spent) needs an AgentEnv signature change, recorded\n- AGENT_PREP deliberately NOT resized (the ration is per run, not per\n  tool; worst case unchanged at 12 turns / 10 calls)\n- 18/18 mutations caught (one initially survived -> source lock added;\n  one false claim corrected — the const assert is the guard)\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): close the pre-pr ensemble round (2 high + mediums + lows)\n\n- runsForJob invalidated on the session's terminal transition (all\n  three ensemble passes converged on this); writable no longer reads\n  the possibly-stale list for the session's own run — a just-finished\n  run can never be locked read-only with hidden Remove/Keep buttons\n- fabrication entries carry the containing LINE (located at report\n  build, omitted when absent/blank/>1000 chars — never guessed);\n  removeEvidenceLines anchors on exact trimmed-line equality and\n  REFUSES without an anchor (executed proof had evidence \"250\"\n  deleting the contact header via substring match); double-Remove race\n  closed with an applying gate; line whitelisted OUT of the agent-tool\n  summary (measured: including it collapses 20 entries to 6)\n- contract completion-signal paragraph rewritten to the settled truth\n  (get(runId).status is the authority; job.failed covers pre-row\n  failures; stage events are progress-only)\n- cursor rule that actually fires: button[aria-disabled=true] in the\n  unlayered block (the [role=radio] selector was inert — options are\n  buttons); guard test asserts selector + declaration\n- notify label clamped to 160 chars (displacement); generatedAt\n  coerced numeric-or-null in the tool summary; wire types honest\n  (finishedAt/issueCount number|null; criticalCount documented absent\n  -not-null); resumePipeline.run job-kind labels en+de; docs: ADR\n  index rows, ADR-033 real toolset zero-arg semantics, ADR-032 retry\n  claim qualified, ADR-0021 link restored, gitignored-handoff\n  reference inlined\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): unique remove anchors, document-agreement resolve, regenerate status recompute\n\nreview round 1 on #969 (claude workflow), all five findings fixed with\nexecuted mutation evidence:\n\n- containing_line now refuses a span that occurs on more than one line:\n  a bare year or figure anchored on whichever line came first in document\n  order (an education year instead of the flagged job entry), which is the\n  substring failure one level up. uniqueness is text-determined, so\n  re-issue determinism is preserved.\n- report::unresolved_count/has_unresolved/still_needs_review now take the\n  current document texts and apply the renderer's isFabricationResolved\n  rule: a recorded remove counts as unfinished until its span leaves the\n  document. resolve_fabrication can no longer flip a run to completed\n  while the panel still shows a removal pending.\n- regenerate_section recomputes the run row status after the wrapper\n  write via the shared recomputed_status helper (mirror of resolve's\n  clearing arm): a regenerated section that introduces a fresh fabrication\n  now moves a completed run back to needsReview.\n- the absence arm of factual.altered_project_link (a source link missing\n  or altered in the output) no longer enters the remove/keep panel: its\n  evidence is by definition not in the document, so the row asked a\n  question with no correct answer. it stays a blocking critical, same as\n  factual.dropped_role. gate scoped to that one code because\n  unsourced_term evidence is normalized and a general presence gate would\n  drop genuine entries.\n- removeEvidenceLines refuses when the anchor line is duplicated at apply\n  time instead of deleting every copy; removalPendingHint now says where\n  to finish the edit.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-12T05:51:30+02:00",
          "tree_id": "ecc334bf79fb35ba56846f4a4fc605a58d483c76",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/65d5f169ea0b9a59e38e7c46a0cae3bb2e0963ef"
        },
        "date": 1786507387851,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2168362,
            "range": "± 41025",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2549989,
            "range": "± 30129",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 293336,
            "range": "± 10757",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "73dbd99b0c9e6b7b0a894a0944e1d5364a9185e0",
          "message": "feat(pipeline): phase 4 — the max generation depth (#970)\n\n* feat(pipeline): max-depth section artifact types\n\nThe typed answers one max-depth section call may return, plus the seeds a\nsection is generated FROM. The split between SectionSeed and the *Out\ntypes is the core rule expressed as types: identity, project links and\neducation lines are seeded from the source and the model authors only the\npresentation. JudgeItem deliberately has no severity field, so the\nWarning-only cap is structural rather than a filter.\n\n* feat(pipeline): per-section generation for max depth\n\nOne structured call per resume section, in a fixed source-gated order, with\nevery answer filtered before it is kept: unbacked citations dropped, a\nskill the source never states removed, an education entry that is not a\nverbatim copy discarded, and a project's name/links/stack re-seeded from\nthe source whatever the model returned. The fan-out checks the run\ndeadline between calls and treats one section's failure as one section,\nnever as the run's.\n\nThe projects-format primitives (entry boundaries, link detection, the\ndescription-line cap) are re-exported from validate::content rather than\nre-implemented, so the generator and the checks that grade its output\ncannot disagree about a truthful document.\n\n* feat(pipeline): assemble the max-depth sections into a resume body\n\nThe finished sections become the SAME plain-text body the quality draft\nproduces, so validate, repair, the report, the editor and the exporters\nsee one shape at both depths. The layout is code-owned: the identity line\nis rendered in the form split_entry reads back, consecutive employment\nentries share one heading, and the projects ladder renders exactly the\nthree shapes consistency.project_structure accepts — a project the source\nnever described gets the compact link line, never a filler blurb.\n\nThe progressive stream appends the same bytes the document will carry, so\nwhat the user watches build is what the run assembles.\n\n* feat(pipeline): run the staged pipeline at max depth\n\nmax_pipeline() + MAX_STAGES, Budget::RESUME_MAX and its own run deadline,\nand the L3 wiring that lets one execute() serve both depths through three\nlookups instead of two copies of the shell. RunHooks gains the\nsection-wise half: per-section pipeline:stage events with sectionKey\nfilled (validated by the generated grammar) and the progressive document\nstream on the existing ai:stream channel, display-only at both depths.\n\nThe max deadline deliberately does NOT buy the re-ask worst case the\nquality one does: 3h20m is a backstop no user sits through, and a max run\nstopped mid-fan-out keeps the sections it already assembled.\n\n* feat(pipeline): the max-depth llm judge, warning-only and skippable\n\nOne structured call reviewing the finished document, contributing advisory\njudge.* Warnings to the report. Three structural caps: JudgeItem has no\nseverity field, every issue is built with Severity::Warning written at the\nconstruction site, and every failure mode (provider error, expired\ndeadline, refused daily cap, unparseable answer) records a reason code and\nreturns Ok — the judge can never fail a run.\n\nDeviation from the settled stage order, with the reason: the judge runs\nAFTER repair, not between validate and repair. repair replaces ctx.report\nwith the one it re-validated, so notes merged in earlier are dropped on\nexactly the documents that were repaired, and a quote taken before a\nrepair names text the finished document no longer contains.\n\n* feat(pipeline): regenerate one max-depth entry from its persisted artifacts\n\nAt max depth the strategy and evidence artifacts are persisted in FULL\nalongside their counts, in the DB event row only — ADR-027 is the LOG\nboundary and the log lines stay counts-only. A later 'regenerate this\nrole' click reads them back and rebuilds exactly that entry through the\nsame generate_one and the same assemble the run used, splicing only its\nline range so the other roles cannot change.\n\nEvery miss is soft: a quality run, a non-experience key, a run older than\nthe detail, an artifact the store's clamp truncated, or an entry the\ndocument no longer has all fall back to the whole-section rewrite. A click\nmust not fail because an optimization was unavailable.\n\n* style(pipeline): satisfy clippy on the max-depth tests\n\nassertions_on_constants: two shipped budgets compared at runtime prove\nnothing a const block does not prove at build time. filter_next: rfind.\n\n* feat(renderer): enable max generation depth\n\nThe staged pipeline runs max now, so the depth flag flips and the panel\nsends the depth the user picked instead of the quality literal. The\nunavailable note follows the same rule as the run: both staged depths\nneed the same server-resolved inputs, so it shows for either.\n\n* feat(renderer): map the max-depth stages onto the run machine\n\n`sections` and `assemble` are max depth's split of `draft`; `llm_judge`\nruns last, after repair, and reads the finished document, so it maps to\nthe checking state rather than the repairing one. A max run therefore\nends its stage trail in `validating` — still busy, because no stage\nevent may ever end a run.\n\n* feat(renderer): fold per-section progress off the stage stream\n\nMax depth reports each section through the same `pipeline:stage`\nchannel, so the session hook folds `sectionKey` + phase into a\nper-section map. Only states an event proves are written: `queued` is\nnot observable (the wire carries the STAGE's index/total and the section\ncounts ride in the persisted artifact), and neither validate nor repair\nattributes its counts to a section, so verdicts stay with the report.\n\n* feat(renderer): live section timeline for a max-depth run\n\nA checklist of the sections the run has actually reported, in generation\norder, hosted next to the stage dots. No placeholder rows: the roster\ndepends on the strategy's company plan and never reaches the renderer,\nso a pre-drawn list would name sections that may never be generated. An\nexperience entry is numbered rather than given a company name the\ncontent-free wire never carried.\n\n* docs(renderer): the display-only stream carries max depth's assembly too\n\nNothing was gating the stream on depth, so nothing needed ungating: both\ndepths ride one `ai:stream` under the run's umbrella job. The doc said\n\"the draft stage's deltas\", which is only half of what arrives now, and\na test pins that the pane renders the progressively assembled text\nwithout a branch — still display-only.\n\n* feat(renderer): label the max-depth judge's remarks as model-written\n\nA `judge.*` message is the model's own prose about this document, not a\nlabel for a fixed check, so the panel renders it verbatim: the namespace\nshort-circuits the catalog lookup structurally, before `i18n.exists`,\nrather than relying on nobody adding the key. It carries a provenance\nbadge — the note is free text derived from an untrusted posting, and it\nstays an inert text node. Per-section repair attribution still does not\nexist at either depth; the note in SectionVerdicts now says so.\n\n* feat(shared): a max-depth run deadline the client can be held above\n\nMirrors `timeouts::max_run_deadline` (6900 s fixed + one effort-scaled\npass, 7200–7800 s) so the renderer has a bound that stays ABOVE the\nbackend's at every tier — the side that knows why a run stopped has to\ngive up first. Hand-mirrored rather than generated, because gen:ipc does\nnot emit the Rust constant; the per-tier table is pinned as literals on\nboth sides so the pair cannot drift silently.\n\n* fix(pipeline): f2 — join a per-entry regenerate on identity, never on position\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(renderer): give the section ladder the `done` rung it needs\n\n`PipelineSectionState` never had `done`, so the fold that writes it on a\n`sections` finish did not type-check — vitest runs esbuild and said\nnothing; `tsc` caught it. Folding onto `checking` instead would claim a\ncheck that has not started, so the type gains the rung the plan's ladder\nalways described. `max` is also a runnable stored depth now, so the\npreference coercion needs a value from outside the vocabulary to stay\nreachable.\n\n* fix(pipeline): f3 — let the zero-call stages run when the deadline expires\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): f4 — delete a posting's run trail with its application\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): f5, f6 — re-cap the report after the judge and count its call\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): f7, f8 — keep the artifact detail off the wire, order partial section lists in place\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): f9-f12 — charge the judge's call, keep the entry separator, drop dead helpers\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(shared): re-sync the max-run deadline with the recomputed backend\n\nThe Rust fix round counts the judge as the 24th flat-bounded call, so\n`MAX_RUN_FIXED_SECS` is 7_200 and the per-tier table moves to\n7_500…8_100 with a 125-minute budget floor. The fan-out assertion is now\nstrict: the pre-judge 6_900 put the bottom tier exactly level with the\ncalls it wraps, and a deadline that leaves the last call no room to\nreturn is the binding constraint this derivation exists to avoid.\n\n* fix(pipeline): f10 — a repair round that loses content is worse than the count says\n\nround_is_worse keeps its count term and gains an absence term: a round is\nworse if the candidate carries an absence-shaped Critical (code, evidence)\npair the draft did not. Absence-shaped is factual.dropped_role plus\nfactual.altered_project_link's absence arm, discriminated by the same\nevidence-presence test report::fabrications uses to keep that arm out of the\nreview panel — so the two places that decide what an absence is cannot\ndisagree.\n\nPairs, not codes: a document that already lost a role stays repairable, while\na round that swaps WHICH employer is missing is caught.\n\nThis also closes the pre-existing QUALITY-depth hole. It is the same loop and\nthe same draft: a rewrite that traded two fabricated metrics for one dropped\nemployer scored 1 < 2, was kept, and deleted a job from the saved resume —\nwith no reviewable finding for the user to act on, because an absence has no\nspan.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(renderer): make the section timeline audible and honestly live\n\nThe host's stage caption cannot announce section progress: a section\nevent carries the STAGE's index/total, so its text is frozen for the\nwhole sections stage and an unchanged live region says nothing — one\nutterance, then silence for the run's longest phase. Adds the prep\npanel's milestone announcer (sr-only, once per transition) and a\nwritten-count the sighted user can watch move, counted against sections\nSEEN because the roster is not on the wire. The list is a list again:\nrole=\"group\" dropped list semantics for a rationale that role never\ncontrolled. Depth-agnostic unavailable copy now that max is selectable,\nthe always-visible max summary carries the two-hour cue, and an\nunparseable entry index renders \"?\" instead of NaN.\n\n* fix(pipeline): h1 — reorder employment entries as one block under one heading\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): h2 — join a per-entry regenerate on the whole seeded triple\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): h3 — cascade the run trail when a generated resume is deleted\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): m1, m2 — hand apply_stop the stage, and floor what a stopped run may overwrite\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): m3 — one spelling of a posting url in the runs store\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): round-2 lows — evidence pin, marker edge, and the accepted trades\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): normalize an imported run's job url, and gate the persist floor on the outcome\n\nA restore bound run.job_url raw while every other write site normalized, so\nany backup taken before the normalization landed restored rows no reader and\nno delete could reach: the runs panel came back empty, delete_for_job matched\nnothing and still reported success, both cascades silently no-opped, and the\nstrategy/evidence detail outlived the owner meant to remove it. data_import\nruns against the live store with no re-open, so the open-time sweep never got\na chance either.\n\nis_persistable now needs the run to have FAILED, not merely to carry a stop\nreason: stages::repair records RunTimeout/Budgeted for a recovered in-loop\nstop and returns Ok, and repair is quality depth's last stage — so a\ncompleted run over a source with no employment section silently saved nothing\nwhile reporting completed.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): gate the save on the source's work history, not on the run\n\nis_persistable read the run instead of the documents, and both readings were\nwrong in their own direction. The stop reason refused a completed run over a\nsource with no employment section once repair hit the daily cap; the outcome\nmissed the case the gate exists for, because the sections fan-out treats a\ncap refusal as Budgeted, breaks, and returns Ok - and nothing downstream\nturns that into an Err. A max run capped right after Summary therefore had\noutcome Ok and overwrote the saved resume with a summary-only document.\n\nComparing the two documents answers both: refuse only when the source has an\nemployment section and the draft does not. A new-grad or academic CV can\nnever trip it; a truncated fan-out over a real work history always does.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): purge the run trail only after the parent delete commits\n\nFive advisory findings from the ensemble round.\n\nThe applications cascade fired before s.delete and was not gated on it, so a\nSQLITE_BUSY or IO failure left the application alive with its trail already\nirreversibly gone - the ordering ai_generations_remove refuses by name. The\nmax deadline's two terms are now emitted by gen-ipc-rust.ts beside the\nquality pair instead of living as two independent literals whose TS-side doc\nclaimed they could not drift. The judge's dropped counter no longer charges\ncap-truncated valid remarks as verbatim-quote failures. plan_sections'\nbudget trim now reports what it cut, so a 13-slot plan losing Education is\nvisible in the artifact instead of silent.\n\nThree stale comments corrected: max depth is admitted now, report_section\nhardcodes attempt 1 like its sibling, and a per-section cache key buys\ncollision avoidance rather than per-entry invalidation.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(contract): the staged pipeline runs both depths now\n\n* docs: update phase 4 (max depth) from planned to shipped state\n\n* fix(pipeline): bound bulk-delete parameter lists and spare a kept generation's trail\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): report a refused save as a failed run, never a keyless section event\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): drop unrenderable sections, hide missing evidence, parse once\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(pipeline): lead the max deadline doc with its summary, share one note cap\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(knowledge): point the max pipeline docs at their source symbols\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): a run whose posting was deleted mid-flight abandons its own output\n\nupsert_run is INSERT OR REPLACE and save_application is a merge-upsert, so a\nrun that finished after its posting was deleted re-inserted the run row and\nre-created the ai_generations aggregate - the posting came back in the runs\npanel, the Documents list and the next backup, with a permanently partial\ntrail because the pre-purge events were already gone. Confirmed by running\nit: purge, one event appended into the gap, terminal upsert, row back with 1\nevent where the run produced 2.\n\nThe run now checks its own row at the terminal write. Gone means some delete\ndoor removed this posting - applications, ai_generations, factory reset or a\nrestore - so the run skips persist, skips the row, sweeps the gap's events\nand reports cancelled. A terminal check rather than cancel-and-await because\nnothing maps a job_url to an in-flight run's cancel token, and the in-flight\nwindow would land here regardless.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-12T15:39:43+02:00",
          "tree_id": "84712d673929e9b60fdd0f22853cb9cee430e5cc",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/73dbd99b0c9e6b7b0a894a0944e1d5364a9185e0"
        },
        "date": 1786542672627,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2140773,
            "range": "± 10420",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2520199,
            "range": "± 17773",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 289610,
            "range": "± 6544",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "49699333+dependabot[bot]@users.noreply.github.com",
            "name": "dependabot[bot]",
            "username": "dependabot[bot]"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "a928665eb280da6650ab27b316232bea38020c36",
          "message": "chore: bump tauri-winrt-notification in /apps/desktop/src-tauri (#975)\n\nBumps [tauri-winrt-notification](https://github.com/tauri-apps/winrt-notification) from 0.7.2 to 0.8.1.\n- [Release notes](https://github.com/tauri-apps/winrt-notification/releases)\n- [Changelog](https://github.com/tauri-apps/winrt-notification/blob/dev/CHANGELOG.md)\n- [Commits](https://github.com/tauri-apps/winrt-notification/compare/tauri-winrt-notification-v0.7.2...tauri-winrt-notification-v0.8.1)\n\n---\nupdated-dependencies:\n- dependency-name: tauri-winrt-notification\n  dependency-version: 0.8.1\n  dependency-type: direct:production\n  update-type: version-update:semver-minor\n...\n\nSigned-off-by: dependabot[bot] <support@github.com>\nCo-authored-by: dependabot[bot] <49699333+dependabot[bot]@users.noreply.github.com>",
          "timestamp": "2026-08-12T18:56:33+02:00",
          "tree_id": "d90f3a0a1abca830dbe5dddc25e2c15d377cc695",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/a928665eb280da6650ab27b316232bea38020c36"
        },
        "date": 1786555930066,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1689754,
            "range": "± 71317",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2044701,
            "range": "± 22227",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 195875,
            "range": "± 13466",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "7cb303671e3033df962f8af4dd9f8f5bcecd7818",
          "message": "chore(deps): sentry 0.49 with tauri-plugin-sentry 0.6, gated at the wire (#984)\n\n* chore(deps): drop the no-op reqwest rustls-native-certs feature\n\nreqwest 0.13.1 exposed `rustls-native-certs` only as the implicit feature\nof an optional dependency it never references in its own source, so\nenabling it changed nothing: OS trust roots already arrive through\n`rustls-platform-verifier` (pulled by reqwest's `rustls` feature) and\nthrough the system stack behind `native-tls`.\n\nreqwest 0.13.2 removed the optional dependency, which turns the stale\nentry into a hard resolve error. Unblocks any bump that floats reqwest\npast 0.13.1.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* chore(deps): bump sentry 0.42 -> 0.49 with tauri-plugin-sentry 0.6\n\nThe coordinated bump dependabot could not do. tauri-plugin-sentry 0.6\nbuilds against sentry 0.49, so the plugin-only PR (#972) duplicated\nsentry/sentry-core/sentry-types in the lock; both sides move together\nhere. sentry-rust-minidump follows transitively, 0.13 -> 0.17 (its\nlib.rs is byte-identical between the two, so no supervisor API change).\n\nRe-verified the four traps the Cargo.toml comment block records:\n\n* Single copy — sentry, sentry-core, sentry-types all resolve to exactly\n  one 0.49.1, shared by the app, the plugin, and the minidump crate. The\n  stale reqwest 0.12 lock entry sentry 0.42 dragged along is gone, so\n  reqwest is now single-version too (0.13.4).\n* Transport — `ureq` and `native-tls` still exist as features in 0.49\n  and still select `UreqHttpTransport`; sentry's resolved deps are\n  httpdate, native-tls, sentry-*, ureq, with no reqwest and no tokio.\n  0.49 also adds `logs` + `metrics` to its defaults; both stay off, they\n  are new egress surfaces ADR-0020 never consented to.\n* Capability — `sentry:default` still expands to allow-envelope +\n  allow-breadcrumb in the generated ACL manifest, unchanged from 0.5.\n* Init + consent — init still runs before tauri::Builder and still forks\n  the supervisor from the same call. `ClientOptions` became\n  #[non_exhaustive] in 0.49, so the struct literal moves to the builder;\n  every field keeps its previous value.\n\nNo paired npm package exists: the browser SDK is a js_init_script inside\nthe crate, so there is nothing on the JS side to bump.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* chore(deps): reconcile the lock after rebasing over the dependabot bumps\n\n* fix(crash-reporting): enforce consent and drop opaque envelopes at the wire\n\nSecurity review found real, new egress in tauri-plugin-sentry 0.6, and a\nclaim of mine that was simply false. `before_send` is not the last hop:\nit has exactly one call site in sentry-core 0.49.1 (client/mod.rs:383,\ninside the capture_event preparation). Anything handed to\n`Client::send_envelope` reaches the transport with no callback between.\n\nThe plugin uses that door. When a renderer envelope fails to parse — it\nroutinely does, because @sentry/vite-plugin's debug-ID injection writes\na `debug_meta` sourcemap image sentry-rust cannot deserialize — 0.6\nrebuilds it via `Envelope::from_bytes_raw` and calls `send_envelope`\n(commands.rs:30-35). Those bytes live in a private `Items::Raw` that\n`to_writer` copies to the socket verbatim, so no redaction of ours can\nreach inside. Plugin 0.5 dropped them; 0.6 ships them.\n\nSo the guarantee moves to the only place that sees every byte. A\n`GuardedTransportFactory` wraps the SDK's own DefaultTransportFactory\n(ureq under our features) and enforces, per envelope:\n\n  * consent, re-read from an atomic mirror of the consent file. This\n    also closes the mid-session opt-out hole: `Hub::current()` is\n    thread-local, so `disable_current` only unbound the calling thread,\n    and the plugin's own Client clone in Tauri state never consults a\n    hub at all.\n  * opaque envelopes dropped, restoring 0.5's semantics. A payload we\n    cannot inspect is one we cannot redact; the privacy guarantee\n    outranks the sourcemap edge case. Counted and logged content-free.\n\nflush/shutdown delegate, so the supervisor's pre-exit flush still works.\n\nAlso here:\n  * `.enable_logs(false)` / `.enable_metrics(false)`. Both default to\n    TRUE in 0.49 (0.42 had them off) and the field is not cfg-gated, so\n    any future crate enabling `sentry/logs` would unify the feature in\n    and open the pipe silently.\n  * deny.toml bans `failspot/enabled` — a fault-injection crate riding\n    in under the crash reporter, inert as locked (its lock entry has no\n    dependencies, so `dep:flagset` is off) and it must stay that way.\n  * Corrected three comments this falsified: lib.rs on renderer events\n    inheriting before_send, the module docs on where redaction is\n    enforced, and `disable_current`'s claim to drop \"every subsequent\n    event\".\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(crash-reporting): stop overclaiming what the options test guards\n\nMutation-checking my own test found one assertion that does not guard\nwhat its doc said it did. Deleting `.send_default_pii(false)` leaves the\ntest green, because `false` is also `ClientOptions::default()` — so that\nline catches a wrong value but not a deleted call, and no test could\ndistinguish the two.\n\nThe other six were each verified by deletion: transport, before_send,\nbefore_breadcrumb, enable_logs, enable_metrics, server_name all turn the\ntest red when their builder call is removed.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(crash-reporting): scope the cold-path claim and pin the bump checklist\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-12T20:49:53+02:00",
          "tree_id": "6eff9b79f84978c088d36a091d0ca860914da573",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/7cb303671e3033df962f8af4dd9f8f5bcecd7818"
        },
        "date": 1786561376433,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2331223,
            "range": "± 98705",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2735030,
            "range": "± 40233",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 306445,
            "range": "± 3594",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "665505a826fa7ff683736aae0db4b285c1ebd86a",
          "message": "feat(pipeline): phase 6 — per-stage models and the local-model advisor (#985)\n\n* feat(shared): codegen the pipeline stage vocabulary into rust\n\nPIPELINE_STAGES is the order-preserved union of QUALITY_STAGES/MAX_STAGES,\nemitted into ipc_contracts::events alongside the phase vocabulary it shares a\ncontract with. A Rust test pins both directions: a depth stage missing from the\ngenerated list, and a generated name belonging to no pipeline, both fail.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(ai-config): per-stage provider/model overrides\n\nNew ai_stage_overrides table on the existing AiConfigStore connection (two\nappended migrations; element 1 untouched): stage PRIMARY KEY drawn from the\ngenerated vocabulary, provider + model + base_url + context_window per row.\n\nRows go through the SAME validate_settings chain as the active provider config\nat write time, and the resolver re-validates at read time because a store can\nalso be filled from an untrusted bundle. snapshot()/apply_snapshot_conn/\nclear_conn all carry the table, so backup, restore and factory reset cover it;\nimport drops invalid stages/providers/base_urls and is bounded by the\nvocabulary size.\n\nAlso adds the nullable context_window column to ai_provider_config — the\nstaged pipeline's only honest source for num_ctx (see the following commit).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(pipeline): resolve a completer per stage\n\nCompleter::from_active_for_stage falls through to from_active unless the user\nexplicitly set a row; Completer::for_stages resolves the run's map ONCE before\nthe first stage so a mid-run edit cannot split a document across two models.\n\nThe cache-key coupling is the load-bearing part: StageCacheKey::rebound binds\neach stage's key to the provider+model THAT stage calls, reached through the\nsame pick() as completer_for, so an artifact can never be filed under a model\nthat did not produce it. An override-free run produces byte-identical keys.\n\nPer-section regenerate resolves through the same path (it is the sections stage,\none section wide). charge_daily now spreads across per-provider buckets — stated\nat the function.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(pipeline): thread the configured context window into staged calls\n\nThe staged pipeline hard-coded context_window: None at all three request sites,\nso Settings' modelLimits.contextWindow reached the renderer's fast path and\nnothing else — the slider silently did nothing at quality/max depth.\n\nSource of truth: a nullable column on ai_provider_config (the window for the\nmodel in that row, replaced with it) and on ai_stage_overrides (an override\nnames a different model, so it carries its own). Not a second store, and never\na guessed size — absent stays absent all the way to the adapter.\n\nstream_complete and structured_call now share one text_request builder, so the\nwindow has a single place to be forwarded from; draft.rs reads it off its own\nresolved completer. Embeddings stay untouched (that path ignores num_ctx by\ndesign).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(spend): record reported thinking tokens\n\nAppended migration adds a NULLABLE thinking_tokens column — NULL, not 0,\nbecause 'not reported' and 'no reasoning' are different facts and every\npre-existing row is the former.\n\nTwo providers report the split and are now parsed: OpenAI\n(completion_tokens_details.reasoning_tokens) and Gemini\n(usageMetadata.thoughtsTokenCount, which this app already opts into via\nthinkingConfig.includeThoughts). Anthropic folds thinking into output_tokens\nand Ollama's eval_count includes the thinking channel, so both stay None —\nthe renderer's live char-based ratio is a different unit and is deliberately\nnot written here as if it were tokens.\n\nrecord_usage now takes the whole Usage so an adapter-parsed field cannot be\ndropped on the way to the store. ai_spend_summary gains thinkingByModel:\nper-model totals over ONLY the calls that reported, so the ratio's denominator\nmatches its numerator and an unmeasured model is absent rather than zero.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(ai): stage-override ipc commands\n\nai_stage_overrides / ai_set_stage_override / ai_clear_stage_override in\ncommands/ai (287 LOC headroom before, well inside the cap) plus the lib.rs\nregistry; nothing added to commands/ai_provider/mod.rs, which has 78 left.\n\nThe contract states the load-bearing part for the renderer track: an ABSENT key\nmeans the active provider, not an override equal to it — so a suggested default\nrenders as a suggestion until the user applies it.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(settings): send the context window on every provider-settings save\n\n`ai_set_provider_settings` REPLACES all four fields, so the three UI call\nsites that each sent a subset were nulling the others: picking a model\ndropped the base URL, saving a base URL dropped the model, and nothing\never sent `contextWindow` at all — which is why the Settings slider did\nnothing at quality/max depth (the staged pipeline reads the stored row,\nnot renderer preferences).\n\nField resolution now lives in one pure `resolveProviderSettingsWrite`\nbehind `useSaveProviderSettings`, so the window follows the MODEL being\nsaved and is never re-attached to a different one. The slider commits on\nrelease (pointer + key) instead of only writing Zustand.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(settings): per-stage model overrides section\n\nOne row per pipeline stage that actually calls a provider (the generated\nPIPELINE_STAGES vocabulary minus assemble/validate, which make no call\nand are refused server-side). A stage with no override reads \"default —\n<active model>\" rather than showing the active model as if it were\npinned: an absent row means the stage follows the active provider, and\nwriting that back would freeze it on today's model.\n\nAn override whose provider is unconfigured warns on the row before the\nrun, because the backend refuses at run time instead of falling back.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(settings): suggest a smaller installed model for the reading stages\n\nanalyze_job/match_evidence/strategy only read the posting and the résumé\nand answer in JSON, so a smaller installed model does that work just as\nwell and much faster. Offered as a suggestion with one-click accept — the\nLocalModelLimits 'Use suggested' contract — never a silent switch, since\naccepting writes three real override rows.\n\nSilent when there is nothing honest to say: a cloud provider, no smaller\ninstalled model, an embedding model as the only candidate, or all three\nstages already pinned. Size comes from the same getModelTier that picks\nthe prompt tier, not a second table.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(settings): model advisor wizard for local models\n\nFour guided steps behind a button on the AI tab: what is installed (per\nmodel /api/show), whether each window holds the real stage prompts, what\nto change, and which combinations are known to go wrong.\n\nContext fit compares each window against the Rust prompt caps summed per\nstage — draft's documented ~32k chars is reproduced by the same arithmetic\n— minus the answer headroom, and NAMES the stages that would be cut. The\nfence truncates with no marker, so this is only useful before a run.\n\nRisk uses both signals: the provider-reported thinking split where it\nexists, and the model-name heuristic, which is the only signal a\nlocal-only user can have (Ollama folds thinking into its own count). An\nunmeasured model reads 'not measured', never 'safe'.\n\nThe wizard writes nothing but per-stage overrides, through the same accept\npath as the settings section; server-level tuning is printed as advice to\nrun yourself, and web search is untouched (ADR-0023).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(settings): take the free-stage list from the shared vocabulary\n\nThe spine now exports PIPELINE_STAGES_FREE, pinned against both depths'\nfree_stage_names(). Consuming it drops the renderer's own copy of\n[assemble, validate] — one owner for the set the backend refuses an\noverride on. Also records where the provider-settings writer collapses to\nif the command gains non-destructive absence.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(settings): follow the provider-settings patch contract\n\nsetProviderSettings became PATCH per field (omitted keeps, null clears,\na value sets), so the writer now sends only what changed instead of\nre-sending the whole row.\n\nOne rule stays: a stored context window describes the model it was stored\nwith, so a MODEL change re-points it to that model's own window — or\nsends an explicit null. Omitting it there would silently run the new\nmodel at the old model's num_ctx, which Settings would still render as\ncorrect.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): bind the stage cache key to the context window\n\nHIGH from review. context_window became an output-affecting input the moment\nthe staged pipeline started sending it (structured_call -> text_request ->\nollama num_ctx decides how much of the prompt the model sees), but the key\nstill hashed only version/provider/model/chain — so an answer produced at 4096\ncould be served to a run that asked for 32768. A clean regression from the\nthreading commit, not a pre-existing gap: before it, every term the cached\nstages could vary was already in the key (text_request hard-codes effort and\ntemperature to None, and the structured path resolves temperature inside the\nadapter from provider+model).\n\nThe key now hashes version/provider/model/context_window/chain via a\nStageIdentity value, so a new routed input means a new field there rather than\na term someone has to remember. One-time invalidation of existing entries: a\nchanged key can only cost a call, never serve a wrong answer.\n\nAlso closes the MEDIUM on the QualityCtx binding: pick+rebound now live in one\ngeneric stage_cache_key_for that production instantiates at Completer and the\ntest instantiates at StageIdentity, so 'the key follows the override' is a test\ninstead of a claim. The residue that still needs an AppHandle is named\nline-by-line at the test module.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ai-config): patch semantics for provider settings\n\nMEDIUM from review: REPLACE semantics failed on first contact — three renderer\ncall sites each saved one field and erased the other two, and a doc comment\nsaying 'send them all' is not a mechanism. Absence is what a caller produces by\naccident, so absence is now the harmless case.\n\nPer field: absent keeps the stored value, explicit null clears, a value sets.\nTelling absent from null needs serde's double_option, which needs a struct\nfield, so the command takes one ProviderSettingsPatch instead of loose\narguments — the contract's own signature is unchanged, only the client's invoke\nbody wraps it. The merge runs under the same lock as the write, and the MERGED\nrow is validated, so a patch that only changes the model is still rejected\nagainst the stored base_url's provider.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ai-config): refuse overrides on stages that make no ai call\n\nMEDIUM from review: assemble and validate are live stages that ask no model\nanything, so an override on either was a control the user could set and never\nobserve — and worse, Completer::for_stages resolves every stored override with\n? before the run starts, so one malformed row on a stage that never calls a\nprovider could abort the whole run.\n\nRefused at write AND at import, plus for_stages is now scoped to the stages a\ndepth can actually pay for. The reject set is codegen'd (PIPELINE_STAGES_FREE)\nbecause the override table lives in an L1 store that cannot reach the L2\npipeline it derives from; a Rust test pins it against free_stage_names() for\nBOTH depths, since 'free' is per-depth and only a stage free in every depth\nthat contains it belongs in the set. MAX_STAGE_OVERRIDES follows the overridable\nvocabulary rather than the whole one.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): re-validate the stored context window at resolve time\n\nMEDIUM from review: the module doc promised every stored value is validated\n'again at READ/RESOLVE time', but from_override re-validated only base_url\nwhile deliberately skipping context_window — under the same hand-edited-store\nthreat model. num_ctx is the one stored number whose absurd value is an\nout-of-memory kill of the user's machine rather than a wrong answer, so it now\ntakes the same gate, fail-closed, on both the active-config and the override\npath.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(ai): review follow-ups on wording and regenerate routing\n\nLOWs from review.\n\n- OpenAI reports reasoning_tokens as a measured 0 on current non-reasoning Chat\n  Completions models rather than omitting it, so Some(0) is a real observation\n  and None means the field was genuinely absent. Reworded in Usage, the parser\n  and the TS contract; the data recorded was correct either way.\n- Per-section regenerate now resolves each sub-path against the stage whose\n  prompt it actually uses — sections for the max artifact rebuild, repair for\n  the text-rewrite fallback (the only one of the two that exists at both\n  depths) — resolved lazily so a click resolves only the path it takes.\n- The R3 allowlist entry says split for cohesion, not for the LOC cap.\n- The import path scrubs an out-of-range context window as a FIELD, keeping the\n  row, matching scrub_settings; the interactive writer still errors, because\n  there a human typed it and can be told.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(pipeline): validate the stored window in the testable seam\n\nMEDIUM from re-verify: the fail-closed re-validation on the active-config\negress path sat in from_active, which needs an AppHandle, so no unit test could\nreach it — replacing it with cfg.context_window left all 4038 green. An egress\nguard nobody can call is a guard nobody notices the deletion of.\n\nMoved into from_config, next to the base_url check it mirrors: AppHandle-free,\nso it is now pinned exactly like its sibling, and both defensive checks live in\none place instead of straddling the seam. from_config returns the window it\nvalidated (a 4-tuple, matching from_override).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): resolve the sections override only on the path that uses it\n\nMEDIUM from re-verify: the comment claimed each regenerate sub-path resolved\nits routing lazily, and for the sections completer that was false — it was an\nargument, so every click resolved it, including the quality-run fallback that\ngoes to repair instead.\n\nNot just a wrong comment: resolving propagates its error, so a malformed\nsections override would fail a quality-run click over a stage that depth does\nnot even have — the same defect class as resolving overrides for stages that\nmake no AI call. Moved inside regenerate_max_entry past its three early\nreturns, the first point at which the max path is certain to run.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(ai-config): pin the free-stage belts, and retract a false claim\n\nMEDIUM + LOW from re-verify.\n\nThe claim on import_drops_an_override_on_a_free_stage did not reproduce — the\ncritic re-ran it and so did I. Reverting the filter to is_pipeline_stage leaves\nthat test green, because validate_stage_override's own arm still drops the row\nthrough the else-continue. The test pins the OUTCOME, which two layers\nguarantee; what pins the FILTER is the cap test, since the filter runs before\ntake(MAX_STAGE_OVERRIDES) and is what stops an inert row from eating a slot a\nreal stage needed. Both comments now say which is which, and the retraction is\nrecorded where the wrong claim was.\n\nNew pins for two belts that had none:\n\n- paying_stages' free filter, for BOTH depths (free is per-depth). Neutering it\n  to |_| true now fails.\n- The read side. Written straight into the table, since the writer refuses it —\n  the case this belt exists for is a row from an older vocabulary.\n\nThe read-side test names what it does NOT pin: the early return in\nstage_override is a redundant fast path, not a belt (it reads through\nstage_overrides_conn, which filters anyway), so deleting it changes no test —\nand no comment now pretends otherwise. The load-bearing filter is the row-level\nguard in stage_overrides_conn, and that is the one the mutation hits.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(shared): barrel-export the paying-stage guard\n\nLOW from re-verify: isPayingPipelineStage had no barrel export and no\nconsumers, so knip flagged it. Exported rather than deleted — the renderer's\nstage-routing derives the same set by filtering PIPELINE_STAGES with\nPIPELINE_STAGES_FREE, and this is the shared form it adopts. knip's finding is\ngone; the two remaining unused types in documents.ts are pre-existing.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(services): guard the window patch, cap the inspect fan-out\n\nThree review findings in the write path:\n\n- an unresolved activeConfig query reads as \"no stored row\", and the\n  model-changed branch treated that as license to send contextWindow:\n  null — clearing a real window on an unchanged model. Clearing now\n  requires a row actually read; a window the renderer holds for the model\n  is still sent, since that destroys nothing.\n- the context-window slider surfaces a rejected write instead of showing\n  a value the backend refused, and skips a release that moved nothing.\n- useModelInspections fanned out one /api/show per installed model with\n  no cap, at the same local server serving generations. Capped at 4\n  through a FIFO limiter (useQueries has no concurrency option).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(settings): measured sizes for the suggestion, one warning per problem\n\nFE-1: the suggestion ranked models with getModelTier, whose name parse\nanswers 'small' for any tag it cannot read — it recommended a 70B model\nas smaller than an 8B one, an embedding model for three JSON stages, and\na reasoning-heavy model the advisor's own last step warns about. It now\nrequires a MEASURED parameter size on both sides (/api/show), a 2x gap,\nand a chat family; anything unmeasured produces silence.\n\nUX-4: a broken active provider rendered as seven identical row warnings\ntelling the user to put each step 'back on the default' — which is not\nsomething you can do to a step already on the default. Row warnings are\nnow only about that row's own override; the active provider is stated\nonce at card level.\n\nAlso: AA contrast on every status/muted class, focus returned to the row\nbutton when the editor closes, aria-controls + a Close label on the\nexpander, partial-failure reporting and a 'Not now' decline on the\nsuggestion, and the advisor entry gated off a cloud-only machine.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(settings): honest advisor states, computed worst case, measured-light\n\n- FE-3: the copy claimed the draft step is the biggest prompt; sections\n  is 2.1x larger by this module's own table. The stage is computed and\n  named, not asserted.\n- FE-4: while /api/show is in flight every model rendered as 'Not\n  measured' — a claim about the model, not about the pending request.\n  Skeletons until the probes settle.\n- FE-5/UX-3: context-fit and known-bad-combos had no empty state; a\n  cloud-only machine got intro prose, nothing, then an ominous footnote.\n- FE-6: a provider-measured, perfectly normal ratio fell into\n  'unmeasured', whose copy claims local-model silence. Added\n  measured-light, and thinking-with-zero-output is the heavy case with\n  no invented ratio.\n- a11y: focusing the step container broke the trap (its tabindex=-1 is\n  excluded from the FOCUSABLE query, so Shift+Tab escaped the dialog);\n  making it a tab stop turned prose into a mystery stop. Focus now stays\n  on the pressed control and the step change is announced politely.\n  Close on every step, counter computed from the step list, AA contrast.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(i18n): plural forms, formal register and nominal stage names\n\n- applied_one/applied_other: one interpolated count string cannot serve\n  both forms.\n- de converted to the Sie register, matching the dominant one in settings\n  (61 Sie strings vs 11 du, 7 of which were this feature's).\n- \"effort\" reads as reasoning effort / Denkaufwand, naming the control it\n  refers to rather than a generic word.\n- stage names are nominal (\"Job analysis\" / \"Stellenanalyse\"), the step\n  titles dropped their hardcoded \"1 of 4\" now that the counter is\n  computed, and the suggestion copy quotes measured sizes and states the\n  re-ask behaviour instead of implying a smaller model is free.\n\nCovered by a test that runs the real i18next instance in both locales — a\nstubbed t() asserts on key strings and can never catch a missing key.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(settings): filter stages with the shared paying-stage guard\n\nThe barrel export landed, so the renderer's own \"everything except the\nfree set\" filter collapses onto `PIPELINE_STAGES.filter(isPayingPipelineStage)`\n— one owner for the question, pinned on the Rust side against both\ndepths' free_stage_names().\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ai-config): remove the per-stage base url entirely\n\nMEDIUM from security review: ai_set_stage_override took a renderer-supplied\nbase_url that NO screen displays back — the editor forwards the provider's\nconfigured URL and the stage row renders provider + model only. It was fully\nSSRF-validated, but validation is not the property that was missing:\nnet::ssrf's threat model says the renderer never controls this value, and here\nit did. A compromised renderer could plant a per-stage egress endpoint that\nreceived every prompt one stage generates while Settings showed the real one.\n\nRemoved from the wire, the command, the struct, and the table — not merely\nrejected. A per-stage endpoint is now unrepresentable rather than validated,\nwhich is a property no future caller can erode. The column goes with it: the\ntable is new on this branch and exists in no shipped database, so the CREATE\nedits in place rather than appending a drop.\n\nDECISION — the endpoint FOLLOWS the provider, it is not snapshotted:\nCompleter::from_active_for_stage reads it from the named provider's own\nsettings row at RESOLVE time (new AiConfigStore::provider_base_url) and passes\nit to from_override. Rationale: a snapshot re-creates the same invisible\ndivergence one layer down — change the provider's URL later and the override\nkeeps silently using the old one, with no screen showing the difference.\nFollowing gives exactly ONE base URL per provider, the one Settings displays,\nso the UI is truthful by construction. It is still re-validated on the egress\npath, because a tampered ai_provider_config row is the same threat model the\ntampered override row was.\n\nImport bundles are the only remaining way a baseUrl key can arrive; serde drops\nthe unknown field, pinned by a test. The renderer keeps the URL for LISTING\nmodels (that call needs it) and simply stops sending it on save.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ai): never wrap an unrepresentable thinking-token count\n\nLOW from security review: both adapters narrowed the provider's reported\nreasoning/thoughts count with `as u32`, which WRAPS — so 2^32 + 7 would be\nrecorded as 7, a plausible number in the spend ledger sourced from a provider\nresponse. u32::try_from(v).ok() instead: a count that cannot be represented is\nno measurement at all, which is exactly what None already means on this field.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(settings): key the window commit latch by model\n\nThe de-duplication ref was one value for every model, and this component\nis not remounted when the selected model changes (no key at the call\nsite). Committing 16384 for model A therefore latched the number\nglobally: the identical commit for model B was skipped, B's backend row\nkept its old window, and every staged run used the stale value while\nSettings showed the new one. \"Use suggested\" made it deterministic — the\nsuggestion is hardware-derived, so it is the same number for every\nun-inspected model.\n\nAlso stops the previous model's measured maximum from clamping the next\nmodel's slider: the inspection is a mutation whose result outlives the\nswitch, so it is trusted only for the model it was run against.\n\nFirst tests for this file: commit-on-release, per-model de-duplication,\nthe model-switch regression, the stale-inspection regression, and the\nrejected-write toast. Both regressions were mutation-checked.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(boot): backfill the context window for already-seeded installs\n\nThe seed is row-presence gated server-side, so an existing install is\nrefused outright — its configured num_ctx stayed in Zustand, where no\nstaged run can read it, while the advisor states that the app \"sends the\ncontext window you configured with every staged request\".\n\nTwo halves: the seed now carries the window for each provider's model\n(fresh installs), and a one-shot patch promotes a renderer-held window\nwhen the backend row has none (upgrades).\n\nThe guard is the DATA, not the ref: it patches only when the row lacks a\nwindow and the renderer holds one for that row's model, and the patch\nmakes the condition false. The ref only prevents a retry storm inside one\nsession, and every other path returns before arming it — so nothing\nlatches on a state it did not act on. Ollama-only, since per-model\nwindows are an Ollama preference.\n\nMutation-checked: removing the call and removing the latch each turn a\ntest red.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(settings): lift suggestion dismissal out of the banner\n\nDismissal lived inside the banner, so the advisor — which decides what to\nrender in its place — could not see it: declining left the \"In this app\"\nsection showing neither the offer nor its fallback. Both hosts now own\nthe state, and the decline button exists only where a host handles it.\n\nInside the dialog the stakes were higher than a blank section: accept and\ndecline each unmount the button that was just pressed, dropping focus to\n<body>, and the focus trap binds its keydown to the panel — so the next\nTab reached the page behind the overlay. Both paths now hand focus to the\nwizard's footer control.\n\nAlso gates the banner's /api/show fan-out on a provider that can actually\nreceive a suggestion, and adds the editor's first behavioural tests\n(payload with/without the window switch, provider switch clearing the\nmodel, the CLI-agent empty-model branch, a stored model missing from the\ncatalogue). Mutation-checked: inverting the window switch reddens two,\ndropping the model reset reddens one.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(tauri-client): type the seed from the shared contract\n\nThe local re-declaration still carried a `baseUrl` on every stage\noverride after that field was deliberately made unrepresentable (the\nendpoint belongs to the provider), so it advertised a field the backend\nwould ignore. Using AiConfigSnapshot means the client cannot drift from\nthe contract again.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ai-config): one bad entry must not fail the whole restore\n\nMEDIUM from the ensemble. A structurally invalid stage-override row failed\nserde BEFORE any of the per-row leniency ran, so the whole aiProviderConfig\nsection aborted: the probe showed active=None, providers=0, overrides=0 — the\nVALID activeProvider and provider rows in the same bundle were lost too. Both\npromises in this module (\"a restore must not fail wholesale on one bad entry\",\n\"an out-of-range value costs the row its window, not its existence\") held only\nfor values that survived deserialization.\n\nProbing five hand-editable mistakes found the reported three plus two more, and\nthe same defect on the PROVIDER map and on activeProvider — every one aborted\nthe section. So the fix is in two layers, because the failures are two kinds:\n\n- Recoverable FIELDS: `lenient_context_window` reads the window as a Value and\n  narrows, so -1, 2^32, \"32768\" and 1.5 cost the row its window and nothing\n  else. `#[serde(default)]` on StageOverride::provider so a provider-less row\n  reaches validate_stage_override, which drops it, instead of failing the parse.\n  Both are on the TYPES, so the seed command gets them too — it takes an\n  AiConfigSnapshot straight from Tauri and never sees the import path.\n- Unsalvageable SHAPES: AiConfigSnapshot::from_untrusted parses the section\n  entry by entry, so a non-object entry or a wrong-typed field costs only\n  itself. Applied to providers as well as overrides. The section not being an\n  object at all is still an error — that is a corrupt bundle, not one bad row.\n\nThe existing scrub test used 9_999_999, which fits in a u32 and so never\nreached the boundary it claimed to test; it now covers the unrepresentable\nvalues and asserts the sibling override, the provider row and the active\nprovider all survive.\n\nThe deserializer checks REPRESENTABILITY only. A range check there passed every\ntest with it deleted — the apply-time scrub already owns that rule — so it is\ngone rather than duplicated.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(knowledge): per-stage model resolution and multi-provider daily caps\n\n* fix(renderer): survive the router globalNotFound type drift\n\n* refactor(shared): one owner for the context-window bounds\n\nCodeRabbit PRRT_kwDOSeuyxc6Y02At. 512/131072 existed in four places: the Rust\nvalidator, the stage-override editor, the local-model panel, and as prose in a\nhook doc comment. Nothing failed if they drifted, and the drift that matters is\nsilent — a UI that offers a value the backend then refuses.\n\npackages/shared/src/ai-context-window.ts now owns them and `pnpm gen:ipc`\nwrites ipc_contracts/context_window.rs, which ai_config re-exports. Same\nmechanism the stage vocabulary already uses, so `gen:ipc:check` fails CI on\ndrift instead of a reviewer noticing.\n\nNote for the renderer side: import CONTEXT_WINDOW_MIN / CONTEXT_WINDOW_MAX /\nCONTEXT_WINDOW_DEFAULT from @ajh/shared. LocalModelLimits' CTX_MIN is 2048, NOT\n512 — a deliberate local-model floor, not a copy of this rule, so it should stay\na local constant. Only its CTX_MAX (131072) is the shared bound.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(shared): a distinct type for the paying-stage subset\n\nCodeRabbit PRRT_kwDOSeuyxc6Y02BZ. isPayingPipelineStage narrowed to\nPipelineStage, which still includes the free stages the runtime check had just\nexcluded — so a guarded value gained no type-level guarantee. New\nPayingPipelineStage (Exclude of the free names) carries it.\n\nAlso closes PRRT_kwDOSeuyxc6Y01-u at the source rather than with another test:\nPIPELINE_STAGES_FREE is now `satisfies readonly PipelineStage[]`, so a free\nname that is not a real stage is a TypeScript error where the list is written,\nbefore codegen can mirror it into Rust.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): resolve for_stages from one store snapshot\n\nCodeRabbit PRRT_kwDOSeuyxc6Y01_M — verified. for_stages read stage_overrides()\nonce, then called from_active_for_stage per hit, which re-read the row and the\nprovider URL. A clear landing between the two reads put a stage IN the map\nmapped to a clone of the ACTIVE completer, which the doc comment says cannot\nhappen (\"absent means the default\"). Nothing observable moves today, because\nthat clone's routing identity equals the default's so no cache key changes —\nbut the invariant is what a later reader would trust.\n\nEach entry now resolves from the map already read, via a shared\nfrom_override_row that from_active_for_stage uses too, so the doc is true by\nconstruction and the per-stage re-reads are gone.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(ai): make four assertions say what they check\n\nCodeRabbit round 1, all verified against HEAD.\n\n- PRRT_kwDOSeuyxc6Y01-z: `expect_err` takes a plain &str, so the panic read\n  literally `{stage} makes no AI call` and never named the stage that\n  regressed. Now a let-else with a real `panic!`.\n- PRRT_kwDOSeuyxc6Y01_R: a removed `\\` continuation left a 14-space run inside\n  an assertion message; cargo fmt does not touch string contents.\n- PRRT_kwDOSeuyxc6Y01_X: the two window rejections asserted only `is_err`, so\n  a `validate_model` refusal of \"ollama\"/\"m\" would satisfy them. Only the call\n  order made them sound. Both this and the active-config sibling now match on\n  \"context window\" in the message, like the base_url case above them.\n- PRRT_kwDOSeuyxc6Y01_b: the wire-bound test rejected 1/511/131073/u32::MAX and\n  accepted 32768, leaving the actual bounds unpinned — widening the range to\n  256 kept it green. It now accepts MIN_CONTEXT_WINDOW and MAX_CONTEXT_WINDOW\n  by name.\n- PRRT_kwDOSeuyxc6Y01-8: paying_stages was only asserted non-empty and\n  free-stage-free, so returning just the first stage passed. Pinned to literal\n  per-depth lists instead of the reviewer's suggested complement, which would\n  have restated the implementation and passed whatever it did.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(services): transfer the limiter slot instead of releasing it\n\nrelease() decremented `active` and then resolved a waiter, but the waiter\nonly increments when its microtask resumes — so every caller arriving in\nthat window saw a free slot and started too. Proved with a test that\nlands exactly there (two microtask hops after the gate opens): peak 2\nunder a cap of 1. The slot now stays counted and is handed straight to\nthe woken waiter; only an empty queue gives it back.\n\nQueued probes also honour React Query's own AbortSignal now: closing the\nadvisor drops work that has not started rather than running it for a\nresult nobody reads, passing the slot on so the queue cannot wedge.\n\nAlso: a rejected model write is reported (it reached nothing, so the row\nkept showing a model the backend had not stored), `model` is no longer\nnullable in the write type (no surface can produce it), and the\nactivation test cleans up the store singleton it seeds.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(settings): report a rejected model write, seed the commit latch\n\nTwo review findings in the provider rows:\n\n- picking a model swallowed a server rejection, so the row kept showing\n  the new model while the backend row was unchanged and staged runs used\n  the old one. It now reports like every other write in the hook.\n- the per-model commit latch started empty, so the FIRST release on each\n  model committed a value the backend already had — a click on the thumb\n  or a Tab key-up was a round trip plus an activeConfig invalidation.\n  Seeded from the value on screen.\n\nTests: no write while dragging, no write on a release that moved nothing,\nthe \"Use suggested\" commit path, and the no-op when the suggestion equals\nwhat is stored. The CloudProviderConfig stub now succeeds by default with\nfailure opted in per case, so the payload assertions no longer run with\nthe component stuck in its error state.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(settings): honest per-row labels, names and live regions\n\n- an override with an empty model on a CLOUD provider was labelled \"CLI\n  default\" right next to the warning saying it will fail; that label now\n  belongs to CLI agents only, and a missing model says so.\n- Reset unmounts its own button, so focus returned to <body> — the same\n  WCAG 2.4.3 drop the editor already guards against.\n- seven buttons named \"Change\" and seven named \"Reset\" now carry\n  stage-scoped accessible names, interpolated so translators keep word\n  order.\n- the skeleton live regions announced nothing (a live region announces\n  TEXT, and skeletons are shapes) and unmounted before the result could\n  be announced either. They now carry a sr-only message and survive the\n  transition.\n- the risk step keyed its model map by name alone, so the same model on\n  two providers collapsed into one row assessed against the wrong\n  provider.\n- the fit step could print a fabricated \"0 tokens\" window in the step\n  whose whole promise is that it never assumes a size.\n\nTyping: stage ids are PipelineStage across the hook, client and advisor\ncontext; the prompt-budget table is keyed by PipelineStage so a rename\nupstream is a compile error; context-window bounds come from the shared\nconstants the spine landed.\n\nTest quality: mock resets between cases, a row lookup that no longer\ndepends on a Tailwind class, a dropdown assertion that was vacuous (the\noptions render in a portal), the boundary cases for the size ratio and\nthe thinking threshold, the switch-ON payload, onDone on success, and\nthe provider actually asked for a catalogue.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(i18n): cover the keys added for the review round\n\nThe accessible names, the no-model label and the two live-region plurals\nall resolve through the real i18next instance in both locales.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(pipeline): pin the context-window bounds as literals\n\nFollow-up to the review round: my first version of this assertion named\nMIN_CONTEXT_WINDOW/MAX_CONTEXT_WINDOW, which is a tautology — the test follows\nthe bound wherever it moves. Verified: with the named form, narrowing the\nminimum to 513 left it green.\n\nLiterals instead. 512 was already accepted literally by\nan_out_of_range_context_window_is_rejected, but 131_072 was accepted NOWHERE in\nthe crate, so narrowing the maximum to 65_536 passed all 4049 tests. It now\nfails.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(services): key the stage-override hooks on the stage vocabulary\n\n`@ajh/shared` exports the generated vocabulary and the editor already\nconsumes it, so widening back to `string` here meant an invalid stage id\ncompiled and failed only in the backend validator — or as a missing\n`settings.ai.stages.names.*` translation.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(docs): correct stage list and cache-key identity in pipeline docs\n\n- anti-abuse-limits.md: remove `validate` (free stage), correct stage count from 7 to 6, rename `judge` to `llm_judge` per MAX_STAGES constant\n- domain-model.md: add context_window and StageIdentity to cache-key documentation, fix stage name to `llm_judge`\n- verified against pipeline/resume/mod.rs (MAX_STAGES), cache.rs (StageCacheKey, StageIdentity)\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-13T08:53:17+02:00",
          "tree_id": "c5dd368161256117292853cedfa0db903d94d9a6",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/665505a826fa7ff683736aae0db4b285c1ebd86a"
        },
        "date": 1786605411910,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2261741,
            "range": "± 36564",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2747719,
            "range": "± 91663",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 306843,
            "range": "± 14966",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "3e37b7d8392374f50b66c788eaa33ed775d3259c",
          "message": "feat(agent): phase 7 — the flow registry and the improve-resume flow (#986)\n\n* feat(agent): budget the improve-resume flow to clear one whole quality run\n\nThe improve flow is the only home of the run-quality-pipeline tool, and the\ncontroller races every tool call against Budget::step_timeout — so its step\nclock has to clear one whole quality run (4500s) plus the last provider call\nthat deadline may still admit (300s) plus a two-clock margin. 90 minutes,\nderived rather than picked, with the relation asserted at compile time next to\nthe constants it reads (agent::tools_pipeline), the same discipline the\nstage-deadline relation already uses.\n\nAlso adds the improve worst-case relations (8 turns / 6 calls) as compile-time\nasserts, the new budget to the consistency + literal-pin tests, and the POSITIVE\nhalf of the least-privilege split: the quality-pipeline tool must be reachable\nfrom exactly the flow whose step can cover it, asserted in both directions.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(agent): add the flow registry and the improve-resume system prompt\n\nAgentFlow bundles the four things that vary between runs of the same controller\nloop — wire kind, fixed system prompt, tool whitelist constructor, budget — and\nFLOWS is the registry a run is looked up in. Pairing them by hand at the call\nsite is what let a prompt name tools its whitelist did not carry (and would let\na whitelist carry a tool its budget cannot afford); the pairing is now asserted.\n\nThe improve-resume prompt reviews a generation this app already produced: read\nthe persisted report first (free, and the only record of the last check), then\nvalidate the GENERATED text — passing it as draft is mandatory, since every\nquality tool silently falls back to the candidate's saved master resume when\ndraft is empty — then fix, re-validate, and offer the corrected text through the\none gated save. The expensive pipeline tool is rationed as one of two optional\nuses.\n\nGuards mirrored from the prep flow, all four mutation-checked: names-exactly its\nown whitelist (now a loop over the registry, so a new flow inherits it),\nordering plus the draft-argument trap, closed list with untrusted results, and\nthe step-budget fit read off the registry entry.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(agent): run the controller to the flow budget instead of a module constant\n\nThe loop held its ceilings in a module const pinned to the prep flow. With a\nsecond flow that could only ever be wrong for one of them: the review flow's\nstep clock is 90 minutes precisely so its one expensive tool can run, and a\ncontroller reading the prep constant would have raced that tool against 6\nminutes and ended every run at a timeout.\n\nrun-agent-with-system now takes the budget as ONE value (all four ceilings,\nconfirm wait included), and the production entry takes the whole flow — prompt,\nwhitelist constructor and budget together — so pairing the wrong ones is only\npossible in the registry, where a test asserts the pairing. The pure test entry\nkeeps a default so its seventeen call sites are untouched.\n\nNew guard, mutation-checked: a caller-supplied max-steps of 2 stops the run at\n2, which fails if anything reads the shipped constant again.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(agent): select the agent flow with a kind on the run request\n\nThe run request gains a kind: a closed, shared vocabulary (one TS const, emitted\nto Rust by gen-ipc so the registry and the wire cannot drift) defaulting to the\nprep flow, so a renderer that omits it keeps the behaviour it always had. The\ncommand resolves it through the registry inside the spawned task; an\nunregistered kind FAILS the run instead of falling back to the default, because\nsilently running prep for a request that asked for a review writes the wrong\ndocument.\n\nThe review flow also needs the document it reviews. Every quality tool falls\nback to the candidate's saved master resume when its draft argument is empty, so\nthe seed message fences three blocks - the generation under review, the master\nresume, the posting - with the generation resolved SERVER-side from the run's\nown job id (posting url then the generations aggregate), never renderer-supplied.\n\nGate denylist call, documented at the denylist and pinned by a test: kind does\nNOT join it. It is inert there (flows resolve once, at run start) and already\nrefused by the schema whitelist as an undeclared field, while adding a word that\nordinary to a recursive key-name ban would refuse legitimate future content args.\n\nThe identity-only lock test is renamed, not weakened: kind is flow routing, so\ncarries-only-identity stopped being true - what the lock protects is that a\nrenderer picks from a menu of backend-declared flows and can still name no\nprovider, no model, no endpoint and no ceiling.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(agent): describe the run command as flow-selected rather than prep-only\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(agent-ui): key the run machine's step mapping on the flow that produced it\n\nThe step-to-event mapping was a flat list of PREP tool names, so every tool of\nthe improve-resume flow answered null and the machine sat in planning for a whole\nreview run - busy/terminal introspection, and any UI keyed on it, all wrong.\n\nThe mapping is now keyed by the run's flow kind: a tool a flow cannot call must\nnot move that flow's machine, and two flows may register the same name for\ndifferent phases. Entries stay ordered so a turn naming two recognized tools\nresolves by precedence rather than by the order the model emitted them.\n\nEvery review tool maps to ONE new reviewing state rather than five: the\nsequence's granularity belongs in the panel checklist that reads the step log,\nand validate_resume legitimately appears twice in a run (the post-fix re-check),\nwhich a self-looping busy state makes idempotent instead of a mis-sequence. The\ngated writes need no entry at all - a confirm request is a step KIND, which is\nwhat lets one branch serve save_cover_letter and save_resume.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(agent-ui): let the confirm gate edit a proposed resume, not just accept it\n\nsave_resume was missing from the confirm card's known-tool registry, so it fell\nto the generic read-only JSON branch: approve and deny worked, editing did not.\nThat is fine for a flow with a second output, and wrong for the improve-resume\nflow, whose ONLY write this is - its whole product could be taken or left.\n\nRegistered by the NAME of its one content property rather than as a second pair\nof extract/rebuild functions: every gated write in the app carries exactly one\ncontent field (the job is fixed by the run), so one keyed accessor covers both\ntools and the next one costs a registry line. The cover letter's behaviour is\nbyte-identical - its existing tests still pin it.\n\nThe editable field now also requires the args to have actually carried that\nproperty. A known tool with an unexpected shape falls back to read-only JSON,\nwhich the old doc comment claimed and the code did not do: an empty editable\nfield would invite approving nothing over the document the user already has.\n\nText arrives clamped by the gate to each tool's own content cap (40k for the\nresume, 20k for the letter), so what is shown and edited is what gets saved;\nnothing is re-clamped here.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(agent): refuse a generation the review flow cannot read whole\n\nCRITICAL, both reviewers: the seed clamps the generation at RESUME_CAP (8000\nchars) with no marker, validate_resume's draftTruncated guard cannot see a cut\nthat happened a layer above it, and save_resume writes up to SAVED_RESUME_CAP\n(40000) back over the SAME aggregate row. A 30k generation would therefore be\nreviewed as an 8k stump and offered back as a replacement for the full document,\nwith the confirm dialog showing exactly what gets saved and disclosing nothing\nabout the missing tail, and no undo behind it.\n\nFail closed: over the cap, the run refuses at start with a typed validation\nerror naming the length and the remedy. Raising the seed cap was rejected as a\nfix - the quality tools still read their first RESUME_CAP chars, so it trades\none silent mismatch for another. The rule is a pure function (this crate has no\nTauri harness) and counts CHARS, the unit the fence clamps in.\n\nAlso extracts the one generation-resolution rule (posting id, cached url,\nfind_for_job) into agent::tools_pipeline::generation_for_job, now shared with\nget_quality_report - two copies is how the report tool and the flow acting on it\nwould come to disagree about which document is under review. It stays policy\nfree on purpose: the report must keep working for documents the review flow\nrefuses.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(agent): enforce the tool-call ceiling in the controller loop\n\nBoth reviewers reached this from opposite ends. Security: run-quality-pipeline is\na 75-minute call reachable from the review flow, the prompt's \"spend AT MOST ONE\nof them\" is prose an injected posting can argue the model out of, and nothing in\nthe loop counted calls - so a steered run could spend its whole allowance on the\nmost expensive tool the app has. Architecture: the reachable ceiling was ~11h\nagainst a documented 120-minute \"honest bound\".\n\nThe loop now counts every executed call, refused unknown-tool names included\n(a loop inventing names is the same runaway, and each refusal costs tokens), and\nstops at MaxToolCalls - the variant's first producer in the crate.\n\nCost, stated in the field doc rather than discovered later: a hard stop spends\nnone of the summary turns the ration reserved, and it makes MaxSteps unreachable\nfor a tool-calling run under every shipped budget. A new test pins that MaxSteps\nis still reachable when the call allowance is not the binding one, so neither\nreason is dead. The pinned not-enforced characterization test is rewritten as\nthe enforcement pin, and the run-timeout doc now carries the reachable-ceiling\narithmetic (~12h) beside the 120-minute intent instead of implying the intent is\na bound.\n\nPer the Q1 ruling the 90-minute step clock stays with no per-tool timeout; its\ndoc now says plainly that send-with-retry-capped bounds every ordinary turn at\nall four adapters, and records the trigger for revisiting: the first tool with\nneither an internal deadline nor a per-call HTTP bound joining a long-clock flow.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(jobs): offer an improve-resume review on a finished tailored resume\n\nThe improve-resume flow shipped with no way to start it. Its entry lives on the\none surface holding both ids it needs: the tailored-resume panel, whose finished\nrun IS the generation the flow reviews (this pane's document text is the same\nfind_for_job record the run resolves server-side). resumeId stays the\ncandidate's MASTER resume - the ground truth claims are checked against - never\nthe generation's id.\n\nThe action is offered only where it can honestly run: a terminal run, of the\nnewest document (the same one-document-per-posting rule the pipeline's own write\nactions carry, since this flow's save merges into that same aggregate), with a\ngeneration to review, and a resume plus provider to run with. The jobless\nsurfaces that show the same quality report get no entry at all - agent.run needs\na real posting id and they have none.\n\nA generation longer than the seed cap is REFUSED rather than started: the flow\nwould read the first 8 000 characters and offer that corrected prefix back\nthrough a save whose own cap is 40 000, overwriting the document with its own\nhead. The button is disabled with a visible reason that names both ways out, one\nof which is the enabled button beside it. The stored text is still the\nauthority, so the run's own refusal is surfaced verbatim too.\n\nProgress renders in the panel that started it rather than in a second dialog:\nthe run SUSPENDS on its gated save, and a confirm behind another focus trap is a\ndecision the user cannot reach. The card is a compact sibling of the prep\nchecklist, not a reuse of it - prep's rows are a sequence this flow never runs.\nRows come from the step log through a matcher per row, because two of them are\nnot tool turns: the gated save is announced as a confirm request whose tool name\nlives in confirm rather than in tools, and the closing summary is the proposal\nstep. The two rationed steps appear only once taken, and the 90-minute rewrite\nsays so instead of looking stuck.\n\nThe run wiring is a hook rather than a second copy of the prep panel's inline\nversion: shared-channel filtering, the superseded-confirm clear, and the\nreconciliation fallback for a terminal event that beat the subscription - which\nthis flow makes the LIKELY path, since it refuses fast on a missing generation.\nThe prep panel deliberately keeps its own copy for now; re-plumbing a shipped\nflow to prove a hook is not worth the risk.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(agent): make seeding a generation a registry field\n\nQ3 ruling: the predicate method only relocated the kind match, and the answer\nstops being derivable from the kind the moment a third flow reviews something.\nAs a field, every future FLOWS entry has to answer at compile time - which is\nwhat a registry is for. Getting it wrong is not cosmetic in either direction:\nfalse on a reviewing flow strands the model with no document, true on a drafting\none makes every run pay for a store read it cannot use.\n\nThe stage-deadline relation is now also asserted over the registry, not only\nover the one budget it is derived from. AGENT-STAGE-DEADLINE is computed off the\nprep budget, but the bound belongs to whatever flow can CALL analyze-job, and\nany flow may register it - a shorter-clocked holder would have its runs ended\nmid-tool by a re-ask the deadline was sized to fit, with nothing in the build\nsaying so. Mirror of the quality-pipeline pair, derived from constants rather\nthan a name list. No flow trips it today; it is written for flow #3.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(jobs): report an improve run that stopped at a ceiling as stopped\n\njob.completed fires for a loop that ran out of steps, tokens or tool calls as\nwell as for one that finished, so reading it as success alone tells the user a\ntruncated review was a clean pass - over a resume they may then approve\nbelieving every check ran. The controller now enforces the tool-call ceiling\ntoo, so that outcome is reachable rather than theoretical.\n\nThe completed payload's stoppedReason is kept and drives the headline: any\nreason other than done drops the success green and names itself, an unmapped\none lands on the neutral stopped label rather than on \"Completed\", and the\n\"the review is finished\" line is withheld from a run that did not finish.\nCopy is the pipeline's own stopped vocabulary, which this panel already speaks,\nthrough the shared enum map - no new keys.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(agent): make the timeout message readable and clamp the rejected flow echo\n\nFour LOWs from the Phase-7 review, all on text or comments that stopped being\ntrue when a second flow appeared.\n\nThe step-timeout message rendered the review flow's clock as \"5400s\" and blamed\n\"the AI provider\" for a stop that a TOOL can cause just as easily - a pipeline\ncall is minutes of work, not one unanswered request. It now reads the bound in\nhuman units above ten minutes and points at the two causes it can honestly name.\n\nA rejected kind is echoed into a stored, logged, rendered failure message and the\nDTO field is a bare String, so it is clamped to 64 chars - the discipline the\ncontroller already applies to a model-chosen tool name - by chars, not bytes.\n\nComments that still said the confirm wait and the step timeout come from\nAGENT-PREP now say they come from the running flow's budget, and the stage margin\nconstant documents both of its uses (one subtracts it, one adds it).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(agent): enforce the tool-call ceiling per call, not per turn\n\nHIGH from the delta review. Providers return tool-calls as an ARRAY - nothing\nsets parallel-tool-calls false, every adapter maps the whole array, and the gate\ntests already drive a two-call turn - so a check at the turn boundary admitted\n(ceiling - 1) + K calls for a K-call final turn. Each executed call races the\nstep timeout individually, so those K calls could each be a 90-minute pipeline\nrun: the tail was unbounded, which falsified the budget doc's claim that\nmax-tool-calls times step-timeout is the ceiling.\n\nThe count is now checked before each call. Past it the remaining calls of the\nturn are refused with an error body instead of executed: the run is ending at\nMaxToolCalls either way, so executing them buys nothing and can cost hours,\nwhile a fenced refusal keeps the turn whole - every requested call still has a\nresult in the transcript. A Write among them is refused without suspending, so\nthe user is never asked to approve a save whose run is already over. The\nreachable-ceiling paragraph now says why the product is a bound at all.\n\nAlso, the ceiling test scripted one call per turn, so \"stop ON the ceiling, not\npast it\" was never exercised for the only shape that breaks it; a four-call turn\nagainst a ceiling of three now is.\n\nFour LOWs: both worst-case relations demand strict slack (equality means the run\nstops the instant the last planned call returns - for both flows that call is a\nsave, so the user would get the pre-save text as the proposal); the seed\nbuilder's doc calls its clamp defence in depth and points at the refusal that\nactually protects the round trip; the duration example no longer uses a value\nthat renders differently than written; and resume-id and job-id are clamped in\nfailure messages like the flow kind already was - same threat, same treatment.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(agent-ui): send only the edited content, and give the confirm room to edit in\n\nThree findings on the confirm card.\n\nAn edit spread the model's ORIGINAL args around the new text. The shell\nre-validates an edit and refuses any undeclared key - but that refusal resolves\nthe gate as a deny while agent.confirm still answers ok true, so the card\nreported success and unmounted with nothing saved. One stray key from the model\nwould silently discard the user's rewrite. Every known tool's schema is\ncontent-only, so the edit now carries the one content property and nothing else.\n\nA full resume is 150-200 lines in a ten-row box that could not be resized. The\nfield gets a real minimum, a viewport-relative cap that keeps the card's own\nactions reachable at the 900x600 floor, and vertical resize; rows stays as the\nno-CSS fallback.\n\nThe card is nested inside a host panel wearing the same brand left-border, so\n\"the run is blocked on you\" looked like \"the run is going\" - it is amber now,\nwith the heading still saying it in words rather than colour alone. The hint\nalso states the five-minute expiry, which was invisible: a request that times\nout counts as denied and the run moves on.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(agent): clamp the ids the not-found errors name, from one shared rule\n\nThe tool-side resume-not-found and job-not-found constructors interpolated\ntheir id unclamped. \"Trusted\" on those ids means trusted-as-ROUTING - they come\nfrom ToolContext, never from tool args - not bounded: ToolContext is built from\nthe request's own resumeId/jobId, unvalidated wire strings. The message reaches\nthe model's transcript as a fenced tool result AND, on the review flow, the user\nverbatim: the generation lookup calls job-not-found through the shared resolver\nand fail-run surfaces it as the run's failure message. Same hole the command's\nown echoes had, one call away.\n\nThe cap and the clamp now live in agent::tools next to the other untrusted-text\ncaps it already owns for both consumers, so there is ONE owner rather than a\ncopy per module; the command drops its private pair and imports them.\n\nTest pins both constructors on the consumer that makes it matter, and each was\nmutated separately to confirm it reds.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(jobs): refetch the saved document once an approved review writes it\n\nAn approved save invalidated nothing, and nothing else was going to: the run\ndetail query has no interval once the run is terminal, and refetch-on-focus and\n-on-reconnect are both off client-wide. The panel kept showing the pre-save\nresume next to a card saying it had been saved - two contradictory claims in one\nviewport, with no way back short of a restart.\n\nBoth gated writes land in the ai_generations aggregate, which is also where the\npipeline's run detail reads its resume text and quality report from, so the\npipeline key PREFIX plus the generations key covers it in two calls - no key\nbuilt from a possibly-null run id.\n\nFired twice on purpose. The write executes AFTER agent.confirm returns (the\ncommand only hands the decision to the suspended tool), so the refetch on\napprove can lose the race; the next step is proof the tool finished, and a\nterminal event backstops a run that ended without one. A deny changed nothing\nand invalidates nothing.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(jobs): tell the truth about a review that stopped before it finished\n\nA run that \"completed\" at a ceiling wore the finished badge over a checklist\nclaiming the opposite of what happened: steps the loop never reached said\nPending - work still to come, on a run that had ended - while the step cut\nmid-flight showed a green Done. That reads as a resume every check passed.\n\nRows now depend on the run being OVER rather than on it having failed: unmatched\nbecomes \"Not reached\", the step in flight when it stopped stays Interrupted, and\na ceiling stop counts as an interruption even though the job completed.\n\nThe badge goes back to being a LABEL - neutral \"Stopped\", warning-coloured - and\nthe specific reason moves into a sentence beside what it means for the resume,\nwhere it can be read at a normal size instead of wrapping a 9px chip. Waiting on\nthe user is amber, not the same blue as running. The terminal outcome is now\nannounced too: without it a screen reader hears the last step and never learns\nthe run ended, or how.\n\nContrast and copy from the same review: body hints move off foreground/45 (4.07\nto 1 on dark) to 11px foreground/60, the interrupted icon drops its opacity\nvariant, pending circles go /25 to /40, the present-tense hint hides once the\nrun is not running, the rewrite step stops calling a 75-minute pass \"a while\",\nand the optional steps get one line explaining why they come and go. Five\npending rows under a \"starting\" spinner said the same nothing twice, so the\nchecklist waits for a first step.\n\nFocus: Stop and Dismiss both remove the control that was clicked, dropping focus\nto body BEHIND the modal portal where the trap cannot recover it. Stop parks on\nthe card heading; Dismiss hands back to the host, which owns a node that\noutlives the card.\n\nThe locale files also carry the entry point's copy from the next commit - one\nJSON, split by nothing.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(jobs): let the improve entry explain itself, and lock what it conflicts with\n\nThe too-long refusal was unreachable in both locales. This repo's Button kills\npointer events on a disabled control, so its title never fires; a native\ndisabled button also leaves the tab order, so the aria-describedby pointing at\nthe reason was never announced; and the reason itself sat below a scrollable\npreview, off-viewport from the footer it belonged to. It now sits IN the footer\ndirectly above the button row, the button stays focusable through aria-disabled\nwith a no-op click guard, and the cap is locale-formatted rather than printed as\na bare machine number.\n\nRun again is locked while a review is in flight: that review is about to offer\nthe CURRENT document back through its gated save, so a generation started\nunderneath it would be silently overwritten by the older reviewed text on\napprove.\n\nThe entry is also withheld on a failed or stopped generation - what those leave\nbehind is not a document to review - while needsReview keeps it, since open\nclaims are exactly what a review helps with.\n\nA suspended confirm was invisible once the modal was closed, and it expires in\nabout five minutes; the closed panel's own trigger now carries a marker with the\nexpiry said out loud, not by colour alone.\n\nVerified by hand rather than through the pre-commit hook: lint-staged stashes\nthe whole worktree, and a concurrent agent is committing in this one - it\nalready dropped this work once. eslint . --max-warnings 0 and the touched vitest\nprojects were run directly.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(jobs): cover the new improve-resume copy in both locales\n\nThe key matrix predates the review round, so five new keys - the stopped-run\nexplanation, the optional-steps line, the waiting marker, the neutral stopped\nlabel and the not-reached row status - were rendering unchecked, and a missing\nGerman one would have surfaced as English at runtime rather than as a red test.\n\nThree assertions the presence check cannot make on its own: the cap is formatted\nby the call site now, so the copy must place a STRING it is handed rather than a\nnumber; the stopped explanation interpolates a reason drawn from the shared\npipeline vocabulary, which has to resolve in the same locale; and both refusal\nsurfaces must name the résumé editor with the term the report's own remove hint\nalready uses, since two names for one editor is how a user ends up looking for\nthe wrong screen.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(jobs): say why a run-start refusal reaches german users in english\n\nThe three refusals arrive on job.failed as free-form strings with no code to key\na translation off, and matching them by substring would be a guess that breaks\nthe next time the Rust copy is reworded - which happened twice while this\nshipped. Recorded at the render site with the real fix named (a typed error code\non the wire), so the next reader does not re-derive it, and the message body\nmoves to the same 11px the rest of the card uses.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(jobs): type the react-query mock through its generic, not an assertion\n\nThe lint autofix stripped the cast on importOriginal as unnecessary, leaving a\nspread of unknown that vitest runs happily and tsc rejects - the esbuild-versus\n-tsc gap this repo has been bitten by before. A type ARGUMENT survives the\nautofix and says the same thing.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(jobs): stop calling a refused save \"done\" because the model asked for it\n\nA turn's tools are what the model REQUESTED, emitted before any of its calls\nrun. Once the tool-call ceiling is spent the rest of that turn is refused\nwithout executing, and a Write among them is refused WITHOUT suspending - so\nthe user is never asked. Keyed on the request alone, the save row reported\n\"Proposing the corrected resume - Done\" for an offer that never reached anyone,\non exactly the run whose other rows now say the checks did not finish.\n\nA row can now declare what counts as PROOF it happened, separately from what\nmatches it. For the save that proof is the suspend itself, the one place asking\nand happening come apart; every other row's tool result is its own evidence. A\nrequested-but-never-reached row reads Not reached once the run is over, and\nstays In progress while the call is still running - asking IS the progress\nthere.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(jobs): withhold the report's writes while a review holds the document\n\nThe write lock stopped at Start. Open report stayed reachable mid-review - one\ndialog replaces the other, so it always was - and its Fix and Remove actions\nwere live: regenerate_section is ADMITTED by the limiter (cap two, the suspended\nrun holds one), writes its result, and is then overwritten wholesale by the\nsave_resume the review captured at run start and is about to offer. No undo.\n\nWithheld at the write level rather than by hiding the report: reading it during\na review is useful and harmless, the writes are the hazard. The report's note\nsays which reason applies - a live review is a different fact from an older run,\nand the older-run copy would have been a lie - and the footer repeats it beside\nthe Run again this same lock already disabled, since a natively disabled Button\nhere can explain itself no other way.\n\nAlso restores the refusal tooltip the aria-disabled switch dropped. That switch\nis what makes the tooltip work at all (pointer events survive aria-disabled,\nunlike a native disable), so leaving the generic \"what this does\" hint on a\nbutton that will refuse the click was the misleading half of the trade - and it\nleft a translated key rendered nowhere while a test claimed titles never fire.\nThe stale comment saying so is corrected.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(jobs): mark the copied review cap for the shared export that is in flight\n\nThe renderer carries the 8000-char cap as a literal because the backend does not\nexport it yet. Named as a TODO rather than left to be rediscovered, with what\nkeeps the copy honest in the meantime: the boundary is asserted on both sides,\nso a divergence is a red test and not a run that fails on click.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(agent): give the gated resume save its own fresh quality report\n\nHIGH from the ensemble. The gated save wrote resume_text and no report;\nsave_application merges the report per top-level key and an absent one means\n\"keep what is stored\", so the new document landed under the PREVIOUS document's\nverdict - findings, fabrication list, sections - with the confirm dialog\ndisclosing nothing and no undo behind it. The improve flow made that permanent:\nits whole purpose is replacing the document a report describes.\n\nThe save now computes the report for the text it is about to persist, through\nthe same deterministic provider-free validator the pipeline's own validate stage\nand regenerate-section use, and carries it in the SAME save. The wrapper\ndeliberately omits the coverLetter key so the merge leaves the stored letter\nslot alone, and its pipeline label says agent rather than inheriting the depth\nof the run that produced the old document.\n\nThe rule was stated in three doc comments and enforced by none, so it is now the\nsave request constructor's precondition: a request that carries no report cannot\nbe built. Both agent flows share that one handler, so the prep flow's save - same\nstaleness, same path - is fixed by construction.\n\nRow side too: the newest run for the posting is moved to the status the fresh\nreport implies, reusing recomputed-status and still-needs-review rather than\nrestating them. The panel keys its headline and its review block on that row, so\na save that clears the last finding while the row still says needsReview shows\nthe previous document's verdict as this one's. A failure there is logged, not\npropagated - the document is saved and is the user-visible outcome.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(agent): pin that a write past the tool-call ceiling never suspends\n\nThe per-call ceiling refuses the remaining calls of a turn once the budget is\nspent; the gate-side consequence had no test. A confirm dialog for a save whose\nrun is already over is worse than no dialog: approving it either does nothing or\nwrites a document from a run the user was told had stopped.\n\nTwo writes in one turn against a ceiling of one - nothing resolves the second,\nand the budget's confirm wait is shortened to five seconds so a regression fails\non a named assertion in seconds instead of hanging for the shipped five minutes.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(shared): generate the agent resume cap instead of hand-copying it\n\nThe renderer's review threshold was a second literal 8000, added in the same\nchange that built a generator for exactly this class of number. The cap now\nlives in packages/shared, is emitted to Rust by gen:ipc, and the Rust fence cap\nis DEFINED as the generated value - so the fence, the backend's over-length\nrefusal and the renderer's disabled entry point are one number, gated by\ngen:ipc:check. Exported as AGENT_RESUME_TEXT_CAP for the renderer to import.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(jobs): take the review cap from the shared contract\n\n* docs(agent): document flow registry, improve_resume, and tool-ceiling enforcement\n\nPhase 7 steward close: added section to domain-model.md describing AgentFlow registry structure,\nflow-to-kind resolution, prep_application vs improve_resume flows, wire vocabulary, budget/step\nbindings, tool-ceiling enforcement (per-call, inside loop), and write-cap-parity rule. Key contracts:\n- improve_resume flow reviews generated résumé (seeds_generation=true), budget 8 max_tool_calls\n- save_resume gated on fresh validate_resume; if ceiling cuts off validation, save is withheld\n- every flow's prompt must name exactly its tool whitelist (mutation-checked)\n- unknown kind fails validation, never falls back to default\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* style(jobs): format the improve-run test\n\n* fix(services): subscribe to agent steps once, so none are lost re-attaching\n\nThe step subscription re-ran whenever the caller's handler changed identity,\nwhich for every real caller is the moment its run id arrives - the moment the\nrun starts emitting. Tauri's listen attaches ASYNCHRONOUSLY, so unsubscribe-then\n-resubscribe leaves a window with no native listener at all, and a\nconfirm_request lost in that window leaves a SUSPENDED run rendered as a running\none until the 300s gate timeout silently denies the save.\n\nThe handler moves to a ref and the effect keys on the client alone - the shape\nuseJobEvents already uses, with a comment describing this exact race. Both agent\nsurfaces are fixed by it, the prep panel included.\n\nMutation-checked, executed: restoring the handler to the dependency array turns\nthe subscribe-once test red.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(jobs): make the improve entry's rule testable, and its refusals speak\n\nThree review findings on the entry point, plus the rule behind it.\n\nThe eligibility rule moves out of the component as a pure function. It could not\nbe tested where it lived: opening another run's report REPLACES this modal, so\nevery state that makes writable false also unmounts the footer the button is in,\nand the \"no button\" assertion passed with or without the term - guard-shaped\ndecoration. Each term is now asserted directly, including the newest-run one\nthat only the extracted form can reach.\n\nThe failure fallback was the alert's own heading, so a backend failure with no\nmessage printed the same sentence twice, stacked. It is a body sentence now.\n\nThe waiting-review marker on the closed panel carried its meaning in an amber\ndot and screen-reader-only text, leaving colour as the only signal for a sighted\nuser who is not running one (WCAG 1.4.1). It says \"Approval needed\" in words,\nwith the expiry still in the sr-only half.\n\nThe cap check also gets the test it exists for: every fixture was ASCII, where\nString.length and the code-point count agree, so a regression to .length would\nhave shipped green. Eight thousand astral characters are 16 000 UTF-16 units and\nmust still be accepted; one more must not.\n\nMutation-checked, executed: reverting each of the four turns exactly its own\ntest red.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(jobs): pin the waiting headline and derive the copy matrix from the unions\n\nTwo review findings, both about checks that could not fail.\n\nThe suspended-save tests asserted the editor and the announcement but never the\nheadline, leaving the ordering in headlineState unprotected - busy is true while\nsuspended, so checking it first makes a run that is blocked on the user read as\na running one, which is the single thing this card exists to make obvious.\n\nThe copy matrix restated the row and status unions by hand, so a row added in\nthe component would still pass the suite and reach users as a raw key through\nthe runtime-built lookup. The lists are derived from the exported unions now,\nchecked in both directions: satisfies rejects a member that is not in the union,\nand an exhaustiveness type rejects a union member missing from the list.\n\nMutation-checked, executed: adding a status to the union without its copy fails\ntsc at the exhaustiveness line, not at some later runtime string.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(jobs): record why the run-record fallback needs no polling timer\n\nThe reconciliation query has no refetchInterval and reads like a one-shot. It is\nneither: useJobEvents invalidates the jobs prefix on every job event, which\nmarks this detail query stale on every transition, and the guard latches only\nafter a terminal status has actually been seen - so a reading that still says\nrunning leaves it open for the next one.\n\nWritten down with what polling would actually buy (one case: the terminal event\nlost while nothing else is in flight) against what it would cost (an IPC and a\nSQLite read every interval for up to two hours), and with the note that the case\nshrank again once the step subscription stopped re-attaching mid-run.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(agent): treat a refused resume save as an error, and compare whitelists by name\n\nCodeRabbit PR #986. ai-generations-save is a tauri command returning Value, so\nit reports failure in band as {error}. The gated save ignored that: it told the\nmodel the resume was saved when the write had failed, and then synced the run's\nreview status - recording a verdict about a document that was never written. It\nnow fails closed on any shape that is not an explicit success, because a failed\nsave reported as success is silent data loss while the opposite costs a retry\nthat merges onto the same row.\n\nAlso compares the registry's whitelists by NAME SET rather than length: the test\nclaims to catch a swapped tools constructor, and two whitelists of equal size\nare exactly the swap a length check cannot see.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): move a run status with an existence-checked update\n\nCodeRabbit PR #986, the #970 delete-race class. The status sync read the newest\nrun row and wrote it back with upsert-run, which is an INSERT OR REPLACE - so a\ngeneration delete, application delete, or factory reset landing between the read\nand the write was UNDONE, recreating an orphan row with no parent, no UI, and no\neviction path. A row-scoped UPDATE changes zero rows when the row is gone, so the\ncheck and the write are one statement with no window between them.\n\nThe two pre-existing sites with this shape (regenerate-section and\nresolve-fabrication) are left alone deliberately: they upsert a full row they\njust read on paths this PR does not touch. Flagged in the PR reply as a\nfollow-up rather than fixed silently mid-review.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(pipeline): label an agent-saved report with the shared depth vocabulary\n\nCodeRabbit PR #986. The wrapper's pipeline field is typed GenerationDepth on the\nrenderer side and its parser maps anything outside GENERATION-DEPTHS to fast,\nthen re-serializes - so the invented agent label survived exactly until the next\nRe-check, which silently PERSISTED the relabel. Inventing a value for a closed\nshared vocabulary is the drift class this branch already closed twice.\n\nWidening the vocabulary was the other option and is worse: that set also types\nthe run request's depth, so adding a token would let a request ask for a depth\nthat is not one. Fast is the honest member - the field separates staged-pipeline\nreports from deterministic-pass ones, and this report is the latter. What it\nmust not do is inherit the previous document's quality or max, which is the\nstale-label twin of the bug the fresh report exists to fix.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(knowledge): make the phase 7 flow section thin pointers\n\nCodeRabbit PR #986, path instruction: knowledge docs are pointers into the owning\nsource, not copies of its literals. The section restated flow kinds, step counts,\ntool-call and token ceilings and timeout literals - the same shape the Max depth\nbullet two sections up already warns about, having drifted once before.\n\nThree of the copied claims were also WRONG about the code they described: the\nceiling was said to check before offering tools (nothing suppresses tool offers -\nthat is the documented not-done fix), the refusal was said to teach the model\n\"no more tools\" (the run ends at that turn, so the model never reads it), and a\nwhole write-cap-parity mechanism was described around a run-quality-report tool\nthat does not exist. Deleting a false claim is not authoring doctrine, but the\nfile is the steward's - flagged in the PR reply for a steward pass.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(agent): end the run when an approved write fails\n\nRaised by the renderer track against 18dca96f. Once a store-refused save returns\nan error from the handler, that error has to reach the USER, not just the\ntranscript. It became an \"error:\" tool-result body, so the model summarized\naround it and the run ended Done - agent-run then emitted a terminal proposal\nand completed the job, while the renderer's save row (which keys on the confirm\nhaving been resolved) showed the proposal as saved over an unchanged document.\nNothing on the wire could tell that apart from a real save.\n\nAn approved write that FAILS now returns WriteResolution::Failed and the loop\npropagates it, which is the same handling a failed provider turn already gets\nand lands in agent-run's existing job-fail arm - so the failure travels on the\njob channel the renderer already reads, and no new wire field exists to be\nignored. Deny, a confirm timeout, and a rejected edit still continue: those are\nstates the model can honestly narrate and the user already knows about.\n\nThe mutation makes the old behaviour visible: folding the failure back into a\nbody finishes the run as Done with final_text \"all saved!\".\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* style(jobs): format the tailored panel\n\n* refactor(renderer): narrow improve-eligibility run state to the session union\n\nThe extracted ImproveEligibility contract typed runState as a bare string,\nso a rename of a ResumePipelineState member would compile past both the\npredicate and its fixture. The field now carries the union itself and the\ntest fixture is annotated, so a dropped or renamed state is a compile error.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-13T11:20:37+02:00",
          "tree_id": "f9057afac62f87bac9382901f1bf1d834916c0dd",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/3e37b7d8392374f50b66c788eaa33ed775d3259c"
        },
        "date": 1786613583302,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2210665,
            "range": "± 91871",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2625552,
            "range": "± 35614",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 309063,
            "range": "± 10534",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "57f782f314e1f9ecb776827f4cc9792ab7d7bf2d",
          "message": "feat(export): phase 8 — sidebar and monogram letter layouts, and a reachable letter ATS toggle (#988)\n\n* feat(export): add the sidebar and monogram cover-letter layouts\n\nTwo new arrangements alongside Classic/Refined/Banded/Navy, each with a DOCX\napproximation and an honest ATS-mode degradation.\n\nSidebar widens the left margin to 62mm, fills it with a pale accent rail, and\n`place`s the letterhead (name/role/contact) into it; the correspondence runs in\nONE column beside it. The rail is a margin position, not a second column of\nprose, so extraction still reads letterhead -> date -> recipient -> body ->\nsign-off. `place` does not participate in layout, so the rail can never push the\nbody around or move a page break.\n\nMonogram sets a pale accent square carrying up to two initials beside a\nname/role/contact lockup, over a rule. The square is pale-fill with accent ink\nrather than a saturated tile with white text, because the accent comes from\nwhichever resume template is active and a light accent would put white on\nnear-white -- and the 85% lightening is the same `band_tint_hex` the DOCX side\nalready uses, which is what lets the two formats show one tint.\n\nThe brief said to mirror how Banded gates its band under ats_mode. It does not:\n`ats_mode` never reached the letter path at all. `render_letter_pdf` had no\n`ats` parameter, `LetterOpts` had no `ats` field, and `pdf/mod.rs` read\n`request.ats_mode` for resumes only -- so a user who turned the toggle on still\nexported a banded cover letter with its band. That is threaded now, as\n`data.opts.ats`, and Banded's band is gated on it too: the discipline has to be\nuniform or the toggle is a lie. Under ATS mode Sidebar drops the rail and stacks\nthe letterhead in one plain column, Monogram drops the device entirely (its\ninitials are real text -- extraction read \"JS Jane Smith\", two characters of\nnoise ahead of the candidate's own name), and Banded drops the band. No layout\nadds or removes a WORD either way, which is what the tests assert alongside the\nmissing fill.\n\n`ats` is a required parameter on `parse_cover_letter` rather than a defaulted\nfield: an ATS toggle that silently fails to reach the renderer looks exactly\nlike one that works, which is how this gap survived four letter layouts.\n\nStructural elements gate on `data.opts`, never on the layout id -- the DE DIN\nBetreff renders and the US subject does not, from the identical layout.\n\nDOCX: `LetterDocxStyle` gains rows for both, read off each `.typ` feature by\nfeature. Sidebar's rail has no DOCX equivalent at all (a text box or a\ntwo-column table would be exactly the multi-column trap the export avoids), so\nit becomes the same shaded name paragraph Banded uses, with the contact LEFT at\nthe margin. That alignment is why `contact_align` is now a stored field instead\nof a function of \"centred or right\": Sidebar and Monogram left-align the contact\nwhile leaving the name left too, which the derived version could not express --\nand inherit-by-omission is the defect that took four review rounds on Navy.\nMonogram's device becomes a shaded RUN at the head of the name paragraph, from\nthe SAME `monogram_initials` the `.typ` reads via `LetterHead.initials`, so the\ntwo formats cannot disagree about what it says and both extract \"JS Jane Smith\"\nrather than putting the initials on a line of their own.\n\n`monogram_initials` is Rust, not Typst, because every interesting case is string\nhandling a `.typ` cannot be tested on: first + LAST token (so \"Jane van der\nBerg\" is JB, not JV), letterless tokens skipped (\"Jane (she/her) Smith\" is JS),\nnon-ASCII capitals preserved, and a hard two-character cap so a long name can\nnever overflow the fixed-size square.\n\nAlso replaces `letter_template_sources()`, which returned a fixed tuple of three\nlayout sources and went stale the moment Navy was added -- the preview generator\ncould not reach the newest layout at all, and being `#[ignore]`d nothing\nnoticed. It now routes through the production picker.\n\nTracking is 0.04em on both new names, inside the 0.03-0.10em band every template\nhere uses. The accented-Latin extraction guard that caught Cologne Navy's 0.14em\n(\"A LVA R O  E S P O S I T O\") now covers both.\n\nVerified: the rail placement is measured against the RENDERED page rather than\ntrusted -- leftmost glyph exactly at the 7mm rail padding, nothing in the\nrail/body gutter, body column at 62mm, and nothing left of 25.4mm under ATS\nmode. A sign error in the `place` offsets would pass every text-only assertion.\nAll three ATS gates were mutation-tested: forcing `ats` false in each `.typ`\nfails the corresponding test.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(export): register the sidebar and monogram letter layouts in the ui\n\nThe renderer half of the two new arrangements: the wire union in the shared IPC\ncontract, the ordered id list the picker and its roving-tabindex both walk, two\npicker options, and en/de strings.\n\nFive registration points exist per letter layout, traced end-to-end from how\nNavy is wired: (1) the Rust `LetterLayout` enum + its kebab-case deserialize arm\n+ the round-trip table, (2) the `.typ` asset plus the `include_str!` const and\n`letter_source` picker arm, (3) the DOCX `LetterDocxStyle` row and dispatch arm,\n(4) this `LetterLayoutId` union in `packages/shared`, and (5) the renderer's\n`LETTER_LAYOUT_IDS` + picker options + translations. Miss (4) and a stale\nfrontend id degrades to Classic silently; miss (5) and the layout exists in the\nbackend but nobody can pick it.\n\nThe picker previously had no guard tying its own `LAYOUTS` array to\n`LETTER_LAYOUT_IDS`, even though the keyboard handler walks the latter while the\nrender walks the former. A layout in one and not the other is invisible, and one\nin a different position makes ArrowDown jump to an option that is not next on\nscreen. Both are now a test failure; verified by deleting the monogram entry.\n\nThe \"pairs with ...\" wording in the descriptions is deliberate. Layout and\nresume template are orthogonal axes in this app -- a layout inherits whichever\npalette is active via `style_from_template`, so Sidebar under Regent gets\nRegent's burgundy rail. The strings suggest a pairing (Sidebar with the sidebar\nfamilies Atelier/Aria/Saffron/Deedy, Monogram with the bold-header families\nAwesome/Jake/Throughline) without coupling anything, which is the same posture\nNavy's \"Pairs with Cologne Navy\" already takes.\n\nBoth descriptions state that the decoration is dropped in ATS mode, so the\ndegradation is discoverable before export rather than a surprise in the output.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(export): stop the new letter layouts hyphenating, mis-reading names and claiming din\n\nReview round 1 on the sidebar/monogram layouts. Five findings, all in the two\nfiles this PR added.\n\nHyphenation (ruling 1, scoped): both new layouts now set `hyphenate: false`.\nTypst turns hyphenation on with `justify`, and a hyphenated break puts a real\nbreak in the PDF text layer -- \"microservices architecture\" extracts as\n\"architec-ture\" and an ATS tokenising on whitespace loses the keyword. Justify\nstays. The guard lives in `letter_lower`, the choke point every extraction in\nthis file already flows through, so no test can regress it -- but gated on a\n`NO_SOFT_HYPHEN_LAYOUTS` roster of the two new layouts, because the four SHIPPED\nones still hyphenate and a family-wide assert would go red on them today. The\nroster grows when the sweep lands.\n\nCorrecting my own measurement, per the critic: of the three U+00AD-adjacent\nbreaks I reported, only ONE is a genuine soft-hyphen break. The others are hard\nhyphens (\"end-to-end\"), which Typst tags with /ActualText and a conforming\nextractor recovers -- asserting on those would be asserting on a non-defect, so\nthe guard checks U+00AD only. And the shipped family is SIX .typ files, not the\nfive I wrote: letter_navy.typ was already there.\n\nInitials, two defects in one function. It searched each token for its first\nalphanumeric ANYWHERE, so a token that merely contained a letter contributed\none: \"Jane Smith (they/them)\" was JT. And it had no notion of a title, so\n\"Dr. Jane Smith\" was DS and the German \"Dipl.-Ing. Max Müller\" was DM. A token\nnow has to START with an alphanumeric, and a token that abbreviates a WORD (two\nor more letters before its period) is a title or qualification and is dropped --\nwhile a single-letter initial keeps counting, so \"J. Smith\" stays JS instead of\ndegrading to S.\n\nMy test for the first of those was a false positive and the critic was right\nabout why: the fixture was \"(she/her)\" and the expected \"JS\" is ALSO what the\nbroken code produced, because its S came from \"she\". Refixtured to\n\"(they/them)\", where correct and broken are distinguishable (JS vs JT), which is\nwhat made the mutation run meaningful.\n\nDevice on a letterhead-less letter: the PDF-side name falls back to the first\nLINE when no candidate name is supplied, and three renderer call sites pass an\nempty one -- so on a letter that opens at the salutation the device rendered\n\"DM\", from \"Dear Hiring Manager,\". Reproduced exactly. The DOCX renderer already\nrefused to treat an opening line as a name; `letterhead_initials` is the PDF\nside of the same guard, scoped to the DEVICE because what `letterhead.name`\nshould show there is a separate pre-existing question across all six layouts.\n\nSidebar's contact links go through `place()` with a negative dx into the margin,\nwhich is the documented annotation-loss shape. They survive -- but nothing said\nso, and a dropped /Annots entry is invisible to every text assertion, so it is\npinned now in both modes.\n\nDIN 5008: the header claimed market conventions were honoured while the\nrecipient block sits at 62mm, so a DE letter cannot show its address through a\nwindow envelope. Chose the honest comment over a locale-conditional rail, and\nnot only because that is heavy -- it is unachievable. Form B needs the\nAnschriftenfeld clear from 20mm, and a rail narrower than that cannot hold a\ncontact line at 9.5pt. DIN compliance and a contact rail want the same strip of\npaper, so Sidebar is a screen/attachment layout and the comment now says to pick\nClassic or Navy for the envelope.\n\nAlso: the monogram device was a bound `#let`, so it read\n`data.letterhead.initials` when the binding evaluated -- before the\n`\"initials\" in data.letterhead` check below it. The guard was dead. It takes the\ninitials as a parameter now, so the read happens only on the checked branch.\n\nEvery new guard was mutation-tested: removing `hyphenate: false` fails four\ntests, the old token rule fails the pronoun and title tests (DS vs JS), removing\nthe salutation guard yields DM and MG, and dropping the `link()` wrapper fails\nwith `found []`.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(shared): export the letter-layout id union from the contracts barrel\n\n`TemplateId` is re-exported here and the renderer imports it instead of keeping\na hand-synced copy, precisely so adding an id on one side surfaces as a `tsc`\nfailure rather than compiling as a silent subset. The letter-layout union had no\nsuch export, which is why the renderer carried a second copy of it.\n\nType-only and additive. The matching renderer-side switch (importing the union\nfrom `@ajh/shared` rather than redeclaring it) is applied in the working tree\nand typechecks, but is NOT in this commit: a frontend-author is editing that\nsame file in parallel for the ats-toggle work, and staging it here would sweep\ntheir uncommitted changes into my commit. It rides along with theirs.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(export): pass cover-letter render knobs as a struct, not eight positionals\n\nRemoves all five `#[allow(clippy::too_many_arguments)]` this PR had added, but\nthe lint was the symptom. The real defect it was silencing:\n\n    render_letter_pdf(text, template, contact, meta_name, market, lang, …)\n    generate_cover_letter_docx(text, meta, template, contact, lang, market, …)\n\n`market` and `lang` are both `&str`, adjacent, and the two renderers took them\nin OPPOSITE orders. Swapping them compiles silently and produces a letter with\nthe wrong page size and the wrong date convention — a US-shaped German letter,\nfrom a typo the compiler cannot see. `LetterRender { market, lang, layout, ats }`\nmakes that unrepresentable.\n\nApplied to the four eight-parameter entry points (`render_letter_pdf`,\n`render_letter_svg_pages`, `build_letter_world`, and both DOCX letter fns).\n`parse_cover_letter` deliberately keeps its positional parameters: it is at\nseven, under the lint's threshold, it has no use for `layout`, and pulling it in\nwould have forced sixteen parser tests to name a layout they do not exercise.\n\nBehaviour-preserving. The proof is the preview generator: re-running\n`generate_cover_template_previews` reproduces all sixteen committed SVGs\nbyte-identically, which it can only do if the letter world is built from exactly\nthe same inputs as before.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(ui): make the cover-letter ATS toggle reachable\n\nNo cover-letter surface exposed an ATS toggle, so the new Monogram layout put\n\"JS \" in front of the candidate's own name in the text layer with no off switch,\nand the shipped Sidebar/Monogram copy promised a switch that did not exist. Two\ngates were responsible: every toggle render site was resume-only\n(`!isCover && isDesignTier`, `activeOut === 'resume' && ...`), and all five\nforce-clear sites wiped atsMode the moment an ATS-TIER resume template was\npicked -- which is exactly the case where the letter is the only document the\nflag still drives.\n\nNO new wire flag. `BaseExportRequest.atsMode` already travels per DOCUMENT (one\nrequest carries one document), and after the Rust threading the letter reads the\nsame field as `data.opts.ats`. A second `letterAtsMode` would only be needed if\none request carried both documents. The only `packages/shared` edit is the\n`atsMode` JSDoc, which now states both halves of the semantics; no type changed.\n\nSemantics: atsMode is ONE per-export flag, and a surface shows the switch when\nthe flag can still change the document on screen -- resume via `isDesignTier`\n(unchanged; a documented no-op for ATS-tier templates per single_column.typ),\nletter via the new `isDecoratedLetterLayout`. `shouldClearAtsMode` replaces the\nbare `!isDesignTier(id)` at all five clear sites and refuses to clear while a\ndecorated letter is reading the flag.\n\n`isDecoratedLetterLayout` is a `Record<LetterLayoutId, boolean>` mirroring the\n`ats` gates in the .typ files one for one, so a new layout id fails tsc until\nsomeone decides whether it degrades. Navy is deliberately FALSE despite being a\n\"design\" arrangement: `letter_navy.typ` has no ats gate, so a toggle there would\nbe a switch that changes nothing. Classic/Refined likewise.\n\nThree letter surfaces render the layout picker, so all three get the toggle:\nStepTemplate (its card switch now carries one hint line per affected document,\nso a 'both' run with a design-tier template states both effects), and\nGenerationOutput + OutputPanelDone via a new shared `AtsModeToggle` -- extracted\nfrom GenerationOutput's inline block rather than copied, so the two toolbar\nstrips cannot drift in markup or a11y wiring. OutputPanelDone matters most: it\nREPLACES the wizard once a run is done, so a Monogram picked there previously\nhad no undo short of regenerating.\n\nCopy: new `atsModeHintLetter` (en+de) naming the three decorations. The layout\ndescriptions now say \"dropped by ATS-safe mode\" instead of \"in ATS mode\" so the\npromise names the control the user has to find, and Banded finally says it too\n(its band became ats-gated in this branch).\n\nTests, each mutation-verified by reverting the guard it covers:\n- `isDecoratedLetterLayout` / `shouldClearAtsMode` incl. the review's worst case\n  (ATS-tier template + monogram must NOT clear).\n- Toggle visible on banded/sidebar/monogram, absent on classic/refined/navy, on\n  all three surfaces; absent on the resume tab; absent when the host owns no\n  atsMode setter.\n- The flag survives to the wire: exportPDF puts `atsMode` on a COVER-LETTER\n  request, and useTailorGeneration carries the host's atsMode onto that call\n  alongside the layout -- so flipping the switch really changes the export.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ui): anchor, name, announce and release the ATS toggle\n\nFive ui-ux-expert findings on the reachable cover-letter ATS toggle.\n\n1 (HIGH) StepTemplate's switch row was `items-center`. With BOTH hint lines up\n  -- the design-tier resume + decorated letter case the previous commit added --\n  free-wrapping 10px German copy grows the row to ~80-90px and floats the 16px\n  track that far below the label it belongs to. `items-start` anchors it. jsdom\n  zeroes rects, so the guard is a class assertion on the row.\n\n2 The two hints both opened \"Recommended for ATS submissions --\" and read as one\n  line printed twice. Each now leads with its document (\"Resume: ...\", \"Cover\n  letter: ...\", \"Lebenslauf:\"/\"Anschreiben:\" in de, Sie-form kept) and the\n  shared preamble moves to the end of the resume lines. Neither line said the\n  switch drives BOTH documents, so `atsModeHintBothDocs` says it -- rendered\n  only when both actually apply, where the ambiguity exists.\n\n3 The switch had no aria-label, so its accessible name was the label plus every\n  hint concatenated. Now `aria-label` + `aria-describedby`, which is what the\n  @ajh/ui Switch primitive already does with its own description. The describedby\n  ids are emitted under the SAME guard as the lines they point at. The old test\n  used a /partial/ name match that passed either way; it now asserts the EXACT\n  accessible name, plus that every referenced id resolves.\n\n4 Nothing announced the toggle's ARRIVAL at the two toolbar surfaces (StepTemplate\n  sits inside GenerateWizard's aria-live wrapper; these do not) -- a screen-reader\n  user picked Monogram and got silence. Both now carry SectionTimeline's pattern:\n  an always-mounted `role=\"status\"` sr-only region, empty while hidden. Always\n  mounted because a live region cannot announce its own first render, and sr-only\n  because it is out of flow -- the toolbar's gap is unchanged.\n\n5 The stale-flag round-trip was real: decorated layout + ATS on -> switch to\n  Classic (toggle vanishes, flag stays true) -> pick Monogram again -> the toggle\n  returns silently ON and the user exports a Monogram letter with no monogram.\n  Fixed by SYMMETRY rather than copy: `shouldClearAtsMode` now also runs on the\n  layout-change path at all three surfaces, with the NEW layout's decoratedness\n  as its second input. Its existing two-input logic is exactly right -- a\n  design-tier resume template still legitimately reads the shared flag, so the\n  round-trip test asserts both halves: OFF when nothing else reads it, still ON\n  under Atelier. Driven through real host state, not prop swaps.\n\n6 (no code) The OutputPanelDone cover strip at the 900x600 floor is a shrink-0\n  sibling, not a scrollport; making it scrollable would mean restructuring the\n  panel. Recorded as a manual-QA item for the PR description instead of a\n  speculative guard.\n\nEvery fix mutation-verified: reverting all four StepTemplate changes fails 18\ntests, removing either live region or either layout-change release fails 4.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ui): describe the ATS toggle and release it in cover-only runs\n\nTwo verified ensemble findings plus the LOW advisory.\n\n1 The toolbar toggle had a NAME (\"ATS-safe mode\") and no DESCRIPTION: the hint\n  sat in `title` on a role-less div, which AT never reads. On a surface showing\n  two documents, \"which document does this change\" is the only part that\n  matters. Now the @ajh/ui Switch's own `description`.\n\n  That prop did not actually work without a `label`, which the finding could not\n  see: `aria-describedby={description ? descId : undefined}` is set in BOTH\n  branches, but the node carrying `descId` rendered only in the label row — so\n  passing `description` to a compact switch produced a DANGLING reference and AT\n  heard nothing at all. No caller passed that combination, so it never shipped.\n  The label-less branch now renders the description visually hidden (out of flow,\n  no layout change), with two guards in the primitive's own suite. The `title`\n  stays as the sighted-hover affordance. Renderer tests moved off `getByTitle` to\n  asserting the switch's accessible description.\n\n2 The layout-change release never fired in a cover-ONLY run: `shouldClearAtsMode`\n  read `isDesignTier(templateId)` as \"a résumé reads the flag\", but a cover-only\n  export renders no résumé from that template — it only supplies the letter's\n  palette. Confirmed path: target=cover, Atelier, Monogram + ATS on → Classic →\n  flag stuck → re-pick Monogram → toggle returns ON and the monogram is silently\n  dropped.\n\n  Fixed inside the ONE decision function, not with per-site booleans: a third\n  `resumeInRun` input (defaulted `true`, so every existing call is unchanged),\n  making the body read as \"does ANY document still read the flag?\" — one reader\n  per parameter. Threaded from what each surface actually knows: `!isCover`\n  (StepTemplate), `target !== 'cover'` (GenerationOutput, both paths), and\n  `Boolean(resumeOut)` (OutputPanelDone, where an empty résumé IS the cover-only\n  signal). StepTemplate's template path keeps its `!isCover` short-circuit: the\n  résumé template pick says nothing about the flag in a cover-only run, and the\n  layout path is what releases it there.\n\n  Round-trips both ways: target=cover reappears OFF despite a design-tier\n  template; target=both with a design-tier résumé still KEEPS it. One existing\n  OutputPanelDone case was pinning the keep-behaviour against an EMPTY resumeOut\n  (a cover-only shape) — given a résumé body, since that is the run it describes.\n\n3 `hintKey` is the key union instead of `string`, so a typo fails tsc.\n\nMutation-verified: reverting the primitive's description node + the toggle's\n`description` fails 10 tests (1 in @ajh/ui, 9 renderer); ignoring `resumeInRun`\nfails 4; typo'ing a hint key is now error TS2820.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(export): refuse a date as a monogram, and stop the tile shading its own gap\n\nTwo device defects, both confirmed by executed evidence.\n\nA letter whose first line is a DATE had no letterhead name, and both formats\ninvented one from it: \"12 March 2025\" rendered a device reading `12`. The PDF\nguard covered salutation, sign-off and subject; the DOCX line filter covered the\nsame three and nothing else. Two copies of one rule, and the fourth case was\nmissing from both.\n\nFixed at the cause rather than in two places: the DOCX renderer now calls the\nSAME `letterhead_initials` the `.typ` side reads via `LetterHead.initials`,\ninstead of the unguarded `monogram_initials` it used to call, and the unguarded\nfunction is no longer exported at all. Calling it directly is exactly how the\ntwo formats came to disagree, so the export is now the guarded one only — a\nfifth opening kind gets added once.\n\nBelt to that brace: `is_name_token` requires a leading LETTER, not merely an\nalphanumeric. An initial is never a digit. Worth having as well as the date\nguard, and the two are genuinely independent — verified by mutation: with the\ndate guard removed the letters-only rule still refuses `12`, but produces `M`\nfrom \"March\", which is a device built out of a month. My first DOCX test\nasserted only `not \"12\"` and therefore passed against that; it now asserts NO\ndevice is emitted at all, which is the thing only the date guard controls.\n\nSeparately: the DOCX tile shaded its own separator. `add_text(\"{initials}  \")`\nput the two spaces inside the shaded run, and docx-rs always writes\n`xml:space=\"preserve\"`, so the tint was painted across them and the tile ran on\npast the initials — the `.typ` square stops at the glyphs. Split into a shaded\nrun carrying the initials and a following unshaded run carrying the gap;\nconfirmed in the emitted XML.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(export): shrink the sidebar letterhead to fit its rail\n\nA long surname or a long e-mail ran straight out of the rail, across the 10mm\ngutter and into the body column. `place` neither reflows nor clips, and text\nonly breaks at spaces, so a single unbreakable token wider than the 38mm column\nhad nowhere to go. Reproduced before fixing: Aria + \"Àlvaro Papadopoulos\" put\nglyphs at x=137.5, 154.6 and 171.9 against a column that ends at 127.6pt, and\nthe e-mail added eight more.\n\nNow the widest token is measured at the intended size and the size scaled down\nby the ratio it overflows by, then walked down 0.25pt at a time until it really\nfits. Name and contact are fitted separately, but role and contact share one\nsize — they share the column, so fitting them apart leaves two visibly\nmismatched lines. Rail only: under ATS mode the letterhead is in the full-width\ncolumn where ordinary wrapping works, so it renders at base size and the fitter\nnever runs.\n\nTwo things the first attempt got wrong, both caught by measuring rather than\nreasoning:\n\nThe floor was \"60% of base\", which sounds conservative and is the opposite. A\ntemplate with a 24pt name floors at 14.4pt while \"Wojciechowski\" needs 13.3pt to\nfit 38mm — so the clamp re-created the overflow it existed to bound, on exactly\nthe templates whose large names cause overflow. It also tied the floor to the\ntemplate rather than to the paper. Now one absolute 6pt floor, and 6pt is\nmeasured: the widest case in the matrix (a 31-character e-mail in a 38mm rail)\nfits between 6 and 7pt, and a 7pt floor put it back in the gutter.\n\nShrinking, not breaking. An e-mail broken with a zero-width space would read\ncorrectly on paper and extract corrupted — the soft-hyphen defect again, so the\ntoken stays intact and the type gets smaller.\n\n`fit-limit` is 97% of the column: a standalone `measure()` under-reports the\nadvance the same token gets once laid out in a paragraph, by ~1.8% on the worst\ncase here.\n\nThe geometry test is now a matrix over five real template/name/e-mail\ncombinations instead of one lucky \"Jane Smith\", and it asserts that nothing\nstarts between the end of the rail's text block and the body column — the only\nvisible symptom, since `place` neither wraps nor clips. \"Alex Li\" and\n\"Jane Smith\" stay in so the fitter cannot pass by shrinking everything.\nMutation-verified: disabling the fit reproduces the original Aria overflow\nexactly.\n\nPreview SVGs are unchanged, and could not have changed: the cover-letter preview\ngenerator renders the CLASSIC layout per résumé template, so it never exercises\nthe rail. Re-ran it anyway — all sixteen byte-identical.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(export): stop the letter-layout enum listing its own ids\n\nThe doc comment enumerated \"classic\" / \"refined\" / \"banded\" as the kebab-case\nids. There are six variants. Listing them in prose is what went stale; the\nvariants below are the list.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: update letter-layout enumerations (6 layouts added)\n\nSync stale letter-layout descriptions across EXPORT_TEMPLATES.md, CONTEXT.md,\nresume-domain.md, and ADR-0007. Adds navy, sidebar, and monogram layouts.\nSidebar cannot meet DIN 5008 Form B (documented in letter_sidebar.typ header).\nATS mode drops decoration (rail, initials device) per layout while preserving\ncore letterhead and body.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(export): pin that the sidebar fitter leaves short letterheads alone\n\nThe matrix comment claimed the two short-name rows stopped the fitter passing by\nshrinking everything. It did not: every assertion in it was a glyph POSITION,\nand a globally shrunk block only ever produces LESS overflow, so forcing\n`fit-size` to always return its 6pt floor passed the entire matrix. The comment\ndescribed an intent nothing enforced.\n\nAdds the half that discriminates. Rows short enough that fitting must be a\nno-op are marked `fits_at_base`, and for those the rail's rendered type size is\ncompared against the SAME name in ATS mode, which always renders at the\ntemplate's full `name_pt`. The rail's base is `name_pt - 4pt`, so the two\nextents have to stand in exactly that ratio — self-calibrating per template,\nno hardcoded point sizes.\n\nReading the size is possible because typst-svg gives every glyph an explicit `x`\nadvance, so the width of one baseline scales linearly with the point size;\n`top_line_extent` turns that into a number and comparing two renders of the same\nstring cancels everything except the ratio.\n\nOne trap worth recording: the rail zone is bounded at the panel's outer edge\n(52mm), NOT at the body column's left edge. A body glyph lands a float-hair\nbelow 62mm, so bounding there picks it up as the rail's topmost line and\ncollapses the measurement to zero — which reads as a broken layout rather than a\nbroken measurement.\n\nMutation-verified both ways. Forcing the floor now fails the short rows —\n\"renders at 0.273x the ATS-mode name, expected 0.818x\" — while with the ratio\ncheck disabled the same mutation passes the whole matrix, long rows included,\nreproducing exactly the hole this closes.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(export): correct a stale comment after the letters-only token rule\n\n`is_name_token` requires a leading LETTER, not a leading alphanumeric — the\ninner comment still described the rule it replaced.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: honest comment on the layout-id pin, banded's band in the ats list\n\nThe LETTER_LAYOUT_IDS comment claimed satisfies pins exhaustiveness (it\nchecks membership only — the Record is the real pin); the glossary and\nresume-domain both omitted Banded from the ATS-droppable decorations and\nresume-domain carried a hard six-file count instead of naming the owning\nsymbols.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(ui): pin GenerateWizard's decorated-letter clear guard\n\nPR #988 review round 1. GenerateWizard's clear guard grew a decorated-letter\nbranch (ATS-tier template + target='both' + a letter whose layout ATS mode\ndegrades → do NOT clear, the letter is the only document still reading the\nflag), and it was the one surface with no direct test for it; StepTemplate,\nGenerationOutput and OutputPanelDone all have the equivalent pair.\n\nAdds that case in the file's existing shape, plus two controls so the pair\ncannot pass vacuously: an undecorated layout on the same pick DOES clear, and a\nrésumé-only run ignores the layout entirely. The undecorated control is also the\nfirst test here to drive the clear through `handleTemplateChange` at all — the\nexisting \"resets ATS mode\" case clicks a stub button that calls\n`onAtsModeChange(false)` directly, which proves prop forwarding, not the guard.\n\nMutation-verified: reverting the guard to the bare `!isDesignTier(id)` form\nfails exactly the new decorated-letter test (1 of 15); the two controls stay\ngreen under both forms, which is what makes the failing one discriminating.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(export): stop the new letter layouts printing the de subject label twice\n\nCodeRabbit's finding, confirmed by measurement before fixing. A DE letter\nrendered \"BETREFF\" over \"Betreff: Bewerbung als Software Engineer\" in both new\nlayouts. Counted on a real render: Sidebar and Monogram emit the label twice,\nClassic / Refined / Banded / Navy emit it once.\n\n`parse_cover_letter` publishes `data.subject` VERBATIM, market label included,\nand these two layouts printed a caption on top of it without stripping. Fixed by\nreusing the rule letter_refined.typ and letter_navy.typ already carry —\n`strip-subject-label` on the body, and suppress the caption when the subject\nstill carries a marker of its own. That is also what the DOCX renderer does in\n`strip_market_label`, so the PDF and DOCX sides now agree; they did not before,\nwhich is the part that made this more than cosmetic.\n\n`subj-used` gating is untouched: whether a subject renders AT ALL stays the\nmarket's call (US omits it), and that is a separate question from how it renders\nonce the market has asked for one.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(export): close the two gaps that let the duplicate de label ship\n\nBoth gaps were named in the review and both were real.\n\n(a) The PDF market-gating test asserted `de.contains(\"betreff\")`, which is\nsatisfied by one label and by two. Now an occurrence COUNT of exactly one.\n\nThe count alone turned out not to be enough, and the mutation run is what showed\nit: reverting the strip left the test GREEN, because the caption-suppression\nguard independently hides the caption once the body still carries its label —\none occurrence either way. Added the assertion that says WHICH occurrence\nsurvived: the colon only exists on an unstripped body, so `not\ncontains(\"betreff: bewerbung\")` pins that the caption is what remains.\n\n(b) No DOCX test had ever used a market with a subject label — every\n`letter_request` passes `locale: None`, which resolves to label-less `intl`. So\nthe DOCX suppression rule was running untested, and with neither side covered\nthe two formats could disagree in silence. Added a DE case across all four\ncaption-bearing layouts, with the same count + colon assertions, which is now\nthe PDF/DOCX parity pin for this behaviour.\n\nMutation matrix, three ways: reverting the STRIP alone fails on the colon\nassertion; reverting the CAPTION SUPPRESSION alone stays green; reverting BOTH\nfails on the count with \"found 2\", reproducing the shipped defect. The middle\nresult is worth recording rather than hiding — with the strip in place the\nsuppression is unreachable for these two layouts in the current market set, so\nit is defence for a future market, not live logic, and no honest test can\nisolate it today. The DOCX test was mutation-checked separately against\n`strip_market_label` and is not vacuous.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-13T14:18:32+02:00",
          "tree_id": "ea456e85b834c623602be1b1e15852c8a50234a5",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/57f782f314e1f9ecb776827f4cc9792ab7d7bf2d"
        },
        "date": 1786624914686,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2233937,
            "range": "± 128048",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2624621,
            "range": "± 18373",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 295319,
            "range": "± 11424",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "546fc36df4ae75a216d0713284b4fe2e0968b53c",
          "message": "feat: polish batch — letters sweep, cache keys, contrast, ai-status, italian tells, autopilot dates (#989)\n\n* feat(autopilot): thread the job posting's publish date into found jobs\n\nAdds FoundJob.posted_at (epoch ms, serde-defaulted so legacy records load\nunchanged) alongside found_at, copies it from JobPosting in build_found_job,\nand backfills/refreshes it in merge_found_jobs on re-surface using the same\nfill-without-clobbering pattern as board/description/salary. Hand-syncs the\nTS AutopilotFoundJob.postedAt contract (response types aren't gen:ipc'd).\nSort order (by_rank) is untouched per ADR-020 addendum.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ui): extend light-scheme opacity remap past the /20-/50 enumeration\n\ntext-foreground/NN and border-foreground/NN are Tailwind opacity-suffix\nutilities compiled to literal escaped selectors over --color-foreground.\nutilities.css only remapped /20-/50 for light; every other value in use\nrendered at its raw alpha, near-invisible on light's near-black ink\n(border-foreground had ZERO coverage: /10-/25 raw ~1.2-1.7:1).\n\nCentral-only fix: add text-foreground/15 and /55 (the only two escaped\nvalues below the point where raw alpha already clears 4.5:1 on light),\nplus a new border-foreground/10,/12,/15,/20,/25 block. /60+ (text) and\n/70 (border) are deliberately left unmapped — already >=AA/3:1 raw, so\nremapping them would only re-tint hundreds of call sites for no gain.\nComponents keep their existing classes.\n\nPins the fix with a red-first contrast seam test (independently\nrecomputes WCAG contrast from the literal color-mix % in the\nstylesheet) plus a duplicate-rule guard, mirroring the existing\nlight-scheme-contrast.test.ts pattern.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(dashboard): AI-status widget reads the active provider, not Ollama only\n\n`system_health.ai.ready` (Rust) is hardcoded to probe the LOCAL Ollama\ndaemon regardless of which provider is actually active, so a user on\nOpenAI/Anthropic/Gemini/a CLI agent — with local Ollama simply not\nrunning — saw a false \"AI Model not available\" despite a fully\nconfigured model.\n\nFrontend-only fix (no src-tauri change needed): the AI-model row now\nreads `useCanUseAI()` (already the app-wide, provider-kind-aware\nreadiness source used by ModelSelector/Analyze/TailorFlow/resume\nbuilder) and `useSelectedModel()` for the detail label, instead of the\nOllama-only `h.ai` field. Reuses the existing `aiSetup.*` copy for the\nblocked reasons (addApiKey/selectModel/installCli) — no new i18n keys.\n\nPinned with a red-first test suite that keeps the mocked Ollama probe\nreporting `ready: false` throughout, proving the row no longer depends\non it.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(cover-letter): add provider + model to the company_brief cache key\n\nThe company_brief KvCache row was keyed on company name alone, so a\nuser who switched AI models kept getting the OLD model's cached brief\nfor the full 7-day TTL. ADR-017 key discipline (StageIdentity\nprecedent, #985): anything that reaches the provider and changes the\nanswer belongs in the key — the brief is the direct output of the\nactive provider's own model doing web search + synthesis\n(Completer::research), so provider + model are cache-key terms.\n\nChecked salary_research for the same gap: it has it too, but\nSalaryResearch::enrich is generic over a SalarySearcher trait (for\ntestability without a live AppHandle) which exposes no\nprovider_id()/model(), so it isn't the same one-line fix — flagging\nas a follow-up rather than fixing it here.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(export): un-hyphenate all six letter layouts, suppress fabricated letterhead names\n\nItem 1: add `#set text(hyphenate: false)` to the four older letter layouts\n(letter.typ, letter_refined.typ, letter_banded.typ, letter_navy.typ), mirroring\nthe guard Sidebar/Monogram already had. Extend NO_SOFT_HYPHEN_LAYOUTS to the\nfull six-layout roster and add a test that actually routes all six through the\nguard's choke point (previously only Sidebar/Monogram were ever exercised).\nJustify stays on for every market including German (full-measure columns).\n\nItem 2: parse_cover_letter now suppresses the letterhead NAME (not just\nMonogram's device) when the \"first line\" fallback isn't a name — a date or\nsalutation opening — via a new shared `is_letterhead_name` predicate. Applied\nat both DOCX renderer call sites too, and fixed a genuine pre-existing Typst\ncrash (`none.split`) plus an empty-tinted-rail defect in letter_sidebar.typ\nthat a fully-suppressed letterhead exposed.\n\nRegenerated preview SVGs: 1 changed (cover-template-previews/swiss-minimal.svg,\na genuine hyphenation-removal reflow).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* feat(autopilot): surface posting date + per-card date sort on found jobs\n\nRenders `job.postedAt` as a relative-time chip (same helper/namespace as\nthe Jobs page, absolute-time title tooltip) and adds a per-card view-side\nsort (relevance/newest/oldest) in the found-jobs panel header. Sort is\nlocal component state, not a session-store field: two expanded autopilots\nmust be sortable independently. The comparator (`sortFoundJobsByDate`)\nnever mutates `ap.foundJobs` — it always sorts a fresh copy, since the\npersisted array order feeds AI-note recipient selection (ADR-020).\n\n* feat(prompts): curate an Italian AI-tell lexicon, wire the it dispatch arm\n\nTask #10 established the four-rule checkability bar for the curated tell\nlexicon (fixed form / zero factual content / unconditional prompt ban / no\nhidden domain meaning); Italian was falling through the empty-slice dispatch\nbecause ctx.lang comes straight from the user-configurable output-language\nsetting, so Italian output already reached voice.* validators today with\nzero coverage.\n\n- natural-voice.ts: genuinely curated Italian lexical/prose/opener arrays\n  (not a bulk translation of the English or German lists), each entry\n  justified against the four-rule bar in the doc comments; rejected\n  candidates (svolge un ruolo fondamentale, implementare soluzioni\n  innovative, chiave di volta, egregi signori) recorded with rationale.\n  all'avanguardia carries Italian elision on purpose, exercising the\n  U+2019/U+0027 apostrophe fold on the checked (not just opener) path.\n- gen-prompts-rust.ts: rustLookupFn dispatches \"it\" alongside \"de\"/\"en\";\n  regenerated lexicon.rs.\n- validate/content/test.rs: fixed the two lexicon contract tests that\n  asserted Italian was uncurated, plus 4 new Italian fixtures (checked-tier\n  fires, apostrophe-fold mutation, clean-letter negative control, résumé\n  lexical-tier vs letter prose-tier split).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(research): key the company brief on the full completer identity and search backend\n\nThe company_brief cache row keyed on the company name alone, so switching\nprovider, model or the resolved search backend (Native vs Exa flips on\ncredential presence at call time) kept serving the other configuration's\nbrief for its 7-day TTL. The key now derives from StageIdentity wholesale\n(the canonical Completer-to-key-terms type, cross-referenced both ways so\nthe derivations cannot drift) plus a new Completer::search_backend() that\nmirrors the real research routing predicate for predicate. Role stays\ndeliberately unkeyed — one paid search per company per identity is the\ndocumented cost tradeoff. Separator is the precedent's U+001F.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ai): useCanUseAI local-server branch now probes daemon health\n\nHIGH finding from frontend-reviewer: useCanUseAI's local-server (Ollama)\nbranch never read useSystemHealth at all, despite provider-meta.ts\ndocumenting this `kind` as \"detected via a health probe\". A configured\nmodel with the daemon crashed/still booting/quit read as usable —\nexactly the case the old (now-replaced) `h.ai` code in AISystemStatus\ngot right. Heals all 14 call sites of the shared hook.\n\nAdds a distinct `startOllama` block reason (new aiSetup.* en+de keys,\nwired into both AiSetupHint's MESSAGE_KEY map and AISystemStatus's own\ncopy) and keeps the existing \"no reason yet\" cold-boot convention while\nhealth is still in flight, so the widget shows \"checking\", not a\npremature \"not available\".\n\nCoverage gap this closes: every existing consumer test mocks\nuseCanUseAI wholesale, so its real branches had zero coverage anywhere\nin the repo. Adds useCanUseAI.test.tsx exercising the REAL hook via a\nmock AppClient — local-server (not-ready/ready/no-model/health-still-\nloading), cli-agent, and cloud branches all pinned. The health-loading\ncase explicitly waits for useActiveConfig to settle first so it can't\npass trivially on the shared initial cold-boot value.\n\nMutation-tested: reverting the health gate turns 2 of the 9 new tests\nred at the exact assertions.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ui): fold /60 into the light-scheme remap to fix the /55-/60 seam\n\nMEDIUM finding from frontend-reviewer: the /55 remap (boosted to 69%\nalpha, 6.0:1) overshot the deliberately-unmapped /60 (raw 60% alpha,\n~4.45:1 — itself sub-AA, not \"already clears 4.5\" as the prior comment\nincorrectly claimed) — a lower opacity step rendering MORE opaque than\na higher one. Maps /60 to 72% (taper-consistent with the existing\ncurve, computed with the test file's own contrast formula), which\nfixes both the ordering inversion and /60's own sub-AA rawness.\n\n/65 stays deliberately unmapped: its raw 65% independently clears\n4.5:1, so remapping it would re-tint ~9 call sites for no contrast\ngain. It now sits slightly below the boosted /60 (65% vs 72%) — a\nmuch smaller, purely cosmetic ordering wrinkle between two\nalready-AA-passing steps, left as-is and disclosed here rather than\nchased into a cascade (the same gap reappears at every subsequent\nstep up to ~/80 for this boost curve, which would balloon into the\nwide-diff sweep docs/NEXT_ISSUES.md already tracks separately).\n\nlight-scheme-contrast.test.ts: /60 now covered by the standard\nduplicate-rule + boosted-above-raw + raw-fails-AA + boosted-clears-AA\nchecks, plus a new explicit ordering regression guard (mapped /55 <\nmapped /60) — the exact defect class this fix addresses. Mutation-\ntested: reverting /60 to raw fails 3 assertions, including the\nordering guard.\n\ndocs/NEXT_ISSUES.md: dropped /55 from the \"still sub-AA\" list (now\ncentrally fixed); /60 was never listed there to begin with.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(export): DOCX contact line must survive a suppressed letterhead name\n\nresume-export-expert review of 9dfb2cb3 (HIGH, empirically probed): at both\nDOCX call sites, profile_contact_md emission was nested inside the\nis_letterhead_name branch, so a nameless (date- or salutation-opening) letter\nwith a real ContactProfile attached lost the user's contact info from the\nDOCX entirely, not just the fabricated name — strictly worse than the\npre-guard behaviour, where a garbage name at least kept the contact line\nalive. PDF was unaffected (contact_runs built independently of name_text),\nreopening the exact PDF/DOCX divergence the previous commit's doc comment\nclaimed to close.\n\nRestructured both renderers around a `renders_name` decision made once per\nletter: the name paragraph (plus Monogram's device / Banded's band, which\nstay gated on a rendered name so no empty tinted box appears) renders only\nwhen `is_letterhead_name` holds, but the profile contact paragraph now always\nrenders when a ContactProfile is attached, independent of that decision —\nmirroring the PDF parser's `contact_md`, built unconditionally before any\nline is classified. Replaced the `docx.document.children.is_empty()` \"first\nline\" proxy (which silently broke once contact could be emitted without a\nname) with an explicit `header_line_seen` flag.\n\nAdded contact_profile_survives_even_when_the_letterhead_name_is_suppressed:\ncontact: Some(..) on both the date-opening AND salutation-opening fixtures\n(previously both test builders hardcoded None), across all six layouts;\nasserts the contact line survives and no name-sized run appears. Mutation-\nverified at both call sites: re-nesting the contact emission inside\n`renders_name` reproduces the exact reported defect (red), restoring the fix\nturns it green again. Existing paragraph-ordering tests (name-then-contact\nfor a valid name) stay green — the restructure preserves render order for\nthe common case.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(autopilot): close the merge-clobber gap and backfill posted_at at dedup time\n\nAddresses rust-backend-architect BLOCK on 2cab2d27:\n\n- autopilot::merge_found_jobs's posted_at guard was untested in the direction\n  that matters: existing Some(X) + resurfaced None must keep X (a board that\n  stops sending a date must not erase a known one). Added the preserve-case\n  test; mutation-checked against an unconditional-overwrite mutant (killed).\n\n- scraping::engine::dedup_cross_source (cross-domain touch, flagging for PR\n  review) upgraded description/extra on cross-source dedup but silently\n  dropped posted_at — an aggregator hit WITH a date, deduped against a\n  dateless direct-board hit for the same canonical key, permanently lost the\n  date. Backfills the incumbent's posted_at from the challenger the same\n  fill-without-clobbering way as the extra union, with a test mirroring the\n  existing salary-survives-dedup case; mutation-checked both the drop-fix and\n  an unconditional-overwrite variant (both killed by the one test).\n\n- Reworded doc comments (FoundJob.posted_at, build_found_job,\n  AutopilotFoundJob.postedAt) that framed posted_at as aggregator-only —\n  at least nine direct full-text boards (arbeitnow, ashby, breezy, comeet,\n  germantechjobs, berlinstartupjobs, lever, jobicy, arbeitsagentur) parse it\n  too.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: name both remap ordering residuals, not just the smallest\n\nThe /60 fold's disclosure named /65 (11 call sites) but left /70 (143 call\nsites, the most-used step in the ramp) to an abstract caveat; both sides\nclear AA so it is cosmetic ordering, tracked with the sweep.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* style: prettier pass on the residuals note\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(prompts): demote spirito di squadra / all'avanguardia, close Italian gaps\n\nAddresses job-match-expert's block on the Italian AI-tell commit:\n\n1. HIGH: demote spirito di squadra to prompt-only. Fails rule 2 in object\n   position (\"ho costruito lo spirito di squadra di 22 atleti\" is a truthful\n   coach/HR/L&D achievement, not decoration); the per-phrase source exemption\n   is verbatim-scoped, and an Italian job ad listing it as a requirement\n   reproduces the documented paramount ad-echo hole. Still banned at the\n   prompt, so zero checked-tier coverage is lost.\n2. MEDIUM: demote all'avanguardia to prompt-only for consistency with the\n   already-rejected chiave di volta — \"avanguardia\" names the real Italian\n   Futurist art movement (rule 4). The apostrophe-fold mutation now proves\n   itself on the opener \"in riferimento all'annuncio\" only; a second lexical\n   proof was redundant with contraction_ai_tells_fire_with_either_apostrophe's\n   existing EN coverage of the same fold function.\n3. MEDIUM: added studi dimostrano / gli esperti concordano / come è noto a\n   tutti / eccellenti capacità comunicative to the checked tier (EN/DE check\n   their full weasel-word + self-praise sets; the omission was a silent gap).\n4. LOW: documented the un ventaglio di / un ampio-vasto ventaglio di residual\n   (intervening adjective breaks the fixed-form substring).\n5. LOW: fixed the stale \"en/de have curated lexicons\" comment to en/de/it.\n\nRegenerated lexicon.rs (13 lexical + 4 prose = 17 checked entries, down from\n15 after -2/+4). Rewrote the slop-letter fixture for the new entry set, added\na dedicated negative control for the two demoted phrases in their\ntruthful/domain-legitimate context, and simplified the apostrophe-fold test\nto the surviving opener fixture.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* test(validate): make the avanguardia negative control use the entry's own surface form\n\nsull'avanguardia can never match the all'avanguardia entry, so the\nfixture passed whether or not the demotion existed — the critic's\nmutation proved it a non-guard. The dative form fires when the entry is\nre-added and stays silent while it is demoted, which is the test's job.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs: update contrast-sweep follow-up status and add DOCX date-contact conflict note\n\n* docs: retire the next-issues backlog file from the repo\n\nOwner call: the deferred-items ledger does not belong in the repository.\nThe file is deleted, gitignored against re-adds, its content archived\noutside the repo, and every citation across docs, css comments and rust\ndoc comments reworded to stand alone.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* style: prettier pass on the italian lexicon test\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(arch): relocate SearchBackend to a new L1 ai_provider module (R7)\n\nPre-push architecture gate caught it: cover_letter/research/mod.rs (L2)\nimporting commands::ai_provider::search::SearchBackend (L3) is an\nupward layer dependency, introduced by the round-2 search-backend key\nterm. pipeline/mod.rs's Completer::search_backend() had the identical\nupward reference in its return-type annotation (not flagged by the\ngate, since pipeline->commands is separately allowlisted debt (W-1),\nbut the same shape).\n\nFixed structurally, not with an allowlist entry: split the pure\nSearchBackend enum + as_str() out of commands::ai_provider::search\ninto a new top-level src/ai_provider/ module, classified L1 in both\ntests/architecture.rs and docs/architecture-rules.md (a deliberately\nminimal seed of the roadmap's \"relocate ai_provider out of commands/\"\nitem -- only the value type moved; search_backend_for's credential/\nAppHandle-reading resolution logic stays L3, in commands/ai_provider,\nwhich now `pub use`s the relocated type so its existing consumers\n(resolve_search_backend, searcher_for, search_backend_for, and the\nsearch/test.rs suite via `use super::*`) are unaffected). Both L2\ncall sites (cover_letter::research, pipeline::Completer::search_backend)\nnow import crate::ai_provider::SearchBackend directly -- L2 depending\non L1, no exception needed.\n\nVerified: cargo test --lib --all-features (4137 passed), cargo test\n--test architecture (18/18, including r7_no_upward_layer_imports and\nr8_no_oversized_modules -- the latter's earlier failure was a\nconcurrent, in-progress export/typst_engine/letter.rs split by\nanother agent in this shared worktree, unrelated to this change and\nresolved on its own once that split completed), clippy --all-targets\n--all-features --locked -D warnings (clean), fmt (clean).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* refactor(export): split letterhead name-guard family out of letter.rs (R8)\n\nPre-push architecture gate (R8, hard cap 1400 LOC): letter.rs had grown to\n1497 LOC from the name-guard implementation + test additions in the last two\ncommits. Per the recorded rule, extracted a cohesive block VERBATIM into a\nnew sibling file rather than trimming anything.\n\nNew apps/desktop/src-tauri/src/export/typst_engine/letterhead.rs holds the\nletterhead name-guard family — looks_like_date, monogram_initials,\nis_letterhead_name, letterhead_initials — plus their 18 directly-related\nunit tests (moved byte-for-byte; behaviour and doc comments unchanged except\nlooks_like_date's visibility, bumped from private to pub(super) so\nletter.rs's parse_cover_letter can still call it, and one dangling\nintra-doc-link comment fixed on LetterHead.initials).\n\nPublic surface identical for every consumer: typst_engine/mod.rs's\npub(crate) use re-exports (letterhead_initials, is_letterhead_name) now\npoint at the new module instead of letter, so docx/mod.rs's\ncrate::export::typst_engine::is_letterhead_name(...) /\n::letterhead_initials(...) call sites are untouched.\n\nletter.rs: 1497 -> 1048 LOC. letterhead.rs: 519 LOC (under both the 1400\nhard cap and the 600 soft-watch threshold). Test count preserved exactly\n(15 in letter.rs + 18 in letterhead.rs = the original 33).\n\nGates: cargo test --lib --all-features (4137 passed), cargo test --test\narchitecture (r8_no_oversized_modules green), clippy --all-targets\n--all-features --locked -D warnings (clean), fmt (clean).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix: unglue the gitignore entries the append concatenated\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(autopilot): unify postedAt presence contract in date-chip render guard\n\nCodeRabbit PR #989 round 1 (Minor): the date-chip render guard used\n`job.postedAt && (...)` — the classic 0-&&-JSX footgun (a stray \"0\" text\nnode for postedAt: 0) — while sortFoundJobsByDate already treats 0 as\ndated via `typeof === 'number'`. Switch the render guard to the same\ntypeof check so sort and render share one presence contract.\n\n* docs(autopilot): correct the merge_found_jobs intra-batch dedup comment\n\nPR #989 CodeRabbit round 1 MAJOR (verified against HEAD, no code change):\nthe incoming-batch intra-collapse's Some(kept) branch (autopilot/mod.rs\n~996-1000) doesn't carry posted_at forward when a later same-key duplicate\nin one batch supersedes the description. Traced the sole production caller\n(commands::autopilot::autopilot_run) end to end:\n\n- engine::dedup_cross_source already collapses the FULL multi-board raw\n  postings batch on canonical_job_key BEFORE anything else runs, and (per\n  the prior fix) already backfills posted_at there without clobbering a\n  known date (scraping/engine/mod.rs:1051-1058, covered by\n  dedup_cross_source_backfills_posted_at_but_never_clobbers_a_known_date).\n- build_found_job (commands/autopilot.rs:712) copies url/title/company\n  verbatim, so merge_key(build_found_job(p)) == canonical_job_key(p.url,\n  p.title, p.company) — the exact key dedup_cross_source already used.\n- Every step between (keyword filter, sort_by, cluster_aware_retain,\n  semantic rerank) only filters/reorders/mutates score fields — none adds\n  a duplicate row.\n\nSo `incoming` can never contain two entries sharing a merge_key via this\ncaller: the Some(kept) branch is provably unreachable in production, and a\nbackfill there would be dead code. Reworded the comment (previously claimed\nan inaccurate \"same-source only\" reason — dedup_cross_source is source-\nagnostic) to state the real reason and cite the caller/evidence, so this\nexact false positive doesn't recur on a future review pass.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* docs(autopilot): posted_at is publish-or-updated, not always a pure publish date\n\nPR #989 CodeRabbit round 1 MEDIUM: verified against every board that reads\nposted_at (the 4 aggregator providers + the 9 direct full-text boards cited\nin the prior round, plus wwr/remoteok/remotive/ycombinator as a sanity\nsweep). Which field each actually reads:\n\nGenuine creation/publish field:\n  Adzuna `created` (adzuna.rs:463), JSearch `job_posted_at_datetime_utc`\n  (providers.rs:250), Apify LinkedIn `postedAt` (providers.rs:736),\n  Arbeitnow `created_at` (arbeitnow/mod.rs:20-21), Ashby `published_at`\n  (ashby/mod.rs:162-164), Breezy `published_date` (breezy/mod.rs:157),\n  Jobicy `pubDate` (jobicy/mod.rs:39,127), Lever `createdAt`\n  (lever/mod.rs:162), GermanTechJobs `<pubdate>` XML tag\n  (germantechjobs/mod.rs:162-177), BerlinStartupJobs/WeWorkRemotely\n  Atom/RSS `entry.published` via feed-rs (berlinstartupjobs/mod.rs:71,\n  wwr/mod.rs:66), RemoteOK `date` (remoteok/mod.rs:26,132), Remotive\n  `publication_date` (remotive/mod.rs:18-19,78), YCombinator (HN) `time`\n  (ycombinator/mod.rs:154).\n\nUpdate/current-only field (confirmed misleading claim):\n  Jooble `updated` (providers.rs:349 — the field is literally named\n  \"updated\", already flagged in that file's own comment), Comeet\n  `time_updated` (comeet/mod.rs:49,96,157 — no creation field exists at\n  all in the modeled career-ops shape), Bundesagentur für Arbeit\n  `aktuelleVeroeffentlichungsdatum` = \"current publication date\"\n  (arbeitsagentur/mod.rs:238-243 — the field name itself says \"current\").\n\nNone of the three is egregiously misleading (all are honest upstream API\nfields), so per (a): reworded FoundJob::posted_at, build_found_job's\ncomment, and AutopilotFoundJob.postedAt to \"publish-or-last-updated date,\nas reported by the board\" and named which sources are update-only, instead\nof claiming a uniform publish date.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ai): distinguish a rejected health probe from \"still checking\"\n\nPR #989 CodeRabbit round 1, two MAJOR findings.\n\n1. useCanUseAI read only `useSystemHealth().data`, never `isError`. A\n   REJECTED health query (network blip, backend hiccup) leaves `data`\n   undefined forever (retry: false) — indistinguishable from \"still\n   pending\" in the local-server branch (permanent, silent \"checking\"\n   that never resolves) and silently misreported as `installCli` (\"go\n   install the CLI\") in the cli-agent branch, since `?? false` treated\n   undefined-from-error the same as undefined-from-loading. A consumer\n   rendering AiSetupHint with no reason then fell back to its own\n   default (`aiSetup.addApiKey`) — wrong for every affected provider\n   kind. Now three-way: pending (no reason, \"checking\") → error (new\n   `healthUnavailable` reason) → settled (the real gate). New\n   aiSetup.healthUnavailable en+de keys (\"Couldn't check AI status. Try\n   again in a moment.\" / \"KI-Status konnte nicht geprüft werden.\n   Versuche es in Kürze erneut.\"), wired into both AiSetupHint's\n   MESSAGE_KEY and AISystemStatus's own copy.\n\n2. AISystemStatus.test.tsx's reason→key table (added when `startOllama`\n   shipped) never grew a row for it — a silent AI_REASON_KEY drift back\n   to the addApiKey fallback would have stayed green. Adds both the\n   startOllama and the new healthUnavailable rows.\n\nCoverage: useCanUseAI.test.tsx gains a rejected-health-query case for\nboth consuming branches (local-server, cli-agent).\n\nMutation-tested per the coordinator's exact asks: dropping startOllama\nfrom AI_REASON_KEY turns its table row red (falls back to addApiKey);\ndisabling the isError checks turns both rejected-query tests red\n(stalls at bare `{ canUse: false }`, no reason, forever).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(ui): border-foreground/10-20 still failed 3:1 after the text-taper reuse\n\nPR #989 CodeRabbit round 1 append (HIGH): the border-foreground/{10,12,\n15,20} boosts were derived by reusing the TEXT remap's percentages\nverbatim (37/38/41/44%) on the theory that border's lower 3:1 floor\nwould clear \"with margin\" at the same alpha that clears text's 4.5:1.\nVerified with the test file's own contrast formula: FOUR of the five\nborder steps still measured under 3:1 (2.29 / 2.35 / 2.54 / 2.76:1) —\nreusing the same percentage doesn't reuse the same margin once the\nfloor itself is lower, and these raw steps (10-25%) are so far below\neither floor that the text taper never gets them there. Only /25 (48%,\n3.09:1) actually cleared.\n\nRe-derived independently for border's 3:1 floor (~47% alpha): /10→49%,\n/12→51%, /15→53%, /20→56%, /25→59% — /25 had to move too, since its\nprior 48% left no room below it for the other four to clear 3:1 while\nstaying ordered beneath it.\n\nlight-scheme-contrast.test.ts: added a per-step \"clears 3:1 once\nboosted\" assertion for every BORDER_STEP (mirrors the text /55↔/60\npair) plus a full-range ordering guard (/10 < /12 < /15 < /20 < /25).\nMutation-tested: reverting to the old percentages fails all four\nnew \"clears 3:1\" assertions at the reviewer's exact numbers.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(export): shared letterhead-name resolution + dead-branch cleanup (CodeRabbit round 1, PR #989)\n\nItem 1 (MAJOR, verified before fixing): both DOCX line-scanners resolved\ncandidate_name via `meta.and_then(...).map(...).unwrap_or(&clean)` with no\nempty-string filter, while the PDF parser already filtered\n(`.filter(|s| !s.trim().is_empty())`) before its own fallback. With the\nreachable `candidate_name: Some(\"\")` shape and a REAL name on the letter's\nown first line, DOCX suppressed it (Some(\"\") beat the fallback) while PDF\nstill rendered it — a genuine PDF/DOCX divergence.\n\nExtracted ONE shared `resolve_letterhead_candidate` helper (in the sibling\n`letterhead` module, re-exported the same way as `is_letterhead_name` /\n`letterhead_initials`) so the PDF parser and both DOCX scanners make the\nidentical \"prefer meta_name unless blank\" decision — one fix, not two\ncopies. Mutation-verified: dropping the filter reproduces the exact\nregression (a DOCX max header font size of 21 instead of 40 — no name-sized\nrun at all) in the new red-first test.\n\nItem 2: extended the date-opening DOCX test to loop [None, Some(String::new())]\n(matching the existing monogram-device test's matrix) and added the missing\npositive case — a NAMED first line with candidate_name: Some(\"\") must still\nrender a name-sized header run, matching PDF. Plus four direct unit tests on\nthe extracted helper itself (the cheapest point to catch this class of\nregression).\n\nItem 3: letterhead.rs's looks_like_date — has_digit was dead in the final\n`has_year || (has_digit && has_sep)` expression (the early `if !has_digit {\nreturn false }` already guarantees it); simplified to `has_year || has_sep`.\n\nItem 4 (declined, in-thread rationale): recovering a candidate name from the\nsign-off line when the letterhead is suppressed is the same is-a-name guess\nthis whole guard family exists to refuse, just relocated to a different line\n— a sign-off closing is no more guaranteed to be a personal name than a\nletter's first line (a shared/team mailbox, a typed \"[Your Name]\" placeholder,\nor a dropped role line all read as plausible text there too). The current\nno-name state is the deliberately owner-approved honest degradation for\nexactly this scenario (\"suppress the name line entirely... the letter simply\nhas no letterhead name\"); recovering a guessed name from elsewhere reopens\nthe fabrication risk that decision closed, from a different position. Left\ndeclined per the reviewer's own advisory marking.\n\nGates: cargo test --lib --all-features export:: (352 passed), cargo test\n--test architecture (18 passed, r8_no_oversized_modules green — letter.rs\n1053 LOC, letterhead.rs 603 LOC, both under the 1400 cap), clippy\n--all-targets --all-features --locked -D warnings (clean), fmt (clean).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* fix(cover-letter): resolve the search route once, close the TOCTOU on the company_brief key\n\nPR #989 CodeRabbit MAJOR: search_backend_for resolved the backend for\nthe company_brief cache KEY, then searcher_for independently re-read\nthe same credentials inside the fetch (Completer::research), with an\n.await-ed provider call in between. If credentials changed in that\nwindow (an Exa key added/removed mid-flight), the brief could get\ncached under a key naming a backend that did NOT produce it, and\nserve the mismatch for the full 7-day TTL.\n\nChose the minimal honest shape over a new routing struct-with-behavior:\nresolve ONCE (commands::ai_provider::search::CompanySearchRoute::resolve),\nderive the key term from that SAME resolved value (route.backend()),\nand thread the value itself through to the fetch\n(Completer::resolve_search_route -> cache_key -> cache check ->\nCompleter::research_via(route, ..) on a miss). search_backend_for and\nthe old Completer::research/search_backend methods are deleted --\nprefer-unrepresentable: there is now exactly one function\n(CompanySearchRoute::resolve) that reads these credentials for a\ncompany-research pass, and the only way to construct the type.\n\nAlso folds in the AI-review MEDIUM (same PR, dovetails with this fix):\nhas_native_search's bypass used to be checked independently in two\nplaces (search_backend_for and the old Completer::research) -- a\nsecond, unforced way the two could drift. Deleting the duplicate\ncollapses it to exactly one check, inside CompanySearchRoute::resolve\n-- verified by grep (see report), not just by construction.\n\nRegression tests: CompanySearchRoute::resolve_via_backend is the pure\nhalf of resolve() (its two candidates are already-resolved, no\nAppHandle needed), so every tag/searcher pairing is a direct unit\ntest -- native-wins-over-exa, exa-fallback, none-configured, and the\nnative-bypass variant never carrying a searcher. The full resolve()\nwiring stays unverified end-to-end for the same reason\nsalary_research/cover_letter::research's existing suites already\ndocument: this crate has no tauri::test mock-app harness, so nothing\ntouching AiProvider's AppHandle-taking methods is unit-testable here\n-- an honest, pre-existing, and repeatedly-documented limitation, not\na fix skipped.\n\nR7: CompanySearchRoute (L3, holds a credential-derived WebSearcher) is\nnever named in cover_letter::research -- only passed through opaquely\nby inferred type -- so it stays exactly where it must (consumed only\nby pipeline::Completer, which already has the allowlisted L2->L3\nedge). Only SearchBackend (the L1 ai_provider type from the prior\nround) crosses into cover_letter by name.\n\nVerified: cargo test --lib --all-features (4146 passed), cargo test\n--test architecture (18/18), cargo test --lib cover_letter::research\n(20/20) + search:: (92/92, incl. 4 new resolve_via_backend tests),\nclippy --all-targets --all-features --locked -D warnings (clean), fmt\n(clean).\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n* style: prettier pass on two round-one test files\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-13T17:35:43+02:00",
          "tree_id": "623a12aae2703673e005d31ad8491f00d5d7966a",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/546fc36df4ae75a216d0713284b4fe2e0968b53c"
        },
        "date": 1786636021646,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2183183,
            "range": "± 50052",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2633947,
            "range": "± 21724",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 250204,
            "range": "± 1352",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "9806f691b681b2f2be69a25e5e4a285d6c61f957",
          "message": "feat(pipeline): code-owned projects section at quality depth with link-label fidelity (#990)\n\n* feat(resume): code-own the projects section at quality depth\n\nAdd pipeline/resume/projects.rs: normalize_projects re-renders the draft's\nProjects section from source-seeded ProjectOut entries (same parser the\nmax-depth seeder uses), preserving draft order, dropping model-invented\nentries, and restoring altered/dropped links verbatim through the existing\nrender_project ladder. Hooked into the draft stage (content-free ledger\ncounts), the repair loop (normalize before revalidate), and the per-section\nregenerate command.\n\nAlso preserve markdown link labels ([Website](url)) from the source résumé\nthrough seeding so PDF/DOCX export keeps the candidate's own link labels\ninstead of losing them to bare hrefs.\n\nreseed_projects and same_project move from stages/section_gen.rs into the\nnew module (section_gen re-imports); max-depth output is unchanged.\n\n* fix(resume): gate projects normalization on plausible source structure\n\nAddresses rust-backend-architect review findings on 5585026b:\n\n- CRITICAL: refuse to normalize when the source's Projects section has no\n  genuine bold/bullet entry boundary (the shape every extraction::* PDF/DOCX/\n  RTF importer produces) or when seeded fields cross-contaminate each other —\n  a plain-text source previously collapsed into one garbage mega-entry that\n  normalization would rewrite a correct draft into, validation-clean.\n- CRITICAL: an all-dropped normalization pass is now a no-op instead of\n  persisting a heading-only Projects section with no undo.\n- HIGH: the regenerate-section hook now only writes when source_resume_for\n  found a real provenance hit, never the generated-text fallback.\n- HIGH: added tests for both the draft-stage hook and the regenerate hook,\n  extracting the deterministic decision out of each into a pure, unit-tested\n  function (apply_projects_normalization, normalize_regenerated_projects).\n- MEDIUM: the regenerate hook is scoped to Projects-key regenerations only,\n  so regenerating another section no longer reverts manual Projects edits.\n- MEDIUM: a splice now re-adds the blank line before a following section;\n  a dedup collision during reseed is counted as dropped; resolve_seed_names\n  refuses to rename when more than one seed shares a link.\n- LOW: link_href moved beside canonical_link in validate::content to kill\n  the source.rs<->projects.rs module cycle; build() parses the document once\n  via the existing split_parsed seam; a captured markdown-link span is only\n  written when it round-trips through urls_in and has balanced parens,\n  otherwise falls back to the bare href; the label regex accepts every URL\n  shape urls_in does (not just https://); a matched span is stripped before\n  the bare-URL harvesting pass so a URL-shaped label isn't counted twice.\n- Added a model::rich test proving a rendered project line's markdown link\n  labels become the hyperlink text.\n\n* fix(resume): never delete an unmatched draft project, gate on seed plausibility\n\nStructural rework of the projects-normalization pass per rust-backend-architect\nreview of caef3d1c (C1 executed reproductions: plain titles + bulleted\nachievements; bold titles + bullet achievements; a plain-text draft merging\ntwo projects).\n\n- Rule 1: normalization's write authority now extends only to draft Projects\n  entries it can confidently match to a seed. An entry that matches nothing\n  is left VERBATIM in its original position, never deleted or invented over\n  — the validators still grade whatever it says. A second entry resolving to\n  an already-used seed is still an allowed dedup drop.\n- Rule 2: seed_projects_for_normalize whole-bails (returns no seeds) when the\n  source's Projects section produces ANY seed with no link, no stack and no\n  description — the signature of a mis-grouped fragment (a swallowed title,\n  an achievement bullet counted as its own entry), which the old\n  \"any entry-start line\" gate could not see (bullets satisfy the entry-start\n  rule; the swallowed plain title never becomes a seed).\n- Rule 3: build() now also bails the whole pass when a seed the draft never\n  matched has its own link present, unattached, somewhere in the draft's\n  Projects text — the signature of the DRAFT's own entry grouping merging two\n  projects into one (a plain-text draft has no gate at all otherwise).\n- Rule 4: seeds_are_plausible now compares only stack entries and links\n  against sibling names/links, dropping the description from the haystack —\n  a truthful description cross-referencing a sibling project by name no\n  longer disables normalization for an honest source.\n- Rule 5: every bail records a content-free reason\n  (projectsNormalizeSkipped: empty_seed | implausible_seeds |\n  draft_parse_disagreement | no_entry_starts | seed_claims_multiple_entries)\n  on the draft-stage ledger artifact, so a silent skip is observable.\n- Rule 6 (N3): source.rs only strips markdown-link spans it actually captured\n  as links before the bare-URL harvest pass; a span the single-URL guard\n  skipped keeps its own bare URL(s) reachable instead of deleting them.\n- Additional guard beyond the seven rules: while testing the primary\n  \"N projects collapse into one seed\" shape from a fully plain-text source,\n  neither the empty-seed nor the plausibility gate catches it when the\n  resulting single seed's merged links/description both happen to be\n  non-empty (no sibling to compare against). build() now also bails\n  (seed_claims_multiple_entries) when a seed the link-fallback matches to a\n  SECOND, differently-named draft entry — the direct signature of one seed\n  having swallowed multiple projects' links, distinct from an ordinary\n  same-name dedup. Flagged here since it goes past the literal seven rules.\n- max: output-preserving; the dropped-metric now also counts a dedup\n  collision it silently missed before (from the previous review round).\n\nextraction of apply_projects_normalization/normalize_regenerated_projects as\npure functions (previous round) is what makes all of the above testable\nwithout a live provider or AppHandle; new regression fixtures encode the\nthree reproduced shapes as no-ops, plus a positive well-formed-source case\nproving the feature still fires (link restored, label preserved, order kept,\nlegitimate description intact).\n\n* fix(resume): bail projects normalization on a link inside a description\n\nArchitect round-2: a plain-text source collapsing into a SINGLE mega-seed\n(name \"Ledger CLI\", links=[ledger, beta], description carrying the\nswallowed \"Beta Sync · <url> Go · gRPC\" text) passed all four prior guards\n— empty_seed (fields non-empty), seeds_are_plausible (single seed has no\nsibling to compare against), seed_claims_multiple_entries (needs a second\ndraft entry to fire), draft_parse_disagreement (the lone seed IS matched)\n— and normalization wrote beta's URL onto the Ledger CLI entry, clean\nunder validation.\n\nAdd a fifth whole-bail reason in seed_projects_for_normalize:\nlink_in_description. The locked signature puts links only on the title\nline, and seed_one_project already strips a stack line's own URLs before\nthey reach `stack`, so a URL surviving in a seed's description or stack\ncan only mean the entry boundary swallowed a following project's title\nline. A legitimate description that cites a URL in prose just disables\nnormalization for that run (fails safe) rather than risk a false negative.\n\nRegression fixtures: a 2-project plain source collapsing into one mega-seed\nnow returns Skipped(link_in_description) and leaves a matching draft\nuntouched; an honest single-project source (link on the title line, per\nthe locked signature) still normalizes fully.\n\n* docs: add projects section quality-depth normalization note\n\nAdd inline note to resume-domain.md documenting the deterministic\nzero-call normalization path for Projects entries at quality depth,\nincluding whole-bail guards on mis-grouped source parse.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_01XR1b17ZtjMrLFnXgF9ke18\n\n* fix(resume): surface regenerate-hook skip reasons, drop expect on matched entries\n\nAddresses CodeRabbit PR #990 findings.\n\n1. normalize_regenerated_projects now returns (String, Option<&'static str>)\n   instead of collapsing every decline to a bare String — a user-triggered\n   Projects regenerate that skips normalization (unprovenanced source,\n   draft_parse_disagreement, link_in_description, etc.) was previously\n   unobservable, while Draft::run's ledger artifact reports the same\n   reasons for a run. Goes through normalize_projects_outcome (not the\n   Option-collapsing normalize_projects) so both seed_skip_reason and a\n   build()-level Skipped outcome surface. The caller folds the reason into\n   the command's existing Span::end_with message (this file's own\n   content-free observability convention), never a new logging call.\n\n2. projects.rs: Resolution::Matched now carries the already-parsed &'a\n   ProjectOut alongside the seed index, replacing the unenforced\n   \"index implies a parsed project\" invariant an .expect() previously\n   guarded on the second pass. The second pass can no longer reach a\n   missing project at all — the fallible runtime check is gone because\n   there is nothing left to check.\n\nNew test: normalize_regenerated_projects_reports_the_seed_skip_reason\n(the link_in_description shape surfaced through the regenerate command).\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-14T11:00:11+02:00",
          "tree_id": "973231dfc3750765e19ead6883dfa0faa3fc3169",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/9806f691b681b2f2be69a25e5e4a285d6c61f957"
        },
        "date": 1786699469143,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2258696,
            "range": "± 27820",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2662382,
            "range": "± 35809",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 310205,
            "range": "± 27941",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "3f9a1daaa35f78b1d6371755e6251c3b6c865556",
          "message": "feat(pipeline): cover letter joins the staged run and a humanize stage removes ai signs (#991)\n\n* feat(pipeline): reserve cover_letter + humanize in the stage vocabulary and deadline budget\n\nLock-chain part 1 of PR-2 (stages land in a follow-up commit): the\nincludeCoverLetter run-request field, the PIPELINE_STAGES union\n(cover_letter after draft, humanize after repair), the humanize-tier\nprompt blocks (ANTI_AI_TELL_LEXICAL/PROSE, HUMANIZE_PROSE) frozen from\n@ajh/prompts, and the quality-depth deadline arithmetic re-derived for\na second streamed pass (the letter) plus humanize's flat ≤2-call\nallowance: QUALITY_RUN_FIXED_SECS 4200->4800s,\nQUALITY_RUN_GENERATION_PASSES 1->2, RESUME_QUALITY.run_timeout\n75->90min. AGENT_IMPROVE.step_timeout/run_timeout raised in lockstep\n(105/135min) so the agent controller's race against a whole quality\nrun still clears the new floor.\n\n* feat(pipeline): add the cover_letter and humanize quality stages\n\ncover_letter (mirrors draft): gated by QualityInput::include_cover_letter,\nstreams one letter under the run's job id via the same ai:stream channel,\nno-ops instantly at zero cost when unset. humanize (runs last): counts\nvoice.* Warnings on both documents deterministically; zero flags is a\nzero-cost finish; a flagged document gets one complete() rewrite of only\nthe flagged lines (never link lines), re-normalized through\nprojects::normalize_projects and revalidated, reverted iff it introduces a\nnew Critical (code, evidence) pair or more voice flags than before.\n\nQualityCtx grows a `letter` field and `letter_text()` (prefer the stage's\nown letter, fall back to the legacy request text via the new free\nfunction `effective_letter_text`, testable without a live Completer);\nvalidate/repair/persist/the terminal-status checks switch to it. The\naggregate's cover_letter_text is written only from the stage's own\noutput, never the fallback, so the merge-upsert's empty-keeps-existing\nrule is preserved for a run that never generated one.\n\nBoth new fence tags (humanize_document, humanize_findings) are registered\nin agent::tools::FENCE_TAG_PATTERNS.\n\n* test(pipeline): cover humanize/cover_letter and repin the deadline ripple\n\nPure-function tests for the new seams: voice_count, voice_findings' link-line\nexclusion, is_usable_rewrite, humanize_is_worse (round_is_worse plus the\nvoice-count clause it cannot express), humanize_one end to end (zero-cost\nno-op, deadline-expired skip, provider-error keeps original, unusable answer\nkeeps original, revert on a new Critical, revert on more voice flags, accept\non fewer, and the normalize-before-grading link-safety guarantee) —\ninjected-closure seam, same shape as repair_loop, since this crate has no\nTauri harness to build a live Completer for a Stage::run test. Also\neffective_letter_text (the free function letter_text delegates to) and the\nincludeCoverLetter wire default.\n\ncargo fmt ran cosmetic-only reflow on the previous two commits' diffs. Fixed\nthree literal pins the RESUME_QUALITY.run_timeout/AGENT_IMPROVE.step_timeout\nbump broke: paying_stages' quality stage list, the run-kind/budget-floor pin,\nand the step-timeout human-readable-duration message (90min -> 105min for\nAGENT_IMPROVE, both the assertion and the doc prose it was pinning).\n\n* feat(renderer): fold cover_letter/humanize into the résumé-pipeline machine\n\nSTAGE_EVENTS gets cover_letter (drafting-family, no new state — a second\nstreamed document) and humanize (its own HUMANIZE event -> new humanizing\nbusy state, since it is quality depth's genuinely distinct last phase). i18n\nen+de for pipeline.stage.cover_letter/humanize and pipeline.state.humanizing.\n\nEvery existing call site sends includeCoverLetter: false explicitly (the\ngenerated ResumePipelineRunRequest type has no optional defaulted fields, so\nz.infer makes it required) — zero runtime behavior change, matching the\nbackend's own no-op default. Also repins the Settings per-stage override UI's\nOVERRIDABLE_PIPELINE_STAGES literal and adds the two new stages' names +\ndescriptions to the settings.ai.stages i18n subtree (en+de) that surface\nalready requires one for.\n\n* fix(pipeline/humanize): high 1, high 2, medium, and low findings from ai-provider-expert\n\nAddresses all 5 findings in the humanize stage per ai-provider-expert review:\n\nHIGH 1: gate letter arm on ctx.input.include_cover_letter AND use ctx.letter\n(never letter_text()) as rewrite input, so a fallback letter is structurally\nnever adoptable. Letter humanization is now skipped when\ninput.include_cover_letter=false, preventing unrequested provider calls and\noverwrites of the user's stored letter.\n\nHIGH 2: tier the usable-rewrite floor by document type:\n- Resume tier: 50% floor (candidate.chars().count() * 2 >= original_len)\n- Letter tier: 90% floor (candidate.chars().count() * 10 >= original_len * 9)\nLetter tier is much stricter because absence of content is NOT backed by\ndeterministic checks, unlike the résumé validators. Document-agnostic code now\naccepts HumanizeTierKind::Resume or HumanizeTierKind::Letter.\n\nMEDIUM: move charge_daily() BEFORE entering humanize_one (not inside the\ncomplete closure), so a Limiter refusal records a distinct capped flag (not\nfailed/called), and does NOT count_call. Charge refusals no longer pollute\nmetrics or block the correct cap/skip reason.\n\nLOW (ledger shape): all three json!() ledger.record exits now use the same full\nkey set: resumeFlagged, letterFlagged, calls, reverted, voiceBefore, voiceAfter,\nfailed, timedOut, capped. All use zero/false defaults for unset fields.\n\nLOW (module doc): add § Language residual documenting that English-lexicon\nrewrite can seed English vocabulary into non-English prose undetected by the\nper-language lexicon checks. Locale dispatch is the future fix.\n\nHumanizeTierKind is now pub(enum) and re-exported from stages/mod.rs for test\nvisibility. is_usable_rewrite now requires a tier parameter. humanize_one now\nrequires a tier parameter. HumanizeAttempt now includes a capped field.\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_01XR1b17ZtjMrLFnXgF9ke18\n\n* fix(pipeline/humanize): finish the ai-provider-expert fix-up and unbreak the build\n\ncommit 22422792 addressed HIGH-2/MEDIUM/LOW but left HIGH-1 half-done and\nthe test suite non-compiling. This finishes it:\n\nHIGH-1 (structural, not just the added gate): the letter arm now reads\nctx.letter directly as its rewrite input, never ctx.letter_text()'s legacy\nfallback, via a new pure predicate should_humanize_letter(letter_flagged,\nletter_body, include_cover_letter) — both the flag and the field-read are\nindependent guards now, so a future change to either alone still leaves the\nletter arm structurally unable to adopt a validate-only coverLetterText.\nConfirmed the persist invariant (cover_letter_text: ctx.letter.clone(),\ncommands/resume_pipeline/mod.rs:566-580) holds: ctx.letter can only ever be\nempty or genuinely stage-generated.\n\nHIGH-2: verified the tier split (resume 50% / letter 90%) is correctly\nimplemented; expanded the doc comment to state the asymmetry's actual\nreason (round_is_worse's absence-shaped Criticals back the résumé floor;\nthe letter has no absence-shaped validator at all, so its length floor is\nthe ONLY backstop against content loss) rather than just restating the\nnumbers.\n\nMEDIUM: verified charge_daily() already runs before humanize_one on both\narms, a cap refusal already sets capped without called/failed and without\ncount_call.\n\nLOW: verified all three ledger.record exits already share one 9-key set,\nand the module doc already carries the language-residual note. Reformatted\nthe stages/mod.rs re-export line (cargo fmt).\n\nBuild/test fix: updated all 14 humanize_one/is_usable_rewrite call sites in\npipeline/resume/test.rs for the new tier parameter (HumanizeTierKind was\nadded to the fn signatures but the call sites were never touched).\n\nAdded the two regression tests the ruling required: (a)\nshould_humanize_letter_refuses_whenever_the_run_never_requested_a_letter —\npins that a non-empty legacy-shaped letter body with includeCoverLetter\noff never adopts, closing the gap HIGH-1's gate-only fix left open; (b) a\nletter candidate truncated to ~60% is kept as unusable (revalidate never\nruns) while the identical 60% candidate is accepted on the résumé tier with\nclean validators, pinning the tier split end to end through humanize_one,\nplus a pure is_usable_rewrite pin at the same ratio.\n\nGates: cargo test --lib (4205 passed), clippy --all-features (clean), fmt\n--check (clean), gen:ipc:check + gen:prompts:check (clean, no-op — no\nIPC/prompt surface touched by this fix).\n\n* fix(pipeline/humanize): close the four advisory findings from the delta review\n\nMEDIUM 1: charge_daily now fires strictly AFTER the deadline-passed and\nempty-findings gates on both arms (mirrored in Humanize::run, the same two\nchecks humanize_one itself runs first), so the two reachable no-call paths\n(all flagged lines on link lines; an already-expired deadline) never burn\nthe user's daily allowance for a call that was never going to be sent.\n\nMEDIUM 2: deleted HumanizeTierKind. is_usable_rewrite and humanize_one now\ntake prompts::HumanizeTier directly (it already derives Copy/PartialEq/Eq),\nand each arm of Humanize::run computes ONE `tier` value used for both the\nprompt (humanize_system(tier, ...)) and the trailing floor argument to\nhumanize_one — one type, one value, so \"letter prompt graded against the\nrésumé's floor\" is no longer two independently-editable literals that\nhappen to agree today.\n\nLOW 1: dropped HumanizeAttempt::capped — it was write-never once charging\nlives in Humanize::run's own gated match arms; the stage-local `capped`\nbool (already feeding the ledger) is the sole carrier now.\n\nLOW 2: deleted the in-file #[cfg(test)] module — pipeline/resume/test.rs\nalready carries the stronger versions (is_usable_rewrite_tier_split_is_pinned_at_sixty_percent\npins the exact ratio where the two tiers diverge; the deleted in-file\n90%-floor test stayed green under a 0.9->0.7 mutation and pinned nothing).\n\nGates: cargo test --lib (4202 passed, -3 from the deleted in-file tests),\nclippy --all-features (clean), fmt --check (clean), gen:ipc:check +\ngen:prompts:check (clean, no-op — no IPC/prompt surface touched).\n\n* fix: address coderabbit findings on pr #991 humanize stage\n\n- skip humanize entirely for a document over HUMANIZE_DOCUMENT_CAP instead\n  of letting fenced() silently truncate it and grading a prefix-only rewrite\n  against the real (untruncated) length floor\n- treat a revalidate error inside humanize_one as a failed attempt (keep\n  original, mark failed) instead of propagating it and failing the run\n- fail closed when the letter revalidate closure produces no report for a\n  non-empty candidate, instead of substituting an accept-shaped empty report\n- extract the ledger's 9-key json! literal (now 10, with tooLarge) into one\n  Artifact struct shared by all three Stage::run exits\n- pin the humanize_findings block as the second untrusted fenced block with\n  hostile content, an evidence-less voice finding still reaching\n  voice_findings, and the exact resume/letter floor boundaries\n- fix stale prose: gen-ipc-rust.ts's hand-authored doc comments and\n  ai-timeouts.test.ts's formula comment still described draft as the only\n  generation pass; en/de generationDepth quality copy still described the\n  pre-PR-2 six-stage/4-call pipeline\n\n---------\n\nCo-authored-by: Claude Fable 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-14T15:35:36+02:00",
          "tree_id": "944f3f803b0c2b2d1a6ac94f13facd1b86d373ca",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/3f9a1daaa35f78b1d6371755e6251c3b6c865556"
        },
        "date": 1786715274623,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2250528,
            "range": "± 46707",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2689863,
            "range": "± 35046",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 302359,
            "range": "± 7709",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "19a6de3e90907406b8d6713435d9e30d135a0cb4",
          "message": "feat(apply): the apply flow runs the staged 4-step pipeline on both surfaces (#992)\n\n* feat(shared): let a résumé-pipeline run start from pasted text, not just ids\n\nresumeId/jobId on ResumePipelineRunSchema get .default('') and gain\nresumeText/jobAdText siblings (same 200_000-byte cap class as the\nresume/job text used elsewhere), plus small-capped jobTitle/companyName/\nboard for the text path's posting identity. Two refinements reject a\nrequest with neither an id nor its text on a side. The old id-only wire\nshape still parses unchanged.\n\nThe exported ResumePipelineRunRequest type switches from z.infer (output,\ndefaults resolved to required strings) to z.input, so an existing caller\nbuilt before these five fields keeps type-checking without edits — nothing\non this transport ever calls .parse() client-side, so there is no runtime\ndifference to protect.\n\nRegenerated the Rust IPC contract via `pnpm gen:ipc`.\n\n* test(shared): cover the résumé-pipeline run schema's id-wins refinements\n\nOld id-only shape still parses; text-only and mixed id/text combos are\naccepted; either side empty on both its id and its text is rejected; the\nnew text-path free-text fields are capped.\n\n* feat(resume-pipeline): resolve résumé/job-ad text with an id-wins rule\n\nexecute() now takes a nonempty resumeId/jobId (DocumentStore/postings-cache\nlookup, hard error on a miss — never a silent fallback to text) OR clamped\nresumeText/jobAdText, per side. sourceResumeId is written to the run's\nmetrics ONLY on the id path, so a text-path run's later regenerateSection\ncorrectly falls through source_resume_for's documented weaker fallback\n(and PR-1's provenance gate still refuses to write projects from it).\n\nAlso persists the text path's job-ad text into the aggregate's job_ad\ncolumn (today deliberately empty on the id path — the postings cache is\nkeyed by a live job id a finished run no longer holds); the id path is\nunchanged and merge_application's pick() keeps any existing job_ad.\n\nThe clamp + the pure id-wins decision moved to a new resolve.rs to keep\nmod.rs under R8's per-module LOC cap.\n\n* test(resume-pipeline): cover the id-wins resolution and text-path caps\n\nresume_source/job_source: id wins even when text is also present, falls\nback to text only when the id is empty, None when both are empty/\nwhitespace. job_meta_from_request reads the four identity fields off the\nclamped request. job_ad_for_persist is empty on the cache path and carries\nthe text on the text path. Two grep-shaped checks (this crate has no\nAppHandle test harness) pin that the id arms never read the text fallback\nand that sourceResumeId is gated on the Store path. Extends the existing\nclamp test for the five new fields.\n\nEvery new assertion was mutation-checked (temporarily broken, confirmed\nred, reverted).\n\n* feat(renderer): split the letter stream and add the 4-step pipeline mapper\n\n- usePipelineDraftStream forwards reasoning/thinking chunks to an optional\n  onThinking callback (previously dropped).\n- useResumePipelineSession snapshots the draft into its own letterDraft\n  buffer once the cover_letter stage starts, so letter tokens stop mashing\n  onto the resume pane; also exposes a thinking accumulator.\n- new pure pipelineStepForStage mapper folds the 8 quality-pipeline stages\n  into the 4 apply-flow steps (analyze/generate/validate/humanize).\n\n* feat(renderer): surface which saved doc backs the resume input card's text\n\nOptional onDocIdChange prop reports the selectedDocId already tracked\ninternally, and clears to null on a manual edit (the paste textarea\npreviously bypassed the id-tracking state entirely). Feeds the apply\nflow's id-wins pipeline run request (resumeId vs resumeText).\n\n* feat(renderer): drop the tailor wizard's depth picker, add the 4-step i18n copy\n\nStepModel loses DepthSelector + depthUnavailableReason plumbing (the apply\nflow always runs the staged quality pipeline now) and shows an honest cost\nline instead. tailor-schema/tailor-state drop depth, add resumeDocId\n(wired from StepResume via ResumeInputCard's new onDocIdChange).\n\nNew pipeline.step.* i18n (en+de, label+description x4) for the coming\n4-step checklist; drop the now-orphaned autopilot.apply.generatingHint/\nreasoningHeavyHint keys.\n\n* feat(renderer): add the staged-run hook and the 4-step generating panel\n\nuseTailorPipeline wraps useResumePipelineSession with the page-local UI\nstate useTailorGeneration used to own (active doc, copy/export, inline-edit\noverrides, quality re-check, section-fix/fabrication-review wiring,\nid-wins run request). Resume text comes from the run detail; letter text\nfrom the job's aggregate ai_generations record (the pipeline writes both\ndirectly). Invalidates that aggregate once the run leaves running.\n\nGeneratingPanel drops the fast-path phase dots for the 4-row checklist\n(analyze/generate/validate/humanize) driven by the new pipeline-steps\nmapper, keeping ThinkingBubble/streaming text/Cancel; dropped the\nreasoning-heavy hint (superseded by the finer per-stage progress).\n\nGenerationOutput gains an optional pipeline prop forwarded to QualityBadge\n(fast-path callers unaffected — undefined by default).\n\nTailoredResumePanel's test fixture gets the two new session fields\n(letterDraft/thinking).\n\n* feat(renderer): give the tailor results panel a staged-run status banner\n\nProminent (not hidden behind a click) needsReview/cancelled/error status\nabove the document viewer, plus this posting's read-only run history\n(PipelineRunsList) below it — both new, additive to the existing\nheight-bounded GenerationOutput body (the scroll-boundary regression\nguard still holds: nothing new sits between it and the panel body).\n\nGenerationOutput now receives the section-fix / fabrication-review bundle\n(pipelineReview) so the quality badge/panel wires Fix and per-bullet\nKeep/Remove for a staged run.\n\n* fix(renderer): cold-entry fallback for the tailor pipeline's resume text\n\nresumeOut and the quality report now fall back to the job's persisted\naggregate record when no live run detail exists yet (a fresh session that\nnever started/reconnected a run) — the letter already worked this way;\nthe resume text and the report did not, which would have shown an empty\ndone stage on reopen even though a saved result existed.\n\n* feat(renderer): cut the tailor flow over to the staged quality pipeline\n\nindex.tsx now runs useTailorPipeline instead of useTailorGeneration:\nstage derivation keys on busy/hasOutput, GeneratingPanel gets the 4-step\nchecklist props, ResultsPanel gets the pipeline-review bundle + run\nstatus + run history. The reconnect target ({runId, jobId}) persists via\na new persistence.runId/runJobId pair (session-store applyRunId/\napplyRunJobId, reset alongside the other one-shot fields on switching\napplications) instead of the deleted generation-store.\n\nuseTailorPipeline.start() now also toasts a failed launch (the session\nitself only sets the persistent banner text) — parity with the fast\npath's onError toast.\n\nRewrote both TailorFlow test files: the extraction-seam suite mocks the\nnew hook's shape; the failure-visibility suite now drives a REAL\nuseTailorPipeline -> useResumePipelineSession chain against a mock\nAppClient (resumePipeline.run/get) instead of the deleted generation\npipeline stubs.\n\n* chore(renderer): delete the fast-path tailor engine, dead after the cutover\n\nstore/generation-store/ and useTailorGeneration(.test).ts had exactly one\nconsumer — TailorFlow — confirmed by grep before deleting; ai-generate and\nresume-builder call lib/generate/generation directly and keep their own\none-shot path untouched.\n\nDrop the now-orphaned autopilot.apply.{depthUnavailable,analyzing,\nwritingResume,writingCover,generating} i18n keys (en+de) — only ever read\nby the deleted hook/store.\n\n* fix(renderer): reseed the resume input card's doc id across a step remount\n\nResumeInputCard's `selectedDocId` was component-local `useState(null)`, so\nit reset on every mount while the wizard's form field (`resumeDocId`) did\nnot. TailorWizard renders `{step === 1 && <StepResume />}`, so navigating\naway from and back to the resume step remounts the card: the form still\nheld the previously-selected doc id, but the card's local state forgot it.\nTwo handlers gate on that local id and both silently misbehaved as a\nresult — `handleTextChange` never told the host a hand edit had\ninvalidated the id (so `useTailorPipeline.start()` kept sending the stale\n`resumeId`, discarding the user's edit per the backend's id-wins rule),\nand `handleRemove` failed to clear the host's id when the removed doc was\nthe one actually backing the text.\n\nFixed at the root instead of patching each gated call site: `useResumeInput`\nnow accepts an optional host-owned `docId`, used only as the `useState`\ninitializer, so a remount re-seeds local state from what the host already\nbelieves. This keeps local and host state in lockstep for every existing\ncall site (including the `handleRemove` sibling) without touching any of\ntheir conditionals, and is a smaller diff than special-casing each one.\n`StepResume` seeds it from `getValues('resumeDocId')`. The backend's\nid-wins rule is untouched — only the renderer's provenance signal was wrong.\n\nTests: hook-level remount regression + happy path in\nuseResumeInput.test.ts, and a StepResume.test.tsx integration test with a\nreal react-hook-form instance shared across an unmount/remount, asserting\nthe form's resumeDocId field itself clears on a post-remount edit.\n\n* fix(resume-pipeline): make id-wins resolution behavioral, not lexical\n\nrust-backend-architect (MEDIUM 1): resume_source/job_source stayed pure,\nbut execute's Store/Cache arms were inline matches — a source-substring\ntest pinned their SPELLING, and a mutation that aliased clamped.resume_text\nthrough a differently-named local passed it while changing the guarded\nbehavior. resolve_resume/resolve_job close this structurally: the id arm\nreceives only an id and an injected lookup closure, so there is nothing in\nscope to fall back to on a miss, provably (the lookup can return None and\nthe function still cannot do anything but error). source_resume_id_for_\nmetrics gets the same treatment for the sourceResumeId metrics gate.\n\nrust-backend-architect (MEDIUM 2): PipelineRunStore::prune partitions\nretention on (job_url, kind), and every unlinked run shared job_url = \"\"\nthere — harmless while rare, but the text path makes an unlinked run\nroutine (a pasted job ad, an Autopilot job with no capturable link), so\nevery pasted posting's history pooled into one shared bucket. run_store_\njob_url substitutes a stable per-text synthetic key (unlinked_run_key) for\nthe RUN ROW only, shaped as an https://…/.invalid url (RFC 2606) so it\nsurvives PipelineRunStore::upsert_run's own normalize_job_url call (which\nneutralizes any other scheme back to \"\") without ever being mistaken for a\nresolvable posting link. The aggregate's own job_url (persist_document's,\nwhat save_application upserts by) is untouched — different table, different\ntrust level, documented on job_ad_for_persist alongside the SEC-LOW-4 note\nthat the text path's aggregate key is the renderer's own unverified claim.\n\nrust-backend-architect (LOW 1) + tauri-security-reviewer (SEC-LOW-3):\nresumeId/jobId are now clamped in clamp_request (JOB_IDENTITY_CAP, 512\nbytes) like every other renderer string on this command, and resolution\nreads the clamped copies — closing the two unbounded echo points (a\nvalidation-error message, metrics_json). The echoed id is additionally\ncapped to 64 chars for the error message specifically, mirroring\nagent::tools::clamped_echo/ECHO_CAP's exact rule but reimplemented locally\nrather than imported: agent::tools is PR-5's deletion target and\nclamped_echo isn't part of its fenced/JOB_CAP/RESUME_CAP extraction, so\ndepending on it here would force PR-5 to widen scope or break this command.\n\nSplit run_store_job_url out alongside the rest of resolve.rs's pure\nresolution logic to keep mod.rs under R8's LOC cap after these additions.\n\n* fix(resume-pipeline): collapse whitespace in the notification posting label\n\ntauri-security-reviewer (SEC-LOW-2): a jobTitle/companyName carrying an\nembedded newline (scraped, or PR-3's untrusted text-path fields) survived\ninto the OS notification body verbatim — a one-line label becoming two —\nand on a Linux desktop whose notification daemon advertises libnotify\nbody-markup, an embedded <b>/<a href> reads as markup rather than text.\ncollapse_whitespace runs before the label is assembled, subsuming the old\n.trim() and closing the newline-driven multi-line case.\n\n* test(resume-pipeline): cover the pure resolve functions and retention key\n\nReplaces the two source-substring grep tests (id-path-never-falls-back,\nsourceResumeId-gated-on-store-path) with behavioral tests on resolve_resume/\nresolve_job/source_resume_id_for_metrics directly — each proves the Store/\nCache miss case errors even when resolve_source/job_source saw text\nalongside the id, which the old grep test could not distinguish from a\ndifferently-named alias of the same value. Two thin grep-shaped tests stay,\nnarrowed to call-site PRESENCE only (execute actually routes through the\npure functions), never branch semantics.\n\nAdds coverage for run_store_job_url/unlinked_run_key: deterministic per\ntext, distinct per posting, survives normalize_job_url's scheme guard\nunchanged, and a store-level retention test (3 unlinked runs of job A + 1\nof job B survive prune() intact; a 4th run of A evicts only A's oldest,\nnever B) pinning the MEDIUM 2 fix directly against PipelineRunStore.\n\nExtends the existing clamp test for the now-clamped resumeId/jobId (LOW 1)\nand adds a whitespace-collapse regression for the notification label\n(SEC-LOW-2). Every new assertion was mutation-checked (temporarily broken,\nconfirmed red, reverted) before landing.\n\n* test(pipeline): add strategy_user to the forgery-resistance sweep\n\ntauri-security-reviewer (SEC-LOW-1): strategy_user was the one résumé-\nconsuming stage prompt missing from every_untrusted_block_is_fenced_and_\nforgery_resistant, even though it fences through the same primitive\n(prompts.rs::fenced/fenced_artifact) every other stage in the sweep does.\nBehavior was already correct; this closes the gap so a future regression\nin that stage specifically would be caught.\n\n* test(shared): assert the company-name cap on the résumé-pipeline schema\n\nrust-backend-architect (LOW 2): jobTitle and board both had a dedicated\noversized-value assertion in the text-path cap test; companyName's 512\ncap had a Rust-side assertion (clamp_request) but no TS one.\n\n* fix(renderer): stop the tailor-run persist loop and the cross-application run leak\n\nF1 — useTailorPipeline's run-persist effect listed the host-supplied\nonRunStarted in its deps with no already-persisted guard. On the real\nDocumentsTab/TailorFlow wiring onRunStarted writes a Zustand slice, which\nalways returns a new object, re-rendering the host, which passes a brand\nnew arrow back in — an infinite loop that threw \"Maximum update depth\nexceeded\" the instant a run started. Fixed by reading onRunStarted from a\nref (dropped from the deps array entirely) plus a persistedRunRef keyed on\n`${runId}|${jobId}` so a re-run of the effect for the same run is a no-op.\n\nF2 — applyRunId/applyRunJobId were reset only inside the\napplyForId-mismatch guard, but both apply entry points\n(usePostingActions.handleTailor, AutopilotPage.handleApply) set applyForId\nto the NEW application's id before navigating, with no compensating clear\nof the reconnect target — so the guard was already a no-op by the time the\nnew application's DocumentsTab (the wizard's default tab) mounted, and its\nuseResumePipelineSession's lazy useState initializer permanently captured\nthe stale run id. Fixed by making the reconnect target self-describing:\napplyRun: { forId, runId, jobId } | null, read only when\nforId === application.id. This is correct on the very first render\n(no ordering to get right) instead of only after a reset effect fires.\n\nTailorFlowPersistence.setRunId/setRunJobId collapse into one setRun(ids)\nso a reconnect target is always written atomically.\n\nTests: an unstable-callback (fresh arrow per render) regression at the\nhook level plus a DocumentsTab-shaped mount-level test through the real\nuseTailorPipeline chain for F1; a forId-mismatch + happy-path pair for F2.\n\n* fix(renderer): resume input card's chip label stops misattributing to the default doc\n\nactiveDoc fell back to the default saved résumé whenever selectedDocId was\nnull, so the resting chip showed the default's title even when the\non-screen text (a hand-edit, upload, paste, or profile import) had nothing\nto do with it. selectedDocId === null now means \"no backing doc, full\nstop\" for the chip label too, matching the fix already applied to\nhandleTextChange/handleRemove's doc-id tracking.\n\nTest: a saved doc is selected, then the text is hand-edited — activeDoc\nmust read undefined, not the (only, default) saved doc.\n\n* fix(renderer): tailor-flow ui polish sweep — indicators, contrast, and the owner's 4-step brief\n\nFrontend/UX review batch against the owner's acceptance bar (\"clean ui ux,\nnot crowded, indicators explaining each section, and when each step is\ndone it should mark it as done\"):\n\nH2 — the 4th step (\"remove AI signs\") could never actually be SHOWN as\ndone: the generating panel unmounts the instant the run finishes, before\ncurrentStep could ever reach the step count. The results panel now\nrenders a compact 4-check \"all steps completed\" summary on a genuinely\nclean finish.\n\nH1/M4 — the thinking bubble now takes defaultExpanded (collapsed by\ndefault in this tight panel, buys back ~200px at the 900x600 floor) and\ndone={currentStep >= 2} instead of a hardcoded false (it was claiming\n\"still thinking\" forever with a blinking caret on a frozen transcript once\nvalidate/humanize started).\n\nH3 — an mm:ss-since-this-step-started caption on the active row; text\nupdates survive prefers-reduced-motion since nothing here is a CSS\nanimation. validate/humanize/repair emit no stream deltas, so without this\nthe panel read as stalled for its longest phases.\n\nH4 — a run that timed out but still saved a document reports completed\n(the backend's timed-out-with-document path); the banner now also shows\nwhen a non-'done' stoppedReason rides along a 'done' runState, instead of\nsuppressing the caveat entirely. The new \"all steps completed\" summary\n(H2) is gated on the SAME clean-finish check, so a truncated run never\nclaims all four steps finished.\n\nH5 — needsReview with zero open claims pointed at an empty list (\"0\nclaims need your verdict\"). Two independent causes fixed: the frontend\nnow sums unresolved fabrications across BOTH resume and coverLetter slots\ninstead of only the active tab's, and a genuinely zero-claim needsReview\nnow shows honest copy pointing at the quality badge. A cold (no live\nsession) needsReview also says the run needs reopening instead of\nimplying in-place interactive controls that aren't wired for it.\n\nH6 — the brand-soft-with-opacity utility never gets the light-scheme class\nremap (only the bare class does) — measured 2.38:1 on light, was\ninvisible-adjacent. Now bare (5.05:1 light) at 10px, doubling as the M5/M2\nfix (active step label + its sole sub-line both use the wizard's own\naffordance).\n\nH7 — the description/circle-numeral text at low opacity (~4.17:1 light)\nand the pending label's opacity (~3.80:1 light) bumped to the repo's own\nestablished floor for sub-14px text, both independently proven to clear\nAA by the existing light-scheme-contrast test's math.\n\nH8 — an sr-only state word per row (done/active/pending) plus one live\nregion announcer for step transitions, mirroring an existing sibling\npattern elsewhere in this tree — an aria-hidden icon was the only state\ncue.\n\nH9 — cancelling before any text streamed returned silently to the wizard\n(cancelled is terminal, the session cancel sets no error, the banner is\ngated on one). A one-line acknowledgement now shows on the configuring\nstage while the generation state is cancelled and no error/output exists.\n\nM1 — the run-history strip (~170px, permanently mounted) moved behind a\nghost button + modal, matching the quality badge's own modal pattern\nelsewhere in this panel (left as-is).\n\nM3 — a one-line header pins above the streaming pane so a résumé-to-letter\nmid-run swap doesn't read as the same document continuing.\n\nM6 — the stage-swap animation wrapper drops focus to the document body on\nevery stage change; the stage body is now a focus target, focused in an\neffect keyed on the stage.\n\nM7/M8/M9 — the failed-run hint, the thinking transcript and its header,\nand the cancel button's contrast all bumped to clear AA.\n\nAlso (frontend, adjacent finding): a cold entry (no run started or\nreconnected this session) derived its status banner from the session\nmachine's own idle state, always reading as a clean \"done\" even when the\nunderlying run was needsReview or failed — it now falls back to this\nposting's already-fetched run list for that case.\n\nL-tier: the pipeline-step mapper documents its currently-unreachable\nmax-depth gap instead of leaving it silent; the stage label's missing-key\nfallback now uses the translated coarse state instead of leaking a raw\nsnake_case stage name; the validate step's description no longer names an\ninternal stage word; a stray status-tint outlier moved to the repo's\nestablished convention; the generating panel's empty-state test now\nasserts the real skeleton marker instead of an incidental layout class.\n\nContrast numbers (sRGB relative-luminance, alpha-over-background,\nmatching the repo's own light-scheme contrast test method) were computed\nfor every change above; every bump was verified to clear 4.5:1 on both\nthe dark card and the light-remapped surface.\n\nTests: new/updated coverage across the generating panel, thinking bubble,\nresults panel, and tailor-flow test suites (elapsed caption, sr-only state\nwords and announcer, streaming-target header, contrast classes, all-done\nsummary, the H4 gate, H5's zero-claims and cold-entry branches, the\nrun-history modal, the H9 hint, M6 focus, and the aggregated open-claims\ncount). H2's guard was mutation-tested: confirmed red against a reverted\nclean-finish check, green against the fix.\n\n* fix(renderer): close the ui-ux-expert re-check on tailor-flow\n\nSmall surgical round on the three new HIGHs from the second ui-ux-expert\npass; two were introduced by the prior fix round itself.\n\nN1 — the status Tag's `pipeline.status.${runState}` never had a real key\nfor 'done'/'error' (the catalog is running/completed/needsReview/failed/\ncancelled, TailorRunState is done/needsReview/cancelled/error) — a\ntimed-out-but-saved run rendered the literal dotted key next to its\n\"stopped\" reason, in both locales. The mock in ResultsPanel.test.tsx could\nnot have caught this: a key-echoing `t` mock is structurally blind to a\nmissing key. Fixed with an exhaustive `Record<TailorRunState, …>` map\n(PIPELINE_STATUS_KEY, exported) so a future TailorRunState member fails to\ntype-check until mapped, PLUS a new test file that imports the REAL locale\nJSON directly (no i18next, no mock) and asserts every reachable dynamic key\nthis PR touches resolves to real content in both locales — the swept set:\npipeline.status.*, pipeline.step.state.*, pipeline.step.*.{label,description},\npipeline.step.allDone, autopilot.apply.target.*.\n\nN2 — the M6 stage-body focus effect fired unconditionally, including\nagainst an open ModalShell dialog (useFocusTrap only intercepts Tab, no\nfocusin re-capture) and on first mount. Two guards added: skip the first\nrun, and bail when the document's active element is inside\n`[aria-modal=\"true\"]`.\n\nN3 — the \"all steps completed\" row and the streaming-target header were\ntext-foreground/45 (4.22:1 dark, under the repo's documented <14px floor\nof /70). Both bumped to /70 (8.04:1 dark / 6.20:1 light).\n\nPlus the gap both reviewers landed on independently: the live-region\nannouncer silently no-op'd once currentStep reached the step count\n(PIPELINE_STEP_KEYS[currentStep] undefined) — a screen-reader user heard\nevery step start and never heard the run finish. Now announces\npipeline.step.allDone (already shipped in both locales, previously\nunwired), same one-utterance-per-transition guard.\n\nLeft alone per the explicit scope note: ThinkingBubble's transcript\ncontrast, the Cancel button contrast, the elapsed-clock reset on\nreconnect, and the cold-entry zero-claims copy — none touched.\n\nEvery new guard mutation-tested (revert -> red -> restore -> green):\nPIPELINE_STATUS_KEY.done, the mount-skip guard, and the modal-bail guard\nall confirmed load-bearing this way.\n\nGates: pnpm typecheck (whole graph, 13/13), pnpm test (whole graph, 488\nfiles / 7118 tests), pnpm lint:strict --max-warnings 0, en/de key parity\n(0 orphans — no new keys added this round, only existing ones wired).\n\n* fix(renderer): resume input card does not select a doc that never loaded its text\n\nhandleSelectSaved called selectDoc(doc.id) unconditionally while onChange\nwas gated on the doc's text existing — a metadata-only or empty saved\nrésumé left the editor showing whatever text was already there while\nselectedDocId claimed it was backed by the empty doc. An id-first pipeline\nrequest would then resolve server-side to the empty stored text, silently\ndiscarding whatever the user was actually looking at. Same defect class as\nthe stale-docId bug fixed in f055b899.\n\nPicked \"don't select\" over CodeRabbit's \"fetch the text like\nhandleFileChange does\": the backend's list response already carries the\nfull text (documents_list serializes the whole DocumentRecord, same source\ndocuments_get_text reads), so a re-fetch would return the identical\n(still-empty) string — there is nothing to fetch. onChange and selectDoc\nnow land together or not at all, matching the auto-fill effect's own\nexisting pattern. The saved menu is left open on a no-op selection instead\nof silently closing, so the user can pick a different entry.\n\nAudited every other selectDoc call site for the same shape (id set without\nits matching text): handleTextChange/handleRemove (already fixed for this\nexact defect), the auto-fill effect, handleFileChange (fetches text before\ncalling either), handleSavePaste (saves the ALREADY-displayed value, so\nthere is no text to load), handleProfileUrlSubmit (always clears to null).\nNo further instance found.\n\nTest: a metadata-only doc fixture (real id/title, no text) proves\nhandleSelectSaved is a no-op for it and does not clobber an\nalready-selected id; mutation-tested (reverted the guard, confirmed both\nnew tests red, restored, confirmed green).\n\n* fix(renderer): flush a pending inline-edit write on unmount instead of dropping it\n\neditActiveOutput debounces the updateAiGeneration.mutate write by\nPERSIST_DEBOUNCE_MS; the unmount cleanup only CLEARED both timers and\nreturned. Type into the résumé or letter and leave the tab (or let the host\nremount DocumentsTab) inside that window and the write never happened — the\nlocal override state dies with the unmount too, so nothing recovers it.\nSilent user data loss.\n\nMatches DocumentsTab's own flushJd posture (ApplicationDetailPage/index.tsx)\nexactly: the pending payload lives in a ref, captured at schedule time (not\nre-read at flush time); one flushPersist function is the sole place a write\nfires — the debounce timeout and the unmount cleanup both just call it; a\nref indirection keeps the unmount effect's empty deps honest without a\nstale closure over flushPersist/mutate.\n\nAlso (CR-8): moved this describe block's vi.useRealTimers() out of each\ntest body into an afterEach — a failed assertion above the old trailing\ncall would have skipped it and leaked fake timers into every later test in\nthe file, an order-dependent green.\n\nTests: edit -> unmount inside the debounce window -> the write fires with\nthe pending text; a second test confirms the (now-cleared) timer cannot\ndouble-fire after the unmount flush. Mutation-tested: reverted the flush to\nclear-only, confirmed the new test red, restored, confirmed green.\n\n* fix(renderer): widen the i18n sweep to pipeline.stopped.* and assert real (non-empty) copy\n\nTwo gaps in the sweep this same file added for N1:\n\n- pipeline.stopped.\\${suffix} (ResultsPanel's stopped-reason line) was not\n  covered at all — stoppedSuffix() folds any unrecognized backend\n  StoppedReason onto STOPPED_SUFFIX's values or UNKNOWN_STOPPED_SUFFIX, so\n  the reachable key set is exactly that table, and a missing entry renders\n  a dotted key in the banner. This is the same class of bug N1 fixed, in\n  the neighbouring key path, that the sweep walked past. Iterated from the\n  shared helper's own tables (@/lib/stopped-reason), not a hand-copied\n  list, so a variant added there is covered automatically.\n\n- toHaveProperty passes for an empty string, so a key that exists but\n  carries no copy (rendering as blank chrome, not a loud failure) slipped\n  through silently. Every check now goes through one expectRealCopy\n  helper asserting a non-empty string in both locales, not just presence.\n\nAlso swept the literal (non-dynamic) keys this PR added to ResultsPanel —\nneedsReviewTitleEmpty/needsReviewHintEmpty/needsReviewHintCold/\nfailedTitle/failedHint/cancelledHint/pipeline.runs.title — where the risk\nis a typo between the call site and the catalog, the same \"renders a\ndotted key\" failure shape a mocked t() cannot catch. Rewrote the file's\nheader comment to describe this scope accurately instead of the original\noverclaim (\"every dynamically-constructed key path\").\n\nMutation-tested both fixes independently against the real locale JSON:\nremoving pipeline.stopped.runTimeout turns the new sweep case red\n(restored); setting it to an empty string turns ONLY the new\nnon-empty-string check red where the old toHaveProperty would have passed\n(restored).\n\n* fix(renderer): repair two test assertions that could not fail for the behavior they claimed\n\nResultsPanel.test.tsx: a stray trailing quote made\nnot.toHaveTextContent('...needsReviewHint\"') unmatchable by ANY node, so\nthe assertion always passed regardless of what rendered. Removing just the\nquote would not have fixed it either — needsReviewHint is a text PREFIX of\nneedsReviewHintEmpty, which DOES render in this scenario, so a plain\nsubstring check would then always FAIL instead. Anchored with a negative\nlookahead (/needsReviewHint(?!Empty)/) that matches the bare hint key\nspecifically, tolerating the Empty-suffixed one.\n\nGeneratingPanel.test.tsx: the active row never renders\npipeline.step.<key>.description regardless of stageLabel (see M2, the\nsibling test above it) — the old assertion (not.toHaveTextContent the\ndescription) was therefore tautologically true whether or not the \"omit\ncaption when stageLabel is empty\" behavior actually worked. Reworked to\nassert the absence of the caption's own content instead (the elapsed mm:ss\nit always includes when it renders at all).\n\nBoth are repairs to guards that were silently passing against broken\nbehavior — the mutation test IS the fix here. Verified for each: forced\nthe component into the state the old assertion was supposed to catch,\nconfirmed the NEW assertion goes red, reverted the mutation, confirmed\ngreen.\n\n* fix(renderer): a persistently-mounted live region for the cancelled and needs-review announcements\n\nSeveral screen readers do not announce content added to the a11y tree in\nthe SAME update that creates the region — a role=\"status\" div that only\nenters the DOM once its condition is already true (the cancelled-no-output\nhint, the results panel's needs-review box) is unreliable for exactly that\nreason. Both were fixed independently by two reviewers landing on the same\ngap.\n\nAdded one sr-only role=\"status\" aria-live=\"polite\" span, mounted for the\ntailor flow's entire lifetime (every stage transition) — only its text\nchanges, keyed on a (stage, error, machine state, run state) transition\nthe same way the generating panel's own per-step announcer works (H8):\nannounce the TRANSITION, not the mount. This is additive, not a\nreplacement — the two visual banners keep their own role=\"status\" too,\nfor AT/browser combinations that DO handle a freshly-mounted status role;\nthe new region is the reliable fallback for the ones that don't. Only ONE\nregion ends up persistently mounted (the new one, in the flow's own root);\nthe results panel's own needs-review box and the cancelled-hint div are\nunchanged and still mount conditionally as before.\n\nAlso (CR-8): the modal-focus test's document.body.removeChild(dialog) ran\nas a trailing statement — a failed assertion above it would skip the\ncleanup and leave a stray aria-modal node in document.body for every later\ntest, which could silently suppress the focus guard depending on run\norder. Moved to onTestFinished. Verified the cleanup actually fires (not\njust registered) by temporarily adding a follow-up test asserting no\nleaked dialog node, confirmed it passed, then removed the temporary test.\n\nTests: the region is present before mount has anything to announce;\nannounces the cancelled state; announces needs-review once the done stage\nrenders with that status. Mutation-tested: removed the announcement\ntext-setting logic, confirmed both new tests red, restored, confirmed\ngreen.\n\n* refactor(renderer): use a direct sibling import in tailor-state.ts\n\ntailor-state.ts already lives in lib/ — '../lib/tailor-target' left lib/\nand re-entered it. Use ./tailor-target.\n\n* test(resume-pipeline): narrow the wiring-check test to name+paren only\n\nThe call-site presence assertions in execute_routes_resolution_through_the_\npure_resolve_functions pinned the exact argument-list spelling, which a\nrustfmt re-wrap or a harmless local-variable rename breaks even though\nwiring is unchanged — the same lexical-substring anti-pattern the resolve_*\nsplit itself exists to avoid, applied to the wiring guard instead of the\ndecision. Matching on the function name plus open paren keeps the guard's\nonly stated purpose (execute actually calls the pure functions) while\ndropping the argument-spelling brittleness.\n\n* fix(resume-pipeline): escape html markup in the run-terminal notification\n\ncollapse_whitespace closes the newline gap in a scraped/renderer-supplied\nposting title but left libnotify body-markup unescaped, so a Linux\nnotification daemon advertising that capability would render an embedded\n<b>/<a href> as a tag instead of text. escape_markup neutralizes &, <, >\n(order matters: & first, or the inserted entities double-escape) after the\nlabel is built and capped, closing the gap the module's own doc comment\nalready flagged as open.\n\n* test(resume-pipeline): add the two forgery checks the strategy turn skipped\n\nEvery other turn in every_untrusted_block_is_fenced_and_forgery_resistant\nasserts the forged sibling tag and the forged tool-result marker are both\nbroken; the strategy_user addition only checked the closing-tag counts.\nSame hostile fixture, so both checks are free — without them a regression\nin marker neutralization specific to the strategy turn passes silently.\nConfirmed by mutation: isolating a marker-neutralization skip to just the\njob_analysis block (leaving every other turn's fencing untouched) trips\nexactly the new assertion, which the pre-existing checks do not catch.\n\n* fix(shared): require a résumé/job source pair on the run request type\n\nz.input left every field of the two source pairs optional (each has a Zod\n.default(...)), so an empty object, or a résumé-only/job-only object,\ntype-checked even though the schema's own .refine rules reject it at parse\ntime and Rust rejects it at runtime. The exported type now intersects two\nseparate two-member unions (resumeId|resumeText, jobId|jobAdText) over the\nrest of the input shape, rather than one RequireOneOf helper applied twice\n— nesting it that way would let the second application subsume the first\nand stop enforcing the résumé side. @ts-expect-error cases pin the three\ninvalid shapes and the four valid combinations at the type level.\n\nAlso closes two runtime coverage gaps in the same schema's tests: the cap\nassertions proved the ceiling+1 is rejected but never that the exact\nceiling is accepted (an off-by-one that rejected the ceiling too would have\npassed silently), and neither .refine's trim()-based emptiness check had a\nwhitespace-only test on the TS side, though the Rust twin does.\n\n* fix(tailor-flow): clear the persistent live region on the null transition\n\nCR-10: the CR-7 announcer effect updated announcedKeyRef on the null\nbranch but left liveAnnouncement holding the previous cancelled/\nneedsReview text, so a later clean run kept exposing stale copy in the\n(always-mounted) live region. Setting it to '' on that branch also\ncovers CodeRabbit's second concern (an identical second cancellation\nnot re-announcing, since React bails a same-value setState) without\nextra machinery: key can't reach 'cancelled' twice without passing\nthrough null in between, so the clear always lands between the two,\nmaking the second one a genuine text change again.\n\nMutation-tested: reverted the else branch, new regression test went\nred, restored, green.\n\n* test(tailor-flow): match the repo's i18n.exists pattern, drop the deep import\n\nCR-11: ResultsPanel.i18n.test.ts reached eight levels up into\npackages/translations/src/locales/*/translation.json directly,\nviolating AGENTS.md rule 6 (package entrypoints, not deep paths) and\ndiverging from all five other *.i18n.test.ts files in this repo, which\nimport the real @ajh/translations entrypoint and check\ni18n.exists(key, { lng, fallbackLng: false }) + i18n.getFixedT(lng).\n\nConfirmed before rewriting: i18n.getResourceBundle reads the actual\nshipped resources, not a stub (packages/translations/src/index.ts\nstatically imports the real locale JSON into i18next.init, and the\nalready-passing i18n/translations-parity.test.ts already depends on\nthat). fallbackLng: false on i18n.exists is what the five sibling\nfiles use for per-locale separation, and it's sufficient here too: a\nkey missing in one locale fails only that locale's case, independent\nof the other. Every case is now a (locale, key) pair rather than one\ncase checking both locales at once, so a de-only gap can't hide behind\na passing en case for the same key. Kept the source-derived key\nenumeration (PIPELINE_STATUS_KEY, STOPPED_SUFFIX/\nUNKNOWN_STOPPED_SUFFIX, PIPELINE_STEP_KEYS) from the CR-3 sweep.\n\nMutation-tested: deleted pipeline.runs.title from de only — only the\nde case went red, the en case for the same key stayed green, restored\nvia git checkout.",
          "timestamp": "2026-08-14T21:39:27+02:00",
          "tree_id": "dbc0f6d358f5c8ddd75af529e09642a7999a6736",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/19a6de3e90907406b8d6713435d9e30d135a0cb4"
        },
        "date": 1786737088576,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2238871,
            "range": "± 6790",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2643169,
            "range": "± 13108",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 297809,
            "range": "± 8826",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "3f5f153f52bd036d26bf039cb8b963a9dcf4ec9e",
          "message": "feat(pipeline): delete the max generation depth and the depth concept (#993)\n\n* fix(ai-generate): drop the dead depth picker from the fine-tune wizard step\n\nThe staged-depth control in StepFineTune wrote aiGenerate.depth but nothing\never read it back — the wizard's Generate button always ran the fast\none-shot path regardless of the picker, so the affordance was decorative.\nRemoves the control plus its now-orphaned session-store field and i18n key.\n\nTailoredResumePanel and its depth-selection UI stay for now: it also hosts\nthe agentic \"Improve this résumé\" review (#986), which #992 never ported to\nthe apply flow's ResultsPanel — deleting it here would silently drop a\nrecently-shipped feature. See .claude/scratch/drop-generation-depths.md.\n\n* feat(jobs): delete the depth-selection ui, keep the tailored-résumé panel as the improve host\n\nRemoves DepthSelector, SectionTimeline, GenerationDepthPreferences, and\nlib/generate/generation-depth.ts now that the apply flow runs the staged\npipeline unconditionally (#992) and every generate/tailor surface stopped\noffering a depth choice. Strips the depth picker and section timeline out\nof the tailored-résumé panel rather than deleting it outright — it still\nhosts the agentic \"Improve this résumé\" review (#986), which a follow-up\nPR deletes alongside its own agent-run plumbing; deleting the panel here\nwould have orphaned that flow for one PR. Every staged run from this panel\nis now a hardcoded quality-depth run.\n\nDrops the pipeline machine's per-section fold (foldSectionStates and the\nsectionStates it fed) since SectionTimeline was its only consumer, and the\ngenerationDepth field from the preferences store/schema. Pins the historic\ndepth-label vocabulary in PipelineRunsList (old run rows still need to\nrender fast/quality/max) and adds an orphan-key rehydrate test for the\nnow-removed preferences field — both mutation-tested.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(pipeline): delete the max generation depth and the llm judge\n\nThe max depth wasted tokens for no acted-on value (12+ calls a run,\nand llm_judge's Warning-only opinions nothing consumed) — PR-4 step 2\nremoves the Rust half: max_pipeline/MAX_STAGES, the section-generation\nstage and its typed answers, the judge stage, GenerationDepth and the\nrun request's depth field, the max-only budget/deadline constants\n(both sides), and the section-wise progress observer. The staged\npipeline is quality-only now.\n\nRunRow.depth keeps its column (writes \"quality\" on every new run; an\nexisting fast/quality/max row still reads back with no migration).\nGENERATION_DEPTHS stays in packages/shared as a read-only historic\nvocabulary — the renderer still needs it to parse an old max-depth\nreport without relabelling it. reseed_projects (max-only, unreferenced\nonce section_gen.rs is gone) is also removed.\n\n* fix(renderer): drop the deleted depth field and pipeline stages from call sites\n\nMechanical fallout of the shared depth-field/PIPELINE_STAGES removal:\nTailoredResumePanel stops sending depth (the request has no field to\ncarry it now), resume-pipeline.machine and the apply-flow checklist no\nlonger fold sections/assemble/llm_judge, and ModelAdvisor's per-stage\ncontext-fit table drops its two max-only entries — the worst-case\nstage is strategy now, not sections. Includes the matching test/fixture\nupdates and a new regression test pinning that a historic max-depth\nquality report still parses as max rather than degrading to fast.\n\n* chore(i18n): remove translation keys for the deleted pipeline stages\n\nsections/llm_judge (settings.ai.stages.names/descriptions) and\nsections/assemble/llm_judge (pipeline.stage) no longer name a stage\nthis build runs — the max generation depth that produced them is gone.\nen and de stay symmetric.\n\n* refactor(pipeline): delete named_entry_range, entry_range, and identity_line\n\nTheir only callers were the deleted max-depth regenerate path and the\ndeleted pipeline/resume/max_test.rs — none of the three ever had a\nreal caller left after PR #990's depth removal, but pub-in-pub-mod\nvisibility meant dead_code could never flag them. Cascading private\nhelpers (entry_range_and_identity, EntryIdentity, same, DATE_COLUMN_GAP)\ngo with them. Rewrites the doc comments that named the deleted symbols.\n\n* test(pipeline): pin the project separator glyph in the normalizer test\n\nrender_project's PROJECT_SEPARATOR had no literal assertion left after\nmax_test.rs was deleted — stubbing render_project still reddens 7\ntests, but the exact glyph was free to drift silently on the owner's\nshowcase Projects section. Mutation-tested: flipping the separator to\n\" - \" reddens this assertion; reverted after confirming.\n\n* docs(ipc): drop the deleted generation depth from the pipeline contract\n\nThe contract doc still described a `quality`/`max` depth choice and\nlisted \"a rejected depth\" as a job.failed-before-the-run-row cause;\nexecute() no longer validates or rejects a depth at all.\n\n* fix(i18n): restore historic pipeline.stage keys, drop the dead depth constant\n\nfrontend-reviewer HIGH: commit a15aa789 removed pipeline.stage.sections/\n.assemble/.llm_judge from both locales, but PipelineRunsList still renders\nthose exact names off PERSISTED pipeline_run_events rows (a max run's\nstored trail, reachable from both TailoredResumePanel and TailorFlow's\nResultsPanel) — no migration touches old rows, so a German user was seeing\nraw English wire tokens next to translated ones. Restores all three keys\nin both locales with the original copy, and adds a render-based test\nagainst a persisted max-depth trail (not a key-existence check) so a\nfuture cleanup pass that reintroduces this has to break an actual\nassertion. Mutation-tested: deleting one restored key reddens both the new\nrender test and the i18n parity test.\n\nSame reasoning, applied further: use-resume-pipeline-session's reconnect\nreplay sets the live stage label straight from the last PERSISTED event\nregardless of whether the coarse machine still maps it, so TailoredResumePanel\nand useTailorPipeline's stage captions were exposed to the identical gap —\nfixed by the same three restored keys, since all three read sites share\nthe `pipeline.stage.*` namespace.\n\nAlso, per frontend-reviewer MEDIUM findings:\n- Drops PIPELINE_RUN_DEPTHS_HISTORIC — it had zero production consumers\n  (the component always interpolated generationDepth.option.*.label\n  directly) and its own test's `toContain` assertion only proved the\n  constant equals itself. The it.each label table — genuinely mutation-\n  tested — is the real guard and stays.\n- Sweeps the orphaned pipeline.section.* subtree (~17 keys x 2 locales):\n  SectionTimeline was its only consumer and it's deleted. Verified first —\n  the \"Section verdicts\" panel (SectionVerdicts/buildSectionVerdicts) uses\n  the unrelated quality.panel.sections.* namespace and labels sections from\n  the document's own heading text, never from these keys.\n\nEnumerated every other persisted column this PR's deletions touch:\ndepth (PipelineRunSummary/Detail) — already guarded, confirmed real.\nai_stage_overrides per-stage rows — safe: the Rust command filters out\noverrides for retired stages server-side, and resolveStageRouting only\never iterates the current OVERRIDABLE_PIPELINE_STAGES list, so a stray\n`sections`/`llm_judge` override key can never reach settings.ai.stages.*.\nreport.pipeline (QualityReport wrapper) — has no label render path at all.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs(pipeline): amend adr-032 for the depth collapse and sweep stale comments\n\nThe staged résumé pipeline is now the only one: analyze_job → match_evidence\n→ strategy → draft → cover_letter → validate → repair → humanize. ADR-032 is\namended to record why the depths collapsed — a Warning-only model opinion that\nnothing acts on is pure token spend — and why the historic vocabulary\n(GENERATION_DEPTHS, pipeline.stage.*) deliberately outlives the feature that\nwrote it, so existing run rows still parse and render.\n\nAlso sweeps present-tense doc comments in surviving source that still described\ndeleted machinery (the max section fan-out, the judge, section_prompts), and\nrecords the section-regeneration degrade: on a historic max run, regenerating\none role now rewrites the whole Work Experience section, because the per-entry\npath went with max. That is the documented fallback, not a new defect, but it\nis user-visible and belongs in the release notes.\n\nComments only in Rust — no behavior change. A first pass removed three entries\nfrom FENCE_TAG_PATTERNS along with their comment; that is ADR-010 injection\nfencing and a security call, not a docs call, so it was reverted and only the\ncomment rewritten.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): reconcile a job.failed that races ahead of resume-pipeline start()\n\nresume_pipeline_run spawns its task and returns the ids immediately, so an\nadmission/validation failure can call job_fail before start()'s mutateAsync\neven resolves — jobIdRef is still null/stale, so the live job.failed\nlistener drops the event, and (with no pipeline_runs row ever written\neither) the session was left spinning in `starting` forever. start() now\nreconciles once, right after the ids land, against the cheap in-memory\nJobTracker read fetchJob already provides elsewhere for this exact class\nof race.\n\n* test(settings): pin what the search-index deletion actually removed\n\nA bare length assertion passes even if the wrong entry were dropped, or a\nduplicate masked a real removal. Assert the change directly instead:\nai-depth is gone, and ai-stages no longer carries the judge/sections\nkeywords that went with it. Mutation-tested by restoring both keywords —\nthe new assertion goes red.\n\n* refactor(pipeline): correct stale repair-is-last-stage deadline comment\n\nhumanize now runs after repair (PR #991), so the deadline comment's claim\nthat repair has no boundary after it was wrong; describe repair's own\nbetween-call check instead.\n\n* refactor(pipeline): fix backwards free-stage deadline rationale\n\nThe Stage::costs_a_provider_call doc argued aborting at a free-stage\nboundary conserves already-paid work; apply_stop does the opposite (it lets\na free stage run past the deadline). Reword to match: NOT aborting at a\nfree-stage boundary is what conserves the work already paid for.\n\n* refactor(pipeline): rename types_max/assemble to project_seed/project_render\n\nBoth modules lost their max-depth contents when that generation depth was\nremoved; only ProjectOut and render_project (PR #990's deterministic\nProjects normalization) survive. Rename the files to name what they\nactually hold and trim the apologetic why-the-filename-is-wrong headers\ndown to their actual contract; that history belongs in the ADR.\n\n* fix(shared): reword generation depths as legacy read-side vocabulary\n\nresumePipeline.run's wire request has no depth field and every new run\npersists \"quality\"; the doc comments still described the constant as if a\nrun could request a depth. Reword both the TS source and the Rust\ngenerator (regenerated output included) to state new-run \"quality\" is\nfixed and fast/quality/max exist only to read historic rows.\n\n* docs(adr-032): separate the base provider-call count from the optional ones\n\nThe amendment said \"4 LLM calls ... draft + letter\", which does not say\nwhich passes are counted. Honest cost is the point of this programme, so the\nbreakdown is now explicit: 4 base calls (analyze, evidence, strategy, draft),\nthen cover_letter only when includeCoverLetter is set (it defaults to false),\nrepair only when there are Criticals to fix, and humanize 0-2 -- it is\ndeterministic-first, so a draft with no voice flags costs nothing at all.\n\nEach claim verified against the stage sources rather than restated.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): close the render-commit window left in the job.failed reconcile\n\njobIdRef/runIdRef/busyRef are all written in the hook's render body, so a\nsetState only reaches them once React actually re-renders — not\nsynchronously, and not necessarily before start()'s own next await resumes\n(a promise resolved entirely through the microtask queue never yields the\nmacrotask turn React's scheduler needs). CodeRabbit found the residual\nwindow this left in the previous reconcile fix: job.failed could arrive\nright after the reconcile read came back clean but before that commit,\nand still get dropped. Empirically, busyRef has the exact same property\n(confirmed by a failing test before this fix), not just jobIdRef/runIdRef.\n\nstart() now assigns all three refs synchronously the instant each becomes\nknowable — busy right after send('START') (which always lands on a busy\nstate), the ids right after they're returned — independent of when React\ngets around to committing.\n\n* docs(adr-032): stop calling the live routes depths\n\n\"Quality depth\" and \"Fast depth\" were the vocabulary this PR removes: depth\nis no longer a per-run setting anyone can choose. They are now the quality\npipeline (the staged run, the apply flow's only route) and the fast path (the\none-shot generation behind the résumé builder and ai-generate, for generating\nwithout a job posting) -- a different job, not a cheaper setting on the same\none.\n\n\"Depth\" now appears only where it is still true: the persisted read-side\nvocabulary that keeps historic run rows rendering.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs(adr-032): point the restated call limits at their owning symbols\n\nThe amendment quotes real numbers (the cover-letter default, the repair round\ncap, the humanize bounds), so it has to say what owns them, or it silently\nasserts a stale figure the first time someone retunes one.\n\nAnchored to the symbols rather than to line numbers -- a line number rots\nfaster than the value it annotates, and points at nothing once the file moves.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-15T08:06:41+02:00",
          "tree_id": "6d46038f4e3298565a8e3d2123f5e2b01c5d837d",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/3f5f153f52bd036d26bf039cb8b963a9dcf4ec9e"
        },
        "date": 1786774695588,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2320741,
            "range": "± 40901",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2754511,
            "range": "± 72048",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 302903,
            "range": "± 7467",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "950b08829629d4d3388863952fbbd5086e4b22b1",
          "message": "build: pin the rust toolchain across local, ci, and release (#994)\n\n* build: pin rust toolchain to 1.96.1 across local, ci, and release\n\nLocal dev, PR CI, and release builds each resolved whatever \"stable\" meant\non the day, so they could silently disagree (a bare cargo fmt --all mass-\nreformatted files that pass cargo fmt --check under the pinned rustfmt).\n\nAdd apps/desktop/src-tauri/rust-toolchain.toml pinning channel 1.96.1 (the\nalready-installed toolchain, frozen in place — not an upgrade) plus the\nrustfmt/clippy/llvm-tools-preview components every gate needs. Add\nrust-version = \"1.95\" to Cargo.toml, the real MSRV floor re-derived from\n`cargo metadata`'s per-package rust_version fields (sysinfo 0.39.6 is the\nhighest at 1.95; serial_test 4.0.1 is next at 1.93.1).\n\ndtolnay/rust-toolchain doesn't read toolchain files -- its @rev IS the\nversion selector -- so .github/actions/setup-rust (consumed by ci-pipeline,\nquality, and security workflows) and release.yml's inline setup step both\nmove from the @stable branch to the repo's own \"1.96.1\" branch SHA.\n\nVerified locally: rustup auto-installs and activates 1.96.1 with the pinned\ncomponents via the toolchain-file override, and cargo fmt --check/test/\nclippy and pnpm typecheck all pass under it -- no reformat commit needed.\n\n* fix: correct the rust pin to 1.97.1 and drop unused components\n\nTwo review findings on the toolchain pin (9d72b134):\n\n1. It was a silent CI downgrade, not a freeze. dtolnay/rust-toolchain@stable\n   tracks rustup's live \"stable\" channel at whatever version is current when\n   the job runs -- it does not hardcode a version on the @stable branch. As\n   of this pin that resolves to 1.97.1 (released 2026-07-16), not the 1.96.1\n   this machine happened to have installed already. Re-verified both\n   directly: rust-lang/rust's release list has no stable release after\n   1.97.1, and dtolnay/rust-toolchain's own \"1.97.1\" branch hardcodes\n   `toolchain: 1.97.1`. Pin to 1.97.1 -- the version CI has actually been\n   validated against -- and fix rust-toolchain.toml's comment, which\n   described only the dev-machine situation.\n\n2. The toml's `components` list made rustup auto-install rustfmt/clippy/\n   llvm-tools-preview for every CI job via the toolchain-file override, even\n   jobs that request none of them (the plain `cargo check` jobs). Drop\n   `components` from the file; each job's own dtolnay/rust-toolchain step\n   already declares what it needs. Verified locally that rustfmt/clippy\n   still resolve without it -- rustup's default install profile includes\n   both already; only llvm-tools-preview (CI-only, for cargo-llvm-cov) is\n   not part of that default and stays job-scoped in CI.\n\nrust-version in Cargo.toml is unaffected (unchanged Cargo.lock, --locked\neverywhere) -- confirmed by an identical cargo test --workspace result\n(4175 passed) under 1.97.1.\n\nRe-ran the full gate: cargo fmt --check / test --workspace --locked /\nclippy -D warnings / pnpm typecheck / pnpm gen:workflows:check all green\nunder the corrected pin; rustup show resolves to 1.97.1 via the toolchain\nfile.",
          "timestamp": "2026-08-15T12:02:18+02:00",
          "tree_id": "1c87684cfdab4b2d297ebc02cef8169cdf6d2b6e",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/950b08829629d4d3388863952fbbd5086e4b22b1"
        },
        "date": 1786789610325,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2166433,
            "range": "± 49658",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2544884,
            "range": "± 20416",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 296975,
            "range": "± 4122",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "93174a1c5123a37043bebe0e46a2247270eb4fa2",
          "message": "feat: delete the agentic surfaces and extract the prompt fencing (#995)\n\n* fix(renderer): close reset()'s stale-ref window in the resume pipeline session\n\nreset() cleared runId/jobId/busy via setState but left runIdRef/jobIdRef/\nbusyRef stale until the next render body ran, mirroring the start() race\nfixed in #993: a pipeline:stage or job.failed listener still mounted on the\njust-reset run can fire in that gap and be misread as belonging to the next\nsession. Assign the three refs synchronously in reset(), same as start().\n\n* refactor(agent): extract adr-010 prompt-fencing primitives into prompt_fence\n\nPR-5 step 1 (drop agentic surfaces): pulls fenced/JOB_CAP/RESUME_CAP/\nFENCE_TAG_PATTERNS/neutralize_transcript_boundaries out of agent::tools into a\nnew dependency-free crate::prompt_fence module so the pipeline, extension\nbridge, and structured-output re-ask path keep their injection defense once\nagent/ is deleted in step 2. AUTOPILOT_NOTE_SYSTEM moves into\nautopilot_helpers, its only remaining consumer.\n\nPure move: every existing test travels with its primitive, unchanged, plus a\nmutation check (verified, then reverted) confirming a broken fenced() still\nfails pipeline::resume::test::every_untrusted_block_is_fenced_and_forgery_resistant.\nECHO_CAP/clamped_echo stay in agent::tools — no surviving consumer needs them.\n\ntests/architecture.rs: classifies prompt_fence as L0, drops the now-dead\npipeline->agent and autopilot_helpers->agent R7 exceptions, and allowlists\nprompt_fence->ipc_contracts for the generated RESUME_CAP constant (same shape\nas the existing pipeline/ai_config/scraping exceptions).\n\n* docs(architecture): sync layer doc with the prompt_fence extraction\n\nMirrors the tests/architecture.rs change from the previous commit: adds\nprompt_fence to the L0 list, replaces the resolved autopilot_helpers->agent\nTODO(arch) note with what actually happened, and folds the new\nprompt_fence->ipc_contracts exception into the R7 table row.\n\n* refactor(agent): delete the agentic backend (prep-application + confirm gate)\n\nPR-5 step 2: removes agent/ in its entirety (controller, flows, gate, tools,\ntools_pipeline, tools_quality) plus commands/agent.rs (the agent_run/\nagent_confirm Tauri commands), their lib.rs/commands/mod.rs registrations,\nthe AgentGate managed state, and Budget::{AGENT_PREP, AGENT_IMPROVE} +\ntheir compile-time relations and tests in pipeline/budget.rs. Everything\nthis programme's step 1 extracted into prompt_fence and autopilot_helpers\nsurvives untouched.\n\nKept deliberately (verified against the still-untouched renderer, PR-5 step\n3): ipc_contracts::agent (AgentRunRequest/AgentConfirmRequest) and\nagent_flow_kinds (AGENT_FLOW_KINDS), the tauri-client agent namespace, and\nthe packages/shared schemas backing them — removing any of these breaks\n`pnpm typecheck` on renderer code this PR does not touch.\nlimits::AGENT_RUN_* stay too: commands/resume_pipeline's admission control\n(the staged pipeline's cancel registration) uses them, confirmed by grep, not\nassumption. commands::resume_pipeline::resume_pipeline_run is now the third\nCancelRegistry registrant in place of the deleted agent_run.\n\nTransitive orphans found by the delete-and-cargo-check method (not by\nreading the call graph) and removed: resume_pipeline::sync_saved_resume_status\n(agent::tools::save_resume_handler was its only caller) and\nCompleter::capabilities (only agent::controller ever called it — the\nprovider tool-calling surface it fed, AiProvider::chat_with_tools/ToolSpec/\nAgentTurn across every provider adapter, is now unreachable too, but lives\nin the ai-provider domain excluded from this author's paths; flagged for\nthat domain's owner rather than deleted here).\n\nEvery surviving file's doc comments that named agent/agent::* code are\nrewritten to say so honestly (past tense, \"now-deleted\") rather than left\npointing at nothing — verified with a targeted grep sweep, not assumed\nclean. tests/architecture.rs drops agent's L3 classification; its own test\nsuite (18 tests) still passes, confirming the layer model stays internally\nconsistent.\n\n* fix(match-resume): drop jobpostingmeta's write-only location field\n\nUnrelated to the agent deletion — found because it turns cargo clippy\n-D warnings red (dead_code), one of this task's gates. Verified via git\nhistory that the deleted agent/tools.rs never read it either, so this\nwas already dead before PR-5 started, not a fallout of this change.\n\n* test(extension): close the answer-assist forgery-defense coverage gap\n\nFound while proving prompt_fence's extraction is safe for PR-5 step 2:\nevery existing answer_assist/answer_rewrite test covers the LEGITIMATE-input\nshape of build_user_message/build_rewrite_user_message (the right tags\nappear), but none feeds either function a hostile payload — disabling\ncrate::prompt_fence::fenced's neutralization pass left all 53 existing\ntests in this module green.\n\nAdds one hostile-input regression test per call site (mirrors the pattern\nalready used in prompt_fence::test and pipeline::resume::test), each\nmutation-verified: disabling neutralization turns the new test red while\nevery other test in the module stays green.\n\n* chore(ipc): keep agent_flow_kinds' generated doc accurate\n\nThe doc comment gen-ipc-rust.ts emits for AGENT_FLOW_KINDS named the\nagent::flows registry this Rust test used to pin — deleted in the previous\ncommit. Updates the generator's template (not the generated file directly,\nwhich is DO-NOT-EDIT-BY-HAND) and regenerates via `pnpm gen:ipc`.\n\n* docs(architecture): drop agent from the l3 layer doc\n\nMirrors the tests/architecture.rs change from the agent-deletion commit:\nremoves agent from the L3 module list (both the layer table and the\nper-layer section header) and updates the prompt_fence exception note now\nthat agent no longer exists to reach.\n\n* refactor(renderer): drop the agent-run flow's renderer surfaces\n\nRemoves PrepApplicationPanel, AgentConfirm, ImproveResumeRun,\nuseAgentRunSession, the use-agent service hooks, and the\nagent-run.machine state machine -- the last renderer consumers of\nagent.run/confirm/onStep. The prep_application and improve_resume\nflows are dropped per owner decision; the extension's answer-assist\nis unaffected (separate backend + extension surface).\n\nThe tauri-client agent namespace and ipc_contracts::agent stay for\nnow -- a later step removes those once nothing in the renderer calls\nthem.\n\n* refactor(renderer): drop the tailored-résumé panel and its wiring\n\nTailoredResumePanel's only remaining reason to exist was hosting the\nnow-deleted ImproveResumeRun review; its OTHER capability (the staged\nrésumé pipeline: quality report, fabrication review, per-section fix,\nrun history) is fully duplicated by TailorFlow's own ResultsPanel, so\nnothing it hosted goes unreachable. JobDetailPane's trigger button is\nremoved too -- the existing one-shot \"Tailor\" button (handleTailor)\nstays as this posting's tailor affordance.\n\nstopped-reason.ts's doc comment is updated: the agent-run consumer of\nits map is gone, leaving only the staged pipeline's pipeline.stopped.*\nlabels.\n\n* chore(translations): prune jobs.tailored / jobs.prep keys, en + de\n\nOrphaned by the agentic-surface and TailoredResumePanel deletions --\nnothing renders these strings any more. Key parity between en and de\nverified after the cut.\n\n* refactor(ipc): delete the agent wire contract end to end\n\nPR-5 step 4 (final): with the renderer's agentic surfaces gone (step 3), the\ntauri-client callers of api.agent.run/confirm/onStep that forced this\ncontract to stay are gone too, so the whole wire contract goes with them —\nschema, generated Rust, TS client, and every layer between.\n\nDeletes: AGENT_FLOW_KINDS/AgentRunRequestSchema/AgentConfirmRequestSchema\nand their inferred types (packages/shared/src/schemas/index.ts);\nipc/contracts/agent.ts (AgentContract, AgentStepEvent, AGENT_CHANNELS) and\nits wiring in ipc/contracts/index.ts; events/agent.ts (AGENT_EVENTS) and its\nwiring in events/index.ts; the tauri-client agent namespace and its\nregistration in tauri-client/index.ts; ipc_contracts::agent and\nipc_contracts::agent_flow_kinds on the Rust side, regenerated away (not\nhand-deleted) by fixing gen-ipc-rust.ts's template and running `pnpm\ngen:ipc` — caught by gen:ipc:check once already this PR, so this time the\ngenerator changed first.\n\nKept and verified, not assumed: agent-caps.ts and the generated\nipc_contracts::agent_caps::AGENT_RESUME_TEXT_CAP it backs are untouched —\nconfirmed byte-identical after regeneration (git status shows no diff on\nagent_caps.rs) — because prompt_fence::RESUME_CAP depends on that path and\nprompt_fence is what keeps the résumé pipeline and the extension's\nanswer-assist fenced. limits::AGENT_RUN_* stay too, per the prior step's\nconfirmed admission-control use in commands/resume_pipeline.\n\nTransitive orphans found and fixed (delete-and-compile, not call-graph\nreading): pipeline::budget::test's AgentRunRequest security-lock test (its\nsubject is gone; the equivalent guard for the one remaining paying flow\nalready exists at commands::resume_pipeline::test::\nrun_request_carries_only_identity_no_budget_and_no_routing, so nothing is\nnewly uncovered) and the renderer's mock-client.ts, whose AppClient literal\ncarried an agent field TypeScript now rejects as excess — both found by\nrunning the exact gates, not by reading the call graph.\n\n* test(prompt-fence): make every fence_tag_patterns entry load-bearing\n\nSecurity review: removing each of the 24 registry entries individually and\nre-running the whole suite found 15 removable with everything green — 10 of\nthose are tags surviving code still emits. Only 9 entries were guarded by\nanything. The gap: every_untrusted_block_is_fenced_and_forgery_resistant\ndoes assert </job_analysis> appears once, but its hostile payload only ever\nforges </job_posting>, so every entry that payload never mentions is inert\nto that assertion no matter how many real tags surround it.\n\nAdds one table-driven test looping an independent copy of the tag\nvocabulary (deliberately NOT FENCE_TAG_PATTERNS.keys() — a first draft using\nthe live map silently stopped testing whatever was deleted from it, since\nthe deleted entry's own iteration disappears too). A set-equality check\nagainst the live registry catches drift in both directions: a tag removed\nfrom the registry is still asserted against by the hardcoded list (and\nfails for real), and a tag added to the registry without a matching\naddition to the list fails the equality check immediately.\n\nMutation-checked the way the review proved the gap: temporarily deleting\n\"web_search_notes\" and, separately, \"section_note\" (two of the ten\npreviously-unguarded tags) from the registry literal, and verifying the\nper-tag loop alone (equality check disabled) also independently catches a\nremoval — both verified by hand, then reverted before landing.\n\n* test(extension): extend answer-assist fence coverage to all six blocks\n\nanswer_assist.rs::build_user_message fences six untrusted blocks; the\nregression test only forged into question. The two with the strongest\nattacker story were untested: company_research (a web-sourced brief) and\nweb_search_notes (search results) — fully attacker-influenced content, and\nboth among the previously-unguarded prompt_fence registry entries.\nBehaviour was already correct; this closes the coverage gap so a future\nregression in any one of the six is caught at its own call site rather than\ninferred from question's.\n\nLoops the same hostile payload (a forged <job_posting> sibling plus a\nforged [tool_result:save_resume] marker) through each of the six slots in\nturn, leaving the rest empty/absent so exactly one block is under test each\ntime. Mutation-checked: disabling fenced's neutralization pass turns this\nred on the first case while the rest of the module's tests stay green.\n\n* refactor: delete three more agent-era orphans the sweep missed\n\nFound by the architect making every module private and diffing the\ncompiler's unused-item list against the same run at base — catches a pub\nfn's dead_code lint exemption that delete-and-check-one-at-a-time relies on\nthe item already being pub(crate)/private to trip.\n\n- commands/resume_pipeline/report.rs: for_saved_resume and\n  agent_save_pipeline. Their only production caller was the deleted\n  agent/tools.rs:843 (save_resume_handler). Two now-unused imports\n  (crate::error::AppResult, crate::pipeline::resume::stages::validate_documents)\n  go with them; the stale doc reference to a test that no longer exists\n  (the_saved_resume_label_round_trips_through_the_shared_vocabulary, which\n  lived in the deleted agent/tools/test.rs) goes with the function it\n  described.\n- pipeline/runs/mod.rs: PipelineRunStore::update_status_if_present. Its sole\n  caller was the sync_saved_resume_status this PR already deleted; its only\n  remaining references were its own test\n  (a_status_move_never_resurrects_a_deleted_run in pipeline/runs/test.rs,\n  removed alongside it), which is exactly why delete-and-compile missed it —\n  a function whose only caller is its own test compiles clean forever.\n\nVerified no further hidden orphans in either touched file: every remaining\npub fn in both has a real, traced production caller.\n\n* refactor(prompt-fence): make resume_cap a genuine local literal\n\nBoth reviewers converged here. RESUME_CAP was imported from the generated\nipc_contracts::agent_caps::AGENT_RESUME_TEXT_CAP, which forced an\nR7_ALLOW exception spanning L0->L3 — and that constant had no TS consumer\nleft after PR-5 step 3 deleted the renderer's improve_resume entry point\n(its only reader), only the generator. JOB_CAP's own doc already states\nthe rule for this exact case: \"A local literal — nothing outside this\ncrate reads it, and generating a number with one consumer is ceremony.\"\n\nDeletes the generator target (genAgentCaps in gen-ipc-rust.ts, its\nAGENT_RESUME_TEXT_CAP import, its outputs entry), the generated\nipc_contracts/agent_caps.rs, packages/shared/src/agent-caps.ts and its\nbarrel export, and the (\"prompt_fence\", \"ipc_contracts\") R7_ALLOW entry —\nprompt_fence is dependency-free for real now, not by exception.\n\nValue verified unchanged: 8_000 before and after. Mutation-checked:\ntemporarily setting the literal to 100 reddens\nbuild_user_message_neutralizes_a_forged_boundary_in_every_untrusted_block\n(the cap truncates the hostile payload before neutralization can reach the\nmarker's tail) — proof the tests still exercise the real value, not\nsomething cached. r7_allowlist_has_no_dead_entries and\nr7_no_upward_layer_imports both still pass with the entry gone.\n\n* test(resume-pipeline): guard the error-echo clamp resolve.rs kept\n\nresolve.rs::echoed clamps a request-supplied id to 64 chars before it is\nechoed into a validation-error message — reimplemented locally because\nagent::tools::ECHO_CAP/clamped_echo, the equivalent it mirrors, were never\npart of the prompt_fence extraction. At base the rule was mutation-checked\nby commands/agent.rs, which PR-5 deletes; nothing re-proved the clamp\nsurvived on its new sole owner, so replacing echoed's body with\nid.to_string() (no clamp at all) left the whole suite green.\n\nImpact was already bounded — JOB_IDENTITY_CAP caps resumeId/jobId at 512\nbytes before either ever reaches echoed — but a clamp guarded by nothing is\none refactor away from silently doing nothing. Adds one test per call site\n(resolve_resume's \"resume not found\" / resolve_job's \"job not found in\ncache\" errors), each with a 100-char id, asserting the message carries\nexactly the first 64 chars and no more. Mutation-checked: dropping the\n.take(...) from echoed's body reddens both.\n\n* docs: repoint agent_run bucket + chat_with_tools comments at pr-5\n\nlimits/mod.rs's AGENT_RUN_RATE_MAX/CONCURRENCY_MAX/QUEUE_MAX docs still\ndescribed the bucket as the deleted agent_run command's; its sole caller\nis now resume_pipeline_run. Reword to say so, and note the ceiling was\ninherited rather than freshly reviewed for its new caller. Constants keep\ntheir agent_run-derived names on purpose (rename is a separate call).\n\nspend/mod.rs's chokepoint list still cited chat_with_tools as agent_run's\nper-turn billing hook. agent_run is gone; chat_with_tools has no live\ncaller now. Flagged inline for the AI-provider owner as a follow-up, not\ntouched here — ToolSpec/AgentTurn dead-code status is theirs to act on.\n\n* docs(knowledge): drop the agent module from the knowledge base\n\ndomain-model.md still documented the flow registry, the wire vocabulary, both\nflows, their budgets, the tool-call ceiling and gated saves via\nreport::for_saved_resume -- all deleted in this PR. architecture.md still listed\nagent/ and commands/agent.rs under Automation and in the feature-ownership\ntable.\n\nLeft standing deliberately: prompt_fence is now an L0 security primitive owning\nADR-010 fencing, and BOTH the staged résumé pipeline and the extension's\nanswer-assist depend on it. The extension's AI drafting survived this deletion\non purpose -- a reader who sees 'the agent was deleted' could reasonably\nconclude otherwise.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix: close the self-closing tag gap in the fence-tag pattern\n\ncompile_fence_tag_pattern required whitespace before any trailing\ncontent after a tag name, so a self-closer with no leading space\n(<job_posting/>) rode through fenced() byte-identical, attributes and\nall. The group now opens on whitespace OR a slash, then reads to the\nnext '>' the same way the attribute form always did, which also closes\nduplicated-slash and slash-then-attribute variants the reviewer's own\nsuggested pattern would still have missed.\n\nExtends the registry-wide regression test to probe every registered\ntag with a self-closing and a plain-opening forgery, not just the one\ntag the earlier example covered.\n\n* docs: drop dangling intra-doc links to the deleted agent budgets\n\nBudget::AGENT_PREP/AGENT_IMPROVE were deleted with the agent module,\nbut the module doc still used rustdoc link syntax for them; cargo\ndoc's broken_intra_doc_links (warn-by-default) flags both. Switched\nto plain backticks, matching the same names' own convention lower in\nthe same file. Swept the crate for other dangling links to deleted\nagent items; none found outside this file.\n\nAlso drives every_shipped_budget_is_internally_consistent from a\nconst slice of shipped budgets instead of reading RESUME_QUALITY\ndirectly, so a future second budget (already anticipated in this\nfile's own doc) is covered by construction instead of the test\nsilently keeping green while checking nothing new.\n\n* docs: correct the skills_present/absent truncation rationale\n\nThe comment described \"the consumer\" truncating skills_present/\nskills_absent into a prompt as an ongoing fact, but its only named\nexample is explicitly the now-deleted agent::tools_quality module and\nthe crate's sole current caller (pipeline::resume::stages::strategy)\nreads only .roles from extract_evidence. Reworded to state the\nordering is sized for a future truncating consumer rather than\nimplying a live one; sort behavior unchanged.\n\n* test(renderer): make the reset() stale-ref test actually fail without the fix\n\nThe race test invoked the pipeline:stage listener via optional chaining, so\na missing registration would silently no-op instead of failing the test.\nAssert both listeners are registered before firing them, exercise jobIdRef\nand busyRef (not just runIdRef) with a synchronous stale job.failed, and\nreword the comment to describe reset()'s synchronous ref guard rather than\nthe pre-fix behavior it replaced.\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-15T13:08:18+02:00",
          "tree_id": "6d016736e787360e457448e6d62b1c83a1dac767",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/93174a1c5123a37043bebe0e46a2247270eb4fa2"
        },
        "date": 1786792785032,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2170806,
            "range": "± 93969",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2598868,
            "range": "± 32929",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 299376,
            "range": "± 7922",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "3c2aadcf48a7247549486399242ae1f483b3e3db",
          "message": "fix: pipeline timeouts, fence leaks, the generation-application fk, and rust edition 2024 (#996)\n\n* chore: apply cargo fix --edition ahead of the rust 2024 switch\n\nMechanical prep for the edition bump, still on edition = \"2021\" so\nthis is reviewable apart from the flag flip in the next commit:\n\n- gen -> r#gen (extension_bridge/{answer_assist,assist_registry,stream}\n  + tests): `gen` becomes a reserved keyword in 2024 (future generators).\n- expr -> expr_2021 macro fragment specifier (tests/eval.rs): 2024\n  widened what `expr` matches; `expr_2021` keeps the old matcher.\n- match-ergonomics closure pattern (commands/autopilot.rs):\n  `|(_, &idx)|` -> `|&(_, &idx)|` for a HashMap::iter() closure.\n- RPIT capture opt-out (pipeline/resume/mod.rs deadline_guard):\n  `+ 'static` -> `+ 'static + use<>` — 2024 auto-captures all in-scope\n  lifetimes in `-> impl Trait`; QualityCtx<'a>'s `'a` was never used by\n  this closure, so `use<>` restores the old non-capturing signature.\n- cargo fmt: r#gen is 2 chars longer than gen, which pushed one line\n  in assist_registry.rs over the wrap width.\n\ncargo test --workspace: 3970 passed (matches the pre-migration baseline).\n\n* chore: switch the tauri crate to rust edition 2024\n\nFlips edition = \"2021\" -> \"2024\" (rust-version stays at 1.95, well\nbelow the toolchain pin of 1.97.1). Two supporting changes land in the\nsame commit since neither is meaningful without the flip:\n\n- rustfmt.toml: pin style_edition = \"2021\" alongside edition = \"2024\".\n  `cargo fmt` passes the crate's Cargo.toml edition to rustfmt, which\n  overrides rustfmt.toml's own (now-stale) `edition` key for style\n  purposes; without this pin, the edition bump silently reformats\n  dozens of untouched files (import sort order, assert! wrapping).\n  Verified zero source reformatting with the pin in place.\n- src/lib.rs: #![allow(clippy::collapsible_if)], next to the existing\n  #![deny(clippy::await_holding_lock)]. Edition 2024 legalizes\n  let-chains, so clippy's (default clippy::all, not pedantic)\n  collapsible_if lint newly fires at ~96 pre-existing nested\n  if/if-let sites across every domain in the crate under\n  -D warnings. None of the 96 are defects — this is a style\n  modernization deliberately deferred to its own follow-up rather\n  than folded into the edition flip (owner decision), not something\n  cargo fix --edition touches or can safely bulk-apply.\n\nPer-site review of the two edition-2024 semantic changes that matter here:\n\n- The 10 `if let Ok(guard) = x.{lock,read,write}()` sites in src/ (grep\n  found exactly 10): none has an else branch, so the \"guard held\n  through else in 2021, dropped before else in 2024\" temporary-scope\n  change cannot apply to any of them — confirmed both by reading each\n  site and by the fact that cargo fix --edition's own drop-order\n  warnings never named any of these files/lines.\n- The 5 `-> impl Trait` sites (grep found exactly 5): 2 are native\n  trait-method RPITIT (salary_research, commands/ai), which capture\n  all in-scope generics by default regardless of edition since their\n  own stabilization; 1 (ats_seed::by_ats) already carries an explicit\n  use<'_> bound that overrides the edition default; 1\n  (Section::bullets) already necessarily captures its only in-scope\n  lifetime via the elided &ParsedLine return; 1\n  (QualityCtx::deadline_guard) needed and got + use<> from\n  cargo fix --edition to keep 'a out of its 'static-bound closure.\n\nPost-flip verification: cargo check --all-targets --all-features is\nwarning-free (matches edition-2021 baseline); cargo clippy --all-targets\n--all-features --locked -- -D warnings is clean (0 errors — confirms\n100% of the pre-allow 96 errors were collapsible_if, no other lint\nclass hid behind it); cargo test --workspace is 3970 passed, identical\nto baseline; cargo doc --no-deps exits 0 with the same 214 pre-existing\nrustdoc link warnings as edition 2021 (unrelated to this change).\n\n* docs(rust): record what the edition-2024 deferrals actually cover\n\nReview found three comments that were true but incomplete, each in a place a\nfuture reader has to trust without re-deriving:\n\n- The collapsible_if allow is WIDER than its stated reason. The lint also\n  covers plain nested ifs, unrelated to let-chains, so it leaves a small real\n  coverage hole -- and it applies to the lib crate root only, so the same code\n  in tests/ or benches/ still hard-fails -D warnings.\n- The style_edition pin is debt with a price tag: unpinning reformats ~200\n  files in 374 hunks, so it needs its own commit rather than riding along.\n- expr_2021 reads as a deliberate pin and is a cargo fix artifact; nothing\n  here needs the old matcher.\n\nComments only -- no behavior, no lint change. fmt and clippy re-run clean.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix: anchor the pipeline elapsed timer on the run's own start time\n\nGeneratingPanel reset its elapsed clock to 0:00 on every remount because it\nwas seeded from a ref set at component-mount time. Navigating away and back\nunmounts the whole TailorFlow tree (ApplicationDetailPage only renders it\nwhile the Documents tab is active) and reconnects to the still-running\nbackend job, so the clock restarted while the run itself hadn't. During the\nlong, non-streaming Analyze stage that timer is the only liveness signal,\nso a reset made a slow-but-healthy run read as stuck.\n\nAnchor on PipelineRunDetail.startedAt instead - a timestamp the backend\nalready records on pipeline_runs and returns on every run-record fetch, so\nit survives a remount for free with no new state to persist. It's a per-run\ntimestamp, not per-step: the run's own event trail could reconstruct a\nper-step start too, but that requires walking a persisted event log across\na step-to-stage fold and can drift for a poll cycle after a live reconnect,\nso a simple, always-correct per-run number wins. The caption's copy changes\naccordingly (pipeline.step.elapsedTotal, en+de) since it now reads total run\ntime rather than time-in-this-step.\n\n* fix(pipeline): reject a humanize/repair candidate that echoes its own fence tag\n\nA real run exported a résumé with <humanize_document> as the name and\n</humanize_document> as the last line: the model echoed the fence wrapper\nhumanize_user sent it in, and every existing guard (length floor,\ncritical/voice-flag comparison) grades content, not shape, so a wrapper\nthat only ADDS length sailed through clean.\n\nis_usable_rewrite now also rejects any candidate containing a registered\nfence tag, checked against the full prompt_fence registry rather than one\nhardcoded name, and reverts to the original (never strips-and-keeps) since\na model that echoed one tag may have echoed other scaffolding too.\n\nrepair's is_usable_replacement gets the same gate: its prompt wraps the\nsection under <resume_section> and asks for the replacement back, the\nsame shape a leak could take.\n\ndraft and cover_letter have no shape validation at all (audited, not\nfixed here — unlike humanize/repair they are the sole producer for a run\nwith no \"original\" to revert to, so a fix needs a fail-open/fail-closed\ndesign decision rather than a one-line gate).\n\n* fix: give a per-call pipeline timeout a stop reason and a message\n\nanalyze_job/match_evidence/strategy (and repair/humanize) hit their\nnon-streaming per-call deadline and the run recorded no reason at all:\nStoppedReason::Timeout existed in the enum but had no producer, so a\ntimed-out run surfaced as \"stopped\" with nothing further to say.\n\n- Each provider's complete_impl (ollama/openai/anthropic/gemini)\n  classifies a reqwest deadline expiry (is_timeout()) as\n  AppError::Timeout instead of folding it into the generic\n  \"unreachable\" Network error.\n- Pipeline::run_hooked flags that on StageOutcome, and\n  RunHooks::after (via the new apply_timeout) records\n  StoppedReason::Timeout plus which stage and how long on the run\n  ledger.\n- execute replaces the propagated error with an actionable message\n  (stage + duration, content-free per ADR-027) before it reaches\n  job_fail, so the existing raw-error banner has something to show.\n- The renderer's STOPPED_SUFFIX map and both locales gained a\n  \"timeout\" entry, so a timeout-stopped run's history row is no\n  longer blank either.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(i18n): stop the per-stage models card describing a deleted setting\n\nThe card said \"a thorough run works in stages\" and \"fast runs use the active\nmodel for everything and ignore this card\" -- thorough/fast is the generation-\ndepth vocabulary deleted in the depth-collapse PR. There is no such choice any\nmore: tailoring against a job posting IS the staged run, unconditionally.\n\nNot strictly false (a separate one-shot path still backs the résumé builder and\nai-generate), but it points at a setting the user no longer has. Reworded to\ndescribe the two entry points by what they are -- with a job posting, or\nwithout -- rather than by a removed setting name. Both locales.\n\nThe card itself stays: per-stage overrides are unrelated to the deleted agentic\nsystem, and the seven rows match the current pipeline exactly (validate is\nabsent because it asks no model anything).\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix: scale the non-streaming completion deadline by effort too\n\nanalyze_job/match_evidence/strategy each make one non-streaming\nCompleter::complete_json round-trip, bounded by a FLAT 300s\n(OLLAMA_COMPLETION) regardless of the run's chosen reasoning effort —\nunlike the streamed draft/cover_letter calls, which already scale via\nstream_deadline. A local model running at a HIGHER effort got no more\ntime on these three (first-running) stages than the baseline did, so\nraising effort could not have saved a run that legitimately needed\nlonger on a slow local model.\n\n- packages/shared/src/ai-timeouts.ts: new OLLAMA_COMPLETION_BASELINE_SECS\n  + ollamaCompletionDeadlineSecs(effort), scaled by the SAME multiplier\n  table stream_deadline uses. QUALITY_RUN_FIXED_SECS now covers only\n  the repair fan-out + humanize (the two call sites with no effort to\n  scale by, via Completer::complete); the three JSON stages moved to a\n  new, effort-scaled QUALITY_RUN_JSON_STAGE_CALLS term in\n  qualityRunDeadlineSecs. Regenerated via `pnpm gen:ipc`.\n- timeouts.rs: OLLAMA_COMPLETION -> OLLAMA_COMPLETION_BASELINE (const)\n  + ollama_completion_deadline(effort) (fn), mirroring stream_deadline.\n  quality_run_deadline re-derived to match the TS formula exactly (both\n  sides pin the same per-tier table).\n- Completer::complete_json/structured_call/text_request now thread the\n  run's effort through to the provider's StructuredCall, so\n  ollama.rs's complete_impl can scale send_with_retry's deadline by it.\n  complete/complete_with_usage (repair, humanize, chat_with_tools) keep\n  the flat baseline unchanged — they take no AiGenerateRequest, so\n  threading effort through them would ripple into every other caller\n  of Completer::complete (autopilot notes, interview questions, the\n  cover-letter fast path).\n- Budget::RESUME_QUALITY.run_timeout (90 min) is UNCHANGED: it's the\n  effort-blind floor, and multiplier is 1.0 there regardless of which\n  term it applies to. Every tier above it grew to match the raised\n  per-call ceiling on the JSON stages — doc comments across\n  budget.rs/timeouts.rs/repair.rs updated to describe the new\n  arithmetic rather than the old flat numbers.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): link a staged-pipeline generation to its application on save\n\npersist_document built its AiGenerationRecord with ..empty_record(), whose\napplication_id is always None, and nothing else in the staged pipeline ever\nset one -- merge_application only ever PRESERVES an existing link\n(incoming.application_id.or(existing.application_id)), it never establishes\none. So the first staged-pipeline save for a posting with no prior linked\ngeneration persisted application_id: NULL, invisible to every reader that\nfilters ai_generations by applicationId.\n\nThe résumé masked it: it has its own run-scoped read path\n(PipelineRunDetail.resumeText, joined by run id, no link involved). The\ncover letter has no such channel and was unreachable for that whole class\nof application.\n\nMirrors the same idempotent ApplicationStore::upsert_for_origin call\nai_generations_save already makes, non-fatal on error like that path.\n\nAlso audited for other application_id-filtered ai_generations consumers this\nsilently broke: applications_delete's \"delete everything\" path\n(AiGenerationStore::remove_for_application) never matched an orphaned row,\nso deleting an application whose only linked document came from the staged\npipeline left that ai_generations row behind permanently -- not fixed here,\nreported for a separate follow-up.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix: include effort in the résumé stage cache key\n\nCloses a stale-answer gap the previous commit opened: analyze_job/\nmatch_evidence/strategy's Completer::complete_json now threads the\nrun's real effort to the provider (needed to scale the per-call\ndeadline), and on a thinking-capable Ollama model that reaches the\nrequest as its own `think` field (ollama::think_level) -- which\nchanges the model's actual reasoning depth, not just how long the\ncaller waits. The stage cache key never carried effort, on the\n(previously true) premise that it never reached the provider at all;\nleft alone, a \"high\"-effort answer could now be served back to a\n\"low\"-effort request for the identical résumé/posting/model.\n\nStageIdentity gains an `effort` field (mirroring context_window's\n\"reaches the request, so it's a key term\" treatment); StageCacheKey\nfolds it into the pre-hash the same positional way. Investigated while\nassessing FIX-3 (stage-artifact reuse on retry) -- the existing cache\nIS the reuse mechanism the task asked about, and this closes the one\ncorrectness gap that would have undermined trusting it.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(applications): backfill the application link on orphaned generation rows\n\nThe FK fix in persist_document stops NEW rows being orphaned; it does\nnothing for rows the staged pipeline already wrote before that fix\nshipped, and applications_delete(keepDocuments=false) matches by\napplication_id -- an orphaned row is invisible to it. The user asks the\napp to delete their documents and the documents stay: a privacy defect\non existing data, not a cosmetic gap.\n\nlink_orphaned_generations, run once per boot from ApplicationStore::open\n(same guarded, non-fatal, cross-database posture as the existing\nbackfill_from_generations), links every ai_generations row with a\nnon-empty job_url and a NULL application_id to the Application that\nalready exists for its normalized job_url.\n\nDeliberately lookup-only, never an upsert: unlike backfill_from_\ngenerations (which predates ADR-0001 -- no Application ever existed for\nthose rows), a staged-pipeline run is always launched FROM an\nApplication's own page, so the Application for an orphaned row almost\nalways already exists. A row this cannot match means either the user\ndeleted that Application (creating one here would resurrect it) or the\nrow was never linked to begin with -- both must stay NULL. A wrong link\nis worse than a missing one.\n\nNo schema migration: the application_id column and its index already\nexist from AiGenerationStore's own add_application_id migration: this is\ndata repair, so there is nothing to append to either store's\nposition-indexed MIGRATIONS array.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* test: prove a retry reuses the json stages that already succeeded\n\nCloses the gap the FIX-3 investigation flagged: the reuse-on-retry\nbehaviour (analyze_job/match_evidence answer instantly from the stage\ncache on a retry after strategy timed out, per the incident) was an\nemergent property of independently-tested pieces -- key derivation,\nKvCache read/write, per-stage hit/miss -- with nothing asserting the\nscenario end to end. A future change to key derivation, the ledger, or\nstage ordering could have broken it while every existing test stayed\ngreen.\n\na_retry_after_a_mid_run_failure_does_not_re_spend_the_stages_that_already_succeeded\nwires a real KvCache (tempdir) through the same StageCacheKey::new /\ncache::get / cache::put sequence each real stage's run() performs\n(cached_stage_call mirrors it verbatim, since this crate has no Tauri\ntest harness to construct a real Completer/AppHandle for the actual\nstage structs). Asserts on a provider-call counter, not wall-clock or\na cached flag:\n\n- first attempt: analyze_job + match_evidence succeed (1 call each),\n  strategy fails (1 call, no cached answer to write);\n- retry with unchanged inputs: analyze_job/match_evidence make ZERO\n  further calls (a re-ask would panic inside the closure), strategy\n  runs for real again since nothing cached it;\n- a third attempt at a DIFFERENT effort must still miss analyze_job's\n  cache -- the scenario where dropping `effort` back out of\n  StageCacheKey (reverting the previous commit) shows up as a REAL\n  re-spend of a stale-effort answer, not just a unit-level hash\n  mismatch.\n\nMutation-tested by hand (applied and reverted, not assumed): forcing\ncache::get to always return None reddens the retry section; dropping\n`effort` from StageCacheKey::key's pre-hash reddens the third section\nspecifically.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* refactor: extract the timeout/network transport-error mapping into one helper\n\nFour provider adapters (anthropic/openai/gemini/ollama) duplicated the same\nis_timeout()-to-AppError classification verbatim, pushing anthropic.rs over\nthe R8 hard LOC cap. map_completion_transport_error in ai_provider/mod.rs\nnow owns the mapping (and its is_timeout() rationale comment) once; each\nadapter calls it with its own provider label and per-call deadline.\n\n* test: pin the timeout/network transport-error classification\n\nmap_completion_transport_error had zero end-to-end coverage after the\nduplication-removal refactor: a mutation test forcing it to always return\nNetwork reddened nothing workspace-wide. Adds a test that drives it with a\nreal is_timeout()-producing local-socket timeout and a real refused\nconnection, asserting AppError::Timeout vs AppError::Network — the first\nlink in the is_timeout() -> Timeout -> StoppedReason::Timeout -> UI-banner\nchain this batch's timeout reporting depends on.\n\n* fix: make the ai_generations fk backfill link-first, never resurrect deletes\n\nlink_orphaned_generations (lookup-only) now runs before the wide,\ncreate-capable backfill_from_generations on every ApplicationStore::open,\nso a row it can resolve never reaches the wide backfill's clobbering merge\n— the previous order made the lookup-only pass dead in production.\n\nThe wide backfill also gains a vintage gate (APPLICATIONS_FEATURE_EPOCH_MS,\na day before PR #359 merged): it may still create an Application for a\nrow provably predating the Applications feature, but a row at or after\nthat boundary is only ever linked, never created for. Without this, an\nunconditional create-on-miss silently resurrected an Application the user\nhad just deleted (either delete arm) on the very next boot.\n\nAdds a boot-path regression test (ApplicationStore::open itself, not the\nprivate methods directly) plus three resurrection guards, each\nmutation-tested by disabling the relevant gate and confirming it reddens.\n\n* fix: refuse to save a résumé or letter that echoes a prompt-fence tag\n\nsave_verdict now checks contains_fence_tag on both the draft résumé and\nthe cover letter, in addition to the existing work-history-loss check —\ndraft and cover_letter are the sole producers of these documents (unlike\nhumanize/repair, which can discard a bad candidate and keep the last-good\ntext), so this is the last chokepoint before persistence that can catch a\nleaked <generated_resume>/<candidate_resume> wrapper before it reaches the\nsaved aggregate and the exported PDF.\n\nRefusing to save (fail-closed) rather than saving with a flagged report is\ndeliberate, matching the existing work-history refusal's precedent: a raw\nframework artifact reaching an employer is worse than a document the user\nhas to retry. SaveVerdict::Refused now carries a user-facing reason so the\ntwo defects don't share a misleading message.\n\n* test: add a store-level guard for the ai_generations application_id fk\n\npersist_document_establishes_the_application_fk_it_used_to_skip asserts on\npersist_document's SOURCE TEXT — it would stay green even if\nupsert_for_origin errored on every call, or try_state::<ApplicationStore>()\nreturned None at runtime, since neither shows up in the source.\n\npersist_document itself still can't be driven directly (no tauri::test\nmock-app harness), so the new test drives the STORE-LEVEL chain it\ncomposes instead: upsert_for_origin's returned id lands on\nAiGenerationRecord.application_id, save_application persists it, and\nremove_for_application actually finds and deletes the row through it —\nmutation-tested by forcing application_id: None, which reddens it.\n\nThe source pin stays (it still catches the call disappearing from\npersist_document, which the store-level test structurally cannot).\n\n* fix: guard the generating-panel elapsed anchor against 0 and clock skew\n\nrunStartedAt now falls back to null for a 0/negative startedAt, not just\nnull/undefined, so GeneratingPanel's own runStartedAt ?? mountFallback\nrecovers instead of anchoring the elapsed caption on the Unix epoch.\nGeneratingPanel additionally clamps the tick at 0 so a startedAt that\npassed the > 0 guard but is still ahead of this client's clock (skew)\ncan't render a negative \"N total\" caption.\n\nNot reachable with today's now_ms()-populated pipeline_runs.started_at\ncolumn, so this is defensive; renderer-only, no frontend agent active.\n\n* fix(pipeline): stop persist_document from resurrecting a deleted application\n\npersist_document called ApplicationStore::upsert_for_origin, which creates\nan Application unconditionally on a job_url miss. A staged run takes\nminutes; if the user deletes the Application it was launched from while\nthe run is still working, the run landing and upserting on that job_url\nsilently resurrected the delete -- no race required, just the plain\nsequence run-in-flight -> delete -> run lands.\n\nSame hazard e695f4cf closed for ApplicationStore::link_orphaned_generations\nat its own call site, three commits earlier in this batch -- that fix\nnever reached this call site. Switch to the same read-only primitive,\nfind_by_job_url: on a miss, leave application_id: None instead of creating\none. The normal case (Application still exists) is unaffected -- the FK\nstill gets set and the cover letter stays findable by Application-scoped\nreaders.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* test(ai-provider): pin the ollama completion baseline, not the stream one\n\nBoth cross-language tests asserted ollama_completion_deadline /\nollamaCompletionDeadlineSecs against STREAM_BASELINE_SECS /\nstream_deadline, but the two baselines are documented as separate\nconstants free to drift independently — they only share a value today.\nThe old assertions would go red the moment someone made that sanctioned\nchange, misdirecting blame at the shared multiplier table. Assert\nagainst OLLAMA_COMPLETION_BASELINE(_SECS) instead, with the multiplier\nlookup reproduced inline so the check still catches real divergence in\nthe shared table rather than becoming a tautology.\n\n* fix: gate the résumé pipeline save on ctx.letter, not letter_text()'s fallback\n\npersist_document (and execute's own copy of the same decision) gated\nsave_verdict on ctx.letter_text(), which falls back to the user's own\npasted reference letter (input.cover_letter) whenever the cover_letter\nstage produced nothing (include_cover_letter = false). That text is\nnever what cover_letter_text persists, so a registered fence tag\noccurring incidentally inside a pasted letter (<question> is a\nrealistic one) refused the entire save - resume, report and all -\nover a defect that could never reach the saved document.\n\nGate ctx.letter instead: the stage's own output, the only letter text\nthis run can actually persist. A leaked tag in the MODEL's own letter\noutput still refuses the save, since it lands in ctx.letter directly.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix: localize the résumé pipeline's per-call timeout banner\n\ntimeout_message spliced the raw internal stage key straight into an\nun-translatable English sentence (\"The \\\"match_evidence\\\" step didn't\nget a response within 302s.\"), reaching job.failed's event data\nverbatim after a run the user may have watched for minutes.\n\njob_fail now has a job_fail_with_data sibling that carries structured\n{ kind, stage, seconds } as the event payload instead of a\npre-formatted string; the resume pipeline's per-call-timeout arm is\nthe one caller that uses it (every other job_fail site is unchanged).\nThe renderer's useResumePipelineSession recognizes the shape and\nrenders it through the existing pipeline.timeout / pipeline.stage.*\ni18n keys (en/de), falling back to the raw stage key for a build that\ndoesn't have a label for it yet. timeout_message's English fallback\nstill lands on the job's own tracked (un-localized) error field.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs(ai-provider): correct the quality-run-deadline headroom claim\n\nquality_run_deadline_clears_the_inner_per_call_bounds asserted >= against\na value that is provably identical at every effort tier, and its doc\ncomment borrowed research_deadline's \"must clear with headroom\" rule even\nthough RunDeadline is only ever polled between calls (never raced against\nan in-flight completion the way research_deadline's tokio::time::timeout\nwrap is), so no headroom is needed for the last inner call's own timeout\nto win. Rename to quality_run_deadline_equals_the_inner_per_call_bounds,\nassert_eq!, and document why exact equality is safe here.\n\n* docs(ai-provider): correct is_timeout() classification claims\n\nreqwest 0.13's Error::is_timeout() walks the whole error source chain\n(its own TimedOut marker, hyper::Error::is_timeout(), a raw\nio::ErrorKind::TimedOut) rather than narrowly matching only the\nclient's own .timeout() call. That's still the correct bucket here:\nnet::http::shared sets no separate connect/read timeout, so every\nrequest's only timing bound is the same per-call .timeout() these\ncall sites set, and it covers connect through the last streamed\nbyte — but the comments overstated the mechanism as excluding\nconnect/DNS/TLS failures generally. Correct map_completion_transport_error's\ndoc and the assumption-pinning test's header comment; the URL/secret\nscrubbing on the Network branch is untouched.\n\n* test(ai-provider): make the refused-connection timeout tests race-safe\n\ntests.rs and anthropic_tests.rs both bind an ephemeral loopback port,\nclose it, then unconditionally assert the next request is refused —\nboth compile into the same test binary and run in parallel by\ndefault, so a sibling test can reclaim the freed port in between and\nturn the connection into an accidental success or a slow response,\nfailing the test for a reason unrelated to is_timeout() classification.\nExtract a refused_connection_error() helper per file that retries\nagainst a fresh port (bounded, 5 attempts) instead of accepting\nwhatever the first attempt produces.\n\n* fix: run the legacy generations backfill exactly once, ever\n\nbackfill_from_generations ran on every ApplicationStore::open and only\nskipped a row once application_id was set or created_at crossed the\nepoch guard. Deleting an Application detaches its generation rows back\nto application_id=NULL without touching created_at, so a genuinely\npre-epoch row stayed pre-epoch and unlinked forever -- the next boot's\nunconditional re-scan recreated the Application the user just deleted.\nThe epoch guard narrowed that window; it never closed it.\n\nAdd a durable backfill_state marker (new append-only migration) and\ncheck it first: once the one-time legacy backfill has completed for a\ndata dir, later boots skip it entirely. link_orphaned_generations stays\nsafe to re-run every boot since it is lookup-only and can never create\nan Application.\n\nSplit the backfill + its marker out of applications/mod.rs into\nmigrations.rs (same store, same pattern as reminders.rs/contact.rs) to\nstay under the R8 LOC cap, and allowlist that file for R3's rusqlite\nconfinement rule, mirroring the existing per-domain-store entries.\n\n* test(applications): close two mutation-surviving gaps in the backfill link tests\n\nEvery existing link_orphaned_generations test seeds the orphan row with\nan already-normalized job_url, so deleting the internal\nnormalize_job_url call inside link_orphaned_generations still leaves\nevery one of them green. Add a test that seeds the RAW url a genuine\npre-fix row would carry.\n\nAlso add a test at exactly APPLICATIONS_FEATURE_EPOCH_MS: every\nexisting epoch-guard test uses a value safely past the boundary, so\n`>` silently passes where `>=` is required. This pins the inclusive\nboundary directly.\n\n* fix: round a per-call timeout up to whole seconds, never truncate to 0s\n\ntimeout_message and timeout_failure_data both divided milliseconds by\n1000, truncating down. A sub-second per-call timeout -- reachable once\neffort scales the deadline down far enough -- rendered \"within 0s\" and\nshipped seconds: 0 to the renderer's timeout banner.\n\nExtract a shared round_up_seconds (div_ceil(1000).max(1)) so the\nEnglish message and the structured event payload always agree on the\nsame non-zero rounded value. Update the two pinned tests for the\nrounding change (300_021ms now reports 301s, not 300s -- ceiling, not\ntruncation) and add a dedicated sub-second regression test where\ntruncation and ceiling diverge to 0 vs 1.\n\n* fix: match the save verdict exhaustively instead of unreachable!()\n\nThe Ok(()) if refused arm re-derived the refusal reason by\nre-destructuring verdict with a let-else unreachable!() fallback,\ntrusting it to stay in sync with the separately-computed refused bool.\nA future edit to one without the other would panic inside the spawned\nrun task, leaving the job with no terminal event.\n\nNest a single exhaustive match on the verdict inside the Ok(()) arm\ninstead, so a new verdict variant is a compile error here, never a\nruntime panic on a fallible path.\n\n* fix(applications): correct the architecture-allowlist claim about the backfill's connection\n\nThe allowlist comment said applications/migrations.rs's persistence \"lives\nentirely inside the applications domain store, on the SAME connection\" -\nfalse: backfill_from_generations (and link_orphaned_generations) open a\nsecond connection onto the sibling ai_generations.db to read and link its\nrows. Reword to match the code.\n\n* fix(applications): stop leaking a path through the boot-time backfill warn logs\n\nBoth open()-time warn! calls interpolated the AppError's Display directly.\nrusqlite::Error::InvalidPath embeds the offending filesystem path in its\nDisplay text, which AppError::from preserves, so a failed\ncrate::db::open(&gen_path) could log an absolute path (repo path-privacy\nrule, and the same thing tests::architecture::r15_no_display_in_log_macros\nalready guards against generally). Log AppError::code() instead - a fixed,\npath-free category string, exactly what it exists for.\n\n* fix(applications): stop legacy_backfill_done swallowing a real read error into false\n\nEvery query_row error, not just QueryReturnedNoRows, was mapped to false via\nis_ok() - a transient SQLite failure looked identical to \"not done yet\" and\nre-ran the one-shot legacy scan, which recreates a deliberately-deleted\nApplication (the exact bug the marker exists to prevent), reachable through\na different door. Return AppResult<bool> and use .optional()? so only\nQueryReturnedNoRows reads as \"not done\"; every other error now propagates\nthrough backfill_from_generations's existing ? into ApplicationStore::\nopen's non-fatal warn-log path.\n\n* fix(applications): log dropped legacy generation rows instead of silently discarding them\n\n.filter_map(|r| r.ok()) discarded any row whose column mapping failed, then\nthe one-shot marker got set as if every row had been processed - so a\ndropped row is never retried. Count the drops and log them at warn once the\nscan finishes; the message is a bare count only, never the row's id/company/\nurl (path-free, content-free per ADR-027), since the marker this scan feeds\nis one-shot and this is the only signal the loss is ever diagnosable from.\n\n* fix(applications): relink stranded legacy generations after a restore, not just at boot\n\ndata_import replaces aiGenerations wholesale via AiGenerationStore::import,\nbut never touched the legacy-backfill marker. Restoring a pre-\nApplicationStore-era backup (one whose bundle has no \"applications\" section)\nleft its generations with application_id = NULL forever: the marker is\nalready set from the very first boot, long before any later restore, so\nApplicationStore::open's one-shot scan never even sees them again.\n\nExtract the scan body into scan_legacy_generations (shared, marker-free) and\nadd ApplicationStore::relink_legacy_generations_after_restore, called from\ndata_import only when the bundle replaced aiGenerations but carries no\n\"applications\" section - i.e. verifiably predates ApplicationStore, so none\nof its rows can be a deliberately AiGenerationStore::detach_application-ed\nrow (that capability did not exist yet when such a bundle was exported).\nA modern bundle that DOES carry \"applications\" is left alone: it already\nrestores self-consistent application_id links via the two imports, and CAN\nlegitimately carry a deliberately-detached row that re-scanning would\nresurrect - the exact bug the one-shot marker exists to prevent.\n\n* test(applications): cover the marker-set-last guarantee on a partial backfill failure\n\nOnly the success path was tested. Poison one row's link-back write with a\nSQLite trigger so backfill_from_generations fails strictly between the\nfirst row's committed Application and the second row's write, then assert\nthe one-shot marker stays unset and a later boot retries and finishes the\nrow it never reached, without spawning a duplicate Application for the row\nwhose creation already succeeded before its own write failed.\n\n* fix(data): resolve the restore relink data dir like setup and test its gate\n\nTwo review findings on the restore-specific relink, both in one file.\n\nThe relink resolved its directory with a bare `app.path().app_data_dir()` and\nskipped entirely when that failed. Setup resolves through\n`resolve_and_export_data_dir`, which falls back to `platform::config::data_dir()`\n— so on a host where the bare call errors, every store still opened through the\nfallback and the one-shot boot marker got set, while the relink (the only thing\nthat can still repair those rows) silently did not run. The marker turns a\nskipped repair into a permanent one, so the two resolutions must not differ.\n\nThe gate `aiGenerations present && applications absent` is the whole boundary\nthat stops a modern full backup from resurrecting a deliberately-detached row,\nand nothing tested it. Lifted into a pure `is_legacy_generations_only_bundle`\nand tested across all four combinations, exactly one of which may return true.\nChosen over a source-pin test: a pin asserts text exists in a file, not that\nthe decision is right, and goes stale silently when the code is reshaped.\n\nMutation-tested all three ways the condition can be weakened — `&&` to `||`,\nand dropping either operand — each reddens the four-combination test.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-16T02:20:05+02:00",
          "tree_id": "c55141a575613ef0e322ea15ab37d190fe744475",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/3c2aadcf48a7247549486399242ae1f483b3e3db"
        },
        "date": 1786840867504,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1766040,
            "range": "± 52593",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2088439,
            "range": "± 21880",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 242629,
            "range": "± 3213",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "571a50b99d62bc73e26a235d16167f8a007bc4bd",
          "message": "fix: complete the cover letter at the export boundary and repair the pipeline regressions (#997)\n\n* build: rename vite configs to .mts for the native config loader\n\nVite warns that ESM syntax in a .ts config loaded as CommonJS breaks under\nconfigLoader: 'native', which becomes the default in a future major. The .mts\nextension is unambiguously ESM regardless of the package's type field, and both\nconfigs are auto-discovered so no build flag changes.\n\nThe desktop config also swaps __dirname for import.meta.dirname, the second\nthing the native loader cannot provide.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(documents): send the resolved market when tailorflow exports\n\nBoth export calls passed undefined in the locale position, which the Rust\nexporter resolves to the intl market. A German letter therefore rendered with\nno DIN 5008 conventions, and once the exporter starts synthesising the\nsalutation it would have written an English one into a German letter.\n\nResolves the market once from the language the hook already detects, via the\nsame resolveMarket helper the ai-generate surface uses.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(export): complete a body-only cover letter at the export boundary\n\nThe staged pipeline's letter prompt tells the model not to write a header,\nsalutation or signature because 'the application adds them at export time'.\nNothing did. The resume side of that same promise is kept by\nContactProfile::apply_to_header; the letter side never existed.\n\nWithout a salutation the parser never starts the body, so every paragraph fell\ninto the pre-salutation branch and rendered as recipient lines: plain text,\nhard line breaks, no bold, no paragraph spacing. The opening paragraph became\nthe letterhead name and was repeated under the sign-off, and a paragraph\ncontaining a digit and a period became the date.\n\ncomplete_letter_text runs once in validate_and_normalize, so PDF, both DOCX\nscanners and the live preview all inherit it, and letters already stored render\ncorrectly with no regeneration. It is a no-op on a letter that already carries\nboth parts, so the full-letter shape still round-trips unchanged.\n\nAlso hardens the two predicates that let prose masquerade: a name is capped at\n64 chars / 8 tokens, a date at 48 chars / 8 tokens. The letterhead name now\nfalls back to the contact profile before the letter's first line, which also\nfills the previously empty PDF author field.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(documents): seed tailor meta from the aggregate and stop blanking the report\n\nThree defects in one hook, all from the staged-pipeline cutover.\n\nThe meta object was fabricated: an empty topRequirements strips the requirement\nmetric out of every Re-check, and because the stub is non-null it also\nshort-circuits the answers assistant's own metadata extraction, which then\nwrites the empty list back to the aggregate. Seed it from the persisted record\ninstead, which carries every field one-to-one.\n\nThe report let a live run win even when its report was null, so a run that\nstopped before the validate stage blanked a perfectly good persisted report.\n\nThe aggregate invalidation sat behind a detail.status gate that a hard-failed\nrun never satisfies, since polling stops and no run record is ever written.\nKey it on the session machine's terminal state and invalidate autopilot too,\nwhich the deleted path did and the staged one dropped.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(documents): render the live preview with the same market as the export\n\nGenerationOutput passed no locale to PdfPreview, so the preview command\nresolved the intl market while the export used the real one. Now that the\nexporter synthesises the market's salutation and sign-off, that showed up as\nthe preview reading 'Dear Hiring Manager' while the downloaded german letter\nread 'Sehr geehrte Damen und Herren'.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): restore market conventions, keyword bold and the requirement list\n\nThe staged letter prompt lost the market-conventions block the deleted TS path\nfenced, so a german letter was written to no etiquette at all: no length band,\nno formality, no Betreff, none of the DACH inclusions. It also banned markdown\noutright, and the resume draft prompt never asked for keyword emphasis, so both\ndocuments came out with no bold at all.\n\nSeparately, top_requirements was only ever read from the request, which the\ntailor surface sends empty. That made the requirement metric None, so the panel\nprinted a dash and missing-requirement findings could never fire, and the empty\nlist was then persisted over a pick-non-empty merge, freezing the keyword chips\nat whatever a pre-refactor run left. The analyze stage already extracts exactly\nthis list; resolve it there and persist the resolved one.\n\nhumanize ran after validate with accept gates that never looked at keyword\ncoverage, so a rewrite could delete job-ad terms and still ship. It now rejects\na candidate that drops coverage past the existing threshold.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(documents): resolve the job market from the posting location\n\nAn english posting fell through to the intl market, whose page is A4, so a us\napplicant got A4 instead of us letter and uk/ie/au postings got intl instead of\nuk conventions. The job object tailorflow already receives carries a location;\nit was never read.\n\ncountryFromLocation builds its country-name index from Intl.DisplayNames over\nthe codes the market map already lists, so both 'Cologne, Germany' and 'Koeln,\nDeutschland' resolve without a hand-maintained table. It matches whole\ndelimiter-separated segments right to left, so Austin never hits Austria and a\nus state code that is not a market key stays inert.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(export): insert the synthesised salutation after the letter furniture\n\nThe completion step prepended the salutation at line zero, which was right\nwhile the prompt emitted body-only prose. Now that the prompt writes a market\nsubject line and a date before the body, a salutation ahead of them made the\nparser start the body early, so Betreff and the date rendered as ordinary body\nsentences and the subject block disappeared.\n\nSkip leading blank, subject and date lines before inserting, reusing the\nparser's own predicates so the two can never disagree. looks_like_date widens\nto pub(crate) rather than being duplicated.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(pipeline): the company research toggle reaches the cover letter again\n\nThe toggle is rendered, capability-seeded and persisted, but the staged run\nnever received it, so research fed only the answers assistant. The letter lost\ncompany grounding entirely when the one-shot path was deleted.\n\nThe stage now researches opt-in and fences the brief as data, with the prompt\ntold to use it as company context and never as the candidate's own experience.\n\nThe call is billable web search, so it goes through the same shared ai_research\nbucket the command uses, via a Completer method in the file already allowlisted\nfor Tauri state. Bucket, rate, concurrency and the provider daily charge are one\nnamed constant across both call sites rather than two matching literals. Every\nfailure path degrades to no block and a letter that still generates.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(documents): send market, date and the research flag into the staged run\n\nAll three run-request fields existed with defaults and none were sent, so the\nletter prompt still wrote to intl conventions, emitted no date, and never\nresearched the company however the toggle was set.\n\nmarket reuses the value already computed for the export locale, so the prompt\nthat writes the letter and the renderer that lays it out cannot disagree. today\nis formatted at start time rather than in a memo, since a session left open\nacross midnight would otherwise date the letter yesterday.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* test(export): pin the body-only letter contract in both formats\n\nEvery existing letter test feeds a complete letter, the shape the old prompt\nproduced, which is exactly why nobody noticed that the current prompt emits body\nonly and the export never completed it.\n\nAdds an adversarial body-only fixture pair carrying the three traps from the\nreal incident: a long opening paragraph that used to become the letterhead name,\na paragraph with digits and a period that used to become the date, and a bold\nkeyword that vanishes the moment a paragraph is misrouted into the recipient\nblock. Asserts the salutation, every body paragraph in reading order, the\nsign-off and the signature name on the extracted pdf text layer and in\ndocument.xml, so the two formats cannot drift.\n\nAll three fail red against a neutered complete_letter_text, verified.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* style: apply rustfmt\n\nThe pre-commit hook formats TS and JS only, so rust formatting from this\nbranch's agent work never ran. CI gates on cargo fmt --check.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: record the cover letter export boundary contract\n\nADR-034 pins the ownership split the fix establishes: generation owns the body\nand the optional subject and date, export owns the salutation, sign-off and\nsignature. ADR-0021 only scoped the resume header, so a reader could reasonably\nconclude the letter needed no export-time completion, which is how the missing\nhalf went unnoticed.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* build: rename the desktop vitest config to .mts\n\nLast config still emitting the native-config-loader warning. The desktop package\nis the only one without type module, so its vitest config was the one loaded as\ncommonjs; the root and every other package were already fine.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(documents): send the market when exporting a letter from the generation card\n\nThe third cover-letter export path still passed undefined for locale, which the\nexporter resolves to the intl market. Now that the exporter synthesises the\nsalutation and sign-off, that appended an english pair to a german letter, so\nthis branch introduced the defect rather than inheriting it.\n\nResume export keeps its unset locale, matching the ai-generate surface, which\ndeliberately leaves the resume to the user's own locale choice. A test locks\nboth halves in.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): stop logging the research brief and make the fence test bite\n\nThe hostile-input fixture forged only one tag, so the new letter-turn\nassertions held whatever the registry said; deleting a tag left them green.\nIt now forges each tag it asserts on, verified by removing letter_date from\nthe registry and watching the test go red.\n\nThe full research brief was logged at info on three paths. App logs ship in the\ndiagnostics bundle and the redaction pass covers paths, urls, credentials, hosts\nand emails, not free web prose about a company. Length only now.\n\nAlso narrows looks_like_date to the export module and admit_research to the\ncrate, and documents the marker-wrapped no-fix case in letter_shape.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: index adr-034 in the knowledge readme\n\nCaught by the pre-push agent-system drift guard.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* test(documents): let the busy-state assertion see the mount render\n\nThe spy was cleared between mounting the drafting-state hook and asserting,\nso a mount-time invalidation was erased before the check. Removing the clear\nmakes the assertion cover the mount; mutating the hook to drop its busy guard\nnow fails it with the two invalidations it should catch.\n\nAlso corrects adr-034: the body-only fixtures never invoke letter_system, so\nthey cannot lock the prompt contract. Only the prompt-text test does, and only\nagainst removal or rewording, not against the model's output shape drifting.\nRecords the marker-section no-fix gap alongside it.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(export): render cover letter bold in docx and stop charging cached research\n\nAddressing review. Two are real defects, not polish.\n\nThe docx cover-letter scanners passed the already-strip_md'd text into\nrender_cover_letter_paragraph, which runs its own inline markdown parse, so\nthere were no ** markers left to find. Body bold never rendered in any docx\nletter layout. A leftover dead parse_inline_md(trimmed) line was the fossil of\nthe intended call. Found because the new contract test asserted paragraph\nseparation but not the bold run it claimed to guard, so tightening the\nassertion turned it red against shipped code with no mutation needed.\n\nResearch admission charged the provider daily ceiling before enrich_with\nchecked its cache, so a cache hit spent a day's allowance on no provider work.\nAdmission is now rate and concurrency only; the daily charge moved after the\ncache miss, inside the shared enricher, which fixes the command path too. Both\ncallers now admit through one implementation instead of two that could drift.\nThe salary path keeps its eager charge: it has no cache ahead of the call.\n\nAlso extracts the duplicated candidate-name chain, replaces a source-scrape\ntest with a compile-time signature assertion, corrects the humanize threshold\nwording plus its boundary case, and documents the byte-vs-utf16 cap asymmetry.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-16T06:31:33+02:00",
          "tree_id": "829aef677a65bacef8f4b5b12ca5341e634a195d",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/571a50b99d62bc73e26a235d16167f8a007bc4bd"
        },
        "date": 1786856131451,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2176195,
            "range": "± 38712",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2617219,
            "range": "± 50998",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 309846,
            "range": "± 13960",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "65ea4188c14390b96372d055663d55f8bccd7af4",
          "message": "docs: fold the second adr tree into the knowledge base (#1000)\n\n* docs: fold the second adr tree into the knowledge base\n\nThe repo had two ADR directories with colliding numbers: docs/adr/0013 was\nemail watching while adr-013 was the resume builder, and both got cited as\n\"ADR-13\". It already caused a wrong citation.\n\ndocs/knowledge/decision-records/ is the canonical one - created 2026-05-31\nwith the knowledge base, indexed in its README, owned by project-steward, and\nthe only one the docs-standards skill points at. docs/adr/ appeared twelve\ndays later, carried no index and no owner, and grew to 23 records because\nnothing stopped it.\n\nThe files are moved but deliberately NOT renumbered. An ADR is a dated record\nand its number is cited from commit messages, merged PR bodies, code comments\nand the published tech radar; the landing benchmark and metrics snapshots are\ngenerated captures of commit text that must not be rewritten. Renumbering\nwould falsify all of it, so the series is folded in as-is and marked closed -\nnew ADRs continue the adr-NNN sequence.\n\nEvery reference follows: 24 live files, the tech radar's ADR_DIR and its\npublic GitHub blob links, and the relative links inside the moved files, which\ngained a directory level. Generated landing snapshots under public/ are left\nuntouched as the historical records they are. check:tech-radar,\ncheck:agent-system and check:landing-drift all pass, and the knowledge README\nnow indexes all 57 ADRs.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* test: repoint the tech-radar fixture at the unified adr directory\n\ncheck-tech-radar.test.mjs builds a synthetic repo and writes its fixture ADR\nto docs/adr/, which no longer exists after the trees were folded together, so\nthe real-adrSlug case resolved against nothing and the check exited 1 where the\ntest expected 0.\n\nThe test was right to fail - it is asserting that a valid slug resolves, and\nthe directory it resolves against moved. Fixture repointed at\ndocs/knowledge/decision-records/; the assertions are unchanged.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: correct accuracy drift the adr move surfaced\n\nMoving 23 ADRs made git read them as new files, so review examined content\nnobody had re-read since it was written. Ten inaccuracies came back, none\nintroduced by the move. Kept separate from the move commit so those 2211\nrelocated lines stay verifiable as byte-identical.\n\nTwo mattered. ADR-0005's privacy guarantee allowed egress only for services\nthe user configures or invokes plus crash reports, while its own body\ndocuments an on-launch updater check - README.md already discloses that, so\nthe ADR was the wrong one; the guarantee now names both classes of automatic\napp-initiated egress. ADR-0009 still described profile.get as authenticated by\na per-frame pairing token, a contract ADR-0010 retired in favour of the mutual\nHMAC handshake, so anyone reading it would have implemented the wrong auth\nmodel; it now points at ADR-0010.\n\nThe rest: ADR-0005 counted seven egress classes where it lists eight; ADR-0008's\ntitle claimed three surfaces where its body describes four and conflated the\nremoved pre-push LLM lane with the deterministic pre-push gates that remain;\nADR-0011 used a field name the bridge does not have and presented snapshot\nsymbols ADR-0012 superseded as current; ADR-0013 claimed email content never\nleaves the device, which conflated IMAP ingress with egress; ADR-0019\nprescribed a prohibited cat command and overstated portability for a file that\nis now ESM-only; ADR-0022 opened two lines with #936 and #937, which Markdown\nparses as malformed headings.\n\nDecisions and rationale are untouched - only claims that no longer match the\ncode.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-16T17:41:41+02:00",
          "tree_id": "b943e5968f2a08b49a22d93a39a4e1d3b96eccba",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/65ea4188c14390b96372d055663d55f8bccd7af4"
        },
        "date": 1786896318787,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2246203,
            "range": "± 44646",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2668273,
            "range": "± 20418",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 296856,
            "range": "± 8800",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "83a322e6fa2967b952d1aee58f8033a6800a2bfb",
          "message": "feat(scraping): treat an implausible scraped company name as absent (#1001)\n\n* feat(scraping): treat an implausible scraped company name as absent\n\nPR #960 shipped six cover letters carrying garbage company names - the\nscraper returned things like \"Apply now | LinkedIn\" as the employer. Every\nexisting guard passed, and correctly: validate/content/factual.rs checks\ngenerated text AGAINST the job ad, so reproducing a garbage company faithfully\nis not a factual error. Nothing checked whether the ingested posting was sane\nin the first place.\n\nAdds is_implausible_company to scraping::trust - CTA debris, board names\nstanding in for an employer, separator and HTML wreckage, placeholders, and\nimplausible shapes - plus a TrustFlag::ImplausibleCompany raised by\nassess_trust. That module's contract is flag-only, enrich never drop, so it\ngains signal and blocks nothing.\n\nActing on it happens at two real boundaries rather than by blocking: the\npipeline blanks an implausible company before it can seed company research or\nthe persisted record, and export's filename falls back to the same default an\nabsent name already gets. Passing None rather than garbage means the existing\nprompt contract takes over - it already says to name only the role when no\ncompany is provided - so no prompt text changed.\n\nThe predicate is deliberately conservative: a false positive silently deletes\na real employer from a letter, which is worse than the bug. Johnson & Johnson,\nBen & Jerry's, Yahoo!, Booking.com and 37signals are pinned as accepted. A\nbare single character is rejected, which does reject X - at that length the\noverwhelming cause is scraper truncation, and the cost of being wrong is soft\nbecause the letter simply names the role instead.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(scraping): stop the sanity gate rejecting real employers, and surface its flag\n\nReview caught the failure mode that matters more than the bug: the predicate\ndeleted real companies. Confirmed by running it - Monster Worldwide, Xing SE,\nIndeed Inc, Glassdoor Inc and Apply On Demand Inc were all judged implausible.\nEvery one hires directly and posts under its own name, so a letter would have\nsilently dropped a real employer.\n\nBoth offending rules matched too loosely. JOB_BOARD_NAMES rejected any name\nCONTAINING a board token as a word; CTA_PHRASES substring-matched, which is\nwhat caught \"Apply On Demand Inc\" via \"apply on \". Both now match only when\nthe entire normalized name IS the board name or CTA phrase, so a bare\n\"LinkedIn\" is still garbage while \"Xing SE\" is not. The originally reported\n\"Apply now | LinkedIn\" is still rejected - by the separator rule, not by\neither loosened list, and a test pins that so the coverage is not accidental.\n\"Apply on Indeed\" becomes an accepted false negative, pinned as a deliberate\nchoice rather than left to be rediscovered as a regression.\n\nThe new TrustFlag variant never reached the frontend. It serializes as\nimplausibleCompany but the hand-maintained TS union and both locale files\nstill listed four flags, and TrustBadge calls t() unconditionally - so the\nbadge would have rendered the literal key on exactly the postings this branch\nexists to fix, in both languages. Adds the union member and both strings.\n\nThe repo's purpose-built i18n key-drift guard stayed green through all of\nthat, because its fixture list derives from the same stale union it guards. A\nguard sourced from the thing it guards is not a guard. Fixed structurally: an\nexhaustive Record<TrustFlag, string> now lives in production code, so a\nmissing variant fails typecheck for the whole app rather than one test file.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* refactor(scraping): scope the company validator to the crate and bound its scan\n\nReview findings on #1001, both correct.\n\nis_implausible_company was pub with no out-of-crate caller - every use is\ncrate::scraping::trust::is_implausible_company from resume_pipeline and\nexport. Narrowed to pub(crate).\n\nIt also collected every char of the trimmed name before the length check\nrejected anything over MAX_COMPANY_CHARS, so an arbitrarily long scraped value\nallocated proportional to its full size. A scraped company field is untrusted,\narbitrary-length input, so bound the collect at MAX + 1 - the only length\nquestion asked is \"> MAX?\", which MAX + 1 answers, and the char-ratio checks\nfurther down run only on names that already passed that gate, where the cap is\na no-op.\n\nMutation-checked that the length rule is still genuinely reachable after the\nchange rather than assuming it: disabling it turns the absurdly-long fixture\nred.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-16T19:51:57+02:00",
          "tree_id": "b955fefbf29f1ba19dfbae686239d63b54120fca",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/83a322e6fa2967b952d1aee58f8033a6800a2bfb"
        },
        "date": 1786903408158,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2263339,
            "range": "± 61453",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2643882,
            "range": "± 49968",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 294747,
            "range": "± 3460",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "93f0fc0d0e191bd5c434b11e7158e24bd5f7afa9",
          "message": "fix(pipeline): catch per-section language drift, drop empty sections, and keep the regenerate language (#1003)\n\n* fix(pipeline): catch per-section language drift, drop empty sections, keep the regenerate language\n\nThree owner-reported defects, all downstream of one architectural change: PRs\n#969 and #992 moved generation from ONE whole-document call to per-section\ncalls that cannot see their siblings. Before that, coherence was free.\n\nLanguage drift. content.language_mismatch read the whole document, and\nlanguages_align returns the DOMINANT language, so a majority vote hid exactly\nthe reported case. Measured: one Italian section in a long English resume went\nundetected, two undetected, three detected. A per-section pass now runs when\nthe document-level read comes back clean.\n\nIt is scoped POSITIVELY to Summary/Experience/Projects rather than by excluding\nSkills. Review caught why that matters: SectionKind has six variants and\nclassify_section has no heading list for Certifications, Awards, Publications\nor Languages-spoken, so all of them land in Other. A correct English \"AWS\nCertified Solutions Architect (Professional, 2022)\" block clears the character\nfloor and reads as non-English - an exclusion list silently admits every kind\nnobody thought of. A false Critical here blanks keywordCoverage and suppresses\nevery alignment finding on the whole document, so the cost of over-reach is\nhigher than the miss.\n\nEmpty sections. My own #998 regression: \"order the sections EXACTLY as … do\nnot drop\" read as a manifest plus a ban on omitting anything, so the model\nemitted Projects and Publications with nothing in them. The prompt now frames\nthe list as an order, not a checklist.\n\nThe guard is a Warning, not a Critical, and the severity is the design. repair\nacts on Criticals and its remedy is to REGENERATE the section - asked to fill\nan empty PROJECTS the model would invent projects, a fabrication in the one\npart of this pipeline built to prevent them. Removal is the right remedy and\nmodel::adapter::push_nonempty_section already does it structurally, so nothing\nempty reaches the rendered document.\n\nRegenerate language. useTailorPipeline re-detected from jobDesc every render\nand treated \"I could not tell\" as \"it is English\", discarding a language the\nrun already knew. It now prefers the persisted value - normalized through\ntoLanguageCode first, because extractMetadata persists a display NAME\n(\"German\") while every other writer stores an ISO code and save_application\nmerges both into one record. Passing \"German\" through would have been worse\nthan the bug: normalize_language truncates it to \"ge\", which matches no\nlanguages_align arm, turning the language checks off entirely for that\ndocument.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): make the splice boundary and the accept rule survive per-section generation\n\nAn audit measured what #969/#992 broke when generation moved from one\nwhole-document call to per-section calls that cannot see their siblings. Five\nfindings, one root cause each side of the same seam: the splice trusted the\nmodel's reply, and the accept rule could not see the damage.\n\nA repair round could silently delete a job. company_survives lowercased the\nENTIRE generated document and substring-searched for the employer, so a\nSummary reading \"…at Acme Payments and Globex Logistics\" spared the deletion\nof the whole Globex entry from EXPERIENCE. Measured: roles 2 to 1,\nfactual.dropped_role never fired, round accepted and persisted. Survival is now\ntested against the generated EXPERIENCE section, all of its lines, so an\nordinary bullet reword still matches.\n\nA reply containing the whole document was accepted and spliced over one\nsection. is_usable_replacement checked for a fence tag, a leading heading and\none body line, and nothing about how many sections came back. Measured: 6\nsections to 10, and because reorder_sections is a stable sort, linearize merely\nparked the duplicates adjacent - the exported PDF showed four sections twice.\nUnder mutation it compounded to 4x as the loop re-accepted round after round.\n\nA reply for a DIFFERENT section was accepted. The target was resolved by\nSectionKey and the reply spliced without re-classifying it, so asking for\nSUMMARY and receiving SKILLS silently destroyed the summary and created a\nsecond skills section, which the next round then could not find. The reply's\nown heading is now re-classified and a mismatch is discarded - the right remedy\nfor a bad attempt is to drop it, not to raise a Critical that would ask the\nmodel to regenerate again.\n\nAnalysis::section_of_kind returned only the FIRST section of a kind, so an\ninvented link in a second Projects section produced zero Criticals. The\nCritical-severity consumer now reads every matching section; the two\nWarning-severity consumers keep the cheaper lookup, since the residual risk\nthere costs a missed Warning rather than a missed Critical.\n\nround_is_worse could only see Criticals and absences, while its sibling\nhumanize_is_worse also guarded coverage and voice - and repair runs FIRST,\nmakes up to eight blind per-section rewrites, and had the weaker rule. It now\nreverts on a drop in roles_output, the one metric that saw the deleted job, and\nshares humanize's coverage guard rather than duplicating it.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(pipeline): let a section rewrite see the document it belongs to\n\nBefore #969/#992 a resume was generated by ONE model call, so language, voice\nand tense coherence came free. The staged pipeline broke generation into\nper-section calls that cannot see their siblings and replaced that implicit\nguarantee with a prompt instruction - and instructions drift. Three\nuser-visible bugs followed, including a resume whose summary was English, work\nexperience Italian, and skills English again.\n\nrepair_user passed the source resume, the one section being rewritten, its\nissues and an optional note. Nothing told the model what the rest of the\ndocument looked like. It now also receives a compact anchor: the Summary,\nwhich carries the register, and one representative Experience bullet, which\ncarries tense and voice - each excluded by SectionKind when it IS the section\nbeing rewritten, so a rewrite never matches the text it is replacing.\n\nAn anchor rather than the whole document because this is charged up to\nMAX_SECTIONS_PER_ROUND x max_repair_attempts = 8 times per run. Capped at\nSIBLING_CONTEXT_CAP, roughly twice the measured worst case in this crate's own\nfixtures and far below the whole-document cap humanize uses for its single\npass. The trade: drift confined to Skills or Education against the anchor is\nnot prevented, only detected.\n\nThis is model-generated text re-entering a prompt, so ADR-010 applies exactly\nas it does to a scraped job ad. document_context is registered in\nFENCE_TAG_PATTERNS and covered by the existing load-bearing forgery loop;\nremoving the registration lets a forged closing tag leak through, which the\ntest now proves.\n\nThe user's own regenerate-this-section button inherits all of it, since\nresume_pipeline_regenerate_section calls the same function.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(pipeline): revert a repair round that introduces cross-section incoherence\n\nAn audit found the codebase's only two cross-section coherence checks -\nduplicate.bullet and consistency.skill_not_demonstrated - are computed and then\nthrown away. repair acts on Criticals only, humanize on voice.* only. Measured\nconsequences, both accepted and persisted: a round that doubled the whole\ndocument produced 5 duplicate.bullet warnings at duplicateRatio 1.00 and\nshipped; a round that deleted an employment entry produced 4\nskill_not_demonstrated warnings and shipped.\n\nThe naive fix is to revert when those warnings are present, and it is wrong.\nThe same audit measured them false-positiving under per-section generation:\nrewording two Experience bullets to drop the exact tokens - the ordinary\noutcome of a rewrite that cannot see the Skills section - raises four warnings\non a truthful document. An absolute threshold would revert good rounds and\nwould need calibrating against noise.\n\nSo the term is a DELTA, not a count. The question is not whether the document\ncarries cross-section warnings but whether THIS ROUND introduced any that were\nnot there before. Four before and four after reverts nothing; four before and\nnine after reverts. That needs no threshold, is immune to the measured baseline\nfalse positives, and matches the before/after shape round_is_worse already uses\nfor its newly-introduced-absence term.\n\nPer code rather than summed, so a round that fixes two duplicate.bullet\nwarnings while introducing two skill_not_demonstrated ones cannot hide a\nregression behind an unrelated improvement - a case a summed total reads as\nflat, and which the mutation check confirmed only the per-code form catches.\n\nAlso closes two advisories from the security review of da4926f0: the anchor is\ntrimmed once at its source, so repair_system's gate and repair_user's own\nemptiness test can no longer disagree about whether a document_context block\nexists; and the context rule now says to match the language and tense OBSERVED\nthere, naming it a writing sample rather than asking the model to conform to\nuntrusted content.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(validate): do not read an unrecognised experience heading as deleted jobs\n\nSecurity review found a HIGH regression introduced by this branch's own\nscoping of the dropped-role survival search.\n\ncompany_survives used to read the whole generated document, which let a\nSummary sentence naming a former employer mask the deletion of that employer's\nentire entry. Scoping the search to SectionKind::Experience closed that, but\nturned a second fact into the same verdict: when the generated resume has NO\nExperience-kind section, the scoped haystack is empty, so every source role\nreads as deleted.\n\nclassify_section knows work experience, berufserfahrung, employment and a few\nmore - it does not know Work History or Selected Roles, which land in Other.\nMeasured on a resume carrying both roles verbatim: heading WORK EXPERIENCE\ngives 0 issues, WORK HISTORY gives 2 factual.dropped_role Criticals, SELECTED\nROLES the same. Those are terminal, not merely wrong: criticals_by_section\nroutes them to SectionKey::Experience, find cannot locate it, no repair runs,\nand the run parks at needsReview telling the user two jobs vanished from a\ndocument that still lists them.\n\nReachability rose in this same branch, which is what makes it urgent rather\nthan theoretical: draft_system was loosened from \"do not reorder, drop, or\ninvent\" to \"an ORDER, not a checklist\" so the model is freer to choose its own\nheading, and matches_requested_kind constrains only a repair reply, never the\ndraft.\n\nAbsent is not deleted. The search now falls back to the whole document when no\nExperience-kind section exists, mirroring the guard project_link_issues\nalready had - preferring a possible miss over a guaranteed false Critical,\nsince the scoping exists to catch a rare regression and must not manufacture a\ncommon one.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): resolve the blocking findings from the pre-merge review ensemble\n\nThree reviewer passes plus a security gate, run over the whole branch. Every\nfinding below was reproduced before being fixed.\n\nThe Rust suite was RED and I reported it green. cargo test --lib and\n--test architecture do not build tests/eval.rs; CI's nextest --all-features\ndoes. The corpus guard was doing exactly its job - the new Italian-drift\nfixture was graded by nothing - and the command I ran was narrow enough to\nmiss it. The fixture is now registered in CASES.\n\ngenerated_experience_lower fell back on TEXT emptiness rather than section\nabsence. A wiped Experience section still yields one blank line, so the joined\ntext is empty, the fallback fires, and an employer named in the Summary masks\nthe loss. Measured: a resume with its entire employment history deleted\nreported factual.dropped_role = 0 and ok = true. It now tests section\nexistence, mirroring project_link_issues' own presence guard.\n\nsection_language_issues gated on a SectionKind allowlist, a proxy for \"is this\nprose\" that was wrong in both directions - too narrow to see an Experience\nsection headed Work History, too wide for a keyword-shaped one that whatlang\nmisreads. Replaced with a prose-density test: the share of words opening\nlowercase, which is the function-word density whatlang needs and is\nlanguage-independent, since German capitalises nouns but still connects them\nwith lowercase articles and verbs. Measured after: drift is caught under all\nthree headings, and a keyword-shaped section no longer fires.\n\nproject_link_issues widened its generated side to every Projects section but\nleft the source side on the first, so a resume with both PROJECTS and SIDE\nPROJECTS accused the user of inventing their own link - unclearably, since\nrepair routes the finding to the first Projects section. Both sides are now\nsymmetric.\n\ncross_section_regression could revert a round that fixed every Critical because\none Warning appeared. It is now gated on the Critical count not improving. My\ncommissioning argument for the delta was wrong: I claimed the measured false\npositives were carried rather than introduced, and round_is_worse's own doc\nrecords the measurement as zero to four.\n\nThe splice gate had no test. Both guard tests rebuilt is_usable_replacement and\nmatches_requested_kind inside their own closure, so deleting a clause from\nproduction left all 137 green while a docstring claimed the mutation was\nverified. Extracted sections::accepts, called by production and tests alike.\n\nAlso: section_count counted heading LINES, so an ALL-CAPS employer inside a\nsingle-section reply read as a second section and the regenerate button\nsilently no-opped; and representative_bullet could anchor a section on its own\nheading.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): resolve the confirmation review, and fix the skills section shape\n\nThe previous commit fixed one review round and introduced regressions doing\nso. Every finding below was measured before being fixed.\n\nRemoving the SectionKind allowlist put Skills sections in scope for the first\ntime, and looks_like_prose reads a canonically-lowercase tool list as prose -\nthe real casing of pandas, numpy, pytest, git, nginx and kubectl IS lowercase,\nand the bar was one word in five. Measured against the pinned whatlang on a\nGerman resume's own KENNTNISSE section: a false Critical, report.ok false, and\nthe repair loop burning calls on a correct section. The guard test for exactly\nthis passed only because its fixture happened to be Title-Case. The Skills\nexclusion is back, ahead of looks_like_prose, which still covers the\nlist-shaped Experience case it was added for.\n\nreal_section_count made the incomplete classifier the sole arbiter of \"is this\na second section\", contradicting the same commit's other finding. certifications,\nawards, languages, publications and work history are all in the parser's\nSECTION_NAMES but absent from classify_section, so a reply naming them passed\nthe gate and was spliced whole - the doubling corruption, reopened. A heading\nnow counts when classify_section knows it OR the parser does.\n\nlooks_like_prose skipped every caseless script, because char::is_lowercase is\nfalse throughout Arabic, Hebrew, CJK, Thai and Devanagari. Measured: an Arabic\nExperience section at 148 significant chars was never checked, where it fired\nbefore. It now decides per word, so an uncased word counts toward prose - the\nwhole-text form fails once the English heading is included in the checked text.\n\nThe Critical gate had removed the only signal that ever saw a doubling round.\nduplicate.bullet is ungated again; only skill_not_demonstrated stays gated,\nwhich matches the measurement that motivated the gate - every noise reading in\nthe doc names shared skill tokens, never duplicates.\n\nThree behaviour changes had shipped with no test at all; validate/content/test.rs\nwas untouched. Five tests added and PROSE_LOWERCASE_WORD_RATIO pinned.\n\nSeparately, the skills section had no specified shape - resume_conventions.skills\nis only the localised heading, so the model chose and the repo's own fixtures\ndisagree. It is now grouped inline lists, one line per group, with the bullet\nform explicitly prohibited rather than merely undescribed: a bullet per skill\nspends a line on one word, and an ATS extracts a comma list just as well.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): make an unroutable language finding a warning, and close three gate gaps\n\nSeven review findings on the open pull request. Every one verified before being\nfixed.\n\nA language-drift Critical on a section the repair grammar cannot route was\nunclearable. key_of maps SectionKind::Other to None, so a finding labelled\nVolunteer, Awards, Certifications or Work History is dropped by\ncriticals_by_section: no repair runs, the regenerate button cannot reach it,\nand the run parks at needsReview on a blocker nothing can lift. My own\nprose-density change made it reachable by removing the kind allowlist, putting\nevery Other-kind section in scope for the first time. An audit of this branch\npredicted this precise shape and I did not act on it.\n\nIt follows the rule this branch has applied throughout: a Critical whose remedy\ncannot be executed is not a Critical. An unroutable section now warns. The\nfinding still reaches the quality report; it no longer blocks. Traded\ndeliberately: drift confined to such a section warns rather than blocks, which\nis better than a block nobody can clear.\n\nreal_section_count missed ATX headings, so a reply using ## Leadership passed\nthe multi-section gate and was spliced whole - the doubling corruption, third\nvariant after the heading-line count and the classifier-only count. It now\nreuses export::parser::strip_atx_heading rather than forking a second copy of\nthe shape test, since duplicated heuristics are a defect class this repo has\npaid for before.\n\nUnioning every generated Projects section made one invented URL report twice,\nbecause the invention loop pushes per occurrence. New with the symmetry fix;\nthe existing test used distinct links so it could not see it. Deduped on the\ncanonical link.\n\ncontext_anchor built the whole Summary and let fenced() truncate it silently;\nit now caps where it is built. A guard test could pass through the identity\ncheck rather than the heading count it targets, so the premise is pinned.\n\nOn the renderer side, resumeLanguage || jobAdLanguage picked the first\nNON-EMPTY and validated only that, so an invalid legacy resumeLanguage\nshort-circuited a perfectly good jobAdLanguage and sent the run to detection.\nEach field is now normalised and validated independently. Presence is not\nvalidity - the same substitution behind every other defect on this branch.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-17T05:31:17+02:00",
          "tree_id": "73fa1f79aac77ceaab754d63d1f4a7bd81bbd4fb",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/93f0fc0d0e191bd5c434b11e7158e24bd5f7afa9"
        },
        "date": 1786938906644,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2182805,
            "range": "± 60801",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2560541,
            "range": "± 25474",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 299222,
            "range": "± 3049",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "d8b693c6a3ae29adc06c4922444b80fcdfcba8c1",
          "message": "fix: generate the résumé in the job ad's language, and notice when it doesn't (#1004)\n\n* fix(pipeline): resolve the target language from the job ad, never the source resume\n\nTwo renderer-side defects that pin a tailored resume to English whatever the\nposting is written in.\n\nThe persisted-language branch added in #1003 reads resumeLanguage/jobAdLanguage,\nbut the staged pipeline writes only target_language and leaves those two empty\nvia empty_record(), and ai_generations::pick keeps the existing value for an\nempty incoming field. So on the tailor flow that branch never fired.\n\nThe fields it did read carry the wrong thing. The extractMetadata heuristic\nfallback seeded targetLanguage from the SOURCE resume language name, so an\nEnglish resume on a German ad was pinned to English by design - and once such a\nrecord existed the memo preferred it and pinned the tailor run too. The\nmodel-JSON path was already correct (validateMetadata sets targetLanguage from\njobAdLanguage); it is left alone with a comment recording that both paths agree.\n\nresolveTargetLanguage is now a pure exported function with an explicit chain:\npersisted targetLanguage, then persisted jobAdLanguage, then a fresh confident\ndetection of the current ad, then en. resumeLanguage is out of the chain\nentirely - it is the source language and is never a legitimate target.\n\nA guess is used but never remembered. The fallback tier reports confident:false\nand start() puts an empty string on the wire, which Zod .default() passes\nthrough (it fires only on an absent key), normalize_language reads as en, and\npick() treats as keep-whatever-is-stored. So detection failing once cannot\nbecome the preferred value on every later run. Verified AiGenerateRequest.locale\nhas no production consumer, so an empty value there is a no-op.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* refactor(validate): split language guards out of content/mod.rs\n\ncontent/mod.rs sat at HARD_CAP_LOC (1400, R8) and needed headroom for the\ncross-language fix that follows. Pure move of the language family\n(is_language_mismatch, language_mismatch_for, source_is_a_reliable_control,\nlanguage_issues, section_language_issues, looks_like_prose, section_text,\nMIN_CHARS_FOR_LANGUAGE_CHECK, PROSE_LOWERCASE_WORD_RATIO) into a new\ncontent/language.rs submodule, matching the existing alignment/ats/consistency/\nduplicates/factual/letter/lexicon/voice shape. significant_chars stays in\nmod.rs (Analysis::posting_language_diverges still needs it) and is shared with\nlanguage.rs via super::, the same pattern alignment.rs already uses.\n\nNo behavior change: cargo test --all-features --locked passes with the same\n4129/0/26 pass/fail/ignored set as before the split.\n\n* fix(validate): score cross-language résumés with corroboration, not the source\n\nThe language guards were gated on languages_align(source_resume, target_lang),\nwhich is false whenever a translation is actually expected -- an English\nsource + a German target failed the control by construction, so both the\ndocument-level and per-section language.language_mismatch Criticals were\ndead in the one scenario they exist for (#1002-adjacent, the Incoretex bug).\n\ndocuments/keywords.rs: add detected_language(text) -> Option<&'static str>,\nthe confidence-gated language-IDENTITY answer, kept separate from\nlanguages_align's stemmer-compatibility question (left untouched).\nConfidence-gated at MIN_DETECTION_CONFIDENCE = 0.9 (whatlang's own\nInfo::is_reliable threshold), calibrated against this crate's own fixtures:\nevery real résumé/job-ad/drifted-section reads at confidence 1.0; the two\ndocumented false-positive shapes (a keyword-soup job ad, a short\ncertifications block) read at 0.08 and 0.13. detect_locale_tag now shares\nlocale_tag_of's 19-language table with detected_language but stays\nUNCONDITIONAL on confidence (it picks a stemmer, not an identity) --\nverified behavior-identical for both its callers in documents/evidence/mod.rs.\n\nvalidate/content/language.rs: is_language_mismatch now asks detected_language\ninstead of languages_align. source_is_a_reliable_control is replaced by\ntarget_is_corroborated(job_ad, source_resume, lang) -- the target is credible\nwhen EITHER witness confidently agrees, not just the source, so a translation\nrun's own translation can finally be graded. The per-section gate no longer\nrequires the whole document to confidently read as the target (that would\nhave switched itself off on exactly the drifted-section shape the pass exists\nto catch -- one bad section lowers whole-document confidence below the\n0.9 bar); it now only requires the document not to confidently read as WRONG.\ndocument_language_mismatch is the one new public entry point both validate\nand the pipeline's draft-retry (a later step of the same fix) are meant to\nshare, so the two can never disagree about what \"wrong language\" means.\n\nRegressions added: the six hand-verified regimes from the fix plan (the\nreported bug fires; a correct translation stays quiet; a same-language\nJapanese run no longer false-fires; an uncurated target like Polish stays\nquiet; neither witness corroborating stays quiet; the existing\nsource-reads-the-same-way test stays green with its premise restated), a\n19-language table-driven sweep, and a mutation check per guard. Three\nexisting per-section tests had languages_align-based premises restated for\ndetected_language; two fixtures were swapped where the old premise would\notherwise have gone vacuous (documented inline), and a certifications-block\nfixture that never exercised the language check at all (silently caught by\nlooks_like_prose first) now does.\n\ncargo test --all-features --locked: 4143 passed, 0 failed, 26 ignored.\n\n* fix(pipeline): order a translation in the resume draft prompt\n\ndraft_system never told the model to TRANSLATE the source résumé, and\ninterpolated the bare 2-char language code (\"in de\") instead of a name a\nmodel reads as an instruction. Add a closed language-name table\n(system_language_name), a translate rule as the first Structure bullet\n(job titles stay untranslated, a stated decision re: consistency.title_drift),\nand pin repair_system's sibling-context clause to the output language rather\nthan whatever the untouched siblings still read as. Adds\ndraft_language_retry_note for the upcoming one-shot draft retry in\nstages/draft.rs (out of scope here; the resulting dead-code clippy warning\nis the expected interim state until that track lands).\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): stop routing a document-wide language critical into repair\n\ncriticals_by_section grouped a document-wide content.language_mismatch\nCritical (section: None) by falling through to sections::containing's raw\nsubstring search over its evidence, which used to be the bare target-language\ncode (e.g. \"de\"). That code matches inside ordinary words (der, Kunden,\ndeveloper, engineer, management), so the Critical mis-routed to whatever\nsection happened to contain it, and repair spent up to max_repair_attempts\nprovider calls regenerating that section against \"offending text: de\" -- a\nrewrite that can never clear a document-wide Critical. The run still ended\nMaxRepairs -> needsReview, just after burning the whole repair budget first.\n\nSkip the document-wide language Critical explicitly, before either lookup\nruns, and leave per-section language Criticals (which carry a section label)\nrouting exactly as before. The draft-stage retry is this Critical's actual\nremedy. Also corrects criticals_by_section's own doc comment, which claimed\nthis case was already \"deliberately excluded\" when it was in fact mis-routed.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): retry the draft once when it comes back in the wrong language\n\nDraft::run now checks the streamed draft with document_language_mismatch --\nthe same check validate uses, wired through the guards fixed in d32f755c --\nand, on a confident disagreement with the run's own deadline not yet passed,\nre-issues the same request once with draft_language_retry_note appended to\nthe SYSTEM slot (Rust-owned; ADR-010 holds). The retry is kept only when it\nactually cleared the mismatch; two wrong-language drafts are equally wrong,\nso a still-wrong retry keeps the first, canonical-prompt draft -- the same\nnever-hand-back-a-worse-document floor repair::round_is_worse holds. A failed\nretry call (provider error or the day's charge ceiling) is caught, not\npropagated -- Completer::stream_captured already charges internally, and the\nrun already has a usable, if wrong-language, first draft, so a retry that\ncannot be afforded or cannot complete must not fail the whole stage over a\nbest-effort correction.\n\nThe decision is pulled into draft_with_language_retry, the same\nprovider-call-injected seam repair::repair_loop and humanize::humanize_one\nalready use, since this crate has no Tauri harness to build a live Completer\nfrom. The bound is structural, not a flag: a single straight-line `if`, with\nnothing surviving past one Draft::run invocation to reset.\n\nConfirmed the failure path this feeds: a still-wrong-after-retry draft raises\nCONTENT_LANGUAGE_MISMATCH with no section (routed nowhere as of the prior\ncommit), content.language_mismatch is not in FABRICATION_CODES so it can\nnever be \"decided\" via the per-bullet review, slot_has_unresolvable_critical\nstays true, and the run parks at needsReview -- no new StoppedReason needed.\n\nAlso verified: Budget::RESUME_QUALITY.max_steps counts pipeline STAGES and is\nunenforced by Pipeline::run_hooked, so the retry's extra provider call inside\none stage has no effect on it.\n\ncargo test --all-features --locked: 4152 passed (baseline 4148 + 4 new), 18\narchitecture, 3 eval, all green. cargo clippy --all-features --all-targets is\nclean except the pre-existing validate/content/test.rs manual_repeat_n\nwarning (out of scope here) -- verified it is NOT actually present on main;\nit was introduced by d32f755c, already landed on this branch before this work\nstarted.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* style(validate): use repeat_n in the language corpus builder\n\nCI runs clippy with -D warnings, and the manual repeat().take() introduced by the language-guard commit would have failed it.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: document cross-language resume generation fix and language concepts\n\n- Amend ADR-032 to specify the corrected language-identity enforcement:\n  confidence-gated detection, corroboration via job ad or source, one-shot\n  draft retry, and explicit confirmation that Criticals do not gate export\n- Add glossary entries to CONTEXT.md for target language, source résumé\n  language, job-ad language, market, locale (overloaded), and UI language\n- Clarify matching-algorithm.md: language detection in scoring uses two\n  independent detectors (franc renderer, whatlang Rust); disagreement results\n  in quiet guard per module posture; languages_align is separate from\n  detected_language (stemmer vs. identity)\n- Update ADR-032 Related section to list language.rs as new validate/content\n  submodule for R8 line-cap compliance\n- Run graphify update to sync code graph with documentation\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: describe the draft retry bound as it is actually implemented\n\nThe amendment called it a loop-local counter; draft_with_language_retry is two straight-line calls with no loop and no flag.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(keywords): delegate language-reliability check to whatlang's own boundary\n\ndetected_language compared confidence() < MIN_DETECTION_CONFIDENCE by hand,\nwhich wrongly accepted a confidence of exactly 0.9 as reliable; whatlang's\nown Info::is_reliable() is strictly greater than 0.9. Delegating to\nis_reliable() makes the two definitions agree by construction instead of by\na coincidental second number. Mutation-checked: raising the const above 0.9\nturns the new boundary test red; reverting turns it green.\n\nAlso in validate/content/test.rs (test-only):\n- Relabel a_long_skills_list_never_trips_the_per_section_language_check,\n  which was triply vacuous (Skills-kind, looks_like_prose, AND the\n  confidence gate all independently suppress the fixture, and its premise\n  asserted against the wrong string) as a no-false-positive regression pin,\n  anchored to the actual validated section body.\n- Add a regression test pinning the DACH-miss accepted cost: an\n  English-language ad plus an untranslated English source against a German\n  target stays silent on both the document and section passes, because\n  neither witness corroborates \"de\".\n\nDoes NOT change target_is_corroborated or document_language_mismatch. The\nreported HIGH (source résumé used as a false corroborating witness) was\ninvestigated: a same-language-as-source equality predicate was measured to\nstill produce a false Critical when a genuine \"before tailoring\" résumé\nindependently misdetects as the same wrong language as the accused document\n(reproduced: two distinct texts in a terse finance register both read\nwhatlang-French at confidence 1.0000). Deferred pending a detector-independent\ndiscriminator instead of shipping a fix already shown insufficient.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: correct the language glossary and the two-detector claims\n\nReview found three factual errors I introduced.\n\nThe 0.9 bar was attributed to the target-commitment path. It belongs to\nwhatlang's post-hoc read of the GENERATED output; the renderer picks the target\nwith franc, which reports no confidence score at all and only gates on length.\n\nresumeLanguage was said to persist as DocumentRecord.locale. It is\nai_generations.resume_language, a different column, and extractMetadata writes\nit unconditionally - so it carries no confidence guarantee, which is precisely\nwhy it must never be consulted as a target.\n\nADR-032 said the bar is 0.9 or greater. The code delegates to whatlang's own\nInfo::is_reliable(), which is strictly greater than 0.9.\n\nmatching-algorithm.md framed detector disagreement as uniformly fail-quiet.\nOnly corroboration fails quiet; reading the generated document fails LOUD, and\nthat asymmetry is the known open limit rather than a graceful degradation.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(validate): require target-language corroboration for section drift\n\nsection_language_issues gated only on the document not confidently reading\nas the wrong language, dropping the positive-corroboration requirement the\nold source_is_a_reliable_control gate provided and never replacing it with\ntarget_is_corroborated (already used three lines away for the document-level\ncheck). Without it, a section could be flagged against a target_language\nneither the job ad nor the source résumé ever vouched for -- reproduced with\nan unreliable job ad + a genuinely French source + target \"en\", which fired\na Critical on an EXPERIENCE section that was never wrong. Since criticals_by_section\nroutes section-scoped Criticals into the repair loop, this burned a real\nregeneration attempt and parked an otherwise-clean run at needsReview.\n\nRestore the corroboration requirement alongside the existing\nis_language_mismatch guard (both required, neither alone sufficient) and add\na_drifted_section_stays_quiet_when_the_target_language_is_not_corroborated as\na permanent regression, mutation-checked red/green. Also corrects the\na_confidently_english_ad_does_not_corroborate_a_german_target_the_dach_miss\ndoc comment, which overstated section_language_issues's old short-circuit as\ngeneral when it only held for a document that reads uniformly wrong, not the\nambiguous/partially-drifted shape a real section-level defect produces.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: point the market glossary at symbols that exist and align the adr\n\nTwo more review findings on docs this branch added.\n\nThe Market entry's \"Contracts:\" list named `Pipeline::market` and\n`export::commands::market_of_job`. Neither exists: `Pipeline<C>` has exactly\ntwo fields, `name` and `stages`, and `market_of_job` appears nowhere in the\nrepo except that line. A pointer that points nowhere is worse than a copied\nliteral. The real owners are `QualityInput::market`, `crate::locale::{letter,\nresume}` and `resolveMarket` on the renderer side.\n\nADR-032 still claimed detector disagreement always fails quiet - the exact\nclaim the previous commit corrected in matching-algorithm.md, leaving two docs\non one branch contradicting each other about the PR's own disclosed known\nissue. Corroboration fails quiet; reading the generated document fails loud,\nbecause whatlang's confidence is a top-1-vs-top-2 margin and not a probability.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): stop the guessed language leaking into the saved aggregate\n\nReview found the never-remember-a-guess invariant held only on session.start.\nTwo sibling save paths in the same flow persisted it anyway, re-creating the\nEnglish lock one door over.\n\nuseTailorPipeline seeds meta.resumeLanguage and meta.jobAdLanguage from\ntargetLanguage when the record's own fields are empty, so an unconfident tier-4\n'en' propagates into all three. useApplicationAnswers and use-interview-questions\nthen pass those straight to aiGenerations.save, and Rust's pick() keeps any\nnon-empty incoming value. Both hooks invalidate the aggregate, the parent\nre-passes it as latestGeneration, and the next run's tier 1 reads the guess back\nas confident. Permanently. Reachable for any ad in a language LANGUAGE_MAP does\nnot carry, not only for short text.\n\nFixed at the save sites rather than by blanking meta itself. meta.targetLanguage\nhas many legitimate current-run readers - RewritePopover's rewriteLocale,\nPdfPreview, the questions modal's locale - and they guard with `?? 'en'`, which\ndoes not catch an empty string, so blanking at the source would have sent an\nempty locale into all of them. The hooks now send '' for all three language\nfields when the resolution was not confident, matching the pattern session.start\nand use-quality-recheck's persistReport already use. Blank rather than omitted,\nso pick() keeps a real stored value instead of destroying it.\n\nuse-interview-questions' own picked language is unaffected: it reads\nmeta.targetLanguage, which still carries the guess for the current run.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): stop the draft retry streaming a second document into the pane\n\nTwo review passes independently found that the language retry rendered the\nresume twice in the live pane.\n\nThe retry streamed under the same job_id as the first attempt.\nusePipelineDraftStream filters only on jobId, appendDraft is a pure append, and\nsetDraft('') runs only in start() and reset() - and no pipeline:stage event\nfires between the two attempts because the retry lives inside the draft stage.\nSo the user watched two contact headers and two of every section, the first half\nin the wrong language, for the whole second generation. Display-only, but on the\nexact path this feature adds and in the most-watched pane of the run.\n\nThe retry now uses a non-streaming completion, so it never reaches ai:stream at\nall. Completer::complete does not charge the daily ceiling on its own, so the\ncharge is made explicitly first - the same pairing repair.rs and humanize.rs\nalready use for every other non-streaming call in this pipeline. A mutation that\ndrops the charge goes red.\n\nThe provider call is behind a DraftEnv seam, mirroring autopilot_helpers::NoteEnv,\nbecause this crate has no harness that can build a live Completer for a unit\ntest. draft_with_language_retry's own signature and tests are untouched: it stays\nopaque to how an attempt is completed.\n\nAlso reworded a mutation-check comment in documents/keywords.rs that still named\nthe confidence comparison an earlier commit replaced with is_reliable().\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: keep the language docs thin pointers instead of copied literals\n\nReview flagged two restatements on this branch.\n\nCONTEXT.md copied the whatlang reliability bar as a literal while the ADR said\nthe decision delegates to the library. Two docs, two mechanisms, and the copied\nnumber is exactly what lets them drift. Only the owning symbol states it now.\n\nmatching-algorithm.md asserted an internal property of whatlang's confidence\nscore that no symbol in this repo owns, so nothing could keep it in sync. The\nobservable consequence is kept and the mechanism points at detected_language.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): persist the target language as an iso code, not a display name\n\nReview caught that my own fix corrected which language the heuristic metadata\ntargets while keeping the wrong shape. detectLanguages returns both jobAd (the\ncode) and jobAdName (the display name); the fallback used the name.\n\nThat matters because targetLanguage is a language TAG everywhere it lands.\nnormalize_language takes the first two alphanumerics, so 'German' becomes 'ge',\nwhich matches no language arm and silently turns the language checks off for\nthat document - the trap #1003's own commit message documents.\n\nTracing the blast radius turned up a second instance nobody had noticed: the F4\nrewrite reads meta.targetLanguage as rewriteLocale, and safeLocale checks it\nagainst two-letter codes only, so 'German' failed the check and clamped to 'en'.\nA German document's rewrite was generated in English. Export and preview feed\nthe same normalize_language path. No consumer renders the field as prose -\nresumeLanguage and jobAdLanguage are the display fields and stay as names.\n\nAlso covers the untested half of languageIsGuess. All three existing tests pass\na non-null meta, so removing `!!meta &&` kept the suite green; the new case pins\nthat a fresh extractMetadata detection IS still persisted. Verified the mutation\nnow fails only that test. use-interview-questions has the same guard and already\nhad a test covering it, so it is unchanged.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): bill only a retry that actually made a round trip\n\nThree review findings, all on guards that were weaker than they looked.\n\ndraft_with_language_retry returned a flat bool that was true on every\nnon-NotNeeded path, including the Err arm - which fires both when charge_daily\nrefuses (zero round trips) and when the round trip itself fails. Draft::run\ngated count_call on that bool, so a refused retry was billed as a call. It now\nreturns LanguageRetryOutcome, whose called() is true only for a completed round\ntrip while attempted() preserves the languageRetry artifact's existing meaning.\nAn enum rather than a struct of bools: the retry is one at-most-once branch, and\ntwo positional bools would reintroduce exactly the swap this finding is about.\n\nThe prompt-naming test looped over de/fr/ja/en but asserted the literal\n\", in de.\", so it could never fail for three of the four. Now parameterized.\nMutation-checking it needed care: a plain assert! in a loop short-circuits, so a\nfull revert only ever proves de catches it first - the loop was isolated to\nfr/ja/en to prove the discriminator fires per language.\n\nThree premises pinned whatlang's verdict on deliberately ambiguous fixtures, so\na library bump would redden the suite for reasons unrelated to this repo. They\nnow state the property actually required - confident, curated, and not the\ntarget - and the detect_locale_tag case derives its expectation instead of\nhardcoding it. Each restated premise is the necessary condition its functional\nmutation check depends on, and every one of those mutations was re-run and still\ngoes red.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-17T16:19:11+02:00",
          "tree_id": "40b31b04b3d66a22d10d5ab2d9a6549d777ea6cc",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/d8b693c6a3ae29adc06c4922444b80fcdfcba8c1"
        },
        "date": 1786977775415,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2275351,
            "range": "± 46721",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2703358,
            "range": "± 45221",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 300232,
            "range": "± 8840",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "1f00aafcc2af49e8eabdae8adb6aaa1f933a3611",
          "message": "refactor(platform): own the test data-dir override where the rule says it belongs (#1006)\n\nThe Stop gate flagged std::env::var in the new SSRF test. The architecture test\ndisagreed and passed, because R4 exempts files it classifies as tests - so the\nrule was enforceable everywhere except the one place a test would actually reach\nfor the variable. The gate was right in spirit: platform/config.rs's own module\ndoc calls it the sole owner of that env var and says no other module may read\nit.\n\nRather than argue the exemption, the override moved to where the rule points.\nplatform::config::DataDirGuard is a test-only RAII scope: a caller names a\ndirectory, never the variable. It restores on Drop instead of at the end of the\ntest body, so a panicking assertion cannot leak the override into whatever runs\nnext - which the previous inline version could.\n\nAlso added #[serial] to data_dir_honors_env_then_falls_back. It mutates the same\nprocess-global variable directly and had no serialization, so it could race the\nnew test. That gap was pre-existing but only became reachable once a second test\nstarted scoping the same variable.\n\nThe test file now contains no env access and does not name the variable in\nprose either, so both the gate's ast-grep matcher and R4's literal search are\nsatisfied for the right reason rather than by an exemption.\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-17T22:36:19+02:00",
          "tree_id": "59d4d561583ac29b9c0e50c77bd11000226c0f01",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/1f00aafcc2af49e8eabdae8adb6aaa1f933a3611"
        },
        "date": 1787000443916,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2180555,
            "range": "± 44610",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2569363,
            "range": "± 24665",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 297200,
            "range": "± 11134",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "2b9d6c238c60921473eba176d39928b09ce6c821",
          "message": "fix(resume): localize every section heading, not the first four (#1007)\n\n* fix(prompts): localize every resume section heading, not the first four\n\nA German resume rendered \"Projekte\" and \"Ausbildung & Sprachen\" as body text\nwhile PROFIL, BERUFSERFAHRUNG and KENNTNISSE rendered as headings.\n\nsection_order_prompt_list handed the model NINE section names as raw English\nSectionId debug strings while ResumeConventions.headers localized only FOUR.\nIts own doc comment admitted it. So the German prompt asked for German prose and\nsupplied English words for Projects, Certifications, Languages, Awards and\nPublications - the model invented German names no vocabulary had heard of.\nEnglish worked by luck, because the model invents \"Projects\" and the parser\nhappens to know that word.\n\nheaders is now a total record over the nine ordered ids, so TypeScript is the\ncompleteness guard: a new ordered section cannot ship without a name in every\nlocale. Removing one from the de locale fails tsc, which is the mutation check.\n\nTwo German candidates were rejected by running classify_section rather than\nreasoning about it. Sprachkenntnisse classifies as Skills, so a regenerate would\nrewrite the wrong section; Weiterbildung classifies as Education and collides\nwith Ausbildung. Sprachen and Zertifikate land in Other and collide with\nnothing. locale/resume.rs's own doc comment recommended Weiterbildung - that\ncomment was wrong and is fixed. Every Italian heading was checked the same way.\n\nPR #1003 reworded the section list to \"an ORDER, not a checklist\" to stop empty\nsections, and in doing so dropped the clause forbidding merged headings. That is\nwhere \"Ausbildung & Sprachen\" came from; the clause is back, without the\nmanifest reading #1003 removed.\n\nItaly silently received the US section order - section_order_for had a DE arm\nand nothing else. IT_ORDER puts education and certifications straight after\nexperience, and diverges from the German order only on Languages before Skills,\nfollowing Europass nesting language competence first. That divergence is marked\nin the doc comment as the reviewable call.\n\nsection_order_prompt_list moved from locale::resume to pipeline::resume::prompts.\nLocalizing it there would have made an L1 module import L2; the architecture\ntest caught it as R7. locale::resume stays a pure order source and the order\nbecomes localized text one layer up.\n\nNote draft_system interpolates its heading slots in the positional order\nsummary, skills, experience, education - not the order the sentence reads. A\ncomment now says so, because reordering them to read naturally silently\nmislabels every heading.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(locale): give italy a profile instead of resolving it to english\n\nItaly was not a known region. LocaleProfile::get and is_known_region covered\nus, uk, gb, de, at, ch, fr, nl, eu and dach, and recommend had no italian arm at\nall, so an italian job auto-resolved to intl() - id \"en\", photo Never, two\npages. A4 was already right so there is no paper bug, but an italian CV may\ncarry a photo, Europass-style CVs run past two pages, and that \"en\" id is what\nfed an italian user the US section order on the auto-recommend path. The\nexplicitly-selected market path already reached \"it\" correctly.\n\nThe profile is a dedicated it() rather than an alias to eu(), even though every\nfield is identical. The id has to stay \"it\": pick_locale forwards it verbatim as\nthe export market string and section_order_for matches the literal \"it\" to\nselect IT_ORDER, so aliasing would fix page and photo while silently handing\nback the default section order - the same bug one hop downstream. That reasoning\nis in the constructor's doc comment, because the fields being identical makes\n\"simplify this to eu()\" an inviting future change.\n\nVerified there is no italian equivalent of the DE/AT/CH-versus-dach split that\nonce gave german users the default order: COUNTRY_TO_MARKET[\"IT\"] and\nLANGUAGE_TO_MARKET[\"it\"] both map to a single \"it\". No mirrored alias test was\nadded, because a test for a split that does not exist would assert nothing.\n\nSpain and Portugal have the identical gap and are deliberately left open - both\nhave curated resume and letter conventions but nobody waiting on them, and\nfolding them in would stop this diff reading as the Italy fix.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(export): recognize the localized headings the prompt now emits\n\nThe producer half localized nine section names across seven locales; the parser\nknew four English project variants and zero non-English ones. Measured before\nthe fix: projekte, projets, proyectos, progetti, projecten and projetos all\nreturned zero occurrences in SECTION_NAMES, while every other german heading was\nalready there - which is exactly why only Projects broke on a german resume.\n\nThe added vocabulary was derived by diffing the generated prompt_blocks header\ntable against SECTION_NAMES rather than transcribed from the design. Portuguese\ncarries both premios and premios spellings: nothing in the header table\ndiscriminates pt-PT from pt-BR, so the recognizer accepts both while the\nproducer emits one.\n\nThe guard that matters is every_producer_heading_is_recognised_by_the_parser -\nall nine ordered ids across all seven locales must parse as a SectionHeader.\nReverting the new entries reproduces the reported defect verbatim: `locale \"de\"\nheading \"Projekte\" (for SectionId::Projects) was not recognised as a\nSectionHeader by export::parser, got Text`.\n\nHonest limit on that guard, worth recording: it works because the producer is\nnow a TOTAL record. Before that commit the prompt used the english Debug name\n\"Projects\" for the five uncovered ids - a string the recognizer already knew -\nso this guard run against the original code would have passed vacuously. The\nhistorical defect was partly the model inventing a german translation of an\nenglish placeholder inside an otherwise-german prompt, which no fixture test\npredicts. The guard is the right shape for the producer-recognizer contract from\nhere on; it is not a claim it would have caught the original.\n\nCombined headings are recognized, not split. is_known_section_name now accepts\n\"X & Y\" when both halves are individually known. Splitting was rejected: there\nis no reliable signal for which half later content belongs to, so a structural\nsplit risks misattributing it - worse than one heading covering both. Real ATS\nbucket by heading text and get no signal at all from a plain paragraph, so\nrecognizing an imperfect heading beats leaving it as prose. Prompt-side\nprevention stays the primary fix; this is for documents already generated.\n\nSectionId::from_header is deliberately NOT localized here. It is a semantic\nclassifier feeding reorder_sections, so a misclassification silently reorders\ncontent - a worse failure than a heading rendering as text, and it needs the\nsame collision analysis the producer did for Sprachkenntnisse and Weiterbildung.\nIts comment promised a \"later phase\" that never came; it now states the gap\nplainly instead.\n\nBlast radius covered by tests: a company named \"Projekte GmbH\", a bullet opening\nwith a section word, prose merely containing one, and a colon-labelled\n\"Sprachen: Deutsch\" skills group all stay non-headings. The whole-line exact\nmatch is what protects them, and it is unchanged. No golden export fixture moved.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* test(resume): make the heading guards enumerate what they claim to enumerate\n\nPre-PR review found three guards on this branch that could not fail for the\nreason they were written, and one live gap they were blind to.\n\nThe producer/recogniser guard's doc says a new SectionId or locale fails\nHERE. It could not: both axes were literal const arrays inside the test, so\nanything added on the TS side was simply never visited. The codegen now\nemits `RESUME_CONVENTION_LOCALES` and `ResumeConventions::ids()`, and the\nguard loops over those. Mutation: added an 8th locale to the TS conventions\nwith headings absent from SECTION_NAMES — red, naming it; it was green\nbefore.\n\n`header()` falls back to the raw English SectionId word rather than\npanicking, and nothing tied the TS id union to the ids `section_order_for`\nemits, so adding `SectionId::Volunteer` to an order const put the literal\nword \"Volunteer\" in a prompt demanding Italian with every gate green. The\nnew guard asserts key MEMBERSHIP, not `header(id) != id` — the value\ncomparison reads as a fallback for French \"Certifications\", which is\ncorrectly spelled the same as English. That is not hypothetical: the\ninequality form failed on fr the first time it ran.\n\nThe collision sweep covered de + it by name and missed three of the four\nGerman words this branch adds. Replacing the literal list with a locale x id\nloop surfaced a live pre-existing defect: Portuguese \"Competências\"\nclassified as Other, so a pt résumé generated by this app did not recognise\nits own skills section. The `ê` blocks `competenc` exactly as `z` blocked it\nfor Italian. First draft of the sweep allowed `want || Other` and could not\nfail; every bucketed id already lands on its own bucket in all 7 locales, so\nthe slack bought nothing and cost the guard.\n\nAlso: `isKnownSectionName` never gained the `\" & \"` join arm Rust has, so\nevery merged heading answered false on the TS side with the section-names\nfixture still green — that fixture compares two name LISTS and is\nstructurally blind to a rule combining two entries. Mirrored, with a shared\n`section-name-joins.json` pinning the predicate on both sides, including the\nthree-part cases that pin `split_once`'s cut-at-the-first-separator\nsemantics (\"certifications & training\" contains a separator of its own).\n\nEvery guard above was mutation-checked: green at baseline, red with the\nfeature deleted, restored.\n\nTwo stale doc comments fixed: the Italy paragraph a later commit in this\nsame PR contradicted, and a JSDoc pointing at the pre-move location of\n`section_order_prompt_list`.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-18T01:04:39+02:00",
          "tree_id": "fd58839a6261a56d719299de47ce25d961c9bc77",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/2b9d6c238c60921473eba176d39928b09ce6c821"
        },
        "date": 1787008628995,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2254566,
            "range": "± 28946",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2659228,
            "range": "± 64024",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 289217,
            "range": "± 3427",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "8733d4ca3662687fc2e614ba48ffd76afdeac76c",
          "message": "feat(scraping): add freehire as the aggregator's keyless tier (#1008)\n\n* feat(scraping): add freehire as the aggregator's keyless tier\n\nIssue #1002, opened by freehire's maintainer, who disclosed the interest. We\npublicly committed to shipping it in the next release.\n\nImplemented from their published openapi.yaml rather than from the PR they\noffered. The reason is rights, not code quality: this repo has no CLA, so a\ncontribution stays under its author's copyright and cannot be relicensed, and\nADR-023 names commercial licensing as a rationale. Reading their PR and\nrewriting it would be derivative — the worst of both worlds — so the spec is\nthe only source used.\n\nWhat it buys: every other provider needs an API key, so a fresh install gets a\nneeds-keys skip and zero jobs. freehire answers unauthenticated, so the\naggregator board can return results before the user has configured anything.\nIt is not a better source than the keyed tiers and is never preferred over\none — it runs LAST, after Adzuna, JSearch and Jooble have each failed to\nproduce a decisive result, and only a non-empty answer from it short-circuits.\nAn empty answer from a provider nobody chose is not evidence about the search,\nso it must not swallow a configured provider's failure diagnostic.\n\nTwo consequences worth calling out rather than burying:\n\n`needs_keys()` is now always false, because a keyless tier is always\n\"configured\". That is the point — the skip existed because a keyless search\ncould only ever return nothing. The cost, accepted and tested: a keyless user\nwhose freehire search finds nothing reads \"no jobs found\" rather than a prompt\nto add keys. A keyring READ FAULT is still classified separately, so the one\ncase the old `false` protected is unaffected.\n\nFailures degrade to Ok(empty), never Err — the only provider with that rule.\nEvery other one reports its failure because a stored key means the user asked\nfor it specifically. Nobody opted into this one, so surfacing its outage would\nturn a third party's downtime into an error banner on a search the user never\naimed at them. There is no SLA and no rate-limit header of any kind (verified:\nno X-RateLimit-*, no Retry-After), so it is treated as always-possibly-absent.\n\nUses the documented /agent/jobs/search with `q` and\n`description_format=markdown` — NOT the undocumented /jobs/search taking\n`query`, which is what the issue described and what a first pass reached for;\nbuilding from the published spec is the whole basis for implementing this\nourselves. Markdown up front is also what keeps scoring from needing a\nper-result detail fetch.\n\nA guessed country is deliberately not sent as a filter: the aggregator defaults\n`country` to \"de\" when the caller supplied none, and pinning a keyless tier to\nthat guess would reproduce the guessed-market bug already fixed for Adzuna.\n`location` is unused because the documented search has no city parameter at\nall, and folding a city into the full-text `q` would drop every posting that\ndoes not spell its city out, remote ones included.\n\nEgress: freehire.me is in the inventory with a public_name, which made the\nlanding drift guard fail until the privacy page, README and SECURITY named it.\nThat guard shipped two PRs ago for exactly this, and this is the first new\negress since — it worked.\n\nEvery behaviour above is pinned by a test that was mutation-checked: green at\nbaseline, red with the feature deleted, restored. The one claim that did not\nsurvive checking is recorded in the test rather than quietly dropped — the\nordering guard catches \"moved ahead of the keyed tiers\" but would NOT catch\n\"moved one rung up past Jooble\", because the fixture short-circuits at Adzuna.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(scraping): stop the keyless tier from masking what it must not mask\n\nPre-PR review reproduced two regressions of existing guards. Both are mine and\nboth came from the same mistake: treating an always-on provider as if it were\njust one more tier, when \"always on\" is exactly what makes it able to hide\nthings.\n\n**It replaced the sparse guessed-market hits instead of merging with them.**\nReproduced in the commonest real configuration — Adzuna-only keys, no country\nsupplied, a real location: two London-relevant Adzuna hits were discarded for\nup to fifty globally-unfiltered freehire ones. That is the guessed-market bug\nthis repo already fixed for Adzuna, re-entered through a new door. It now\nmerges, sparse hits first so dedupe's first-seen-wins order preserves them.\n\nThe comment justifying the original behaviour claimed \"the free-text location\nstill reaches `q`, so a real place name is not lost\". That was false, and I\nmade it false myself: the sentence was written before I removed `location`\nfrom the function on discovering the API has no city parameter at all. On a\nguessed market this tier is location-blind, which is precisely why it must not\nreplace hits that are not.\n\n**It swallowed every configured provider's failure diagnostic.** Reproduced:\nthree revoked keys, and `BoardScrapeSummary.error` came back `None`. Since\n`needs_keys()` is now permanently false, no surface anywhere in the app would\nhave told the user their key was broken — indefinitely. It is now skipped\nwhenever a configured provider actually failed.\n\nThat guard was wrong on its first attempt too, and the test caught it: the\nguessed-market branch records its fall-through in `adzuna_configured_failed`,\nso a naive `is_some()` check read a routing decision as a key failure and\nswitched the tier off in the one case it is most useful. Split into a distinct\n`adzuna_distrusted_a_guessed_market` flag.\n\nAlso from the review:\n\n- The privacy page said freehire runs after every keyed tier \"has come back\n  empty or failed\". A keyed tier's `Ok(empty)` is decisive and returns\n  immediately, so that was a false statement on a public disclosure page. Now\n  \"is unconfigured or has failed\". The drift guard could not catch this — it\n  checks that a named host appears, not that the claim around it is true.\n- `freehire_slugless_rows_key_off_their_url_not_each_other` asserted only\n  pairwise inequality and stayed GREEN when the reviewer mutated the fallback\n  id to a non-deterministic counter — a change that would break cross-run\n  dedupe and resurface every slugless posting as new on each re-scrape. It now\n  asserts the concrete value. This is the repo's recorded pass-for-the-wrong-\n  reason shape and I reproduced it in my own new test.\n- `description` now goes through `html_to_markdown` like every sibling\n  provider. The \"already markdown, no conversion needed\" rationale was wrong\n  in both directions: the call is free (it early-returns tag-free input\n  verbatim) and it is the only defence when freehire's upstreams leave HTML in\n  a field labelled markdown.\n- Stale contract docs the change invalidated: the aggregator module header\n  still described a three-provider chain and claimed \"none configured →\n  keyless-empty\"; `Scraper::needs_keys` and the `boards.ts` skip contract still\n  described needs-keys as live; `scraping-domain.md` omitted the tier entirely.\n\nThree new chain tests, all mutation-checked green-at-baseline / red-with-the-\nfeature-deleted, including one asserting the tier still answers when nothing\nfailed — without it, tightening the guard to \"never run\" would leave both\nfailure-direction tests green.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(scraping): dedupe the cross-provider merge by url, and correct two docs\n\nAll three from CodeRabbit on #1008; all three verified against the code before\nacting rather than applied on sight.\n\n**Dedupe by URL after the merge.** `dedupe` keys on `external_id`, which is\nprovider-prefixed, so the same job surfaced by both the sparse Adzuna tier and\nfreehire was two distinct keys and rendered twice. `dedupe_by_url` already\nexists for exactly this and is already used on the additive Apify merge — the\nfreehire merge is the only OTHER cross-provider merge in `primary_chain`, so it\nneeded the same second pass. The single-provider path keeps `dedupe` alone,\nsince `canonical_url` collapsing is not wanted there. New test, mutation-checked.\n\n**The privacy page was still wrong, in a second way.** The previous commit fixed\n\"empty or failed\" → \"unconfigured or has failed\", but the same commit had also\nmade freehire SKIP on a configured provider's failure, so \"has failed\" was now\nfalse too. It now says freehire runs only when none of the keyed tiers failed,\nand says why: an erroring key shows you the error rather than being quietly\nanswered around. Two passes to get one sentence true is the cost of changing\nbehaviour and copy in the same change.\n\n**`scraping-domain.md` is a shape-only page** — it says so in its own header —\nand the freehire section had grown into a copy of the runtime contract, which is\nhow docs go stale. Reduced to pointers at the module doc, `primary_chain` and\nADR-0005, keeping only the one consequence that is invisible from those files:\n`needs_keys()` never returns true in production any more.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-18T02:56:38+02:00",
          "tree_id": "66d439807e80e5ae249bb2c721281726141e7559",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/8733d4ca3662687fc2e614ba48ffd76afdeac76c"
        },
        "date": 1787016116817,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2332721,
            "range": "± 73686",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2827840,
            "range": "± 101533",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 251862,
            "range": "± 3469",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "82772ddf250cfafa9b5a3b94b675a3c73f52d4c6",
          "message": "fix(locale): give Spain, Portugal and Brazil the profiles they were already promised (#1009)\n\n* fix(locale): give spain, portugal and brazil the profiles they were already promised\n\n#1007 fixed Italy and explicitly deferred the identical gap for Spain and\nPortugal. This closes it, and Brazil with it.\n\nAll three were already live market ids on the TS side — `COUNTRY_TO_MARKET`\nmaps `ES`/`PT`/`BR` and `LANGUAGE_TO_MARKET` maps `es`/`pt` — and the Rust side\nhad an arm for none of them, so they fell through to the international default.\nThe visible result was half a localization: `resume_conventions` HAS curated\nes/pt entries, so the model wrote \"Experiencia profesional\" and \"Formação\" and\nthen arranged them in the American order, with `photo: Never` and the 2-page\nAnglophone budget. Half-localized reads worse than not localized, because the\ndocument then matches no market's expectations.\n\n`IT_ORDER` is renamed `EUROPASS_ORDER` and now serves all four. That is what it\nalways was — the doc already justified it by pointing at Europass, not at\nanything specifically Italian.\n\nScope is principled, not arbitrary: these are exactly the markets whose section\nHEADINGS are already curated. A market with no curated headings (tr, ru, cn, jp,\nkr are all live TS ids too) gets English headings, and giving it a non-English\nsection ORDER would create the same half-localized document this commit is\nfixing. The order axis tracks the heading axis deliberately.\n\nTwo per-market calls, both flagged as reviewable rather than asserted:\n\n- Spain and Portugal cap at **2 pages**, not Italy's 3. Spanish guidance is\n  consistently \"máximo dos páginas\", and this cap is advice the trim panel acts\n  on, so inheriting 3 would license a longer document than the market expects.\n- Brazil gets `photo: Never` where Portugal gets `Optional` — Brazilian hiring\n  guidance has moved against CV photos on anti-discrimination grounds. Same\n  language and same section order, different norm; that is precisely why it is\n  a distinct profile rather than an alias.\n\n`all_markets_are_distinct_and_present` asserted a hardcoded count and id list,\nso it could not have caught any of this — it passed throughout. It now asserts\nthe property instead: every profile `all()` advertises must round-trip through\n`get()` back to itself. Mutation-checked by deleting the `\"es\"` arm, which is\nthe exact shape of the bug that shipped; the old test stayed green under it.\n\nEvery guard here mutation-checked: green at baseline, red with the feature\ndeleted, restored.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(locale): reattach italy's doc to italy, and stop claiming the page cap is enforced\n\nCodeRabbit flagged a contradiction on #1009: the Spain profile said the page cap\nis \"advice the export actually enforces\", while `max_pages`'s own field doc says\nit is advisory and that nothing blocks a longer export. The field doc is right,\nand it predates this branch.\n\nChasing it found the actual cause, which the finding described only as a\nsymptom. The new constructors were inserted at the wrong offset — between\nItaly's doc comment and `pub fn it()`. Rust attaches a doc comment to whatever\nitem follows it, so **Italy's documentation had silently become Spain's**, and\n`it()` was left with none. That is why two page-limit paragraphs ended up\nadjacent and disagreeing: they were never meant to be read together.\n\nMoved the three constructors below `it()`, restoring the original attachment,\nand reworded the Spain note to state what actually happens: inheriting Italy's\n3 would not break an export, it would stop the trim panel ever offering a\nsuggestion to a Spanish user whose CV has run a page long.\n\nAlso finishes the `IT_ORDER` → `EUROPASS_ORDER` rename — three doc references\nin `locale/mod.rs` and `pipeline/resume/test.rs` still named the old const.\nNothing failed, because a doc comment naming a symbol that no longer exists\ncompiles perfectly well; that is precisely the class this repo treats as a\ndefect rather than a nit.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-18T03:55:42+02:00",
          "tree_id": "a7935ef0c84bbdf28296cdb06c69f024cee74601",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/82772ddf250cfafa9b5a3b94b675a3c73f52d4c6"
        },
        "date": 1787018875162,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2263944,
            "range": "± 30499",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2642606,
            "range": "± 88145",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 293193,
            "range": "± 1932",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "9f0f1de1f0705438e61ad0270e73b7113fdbf992",
          "message": "ci: enforce the Rust toolchain pin instead of asking for it in a comment (#1010)\n\n* ci: enforce the rust toolchain pin instead of asking for it in a comment\n\nThe pin itself already shipped and is correct: `rust-toolchain.toml` pins\n1.97.1, and both `dtolnay/rust-toolchain` uses are pinned to the SHA for that\nsame version. What was missing is any enforcement.\n\nThe two halves cannot reference each other — that action does not read toolchain\nfiles, its `@rev` IS the version selector — so the only thing tying them\ntogether was a comment in three files saying \"bump both together\". That is the\nshape this repo treats as a defect: a stated invariant with nothing checking it.\n\nThe failure is quiet, which is what makes it worth a check. Bump the TOML alone\nand CI keeps building on the old compiler; bump the action alone and every\ndeveloper's machine drifts off what CI validates. Neither goes red on its own —\nthe pin simply stops meaning anything, which is exactly the state it was created\nto end.\n\n`scripts/check-toolchain-pin.mjs` asserts four things: the channel is an exact\nthree-part version rather than a floating one; every `dtolnay/rust-toolchain`\nuse under `.github/` carries a trailing `# <version>` comment (an opaque SHA is\nunreviewable by construction, so the comment is load-bearing, not decoration);\nevery one of those declared versions equals the channel; and the Cargo MSRV is\nnot above the pin, since that pairing cannot compile at all.\n\nIt deliberately does NOT verify that the SHA truly is that version — that needs\na network call, and it is not the mistake people make. The realistic error is\nupdating one place and forgetting the other, which is fully catchable offline.\n\nWired into the `lint-format` job, which has no path filter and so runs on every\nPR — a toolchain drift guard that only fires when Rust files change would miss\nthe case where someone edits only the workflow. The three comments now name\ntheir enforcer instead of asking nicely.\n\nGuard is mutation-tested five ways against the real repo (bump either half\nalone, float the channel back to `stable`, strip a version comment, raise the\nMSRV past the pin) — all red, tree restored clean — plus 11 black-box CLI tests\nover a fake repo tree, following `check-tech-radar.test.mjs`. One of those\ncovers removing CI's pin entirely, which would otherwise pass vacuously: with\nnothing to compare, nothing disagrees.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(ci): reject a moving action ref, not just a mismatched version comment\n\nCodeRabbit on #1010, and it is the same hole as the one this guard was written\nto close.\n\nThe check validated the `# <version>` comment beside each\n`dtolnay/rust-toolchain@<rev>` and never the rev itself, so\n`uses: dtolnay/rust-toolchain@stable # 1.97.1` passed. A moving ref defeats the\npin exactly the way a `stable` channel does — and arguably worse, because the\ncomment beside it is then a claim nothing keeps true: the action can re-point at\na different compiler with no diff in this repo at all. I checked the channel for\nfloating and forgot the other half.\n\nThe test helper defaulted every rev to `deadbeef` — eight characters — and all\n11 tests passed, which is the same blind spot expressed twice. It now uses a\nreal 40-hex SHA, and there are regressions for `@stable`, `@main`, `@v1`,\n`@master` and a truncated SHA, each with a MATCHING version comment so they fail\non the rev rather than incidentally on the comment.\n\nVerified both directions: pointing the real repo's action at `@stable` goes red,\nand removing the SHA check turns two tests red.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-18T04:55:37+02:00",
          "tree_id": "99a9b8aae0f4a43cabcb39fb97d2e0b86ca98056",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/9f0f1de1f0705438e61ad0270e73b7113fdbf992"
        },
        "date": 1787022559052,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2240861,
            "range": "± 45236",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2731066,
            "range": "± 100256",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 306602,
            "range": "± 19146",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "21ba80e3feafb91f34c1e85e8bf9f031d2431321",
          "message": "fix(evidence): stop filing academic records as work experience (#1011)\n\n* fix(evidence): stop filing german academic records as work experience\n\nFound by sweeping every term in the renderer's `SECTION_LEXICON` through\n`classify_section` — 197 terms, which nothing had ever compared. The two sides\nanswer the same question from different data: the TS lexicon drives\n`detectSections`, this classifier drives `extract_evidence`.\n\n**The bug.** German \"Akademischer Werdegang\" — a standard heading for an\nacademic record — contains `werdegang`, which is an unconditional entry in\n`EXPERIENCE_HEADINGS`, and that test runs first. So it classified as\nExperience. Not a cosmetic mislabel: prose under an Experience heading reaches\n`extract_evidence`'s role arm, so degree entries became work bullets under\nroles the candidate never held. Same failure the module already documents for\n\"Kenntnisse und Erfahrungen\", one list away.\n\nFixed with a closed list of three exact phrases checked BEFORE the experience\ntest, not by generalising. The general fix — letting\n`AMBIGUOUS_EXPERIENCE_HEADINGS` yield to education as it yields to summary and\nskills — would overturn a documented decision: Education and Projects are\nexcluded from that yield set on purpose, because \"Project Experience\" really is\na work history in a consultant's CV. These three phrases have no second\nreading, so they need no rule. A control asserts \"Beruflicher Werdegang\" and\nbare \"Werdegang\" are still Experience, so the exception did not swallow the\nrule.\n\n**The wider gap.** The same sweep found 44 lexicon terms classifying as `Other`\n— coverage gaps rather than misfiles, since the section checks just don't run.\nClosed the ones for the two markets the owner prioritised and that carry no\nambiguity: `praxiserfahrung`, `studium` (de), `istruzione`, `esperienze` (it).\n`istruzione` is the very heading `resume_conventions(\"it\")` teaches the model\nto write, so the producer and the recogniser disagreed about Italian education.\n\nThe remaining 40 are enumerated in `KNOWN_MISSES` rather than tolerated in\nbulk. The list is asserted in BOTH directions: a new lexicon term that nothing\nrecognises fails, and fixing one without removing it from the list fails too —\nso it can only shrink deliberately. That turns a silent gap into a reviewed\ninventory. They are mostly Summary/Skills synonyms in fr/es/it/nl/pt, and they\nare not free to close: these consts are substring tests, and `expertise`,\n`portfolio` and `studi` have second readings (Italian \"studio\" contains\n`studi`), so they need word-bounded handling rather than a bare push.\n\n`Languages` terms landing on `Skills` are allowlisted explicitly with the\nreason — `SectionKind` has no `Languages` variant, and \"Sprachkenntnisse\" was\nrejected as a German producer heading for exactly this — rather than being\nswept up by the wrong-bucket rule.\n\nThe fixture is a flattened copy of `SECTION_LEXICON` because Rust cannot parse\nthe TS source; a vitest guard asserts the copy still equals the live lexicon,\nwhich is what stops the Rust sweep from checking yesterday's vocabulary and\nreporting green.\n\nFour mutations run, all red at baseline-green: dropping the precedence branch,\nremoving a newly-taught term, shrinking KNOWN_MISSES without fixing anything,\nand fixing a miss while leaving it listed.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(evidence): stop the italian fix from re-creating the bug it was fixing\n\nPre-PR review BLOCKED the first draft, and the finding is the sharpest kind:\nthe same commit that fixed \"Akademischer Werdegang\" re-introduced that exact\nbug one language over.\n\nPutting the bare stem `esperienze` in `AMBIGUOUS_EXPERIENCE_HEADINGS` looked\nlike a free plural of `esperienza`. It is not: that set yields to Summary and\nSkills but never to Education, and the experience arm runs first. So\n\"Esperienze di formazione\" — an Italian EDUCATION heading — went Education →\nExperience. Priced end to end, a degree entry under it became a fabricated job:\nthe candidate \"held\" the title `Laurea Magistrale in Informatica` at\n`Politecnico di Milano`. It also defeated this change's own new `istruzione`\nentry on \"Istruzione ed esperienze\".\n\nOnly the WORK-QUALIFIED plurals are taught, in the unconditional list beside\ntheir singular: `esperienze professionali`, `esperienze lavorative`. That also\nkeeps them out of the section-DELETING hole the ambiguous list's own doc warns\nabout — \"Esperienze professionali e competenze\" now reaches Experience, where\nthe bare stem lost it to Skills. The bare word returns to `KNOWN_MISSES` as an\nhonest miss.\n\nThe rest, all reproduced by the review:\n\n- The exact-phrase override list missed the nearest siblings of the heading it\n  was written for. `Wissenschaftlicher Werdegang` heads the academic CV this\n  feature exists for, and filed a degree as a job. Added it plus\n  `schulischer`/`bildungs`- and the `akademischen`/`wissenschaftlichen`\n  declensions. German compounds and declines `werdegang` freely, so membership\n  is the cost of the closed-list approach — paid here rather than deferred.\n- `istruzione` as a bare substring also matched `distruzione`. Moved to the\n  word-bounded list, which exists in this file for precisely this\n  (`formation` ⊂ `information`). Controls verified: `costruzione`,\n  `ricostruzione`, `Informazioni personali` all still `Other`.\n- **`EXPERIENCE_HEADINGS` had lost its doc comment.** The new const was\n  inserted between that doc and its item, so the whole run attached to the new\n  const and the old one rendered undocumented. Second time this exact slip\n  happened tonight — the Italy locale profile had it too.\n- `KNOWN_MISSES` could be padded with entries naming no lexicon term at all;\n  the review proved it with `\"zzz-not-a-lexicon-term\"`. Third assertion added,\n  covering the Languages allowlist too.\n- `parcours professionnel` is work-qualified French with no second reading, so\n  it is taught rather than listed as a gap.\n\nBoth remaining corrections were forced by the guard itself rather than by me:\nremoving bare `esperienze` made the sweep demand it back on the list, and\nteaching `parcours professionnel` made the sweep demand it be removed from it.\nThat is the bidirectional inventory doing its job on its own author.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* test(evidence): cover every override phrase, both directions\n\nAI Review on #1011: 5 of the 8 `EDUCATION_OVERRIDES_EXPERIENCE` phrases had no\ntest at all — the assertion list still named the original three after the list\ngrew to eight, so a typo in any of the five would have shipped silently. Same\nfor the new `esperienze professionali`/`lavorative` entries, and the doc comment\nstill said \"these three phrases\".\n\nFixing it the obvious way produced a test that passes for the wrong reason, and\na mutation run caught it: driving the loop off the const covers a phrase ADDED\nwithout thought, but it is self-referential — deleting an entry deletes its own\nassertion. Removing `wissenschaftlicher werdegang` left the test GREEN.\n\nSo both halves are now present: a hand-written membership assertion (catches a\ndeletion) and the const-driven loop (catches an addition), the loop also\ncase-varying each phrase to prove the lowercasing rather than assume it.\n\nAdds `italian_experience_plurals_do_not_capture_education_headings`, which pins\nthe HIGH the earlier review reproduced — \"Esperienze di formazione\" must stay\nEducation, the work-qualified plurals must reach Experience, \"Esperienze\nprofessionali e competenze\" must not fall into the section-deleting Skills\nhole, and the bare stem must stay an honest `Other`. Those behaviours were\nverified with a throwaway probe when the fix landed and pinned by nothing.\n\nMutations, all red at baseline-green: dropping a previously-untested override\nphrase, dropping a taught Italian plural, re-introducing the bare `esperienze`\nstem, and reverting `istruzione` to a bare substring.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-18T05:59:42+02:00",
          "tree_id": "c1411353858c4d6a81541db0f20cbc4cdc713d49",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/21ba80e3feafb91f34c1e85e8bf9f031d2431321"
        },
        "date": 1787026336069,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1981210,
            "range": "± 49264",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2370861,
            "range": "± 11251",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 210365,
            "range": "± 1557",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "531a74c868b176603f4282f6803f0ad77282bd14",
          "message": "test(export): enforce adr-002's golden-parity claim for the first time (#1012)\n\nADR-002 says the PDF and DOCX backends are kept \"in golden parity where the\ndesign requires, pinned by deterministic golden snapshot tests\". The\nper-backend tests were real, but nothing rendered ONE document through BOTH and\nchecked the same facts came out. The parity half of the claim was documented,\nnot enforced — the gap the architecture audit filed as Track A3.\n\nNot hypothetical for this codebase: DOCX body bold had never rendered at all,\nand a macOS incident shipped with the two formats disagreeing. An ATS reads the\nextracted text, so a fact that survives one export and not the other means the\ncandidate submits a materially different résumé depending on which button they\npressed — and only the backend that still had the content would look fine in\nits own snapshot.\n\nThe harness renders every canonical template through both backends from the\nsame `ExportRequest`, extracts text from each, and asserts a fixed set of\nfacts (name, section headings, employer, title, a bullet, degree) survived\ninto both.\n\n**Anchored on the SOURCE, not on the other backend.** Comparing PDF text to\nDOCX text is the shape this repo has shipped broken repeatedly — two derived\nvalues with nothing absolute behind them, so a change that drops Education from\nBOTH stays green forever. Each rendering is checked against the input fixture\ninstead, which cannot pass that way.\n\nIt asserts facts rather than the fixture verbatim, because line breaks,\nhyphenation, column order and glyph runs legitimately differ between a Typst\npage and a Word document. What may not differ is whether a fact survived.\n\nIncludes a vacuity guard: an extractor returning almost nothing would make\nevery containment check pass trivially, so both extractions must exceed a\nlength floor before they are trusted.\n\nRuns the whole roster rather than a sample, for the reason the sibling PDF\nmatrix gives — a template nobody rendered is a template nobody validated.\n\nMutations, both red at baseline-green: making the DOCX renderer silently skip\nthe Education section, and making the DOCX extractor return an empty string\n(caught by the vacuity guard, not by the parity assertions).\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-18T06:14:11+02:00",
          "tree_id": "c63c5426bfb2faec581e2f0622ce8384ab64a9fb",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/531a74c868b176603f4282f6803f0ad77282bd14"
        },
        "date": 1787027222485,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1771469,
            "range": "± 26389",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2225746,
            "range": "± 66461",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 271899,
            "range": "± 33582",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "a62bbd25d789145aa50703fc26e88e7324584c6a",
          "message": "fix(scraping): register jobicy in the shared board list and make the drift impossible (#1014)\n\n`SCRAPERS` holds 24 boards; the renderer's `BOARD_IDS` held 23. The missing one\nis `jobicy` — a fully registered scraper with `id() == \"jobicy\"`, `listed()`\ndefaulting to true (so the catalog offers it), and en/de labels already written.\nIt was simply never added to the shared list, and nothing on either side could\nsee that, because nothing compared them.\n\nHarmless today only by accident: the codegen lowers `z.enum` to `Vec<String>`,\nso the id that actually crosses IPC is validated by the registry lookup rather\nthan by the enum. The schema comment claimed the opposite — that each entry\n\"is already constrained to a known BoardId\" server-side — which is why the gap\nread as safe. Corrected in place.\n\n`pnpm gen:ipc` now emits the shared list to `ipc_contracts/board_ids.rs` (same\nshape as the existing DATE_FILTER_OPTIONS / AI_GENERATE_INTENTS emitters), and a\nRust test compares it against `SCRAPERS` in both directions: one catches a board\nadded to the registry and forgotten in TS, the other a board removed from the\nregistry while the TS list still promises it to the `BoardId` type.\n\nMutation-checked: removing `jobicy` from the shared list again fails the test\nwith `registered here but absent from packages/shared BOARD_IDS: [\"jobicy\"]`.\n\nDeliberately NOT done: `assert_eq!(health.scrapers.len(), 24)` stays a literal.\nDeriving it from `SCRAPERS.len()` — as the original audit item suggested — would\nmake it vacuous, since it is the only guard that notices a scraper being\ndeleted. The parity test above removes the duplication that actually drifts; the\nliteral keeps guarding the thing a self-referential count cannot.\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-18T07:31:33+02:00",
          "tree_id": "c777e5900a6c7f5c64690c281457e7570d275dec",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/a62bbd25d789145aa50703fc26e88e7324584c6a"
        },
        "date": 1787032581381,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2224888,
            "range": "± 98690",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2713999,
            "range": "± 42375",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 319848,
            "range": "± 4763",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "160d4a83b7d316ddc0c40b6270729cf6f9fa05a9",
          "message": "fix(autopilot): jitter scheduled runs so default installs stop herding (#1018)\n\nTrack B3 of the architecture audit.\n\nSchedules are clock-anchored, the tick is 60 s wide, and every record created\nbefore the run-time picker defaults to 09:00. So every default-schedule install\nin a time zone hits the same third-party APIs inside the same minute. This is\nnot theoretical here: the desktop log already shows scheduled runs failing with\n`adzuna: HTTP 503`.\n\nThe offset is DETERMINISTIC per autopilot, derived from its id, not random —\nthat is the whole design. A random delay would have meant sleeping inside the\ntick, which breaks the two properties everything else is built on: catch-up\nafter a missed occurrence, and no-double-run once `lastRunAt` is stamped at or\nafter the occurrence. Shifting the OCCURRENCE instead keeps it a single\nwell-defined instant, so both still hold, and tests stay reproducible.\n\nFNV-1a rather than `DefaultHasher`: the standard hasher is explicitly not stable\nacross Rust releases, and an offset that moved on a toolchain bump would\nsilently shift every user's schedule. Ids are UUIDs, so the low bits are already\nwell distributed.\n\nTen minutes: wide enough to spread a herd across ten tick windows, far enough\nunder the shortest interval (hourly) that a shifted occurrence can never\novertake the next one, small enough not to surprise someone who picked 09:00.\nThat upper bound is a `const _: () = assert!(...)` beside the constant rather\nthan a test — a test comparing two constants is a tautology clippy rejects, and\nthis makes a bad value unbuildable instead of merely reported.\n\nTests cover the offset being stable for an id and inside the window, the\noccurrence moving by exactly the offset, and the behaviour a user would notice:\nbetween the nominal time and the offset, today's occurrence has NOT been reached,\nso the latest is still yesterday's. Plus a full-day minute-by-minute sweep\nasserting exactly one transition — one run per day, no double-run, no skip.\n\nMutation-checked: making the offset always zero fails\n`jitter_actually_spreads_a_herd` with \"200 autopilots landed in only 1 distinct\nminutes\".\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-18T08:44:43+02:00",
          "tree_id": "d37f24986f9533fb8e08787f7a5e85b852205b53",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/160d4a83b7d316ddc0c40b6270729cf6f9fa05a9"
        },
        "date": 1787036896953,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2188492,
            "range": "± 57068",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2606885,
            "range": "± 48483",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 300123,
            "range": "± 2128",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "b0de8e062b1f476a80f44132408696cc948bb917",
          "message": "test(pipeline): pin what a killed resume run leaves on disk and what nothing cleans up (#1020)\n\n* test(pipeline): pin what a killed resume run leaves on disk and what nothing cleans up\n\nTracing the recovery contract for a resume pipeline killed mid-run found that there is no recovery contract: `jobs::JobTracker::open`\nsweeps interrupted jobs to `failed` on every startup and `AutopilotStore::mark_interrupted_runs` does the same for autopilot, but\n`PipelineRunStore::open` runs migrations and a url normalisation and nothing else. A `pipeline_runs` row written `running` before the\nfirst stage stays `running` forever after a kill.\n\nThese tests document that gap rather than assert it is correct.\n\n`tests/pipeline_kill_recovery.rs` performs a real kill: the test binary re-executes itself as a child, the child opens the same stores\nthe app opens and writes the state a run interrupted during `draft` would have, and the parent kills the process outright before\nreopening those files the way startup does. The run row and its partial stage trail come back untouched; the same kill's jobs.db row\ncomes back `failed` with a stated reason. That contrast is the finding, and the jobs.db half doubles as the positive control.\n\n`a_crashed_running_run_locks_out_the_last_good_runs_report` pins the consequence against the real decision function: the orphan is\npermanently the newest run for its posting, so `ensure_latest_run` refuses `regenerateSection` and `resolveFabrication` against the run\nthat actually holds the saved document, telling the user to wait for a run whose process no longer exists.\n\nMutation-checked four ways: neutering the jobs.db sweep, adding reconciliation to the run store, breaking the child readiness\nhandshake, and neutering `ensure_latest_run` each turn the relevant tests red.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* test(pipeline): make the killed-run fixture guard catch a reorder\n\nAddresses three review findings on #1020.\n\nThe fixture guard checked membership and phase vocabulary only, so a REORDER of\n`QUALITY_STAGES` — or a stage inserted ahead of `draft` — left `KILLED_TRAIL` describing a\nrun nothing can write while the test stayed green, and `len() < len() * 2` accepted any\nshorter trail at all. That guard sits under the whole file: if the fixture is not a\nfaithful stand-in for a real run, \"what a killed run leaves on disk\" is fiction. It now\nasserts prefix equality against `QUALITY_STAGES` expanded into its start/finish pairs, that\nthe trail ends on an opened stage, and that `seq` runs contiguously from 0. The expected\nvalue stays hand-written; only the assertion is derived, so the fixture can still disagree\nwith the pipeline.\n\nThe `RUN_KIND`/`RUN_DEPTH` note cited a test that reads those consts through `super::` and\ntherefore moves with them. It now cites\n`the_run_kind_the_budget_floor_and_the_run_depth_are_pinned`, which pins them against the\nsame literals this fixture repeats.\n\nBoth gap doc comments now reference #1020, so a maintainer meeting a deliberately-red\nassertion has the finding — the lockout, the missing run-to-job link on disk, and the fact\nthat `ensure_latest_run` keys on recency rather than status — without reconstructing it.\n\nMutation checks, all executed: reordering two stages and inserting one ahead of `draft`\neach turn the new guard red where the old one passed, and adding reconciliation at\n`PipelineRunStore::open` still turns both gap assertions red.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-18T11:25:17+02:00",
          "tree_id": "4f31416f096cc643d617c297503d53720ac2376b",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/b0de8e062b1f476a80f44132408696cc948bb917"
        },
        "date": 1787046644448,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2196678,
            "range": "± 85321",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2613698,
            "range": "± 37309",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 313832,
            "range": "± 5937",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "cfa85c26f90f93e34b49423c8d69f8d96d689576",
          "message": "feat(match): report hard constraints beside the score instead of blending them into it (#1023)\n\n* feat(match): report hard constraints beside the score instead of blending them into it\n\nTrack E1 of the architecture audit.\n\n`combined = f(semantic, ats)` measures how much of a posting's vocabulary the\nrésumé carries. It is a good relevance number and it structurally cannot answer\n\"could I actually take this job?\", so a candidate who fails a non-negotiable\nstill scores well on keyword overlap. The fix is not to fold a penalty into the\nnumber — it is to answer the second question separately.\n\n`match_resume` now returns a `constraints.checks` field beside the score. Three\nrules hold it up:\n\n* it never touches the score. The verdict is computed in the L3 command AFTER\n  the kernel returns, so it is not an input to `score_one` and never enters the\n  `match_scores` cache — whose key is composed of SCORING inputs only, and would\n  therefore have served a stale verdict for a whole TTL the moment the user\n  edited their preferences. Out here, a cache HIT still gets a fresh verdict,\n  and the Autopilot and extension entry points that share the kernel are\n  byte-identical;\n* it never hides a job. Nothing filters, sorts or gates on it. A wrong knock-out\n  tells the user not to bother applying, which costs far more than a wrong\n  percentage;\n* absence of evidence is never evidence. `ConstraintStatus` has FOUR values, and\n  the two \"we don't know\" ones are distinct from both a pass and a fail:\n  `unknown` (the candidate stated a preference, the posting says nothing, or\n  what they stated is not comparable) and `noPreference` (the candidate has\n  expressed nothing at all — differently actionable, since the user can fix that\n  one). A `notMet` requires positive evidence on BOTH sides, and each check\n  carries each side's own words so that is a checkable property of the payload\n  rather than a promise. It is enforced inside the one constructor every check\n  goes through, not as a post-pass a later constraint could skip.\n\nONE constraint ships, because one is what the app has honest candidate-side data\nfor. `location`: the user's stored job-search location (and its geocode-picked\ncountry code) against the posting's location text and its board remote flag.\nBoth sides are real, persisted, user-entered data.\n\nThe audit's other three are refused, and the module says so in full:\n\n* work authorization, ranked first, has NO candidate-side field anywhere — not\n  on the contact profile, not in job preferences. The tempting proxy, the\n  candidate's own address, is exactly the false accusation this repo already has\n  a live incident from: living in one country is not evidence of lacking\n  authorization in another;\n* employment type has no persisted preference. `AutopilotTarget::work_type` is a\n  per-autopilot SEARCH filter, and a work arrangement rather than an employment\n  type;\n* salary floor: `salary_expectation` is free text (\"80k DOE\") documented as an\n  application ANSWER, not a stated floor, with no currency and no period, and\n  the posting side is a structured range on one board. Comparing them needs a\n  money parser plus an FX assumption — two invented facts per verdict.\n\nThe comparison itself reuses the scrape-time location matcher rather than\nforking one. `location_filter` grows a three-valued `location_verdict`, and the\nexisting binary `location_mismatch` is redefined as exactly its `Mismatch`\nprojection — same behaviour, one home for the remote-marker list, the diaeresis\nfolding and the curated exonym table. `languages_align` does not apply: nothing\nhere reads the résumé or the JD body, stems a token or extracts a keyword.\n\nMutation-checked, ten ways. Two survived the first round and both were real\nholes, now closed: the two-sided-evidence guard was a post-pass its test called\ndirectly, so deleting it from `evaluate` stayed green — it is a constructor now;\nand deleting the one line that wires the pass into the command left the whole\nsuite green, because a `#[tauri::command]` cannot be called from a test in this\ncrate, so a compile-time source scan pins it. The other eight — rendering\nunknown as a pass, dropping the no-preference branch, deleting either remote\nfeature, dropping the posting-side evidence, making the scrape filter drop on\nundecided, folding the verdict into `combined`, removing the evidence byte cap —\neach fail by name.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(match): stop the location constraint publishing a knock-out it cannot justify\n\nReview was right on both counts, and the shipped verdict was the defect.\n\nThe first cut published `notMet` for location. Two independent facts say it\nmust not:\n\n* the candidate side is a search-personalization setting, not a mobility\n  statement. `job_preferences.location` is written by one free-text \"Preferred\n  Location\" input whose own description says it \"personalizes search results and\n  recommendations\". `JobsPage` seeds its scrape form from it ONE WAY and never\n  writes back, so a user who deliberately searches Austin while Settings still\n  says Berlin leaves no trace. `PostingsCache` keeps postings from every past\n  search and retains no per-posting `LocationSpec`, so this pass cannot even\n  recover which search a posting came from — the honest sentence would have been\n  \"a setting you last touched months ago says Berlin\", against every cached row;\n* a failed substring match is absence of evidence, not evidence of conflict.\n  `Mismatch` fires on `Germany`/`Berlin` (granularity), `Vienna`/`Wien` and\n  `NYC`/`New York` (exonym and abbreviation outside the curated table),\n  `Berlin`/`EMEA` and `Berlin`/`Multiple locations` (not place names), and\n  `Berlin`/`Telecommute` (a remote synonym the marker list lacks). It would also\n  fire across most of the primary aggregator: Adzuna never writes\n  `extra.remote` — only the three remote-only boards do — so remote detection\n  there rests entirely on location text Adzuna's `display_name` never carries.\n\nThe module's own rule 3 convicted it: two non-empty strings satisfied the\nconstructor's floor while not being evidence of a CONTRADICTION.\n\nSo `Mismatch` now maps to `Unknown`, and the location check emits `Met` /\n`Unknown` / `NoPreference` only. `Met` survives on positive evidence in both\ndirections and never accuses. The three-valued matcher still earns its keep —\nwithout it `Met` could not be told apart from \"the posting states no location\".\n\nThe severity distinction is the lesson, and it is recorded in the module: the\nidentical predicate is safe as a scrape-time post-filter, judging a posting\nagainst a location the user typed seconds earlier and costing one row in one\nsearch, and unsafe as a published verdict against every cached posting keyed off\na settings field. Reuse the matcher; re-derive the epistemics.\n\nNet result for Track E1: NO shipped constraint can emit a knock-out.\n`ConstraintStatus::NotMet` stays as the contract slot a future constraint with\nreal two-sided evidence will use, and a test pins that it is unreachable rather\nthan merely unused.\n\nAlso fixed:\n\n* the wiring pin was blind to a present-but-dead call. It now matches the call\n  as a WHOLE LINE, which rejects both `let _ = constraints::attach(…)` and a\n  commented-out call, and leans on the compiler for the rest: the line carries\n  no trailing semicolon, and a `Value`-typed expression statement that is not\n  the tail does not compile. The first version embedded a closing brace in a\n  string literal, which silently truncated the region the egress inventory's\n  naive brace counter strips from this file — caught by that guard, fixed here\n  rather than added to its exception list;\n* the constructor was not the structural guarantee its doc claimed. Rust field\n  privacy is module-scoped, so a struct literal compiled anywhere inside\n  `mod constraints` — exactly where the next constraint gets written. The type\n  is sealed in its own `mod check` now, and the bypass is a compile error;\n* `isKnockOut(check)` ships beside `ConstraintStatus` before any UI reads it:\n  `status !== 'met'` silently turns both unknowable states into an accusation;\n* `country_code` was inert on both ends — no renderer writes it, and the matcher\n  draws needles from city/region text only. Dropped rather than left as a claim\n  the code does not honour.\n\nTests: the review's pairs are asserted individually and as a corpus, and the old\n`knock_outs == 4` — which could only confirm the code agreed with itself, its\ncorpus holding only pairings it already agreed with — is replaced by the full\nstatus distribution over 140 cells (40 noPreference, 58 met, 42 unknown, 0\nnotMet), hand-derived so it cannot pass by collapsing to one answer.\n\nMutation-checked 14 ways, all caught, including the two the review named:\n`let _ = constraints::attach(…)` is red, and the struct-literal bypass no longer\ncompiles. Re-publishing `notMet` for a failed place-name match is red. The TS\npredicate mutated to `status !== 'met'` fails 3 tests.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(match): require a whole-token place match before claiming a location fits\n\nThe four-state contract's one confident state was still loose. `met` is a\npositive claim rendered to the user — \"this posting matches where you're\nlooking\" — and it rested on the scrape filter's substring, any-token reading, so\na `San Francisco` preference agreed with a `San Diego` posting on the shared\n`san`. Erring toward agreement rather than accusation makes that cheap, not\ncorrect. A contract whose only confident state is loose has moved the\nimprecision, not removed it.\n\nThe strictness lives in the epistemics layer, not in the shared matcher: the\nscrape filter's conservatism is right where it is, because there the expensive\nmistake is discarding a job.\n\nSo the matcher now reports WHAT it found and the constraint pass decides what is\nclaimable. `LocationVerdict::Match` splits three ways:\n\n* `Remote` — the board flagged it or its location text carries a marker, so no\n  place comparison applies;\n* `PlaceMatch` — every token the user actually typed appears as a WHOLE token of\n  the posting's location;\n* `PlaceOverlap` — something matched, but only as a substring or on part of a\n  multi-token request.\n\n`location_mismatch` is still exactly the `Mismatch` projection, so the scrape\nfilter is byte-for-byte unchanged: `PlaceOverlap` keeps, as it always did. The\nconstraint pass maps `Remote | PlaceMatch` to `met` and everything else,\n`PlaceOverlap` included, to `unknown`.\n\nExonyms are expanded PER TOKEN in the strict matcher rather than over the whole\nneedle list. Expansion adds a word the user never wrote — a \"Munich\" request\ngains \"münchen\" — so requiring every needle in the expanded list to match would\nfail the exact pair the curated table exists to bridge.\n\nThe cost is deliberate and one-directional: a request MORE specific than the\nposting (\"Berlin, Germany\" against a bare \"Berlin\") now reads `unknown` rather\nthan `met`. That loses a true positive and never states a false one, and\n`unknown` claims nothing.\n\nTests: a dedicated overlap case (San Diego, Santa Monica, Newark, and the\nmore-specific request) with a same-preference control that still reads `met`, so\nthis is a strictness rule and not a blanket refusal to match a two-token place.\nThe 140-cell distribution grows to 176 (11 places × 8 preferences × 2 flags) to\ncarry the San Francisco / San Diego pairing, and its hand-derived counts — 44\nnoPreference, 75 met, 57 unknown, 0 notMet — are what fail if the strict rule is\nrelaxed: that one cell moves from unknown to met.\n\nMutation-checked 19 ways, all caught. New this round: publishing `met` for an\noverlap, grading every hit as a whole-token match, requiring only SOME requested\ntoken instead of every one, dropping the per-token exonym expansion, degrading\nthe whole-token test to a substring test, and making the scrape filter drop on\nan overlap.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(match): bound the met claim to the setting it reads, and pin what it rests on\n\nFive review findings. The first is the one that mattered: the epistemics were\nre-derived in ONE direction only.\n\nEvery fact used to kill `notMet` is direction-neutral — `job_preferences.location`\nis a search-personalization setting, `JobsPage` seeds from it one-way, and\n`PostingsCache` keeps postings from every past search with no record of which\n`LocationSpec` produced them. So the mirror case was equally false: the same\nstale `Berlin` setting, against `Berlin` rows left over from an earlier search,\nreported `met` — and if that rendered as \"this posting matches where you're\nlooking\" it was wrong for the same reason, from the same field.\n\nDeleting `met` is not the answer; it would leave a contract with no positive\nstate and make the pass vacuous. The asymmetry is real and it is in what each\nstatus ASSERTS:\n\n* `notMet` asserted a CONFLICT — that the posting is somewhere the candidate will\n  not go. Nothing establishes that relation, and no phrasing repairs it, because\n  the fact was never in hand;\n* `met` asserts a MATCH between two strings held in full. The relation — every\n  token of the stored preference present in the posting's location — is\n  established deterministically and stays true whichever search produced the row.\n\nSo `met` is repaired by bounding what it is a claim ABOUT. It says \"your stored\nPreferred Location matches this posting\", never \"matches where you're looking\".\nFixed where the meaning is defined, since there is no renderer yet to correct\nlater: the check's wire id is now `preferredLocation` rather than `location` —\nthe id is what a renderer keys i18n off, so it is what stops the stronger\nsentence being written by accident, and it leaves `location` free for a real\nmobility constraint if candidate-side data ever exists. Also stated on the\nvariant, the module doc, and the three shared TS docs a UI author reads.\n\nAlso fixed:\n\n* `met` for the shipped UI defaults silently depended on `MIN_TOKEN_LEN`. \"San\n  Francisco, CA\", \"New York, NY\" and \"London, UK\" all read as matches ONLY\n  because 3 drops the two-letter qualifier, while that constant's doc justified\n  3 purely as scrape-filter noise control. Lowering it to 2 for a scrape-side\n  reason flipped all three to `unknown` with nothing failing. Now pinned by\n  test, and the constant records that it has a second consumer with a different\n  risk posture;\n* the verdict and the evidence were computed from different bytes — the\n  unclamped location was compared, the clamped one reported — so a >200-byte\n  location could match on a token outside its own evidence. Both sides are\n  clamped once, before the comparison. The test is a replay: re-running the\n  evaluation on exactly what the check REPORTS must reproduce what it decided;\n* `checks_for_job` took the `PostingsCache` lock and re-scanned for a job id\n  `job_text_for` had just scanned for. Since the verdict is deliberately\n  recomputed on a `match_scores` cache HIT, that duplicate ran on every\n  Jobs-page call — the path where the score costs nothing. The posting is\n  resolved once and handed over; the pure half is split out so the hand-off is\n  testable;\n* `PlaceOverlap` named two situations of opposite evidential strength: a\n  coincidental substring (\"San Francisco\"/\"San Diego\" on `san`) and a genuine\n  whole-token hit with one unmatched qualifier (\"Berlin, Germany\"/\"Berlin\").\n  Renamed `PlaceIncomplete` — named for the incompleteness both share rather\n  than a strength neither guarantees — with both readings documented. Not split:\n  nothing needs to tell them apart, and inventing a distinction ahead of a\n  consumer is what was refused for salary and employment type.\n\nMutation-checked 24 ways, all caught. Two survived the first pass and both were\nreal gaps, now closed: the clamp fix was tested on long POSTINGS but not long\nPREFERENCES, and substituting a default for the handed-over posting facts was\ninvisible to the whole crate — the same untested-hand-off shape that bit the\ncommand's tail expression earlier in this branch.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-18T12:47:41+02:00",
          "tree_id": "aa9b879c86be2f10da110da73dc77143c8b68e54",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/cfa85c26f90f93e34b49423c8d69f8d96d689576"
        },
        "date": 1787051491538,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2179803,
            "range": "± 48436",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2593750,
            "range": "± 48817",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 304977,
            "range": "± 3320",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "36ddb9e28aaa4d233f1a9ebceb0cb6cd53c3ab8f",
          "message": "fix(scraping): wire the freehire params their spec never documented (#1035)\n\n* feat(scraping): add per-request user-agent override to fetch_text\n\nFetchOptions gains user_agent: Option<String>. None (every existing\ncall site) is byte-identical to today: fetch_text still writes\nDEFAULT_UA. A board that wants to identify itself now has a real\noverride instead of the only prior option (putting user-agent in\nopts.headers), which does not work: header() on RequestBuilder\nappends rather than replaces, so a header entry would have ridden\nalongside DEFAULT_UA as a second header line instead of overriding it.\n\nRegression test proves the override REPLACES, not appends, by\ninspecting the actual request wiremock received (not just a header()\nmatcher, which only proves the override value is present, not that\nthe default is absent). Mutation-checked by execution: reverted the\nfetch_text line to the old unconditional DEFAULT_UA write, confirmed\nthe test goes red with the wrong value on the wire, restored.\n\n* fix(scraping): wire freehire date filter and adopt issue #1026 spec fixes\n\nfreehire's own maintainer audited their published spec after PR #1008\nand disclosed (issue #1026) that it had documented 17 of ~35 real\nquery parameters. Live-verified against the production API before\nwriting any of this down (spot-checked posted_within_days, reality,\nmeta.ignored_params, and the rate-limit headers against\nhttps://freehire.me directly, 2026-08-18) rather than taken purely on\nthe maintainer's word.\n\n- The real bug: date_filter reaches the wire. fetch_freehire took\n  _date_filter: Option<&str> — accepted and thrown away, because the\n  old spec had no date parameter. Maps our 9-token date_filter\n  vocabulary onto posted_within_days (whole days, live-verified),\n  reusing the SAME 3-day sub-day floor already established for\n  adzuna_max_days_old/jsearch_date_posted rather than inventing a new\n  number. None (no filter) omits the parameter entirely, since\n  freehire's own \"omit\" semantics is genuinely unfiltered.\n\n- Identifying User-Agent (freehire_user_agent, built on the prior\n  commit's FetchOptions::user_agent): \"ai-job-hunter/<crate version>\n  (+https://github.com/saeedkolivand/ai-job-hunter-app)\". Carries only\n  the app name, the real crate version (env!(\"CARGO_PKG_VERSION\")),\n  and the public repo URL — no user data, no machine id, no locale.\n  Not enforced or validated by freehire; buys advance notice before a\n  field/limit changes instead of a 429 being the first sign.\n\n- Rate limits: no new limiter code. The maintainer's disclosed budgets\n  (600 req/min ordinary, 300 req/min for the agent search endpoint\n  this module calls) are already handled by fetch_text's existing\n  429/503 backoff, which already honors Retry-After — confirmed by\n  reading scraping/http/mod.rs before writing anything, per the task\n  brief. This tier's own traffic (one request per aggregator search,\n  reached only after every keyed tier has failed or come back empty)\n  sits nowhere near either ceiling.\n\n- meta.ignored_params guard: freehire ignores an unknown parameter\n  rather than rejecting it, so a typo returns the whole catalogue\n  looking exactly like a legitimate broad result (live-verified: a\n  country=gb typo returns 200 with\n  meta.ignored_params:[{\"param\":\"country\",\"did_you_mean\":\"countries\"}]\n  and the FULL unfiltered set). fetch_freehire now checks\n  resp.meta.ignored_params and returns Err — not the unfiltered\n  data — when any of this module's own params are ever reported\n  ignored, so a future upstream rename degrades this tier to\n  Ok(empty) (the existing silent-degradation boundary) instead of\n  shipping an unfiltered result set dressed as a filtered one.\n\n- reality=fresh sent unconditionally, no user-facing toggle. Every\n  other quality improvement this aggregator applies (sort_by=date on\n  Adzuna/JSearch, dedupe_by_url everywhere) is likewise always-on with\n  no settings knob anywhere in this board, so a bespoke toggle for\n  just this one facet on just this one last-resort tier would be the\n  odd one out. Live-verified to cut the unfiltered catalogue roughly\n  in half (1,477,440 -> 662,112), which matters for a tier whose whole\n  point is \"enough to be useful without pulling a page nobody reads.\"\n\n- Module doc rewritten to describe the API as it is now (documented\n  rate limits + headers exist), not as a narration of the change.\n\nExplicitly NOT adding cities: confirmed real, but geography (regions/\ncountries/cities) is a single OR-group on this API — countries=gb is\n89,211 results, countries=gb&cities=London is 89,617 (WIDER, not\nnarrower, live-verified) — so adding it needs a location-UI decision\nthis change doesn't make. Left as a follow-up.\n\nEvery new guard mutation-checked by execution (deleted the feature,\nran, confirmed red, restored): the posted_within_days floor, the\nidentifying-UA wiring, the ignored_params guard, and reality=fresh\nbeing unconditional.",
          "timestamp": "2026-08-18T17:12:14+02:00",
          "tree_id": "e0cf0c592cf2a2214124399df837b9d7ba15ae91",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/36ddb9e28aaa4d233f1a9ebceb0cb6cd53c3ab8f"
        },
        "date": 1787067370660,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2207223,
            "range": "± 49797",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2667990,
            "range": "± 44828",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 316618,
            "range": "± 10350",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "f28b23d5410cefbaac99d95631b8810c9be2a5db",
          "message": "fix: make in-flight backend work survive navigation, and stop logs leaking paths (#1036)\n\n* test(scripts): make a stale subscription note fail the build\n\nAn audit of all nine route-scoped entries against the code found eight of the\nnine notes wrong. Three described a loss that had already been fixed, four\nunderstated what is lost, and one named a cause that is simply not what the\ncode does. Only the activity-feed note was accurate.\n\nThe notes rot because nothing couples them to the code they describe: #1019\nfixed the jobs scrape and never touched the note beside it, and #1025 later\nedited this very file without noticing that note was by then obsolete. The\nguard enforced that a note EXISTS, never that it is true.\n\nSo each route-scoped entry now carries a short content hash of the file it\ndescribes, and the check fails when the file drifts, printing the note so\nre-reading it costs seconds. It over-fires by design: a formatting change also\ntrips it, and the fix is a one-line hash update. Always-mounted entries are\nexempt because their note is a structural claim, not a description of loss.\n\nNotes rewritten to lead with the observable symptom, and to say what is NOT\nlost as well — several of these misled a reader precisely because they listed\nonly the loss and stayed silent about recovery that had since landed.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(logging): stop leaking paths/urls/credentials via bare {e} in log macros\n\nAGENTS.md's path-privacy rule bans absolute paths, usernames, and home dirs\nfrom ever reaching a log line. ~100 non-scraping call sites did it anyway via\nlog::{warn,error,info,debug}!(\"...: {e}\") where e wraps a rusqlite::Error\n(InvalidPath embeds the DB path), a filesystem std::io::Error, or a\nreqwest::Error (embeds the full request URL, sometimes with a credential in\nthe query string) - the shape applications::ApplicationStore::open already\ndocuments the fix for.\n\nTriage: fixed with .code() where the site is an exact match for that\ndocumented precedent (a *Store::open failure), or with the more informative\nobservability::sanitize_reason(&e.to_string()) everywhere else, since it\nkeeps the safe part of the message instead of discarding it. 12 sites were\nleft as bare {e} with a stated reason (a pure parse/logic error that cannot\nstructurally carry a path/URL/host/credential, e.g. an in-memory PDF parse,\na fixed-template rate-limit message, or a reqwest error already stripped of\nits URL upstream via .without_url()).\n\nextension_bridge/mod.rs crossed the R8 hard LOC cap once its five fixes\nexpanded past one line each; split its token/opt-in persistence functions\ninto a new extension_bridge/persist.rs sibling module, mirroring the\nautotrack.rs split already documented in that file.\n\nscraping/** (45 sites, mostly reqwest/keyring errors) is scraping-applier's\ndomain, out of this pass's primary-path scope - tracked as debt in the\ncompanion guard (scripts/check-log-error-leaks.mjs), not fixed here.\n\n* test(logging): add a drift guard for the {e}-in-log-macro leak class\n\nFixing every known site is not durable on its own - #125 lands next week\nwithout something that fails the build on a new one. Mirrors the house\nscripts/check-*.mjs + .test.mjs shape (check-event-subscriptions.mjs's\ndeclare-it-or-fail inventory in particular): every log::{warn,error,info,\ndebug}!(...{e}...) site in apps/desktop/src-tauri/src is either fixed (so it\nno longer matches) or declared in ALLOWLIST with a one-line reason, tagged\nsafe (provably cannot leak) or debt (scraping/** sites left for that\ndomain's own author). Both directions are checked so the list cannot rot: an\nundeclared site fails, and so does an ALLOWLIST entry whose site got fixed,\nmoved, or deleted without the entry being removed.\n\nMutation-tested by hand against the real repo before wiring it in: reintroduced\na leaking {e}, confirmed the guard goes red with the new site named; reverted,\nconfirmed green; separately fixed a debt site in place without touching its\nALLOWLIST entry and confirmed the guard goes red for the OTHER failure mode\n(a stale entry), then reverted that too. A guard driven only off its own data\ncan't catch a deletion - ALLOWLIST is hand-written, not generated from the\nscan it's checked against.\n\nWired into package.json (check:log-error-leaks), the CI lint-format job\n(alongside toolchain-pin/event-subscriptions/adr-citations), and the\npre-push hook's RUST-gated section (pure text scan, no cargo needed, so it\nruns before the cd into src-tauri).\n\n* fix(jobs): persist scrape progress across a remount\n\nuseScrapeProgress resets to null on every mount and, on the default\nsingle-board scrape, only ever fires once as the run ends -- so\nnavigating away and back showed a false 0% for the rest of the run.\njob_progress already persists the same fraction into JobRecord, and\nthe watchdog already polls that record; feed it into a fallback used\nwhenever no live progress event has arrived this mount. Also give the\nwatchdog poll a leading call so the fallback lands immediately instead\nof after the first 2.5s tick.\n\n* fix(resume-pipeline): recover the failure reason after a remount\n\nsetError was live-listener-only, so a run that fails while the tailor\npanel is unmounted (job.failed never reaches it) left the wizard with\nno trace of what happened on remount. Fall back to the persisted\nstoppedReason (via the existing stoppedSuffix mapping) when no live\nerror landed.\n\n* fix(updater): stop check/download from discarding an in-flight update\n\nupdater_check unconditionally reset downloaded_bytes and swapped the\nUpdate object, so a returning caller discarded a completed download\n(forcing a full re-download) or raced one still in progress; report\nback the known state instead. updater_download gets the same\nre-entrancy guard job_start_exclusive already gives ai.reembed/\nai.indexStale, plus a Drop-based guard so a mid-transfer panic can't\nleave the flag stuck.\n\nai_pull_model now uses job_start_exclusive so a second concurrent\npull of the same model re-attaches to the existing job id instead of\nstarting a fresh multi-GB download.\n\n* fix(jobs): explain a scrape that fails off-page and refresh postings\n\nThe watchdog's failed/cancelled branches passed no summaries to\nfinishScrape, so a scrape that failed while the user was on another\nroute came back to an empty chip strip and no note -- the only copy of\nthe reason lived in scrapeOutcome, which renders solely inside the\ndrawer handleStartScrape already closed. Restore scrapeSummaries/\nscrapeFailureNote on recovery, sanitizing the backend error the same\nway the live job.failed event path already does (an unsanitized path\nwas landing straight in the UI). Also invalidate postings after a\ncompleted recovery, matching the live event handler, so a 30s\nstaleTime can't leave the pre-scrape list on screen.\n\n* fix(autopilot): close the run-status staleness hole and persist the error banner\n\nuseAutopilots inherited the 30s default staleTime with nothing invalidating\nit when a run starts, so a remount inside that window could show idle with\nan enabled Run button for a run still executing. Give it staleTime: 0.\n\nMove the Run/Apply error banner from a useAutopilotRun-local useState into\nthe session store so it survives a navigate-away-and-back, matching the\npersisted-badge cases and covering the concurrent-run refusal, which has\nno other trace. Also corrects a comment that credited the terminal-step\ninvalidation with recovering a run that finished off-page, which it can't\n(the subscription itself unsubscribes on unmount).\n\n* fix(onboarding): reattach the ollama pull to a job already running\n\npullJobId lived only in useModelPull's own state, so any unmount of the\npanel (tab switch, back/forward, or leaving the wizard) lost it forever\nand no later job.stream/job.completed for the still-running pull could\nmatch again -- the panel silently re-offered Download for work already\nin flight.\n\nOn mount, re-read the job registry for an active ai.pull_model job and\nresume tracking its id. ai_pull_model is exclusive, so at most one can\nbe running app-wide.\n\n* fix(settings): stop the updater hook resetting to idle on remount\n\nThe settings panel's useUpdater() is one of three independent\ninstances (banner, menu, this panel), each with its own local status\nand its own updater:status subscription. The banner/menu never\nunmount so they never lose it, but this panel mounts/unmounts with\nroute navigation and had no way to learn the current status on\nremount -- there is no backend command to ask for it, only the push\nevent -- so it rendered a stale idle, including a live \"Check now\"\nbutton for an update already downloading or done.\n\nMove status into a small synchronous external store shared by every\nuseUpdater() instance, so a remounted one immediately reads whatever\nany other mounted instance already learned.\n\n* docs(scripts): rewrite the subscription notes against the fixed code\n\nThe tripwire added earlier on this branch fired on its own merge, for four of\nthe six files these fixes touched. That is the mechanism working: each note\ndescribed a defect that no longer exists.\n\nRewritten to describe what the code does now, and to keep saying what is still\nlost — the streamed resume draft, the autopilot step log, and a model pull that\nreaches a terminal state inside the unmount/remount gap are all accepted\nlosses, not oversights, and a note that lists only the good news is how these\nwent stale the first time.\n\nRecords a blind spot the first real firing exposed: the hash covers the\ndeclared file only, so a note also goes stale when the behaviour it describes\nchanges in something that file merely calls. Both misses came from this one\nbatch — JobsPage's note was fixed inside useScraping, update-section's inside\nthe updater service and its Rust command — and neither declared file was\ntouched, so neither tripped. Hashing the transitive import graph would fire on\nnearly every commit and be ignored within a week, so the trade stands and the\ntwo entries say in their own text that they were fixed elsewhere.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(scraping): close the log-error-leak debt the fleet sweep skipped\n\nThe prior sweep (8a01a297) fixed ~100 non-scraping log::{warn,error,info,debug}!\nsites that interpolated a caught error via bare {e} and left the 45 scraping/**\nsites as tracked debt, guessing they were pattern-identical reqwest/keyring\nleaks. Tracing each site's concrete type instead of pattern-matching the macro:\n\n- 12 were genuine leaks and are now fixed with\n  observability::sanitize_reason(&e.to_string()): 7 OS-keyring errors\n  (Adzuna/JSearch/Jooble/Apify credential slots, Comeet company uid + token),\n  a rusqlite execute() error clearing board_health on factory reset, cookie\n  export/import filesystem paths (board_login/mod.rs + import.rs, both reading\n  real browser-profile paths despite a \"log without values\" comment that the\n  code didn't back up), and a genuine one: scraping/linkedin/api_client's page\n  loop wraps reqwest::Error via error.rs's default From impl (no\n  .without_url()), so a transport failure embedded the full LinkedIn search\n  URL - including the user's free-text keywords/location - in the log.\n\n- The remaining 33 were already safe and are now declared as such, not fixed:\n  every named-board fetcher routes through scraping::http::fetch_text/\n  fetch_json, whose transport-failure branch already strips the request URL\n  via reqwest::Error::without_url() before wrapping in AppError::Network -\n  the same protection net/http.rs's accumulate_capped applies fleet-wide - so\n  the Adzuna/JSearch/Jooble/Apify API key living in that URL's query string\n  never reaches these log lines even on failure. The rest are pure in-memory\n  serde_json parse failures, an htmd conversion error, and a tokio JoinError -\n  none of which can structurally carry a path/URL/host/credential.\n\nscripts/check-log-error-leaks.mjs's ALLOWLIST now carries zero debt entries;\nthe two reclassification factories (httpChokepointSafe/inMemoryParseSafe)\ndocument the shared reasoning once instead of 33 times.\n\n* test(scraping): bound the transport-error leak test so it stops costing a minute\n\nThe new query-secret leak test dials port 9, the discard port. Unix refuses\nthat instantly; Windows lets the connect attempt sit until its own timeout, so\nthe test took over sixty seconds and every local `cargo test` run paid it.\n\n`FetchOptions.timeout` already exists for exactly this kind of ceiling. Setting\nit to 500ms does not weaken the assertion — a refused connection still fails\nfast, a hung one now fails at 500ms, and both are the transport errors whose\n`Display` must not carry the query string. Runtime went from over a minute to\nhalf a second.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(scripts): close the hashless route-scoped bypass in the subscription guard\n\nA route-scoped SUBSCRIBERS entry with no hash skipped staleNoteEntries\nforever, so its note could drift from the code with nothing to catch it.\nviolations() now rejects a route-scoped entry with a missing or malformed\nhash before staleness is even computed; always-mounted entries stay exempt.\n\n* fix(jobs): sanitize job_fail errors before they reach the tracker or ipc\n\njob_fail/job_fail_with_data stored and emitted raw e.to_string() error\ntext, reaching the renderer through jobs_get, jobs_list, and the\njob.failed event -- the same URL/path/hostname leak class PR #1036\nclosed for log:: call sites, on a different channel it never touched.\n\nRoute both through sanitize_reason at the single mutator boundary so\nevery existing and future caller is covered. The structured `data`\npayload job_fail_with_data carries is left untouched -- only the\nfree-text `message` half is sanitized.\n\n* fix(scripts): widen the log-error-leak scanner past literal {e}\n\nThe scanner only matched the literal `{e}` capture, so `{err}` (live at\npostings/mod.rs:382 and platform/linux_appimage.rs:170) and a positional\n\"{}\", e argument (live at autopilot_helpers/mod.rs:151) bypassed it and CI\nentirely. findLeaks() now catches captured {e}/{err}/{error} and a bare\npositional error-binding argument, bounded to the three names this crate\nactually uses (measured against every log call site) so ordinary data\nfields like {host}/{port} stay unflagged. All three newly-surfaced sites\nwere traced and declared safe with a one-line reason each.\n\n* fix(documents): redact a posting id that embeds a full url before logging\n\nCodeRabbit's premise (\"PostingsCache uses normalized posting URLs as\nIDs\") is false for most boards -- greenhouse/lever/etc build an opaque\nboard:external-id -- but true for breezy/pinpoint/themuse, which build\nJobPosting.id as format!(\"{BOARD_ID}:{url}\"), embedding the posting's\nfull URL. That id reaches posting_vector_or_embed's failed-upsert log\nline raw via match_resume's renderer-supplied job_id.\n\nRedact job_id with the same shape-based redact_token classifier the\nerror already goes through, rather than hashing -- that would cost\nevery other board's debuggable id to close a leak only these three\nhave. match_resume.rs:217's \"job not found in cache\" error is left\nalone: it echoes the caller's own job_id back to the same renderer\nthat supplied it (an established echo-point pattern elsewhere in this\ncommand), so no new information is disclosed there.\n\n* ci(windows): run the atomic file-replace test, not just cargo check\n\ncargo-check-windows only ran cargo check --all-targets, so\nstd::fs::rename over an existing file (postings::InteractionStore\n::save's write-then-rename) was never actually executed on this\nplatform -- rename-over-existing has different underlying semantics\nper OS, and this repo has a recorded history of platform-exclusive\ncode compiling nowhere until release.\n\nAdd one narrowly-targeted cargo test for that single rename call, not\nthe whole suite -- keeps the job cheap (Windows runners bill at 2x)\nwhile closing the specific gap.\n\n* fix(extension-bridge): shape-constrain rejected origins before they hit the log\n\nwarn_rejected_origin_once logged the raw attacker-supplied Origin header at\nall three of its call sites (PR #1036 sanitized log_handshake_failure's\ne.to_string() in the same file but left these). sanitize_reason's\ncredential-token redaction is the wrong tool here: the origin is the one\ndiagnostic value a developer needs verbatim to add to extensionDevOrigins.\n\nAdd a narrow sanitize_log_origin helper instead: strip control characters\n(newline/CR/ESC/NUL, the log-line-forgery and ANSI-escape shapes) and cap the\nlength, so the origin's content survives but its shape can't smuggle a\nforged log line or flood the log.\n\n* fix(extension-bridge): close the pairing token's brief world-readable window\n\npersist_token wrote the token via std::fs::write then set_permissions(0o600)\nafterward, so on unix the file briefly existed at the process umask between\nthe two calls -- group- or world-readable on a multi-user box. The\nset_permissions failure was also swallowed with `let _ =`, so a box where it\nfails silently kept a readable pairing secret.\n\nOpen the file with the 0o600 mode baked into the create syscall itself\n(OpenOptions::mode) so a brand-new file is owner-only from its first byte on\ndisk, with no window. `.mode()` only applies on creation though, so keep the\ntrailing set_permissions call to self-heal a pre-existing wrong-permission\nfile (first-create and rotate both go through this one function); its\nfailure is now logged and propagated instead of discarded.\n\nMutation-checked in a standalone repro against the old two-step pattern:\nboth a final-mode assertion and a concurrent-poller race test (permissive\numask, 2M busy-poll iterations, 4 runs) could not distinguish old from new\nin the success path -- the old code reaches the same end state and the\nTOCTOU window is real but too short for a black-box test to observe. Noted\nplainly in the test module rather than claiming coverage the tests don't\nhave.\n\n* test(scraping): pin that every board runs through the http chokepoint\n\nThe log-leak allowlist declares autopilot_helpers' scrape-failure log safe, and\nthat reasoning runs three hops: the error comes from engine::run_boards, which\nreaches every board through scraping::http::fetch_text/fetch_json, whose\ntransport-error branch already strips the url. The last hop holds only while no\nboard takes the Browser arm, and nothing enforced it.\n\nSo the first browser-mode scraper would have silently re-opened a url leak a\nreviewer had already signed off as impossible, with no test anywhere to notice.\nThe assertion is on the registry rather than in the allowlist because that is\nwhere the property actually lives.\n\nMutation-checked by inverting the predicate: the test fails and names all 24\nboards.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(onboarding): settle a model pull whose terminal event raced its adoption\n\nPR #1036 review finding: useJobEvents drops a job.completed/job.failed\nthat arrives before pullJobId commits (jobs.list is an IPC round trip),\nand ai.pull_model fires that event exactly once. The reattach effect now\nre-reads the job's own status right after adopting it and settles\nimmediately, instead of depending on catching the event at the right\nmoment. The commit into the ref backing that check is synchronous too:\nan already-resolved promise can unwind entirely through microtasks\nbefore a setState-triggered render ever gets a scheduler turn.\n\n* test(scripts): flag a positionally-passed error.to_string() in the leak scanner\n\nThe scanner caught a bare positional `e` but not `e.to_string()` — the same\nleak, one method call away from detection. `.to_owned()` has the same shape.\n\nMeasured before widening rather than after: there are currently ZERO such sites\nin the crate. All 93 `.to_string()` occurrences near a log macro are the\nsanctioned `sanitize_reason(&e.to_string())` wrapper. So this is a tripwire for\na shape nobody writes today, not a fix for a live leak, and the doc comment\nsays exactly that so a later reader does not infer a leak was found here.\n\nThe wrapper is excluded structurally, not by string matching: the whole\ntop-level argument is tested, and `sanitize_reason(&e.to_string())` is a\ndifferent call from `e.to_string()`. Pinned by its own test, because a pattern\nthat flagged the sanctioned fix would light up ~100 correct sites and turn the\nguard into noise nobody reads.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(desktop): share updater download-progress metrics across instances\n\nOnly status moved to the module-level store; downloadSpeed/downloadedBytes/\ntotalBytes/timeRemaining stayed per-instance useState, so a remounted panel\nshowed the right label over a blank/zero progress readout for one tick.\nByte counts now derive straight off the shared status (downloading already\ncarries downloaded/total); speed/time-remaining move into the same shared\nsnapshot with their rate history, so a fresh mount sees the real numbers\nimmediately instead of restarting a blank history.\n\n* docs(updater): stop asserting the banner never unmounts\n\nThe shared rate history is safe because a listener stays alive for the whole\ndownload, and the comment justified that with \"the banner never unmounts\".\nIt nearly does: __root.tsx renders it inside ProtocolVersionGate, which swaps\nin an ErrorState instead of children on a protocol mismatch, so every listener\ncan go away at once.\n\nThe consequence is bounded — one speed reading computed against a stale\ntimestamp after the gate reopens, then self-correcting — and only reachable\nfrom a state where the app already shows a fatal error, so it is not worth\nguarding. Recorded rather than fixed, because this same always-mounted\nassumption has now been stated twice on this PR and was imprecise both times.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(scraping): sanitize the autopilot board-failure log, drop the chokepoint proof\n\nRound-2 review on #1036 found the chokepoint argument for autopilot_helpers/mod.rs:151's\nlog-error-leak exemption was unsound: it reasoned every board reaches the network\nthrough scraping::http::fetch_text/fetch_json because every registered scraper reports\nScraperMode::Http, but that mode is declared transport, not enforced transport.\nLinkedInScraper reports Http and still bypasses the chokepoint two ways: its\nLinkedInHttpClient sends raw reqwest requests whose failures convert via the un-redacted\nFrom<reqwest::Error> impl, and boards/linkedin/mod.rs's resolve_geo_id calls\nnet::http::shared() directly. Tracing it further: a first-page search failure surfaces\nthat raw AppError::Network(url) as BoardScrapeSummary.error, which used to reach this\nlog line as a bare positional {err} with nothing between it and the reader — a real leak\nof the user's free-text search keywords/location, not a hypothetical one.\n\nRemoves the need for the argument instead of repairing it: sanitize_reason(err) at the\nlog site now redacts paths/URLs/hosts/credentials generically, regardless of which\ntransport produced the string, matching the identical pattern already used two lines\nbelow (scrape_diagnostics) and by board_health's clean_error. The ALLOWLIST entry for\nthis site is dropped — the log call no longer interpolates a bare error identifier, so\nthe scanner no longer flags it at all.\n\nDeletes every_board_runs_through_the_http_chokepoint (scraping/boards/mod.rs): it\nasserted the declared ScraperMode, not the transport actually used, so it was coverage\ntheatre for the property that mattered. Nothing depends on \"every board is Http\"\nanymore — the 24 httpChokepointSafe() allowlist entries were independently re-verified:\neach is traced to scraping::boards/aggregator's own fetch_text/fetch_json call, none\nsits on a LinkedIn path, so their soundness never rested on this test's property either.\n\nConsidered a mechanical guard scanning scraping/boards/**+scraping/linkedin/** for raw\nreqwest::Client/.send() (mirroring check-log-error-leaks.mjs's shape). Viable, but not\nbuilt here: nothing currently depends on the invariant it would encode (LinkedIn's own\ntwo log sites are already independently sanitized), and it would need real CI/pre-push\nwiring plus a paired test file to be more than dead weight — out of proportion to this\nreview-round fix. Left as a follow-up candidate, not built as a silent no-op.\n\nMutation-checked by execution: reverting the sanitize_reason(err) call makes\ncheck-log-error-leaks.mjs fail loudly on the undeclared site again; restoring makes it\npass. cargo fmt --check / clippy --all-features -D warnings / cargo test --lib\n(4357 passed) all green.\n\n* fix(jobs): key ai.pull_model's exclusivity on the model, not just the kind\n\nReview finding on PR #1036: job_start_exclusive dedupes ai.pull_model by\nKIND alone, so a pull of \"qwen2.5\" while \"llama3\" is downloading silently\njoined the llama3 job and returned its id -- the renderer would then watch\n(and eventually believe it finished) a download it never asked for, while\nthe requested model never started at all.\n\nJobTracker::start_exclusive_keyed groups on (kind, key) instead, stamping\nthe key onto the new job's payload so it round-trips through jobs_list/\njobs_get. Same model still joins the running job (unchanged behaviour);\na different model is refused with AppError::Validation naming the model\nactually in flight, rather than silently adopted -- two concurrent\nmulti-GB pulls would compete for the same bandwidth and disk anyway.\nclaim_embed_job/EMBED_JOB_KINDS keep using the untouched start_exclusive.\n\nFor the renderer's reattach effect in useModelPull.ts: it must match on\njob.kind === 'ai.pull_model' AND job.payload?.model === selectedModel,\nnot kind alone, or a remount while a different model is pulling will\nstill adopt that job.\n\n* fix(onboarding): stop the model-pull reconcile read re-adopting itself\n\nPR #1036 review finding (MAJOR): the reconcile read's terminal branches\ncall resetTracking(), which clears pullJobId and re-triggers the reattach\neffect. jobQueue's cached job-list snapshot has no reason to refetch on its\nown, so it still reports the job as 'running' — the effect re-adopts it,\nre-reads jobs.get, and fires the success/failure toast again, looping at\nthe rate of one redundant IPC round trip per cycle until an unrelated job\nevent happens to invalidate the cache.\n\nTrack already-settled job ids in a ref and exclude them from the adoption\npredicate, so a stale snapshot can no longer re-adopt a job this hook\ninstance has already reconciled. finishOk/finishFailed now take the\nsettling job's id to record it. Tightened the two race tests from\ntoHaveBeenCalled() to toHaveBeenCalledTimes(1) so a regression of this loop\nfails them again.\n\n* fix(onboarding): restore the mounted ref on effect setup, not cleanup only\n\nPR #1036 review finding (MINOR): React StrictMode (the production app\nrenders inside it) double-invokes an effect in development - setup,\ncleanup, setup - while preserving hook state between the two setups.\nmountedRef was set true only from the initial useRef and back to false by\nthe cleanup, with nothing restoring it on the second setup, so after\nStrictMode's dance it stayed permanently false and the reconcile read's\nguard silently dropped every settle for the rest of the hook instance's\nlife.\n\nVerified real double-invoke does NOT happen through renderHook's wrapper\noption even when it renders a StrictMode element - only testing-library's\nown reactStrictMode render option triggers it; the new test uses that\noption, not a hand-rolled wrapper.\n\n* test(onboarding): assert the live listener actually fired before trusting it\n\nPR #1036 review finding (MINOR, but the important one): in both reconcile\nrace tests, handler stays null if jobs.onEvent is never called, so\nhandler?.(...) is a silent no-op - yet both tests still reached their\nexpected terminal state, because jobs.get alone (the reconcile path) is\nenough to settle it regardless of whether the live event was ever\ndelivered. The tests were \"passing\" without pinning the ordering their\nnames describe: a broken or removed live-event subscription would not have\nfailed them.\n\nAssert the listener was actually registered (jobs.onEvent called, handler\nnot null) before firing it in either test, so a regression there fails\nloudly instead of silently degrading to the reconcile path alone.\n\n* fix(onboarding): key the reattach adoption on model, not just job kind\n\nConcurrent rust-backend-author fix: ai_pull_model's exclusivity moved from\njob_start_exclusive (keyed on kind alone) to job_start_exclusive_keyed\n(keyed on kind + model), stamping the model onto the job payload from\nstart. Two different models can now run concurrently, so this hook's\nreattach effect - which previously matched any job.kind === 'ai.pull_model'\n- would adopt a running pull of a DIFFERENT model than the one the panel\nshows and render its progress under the wrong card. Refusing a second pull\nat the command only guards a fresh handlePull call, not this registry scan.\n\nFilter the adoption predicate on payload.model === selectedModel too, so a\njob that fails the check is never adopted at all rather than adopted and\nreconciled away.\n\n* fix(jobs): replace start_exclusive_keyed's stringly result with an enum\n\nR6 flagged Result<Option<String>, String> for a call that never fails: Err\ncarried the OTHER active model's name, not a diagnostic. KeyedExclusiveStart\n(Started/Joined/Busy) says what it means at the call site and drops the\nResult convention a reader had to know to decode it. Behaviour, the busy\nmessage, and the payload shape ({\"model\": \"<name>\"}) are unchanged.\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-18T23:42:29+02:00",
          "tree_id": "2e855779e23d2cf9f2d743127d7990001bbff856",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/f28b23d5410cefbaac99d95631b8810c9be2a5db"
        },
        "date": 1787090117659,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1754061,
            "range": "± 27878",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2106773,
            "range": "± 31461",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 251196,
            "range": "± 2593",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "b10b045222a0eef53ed290bc494dae57464b9807",
          "message": "fix: stop a false language critical and read today as a date column (#1038)\n\n* fix(documents): recognise today/actualidad/oggi/aujourd'hui as date columns\n\nis_date_only used exact-token equality against PRESENT_MARKERS, which never\ncarried English \"Today\" or its Spanish/French/Italian equivalents, so an\nentry line ending in one of those spellings failed to open its own role and\nhad its header and bullets absorbed into the entry above it.\n\nAdded a separate DATE_ONLY_MARKERS list consumed only by is_date_only rather\nthan extending the shared PRESENT_MARKERS: that list is also matched by\nis_open_ended and validate::content::factual via single-word, no-structure-\nrequired matching, and \"today\" is common enough English prose vocabulary\nthat adding it there would risk turning ordinary bullets into false date\ncontexts (confirmed the same failure mode already live for \"actual\").\nis_date_only requires every token on the line to be a digit, month or\nmarker, so exact-token equality against a whole date column is safe by\nconstruction regardless of what the list holds.\n\n* fix(documents): widen date-only markers to the currently/presente family\n\nDATE_ONLY_MARKERS only covered the \"today\" spelling family, so English\n\"Currently\" and the Iberian/Italian \"Presente\" spelling — both ordinary\nCV date-column endings, and Presente reachable via the es/pt/br locale\nprofiles shipped in #1009 — still failed to open their own role.\n\nExtended the list with the \"currently\" family (currently, presente,\nhoje, atualmente, hoy, actuellement, attualmente, derzeit, vandaag),\nkept as literal spellings rather than a shared stem: this const is\nalready read by a whole-line gate, and nothing stops a future caller\nreading it against free text the way PRESENT_MARKERS is, so a prefix\nrule would reintroduce the exact hazard the list is separate to avoid.\n\"nu\" (Dutch/Swedish \"now\") is deliberately left out and documented —\ntwo letters collides far more readily than the rest of the list, and\n\"heden\" already covers the Dutch case.\n\nMeasured the tenure.rs path for the same gap (2020 - Atualmente):\ncareer_span_years still reads it as ongoing, because source_is_ongoing\nis structural (year + separator + non-year tail) rather than\nvocabulary-driven, so no second defect exists there.\n\n* fix(validate): require function-word evidence before a language critical\n\nwhatlang's confidence() is a top-1-vs-top-2 margin, not a correctness\nprobability: a noun-phrase-heavy block (an ordinary skills line, a terse\nCV) starves its n-gram model of the closed-class function words it needs\nto read a language from at all, so it can land on the wrong Latin-script\nlanguage with maximum confidence and is_reliable() == true.\n\nis_language_mismatch now also requires collision-pruned, distinctive\nfunction-word evidence of some other curated language before a confident\nLatin-script read becomes an accusation. Non-Latin scripts are unaffected\n- whatlang already reads those reliably regardless of function-word\ndensity. The per-section pass now routes through the same function\ninstead of a duplicated check, closing the same gap at both scopes.\n\n* fix(documents): add spanish actualmente, harden the date-marker tests\n\nSpanish resumes writing an open-ended column as \"Actualmente\" (one\nletter off Portuguese \"atualmente\", already present) reproduced the\noriginal defect: the header and bullets absorbed into the entry above.\nAdded the missing spelling and recorded the multi-word-column boundary\n(en la actualidad, en cours, ad oggi, bis heute, etc.) as a known,\ndeliberate limitation rather than a silent gap.\n\nTest hardening from review:\n\n- table test now collects every failing spelling instead of asserting\n  inside the loop, so a future dropped/typo'd marker is reported by\n  name instead of being hidden behind the first failure\n- dropped the paired \"prose\" loop: every row only passed because the\n  tokenizer gate rejects ordinary sentences before any marker is\n  consulted, so it proved nothing about DATE_ONLY_MARKERS specifically\n  and duplicated a_date_column_needs_more_than_a_year_in_it under a\n  misleading name\n- added a one-line disjointness assertion so DATE_ONLY_MARKERS and\n  PRESENT_MARKERS never silently start sharing a spelling, which is\n  the entire safety argument for keeping the two lists separate\n\nDocumented a second safety property the review surfaced: since no\nDATE_ONLY_MARKERS entry is ever added to PRESENT_MARKERS, a line built\nentirely from digits/months/DATE_ONLY_MARKERS tokens still can't pass\nis_date_only without a real year or a genuine PRESENT_MARKERS marker,\nbecause looks_like_date_span never consults the new list.\n\n* fix(validate): redesign language evidence as pairwise, not a global pool\n\nThree review findings, all confirmed by measurement:\n\n- a single function-word pool pruned across all seven curated languages\n  left Spanish (30 survivors) and Portuguese (33) too thin to evidence\n  themselves, so a genuinely Spanish/Portuguese document against another\n  target silently stopped raising a real Critical. Evidence is now\n  pairwise per comparison (found vs target), not globally pruned.\n- short tokens (ci, vi, io, ha, da, ti, os, em, am, im, zu, el, na, ai,\n  et, au) collide with common English abbreviations/TLD fragments in\n  free text; closed with a named denylist plus curating \"per\" into the\n  English list, rather than a blanket length floor (which was tried\n  first and measured to also exclude Italian's own short core\n  vocabulary, briefly regressing a true positive).\n- the section-scoped pass had its own untested detected_language\n  comparison; it now routes through the same is_language_mismatch the\n  document pass uses.\n\nAdds a comparative margin (found's evidence must exceed target's own in\nthe same text) and a title-case-sandwich exclusion (a connector wedged\nbetween two Capitalised words is almost always part of a proper noun's\nown official name) to separate \"an English document naming a foreign\ninstitution\" from a genuine mismatch, without reopening the Spanish\nregression. Also fixes the tr/vi corroboration gate to key off Latin\nscript rather than curated-vocabulary membership, closing a\nzero-evidence Critical hole.\n\n* fix(validate): dedupe the portuguese word list, add a duplicate guard\n\nA wider sweep for the zijn-in-NL class of bug found a second instance:\n\"a\" and \"se\" each listed twice in FUNCTION_WORDS_PT. Harmless under the\ncurrent HashSet-based evidence count, but a silent authoring mistake.\nAdds a permanent test asserting no curated list has an internal\nduplicate, so a third one fails loudly instead of by inspection.\n\n* fix(validate): remove two unproven language-evidence suppressors\n\nDelete AMBIGUOUS_SHORT_TOKENS and distinctive_evidence_confirms's\ncomparative margin after measuring, for each, that no realistic\ndocument needs it: disabling either left every validate::content\nbehavioural test green, and only the mechanism's own manufactured unit\ntest went red. The denylist's one measured trigger required an\nadversarial string of disconnected foreign idioms with no connecting\nEnglish grammar, a shape this validator's real input never produces,\nat a real cost - it permanently denied el/zu/na/et/di/ci, high-\nfrequency function words of the exact languages the pairwise fix was\nwritten to protect. Removed on the strength of measurement, not kept\non the strength of the argument for them, per this module's own\nhistory.\n\nReplaces the deleted unit tests with ones for the mechanisms that\nremain (the single-letter floor, the absolute evidence floor) and\nrecords the measurement in the module doc so a future edit does not\nre-add either suppressor without re-measuring.\n\n* fix(validate): replace the title-case sandwich with a measured floor\n\nThe title-case-sandwich exclusion in pairwise_evidence_count deleted\nGerman's evidence wholesale: standard German CV register is nominal\n(\"Aufbau und Betrieb der Zahlungsplattform\"), German capitalises every\nnoun, so every connector in that register sits between two capitalised\nwords whether or not it is inside a proper noun. Both the document and\nsection passes route through the same function, so this was a single\npoint of failure with no second line of defence, and it also reopened\nthe ats/consistency false-positive both already suppress on a language\nmismatch.\n\nDeletes the exclusion outright rather than special-casing German - a\nshape assumption has now been wrong for Spanish, unfalsifiable for the\ndenylist, and wrong for German, in three straight rounds. Replaces it\nwith a threshold, re-derived from a corpus swept fresh for this change\n(not carried over from an earlier number): quiet fixtures top out at\n4, fire fixtures start at 6, so MIN_DISTINCTIVE_HITS = 5 separates\nwith a full unit of margin either side. Reports the residual risk\nmeasured at the boundary (an institution-dense English document can\nstill exceed it) rather than hiding it behind a shape rule.\n\nAdds the missing nominal-register German fixture at both document and\nsection scope, a Title-Case French fixture, and mutation-checks the\nfloor at the whole-suite level in both directions. Cross-references\nthe two same-named FUNCTION_WORDS_DE/function-word primitives in\ndocuments::evidence and validate::content::language so a future reader\ndoes not merge them.\n\n* docs(validate): correct the min_distinctive_hits corpus table and pin two gaps\n\nRound-5 follow-up to bc23ed43, docs and coverage only — no threshold or\nmechanism change.\n\n1. The residual-risk paragraph on MIN_DISTINCTIVE_HITS named German's\n   institution-stacking case as the worst boundary (five institutions,\n   evidence 8). A fresh sweep of French/Italian/Dutch/German with real\n   institution names (same shape, one per line, en target) shows German\n   is actually the BEST case: even six real German names stay at\n   evidence 3, well under the floor. French is the genuinely thin one:\n   two real names (INRIA, CNAM) reach evidence 6 at 144 chars, TYING the\n   thinnest genuine positive already in the table. Italian crosses at\n   five names, landing exactly on the floor (5). Dutch, stacked to the\n   same six-name shape as German, also never crosses. Rewrote the doc\n   comment with all four measured rows and the honest claim: the margin\n   holds for German-shaped proper nouns, not for French or Italian ones.\n\n2. Pinned the close-relative-target silent band as an accepted miss, the\n   same discipline this file already applies to the DACH miss and the\n   tr/vi miss. Measured: a genuine 120-190 char two-sentence paragraph in\n   one Romance language carries far less PAIRWISE evidence against a\n   close relative than against a distant target (es-vs-fr: 1; pt-vs-es/\n   fr/nl: 4 each — all under the floor) while the SAME text fires\n   cleanly against en (6). This repo ships es/pt/it/nl locale profiles,\n   so this is a real, supported scenario. New test:\n   a_short_paragraph_against_a_close_relative_target_is_an_accepted_miss.\n\n3. per_language_samples()'s round-4 3->7 rep bump for all seven Latin\n   languages was load-bearing for the worst pair (es-vs-fr) but left the\n   only non-English-target sweep coverage exclusively at 441 characters,\n   outside the 120-315 char band the floor actually decides in - exactly\n   the gap that hid item 2. Reduced Spanish specifically back to 3x (189\n   chars, in-band); swept reps 1-7 against all six other curated targets\n   first to confirm only the es-vs-fr cell goes silent at that length\n   (fr: 3, en/de/it/pt/nl: 18/18/15/9/6, all still clearing the floor).\n   every_curated_language_is_silent_for_itself_and_fires_for_every_other\n   now excludes that one cell explicitly, tied to the new accepted-miss\n   test rather than hidden by re-inflating the sample.\n\nVerification: cargo fmt --all -- --check, cargo clippy --all-features\n--locked --all-targets -- -D warnings, and cargo test --all-features\n--locked all pass (4378 passed, 0 failed). Mutation check performed:\ntemporarily removed the new es/fr exemption from the cross-product\nloop - it went red on exactly that cell (\"a document confidently\nwritten in es must mismatch target fr\") - then reverted.\n\ndocuments/evidence/entry.rs untouched, verified via git status before\nstaging.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(validate): update stale sandwich-exclusion docs, pin replace premises\n\nTwo comment blocks in the language-mismatch test module still described\nthe removed title-case-sandwich exclusion in present tense and pointed\nat a renamed test; rewrite them to name MIN_DISTINCTIVE_HITS as the\ncurrent mechanism and fix the dangling doc reference.\n\nAlso add a premise assertion after the two EN_CLEAN.replace() calls this\nbranch introduced so a future fixture rewording that no-ops the replace\nfails loudly instead of silently asserting silence on an unmodified\nEnglish fixture.\n\n* test(validate): pin the connector-sparse german miss as accepted\n\nMIN_DISTINCTIVE_HITS's floor cannot separate an English document that\nmerely names a foreign institution from a genuinely foreign document\nwritten in a connector-sparse register (standard German CV participle\nstyle drops the article and finite-verb clause a sentence would need) -\nboth land in the same 0-4 evidence band. Measured a whole, genuinely\nGerman resume in that register at evidence 3 on 695 significant chars,\ntwo hits under the floor and quiet through both is_language_mismatch\nand document_language_mismatch (the same fn the draft-retry gates on).\n\nDocuments this as the third accepted miss (after the uncurated tr/vi\nmiss and the close-relative-target Romance miss), corrects the corpus\ntable's \"quiet tops out at 4\" conclusion to say the quiet class holds\ntwo different shapes rather than only the correctly-quiet one, and\npins the fixture with a·test alongside the existing accepted-miss\ntests so a future recall fix has something concrete to turn green.\n\nNo mechanism, threshold or curated word list changed.\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-19T13:40:31+02:00",
          "tree_id": "fb93a1fbfbbd7ba39c3fc652c7fa1e1be091a35e",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/b10b045222a0eef53ed290bc494dae57464b9807"
        },
        "date": 1787140967823,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1690366,
            "range": "± 31823",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2045030,
            "range": "± 49090",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 185456,
            "range": "± 5380",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "74a54b44c9c6ffa29d0890fdd2593012239ae742",
          "message": "feat: job-ad score surfaces, and email tracking v2 with opt-in auto-write (#1056)\n\n* feat(ui): add a score tab to the job-ad view\n\nThird tab in JobAdView showing how the stored resume scores against the\nposting, read from the same match cache the Jobs page uses. No new scoring\npath: it routes through the existing useJobMatchScore hook and cache key.\n\nNever labelled \"ATS score\" - the analyzer emits an LLM judgement while\nMatchScore.ats is deterministic keyword coverage, so a shared label would\nmake both numbers look wrong. Every unmeasured value renders a stated\nreason rather than a zero, and a genuine 0% still renders 0%.\n\nMatchScore.scoreSource is added to the TS type only; the Rust kernel has\nalways emitted it, and it is the one honest signal that semantic ran.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(email): classify application-email intent from the body, not the subject\n\nFour-way classifier (confirmation, rejection, interview, offer) over a\n173-phrase corpus that survived an adversarial pass killing 229 of 402\nproposals. Nothing writes a status yet: this is the correctness constraint\nparser.rs's own known_false_positive tests demand before any auto-write.\n\nA real ATS rejection reuses the confirmation subject line from earlier in\nthe thread, so the subject cannot separate them - the discriminating signal\nis in the body. Negations live inside the patterns, never inferred: \"moving\nforward with your application\" is an interview invite and only \"not be\nmoving forward with your application\" is a rejection.\n\nRejection wins whenever it fires alongside another intent, and only a\ndiscriminating phrase can decide one. The three known_false_positive tests\nnow assert the correct intent instead of documenting an accepted risk.\n\nAlso lands the pure ladder rule the write slice will consume: a late\nconfirmation arriving at Offer is a no-op, never a downgrade.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(email): scan enough of the body to see a rejection phrase\n\nIntent classification was bounded by BODY_SNIPPET_BYTES (500), a constant\nsized for the cheap fingerprint gate that decides whether a message looks\nlike an application email at all. Intent classification is a different job:\na real ATS rejection opens with a greeting and thank-you boilerplate, so\nits discriminating phrase routinely sits past that mark, and missing a\nrejection is the failure this classifier exists to prevent.\n\nINTENT_SCAN_BYTES (20_000) replaces it, sized to cover a realistic ATS\nemail while still bounding a hostile body. Verified the bound is reachable:\nthe wire-level cap is MAX_BODY_BYTES (200_000) via a partial-octet FETCH,\nso nothing upstream was truncating near 500 bytes.\n\nThe wider window makes one intent worse and that is recorded, not hidden:\na stale quoted offer phrase from an older thread message can now outrank a\ncurrent interview phrase, since the tie-break has no positional awareness.\nRejection is unaffected - it wins first, position-independently.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(match): score a resume against job-ad text over ipc\n\nThe Score tab had no reachable input: TailorFlow receives an Application or\nan AutopilotFoundJob, match.resume needs a PostingsCache id, and that cache\nis RAM-only and transient by design, so a saved application could never\nhave an entry. JobAdView already holds the posting as text.\n\nmatch.text scores a stored resume against that text through the same\nscore_one kernel, so the surface cannot drift from the Jobs page. A new\nMatchSurface::JobAdText translates like JobsPage rather than reusing the\nextension's keyword-only path, whose zero-egress guarantee is structural\nbecause its caller is the untrusted browser bridge.\n\nParity is pinned by a German-to-English fixture driven through both\nsurfaces: a same-language fixture would have kept a regression to\nnon-translating invisible. Job text is content-addressed for the cache key\nand clamped at the command boundary, since serde does not enforce the zod\ncap.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(ui): score the job-ad text the tab actually holds\n\nSwaps the Score tab off the dead match.resume path onto match.text. The\nold wiring could never render a number: it needed a PostingsCache id that\nno TailorFlow caller carries, so jobId was undefined at every call site\nsince the commit that introduced it. Deleted rather than left wired.\n\nThe new query key is content-addressed on the posting text and the source\ntab is an editable textarea, so live text would mint a key per keystroke -\nand this surface translates, which can reach a local model. The tab\nsnapshots the text when it is opened and scores that, so typing cannot\nfire a request. Pinned by asserting the hook's actual enabled-call count\nagainst absolute numbers.\n\nEmpty states keep stating a reason instead of showing a zero, and a\ngenuine 0% still renders 0%.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(email): write an email-derived status as provisional, adjudicable\n\nstatus_events gains source and confirmed via an appended migration, so an\nemail-derived transition is recorded as unconfirmed and the user can accept\nor reject it from the timeline. Nothing calls this yet: apply_matched_intent\nhas no caller outside tests, so ADR-0013's notify-only guarantee still holds\nin shipped behaviour until the provenance gate lands.\n\nThe compare-and-set primitive is reused, never reimplemented, so a reject\ncannot clobber a status the user changed by hand in the meantime - the\nprovisional row is marked reviewed instead. The table stays append-only: a\nreject appends a reversal rather than editing history, because the trail has\nto show that an email got it wrong.\n\nstatus_events moves to its own module: the additions pushed applications\nmod.rs to 1554 lines against the 1400 hard cap, following the reminders and\nmigrations precedent already set in that file.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(ui): stop the score tab laundering failures into \"not scored\"\n\nA failed match:text call settled at data undefined and fell through to the\nsame \"not scored\" copy as a legitimate empty state, so a user could wait\ntwo minutes for a failure and be told what an empty posting is told. It now\nrenders a distinct error state with retry.\n\nA malformed result rendered NaN% under a red Low badge; the regression test\nreproduced it on both metric rows before the fix, since a keyword-only\nscore always has combined equal to ats. The Match row only renders when\nsemantic actually ran - it was printing the same number as Coverage under a\ndifferent band, so 60% read MEDIUM above HIGH on one value.\n\nThe fixture that hid this was impossible: ats 60 with combined 55 cannot\noccur on a keyword-only run.\n\nGerman labels now reuse the autopilot keys for these same fields instead of\nminting a second translation of one engine's numbers. The loading state\nannounces itself, the kernel's explanation and top gaps are rendered\ninstead of discarded, and the storm guard counts distinct query keys.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(email): normalize before matching, and never auto-write from a cold sender\n\nPhrase matching was byte-exact contains against un-normalized mail, so it\nfailed OPEN - the direction that auto-marks a rejected application as merely\nconfirmed. Two reviewers measured it independently: a 72-col wrapped body\nclassified None at every width, and across wrap offsets 40% of rejections\ncame back Confirmation. NBSP from html-to-text, curly apostrophes and NFD\naccents each defeated it too, the last killing 16 of 16 accented\ndiscriminating rejection phrases.\n\nMatching now strips quote markers, joins wrapped lines, collapses whitespace,\nfolds typographic apostrophes and composes NFC - on both the haystack and,\nonce at build time, the needles. Every defect was reproduced as a failing\ntest before the fix.\n\nRejected was absorbing while attribution was attacker-supplied, so one cold\nemail could freeze an application forever and silently stop tracking it. An\nauto-write now requires a recognised sender, and a terminal status that was\nitself an unconfirmed email write can be superseded; a user-set one still\nabsorbs.\n\nThe corpus parse falls back to empty instead of aborting the process, and the\nguards that could not catch what they existed for - a bound derived from the\nconstant it tested, a shape test blind to the discriminating flag - now pin\nliterals.\n\nunicode-normalization was already in the tree via typst and pdf-extract, so\nthis adds no new supply-chain surface.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs(adr): amend adr-0013 for adjudicable email auto-write\n\nRecords the v2 relaxation of \"version 1 = notify + confirm, no auto-write\":\na matched intent writes immediately but always unconfirmed, adjudicable from\nthe timeline, gated by a separate toggle defaulting on. The unconfirmed row\nand the accept/reject affordance are the mitigation that replaces ask-first\nconsent - the toggle is an escape hatch, not the safeguard.\n\nStates plainly that auto-write is infrastructure only: apply_matched_intent\nhas no non-test caller, so shipped behaviour is still v1 notify-only.\n\nDocuments what the record did not previously describe: an interview intent\nskips straight to interviewing, terminal statuses absorb unless they were\nthemselves unconfirmed email writes, a cold sender never writes, and the\nrecall gaps are stated per intent rather than only for German. Zero\nemail-content egress is unchanged and, if anything, stronger - the\nclassifier is a local phrase table, not a model call.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(match): feed the score tab the same pre-processed text as the jobs page\n\nmatch.text passed the posting straight into score_one while the Jobs page\nruns it through posting_text_blob first, which strips markdown, prepends the\ntitle and appends requirements. Postings really are markdown - both the\nadzuna and freehire adapters run html_to_markdown before storing - so an\ninline link became the job keywords https, acme, example and com, inflating\nthe coverage denominator with tokens no resume can contain. Same posting,\ntwo different percentages under one label.\n\nThe text now goes through the same blob builder before it is hashed and\nscored, so the content-addressed cache key covers the pre-processed form\ntoo. A test feeds one markdown description to both paths and asserts the\nblobs are byte-equal.\n\nThe parity test compared two derived values with nothing anchored, so a\nkernel that degraded on both sides would have stayed green; it now asserts\nan absolute floor on one side first.\n\nThe type doc claimed the surfaces never differ in pre-processing. They still\ndiffer on two axes - this surface has no title or requirements, and semantic\nis hardcoded off here but follows the user's preference on the Jobs page, so\ncombined is two formulas under one name. Documented instead of re-asserted.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(email): wire the auto-write and expose accept/reject over ipc\n\napply_matched_intent now has a real caller, gated toggle then sender\nprovenance then intent - each a silent no-op, each with its own test. The\ncall sits in the scheduler, not the poller: the layer table enforced by CI\nputs the AppHandle reach there and keeps the poller pure. That was checked\nagainst tests/architecture.rs rather than against the ADR's prose.\n\nAccept and reject reach the timeline through the 5-step contract flow, both\nfactored through a pure core so the command path and the direct store call\nare comparable in tests. The auto-write toggle joins the email-watch status\npayload so settings needs no second round-trip.\n\nconfirmed = 1 is still only ever written by a human-triggered accept, never\nby the write path.\n\nScheduler failures log an error code, never the error text, so email content\ncannot reach a log through a new surface.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(ui): adjudicate email-derived status changes in the timeline\n\nAn email-derived row renders as provisional with accept and reject, a\nreversal renders as a correction, and everything else renders as before.\nThe timeline tab is now its own component so the mutations mount only when\nit is visited.\n\nReject never claims the status was reverted. It may legitimately only\ndismiss the row - the compare-and-set refuses to clobber a status the user\nchanged by hand - so the toast stays neutral and the outcome is read off\nthe refetched events: a correction row if the revert won, a simply-settled\nrow if it did not.\n\nThe backend's reversal note is not rendered; it is English and would bypass\ni18n, so the localized badge carries the meaning instead.\n\nThe auto-write toggle says what it costs: a matched email changes the\nstatus right away, before confirmation, and the timeline is where that gets\naccepted or rejected. It defaults on, so the copy makes that bargain\nvisible rather than burying it.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* refactor(shared): keep the status-event sources in one place\n\nThe renderer re-declared 'email' and 'email_reject' locally because the Rust\nconstants are pub(crate). That put the values in three places, and a rename\nwould have failed silently: provisional rows would render as settled\nhistory and the accept/reject affordance would vanish, with a green suite\nreporting nothing. Adjudication is the whole safety model for auto-write, so\nlosing it quietly is the worst available outcome.\n\nThe values are exported once from shared and imported by the renderer. A\ntest pins each against a hand-written literal rather than against the other\nconstants, so renaming them in lockstep still fails.\n\nsource stays a free-form string on purpose - an unrecognised source must\nkeep falling through to normal rendering - so these are a comparison set,\nnot a closed union.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(email): match every application the ladder can act on, not just saved\n\nThe matcher only ever considered applications at Saved, so the auto-write\ncould produce nothing but Saved to X. A rejection, interview or offer for an\napplication already at Applied or Screening never matched, which left most\nof the ladder unreachable and v2 delivering little more than v1.\n\nWorse, it quietly undid the absorbing-Rejected fix: a terminal status that\nwas itself an unconfirmed email write is allowed to be superseded, but a\ncorrecting email could never reach that rule because the matcher dropped the\napplication first.\n\nBoth layers now read one shared predicate - live, or an unconfirmed email\nwrite - and two tests pin each layer against that predicate directly rather\nthan against each other, so they cannot drift into agreeing on the wrong\nthing.\n\nWidening was checked as the shared-predicate change it is: notify was under\nthe same Saved-only restriction, so both callers improve. The wider\ncandidate pool does not need caller-side narrowing - a title-less email\nstill hits the existing exact-tie rule, and a title that favours one\napplication is the intended disambiguation - and that reasoning now lives in\nthe module doc instead of being implicit.\n\nThe ladder moves to its own module: widening pushed intent.rs past the\n1400-line cap, so it is a split, not an allowlist exception.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs(adr): correct adr-0013 now that auto-write is wired\n\nThe amendment was written before the wiring landed and said auto-write had\nno caller and shipped behaviour was still notify-only. That is no longer\ntrue: the scheduler calls it, gated by the toggle then provenance then a\ndecided intent.\n\nAlso records which applications are eligible, since the matcher no longer\nlooks only at Saved: any live status, plus a terminal one that is itself an\nunconfirmed email write. Excluding terminal at the matcher would have\nsilently reinstated the absorbing-Rejected problem the exception exists to\nprevent, so both layers read one shared predicate.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* test(applications): pin the event-source values on the rust side too\n\nThe renderer's half of this boundary is pinned in shared; this is the other\nhalf. Each constant is compared against a hand-written literal rather than\nagainst the other side, so renaming both in lockstep still fails - each\nliteral has to be edited independently for the suites to pass.\n\nThe pin looks trivial and is not: nothing checks these values across the\nboundary, so a rename fails silently. Provisional rows would render as\nsettled history and accept/reject would disappear, with a green suite\nsaying nothing.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(email): adjudicate the row the user clicked, and authenticate the sender\n\nTwo blocking findings from the security gate, both in the write path.\n\nAccept and reject resolved the newest provisional row rather than the one\nclicked, while the UI rendered a pair on every row with a label naming that\nrow's transition. Two provisional rows coexist on the ordinary happy path -\na confirmation, then a rejection on a later tick - so accepting \"saved to\napplied\" confirmed the rejection instead. Nothing un-sets confirmed, so the\nuser irreversibly ratified a rejection they never reviewed, by clicking a\nbutton labelled something else. The row identity now crosses ipc and the\nstore matches on it; both suites had missed this because every fixture built\nexactly one pending row.\n\nThe write gate was a From-header string comparison with no email\nauthentication fetched anywhere, so \"a cold sender never writes\" was false:\nthe listed relays carry attacker-authored text from their own valid\ninfrastructure, and a free tenant on a listed vendor sends from it directly.\nAuthentication-Results is now fetched and DMARC must pass aligned to the\nvisible From domain, failing closed when the header is absent. The domain\nlist is split so a score boost can never again double as authorisation.\n\nThis narrows rather than closes the free-tenant case, and auto-write will be\nrare on hosts that do not stamp the header - both documented rather than\nassumed away.\n\nAlso: a rejected verdict could be routed around through an intermediate\nstatus, tie-less ordering could adjudicate an arbitrary row on a millisecond\ncollision, the toggle rendered off while loading when the default is on, and\nan untranslated backend note reached the timeline.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(email): trust only the topmost authentication-results header\n\nThe DMARC gate scanned every Authentication-Results header in the message\nand accepted a pass from any of them. That header carries no privilege: a\nreceiving server prepends its own stamp and does not strip ones already\npresent, so an attacker could add \"dmarc=pass header.from=<vendor>\" to their\nown message and open the write gate by typing a line. Worse than the weak\nheuristic it replaced, because the code claimed authentication.\n\nOnly the topmost header is read now - the final receiving server prepends,\nso anything below it is an earlier hop or sender-supplied text. The ordering\nthis rests on was verified against mail-parser's source and then proved\nempirically with a real two-header message, since a source read can be wrong\nabout which path runs.\n\nA genuine fail above a forged pass now correctly refuses; that is the case\nthe old scan got exactly backwards.\n\nOne residual stays open and is documented rather than papered over: a lone\nforged header copying the expected authserv-id cannot be told from a genuine\none by reading text, and the authserv-id comparison that looks like it would\nclose it does not - the attacker writes that string too. Real defense is the\nprovider overwriting inbound headers that claim its identity, which happens\noff-device, or independent DKIM verification, which is a feature not a fix.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(email): stop a hostile header aborting the app, and gate on the host\n\nA header carrying any character whose lowercase differs in utf-8 length\ndesynced an offset taken from the lowercased string from a slice into the\noriginal, and a skewed index landing mid-character panicked. Release aborts\non panic and the uid watermark is written after the tick, so one message\ncould put the app in a permanent boot-crash loop that anyone able to email\nthe inbox could trigger. Slicing now happens only from the lowercased\nstring, so the class is gone by construction rather than by care, with the\nreproduction and three more width-changing cases pinned.\n\nAuto-write now also requires the account's own imap host to be one known to\nstamp authentication results. That is locally stored config no sender can\ninfluence, which is the reason the earlier \"unclosable\" note was wrong: it\nonly considered attacker-writable text. Closed for known hosts, still open\nfor unknown ones, now stated precisely instead of dismissed.\n\nReject's final update repeats the full conjunction its guarding select\napplies rather than keying on the rowid alone.\n\nThe scanner remains exploitable through attacker text echoed inside a\ngenuine header; that is recorded as a test asserting the vulnerable\nbehaviour so ci cannot report it as resolved.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(email): parse authentication-results instead of scanning it\n\nThe substring scan is deleted. It could not tell a property value from a\nproperty name, which is why a genuine gmail header could be made to read\npass when its real verdict was fail: an attacker sends from their own\ndomain with a quoted local part carrying \"dmarc=pass header.from=...\", and\nthe receiving server's authentic spf evaluation echoes that text verbatim\ninside its own header, ahead of the real verdict.\n\nA tokeniser closes that structurally. It tracks nesting comment depth and\nquoted-string state as it walks, so text inside a comment or a value is\nnever a token, and it matches the dmarc method and its header.from at token\nboundaries within one section. It reads chars rather than byte offsets, so\nthe abort-on-hostile-header class cannot return either.\n\nBuilding it surfaced three defects in itself, the sharpest being that the\nexploit reproduction did not parse at all: a quoted local part glued to an\nunquoted domain is one address value, and reading only one form left the\nrest unconsumed. That is the single input this module most needs to be\nright about.\n\nThe test that asserted the vulnerable behaviour now asserts the fix, flipped\nbecause the scanner was replaced rather than to make ci quiet.\n\nNo new dependency and no new egress; the no-egress promise is unchanged.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(email): treat a truncated parse as malformed, not as a verdict\n\nAn empty method token broke out of the section loop, and breaking returned\nwhatever verdict had already been found rather than nothing. So a single\nstray top-level semicolon - or any of five other stop characters - ended the\nscan early and returned an attacker's injected pass while the genuine fail\nsat unread below it.\n\nThe delivery is the primitive this module's own test already pins: a quoted\nenvelope local part is echoed verbatim into the server's authentic spf\ncomment, and a close paren inside it legitimately ends that comment, because\na quote has no meaning inside one. Everything after parses as real grammar.\n\nOnly end of input may now end the loop; anything else with input remaining\nfails closed. Every other loop exit in the module was audited rather than\nspot-checked, and the propspec and value loops were confirmed to have a\ndifferent, safe shape.\n\nA duplicate header.from within one section now fails closed on disagreement\nlike two disagreeing sections already did, so that safety stops depending on\na survey of how four providers happen to order properties.\n\nThe module doc claimed it failed closed on anything not confidently read.\nThat was the sentence a reviewer would trust instead of re-deriving, and it\nwas false; it now carries why.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(email): make auto-write opt-in and say why it is not airtight\n\nFive security rounds established that the sender check cannot be made sound\nby reading the email's own headers: a genuine stamp that carries no dmarc\nclause of its own is indistinguishable from an attacker's echoed text, and\ntwo candidate fixes were built and measured against that before being\ndiscarded. So the default changes rather than the claim.\n\nAuto-write now defaults off. The gate stops being load-bearing once nobody\ngets it without asking, and adjudication stays the backstop for anyone who\nturns it on. The migration is edited in place rather than superseded, since\nit has never reached main and there is no shipped value to preserve.\n\nThe settings copy states the trade in plain words - it changes statuses\nbefore you have looked, the timeline is where you accept or reject, and the\nsender check can be fooled - without naming a single protocol, because the\nperson deciding does not know what dmarc is.\n\nThe docs that called the residual closed are corrected. A host known to\nstamp headers proves it stamps a header, not that it stamps the clause the\ngate reads, and that reasoning survived three reviews because it was\nconfidently written.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs(adr): record the opt-in reversal and the live sender residual\n\nThe earlier amendment described auto-write defaulting on, with adjudication\nstanding in for ask-first consent. That is reversed: the default is off and\nopt-in is the primary safeguard, because five rounds established the sender\ngate cannot be made sound by reading the email's own headers.\n\nThe record now says what host_is_known_to_stamp actually proves - that a host\nstamps a header of that name, not that it stamps the dmarc clause the gate\nreads for every sender domain. A message can carry a genuine stamp with no\ndmarc clause of its own, and the attacker's echoed text then supplies the\nonly one. That reasoning was called closed and survived three reviews on the\nstrength of how confidently it was written.\n\nThe residual is recorded as live, with its mitigations: opt-in, every write\nunconfirmed and reversible from the timeline, and a cold sender never writes.\nClosing it needs verification that does not trust the header, which means a\ndependency and an egress class this feature deliberately does not have.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs(adr): name the write gate the code actually uses\n\nThe record described the sender gate as a domain_hint hit on the envelope\nFrom domain. Both halves were wrong: the gate reads write_gate_domain, the\nnarrower list that drops the relays, and the domain comes from the visible\nFrom header, not the smtp envelope.\n\nNaming domain_hint described the wider scoring list, which re-merges in the\ndocumentation exactly the two roles that were split so a scoring heuristic\ncould never double as an authorisation.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(ui): scope row pending state, and stop laundering accept errors\n\nAccept and reject read a single mutation's isPending and handed it to every\nprovisional row, so acting on one row spun and disabled the others. That is\nthe same \"which row am I acting on\" ambiguity the event id removed from the\nbackend, reintroduced visually. The flag is now gated on the in-flight\nevent id.\n\nBoth handlers treated any resolved value as success. The commands return a\nvalue rather than a result, so a storage failure resolves as an error object\nand onError never runs - a transient failure showed \"confirmed\" while the row\nstayed provisional. They now branch on the error first, the way the contact\nwrites in the same component already did.\n\nThe auto-write switch had an unreachable skeleton branch, and the test named\nafter it passed for an unrelated reason. Testing the optional chain directly\nlets the compiler prove the value is defined, so the dead branch is now\nimpossible rather than merely removed.\n\nThe section comment still said a match never auto-writes.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(email): close the opt-in gate, and make the write visible to the ui\n\nThe gate that decides whether auto-write runs at all read the toggle with a\nfallback of true, so a transient storage error opened it for a user who never\nopted in. That fallback was correct when it was written, because the default\nwas on then; flipping a default silently invalidates every fallback that\nencoded it. Every other unwrap in the module family was swept for the same\nshape and cleared.\n\nThe write mutated a status and appended a provisional row without emitting\napplications:changed, and it was the only backend-initiated application write\nthat did not. With focus and reconnect refetching both off, a mounted\ntimeline never refetched, so the accept and reject controls never appeared\nwhile the user was on the page - the backstop the design calls mandatory was\nabsent exactly when it was being watched.\n\nThe match notification announced every intent as a possible confirmation,\nwhich stopped being true when the matcher widened past saved applications.\n\nA factory reset preserved the auto-write opt-in, so connecting a different\nmailbox inherited consent that account never gave. Disconnect still keeps the\npreference; a privacy reset now clears it, and the reset wiring is tested\nrather than only the call it delegates to.\n\nDocs that said the opposite of the code are corrected, including one that had\naccept clearing the flag it sets. cargo doc found three more broken links\nintroduced earlier in this work that clippy cannot see.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(ui): track in-flight rows in state, not on a shared mutation\n\nThe per-row gate read the shared mutation's variables, which only ever hold\nthe most recent call, so starting a second row's accept cleared the first\nrow's pending state while its write was still open - the ui asserting a\nwrite had finished when it had not. No expression over one observer can\nrepresent two concurrent operations, so the component now holds a set of\nin-flight event ids, added before mutate and removed in onSettled so an\nerror clears them too.\n\nThe replacement test drives two genuinely overlapping clicks and was\nconfirmed failing against the pre-fix component. The old test could not have\ncaught this: it set globals the fixed component no longer reads, so it would\nhave gone on passing while testing nothing, which is the same shape flagged\none round earlier. That scaffolding is deleted rather than left green.\n\nThe accept hook's doc claimed it clears the confirmed flag it sets.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(email): clear the auto-write opt-in on disconnect, and stop saying accept clears\n\nPreserving the opt-in across a disconnect was justified by it being the same\nmailbox reconnecting. That premise could not be checked: the statement that\npreserved the flag nulled the address in the same breath, and the settings\npanel offers only disconnect, so switching accounts was the only flow there\nis and it inherited consent the new mailbox never gave.\n\nThe flag is cleared on disconnect now. That costs one re-opt-in after a\ngenuine reconnect and makes the invariant checkable instead of asserted.\nWith that, clear and factory_reset were byte-identical, so they collapse\nback into one function - two entry points running the same statement is how\nthe drift that caused this starts.\n\nThe claim that accept clears the flag it sets was in more places than the\nreview cited: three in the contract that generates the published page, one\nin reject's own doc that an earlier pass of this same fix missed, two test\nmessages, and a test whose name asserted it.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs(adr): record what a backup restore does to adjudication state\n\nImport deletes every status_events row and re-seeds one settled user event\nper application. A status that was still an unadjudicated email write at\nexport time therefore returns as confirmed history, with no provisional\nbadge and no record an email asserted it, and every rejection row is dropped\nalong with it - which is the memory the repeat guard reads, so a target the\nuser explicitly disputed can be re-applied later.\n\nAccepted rather than fixed: a restore is a deliberate replace-everything\naction and a re-applied write still lands unconfirmed. Closing it means\nexporting and importing email-sourced rows, which is its own change.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(email): reset the opt-in in the store, not in the renderer\n\nConnecting a different address wiped the watermark and the seen set but kept\nauto-write consent, so mailbox B inherited mailbox A's choice. Nothing\nexploited it, because the connect form only renders while disconnected - the\ninvariant was held by which form the renderer shows and by a comment saying\nso. That is the same reasoning that failed twice already on this branch, and\nthe status query is briefly undefined on first paint, so even the ui half was\nnot absolute.\n\nThe address-changed branch clears it now, scoped to that column: the poller\nopt-in is deliberately preserved there and cannot act on its own.\n\nThe repeat guard folded a read error into \"no rejection on record\", so a\nfailed read could resurrect a transition the user had explicitly disputed. It\nblocks on error now, matching the direction its own doc says it prefers.\n\nBoth fixes are one line, which is exactly when the regression test is the\nonly thing standing between the next reader and the old behaviour; each was\nconfirmed failing first.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(ui): stop promising the app never marks anything applied\n\nThe consent text shown before the user enters an app password still said a\nmatch only ever creates a notification and nothing is ever marked applied\nautomatically. That is false once auto-write is on, and it is the worst place\non the branch for a stale absolute: the safety argument for this feature is\ninformed opt-in, and that string is where the understanding is formed. Both\nit and the watch description now state the claim against the toggle, in both\nlanguages, with the register each namespace uses.\n\nThe timeline dropped the backend's rowid tiebreak, and sort is stable, so two\nevents sharing a millisecond kept ascending order while everything around\nthem descended - a correction could render above the row it corrects, on the\nsurface whose only job is showing what happened.\n\nThree tests asserted less than their names claimed: one forwarded a flag but\nonly ever exercised false, one leaked a stub between cases, one paired an\nollama provider with an openai model.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: state the confirmed invariant in the direction that is true\n\nThe contract claimed nothing writes confirmed true except accept and reject.\nEvery user-sourced write lands true, as does the column default, so the claim\nwas backwards and it is published: the page is generated from that comment.\nThe invariant that carries the safety model runs the other way and was\nalready stated correctly on the field itself - nothing writes false except\nthe email-derived auto-write, so false marks exactly the rows no human has\nruled on yet.\n\nThe record also still inventoried five email-watch commands and described the\nsettings section as notify-only, having gained a sixth command and the\nauto-write toggle in this branch. That inventory is what a security reviewer\nreads to enumerate the ipc surface, so an undercount there costs more than\nordinary prose drift.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(email): read the live status at write time, and let one predicate decide terminal\n\nTwo matched messages in one tick shared a pre-tick status snapshot, so the\nsecond compare-and-set lost against the value the first had just written and\nreturned a no-op - while mark_seen had already stamped its uid, so it was\nnever reconsidered. A confirmation followed by a rejection in one poll window\nis an ordinary ats thread, and the rejection was the one discarded, silently\nand permanently. The write path no longer takes a status argument at all; it\nreads the live value itself, so there is nothing left for a caller to pass\nstale.\n\nThe ladder kept its own list of live statuses and excluded ghosted, while the\ndomain type deliberately excludes ghosted from terminal because a ghosted\npursuit can still revive. A ghosted application was therefore never a match\ncandidate and never advanced - the exact revival the type was designed for.\nThe ladder now asks the domain type instead of keeping a second opinion,\nwhich is the third time this branch has had two predicates disagree about one\nconcept.\n\nThe id guard trimmed for its emptiness check and forwarded the original, so a\npadded id passed validation, matched nothing and reported success. It returns\nthe trimmed value now, so using the raw one is not expressible.\n\nThe alignment check is exact where dmarc allows organizational matching. That\nunder-matches rather than over-trusts, and the doc now says so instead of\nclaiming plain alignment.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: point at the symbols that own these rules instead of copying them\n\nThe fix for the inverted confirmed claim treated the symptom and deepened the\ncause: it enumerated four internal rust writers inside a ts contract comment,\nwhich the published page is generated from. Four symbol names in a doc that\ncannot see them rename is the drift this branch has already been bitten by\nthree times. The public invariant stays; the enumeration moves back to the\nfield that owns it.\n\nTwo record passages did the same thing. Which module owns the write call and\nwhy the layer rules put it there is enforced by a test and documented on the\nfunction - restating it in prose only creates a second copy to go stale. The\nprovenance gate now states the property that matters, that the list\nauthorising a write is deliberately narrower than the list boosting a score\nand why the two must never merge, and points at the module holding both\nrather than transcribing them.\n\nEverything the record exists for is untouched: why the default reversed, the\nthreat model, the residuals, and the user-visible rules about which\napplications are eligible and how terminal statuses absorb.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* test(match): clamp oversized multibyte job text, not just ascii\n\nThe existing clamp test padded with \"x\", so the char-boundary walk-back in\nclamp_to_bytes was never exercised. Replacing that walk-back with a naive\nbyte truncate leaves the ascii test green and fails only the new one, which\nis the point: an ascii-only fixture cannot see this class, and the same\nblindness produced the byte-offset crash earlier in this branch.\n\nThe schema and the clamp are unchanged. The zod cap counts utf-16 units\nwhile the server clamps bytes, but the server is authoritative and walks back\nto a boundary, so the mismatch truncates safely rather than corrupting.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-20T14:13:44+02:00",
          "tree_id": "0e1b5f78c644fe08293124d5a4efcb1a1efc6b31",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/74a54b44c9c6ffa29d0890fdd2593012239ae742"
        },
        "date": 1787229466055,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2179188,
            "range": "± 13451",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2538116,
            "range": "± 26702",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 296756,
            "range": "± 9513",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "0c5d4c00c1e3da030e20e8c817f69eaa37a086ca",
          "message": "fix: make the v0.139.0 score tab actually score, and stop embeds timing out under load (#1057)\n\n* fix(documents): seed the résumé doc id so the score tab can score\n\nThe job-ad Score tab shipped in v0.139.0 rendered \"Save a résumé to score\" and\nnothing else on every cold start. `useJobAdTextMatchScore` is gated on\n`!!resumeId`, but the wizard only ever carried résumé TEXT: `buildTailorDefaults`\nnever set `resumeDocId`, and `persistence.wizardForm` — the only other carrier —\nis session-store state reset to null on launch.\n\nWalking to the Resume step did not fix it either. `ResumeInputCard`'s auto-select\nshort-circuits on pre-seeded text (`if (value) return;`), so `selectDoc` never\nfired and the id stayed empty unless the user manually re-picked a résumé from\nthe Saved menu — which nobody would think to do, because the text was already\nright.\n\n`ApplicationDetailPage` had the id in hand the whole time: it fetches the default\nrésumé's text BY id via `useDocumentText(defaultResumeId)` and forwarded only the\ntext. It now forwards the id too, but only for the branch that actually has a\nsaved-document backing — the autopilot one-shot seed and a previous generation's\n`resumeText` have none, and an id that does not match the visible text is the\nexact drift `useResumeInput`'s selectDoc contract exists to prevent.\n\nDeliberately NOT fixed in `useResumeInput`: making its short-circuit call\n`selectDoc` would break that contract (onChange and selectDoc must land together\nor not at all, twice-fixed per its own comment). Seeding a matched pair at the\nsource satisfies it instead.\n\nNo payload regression: `resumeText: resumeId ? '' : values.resume` now takes the\nid branch for an untouched résumé, which is what that line was written for, and\n`handleTextChange` still clears the id on any hand-edit.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* ui(documents): stop the score panel disclosing egress it never performs\n\nThe CLI-agent egress notice (\"If this posting needs translation, its text is sent\nto {{provider}}\") rendered ABOVE the panel's five-way body branch, so it also\nappeared on the no-résumé, no-posting and error states — warning about a network\nsend on a panel that will never send anything. It is now computed once and\nrendered only in the loading and measured branches, which are the two that\nactually score.\n\nThe two no-data branches rendered a bare <p> inside a `flex min-h-0 flex-1`\ncontainer, which stretched one line of grey text over roughly 600px of empty box.\nThey use EmptyState now.\n\nAlso renders `MatchScore.recommendations`, which the Rust kernel has always built\n(\"Consider adding evidence of: …\") and no component read. Gated on `hasCoverage`\nso the \"no extractable keywords\" placeholder cannot produce advice about a score\nthat was never measured. Backend-authored prose, left untranslated for the same\nreason `explanationText` above it is.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(renderer): catch route render errors instead of white-screening\n\nA diagnostics bundle showed AutopilotPage throwing a Rules-of-Hooks error, and\nTanStack Router logging \"The following error wasn't caught by any route! At the\nvery least, consider setting an 'errorComponent' in your RootRoute!\" — the root\nroute had none, so the whole window went blank with no way back.\n\nAdds an errorComponent built on ErrorState, with `reset` wired to its retry. This\ndoes not address the hook-order bug itself; there is no repro for that yet, and a\nrecoverable error state is worth having regardless of which component throws.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(ai): give local embeddings a budget a loaded gpu can meet\n\nA user's diagnostics bundle showed 66 `embed failed … Ollama unreachable` lines.\nReading their database rather than theorising from the log settled what it\nactually was: `embedding_config` was ('ollama', 'qwen3-embedding:4b'), their\ndocument was indexed, and the stored vector matched that model at dim 2560. The\nconfiguration was correct and deliberate.\n\nThe failure cycles are exactly 45s — three attempts at the 15s OLLAMA_EMBED bound\n— and run concurrently with a local /api/chat on a 27B model that itself held the\nGPU for 213s before failing. Ollama was up. Ollama serialises by default, so the\nembed queued behind that generation and burned its whole budget waiting. Before\nv0.138.1 a transport timeout and a refused connection both printed \"unreachable\",\nwhich is why it read as a dead daemon.\n\nOLLAMA_EMBED goes to 60s. That makes the local bound LOOSER than the 30s cloud\none, inverting the usual local-is-faster assumption, so the doc comment now says\nso and why. A cold load of a 4B/2560-dim embedding model can exceed 15s unaided\ntoo. Cost: a genuinely dead daemon takes 3x60s to give up rather than 3x15s — a\nrefused connection still fails fast, being a transport error and not a timeout.\nThree now-stale \"15 s\" references in retry.rs are corrected, including the\nMIN_RETRY_ATTEMPT_FLOOR argument, whose \"smallest per-attempt bound reaching this\nloop\" is `EMBED` at 30s now that OLLAMA_EMBED has widened past it.\n\nSeparately adds ollama-cloud to the embeddings provider list. OllamaCloudClient\nimplements embed and reports supports_embeddings, but the panel never offered it,\nso an Ollama Cloud user had no non-local option at all. Its defaultModel is\ndeliberately empty, matching openai-compatible: default_embedding_model returns\nNone for that client on purpose, and guessing a model name ollama.com serves is\nthe exact mistake #950 removed.\n\nRe-pins the EmbeddingsSettings event-subscription note hash. The note describes\nthe re-index toast lifecycle and local busy state; this diff adds a dropdown\noption and comments and touches none of it, so the note stands as written.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(ai): cap the embed timeout where the re-rank breaker can still fire\n\nThe 60s bound this branch first shipped silently disabled RERANK_DEGRADE_BREAKER.\nrerank.rs derives RERANK_STEP_TIMEOUT as SEMANTIC_RERANK_MAX * 15, and that bare\n15 was OLLAMA_EMBED. One degraded job costs a whole embed budget, so at 60s a job\ncosts 60x3 = 180s and the breaker's three consecutive degrades need 540s — past\nthe 300s wall clock that wraps the phase. The phase would die on the clock\nmid-job every time and the breaker would never reach its threshold, which is\nverbatim the \"full phase burned hourly to produce nothing\" the breaker exists to\nprevent. Visible in the same field logs that motivated the raise.\n\n30s is the largest value that keeps it: 3 x (30 x 3) = 270s < 300s. That also\nlands it equal to the cloud EMBED bound rather than past it, so local is simply\n\"not reliably faster\", which is the honest claim.\n\nThe coupling was invisible from timeouts.rs, so it is now a compile-time\nassertion in rerank.rs instead of a comment: widening OLLAMA_EMBED past ~33s\nfails the build. Mutation-checked — restoring 60s produces\n\"evaluation panicked: RERANK_DEGRADE_BREAKER embed budgets must fit inside\nRERANK_STEP_TIMEOUT\". EMBED_BUDGET_ATTEMPTS is re-exported from ai_provider so\nthe assertion can name the real constant rather than duplicate the number.\n\nAlso corrects send_embed_with_retry's claim that \"no outer deadline is derived\nfrom the embed constants\". True of indexing, false of the re-rank phase, and\nthis branch had just made it load-bearing.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(renderer): give the error boundary an escape hatch that exists\n\nThe boundary's own doc claimed the shell stays mounted so \"pick another page\" is\na real escape hatch, and both locales told the user to pick one from the sidebar.\nNeither is true: the router wraps a match's component in its catch boundary, and\nthe root match's component IS RootLayout — Titlebar, Sidebar, StatusBar and every\nalways-mounted bridge are replaced along with the failed route. The copy pointed\nat a control that is provably not on screen.\n\nThat also leaves reset as the only action, and reset re-renders the route that\nthrew — fine for a transient failure, a loop for the deterministic render bug\nthis was built for.\n\nAdds a dashboard button (useNavigate resolves fine inside the boundary; landing\non / remounts the shell), rewords both locales to stop promising a sidebar, and\ncorrects the doc comment to say what actually happens.\n\nThe test file asserted the component needs no router context, which stopped being\ntrue. It now mocks useNavigate and asserts the button asks for /, and its header\nrecords why standalone rendering cannot prove the unmount itself.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(documents): disclose egress on the error branch, and drop a dangling doc id\n\nTwo follow-ups from review of this branch.\n\nThe egress notice was moved to \"only branches that send something\", but the error\nbranch was excluded on a false premise. Its second disjunct is\n`score && !isMeasured(score)`, which requires the query to have RESOLVED — the\nround trip completed and the posting text was already sent. The disclosure now\nrenders there too, and the comment says which branches are genuinely silent and\nwhy (they short-circuit before scoreEnabled turns on).\n\nSeeding resumeDocId for everyone widened an existing hazard. resume_source is\nID-WINS with NO fallback: resolve_resume answers \"resume not found: <id>\" and\nnever consults resumeText, which useTailorPipeline has already blanked precisely\nBECAUSE an id was set. Advance a wizard step (snapshotting the form into the\nmemory-only applyWizardForm), delete that résumé on the Documents page, come\nback, and the run hard-fails with an opaque id echo while the text sits visible\non screen. The persisted id is now dropped when it names no live document —\nApplicationDetailPage is the one place holding both the persisted form and the\ndocument list, so it is the only place that can tell.\n\nThat guard reads _id, not id: useDocuments is TYPED as DocumentRecord[] but the\nbackend really returns _id, which is why useDefaultResume casts through RawDoc.\nReading .id typechecks and is undefined at runtime, which would have stripped\nevery persisted id rather than only stale ones. Both mutations are covered by\ntests — removing the guard fails \"strips a persisted résumé id whose document no\nlonger exists\", and swapping _id for id fails \"keeps a persisted résumé id whose\ndocument still exists\".\n\nAlso de-vacuums the generation-fallback seeding test. It passed with the equality\nguard removed, because mockDocsData was empty and the `?? undefined` fallback\ncarried it alone. It now uses a default document with EMPTY text: a real\ndefaultResumeId exists, but the || chain still falls through to the generation's\ntext, so only the equality guard prevents the seed.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* test(ai): pin the embed bound and cover the error-state disclosure\n\nThree review findings, all valid against HEAD.\n\nThe retry doc said the worst case was \"MAX_ATTEMPTS × the per-attempt timeout +\nbackoff\". Backoff is not added on top: `budget` is wall clock from the first\nattempt and `send_with_retry_capped` pays each sleep OUT of it, projecting\n`spent` past the sleep before deciding another attempt still fits. So one call\ncosts at most per_attempt × EMBED_BUDGET_ATTEMPTS in total. That wording is now\nexact, which matters because rerank.rs's compile-time assertion is built on this\nnumber — and the assertion is sound either way, since backoff comes out of the\nbudget rather than extending it.\n\nOLLAMA_EMBED had no direct test. The rerank assertion bounds it from ABOVE only,\nso reverting to the old 15s would have satisfied it and silently restored the\nbudget the field measurement showed was too small. Pinned at 30s, with the doc's\nown claim (\"the same bound as cloud EMBED, not a tighter one\") asserted beside it\nso the two cannot drift. Mutation-checked: 15s fails the test.\n\nThe error branch's egress disclosure shipped untested. Reaching that branch means\nthe request went out, and its `score && !isMeasured(score)` disjunct means it came\nback, so the posting text was already sent — the one failure state that still owes\na disclosure. Mutation-checked: removing the notice fails the test.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-20T17:37:43+02:00",
          "tree_id": "ec2f07d5b0dfdde48d4d0ffd0aa5130a12139d77",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/0c5d4c00c1e3da030e20e8c817f69eaa37a086ca"
        },
        "date": 1787241822503,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2243566,
            "range": "± 72980",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2675490,
            "range": "± 44573",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 307333,
            "range": "± 11706",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "b7764dda472b50b7f6c4d408d7935b4c6461d27e",
          "message": "fix(match): score non-english postings honestly, and let the score tab run semantic (#1060)\n\n* fix(match): filter stopwords in the posting's own language, not just english\n\nA German posting scored \"7% across 291 job keywords\" with `abgeschlossenes`,\n`abgestimmt`, `abseits` — and `13385`, a Berlin postcode — listed as missing\nkeywords. Those are function words and a number, not skills, so the coverage\ndenominator was inflated with them and the percentage meant nothing. For a user\napplying to German ads that is every score they see.\n\n`documents::keywords::STOPWORDS` was 89 English words applied unconditionally,\nand the token filter admitted anything longer than three characters. The\nlanguage plumbing already existed one function away: `make_stemmer` has always\ndetected the posting language and picked the matching Snowball algorithm. This\nreuses that detection to pick the stopword list too, adds curated lists for the\nsame six languages the stemmer supports, and drops pure-numeric tokens.\n\nMeasured on a German fixture, comparing against a helper that reproduces the\npre-fix behaviour rather than against another derived value: coverage 20% -> 29%.\n\nCuration is deliberately incomplete. `agilen`, `academy` and `analysierst` are\nleft IN the keyword set: an inflated denominator is recoverable, silently\ndeleting a real skill is not. The French test pins the same residual honestly —\n`cherchons` still leaks — instead of hiding it.\n\nMATCH_FORMULA_VERSION 2 -> 3. It is part of `match_scores`' composite primary\nkey, so old rows become a structural miss rather than a stale hit; every score\nrecomputes once and nothing needs migrating.\n\nTwo subtleties this turned up, both fixed here rather than left as traps:\n\nPer-call `whatlang::detect()` cannot be used for the stopword choice. Short\nfragments are not reliably identifiable — the German skills line \"Kenntnisse in\nRust, Python, Kubernetes, Terraform und Kafka\" reads as ESTONIAN at confidence\n0.23 in isolation — so a per-call detection disagreed with the whole-document\nlanguage resolved elsewhere, and a filler word survived at one call site while\nbeing absent from the stem->display map built at another, leaking a raw\nunstemmed stub into a user-facing message. Call sites that tokenize fragments\n(`DocumentTokens`, `Analysis`, `JobVocabulary`) now pass the already-resolved\nlanguage explicitly.\n\n`evidence`'s own `FUNCTION_WORDS_DE` is untouched. It answers a different\nquestion (display-only `skills_present`/`skills_absent`) and is deliberately not\nformula-pinned. Its guard test needed a new probe word, not a weaker assertion:\n`unsere` proved the two filters are independent only while the kernel ignored\nit, so the probe is now `profil` — in `FUNCTION_WORDS_DE`, absent from\n`STOPWORDS_DE`. The property under test is unchanged.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* refactor(match): split the keyword tests to a sibling file for r8\n\nThe stopword work pushed `documents/keywords.rs` to 2303 LOC, past R8's 1400\nhard cap, so `cargo test --test architecture` failed. Neither the `--lib` run\nnor my own gate caught it, because both of us only ran `cargo test --lib`.\n\nPure move, no behaviour change: the `#[cfg(test)] mod test` block becomes\n`documents/keywords/test.rs`, following the `commands/match_resume.rs` +\n`match_resume/` precedent already in the tree. Parent drops to 1155 LOC, the\nsibling is 1154, and the 42 keyword tests are unchanged and still pass.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(ai): stop local embeds spending their budget in ollama's queue\n\nMeasured on a real machine: embeds failed in exactly 45s cycles — three attempts\nat the then-15s bound — while a local 27B chat held the GPU for 213s. Ollama was\nUP. It serialises by default, so the embed sat in its queue and its timeout clock\nran the whole time it was queued. Every attempt expired without the request ever\nbeing serviced, and the whole budget bought nothing.\n\nRaising the bound to 30s (this branch's parent) did not change that shape: the\nbudget was still being spent on queue wait rather than on a request.\n\nLocal Ollama chat now maintains an in-flight gauge, and a local embed waits a\nbounded OLLAMA_EMBED_QUIET_WAIT for it to clear before dispatching with a FULL\ntimeout. Busy now degrades in seconds instead of buying three doomed attempts.\n\nChat is untouched by construction, not merely by test: it only increments and\ndecrements an atomic behind an RAII guard, and never reads or awaits the gauge,\nso its concurrency, ordering and latency cannot change. `tokio::sync::RwLock`\n(chat=read, embed=write) was the obvious shape and is WRONG here — tokio's write\nlock is fair, so a queued embed would delay the next generation. Verified against\ntokio's own source doc rather than assumed. `ollama-cloud` never reaches this\npath; it delegates to the OpenAI-compatible client.\n\nA timeout that follows a still-in-flight local chat is now worded distinctly from\na dead daemon, mirroring the timeout-vs-refused split #1051 introduced. A later\nchange surfaces that state in the UI.\n\nThe rerank compile-time assertion is updated, not weakened — the new wait sits\noutside the retry budget, so one degraded job now costs\n`OLLAMA_EMBED × EMBED_BUDGET_ATTEMPTS + OLLAMA_EMBED_QUIET_WAIT`:\n`((30 × 3) + 5) × 3 = 285 < 300`, keeping 15s of margin under\nRERANK_STEP_TIMEOUT. Mutation-checked: a 15s quiet wait makes it 315 and fails\nthe build.\n\nThe gauge is process-global, so the tests that touch it are serialised — found\nlive, when the concurrency test failed under default test parallelism.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(match): let the score tab run semantic scoring\n\nThe job-ad Score tab printed \"Semantic similarity: Not scored\" on every render,\nfor every posting, forever: `match_resume_text` passed `semantic_enabled = 0`\nunconditionally, so `scoreSource` could never be `'combined'` and the row was\nstructurally unreachable.\n\nIt now threads the user's existing `semanticScoring` preference end to end,\nmirroring `match:resume` rather than forking a second pattern —\n`MatchTextRequestSchema` gains the optional flag, `ipc_contracts/matching.rs` is\nREGENERATED (`pnpm gen:ipc`, never hand-edited), and `score_resume_against_text`\ntakes the bit instead of hardcoding it. No second setting, and it does not\ndefault on.\n\nThe query key carries the flag. Without that a keyword-only result is served\nunder the semantic key the moment the preference flips, and the number silently\ncontradicts the setting that produced it.\n\nEmbeddings matter more here than the English case suggests: they are\nlanguage-aware where a stopword list is not, so for a German posting semantic\nsimilarity carries signal that keyword coverage structurally cannot.\n\nFour doc comments were about to become lies and are rewritten with the code:\n`MatchSurface`'s axis-2 bullet, `score_resume_against_text`'s and\n`match_resume_text`'s docs, `MatchContract.text()`'s TSDoc, and `JobAdView`'s\n`hasSemantic` comment plus its \"never true through this endpoint\" footnote. This\nrepo has twice shipped a bug that hid behind a comment describing intent while\nthe code did something else.\n\nAxis 1 is NOT fixed and is kept honest: the Jobs page composes title +\ndescription + requirements, `JobAdView` only ever holds the description, so the\ntwo surfaces still legitimately differ. No parity is claimed that was not\nachieved.\n\n`JobAdView.tsx` needed no JSX change — its existing ternary was already correct\nonce `hasSemantic` became reachable, and the kernel's own \"semantic similarity\ncould not be computed\" sentence already surfaced through `explanationText`.\nVerified by mutation rather than assumed; the diff there is comment-only.\n\nA degraded embed still yields `scoreSource: 'keyword'` and is not cached under\nthe semantic key, so a provider outage cannot poison the cache for the whole TTL\n— pinned by a test that scores again with the provider back and gets the real\nanswer.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(validate): stop a truthful german résumé reading as keyword stuffing\n\nThe stopword change earlier in this branch made `de_generated_paraphrased.txt` —\na fixture labelled TRUTHFUL — fire `ats.keyword_density`. Caught by\n`cargo test --test eval`, which `cargo test --lib` does not build, so neither my\ngate nor the authoring agent's saw it.\n\nFiltering a posting's own function words is correct, and it makes documents\nshorter in CONTENT tokens. Measured on that fixture rather than reasoned about:\n\n  kernel tokens        89 -> 73\n  after this check     82 -> 72\n  \"backend\" x3       3.66% -> 4.17%   (threshold 4%)\n\nAn honest résumé saying \"backend\" three times is not stuffing. The 4% ratio was\ncalibrated against a denominator inflated with filler; the filler left, so the\ncalibration had to follow. Two changes, neither of them the threshold itself:\n\nThe second filter is gone. `keyword_density_issues` ran kernel tokens through\n`function_words` a second time, and its doc said why: \"the kernel's own\n`STOPWORDS` is English-only\". That premise is exactly what this branch fixed.\nWorth 7 tokens before, 1 now — redundant, and it was double-shrinking the\ndenominator. English is unaffected: `function_words(\"en\")` was already empty.\n\nMIN_TOKENS_FOR_DENSITY 50 -> 75, derived not fitted: `3/total > 0.04` holds for\nevery `total < 75`, so 75 is precisely the point where three ordinary repeats\nstops being accusable and four is required. Below it the absolute ceiling of 6\nstill applies, so real stuffing in a short document is still caught.\nMAX_KEYWORD_DENSITY_RATIO and MAX_KEYWORD_OCCURRENCES are untouched.\n\nThe pinned-threshold guard is updated WITH the reason rather than silently\nre-pinned — moving that number should stay deliberate. Mutation-checked: putting\nthe floor back to 50 fails `eval` again.\n\nThe gate stays `en | de`. The kernel now carries partial lists for fr/es/it/pt/nl,\nbut partial is the operative word — an uncurated inflected verb form still\nsurvives — so this does not widen an accusation onto the back of a list that is\ncurated, not exhaustive.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(validate): tokenize density in the resolved language, not a fresh detection\n\nReview catch, and it is a defect I introduced earlier in this branch. Removing\nthe redundant `function_words(&ctx.lang)` pass left `keywords_normalized_list`\nas the ONLY language decision in `keyword_density_issues` — and that variant\nre-detects on `generated` alone. Before, the second filter at least used\n`ctx.lang`, so I made this strictly worse.\n\nThe gate one line above already commits to `ctx.lang`; the tokenizer has to\nagree with it. A résumé body is mostly language-neutral tokens, so whatlang has\nlittle to work with: a disagreement silently selects a different stopword\nprofile, moves the denominator, and can flip whether the issue is emitted.\n\nTested at the KERNEL level, deliberately, and the reason is worth recording. The\nend-to-end version cannot be written honestly: tripping the absolute ceiling\nneeds a German-only word 7+ times, and that many repeats add enough German\nsignal to make detection CORRECT again. I wrote that vacuous test three times\nbefore measuring it — each version passed with the fix REVERTED, which is the\nonly reason I noticed. The kernel test pins the real hazard instead: a tech-dense\nbody that misdetects as Dutch keeps `wurde` (German-only; Dutch is `werd`) under\nthe auto profile and drops it under `de`.\n\nAlso scopes the CodeRabbit docs thin-pointer instruction away from `docs/API.md`.\nThat file opens with \"GENERATED FILE — DO NOT EDIT\", is rendered by\n`scripts/gen-api-docs.mjs` from the contract TSDoc, and `pnpm gen:api:check`\nfails CI on a hand edit — so the finding it drew was unactionable by\nconstruction, and would recur on every PR that touches an IPC contract. The rule\nstill applies to every hand-written doc.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-20T22:00:08+02:00",
          "tree_id": "1005a8dace701c4072cbf933aaed6451857377c5",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/b7764dda472b50b7f6c4d408d7935b4c6461d27e"
        },
        "date": 1787257439498,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2278686,
            "range": "± 18287",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2753571,
            "range": "± 77462",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 304131,
            "range": "± 3585",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "21a74eea65b6e2d67e701f1fe5dae0629d7b0f6d",
          "message": "fix(export): give résumé entries the hierarchy they were never structured for (#1061)\n\n* fix(export): give résumé entries the hierarchy they were never structured for\n\nThe owner asked for bold company names and a distinguishable duration and got\nneither, because the entries were not styled at all — they were never parsed as\nentries. The exported text reads\n\n    Independent / Open-Source R&D . Self-directed\n    December 2025 – Present, Köln, Deutschland\n\nand `export/parser` recognised three job-entry shapes, ALL of which require the\ndate on the same line as the title. This shape matched none, so the whole entry\nfell through to `LineKind::Text` → `Block::Paragraph` and rendered in plain body\nweight. Tuning the styling knobs alone would have been cosmetic.\n\nThe parser now also recognises a title line followed by a bare leading date\nrange, attaching the date and taking the remainder as the subtitle. Guarded by\n`is_entry_title_shaped`, and the not-a-heading exclusion is load-bearing: without\nit a real section heading followed by a leading-date line reads as \"consumed\" and\nthe date is silently dropped. `DATE_RE`'s middle span also had to go greedy so\n`.find()` captures the whole range rather than stopping at the first year —\nverified a no-op for every other call site, all of which are boolean\n`.is_match()`.\n\nOnce entries parse, bold falls out: titles were already unconditionally bold and\n`emphasize_education` is already hardcoded on, so company, project, education and\ncertification titles all get it. The date now renders italic rather than\ninheriting the title's bold, which is the fast-scan distinction that was asked\nfor — and no unsafe technique was needed for it: no colour-only signal, no\ntables, no letter-spacing.\n\nItalic was never a font problem. All four Carlito faces including italic are\nalready registered in the Typst `FontBook`; only Regular and Bold were embedded\nbecause no italic run had ever executed. There is now an assertion that a real\nitalic `/BaseFont` is embedded, so a synthetic slant cannot pass for one.\n\nDOCX carried the same gap in the other direction — its date run was plain while\nthe PDF's was bold, so the two formats disagreed. Both are italic now, pinned by\nan XML-level test asserting `<w:i />` and no `<w:b />`.\n\n`url_label` keeps the full `domain/user/repo` text for a repository URL instead\nof collapsing it to \"GitHub\", matching the reference résumé's bare-domain project\nlinks. A profile URL with a single path segment still collapses.\n\nThe header contact line was emitting doubled separators\n(`Köln |  · mail ·  | phone`); each raw line's own edge separator is now stripped\nbefore the join.\n\nScoped to `single_column.typ`, shared by six templates that are all\n`TemplateTier::Ats`; the nine bespoke templates are untouched.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(export): stop the new entry branch swallowing a contact line\n\nFour review findings, all valid against HEAD.\n\nThe next-line-date branch had no contact guard, and it is checked BEFORE the\n`is_contact_shaped` branch that would otherwise claim the line. A header contact\nline is title-SHAPED by every other measure — short, unpunctuated, not a heading\n— so a contact line followed by a leading-date line became a fabricated job\nentry. Reproduced before fixing: \"Köln, Deutschland · max@example.de · 0179\n1402319\" parsed as JobEntry carrying right_text \"Jan 2021 – Heute\", i.e. the\nuser's contact details vanish from the header and resurface as a job title.\nEvery sibling job-entry branch already guarded on this; this one does now too.\n\nThe embedded-italic assertion could pass for the wrong reason. Its fixture also\nproduces italic SUBTITLE runs from location remainders, and `single_column.typ`\nrenders subtitles italic, so the assertion would have survived the entry date\nsilently reverting to non-italic. Moved to its own fixture with a date and no\nsubtitle. Mutation-checked at the right line this time: dropping `style:\n\"italic\"` from the DATE (line 200, not the markdown-italic run at 112) leaves\nonly Carlito-Bold and Carlito-Regular embedded and fails the test.\n\nThe sample-PDF test printed an absolute path. `CARGO_MANIFEST_DIR` makes\n`out_path` absolute and both arms passed it to `eprintln!`, against the\npath-privacy rule that covers logs explicitly. It prints the repo-relative\nartifact name now.\n\n`url_label` kept a URL's query and fragment, so `github.com/user/repo?tab=readme`\nwould have been printed verbatim on a résumé. Query and fragment are stripped\nbefore the path is split — which also stops a query on a PROFILE link\nmanufacturing a second segment and promoting it to a repo-shaped label.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-20T23:00:06+02:00",
          "tree_id": "b85adf69605acaafd5bb6a8ae9b65fd4a52f66fa",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/21a74eea65b6e2d67e701f1fe5dae0629d7b0f6d"
        },
        "date": 1787260312679,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2307543,
            "range": "± 52535",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2739455,
            "range": "± 28744",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 309320,
            "range": "± 6031",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "ef07f430f049cf1b300a9b449534d0987730bba5",
          "message": "feat: put the growth charts on the home page, and stop the repo doubling yearly (#1065)\n\n* feat: put the growth charts on the home page, and stop the repo doubling yearly\n\nThree things, one PR, because they touch the same workflows.\n\n## The charts were only ever in the README\n\nstars.svg and downloads.svg have regenerated daily since #1063 and were embedded\nnowhere but README.md. They now sit in a dark band between the testimonials and\nthe finale — the one place on the page whose background is the body's own\n#0a0c11, because gold measures 10.55:1 there and 1.44:1 on the finale's cream,\nwhich fails even the 3:1 floor for graphics.\n\npages.yml fetches them off the `badges` branch at build time rather than anyone\ncommitting them: 77 KB that changes every day is ~28 MB of history a year, the\nexact cost the parentless badges branch exists to avoid. They land in public/ so\nthey are served SAME-ORIGIN — pointing at raw.githubusercontent.com would have\nbeen the first third-party request this site has ever made, and is blocked\noutright on /mission-control by `img-src 'self' data:`. The fetch runs after the\nNext cache step, whose key hashes public/**, so a daily-changing file cannot bust\nthe build cache. A missing or empty file fails the build: the last good site\nstays up, rather than shipping a broken img onto the home page.\n\nrepo-charts.yml now dispatches pages.yml after publishing. Without it the site\ntrailed the README by a day — the nightly deploy fires at 03:17 and the charts\nregenerate at 04:17.\n\n## The animation was hiding the data, including in the README\n\nRemoved from the renderer entirely. Measured side by side on one page: embedded\nvia an img tag, the animated SVG drew its card, title, gridlines and axis labels\nbut NEVER the line, fill or annotation, while the identical file with the\nanimation stripped rendered perfectly. Chrome applies the animation there but\ndoes not advance its timeline, so animation-fill-mode:backwards pins the\nfrom-state forever.\n\nGitHub serves README images through camo the same way, so the charts merged this\nmorning have been rendering as empty cards on GitHub since. The next scheduled\nrun republishes them fixed. A chart whose data is visible only if a stylesheet\nanimates is not a chart.\n\n## The metrics snapshot was adding ~70 MB of history a year\n\nmetrics-snapshot.yml committed 3.4 MB of raw REST bodies to main nightly, on a\n54 MB repo — undoing the 147 MB -> 54 MB rewrite within a year. Every payload is\nnow field-allowlisted against the interfaces in types.ts, whose header already\nsays only the used fields are typed. 3.4 MB -> 155 KB:\n\n  closed-pulls  1210 KB ->  23 KB   (1.2 MB nightly for one median and one ratio)\n  releases       988 KB ->  58 KB\n  runs           805 KB ->  24 KB\n  commits        361 KB ->  30 KB\n\ncommits.json keeps only the subject line: its sole consumer is\n`.split('\\n', 1)[0]`, and the bodies in this repo run to forty lines. Verified\nequivalent on a single API response — same typed/fixish counts, same per-day\nsparkline buckets, same sha order.\n\nissues.json is deliberately left raw. realIssues() discriminates a PR with\n`i.pull_request === undefined`, a STRICT undefined check, and jq writes an absent\nkey as null. `null === undefined` is false, so the obvious filter reclassifies\nevery issue as a PR and silently zeroes the critical count, the needs-attention\nlist and time-to-first-response. Six kilobytes is not worth that.\n\nVerified by loading /mission-control against the slimmed files: all fifteen cards\npopulate. The one dash — CI gating pass rate — is pre-existing, not a regression:\nthe original committed runs.json has the same zero matches, because\nci-pipeline.yml triggers on pull_request so its runs never carry head_branch=main,\nwhich the snapshot query and gatingRuns() both require.\n\n## Also\n\nbenchmarks/data.js had 199 entries over 76 days and no cap — ~20 KB/day, forever.\nmax-items-in-chart: 100 bounds it; the alert compares against the previous entry\nso trimming old points cannot weaken it. docs/assets/banner.png (645 KB) is\ndeleted: README links banner.svg, and banner.png has zero references anywhere.\n\n* fix(landing): drop the dark band, keep the dark charts\n\nThe growth section was a full-bleed #0a0c11 band sitting between two cream\nsections, which meant it needed a cross-fade at BOTH edges. A\ncream-to-transparent gradient painted over near-black does not blend — it\nrenders as a white-to-black smear, twice, and that was the actual complaint. The\ncharts were never the problem.\n\nThe seam system works for the page's dark beats because those neighbour each\nother. It cannot rescue a single dark slab dropped into the cream run at the end\nof the page. So the section is now `--paper`, matching .testi directly above it,\nand there is no luminance jump left to fade: .testi's bottom fade becomes a\nno-op against its own colour, .finale's ::before goes away again, and only the\nbottom edge keeps a seam into .finale's slightly deeper cream — exactly the\nboundary .testi owned before this section existed.\n\nThe charts stay dark and sit on the cream as inset cards. The page already does\nthis with .screencap: a bounded dark card reads as an embedded screen, where a\nfull-bleed dark band read as a foreign slab.\n\nAlso fixes the caption, which was #b9c4d4 — a token for the dark beats, and\n1.50:1 on --paper, a clear fail for 15px body text. It now uses creature.css's\nown --muted #6b6459, chosen as the warm grey that clears AA on this surface at\n4.98:1.\n\nDetour worth recording: I first read this as a chart-colour problem and built a\nwhole second cream palette for them, since gold is 1.58:1 on paper. That was\nsolving the wrong thing — reverted. Fixing the section removed the need for it.\n\n* fix: name what the growth charts plot, not which way the line goes\n\nReview pass on the Growth section. Four real problems, all of them the same\nshape: a claim in the source that nothing would falsify once it went stale.\n\n- The alt text said the star count was \"climbing over time\". The SVGs are\n  rebuilt nightly from data this component never sees, so that is an assertion\n  about a number it cannot check — a lie the first flat or falling week. Both\n  alts now name the metric and stop there.\n- Three comments still described .growth as a dark #0a0c11 band, which it was\n  for exactly one draft before it became cream. One of them sat directly above\n  a second comment that contradicted it.\n- .finale > * was dead on arrival: it lifts content above a .finale::before\n  seam that this branch removed before merging. Added and orphaned in the same\n  PR.\n- .growth's seam gradient ended on a hand-copied #ece2cf whose entire job was\n  to equal .finale's background, kept in agreement by nothing. Both now read\n  --paper-deep, so the seam cannot silently drift off the section it fades into.\n\nGuards, each mutation-tested rather than assumed:\n\n- scripts/lib/line-chart-svg.test.mjs asserts the area fill, both pencil\n  passes, the endpoint and the peak annotation are present, and that nothing\n  in the output animates. That set is not arbitrary — it is exactly what the\n  animated build failed to paint, since Chrome applies a draw-on animation\n  inside an img but never advances its timeline, so the card and gridlines\n  rendered while the data did not. Five mutations (drop the fill, drop a pass,\n  drop the annotation, re-add an animation, strip the aria-label value) each\n  fail the suite.\n- The alt assertion was alt.length > 0, which alt=\" \" satisfies. It now\n  requires the metric noun and rejects directional words; restoring\n  \"climbing\", blanking an alt, or dropping the noun each fail.\n\nThe i18n and stylelint findings were checked and do not apply: apps/landing\ndepends on next, react and react-dom only, has zero @ajh/translations imports\nand is not localized, and this repo has no stylelint config at all — the rule\ncited fires on .testi's identical, untouched shape. var(--muted) was also\nrejected for .growth-note: the home route loads home.css alone, so it resolves\nto nothing there and the colour would fall back to full --ink. The comment now\nsays so, since that is the edit the next reader will reach for.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-21T17:21:34+02:00",
          "tree_id": "6599270bfd8aa812d455ea100ced70c528f9ae09",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/ef07f430f049cf1b300a9b449534d0987730bba5"
        },
        "date": 1787327083739,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2288619,
            "range": "± 36011",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2693275,
            "range": "± 15456",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 300644,
            "range": "± 8010",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "e0a9066bacecc55260891ed0ced7550f350e1cdb",
          "message": "chore: stop committing generated images, and fix the note that said otherwise (#1066)\n\nTwenty files, 6.38 MB, none of them source. Each is rendered output of a\ngenerator that is itself committed, so every regeneration wrote another few MB\ninto history that no clone needs but every clone pays for.\n\nWhere each set went, and why they could not all go to the same place:\n\n- branding/marketing/0{1..4}.png and the extension's screenshots + promo tiles\n  are attached to the assets-v1 release as two zips. They are downloaded once\n  and re-uploaded by hand to Product Hunt and the Chrome Web Store, so GitHub\n  serving release assets as application/octet-stream with\n  Content-Disposition: attachment is the shape their use already wants.\n- docs/assets/templates-showcase.png could NOT go there. The README embeds it,\n  and an attachment-disposition octet-stream does not render in an img tag. It\n  now lives on a parentless `assets` branch and is served from\n  raw.githubusercontent.com, which answers image/png — the same transport the\n  README's badges and growth charts already use.\n\nicon-512.png stays tracked: build.mjs reads it as base64, so it is an input,\nnot output. The gitignore negates it explicitly.\n\nThree places would have quietly undone this:\n\n- docs/EXPORT_TEMPLATES.md instructed the reader to regenerate the showcase\n  banner \"and commit\" it. It now says where to publish it instead, and carries\n  the plumbing to do so. That snippet is tested, not assumed: run verbatim it\n  reproduces the exact tree hash this branch published, and its first draft\n  did not — `mktemp` creates an empty file and git rejects an empty index, so\n  it needs -u to hand back a path.\n- typst_engine/test.rs still writes the banner to the now-ignored path, which\n  is correct, but nothing there said not to force-add the result. It does now.\n- The extension's store-assets README described files that are no longer in a\n  clone. It points at the release zip.\n\nAlso corrected while adjacent: the gitignore claimed the /world clips \"ship as\nGitHub release assets (world-assets-vN)\". No such tag was ever cut and\nworld-config.ts says Cloudflare R2 — because scrub-engine.js loads clips with\nfetch().blob() and release downloads carry no Access-Control-Allow-Origin. The\nabandoned plan is now recorded as abandoned, with the reason.\n\nThis stops the growth; it does not undo it. The blobs stay in history until a\nrewrite, and the gitignore says so where someone would otherwise assume the\nrepo just got 6 MB smaller.\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-21T18:17:14+02:00",
          "tree_id": "ba2522bdf16a4dc8d5f3c953adb633aa2ff1a310",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/e0a9066bacecc55260891ed0ced7550f350e1cdb"
        },
        "date": 1787329754811,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2264226,
            "range": "± 50218",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2678144,
            "range": "± 27954",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 322108,
            "range": "± 5548",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "1ff8aa3bb9c770b0bb74540833469f38cf314598",
          "message": "feat(export): render projects as bold name, technologies meta line, description (#1071)\n\n* feat(export): render projects as bold name, tech-stack meta line, description\n\nA project's locked signature (bold name + links, a middot-separated stack\nline, then prose) arrived at the renderer as three indistinguishable\nparagraphs: the parser reads the title line as Contact because it holds a\nURL, and the stack line as Contact or Text depending only on how many\ntechnologies are listed. Every template already styles an entry title and\nsubtitle; nothing styles three paragraphs.\n\nRegroup those lines back into the EntryBlock the text always described,\nscoped to a Projects section and to the line directly under a title. No\ntemplate, DOCX renderer or model field changes: both export formats build\nfrom model_from_resume_text, so PDF, DOCX and the live SVG preview pick it\nup together.\n\nAlso localize the Projects arm of SectionId::from_header. It matched only\n\"project\", so a German Projekte heading classified as Custom and\nreorder_sections sorts an unknown id last - meaning ATS mode dumped those\nprojects at the bottom of the document, contradicting DE_ORDER. The other\nsections stay English-only; that is a wider gap with its own blast radius.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(builder): let projects carry a technologies list\n\nThe export path now renders a project's tech line as its own styled meta\nline, but nothing outside an uploaded CV could author one: the builder\nwizard's project form had name, description and link, and the GitHub\nimport fed language/topics into the prompt only to get name and\ndescription back.\n\nAdd a technologies field to the interview contract and the wizard form,\nnormalize whatever separators the candidate types to the one the resume\nrenders with, and tell the model the three-line project shape at all three\nprompt depths from one shared rule.\n\nThe GitHub import fills it deterministically from the repo's own language\nand topics rather than asking the model, which would add a fabrication\nsurface for data the caller already holds verbatim. Topics are untrusted\ntext, so the list is deduped and capped the same way the prompt block caps\nthem, and lands in an editable field the candidate reviews first.\n\nThe zod field is optional on purpose: builder drafts persist, and a\nrequired string would fail safeParse on every draft saved before this\nfield existed and discard the candidate's answers.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(export): keep contact lines out of the project tech-stack slot\n\nA line directly under a project title was read as the technology list on\nseparator count alone, so a resume that puts its project links on their own\nline got them styled as the tech stack.\n\nReject email, phone, a URL scheme, a markdown link and the known contact\nhosts instead. Not is_contact_shaped: it treats two or more separators as\ncontact-shaped, which is what a three-item stack looks like. URL_RE alone is\nalso not enough, since its scheme arm is anchored at the start of the line\nand a trailing link like \"Demo - https://example.dev\" slips past it; the\nunanchored :// test is what catches that.\n\nNo bare-domain test on purpose. Node.js - socket.io - Express is a real\nstack, and every heuristic that catches demo.example.dev catches socket.io\ntoo. A bare-domain link line stays accepted: that failure is cosmetic, the\nother one corrupts a genuine stack.\n\nBoth new guards were mutation-checked - reverting the predicate fails them.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-21T23:35:38+02:00",
          "tree_id": "ea90d0cf942efb1ffb86b903ecb5996c22d9bd48",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/1ff8aa3bb9c770b0bb74540833469f38cf314598"
        },
        "date": 1787349610870,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2205734,
            "range": "± 18746",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2604762,
            "range": "± 21237",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 312762,
            "range": "± 11986",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "c2df7bf6ea04914c8981ccf1961f4457a44ab228",
          "message": "fix(export): render projects from resumes imported as pdf or docx (#1072)\n\n* fix(export): recognize project titles in resumes imported from pdf or docx\n\nA project entry only opened on a leading bold run. Bold is markdown, and an\nimported resume has none - extraction keeps the words and drops the styling.\nSo a candidate's own CV carrying a perfectly formed project block\n\n    AI Job Hunter   aijobhunter.app\n    Tauri 2 - Rust - React 19 - TypeScript\n    Local-first Windows and macOS desktop application.\n\nstill rendered as six loose paragraphs, which is the exact flat output this\nfeature exists to remove. Reported from a real export.\n\nFall back to the shape when there is no bold: a short, non-sentence line whose\nNEXT line is a technology stack is a project title. The stack line is the\ndiscriminator - prose does not sit above a middot-separated list - and the\nline must not be a stack itself, or a two-stack sequence would open an entry\non the second one. Lookahead skips blanks, so a project's last description\nline sees the next project's title rather than stopping at the gap.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): group imported project entries by shape, and ask for a tech line\n\nSeeding shared the export path's bold-or-bullet rule for \"where does a project\nentry begin\", so an imported resume - which has no markdown at all, because\npdf and docx extraction keeps the words and drops the styling - fell through\nto the \"first line of the section\" arm. Measured on a real user source: three\nprojects collapsed into ONE seed whose description swallowed the next\nproject's title and stack. seed_projects_for_normalize then correctly refused\nto normalize from it, which is why a generated Projects section kept whatever\nfree-form shape the model chose.\n\nTeach project_entry_starts the same shape signal the export adapter now uses,\ndefined ONCE in export::parser::is_project_title_shaped and shared by both, so\nthe two can never disagree about where an entry starts - the property that\npredicate's own doc comment exists to protect. The same source now seeds two\nclean projects with no bail reason.\n\nAlso tell the job-ad resume prompt to put a project's technologies on their\nown line. Nothing asked for it before, so the model merged the stack into the\ndescription sentence and the export had no meta line to style. The builder\nprompt already carried this rule; the job-ad path did not.\n\nLimits pinned as tests rather than left to be rediscovered: the signal needs a\nstack line, so a prose-only Projects section still collapses, and the\nwhole-bail guards do not catch that shape. Pre-existing, unchanged here.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(export): bound the project shape signal to its section and to paragraph starts\n\nBoth review findings reproduced before fixing, and both were real.\n\nCross-section lookahead: next_content_line scanned the whole document, so the\nlast line of Projects could be made a title by a separator-bearing HEADING in\nthe next section. \"SKILLS - TOOLS\" did exactly that, and the German and French\nheading pairs have the same shape. It now stops at a section header, which is\nhow the two sibling groupings were already scoped.\n\nSentence-like prose: any line under 100 characters could open an entry, so an\nunpunctuated body line above a SECOND stack line became a title - measured,\n\"Used by 200 teams\" above \"Go - gRPC - Redis\" split one project into two.\nA word count would not have caught that phrase; what separates a title from\nbody text is that entries are blank-separated and a description line is not.\nThe rule now requires the line to open a paragraph.\n\nBoth call sites in the pipeline compute that flag before the blank filter\ndestroys it, so all three groupings keep answering alike. Both guards\nmutation-checked: reverting either fix fails its test.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-22T02:13:21+02:00",
          "tree_id": "6a7b995f8e6a2aa5fe60eb04b496b20f71e20de3",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/c2df7bf6ea04914c8981ccf1961f4457a44ab228"
        },
        "date": 1787359045796,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2325851,
            "range": "± 53470",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2847921,
            "range": "± 127113",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 318881,
            "range": "± 9503",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "52830b0c29885830bf7942af38865530167dba94",
          "message": "fix: make letter-spaced headings, plain-text links and placeholder sections behave (#1073)\n\n* fix(parser): collapse letter-spaced headings so their sections are visible\n\nDesigners set headings with wide tracking and some pdf producers bake it into\nthe text layer, so a real CV extracts its headings as\n\"S E L E C T E D   P R O J E C T S\". Every heading test in the parser, in\ndocuments::evidence and in SectionId is a string match, so that line matched\nnothing at all: the section was invisible. Measured on the reported document -\nseed_projects returned ZERO projects and the evidence extractor logged\nprojects=0, so nothing seeded, no links were collected, and the section\nrendered as body text. After the fix the same CV seeds four projects, each\nwith its own stack, and the normalize guards report no bail.\n\nSingle-character tokens are the signal; word gaps survive as runs of two or\nmore spaces, so the heading restores as \"SELECTED PROJECTS\" rather than one\nwelded word. The rewrite is gated on the collapsed form being a KNOWN heading,\nso an ordinary line can never be rewritten by accident, and it happens on the\nparsed text every consumer reads rather than in three separate matchers.\n\nWide tracking is an ATS hazard in its own right, so collapsing it is also what\na real applicant-tracking parser would need to see.\n\nAdds \"selected projects\" and \"core skills\", both from the reported CV, to\nSECTION_NAMES and to the shared TS parity fixture.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(extraction): recover plain-text links from a pdf with no annotation layer\n\nThe reference list a resume carries is built from a pdf's /Annot link layer.\nA CV typeset without real hyperlinks has none, so its urls survive only as\ncharacters - and every consumer downstream reads that list: the link injector\nthat re-attaches a project url to its item, and the resume seeder. Measured on\nthe reported document: zero links extracted, so the generated resume kept a\nproject url only when the model happened to copy one, and this time it did not.\n\nHarvest url-ish tokens from the text when the annotation layer is empty. The\nsame CV now yields three contact links and four body links where it yielded\nnone, and the injector has something to re-attach.\n\nA path-less host is only accepted for a TLD a technology name does not use.\n.io is excluded there on purpose - socket.io and crates.io are a library and a\nregistry, not the candidate's links, and nothing about their shape says\notherwise. A .io host WITH a path is unambiguous and kept.\n\nLocal to the extractor rather than a relaxation of factual::URL_RE: that regex\ngrades link Criticals, and widening it would change what counts as a claimed\nlink everywhere. Annotations still win when present, so an accurate anchor is\nnever replaced by a guessed one.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(export): drop a section whose only content is a parenthetical note\n\nA generator told to omit a section it has no content for sometimes writes the\nheading anyway plus a note explaining the absence. Reported from a real export:\ntwo headings advertising that the candidate has no awards and no publications,\nwhich is worse than printing neither.\n\npush_nonempty_section could not catch it - the section is not empty - so it\nreached the page as a real heading. Tested on the SHAPE, not on wording: the\nnote is written in the resume's own language, so a keyword list would be a\npermanent translation debt. A lone paragraph wrapped end to end in parentheses\nis a meta-comment about the document in any language; real resume content is\nnot written that way, and a section with real content that merely contains a\nparenthetical is untouched.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* test(egress): declare the extractor's scheme-prefixing literal\n\nThe link harvest writes a scheme onto a host the CV spelled out without one, to\nbuild the extractor's own markdown reference list. It builds a string and never\nfetches it, the same reason model/rich.rs is declared beside it.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(export): require a true single parenthetical, and keep balanced parens in a url\n\nBoth review findings reproduced first, and both were real.\n\nData loss: the placeholder-section test checked only the first and last\ncharacter, so \"(B.Sc.) Computer Science (2020)\" satisfied it and the whole\nEDUCATION section was silently deleted - measured, the section vanished from\nthe model. It now tracks depth and requires the opening paren to stay unclosed\nuntil the final character, so a paragraph that merely starts and ends with\nparens keeps its section.\n\nTruncated links: the url regex stopped at every \")\", so\n\"example.com/Function_(mathematics)\" lost its closing paren and became a dead\nlink. The regex now keeps \")\" and a balance check decides afterwards - a\nclosing paren survives only when an unclosed \"(\" inside the token is waiting\nfor it, so \"(see example.com/a).\" still trims correctly either way round.\n\nThe test module moved to model/adapter/tests.rs, matching model_docx.rs: the\nnew cases pushed adapter.rs past the R8 module cap.\n\nBoth guards mutation-checked - reverting either predicate fails its test.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-22T04:39:01+02:00",
          "tree_id": "92adb92a7835264fea3479de6d90bc8e02784471",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/52830b0c29885830bf7942af38865530167dba94"
        },
        "date": 1787367802838,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1712942,
            "range": "± 59439",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2042890,
            "range": "± 17196",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 188285,
            "range": "± 1832",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "1d70c491c3f327d84da5a81059a65da4689ad1d3",
          "message": "feat(scraping): make freehire its own selectable board (#1074)\n\n* feat(scraping): make freehire its own selectable board\n\nfreehire was only reachable as the aggregator's keyless last tier, so a user\nwho wanted it had to select the aggregator and hope every keyed provider\nfailed or came back empty first. It is now a board in the catalog, chosen\nlike any other.\n\nMoved out of the aggregator entirely rather than kept in both places, so no\nsearch fetches it twice and each board does one thing. The consequences are\ndeliberate:\n\n- The aggregator is keyed providers only again, so needs_keys() reports true\n  without keys and the needs-keys skip means what it says. That skip was\n  unreachable while an always-on keyless tier sat underneath.\n- A failure is now REPORTED instead of degraded to an empty result. Silent\n  degradation existed because nobody had opted into an always-on tier; picking\n  the board in the catalog is opting in, and an empty result would read as \"no\n  such jobs\" rather than \"the source is down\".\n- supports_location stays false and no country is sent. freehire's geography\n  facets are one OR-group - measured, countries=gb returns 89,211 and\n  countries=gb+cities=London returns 89,617, so naming a place WIDENS the\n  result. Claiming server-side support would switch off the engine's central\n  post-filter, the only thing actually narrowing this board to the location.\n\nThe tier's own rules went with the tier: last-rung position, skip-on-real-\nfailure and merge-behind-sparse-hits are gone, along with their tests and the\nflag in primary_chain that existed only to feed the skip-on-failure guard. The\nclient tests moved with the client and still pass unchanged.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs(landing): sync the architecture map's board count to the registry\n\nThe copy-drift guard reads the SCRAPERS registry, and freehire's registration\nmade the map's 24 stale.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs(scraping): drop the board count that just went stale, and thin the freehire section\n\nThe page opened with \"board registry (24 active scrapers)\". Registering\nfreehire made that 25, so the number silently became a lie - which is the\nfailure rule 17 describes, and bumping it to 25 would only reset the same trap.\nReplaced with a pointer to SCRAPERS itself.\n\nThe freehire section this PR added also spelled out the endpoint path. The\nmodule doc owns that, so the section now names what lives there instead of\nrepeating it.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-22T08:06:28+02:00",
          "tree_id": "d50b95a9316fd1ab762389a8b2432743bd05d221",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/1d70c491c3f327d84da5a81059a65da4689ad1d3"
        },
        "date": 1787380100582,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1696227,
            "range": "± 55237",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2043786,
            "range": "± 10924",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 189252,
            "range": "± 3048",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "fc339661d47229b12f5699d97933470e47d96c24",
          "message": "fix(scraping): send the requested location to freehire (#1075)\n\n* fix(scraping): send the requested location to freehire\n\nfreehire never received the location at all. `FreehireScraper::search` passed\n`None` for geography, so every search fetched the worldwide first page and left\nthe whole job to the engine's central post-filter — which can only narrow the\npage it is handed. A \"Berlin\" search fetched Johannesburg, Bengaluru and\nSingapore, then discarded 38 of 50 rows; the survivors were the ones with a\nblank or remote location the filter conservatively refuses to drop.\n\nThe old comment claimed freehire has no city parameter and that naming a place\nWIDENS the search. Half right. `cities` is documented, and geography is one\nOR-group — `countries=de&cities=Berlin` reads \"Germany or Berlin\" and is indeed\nwider. But a city ALONE narrows, which was never measured (live):\n\n  (none)                      1,348,012\n  countries=de                   41,154\n  countries=de&cities=Berlin     41,241   <- the widening that was measured\n  cities=Berlin                   8,000   <- never measured\n\nSo send exactly ONE geography parameter. The city is resolved through\nfreehire's own `/geo/cities` typeahead first, because the facet holds canonical\ndisplay names and matches nothing on a near miss (`Munich` -> 6,105,\n`Muenchen` -> 0). An unresolvable place falls back to the country, then to no\ngeography — the resolver is an optimisation, never a dependency, so its outage\ndegrades the filter instead of failing the search.\n\nTwo details the API forces:\n\n- Only the city segment is sent. The UI's location is a \"{city}, {country}\"\n  label and freehire splits a comma into two OR'd values, so passing it whole\n  widens (`cities=Berlin, Germany` -> 8,360).\n- The resolved city is re-checked against the country code client-side. City\n  names are not unique (\"London\" is both gb and ca) and the `cities` facet has\n  no country qualifier, so pairing it with `countries` would trip the OR-group.\n\n`supports_location` stays false on purpose: that flag switches OFF the central\npost-filter, and freehire's filter is coarser than the request (`cities=Berlin`\nlegitimately returns \"Remote (Karlsruhe, Berlin, Muenchen, Hamburg)\"). The\nserver-side parameter narrows the page; the post-filter still decides.\n\nLive after the fix, same query: 50 of 50 results in Berlin.\n\nFive tests added, each mutation-checked — reverting to no geography, sending\ncity and country together, dropping the country cross-check, and keeping the\ncomma all fail a distinct test.\n\nAlso corrects two docs that the fix turned into lies: ADR-0005 said freehire\n\"sends no location at all today\", and the scraping domain note pointed at\n`supports_location` for a geography rule that now lives on `fetch_freehire`.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: keep the freehire scraping note a thin pointer\n\nThe paragraph opens by promising \"shape only, because the remaining rules are\nload-bearing and must not be paraphrased here\" — and then the location edit\nparaphrased one: it spelled out the geography facet names and the OR-group\nbehaviour, both of which live on `fetch_freehire`. Name the topic, point at the\nsymbol, and let the source own the rule.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-22T16:58:15+02:00",
          "tree_id": "a5873d7d574e61ed2005e9a45392ca5054e30667",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/fc339661d47229b12f5699d97933470e47d96c24"
        },
        "date": 1787412144968,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2281471,
            "range": "± 22489",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2708825,
            "range": "± 32956",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 309589,
            "range": "± 12719",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "f2f28361247a3e57d5ff49d04909bd085e6043c8",
          "message": "fix(pipeline): stop a cover-letter-only run from generating a résumé (#1078)\n\n* fix(pipeline): stop a cover-letter-only run from generating a résumé\n\nPicking \"Cover letter\" in the Tailor wizard generated, validated, repaired\nand persisted a résumé, and then showed it instead of the letter. \"Both\"\nworked. Two defects stacked.\n\nThe choice was lost on the wire: useTailorPipeline encoded the three-way\noutputType as one boolean (includeCoverLetter: outputType !== 'resume'), so\n'cover' and 'both' sent a byte-identical request — there was no résumé flag\nto turn off. And the résumé stage was unconditional: CoverLetter::run had a\ngate, Draft::run had none.\n\nAdds includeResume (default true) to ResumePipelineRunSchema, threads it to\nQualityInput, and gates Draft::run exactly where CoverLetter::run gates its\nown — before the completer resolution, so a skip costs nothing. One stage\nlist, unchanged: paying_stages(), PIPELINE_STAGES and the renderer's step map\nare all derived from it.\n\nThree downstream readers needed scoping, two of which failed quietly:\n\n- validate no longer keeps a résumé report for a résumé that was never\n  written — an empty draft graded under DocKind::Resume is a dropped_role\n  Critical per employer, which parked the run at needsReview;\n- humanize no longer keys the WHOLE stage on the résumé's report. Its\n  `let Some(resume_report) = ctx.report … else { return }` head ran before\n  letter_flagged was computed, so a cover-only run skipped the letter's\n  polish pass and recorded a zero-flag artifact saying nothing was flagged;\n- save_verdict asks its two résumé questions only of a run that has one.\n  An empty draft returned Nothing (discarding the letter the run had just\n  paid four calls for), and is_persistable then called it Refused — a \"your\n  résumé came back without any of your work history\" banner on a run that\n  never asked for a résumé. persist_document passes report::build an Option,\n  so the wrapper omits the resume key and the merge leaves the posting's\n  stored slot, verdicts included, untouched.\n\nrepair needed no change: its ctx.report early return was already correct.\n\nRenderer: activeOut is derived from the run's target with a tab click as the\noverride (covering the cold remount, where no start() survives to seed it);\nthe tab list comes off target rather than off activeOut; a cover-only run on\na posting with an older tailored résumé still shows it, exportable but\nreview-inert — resume_pipeline_get reads report off the aggregate, so Fix\nsection would otherwise accept a click and rewrite a document this run never\nproduced.\n\nA cover-only run now costs 4 provider calls guaranteed and at most 6, down\nfrom 5 and up to 17.\n\nsave_verdict/is_persistable move to their own module: mod.rs crossed R8's\n1400-LOC cap.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(pipeline): close the two write paths a borrowed résumé tab left open\n\nBoth raised in review on #1078, both verified against the code first, and both\nintroduced by that PR: before it, a cover-letter-only run still produced its own\ndraft, so \"the aggregate has a résumé\" and \"this run wrote one\" were the same\nquestion.\n\nresume_pipeline_regenerate_section gated on a PROXY — ensure_latest_run plus a\nnon-empty ai_generations.resume_text. A cover-only run satisfies both: it IS the\nposting's newest run, and the aggregate still carries whatever an earlier run\nsaved. The command would splice, re-validate and persist a section of a résumé\nthis run never wrote, spending a provider call and overwriting the posting's\nreport on the way. execute now records metrics_json.resumeInRun — the same\nprovenance-in-metrics escape hatch sourceResumeId already documents, so no\nmigration — and the command refuses before it reads the aggregate. Absent reads\nas true: no migration touches the rows that predate includeResume, and every one\nof them wrote a résumé.\n\nuseTailorPipeline still handed Re-check the borrowed document. It re-validates\nwhatever is active and persistReports the merged wrapper back onto the\naggregate, so it would overwrite the posting's résumé report from a run with no\nrésumé of its own. Gated by withholding onReportChange rather than by a second\ncondition on recheck, so it reuses useQualityRecheck's own \"no session writer,\nno action\" rule instead of a parallel one that could drift.\n\nAudited every other activeOut reader in the hook rather than patching the two\nthat were reported: output and the export docType are read-only, and inline\nediting stays live deliberately — a hand edit is the user's own typing on a\ndocument they already have saved, which is the reason the tab is shown.\nresolve_fabrication needs no guard: it records a verdict, spends nothing, and\nlands in the same aggregate slot either way.\n\nThree of the new tests were mutation-verified the usual way. The fourth had to\nbe added because mutation testing found the hole: deleting the guard's CALL SITE\nleft both original tests green — they pinned run_wrote_a_resume and the metrics\nwrite, neither of which says the command consults either. A function nobody\ncalls is not a guard.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-24T22:02:31+02:00",
          "tree_id": "d4a1948e9f750684ecadeee34ee4a536d77d17fd",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/f2f28361247a3e57d5ff49d04909bd085e6043c8"
        },
        "date": 1787603169723,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2328918,
            "range": "± 38890",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2701131,
            "range": "± 71099",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 306183,
            "range": "± 5562",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "9bfb174a8d3daa84004183988c0463f46cb0f669",
          "message": "feat(autopilot): surface the strongest jobs across every autopilot in one place (#1083)\n\n* feat(autopilot): add cross-autopilot best-matches backend (phase 1)\n\nAdds the shared MATCH_TIER_CUTS constant (generated to Rust), the\ndismissed interaction type, the AutopilotContract.bestMatches wire\ncontract, and the autopilot_best_matches command that clusters every\nnon-archived autopilot's found jobs at query time and qualifies them\nagainst their own score kernel's tier cut. Renderer wiring is phase 2.\n\n* fix(shared): export the best-match wire types and harden the tier emitter\n\nThe contracts barrel re-exports a named list rather than `export *`, so the\nthree new AutopilotBestMatch* types never reached `@ajh/shared` and every\nconsumer failed to typecheck. Found by review, not by the pre-push hook, which\nruns gen:ipc:check but not typecheck from a consumer package.\n\ngenMatchTiers hardcoded four const names and built each f64 by concatenating\n\".0\", so a fractional cut point would emit invalid Rust and a new variant on\nMATCH_TIER_CUTS would be dropped silently — gen:ipc:check regenerates from the\nsame function, so it would compare the omission against itself and stay green.\nFormat with toFixed(1) and assert the variant set, verified by mutation.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(autopilot): stop found-job views colliding on the viewed badge\n\nInteractionStore::upsert keys on (job_id, interaction_type); scrape_persist_job\ndefaults job_id to \"\" when absent. AutopilotCard.handleJobClick never sent an\nid, so every autopilot found job collapsed onto the same (\"\", \"viewed\") slot\nand only the most-recently-opened job ever showed the Viewed badge. Pass\njob.url as the id, mirroring how the rest of the app keys interactions.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* refactor(autopilot): consume the shared match-tier cut points in scoretier\n\nmatch-band.tsx hardcoded the 55/30 and 75/50 band cut points; import the\nsingle shared MATCH_TIER_CUTS source (now also the Rust autopilot_best_matches\nqualification bar) instead of a second literal copy that could drift.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(autopilot): wire the best-matches ipc channel through the renderer\n\n- tauri-client autopilot namespace: bestMatches() -> invoke('autopilot_best_matches')\n- mock-client: bestMatches stub (unblocks typecheck against Phase 1's contract)\n- query-client: keys.autopilot.bestMatches, a deliberate CHILD of\n  keys.autopilot.all so every existing autopilot invalidation refreshes it by\n  prefix match with no extra wiring\n- use-autopilot: useBestMatches() service hook\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* refactor: point at the relocated salary-formatter import path\n\napps/desktop/src/renderer/lib/salary.ts moved to lib/format-salary.ts in\n7051055b (folded into that commit's staged changes from a shared worktree) so\na second feature (BestMatchRow) can reuse it without an illegal\nfeatures/applications -> features/best-matches cross-feature import. Update\nthe two references that still pointed at the old path.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(best-matches): add the cross-autopilot best-matches page and preview\n\nNew /best-matches route: sort by score (backend order, untouched) / newest /\nsalary, honest empty states (no autopilot has run vs. nothing has cleared the\ntier bar yet), a truncated notice when the backend capped the list, and a\nsalary-coverage caption on the salary sort. Row actions (View/Save/Apply/\nDismiss) and the \"Found by\" source chip -> /autopilot focus jump are shared\nwith the /autopilot top-3 preview strip via one BestMatchRow component\n(components/job/, since features/autopilot may not import features/best-matches)\nand one useBestMatchActions hook.\n\nDismiss hides the row optimistically (mirrors useRemoveAutopilot's own\noptimistic-delete pattern) with an inline \"Dismissed — Undo\" row, and its\npersistJob payload carries id + url + title + company — Rust matches a\ndismissal by deriving canonical_job_key(url, title, company) against every\ncluster member's own key, so a payload missing any of those never matches and\nthe row never disappears.\n\nApply resolves the source autopilot from sources[0].autopilotId against the\nuseAutopilots() cache and reuses use-apply-to-found-job.ts (lifted separately).\n\nAll scraped/LLM-generated fields (title, company, location, salaryCurrency,\nassistantNotes) render as plain JSX text nodes, never dangerouslySetInnerHTML;\nurl only ever reaches useOpenExternal, never a raw href.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(autopilot): surface the best-matches preview above the found-jobs list\n\nRenders <BestMatchesPreview /> after the error banner, before the card list\n(renders nothing itself while there are no qualifying matches). Also switches\nhandleApply to the newly-lifted useApplyToFoundJob hook, so /autopilot and\n/best-matches share exactly one Apply implementation instead of a second copy\nthat could drift.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(dashboard): exclude dismissed interactions from total tracked\n\nuseInteractions() with no filter returns every interaction type, so the\ndashboard's \"Total tracked\" tile was about to start counting the new\ndismissed type once best-matches ships it — inflating the headline number for\na job the user explicitly rejected, and silencing the empty-state hint\nwhenever only dismissals were present. Filter to an explicit allowlist\n(viewed/opened/applied/bookmarked) instead of \"everything except dismissed\",\nso a future sixth type needs a deliberate decision here too.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(documents): localize the dismissed activity-feed badge\n\nINTERACTION_TYPES had entries for applied/viewed/bookmarked only; a dismissed\nrecord fell through InteractionRow's forward-compat fallback and rendered the\nraw English word \"dismissed\" in every locale, once best-matches starts\nwriting that interaction type. Add the missing config entry (labelKey + icon\n+ colours), localized in en/de.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(i18n): add best-matches copy and the dismissed activity label\n\nNew bestMatches.* sub-tree + nav.bestMatches for the /best-matches page and\npreview strip, and resumes.activity.dismissed for the Activity feed's\ndismissed badge. Real German throughout, not English placeholders.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: regenerate api.md for the autopilot best-matches method\n\nPhase 1 added the bestMatches contract method (with TSDoc) but did not run\npnpm gen:api; docs/API.md was stale. Regenerated, no hand edits.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(best-matches): stop the dismiss success invalidation from killing undo\n\nhandleDismiss invalidated keys.autopilot.all on a successful persist. That\ninvalidation is a prefix match on keys.autopilot.bestMatches, so it forced an\nimmediate refetch — and the dismiss write is a local JSON persist that\nresolves in milliseconds, long before a user could ever reach for undo.\ncompute_best_matches already excludes a dismissed job from that refetch, so\nthe row was evicted from the query cache entirely; undoDismiss then deleted a\nkey from dismissedKeys for a row that no longer existed anywhere to render.\nAn affordance that renders but does nothing is worse than not shipping one.\n\nThe optimistic local hide is sufficient on its own: the backend is already\nauthoritative, so any natural refetch this hook doesn't force (a remount, a\nroute change, another autopilot mutation) reflects the dismissal once it\nhappens. Local state and the backend only ever disagree about *when* the row\nleaves, never *whether* it does. Dropped the success invalidation entirely.\n\nChecked handleView/handleSave for the same shape of problem: neither\nparticipates in compute_best_matches's qualification predicate, so there was\nnothing to invalidate — confirmed with dedicated tests, not by omission.\n\nThe old dismiss test asserted only that dismissedKeys gained then lost a\nkey — true even with the feature fully broken (a pure useState-setter test).\nReplaced/added:\n  - a unit test asserting no success callback is wired to the dismiss\n    mutation at all\n  - use-best-match-actions.dismiss-undo.test.tsx: a real-QueryClient,\n    real-AppClient integration test that renders the row, dismisses it,\n    asserts it's gone, clicks undo, and asserts it's visible again, with a\n    mock backend that (like compute_best_matches) excludes a dismissed job\n    from any later fetch\n\nMutation-verified: reintroduced the exact success invalidation, reran the\nnew integration test — red, failing even earlier than expected (the\noptimistic placeholder assertion itself fails, since the invalidation-driven\nrefetch evicts the row before that assertion runs, not just before undo).\nRestored the fix, reran — green. Full desktop suite green.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(autopilot): fix cross-scale scoring and cluster-id collisions in best matches\n\nThree independent reviews of the best-matches command found the same two\nroot causes: best-member selection and the row sort both compared a\nCombined semantic score against a Keyword coverage score on one raw axis,\nand canonical_job_key collisions across title/company blocks let unrelated\npostings share a cluster id. Both are fixed structurally - member/row\nordering now goes through the shared ADR-020 two-block rule, and the union\nis deduped by canonical_job_key before clustering ever runs, which also\nremoves a second full-struct clone of every found job. Also fixes: the\nuntested dismissed interaction type, applied-status matching only the\ncanonical url instead of every board copy, the command blocking the IPC\nthread instead of running via spawn_blocking, and several undertested\ncontract clauses (foundAt earliest, autopilotCount, tombstone veto, exact\nscore-cut boundaries).\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: close best-matches feature (adr-036, automation-domain update)\n\nAdd adr-036 documenting cross-autopilot best-matches query-time clustering\nwith two-block ranking (combined block first, then keyword), union dedup by\ncanonical_job_key, per-member qualification against own kernel's high cut,\npaused autopilot inclusion, and per-member dismissal identity.\n\nUpdate automation-domain.md with best matches subsection covering ipc contract,\nrust command (clustering, filtering, sorting), renderer surfaces (route, sort\noptions, dismiss action), and query-key strategy (prefix invalidation).\n\nAdd adr-036 to decision-records index in readme.md.\n\nAll validation gates pass: check:adr-citations ok, check:agent-system ok.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(autopilot): read best-matches assistant notes from the canonical member\n\nassistant_notes previously read whichever cluster member came first in\ninput order, which on a merged cross-autopilot row could surface a note\nwritten against a different autopilot's resume/provider context with no\nprovenance on the payload. Now prefers canonical, then best-scored, then\nany member, matching every other display field.\n\nAlso adds a regression test pinning is_degenerate_key's guard (deleting\nit does not fail any existing test) and corrects the clusterMembers\nTSDoc, which claimed the list excludes the canonical copy when the\nbackend always includes it -- the completeness dismiss-matching relies on.\n\n* docs: point at the payload cap and say non-archived, not active\n\nThe living automation-domain page copied the 100-row cap as a literal, which\nrule 17 exists to stop: tuning BEST_MATCHES_CAP would have left the page\nasserting a number the code no longer uses. Point at the constant instead.\n\nBoth pages also said \"active autopilots\" where the contract means non-archived\n— paused autopilots contribute, so \"active\" understated the source ceiling.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(best-matches): make undo actually reverse a dismissal\n\nUndo only removed a local dismissedKeys entry, so the persisted\ndismissed interaction survived every refetch/remount and the row kept\ndisappearing. Add InteractionStore::remove and a\nscrape.removeInteraction IPC round trip, then wire undoDismiss to call\nit (keyed on the same job url the dismiss wrote) and invalidate\nautopilot on success so the row genuinely comes back.\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-28T00:04:31+02:00",
          "tree_id": "e319d03d444cdd9da75e1bf6cdc3886e2bce5fd3",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/9bfb174a8d3daa84004183988c0463f46cb0f669"
        },
        "date": 1787869683017,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2195056,
            "range": "± 18268",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2548474,
            "range": "± 18093",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 297096,
            "range": "± 5749",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "93b09cd68ad2e746dfff944378e91096adc0bd67",
          "message": "feat: agent-facing CLI so an AI agent can drive the app (#1085)\n\n* feat(bridge): serve a read-only agent.query surface over the loopback bridge\n\nIssue #1084 asked for a way for an AI agent to read the app, which has no\nsurface an agent can reach. Rather than a localhost HTTP server, this serves\nfive read-only resources over the loopback bridge the native host already\nrelays to, so there is no new port, listener, token or origin allowlist.\n\nResources: best-matches, job <url>, profile, automations, schema. `url` is the\ncross-resource key, not an id -- BestMatchRow.key is a cluster id, not a stable\nhandle. There is deliberately no apply or submit verb.\n\nEvery payload is an allowlist projection modeled on AutofillProfile, not a\ndelegated record. Autopilot carries resume_text, cover_letter, assistant_notes\nand full found_jobs descriptions; the projection structs have no field that can\nexpress them, so they are absent by construction rather than by omission.\n\nAgentQueryThrottle lives on BridgeState, not per-connection: every CLI\ninvocation is a fresh process and socket, so a per-connection bucket would be\nbypassed by construction. Two independently-sized buckets -- best-matches gets\nburst 1 / 30s against its measured 12.3s worst case, cheap reads burst 10 / 1s.\n\nKnown limit: best-matches calls the existing command unmodified, so the\nclustering compute stays unbounded per admitted call; the tight bucket bounds\nrepetition, not one call's cost. A compute-side cap belongs to the matching\ndomain.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(cli): add an agent mode that reads the app over the loopback bridge\n\nCompletes issue #1084 PR 1. `ajh-tauri agent <verb>` connects to the running\napp's bridge, sends one agent.query, prints JSON on stdout and exits. It is a\nmode of the existing binary, not a second [[bin]] -- the release upload globs\nonly read target/release/bundle/**, so a second binary would ship to nobody.\n\nThe sentinel sits below the native-host short-circuit and above run(): run()\nforks the minidump supervisor as its first act, and the single-instance plugin\nwould otherwise hand this argv to the running GUI, popping its window instead\nof ever printing.\n\nconnect_bridge returns on the first successful WS upgrade, before any protocol\nframe, so a port squatter would take the CLI down and read as a pairing\nfailure. This adds a separate port loop that carries the full v2 mutual\nhandshake per port and accepts only the port whose server proof verifies,\nleaving the native host's path untouched. verify_server_proof is the missing\nclient-side counterpart to the existing verify_client_proof.\n\nOn Windows the release build is windows_subsystem = \"windows\" with\npanic = \"abort\", so a bare interactive run has a NULL stdout handle and\nprintln! aborts. GetStdHandle is probed FIRST and an inherited pipe -- the\nagent case -- is left alone; only an invalid handle attaches to the parent\nconsole. Attaching unconditionally would replace the pipe.\n\nThe binary is not on PATH on Windows, macOS or Linux AppImage, so the app now\nwrites a pointer JSON carrying exePath and dataDir on every launch, on\nregister_native_host's existing best-effort lifecycle. home_dir() read only\n$HOME, which is unset on Windows, so the pointer would silently never have\nbeen written there; it now mirrors data_dir()'s USERPROFILE-then-HOME.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(cli): report a missing pointer as app_not_located, not app_not_running\n\nEnd-to-end verification against a live bridge reported `app_not_running` while\nthe app was demonstrably running and listening -- the pointer file simply had\nnot been written, and an unreadable token collapsed into the same sentinel.\nThree distinct causes shared one misleading name, and it sent this feature's\nown verification pass looking at the wrong thing for several minutes.\n\nA missing pointer is now `app_not_located` and an unreadable token is\n`pairing_token_unavailable`; the port-loop buckets are unchanged.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(cli): bound next_json by an absolute deadline instead of re-arming it\n\nnext_json re-armed its timeout on every loop pass while silently skipping\nping/pong frames, so a peer pinging faster than the budget kept it blocked\nindefinitely. That also defeated the caller's bound: send_agent_query\nrecomputes a shrinking `remaining`, but that recomputation never runs while\nnext_json is stuck inside its own loop. A squatter on any port in the range\ncould therefore stall a whole invocation -- exactly what the module doc\nclaimed it prevented.\n\nThe deadline is now computed once on entry and shrinks each pass, the pattern\nsend_agent_query already used. The ping-skip branch had zero coverage; the new\nregression test floods pings every 10ms against a 150ms budget and was\nconfirmed failing against the unfixed code before the fix landed.\n\nAlso pins two properties that no test held: the agent throttle is now driven\nthrough BridgeState across a simulated reconnect (mutation-checked -- making\ntry_acquire_agent always return true fails it), and the two new wire constants\njoin reserved_types_are_distinct. Their omission from the TS-parity test is\ncorrect and stays, but distinctness is a different invariant with no TS\ndependency and should not have inherited that exclusion.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs(cli): correct the console probe's claim that nothing can panic\n\nThe comment said this function \"never panics either way -- the caller degrades\nto a non-zero exit with no printed JSON, not a crash\". The first half is true\nand the second is not: when AttachConsole fails, stdout stays invalid and\nprintln! panics, and since release sets panic = \"abort\" and the CLI\nshort-circuits above run() (so crash reporting never initialised), that is a\nsilent abort with no message and no report.\n\nThe probe-before-attach design is right and unchanged; only the claim was\nwrong. The comment now says what it actually costs, and names where the fix\nbelongs -- the print paths, via writeln! with the Err ignored.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(bridge): fence scraped descriptions and close the security review findings\n\nAdds `ajh-tauri agent --help` (also -h and a bare `help`), generated from the\nsame table parse_verb matches on so it cannot advertise a verb that does not\nparse. It works with the app closed -- no pointer, token or socket -- because\nhelp that needs the thing you are trying to reach is useless.\n\nHIGH: job descriptions are third-party-authored text emitted to a consumer\nwhose whole purpose is \"an AI agent reads this\", and answer_assist.rs already\nfences the identical string. They now go through prompt_fence with the same\ntag and cap. Verified on real data: an 8697-char posting comes back wrapped\nand truncated.\n\n\"Absent by construction\" held only at the top level -- `trust` was the source\nstruct serialized whole, and both guards were blind to it (one matches a name\nlist, the other only top-level keys). Adds an AgentTrust allowlist and makes\nthe key assertions descend into nested objects.\n\nAlso: agent.query gets its own origin so the server can tell the CLI from the\nextension arriving via the relay, and is gated on it; it is spawned instead of\nrunning inline, so a multi-second call no longer stalls the read loop or its\ntoken.revoked observation; an unvalidated dataDir can no longer be a UNC path,\nwhich turned a local file read into outbound SMB; the timeout sentinel is\nreachable instead of dead code and a wrong-type reply on our own reqId fails\nfast as unsupported_by_app rather than burning 30s; and usage errors no longer\necho the typed argv, which could be a path with a username in it.\n\nmod.rs would have crossed the 1400-line cap, so the self-contained autofill\nsection moved to autofill_profile.rs unchanged.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(agents): wire cleanup and test-author to the skill contracts\n\nAn audit of the fleet found two agents whose skills could never reach them.\ncleanup holds Edit/Write and its job is DELETING code, yet it referenced no\nskill at all -- not author-contract, not token-efficiency -- so the repo's\nhighest-stakes writer operated with no smallest-diff discipline, no mandatory\nvalidation gate, and no never-approve-your-own-work rule, despite CLAUDE.md\nstating that every author loads author-contract.\n\ntest-author named testing-rules, author-contract and token-efficiency but\nnever as .claude/skills/<name>/SKILL.md paths, unlike the other 23.\n\nThis is the same defect class as the project-steward skill gap: a skill is\ninert unless an agent file names it, and naming it is not the same as naming\nwhere it lives. Audit command: for each agent, rg -q \"skills/\".\n\nNote CLAUDE.md files cleanup under \"Cross-cutting critics (no author)\" while\nit holds Edit/Write -- a writer shelved with the read-only agents, which is\nplausibly how it slipped the rule. Not changed here.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs(agents): reclassify cleanup as a writer, not a cross-cutting critic\n\ncleanup holds Edit/Write and deletes files, exports and dependencies, but it\nwas listed under \"Cross-cutting critics (no author)\". That grouping is\nplausibly why it loaded neither contract skill until the previous commit: the\nrule says every critic loads critic-contract and every author loads\nauthor-contract, and an agent filed under critics that is actually a writer\nfalls between them.\n\nIt now has a row in the author/critic table. Its critic is the owning domain\ncritic rather than a dedicated pair -- a deletion is reviewed by whoever owns\nthe code being deleted -- so no new agent file and no PAIRS entry.\n\nThe contract rule now states the test explicitly: holding Edit/Write makes an\nagent an author for that rule, wherever it sits in the table.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(agents): teach every critic how to state and hold a finding\n\nThe critic contract covered how to ATTACK a diff but nothing about the quality\nof the findings that come out, and all 13 critics load it. Three additions,\neach from measured evidence rather than taste:\n\nQuote the exact span. A finding that cannot name a file:line and the verbatim\ncode cannot be verified or refuted by anyone else, and it is what makes a\nrefutation stage possible at all.\n\nTake the UNKNOWN escape hatch. A model given no way to say \"I lack the\ninformation\" manufactures a justification instead -- which is how a nitpick\ngets written up as a CRITICAL. Related: assume most findings are wrong. The\nbest measured automated review system scores about 17% precision, so dropping\na weak finding costs nothing and shipping one costs credibility.\n\nOnly new evidence moves a severity. Models measurably abandon correct answers\nwhen merely challenged -- \"are you sure?\", \"that's intentional\" -- and do so\nfrom high confidence, so the pull toward conceding says nothing about the\nfinding's merit. A revision must name the line, result or spec clause that\nmoved it; \"the author explained it\" is not that, since the handoff was already\ncontext and never evidence.\n\nAlso separates functional from stylistic findings, with only functional ones\nblocking: reviewers score far better on logic and resource defects than on\norganisation or style, and mixing them buries what matters.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(agents): add the agent-cli author/critic pair, skill and review command\n\nThe agent CLI had no owner in the fleet. It now has the standard pair plus an\nagent-cli-standards skill carrying the rules the surface has already learned\nthe hard way, and a /review-agent-cli command.\n\nThe reviewer runs on Opus while the author runs on Sonnet deliberately, not for\ncapability: self-preference bias in LLM-as-judge is measured and directional,\nso a critic sharing the author's model family grades its own habits. The\ntiering paragraph in CLAUDE.md is the checker's single source, so it moved in\nlockstep.\n\nThe skill encodes what external research changed about the design. Fencing\nuntrusted text is attenuation, not a gate -- adaptive attacks defeat over 90%\nof published prompt-injection defenses -- so untrusted content may supply data\nbut never control flow: a job posting must not influence which verb runs, which\nid it targets, or whether a confirmation is satisfied. The threat is live\nrather than hypothetical: about 1% of 200,000 real resumes carried injections,\nrising sevenfold in 16 months, and employers have begun seeding postings.\n\nFor mutations it adds exit code 4 and a confirmation envelope instead of an\ninteractive prompt, because an autonomous caller cannot answer y/N and will\nreach for --force instead; the severe tier takes the resource's own name, which\nis the one confirmation a hallucinated argument cannot satisfy; an empty\nselector must error rather than mean \"all\"; and unknown verbs and flags are\nhard failures with no fuzzy matching or prefix abbreviation, which is the path\nfrom a typo to a destructive neighbour.\n\nWires all five integration points: the CLAUDE.md table, review-routes primary\nglobs and lessons domain, the checker's PAIRS list, and the public fleet\nexplainer on the landing site.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* chore: re-anchor two leak-guard allowlist entries after line shifts\n\nThe leak guard keys its allowlist by file:line, so inserting write_agent_pointer\nabove the current_exe() warn moved register.rs:114 to :138, and the bridge\nchanges moved mod.rs:483 to :507. Both sites are pre-existing and unchanged;\ntheir recorded reasons still hold verbatim -- current_exe() takes no input path\nto leak, and TcpListener::accept() carries no peer address.\n\nOnly the line numbers moved. Flagging the brittleness rather than fixing it\nhere: any edit above an allowlisted site breaks the guard in both directions at\nonce, reporting a fresh violation and a stale entry for what is one displaced\nline.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(landing): add the agent-cli pair to the fleet map, and address review\n\nCI Tests went red on two landing tests. The fleet explainer has TWO structures\nand I updated one: AUTHORS/CRITICS feed AGENT_COUNT, while the constellation\nrenders from PAIRS + CROSS_NODES. Adding the pair to the first moved\nAGENT_COUNT 25 -> 27 while the map still emitted 25 nodes, so both\n\"renders exactly AGENT_COUNT nodes\" assertions failed. Those tests are doing\ntheir job -- they pin the two structures to each other.\n\nI ran the landing typecheck before pushing but not its tests, which is what let\nthis through; a typecheck cannot catch a count that two structures disagree on.\n\nAlso addresses PR review findings in the agent files:\n\nThe completion gates listed fmt/clippy/test/architecture, but\ndocs/architecture-rules.md names deny, audit and machete too (R10-R13), plus\nthe EGRESS inventory as an arch test. Both the skill and the author now list\nthe full gate.\n\nmsg.rs, auth.rs and the mod.rs dispatch arm carry CLI frames but belong to the\nextension protocol. Rather than take those globs, the reviewer and command now\nscope the CLI's slice of them and hand off to extension-reviewer explicitly, so\na CLI frame added in msg.rs cannot end up with no reviewer.\n\nNot taking the MD041 suggestion to add H1 headings after front matter: zero of\n27 agent files have one, the repo has no markdownlint config and does not run\nit in CI, so adopting it would make four files inconsistent with twenty-three\nto satisfy a linter this repo does not use.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(cli): bound the connect and upgrade, and cancel a query on revocation\n\nSix review findings from PR #1085, each verified against the code first.\n\nThe two UNBOUNDED steps in an otherwise fully budgeted function: connect and\nthe WebSocket upgrade. A process that accepts on a PORT_RANGE port and never\ncompletes the upgrade -- a wedged previous instance whose listener is still\nbound but whose accept loop stopped -- parked the whole port loop forever. Both\nare now wrapped, and the whole invocation carries an outer deadline. Proven by\nstanding up a real TCP squatter: the built binary now returns app_not_running\nin ~15s instead of never returning.\n\nThe UNC guard only rejected a literal \\ or // prefix, but Windows treats the\nseparators interchangeably, so /\\host\\share and \\/host/share were absolute,\nparsed as a UNC root, and passed -- defeating the SMB/NTLM mitigation the doc\nclaimed. Now checks either separator in either position.\n\nThe unknown-flag branch still echoed argv into the exit-2 detail; that leak was\nfixed for the unknown-verb branch and missed one case over, with the test\npinning only the branch that was fixed. Usage errors now also carry a null\nresource so every exit-2 reply has the same shape.\n\nAn in-flight agent.query was not cancelled on token revocation -- a gap\nintroduced by the earlier fix that moved dispatch off the read loop, which is\nwhere fresh defects come from. It now races a per-connection cancellation token\ncancelled at the shared teardown site. Two honest limits: best-matches' blocking\nCPU pass has no cooperative checkpoint, so the reply is suppressed and the\nconnection freed but running compute is not preempted; and spawn_answer_assist\nhas the SAME stale-reply-after-cancellation gap, which was left unfixed rather\nthan copied, and needs its own follow-up.\n\nAccumulated docs pushed agent_cli.rs past R8's 1400-line cap, so the test module\nmoved to agent_cli/tests.rs, the split convention commands/autopilot.rs uses.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-08-31T20:44:22+02:00",
          "tree_id": "ba97f428169d1947a8245f3f2e84241f662b39be",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/93b09cd68ad2e746dfff944378e91096adc0bd67"
        },
        "date": 1788202484084,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 1762074,
            "range": "± 30052",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2080785,
            "range": "± 15793",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 242118,
            "range": "± 5569",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "38b990d0e9491845313946ad7de304e8a492d15c",
          "message": "feat: generic command dispatch for the agent CLI, read effects first (#1087)\n\n* feat(cli): classify every command in a policy table the build enforces\n\nPhase 1 of ADR-038. Every registered command now carries a declared Effect --\nRead, Reversible, Irreversible, or NotExposed with a reason -- and a test\nasserts the table and generate_handler! agree in both directions. That is\nADR-014's static-allowlist invariant applied to inbound dispatch: a new command\ncannot ship without being classified, because the build fails first.\n\nNo dispatch, no new verb. The table is data; the phases that use it come next.\n\nEffects were assigned by reading each command body, not from its name.\nAnything that could not be verified was classified pessimistically, since an\nover-cautious row costs a ceremony and an over-permissive one costs data. Three\njudgment calls worth knowing: ai_set_embedding_config clears only recomputable\nderived caches so it is Reversible, while scrape_clear_postings destroys\nuser-visible interaction history and is not; and support_export_diagnostics\ntakes a caller-supplied destination straight to File::create, which truncates\nwhatever is already there, so it is Irreversible for the overwrite rather than\nfor the export.\n\nTwo stubs found while classifying, both relevant to the dispatch phase:\nai_unload_model ignores its argument and always reports success, and\nsupport_get_system_info returns null behind an implement-when-needed comment.\nWired through by name, a caller would get a convincing false success from both.\n\nThe guards are mutation-checked in both directions -- removing a row fails the\ncount and the set equality, adding a bogus one fails the other way. The count\nassertion is a hand-written literal rather than derived from either source,\nbecause a test that loops over the table it guards covers additions only.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(scraping): seed the company typeahead from a vendored slug directory\n\nADR-030 built passive slug harvesting and named this feeder in advance: the\nsource column is free text \"so future feeders (active probers, extension\nfingerprinting, community slug directories) require no schema changes\". Until a\nuser has organically scraped enough postings the typeahead is empty. This\nvendors ~27k slugs across the four platforms extract_ats_ref actually parses --\nbamboohr, greenhouse, lever, ashby. icims, paylocity and workday come from the\nsame upstream and are deliberately excluded: no extractor, so listing them\nwould advertise companies we cannot scrape.\n\nDeliberately NOT wired into the existing curated-seed list. boards/ats_seed.rs\nlooks like the natural home, but engine/mod.rs feeds it into seeded_companies,\nwhich the engine scrapes when the user leaves the company field blank --\nextending that to 27k entries turns one blank-field run into thousands of live\nrequests. The vendored directory is reachable only from the discovery typeahead\nand nothing else, and no rows are inserted: a slug becomes a discovered_companies\nrow only if the user stars it, through the pre-existing path.\n\nFormat follows geodata/cities.tsv.gz -- gzip embedded via include_bytes!, parsed\nonce, with recorded digests. 148 KB on disk, no new dependency, no runtime\nnetwork. Measured rather than assumed: index build ~2.9ms once, worst-case\nsingle-letter query ~1.8ms, typical ~0.5ms, against a 250ms UI debounce.\n\nLicensing is the part worth reading twice. Upstream's LICENSE is MIT, but its\nREADME carves the data/ datasets out under CC BY-NC 4.0, requiring attribution\nand forbidding commercial use without permission. This app is PolyForm\nNoncommercial (ADR-023) so there is no conflict today, but ADR-023 exists to\npreserve commercial licensing -- so this data is isolated in one directory that\ncan be deleted wholesale if that ever happens. Attribution to Riley Dorrington,\nthe upstream commit and per-file SHA-256 are recorded in ats-slugs/README.md.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(scraping): log the decode error kind, not its message\n\nThe leak guard flagged the new gunzip warn site. The error is an io::Error from\na reader over an include_bytes! slice compiled into the binary, so it carries no\npath or host and could have been allowlisted with that reasoning.\n\nLogging e.kind() instead removes the need for the argument entirely: a fixed\nenum cannot carry free text, so nothing has to stay true about the error's\nDisplay for the site to be safe. Fewer safety arguments beats stronger ones.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(scraping): stop the rate limiter underflowing its own window\n\nwait_for_slot sampled `now` BEFORE awaiting the requests lock, so while it\nwaited another task could record a timestamp later than `now`; the window sweep\nthen evaluated `now - t` with t > now and panicked with \"attempt to subtract\nwith overflow\". That is the intermittent failure that has been rotating around\nthe scraping tests and blocking pushes -- it is a real bug in a rate limiter,\nnot a flaky test.\n\n`now` is now sampled after the lock, which removes the race at its root. The\nsweep also uses saturating_sub, for an independent reason: timestamps are\nrecorded in a different critical section, so a backward wall-clock step can put\nt ahead of now even with correct lock ordering.\n\nThe regression test plants a future timestamp and was confirmed to reproduce\nthe exact panic before the fix.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(cli): dispatch read-effect commands generically through the bridge\n\nPhase 2 of ADR-038. `agent call <ns>:<command> --input '<json>'` reaches any\nregistered command whose policy row says Effect::Read; every other class\nrefuses with its own sentinel. Dispatch goes through Webview::on_message with\nAppHandle::invoke_key -- the route Tauri's own docs name for this (\"the invoke\nkey that must be referenced when using InvokeRequest\") -- so the real command\nbody runs, and the spend and rate limits living inside those bodies apply\nunchanged. No codegen, no service-layer shortcut that would have bypassed them.\n\nThe request URL is read live from the main window rather than hardcoded.\non_message compares it against the app's configured protocol origin, which\ndiffers across macOS, Windows and dev builds; reading the window's own URL is\ncorrect on all of them without special-casing any. The invoke key is read fresh\nper call and only ever placed in the request struct -- never logged, echoed or\nreturned, per Tauri's warning not to expose it.\n\n`dispatched`, not `ok`: a command's own in-band error becomes the data payload\nwith dispatched:true, because ~47 commands report failure inside a Value and\nthe dispatcher cannot honestly claim to know. The curated five keep a truthful\nok. Scraped text is still fenced -- verified against a live posting fetch, not\na fixture.\n\nThe two stubs found in Phase 1 are now NotExposed rather than Read.\nai_unload_model ignores its argument and support_get_system_info returns null,\nso classifying them by \"does this mutate state\" was right when nothing\ndispatched and wrong the moment something did: by name they would hand back a\nconvincing false success. Reclassifying reuses the mechanism every other\nNotExposed row uses instead of a bespoke blocklist.\n\nautopilot_best_matches shares the curated tier's tight throttle bucket rather\nthan getting its own, so alternating between the two tiers cannot double an\nallowance.\n\nAlso re-anchors a leak-guard allowlist line that moved again. Third time this\nbranch: the guard is keyed by file:line, so any edit above an allowlisted site\nbreaks it in both directions.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(docs): re-pad the index table row that broke the format check\n\nMy earlier edit shortened the adr-038 title without re-padding its table cell,\nleaving it four spaces wide. That landed on main, so `prettier --check` is\ncurrently failing there and every branch cut from it inherits the failure --\nincluding this pull request, where it surfaced as a red Lint & Format job.\n\nI initially concluded main was clean by checking a copy of the file in a temp\ndirectory; prettier resolves config by path, so that test used different\nsettings than the repo does and passed for the wrong reason.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(scripts): key the leak allowlist by message text, not line number\n\nThe allowlist was keyed by file:line, so any edit above a declared site broke\nthe guard in both directions at once -- a phantom \"undeclared\" violation and a\nstale entry, for one unchanged line that merely moved. It fired three times on\nthis branch alone, each time blocking a push and each time fixed by hand-editing\na number.\n\nThe mechanism already existed: a `sig` field holding the call site's own format\nstring, added earlier but wired to exactly one of 45 entries, with a header note\nsaying migrating the rest was deferred. That deferral is what kept breaking --\nboth entries that recurred were ones never migrated. All 45 now carry a `sig`,\ngenerated from the checker's own output rather than transcribed by hand.\n\nThe half that was missing: `sig` was only consulted when a line had moved, so\nediting a message in place kept passing because line-exact matching checked the\nkey and never the content. A declared entry is now satisfied only if the site\nat that key still carries the same signature, so a rewritten message falls\nthrough to the undeclared and stale checks exactly as a move would. Verified\nindependently: shifting a site five lines keeps the guard green, rewriting its\nmessage in place is caught.\n\nAlso replaces a literal NUL byte used as a bucket separator with the `\\0`\nescape -- same value, but the file stops registering as binary, which is what\nmade ripgrep refuse to search it.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: make the vendored-slug note a pointer, not a copy\n\nThe section I added restated the upstream commit, the platform list, the\nlicense carve-out and the embedding mechanism -- all of which already live\nnext to the data in ats-slugs/README.md. Rule 17 exists for exactly this: the\nplatform list in particular would have gone stale the moment a parser was\nadded, since the real set is whatever extract_ats_ref can parse.\n\nIt now says what the data is for, that it needs no network, and where the\nprovenance and license terms actually live -- with the one warning worth\nrepeating at the call site, that the datasets carry different terms from the\nupstream repo's own code.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat(cli): dispatch reversible commands and gate irreversible ones on a withheld proof\n\nPhases 4 and 3 of adr-038. Reversible rows dispatch through the same path Read\nalready used. Irreversible rows demand a proof the refusal deliberately does\nnot contain.\n\nThere is no derivable dry-run -- no simulation path exists anywhere in the app\n-- so the entire safety property comes from where the proof lives. The refusal\nnames which read command yields it and withholds the value; a mismatch says\nonly that it did not match, never what would have. Satisfying it therefore\nrequires a second call to a different verb, and so requires having actually\nread the record. A one-hop ceremony that hands back its own answer stops\nnothing. Verified live rather than by unit test: the refusal bytes were grepped\nfor the real value and it does not appear, in either the initial refusal or the\nmismatch.\n\nA test pins every proof source to being itself a classified Read row, so a\nproof can never require something the caller is not allowed to fetch.\n\nNot every row gets a strong proof, and the weak ones say so in place rather\nthan looking strict. Deletes keyed by id ask for the record's own name and are\ngenuinely strong. Commands with no record to read fall back to a scalar --\nprivacy_reset_app is the weakest row in the table, and regenerate_token asks\nthe caller to prove reading a token it already holds locally. Those are\nrecorded as known-weak instead of dressed up.\n\nAlso closes four review findings: ai_embed is NotExposed until its missing\nspend charge lands, fencing now recurses the whole response tree rather than\none level, dispatch emits a span carrying only the command and outcome, and the\nhandler-list extraction skips comments before finding its terminator.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(ai): gate ai_embed against unbounded paid embedding spend\n\nai_embed reached a paid provider with no rate guard, no concurrency guard and\nno daily charge -- unlike ai_generate beside it, whose gate exists so \"a\nlooping/XSS'd renderer can't drive unbounded paid-API spend\". It was reachable\nfrom the renderer already; the agent CLI made it loop-friendly, which is how it\nsurfaced.\n\nThe charge lands on the EMBEDDING provider, resolved through the same\nembedding_config read documents::embed makes internally -- not on the\ngeneration provider, which would have silently throttled something unrelated.\n\nIts own bucket rather than reusing the generation caps: embedding is one cheap\nforward pass with no output tokens, so the generation ceiling would throttle\nordinary rapid embedding long before real abuse. The daily provider ceiling is\nshared, which is correct -- that bucket is already per day and per provider.\n\nAlso clamps the input. A request-count cap alone is not a cost cap here:\nembed_adaptive chunks oversized input into up to 32 provider round trips rather\nthan rejecting it, so one admitted call could bill dozens. The shared schema\ndeclares max(200_000) but that is zod, renderer-side only, and serde enforces\nnothing -- the same validation gap this repo has already documented twice\nelsewhere. It now clamps in Rust through the existing helper.\n\nBulk indexing and match scoring are unaffected: both call documents::embed\ndirectly and never pass through this command, confirmed by reading the callers\nrather than trusting a blast-radius tool.\n\nThe refusal test was mutation-checked -- removing the charge fails it, and the\nwrong-provider and normal-call tests correctly stay green.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(cli): replace two ceremonies that proved nothing\n\nBoth rows satisfied the letter of the confirmation gate and none of its\npurpose: each asked the caller to prove reading something it already held.\n\nregenerate_token is now NotExposed. Its status payload is {port, connected,\ntoken}; the CLI already knows port and token by construction, since it scans\nthe range and reads the token off disk before it can authenticate at all. And\nconnected is not a coin flip -- is_connected() is connected_count > 0, and the\nCLI's own authenticated socket increments it, so it reads true for the whole\nduration of every ceremony attempt. A hallucinated value could not lose. No\nother reachable read is bound to the pairing session, and rotating a pairing\ntoken from a shell has no job-hunting workflow behind it, so an honest refusal\nbeats a gate that cannot fail.\n\nprivacy_reset_app now counts applications rather than ai generations: a store\nthe reset actually wipes, holding the user's own tracked search rather than a\nsecondary table of model output. It keeps the property that its strength scales\nwith the stakes -- a populated store yields an unguessable number, and a user\nwith none has nothing to lose. Still partial, one of nineteen stores, and said\nso in place.\n\nThe exhaustive gate test added alongside proves less than its name suggested,\nand its doc now says so: it walks every row asserting the gate agrees with the\ndeclared effect, but the assertions key off that same declared effect, so a\nmis-classified row moves to a different self-consistent branch and passes.\nMutating the gate's own match arm is what fails it. Classifications remain\nguarded by reading, and by the count and set tests -- not by this.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(scraping): make the rate limiter monotonic and fill the typeahead page\n\nMy earlier fix here swapped an underflow panic for a stall. saturating_sub\nmakes a future timestamp yield zero, which is inside the window, so the entry\nwas RETAINED as a recent request and a full window then waited until that\ntimestamp plus a whole window. A backward clock step could delay every request\nfor the window plus the skew. My regression test left 29 of 30 slots free, so\nit never touched the blocking path at all -- it proved the panic was gone and\nnothing about what replaced it.\n\nThe timestamps are now Instant rather than SystemTime millis, which removes the\nclass rather than patching it: Instant is monotonic, so no clock step can put a\nstored entry ahead of a later sample. Verified first that they never leave the\nmodule -- no serialization, no cross-process reader -- so nothing blocked the\nchange. A min(window) cap on the computed wait makes the bound provable rather\nthan merely argued, and that is what the new full-window regression test\nexercises; removing the cap fails it with an hour-long wait.\n\nThe under-throttling direction is covered too, since over-throttling live\nscraping would be a user-facing regression: disabling the gate fails that test\nat 400ns.\n\nSeparately, the vendored typeahead returned short pages. The vendor pool was\nsized to the remaining capacity and dedup ran afterwards, so any collision\nsilently cost a row. The pool is now sized to absorb every possible collision\nand the cap is applied after dedup. Both halves are mutation-checked, the first\nreproducing the reported 49-instead-of-50 exactly.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(ui): surface the vendored slug directory's licence credit in the app\n\nThe branch compiles ~27k company slugs into the shipped binary, and that data\nis CC BY-NC 4.0 -- attribution is a condition of distribution, not a courtesy.\nIt existed only in source comments, the data directory's own readme and\ndocs/SCRAPING_ENDPOINTS.md. The data shipped to users; the credit did not.\n\nIt now rides the credits line that already carries the GeoNames and Photon-OSM\nattributions, rather than a parallel key or a new screen -- that surface exists\nprecisely for this, and the ensemble's suggestion to \"add\" the key was wrong,\nit needed extending. Both shipped locales carry it; en and de are the whole set.\n\nThe test asserts the credit renders in the DOM rather than that the string\nexists in a json file. A locale key nothing reads is exactly how this class of\nbug hides, so the check reads the same path the live screen does.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(cli): stop reporting failed calls as dispatched and repair three ceremonies\n\nSix blocking findings from the pre-PR ensemble, each independently verified.\n\nA tauri-level invoke error -- bad arguments, an acl denial, no such command --\nwas folded into the ok path, so a call whose body never ran came back\ndispatched:true and exit 0. Deleting an application while omitting a required\nargument told the caller it was deleted. It was not. I had inspected that arm\nearlier and approved it: an in-band error from a command that DID run is\ngenuinely dispatched, a transport-level rejection is not, and folding both\ntogether erased the distinction. It now has its own sentinel and a non-zero\nexit. The proof read needed the same fix, where a failed read could otherwise\nbe stringified into a resolved proof value.\n\nTwo ceremonies could never be satisfied. documents_list serialises its id as\n_id, and the rows matched on id, so documents_remove and resume_pipeline_run\nalways refused with text falsely saying the record may not exist -- the wire\nshape this repo already has a lesson about. The fixture is now built from the\nreal struct rather than hand-typed, which is what let it pass.\n\nWorse, two rows read their selector from the top level of the input while their\ncommands take a wrapped req, and tauri ignores unknown top-level keys. A caller\ncould satisfy the proof against one record while the command acted on another,\nso the property the whole ceremony exists for did not hold. Selectors are now\npaths, walked from the same place the command reads its target.\n\nFencing was keyed on command name and a field called description, so five other\nread paths returned the same attacker-authored posting text unfenced under\njobAd and jobDescription. It is now keyed on the field wherever it appears,\nwith a completeness test. Verified against the real store: every one of 144\napplications, 2416 found jobs and 115 generations comes back fenced.\n\nTwo rows changed class on evidence rather than instruction. match_resume and\nmatch_resume_text call embed_charged with no budget, documented as deliberately\nuncharged, so they are not dispatchable. And ai_embed became Irreversible with\na spend proof rather than Read as I had directed -- ai_generate carries the\nidentical gate and the same class, and making the one paid command freely\ndispatchable would have reintroduced what the gate was written to stop.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(cli): classify commands by where their input sends data, not just by writes\n\nSecurity review returned request-changes: two critical, three high. The\nunifying defect was the classification method, not the rows. Every command was\njudged on \"does this persist anything\", and three that answer no are\ndestination primitives whose caller-supplied input chooses where data goes.\n\nai_test_provider_key and ai_list_provider_models were Read while taking a\ncaller-supplied base_url, and the ssrf guard blocks only a non-http scheme and\nthe metadata address. One free call posted the stored provider key to any host\n-- and since the key resolver returns none rather than erroring when unset, the\negress fired even with no key configured, with the attacker's own response body\nfolded into the reply unfenced. That was an attacker-controlled injection\nchannel needing no job board at all.\n\nextract_resume was Read with an unvalidated path, and the read happens before\nthe extension check, so a unc path performed an smb connection and leaked ntlm.\nThis branch added a guard for that exact primitive two hundred lines away, on a\npath the app itself writes. It is also one of the commands no renderer calls --\ndead code the surface resurrected.\n\nBoth are now NotExposed, as is support_export_diagnostics, whose only proof was\nthe compile-time version constant -- vacuous by this file's own rule, the one\nthat demoted the token rotation row, while it truncates a caller-chosen file.\n\nThe provider settings rows moved to Irreversible. Reversible described the\nconfig row, which is undoable; it did not describe every later generation the\nUSER runs shipping resume text, contact details and the key to the new host.\n\nFencing covered description-shaped keys only, so a posting whose TITLE carried\nthe injection was never fenced, and array fields were skipped entirely. It now\ncovers title, company, location and requirements, fences string elements inside\narrays, and detects posting-shaped objects to catch the flattened extra map.\nVerified against the real store rather than fixtures.\n\nThe whole table was swept again on the input axis; no further destination\nprimitives were found, and the borderline rows now carry their reasoning.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(cli): close a persistent egress redirect and repair two broken ceremonies\n\nRound four. The previous sweep missed a row because its comment already\nreasoned convincingly about persistence, so it read as a completed audit and\nwas skipped. Re-deriving every row from its command SIGNATURE rather than its\ncomment found that row, and a fourth one nobody had named.\n\nai_set_embedding_config persists a caller-supplied base_url and every later\nembed sends the keychain key plus resume and job-ad text there, fired by\nordinary user activity long after the call. ai_seed_active_config was worse\nstill: its payload carries a provider map with per-provider base_urls AND the\nactive provider, so it persists and activates a redirect in a single call. Its\nrow-presence gate narrows that to a first-run window rather than closing it.\nBoth are now NotExposed.\n\nai_set_provider_settings joins them. Its proof was the active provider's name,\nbut the patch rewrites any provider's base_url and the active one does not\nchange -- so the confirmed value had no relationship to the target, the same\nshape that demoted the diagnostics export an hour earlier. A bound proof cannot\nbe expressed today, since the providers map cannot be reached by a static path.\nai_set_active_provider stays as it was: its proof is the field it overwrites.\n\nAdding title to the fence list had broken two ceremonies. The proof read\nbypassed fencing while every read a caller could actually make went through it,\nso following the hint compared a fenced value against a raw one and failed\nevery time -- and for a title containing a fence marker the raw value is not\neven recoverable. Both sides now read the same fenced value. The test that\nwould have caught it walks every irreversible row through the real fencing\npath; the first version of that test passed with the fix deleted, because it\nbuilt its own comparison instead of exercising production code.\n\nFencing title, company and location also meant a read-then-persist round trip\nwrote fence wrappers into the user's real store. The write path now strips\nthem, rather than dropping those fields from fencing and reopening the hole\nthey were added to close.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(scripts): check every leak on its own, not one survivor per key\n\nThe guard built a map from key to leak, so two sites sharing a key collapsed to\nwhichever was inserted last, and the check then re-derived \"the\" leak for that\nkey and compared ITS signature. When the declared site won that collapse -- the\nrealistic ordering when the undeclared call sits earlier in the file -- the\nundeclared leak's own signature was never looked at, and the guard reported OK\nwhile a genuine undeclared leak sat in the tree. The mirror bug hid staleness\nthe same way.\n\nThis is the second defect from this file's rework, and the same shape as the\nfirst: reasoning about a survivor as if it stood for all the records it\nreplaced. The earlier one let a message edited in place pass because the\nsignature was consulted only when a line had moved.\n\nThe map is now a full bucket and the single key-based predicate is split in\ntwo, one asking whether a given leak is satisfied by its own signature and one\nasking whether any leak in a key's bucket keeps an entry live. Nothing is\nre-derived from a key any more.\n\nConfirmed by putting the old code back: the new collision test reports zero\nproblems, which is the silent acceptance itself. Two properties that were only\ncovered indirectly -- a message changed in place, and two different declared\nsites in one file -- now have named tests rather than being inferred from the\nreal-repo integration case.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(ai): bill every embedding round trip, and resolve the provider once\n\nThe spend gate added earlier on this branch was right in shape and wrong in\narithmetic. It charged one daily unit per admitted call while the embedder\nsplits oversized input across many provider requests -- up to the chunk cap,\nand further again on a context-length retry -- so the daily ceiling was far\nlooser than it read. The input clamp bounds the input, never the number of\nrequests it becomes.\n\nThe counter is unchanged; only the moment of charging moved. A metering wrapper\ncharges immediately before each real attempt the embedder makes, retries\nincluded, so the count matches what was actually sent. Reserve-and-release was\nthe other option and was rejected: the limiter has no refund primitive, and\ninventing a partial-refund ledger to serve one caller is the wrong shape.\n\nSeparately, the charge and the dispatch each read the embedding config, so a\nconfig change landing between them billed one provider while the request went\nto another. The config is now read once and that single value feeds both.\n\nBulk re-index and match scoring are untouched: they call the embed helper\ndirectly, which still reads the store itself and passes no charge, so their\nbehaviour is byte for byte what it was.\n\nNote for whoever edits next: ai_provider/mod.rs is now 1399 lines against the\n1400 hard cap -- this change took it from 1390, so the next line added there\nfails the architecture test. It wants an extraction before it grows again.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix(cli): fence by struct fixture, and carry the confirm invariant in the type\n\nEight review findings across both reviewers.\n\nThe confirm path ended in an expect on an externally reachable ipc path, in a\ncrate that aborts on panic above crash reporting, guarded only by a separate\ncall agreeing with it. The gate now returns the decision and the proof source\nand the confirm value together, so reaching the confirmed case IS the evidence\nthe value was supplied and there is nothing left to unwrap.\n\nFencing missed six more prose fields, and this was the fourth round on that\nclass. Adding names buys one round, so the fix is the test: fixtures built from\nthe real structs assert every string field is fenced or explicitly safelisted,\nso a new prose field fails a test instead of leaking. PII and first-party\nfields are deliberately excluded and documented -- fencing exists to stop\nattacker text steering the agent, not to redact the user's own data, which is\na separate axis the adr settles.\n\nError details were never fenced, only success payloads, so a command whose\nerror embeds a remote server's message returned it verbatim. That channel is\nclosed. The two commands the finding named cannot actually produce it, since\nthey return a bare value rather than a result -- the example was wrong, the\nchannel was real.\n\nscrape_resolve_url claimed no persistence and upserts into the discovered\ncompany store on every resolve; it is Reversible now, with the comment\ncorrected. ai_pull_model claimed no data destroyed and additive, but nothing in\nthe app can delete a pulled model, so a multi-gigabyte write had no undo; it is\nIrreversible with a weak proof that at least forces a read first.\n\nFence stripping was applied at one writer; it now runs at the single dispatch\nchokepoint every invocation passes through, so a future writer inherits it\nrather than needing to remember. The circularity guard now resolves a proof's\nread command to its own row and rejects any irreversible target, covering the\ncase its doc always claimed.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-09-01T05:08:29+02:00",
          "tree_id": "a710d93c1c26fe7ea942a951b7ac11e2608604ab",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/38b990d0e9491845313946ad7de304e8a492d15c"
        },
        "date": 1788232843848,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2180413,
            "range": "± 70102",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2570679,
            "range": "± 41962",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 229238,
            "range": "± 5084",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "f65103265f63cbe18143710d4448bd0c41d95007",
          "message": "fix: make the agent CLI reachable after an install or update (#1088)\n\n* fix: symlink the agent cli onto path for homebrew installs\n\nThe `ajh-tauri agent <verb>` CLI (ADR-037/038) ships inside every bundle but\nwas reachable only by callers that read the app's ~/.ajh-agent/agent.json\npointer file. A human typing `ajh-tauri agent` got \"command not found\" on a\nmachine where the app was installed.\n\nThe cask now symlinks the bundle's executable onto PATH. The binary name was\nverified against the shipped artifact rather than inferred: the macOS bundle\nlays out as `AI Job Hunter.app/Contents/MacOS/ajh-tauri`, i.e. named after the\nCargo [[bin]], not productName.\n\nLinux needs no equivalent change: the deb already installs `usr/bin/ajh-tauri`.\nWindows (nsis, installMode currentUser) is still unreachable and is handled\nseparately.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: document the agent cli for users\n\nThe `ajh-tauri agent <verb>` surface (ADR-037/ADR-038) was invisible: it\nappeared nowhere in the README, nowhere under docs/ except CONTEXT.md, and\nnowhere on the landing site. The only landing mention is the `.claude/`\ndevelopment fleet, which a reader will conflate with it.\n\nAdds a README usage section and a knowledge-base entry, registered in the\nindex. The knowledge page states shape and points at `--help` and the owning\nsymbols rather than copying the verb table, the sentinel list or `max 50`,\nper AGENTS.md rule 17.\n\nBinary locations are stated per platform from shipped v0.145.0 artifacts, not\ninferred. Windows is documented as NOT on PATH, which is true today; the NSIS\nhook that changes it is a separate commit.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix: add the agent cli directory to the per-user path on windows\n\nCompanion to the Homebrew cask change: the `ajh-tauri agent <verb>` CLI\n(ADR-037/ADR-038) ships inside the Windows bundle but the per-user install\ndirectory is not on PATH, so a human could not invoke it. Linux already\ninstalls to /usr/bin, and macOS is covered by the cask's binary stanza.\n\nAdds an NSIS POSTINSTALL/POSTUNINSTALL hook pair. PATH is a value whose\ncorruption breaks the user's shell, so every branch is defensive:\n\n- Truncation guard. ReadRegStr SILENTLY truncates past NSIS_MAX_STRLEN\n  (1024 in the Unicode build Tauri bundles). A value read within one char\n  of the cap cannot be trusted to be complete, and writing it back would\n  permanently drop the remainder, so PATH is left untouched instead. A\n  second budget check refuses an append that would land near the cap.\n- Idempotent across updates. The installer re-runs on every update, so both\n  haystack and needle are ';'-padded before searching; a directory sharing\n  our prefix cannot match, and the entry is never appended twice.\n- Type preserving. REG_SZ vs REG_EXPAND_SZ is queried via Advapi32 and the\n  write-back matches; any other type is left untouched.\n- Uninstall splices out exactly our segment and the one delimiter it owns,\n  leaving no doubled, leading or trailing ';'.\n- Best effort throughout: every failure path DetailPrints and continues, so\n  a PATH problem can never abort an install or uninstall.\n\nVerified by compiling these macros with makensis and running them against a\nscratch registry key: absent/REG_SZ/REG_EXPAND_SZ values, sibling-prefix\ndirectories, re-runs, both truncation guards, removal from first/middle/last/\nonly position, and an unexpected REG_DWORD. A full release-bundle install was\nnot run.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: note the windows path hook lands in the next release\n\nThe previous commit added the NSIS hook, so \"Windows is not on PATH\" is only\ntrue for v0.145.0 and earlier. Stated as a release boundary rather than a\nbare \"yes\", since no shipped installer carries the hook yet.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix: make the windows path hook compile and survive a failed probe\n\nSecurity review of 7b9e1755 blocked it on two defects and one that made the\nfeature a no-op on a realistic machine. All three are fixed here.\n\nThe uninstall hook did not compile. `${StrLoc}` expands to `Call StrLoc`, and\nNSIS forbids calling a non-`un.` function from an uninstall section. PR CI\nruns `cargo check` and never bundles, so this would first have appeared in\nthe release job. StrFunc is gone entirely, replaced by `Shlwapi::StrStrW`\nthrough `System::Call` — a plugin call, so the `un.` rule cannot apply.\n\nA failed probe took the destructive branch. `$1` was uninitialised, and a\n`System::Call` that fails to resolve writes the literal string \"error\" into\nits output register, which `${If} $1 != 0` treats as \"no PATH value exists\" —\nreplacing the whole per-user PATH with just $INSTDIR, then reporting success.\nOnly a literal ERROR_FILE_NOT_FOUND (2) now creates a value; every other\noutcome leaves PATH untouched.\n\nThe 1024-char ceiling made it a no-op on ordinary machines. NSIS string\nvariables cap at NSIS_MAX_STRLEN, and the old budget refused any PATH past\n~1016 chars — a developer machine with a 988-char PATH plus the install dir\nwas already over it, so the installer would silently do nothing while the\ndocs promised otherwise. The value's content now never enters an NSIS\nvariable: RegQueryValueExW/RegSetValueExW against GlobalAlloc'd memory, with\nStrStrW for the whole-segment search.\n\nCorrects the record from 7b9e1755: ReadRegStr does NOT silently truncate past\nthe cap. Measured, it sets the error flag and returns an empty string, so the\nreal protection was always the ${Errors} check. `${NSIS_MAX_STRLEN}` is also\na live compiler builtin, contrary to the previous comment.\n\nAdds a compile-time guard: the hook hardcodes HKCU, so it now refuses to\nbuild under any installMode other than currentUser rather than silently\nwriting an elevating admin's hive.\n\nVerified independently of the author: both macros compile clean in a real\nSection Install and a real Section Uninstall under makensis 3.11, and the\ninstallMode guard aborts the build when flipped to perMachine.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix: gate every buffer input in the windows path hook\n\nSecurity re-review of a026a81a found no blocking defect but three crash\npaths, all one root cause: a length or pointer reached RtlMoveMemory or\nlstrcpyW without a precondition. Fixed as a single gate rather than three\npatches, since patching each symptom as it appears is what produced two\nearlier bugs in this file.\n\n- A registry value with cbData = 0 is legal Win32, and made `$3 - 2` yield\n  -2, which RtlMoveMemory reads as 0xFFFFFFFE. The install macro had no\n  clamp on the value size at all; uninstall survived the same input only\n  because StrStrW failed to match first. Both now refuse a value too small\n  to be a valid PATH before any arithmetic runs.\n- GlobalAlloc's return was dereferenced unchecked at four sites. Each now\n  bails, and the two second allocations free the first buffer on the way.\n- `System::Call` writes the literal string \"error\" into its output register\n  on a resolution failure, so uninstall's `${If} $5 == 0` fell through into\n  pointer arithmetic with $5 parsing as 0. Both sides now compare\n  numerically, so the sentinel lands in the safe branch by construction\n  rather than by luck of polarity.\n\nAlso restores a guard the previous rewrite dropped silently: a PATH already\nending in ';' no longer gains a doubled separator.\n\nPATH was left untouched in every injected failure before this change too —\nthese were liveness defects, not data-loss ones. Verified: the 15-case\nregression matrix still passes including the >1024-char case, cbData = 0 no\nlonger crashes, and both macros still compile in a real Section Install and\nSection Uninstall. Forcing GlobalAlloc to fail at the real ~20-100 byte\nallocation sizes was not achievable, so that branch is verified by pattern\nrather than end to end.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-09-01T20:09:57+02:00",
          "tree_id": "df46d46872a2513c96fb9bc53f3b3e72ac4e6050",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/f65103265f63cbe18143710d4448bd0c41d95007"
        },
        "date": 1788286885327,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2190180,
            "range": "± 15592",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2581973,
            "range": "± 16085",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 302241,
            "range": "± 7011",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "f336aee29da2093700de054c75e2dbed99bef6c5",
          "message": "fix: move the nsis installMode guard inside the hook macros (#1089)\n\nThe guard added in #1088 aborted every Windows bundle, so the Release\nworkflow failed on main. Tauri's generated installer.nsi does\n`!include \"<hooks file>\"` about ten lines BEFORE `!define INSTALLMODE`, so at\nfile-scope include time the symbol is undefined. NSIS leaves an undefined\n${SYMBOL} as literal text, which is never equal to \"currentUser\", so the\n!error fired unconditionally:\n\n  !error: windows/hooks.nsh hardcodes HKCU for the per-user PATH ...\n  Error in script installer.nsi -- aborting creation process\n  failed to bundle project: `Failed to bundle app with makensis`\n\nBoth the local verification and the security review missed it because their\nharnesses defined INSTALLMODE BEFORE including the hook file -- the reverse\nof the real ordering. The guard was the one part of that change never\nexercised against the real generated script, and it is the part that broke.\n\nThe check now lives inside both macros, which expand at their insertion\npoints inside the install and uninstall sections, far below the define,\nwhere the value is real. The reason is recorded at the top of the file so\nnobody moves it back.\n\nVerified with a harness that includes the hook BEFORE defining INSTALLMODE,\nmatching the real template. The control matters more than the fix: the\npre-fix file reproduces the exact CI error under that harness, the fixed\nfile compiles, and flipping the harness to perMachine still aborts, so the\nguard kept its purpose rather than being defanged.\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-09-01T20:52:01+02:00",
          "tree_id": "349fe779e4aa93190174ec786fe51c504380e45e",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/f336aee29da2093700de054c75e2dbed99bef6c5"
        },
        "date": 1788289446029,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2195028,
            "range": "± 10895",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2594370,
            "range": "± 22819",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 308844,
            "range": "± 7980",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "a27d89da76e2edf8921b3eac28448cda636ba173",
          "message": "feat: hybrid search over the live postings cache (#1090)\n\n* feat: hybrid search over the live postings cache\n\nThe README has claimed \"hybrid vector + keyword search\" in three places while\nno such thing existed: what shipped was hybrid *scoring*, a pairwise blend of\none resume against one posting. There was no index, no top-k, no search. This\nis the retrieval half.\n\nNew L1 `retrieval/` module, kept pure so it names neither EmbeddingVector nor\nCompleter -- fusion consumes ranks and the caller does its own embedding, with\nthe reranker declared as a port and implemented at L3.\n\n- Lexical arm: FTS5 over an in-memory connection built from the live cache.\n  Nothing is persisted, so this adds no retention or support-bundle surface.\n  FTS5 needed no dependency; it is already compiled into the shipped binary.\n  User input is sanitised into quoted phrases so FTS5 operator syntax cannot\n  error or misfire.\n- Dense arm: revives PostingsCache's embedding API, dead since it was written.\n  The f32-only surface is deliberate -- at N=2000/dim=768 a scan is 116.2ms of\n  which JSON parsing is 115.8ms, so the format was the bottleneck, not the\n  absence of an index.\n- Fusion: reciprocal rank fusion over ranks, not scores.\n- Rerank: top candidates through the existing structured-completion path, so\n  it inherits charge/ask/record and adds no egress host. Every candidate is\n  fenced under its own tag; a job ad is attacker-authored text entering a\n  ranking prompt.\n\nRanking is scoped to an eligible-id allowlist rather than the whole cache: the\nJobs page applies cluster-canonical, agency and work-type filters after the\nlist is built, so ranking everything would make a \"top 10\" render four rows,\nand the survivors would not be the ten best eligible postings.\n\nThe command snapshots a cache generation counter and refuses to return results\nif a replace-scrape cleared the corpus mid-search. Degradation is reported\nrather than hidden -- the reply carries which arms actually ran, so a lexical\nlist can never be presented as hybrid. semantic_scoring defaults to false, so\nthat is the common path, not an edge case.\n\nAdding a command forces the ADR-038 policy table; the new row is classified\nIrreversible because it can charge the per-provider daily ceiling. Note\npolicy.rs is now 1399 of R8's 1400-line cap -- pre-existing debt, but the next\ncommand addition must split it.\n\nRenderer work, the README correction and the ADR are not in this commit.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* feat: ranked search ui over the postings list\n\nWires the hybrid search command into the Jobs page. The interesting part is\nnot the search box, it is refusing to present a keyword list as a hybrid one.\n\nsemantic_scoring defaults to false, so on a default install the dense arm\nreports \"skipped\" and results are keyword-only. That is the common path, not\nan edge case, which is why the command reports which arms actually ran. The\nresults banner states what ranked the list, and when semantic ranking is off\nit offers a one-click path to enable it rather than a message the user cannot\nact on. Zero-hit, stale-corpus, cancelled and rerank-unavailable are all\ndistinct states -- in particular a query with no matches must never read like\n\"you have not scraped any jobs yet\", which is a different situation.\n\nOne input, two actions rather than two competing text boxes: the existing\nfilter stays an instant substring match on every keystroke, and Enter (or the\nSearch button) commits the same text as a ranked search. A committed search\nreplaces the substring view rather than layering on it -- pre-filtering the\neligible set by literal substring would defeat the point of semantic\nretrieval, which is surfacing matches that do not contain the query verbatim.\nWhether a search is active is derived from whether the committed query still\nequals the current text, so a stale ranked list can never sit under new text.\n\nRanking is scoped to the postings that will actually be shown: cluster-\ncanonical, agency and work-type filters decide the eligible ids that go to the\nbackend, and hits are re-intersected against the current eligible set on every\nrender. Ranking the whole cache and filtering afterwards would make a \"top 10\"\nrender four rows.\n\nA superseded search is cancelled before the next one fires, since a Tauri\ninvoke is not abortable from the renderer and would otherwise keep embedding\nand reranking for a query the user has already replaced.\n\nen and de both land in this commit at 2544 leaf keys with zero key-path\ndivergence; nothing in the repo enforces that, so a missing de key would be a\nsilent runtime regression rather than a build failure.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: correct the hybrid-search claim in the readme\n\nThe README claimed \"hybrid vector + keyword search\" in three places while no\nsearch existed at all -- what shipped was hybrid scoring, a pairwise blend of\none resume against one posting, with no index and no top-k. An interviewer\ngrepping for the index would have found two stale comments describing a\nremoved feature and a dead embedding API with zero callers.\n\nSearch now exists, so the claim is nearly true -- but not the way it was\nworded. semantic_scoring defaults to false, so a default install gets BM25\nonly and the dense arm reports \"skipped\". Calling that \"hybrid\" would swap\none overclaim for another. The three sites now say keyword by default, hybrid\nwhen semantic ranking is enabled, and the tech-stack row names what actually\nruns rather than a capability label.\n\nAlso separates two things the old wording conflated: ranked search over\nscraped postings is new, and per-posting scoring against the resume is the\npre-existing feature.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* docs: record adr-039 and resync the pages hybrid search made stale\n\nAdds ADR-039 for hybrid postings search, recording each decision with the\nalternative it rejected: the in-memory cache as corpus (persisting postings was\nrejected for the migration, retention and support-bundle surface it would add),\nFTS5 needing no dependency, f32 vectors over the existing JSON-TEXT f64 (a scan\nwas 116.2ms of which 115.8ms was JSON parsing, so the format was the bottleneck\nand an ANN index was not needed), LLM listwise reranking over a cross-encoder\n(which would need an ML runtime in a three-OS build), ranking scoped to an\neligible-id allowlist, and reporting degradation rather than hiding it.\n\nThree pages went stale on this feature and a review caught all three:\n\n- anti-abuse-limits.md enumerates the limiter bucket inventory, which gained\n  hybrid_search_rerank. A first pass claimed no bucket was added; the constant\n  says otherwise.\n- matching-algorithm.md enumerated exactly two scoring modes, both of which\n  score a resume against a posting. Postings search ranks postings against a\n  user QUERY and shares no kernel with them, so it is listed as a third,\n  explicitly separate surface rather than left to be mistaken for one of them.\n- architecture-rules.md's L1 module list omitted retrieval; the const in\n  tests/architecture.rs is authoritative and the prose now matches it.\n\nThe ADR names the limiter bucket outright rather than relying on the inventory\npage, since a list that goes stale silently is the same failure class as the\nREADME overclaim this feature exists to fix.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* test: pin the hybrid-search wire contract and adversarial rerank output\n\nSix tests, each mutation-checked rather than assumed: the mutation is named,\nthe test that caught it is named, and the production file is byte-identical\nafterwards.\n\nThe most valuable three pin the wire strings the renderer's arm-honesty UI\ndepends on -- \"ran\"/\"skipped\"/\"unavailable\", \"ok\"/\"cancelled\"/\"staleCorpus\",\nand camelCase field casing. Nothing locked these before, so dropping a serde\nrename would have broken the renderer silently with no compiler error on\neither side of the IPC boundary. Verified by dropping the rename and watching\nthem fail.\n\nThe others cover an adversarial reranker returning an invented id, a duplicate\nand a dropped candidate in one response (not three isolated cases), the\nboundary between an absent allowlist and one that matches nothing, and that\nthe single choke point every early return funnels through can never emit hits.\n\nKNOWN GAP, recorded rather than papered over: the live orchestration is not\nintegration-tested. run_search/run_dense_arm/maybe_rerank take &AppHandle and\nread cache/store/limiter/preferences off it inline, and this crate has no mock\nAppHandle -- enabling Tauri's MockRuntime was rejected twice before in this\ncodebase for the same tradeoff (extension_bridge/test.rs:1225-1228 and the\ncommands/system/test.rs notes). So dense skipped-vs-unavailable-vs-ran as\nactually produced, rerank unavailable on a real provider error, staleCorpus\nand cancelled as actually detected, and the over-long allowlist rejection all\nremain uncovered. The fix is an AppHandle-free env trait mirroring RerankEnv,\nwhich is a production restructuring and deliberately out of scope here.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\n\n* fix: surface a failed semantic-scoring sync and keep the banner out of the scroller\n\nReview of the ranked-search UI found one blocking defect and three smaller\nones; all four are addressed here.\n\nThe one-click \"enable semantic ranking\" path wrote the preference through a\nfire-and-forget mutation with no error handler, and the QueryClient has no\nglobal mutation onError. A failed backend write therefore flipped the local\npreference to on, re-ran the search and showed results, leaving the user with\nno sign the persisted mirror never updated -- worse than silent, because the\nUI looked fully successful. The hook's own doc claimed to mirror the settings\npanel's call site, which does handle the error. It now does the same, reusing\nthe existing translation key, and a test fails if the handler is removed.\n\nThe results banner rendered inside the same overflow container as the\nvirtualised list without a scrollMargin, so scrolling a ranked list to the\nbottom left a gap the height of the banner. Rather than measure the banner\n(its height varies with which arm notices render), it now sits outside the\nscroller as a sibling; a test asserts it is never a descendant of the\nscrolling element.\n\nRetyping the last committed query resurrected the cached ranking without\nre-running against postings that may have arrived since. Deciding whether a\nbackground scrape should silently evict a ranked view the user is reading is\na product judgment, not a mechanical fix, so the text-only predicate is\nextracted, documented as intentional, and pinned by a test whose name states\nthe no-corpus-signal contract.\n\nThe zero-hit state reported the corpus size at search time, so a filter\ntoggle afterwards could claim \"no matches among 45\" when 30 were eligible;\nit now reports the current eligible count.\n\nSearch ids are minted with a `search-` prefix to match the Rust side's new\nvalidation, so a caller can never collide with a `job-{uuid}` in the shared\ncancel registry.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: state what hybrid search does not do, and fix five false claims in adr-039\n\nReview found five claims in ADR-039 false against the code it documents, and\na verification pass found the feature's headline claim structurally false in\nthe common case. Both are corrected here.\n\nThe dense arm's candidate pool is the lexical top-40 whenever keyword search\nfinds anything, so a posting BM25 missed is never embedded and cannot surface\nthrough dense or fused ranking. The dense arm and rerank RE-ORDER keyword\nhits; they RETRIEVE only when keyword search finds nothing, and then over the\nfirst 40 postings in cache order. The ADR said \"reranking is the mitigation\"\nfor FTS5's non-semantic nature -- it is not, because the rerank never sees a\nnon-lexical candidate. A \"What this does and does not do\" section now states\nthe policy and points at DENSE_CANDIDATE_MAX as the owning source, and the\nREADME's two claim sites are narrowed so they cannot be read as \"semantic\nsearch finds things keyword search misses\".\n\nThe five corrections: the rerank is described as gated on semantic_scoring\nalongside the dense arm (true as of the accompanying fix, enforced by a test);\nthe reply field is `arms`, not `degraded`; eligible ids are filtered at L3 and\nnever reach retrieval/; PostingsCache has no text hash, it is an id-keyed map\nwith explicit invalidation; and the f32 decision no longer cites a 116 ms\nJSON-parse measurement, which was of the SQLite posting_vectors store, not the\nin-memory cache. The honest reason is scale: the cache is bounded, so no ANN.\n\nA \"What is measured and what is not\" section records that ranking invariants\nare asserted in CI, retrieval quality is not measured because there is no\nlabelled relevance data, and the fusion constants are reasoned with cited\nsources rather than tuned. Extending this repo's deterministic-first candour\nto retrieval is the point.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: gate the rerank, make it cancellable, and never score across embedding spaces\n\nThree independent reviews of the hybrid-search backend converged on the same\ndefects. All are fixed here, each gate mutation-checked: the guard deleted,\nthe test watched to fail, the guard restored.\n\nThe rerank ran unconditionally. The dense arm read semantic_scoring; the\nrerank did not, so a default install with a cloud provider sent the query and\nup to twenty scraped postings to a paid provider on every Enter, while the\nmodule doc, ADR-039 and the README all said it was off by default. Three\nprose restatements of a missing gate are three copies of the same lie, not\nthree checks. should_rerank() is now the single gate, mirroring\nautopilot::rerank::should_semantic_rerank, and a test fails if it is removed.\n\nThe rerank could not be cancelled and inherited a 300s generation deadline.\nIt was a bare await, never raced against the cancellation token, so\njobs.cancel did nothing once it started -- contradicting the hook's own doc --\nand it held one of three concurrency slots for the duration. It now runs\nunder tokio::select! against the token and a new HYBRID_SEARCH_RERANK\ninteractive-tier timeout, justified in timeouts.rs against the existing\ntiers; each embed is raced the same way. Cancelled, timed out and errored all\ncollapse to Unavailable plus the fused order.\n\nThe #1087 billing race was back. The embedding config was snapshotted for\nthe budget but AppEmbedder re-read it at dispatch, so ai_set_embedding_config\nlanding between them charged one provider while billing another -- the exact\nshape commands/ai/mod.rs documents fixing. Both embeds now go through\nembed_with_config from one snapshot. Separately, the freshly-embedded branch\nnever checked the returned vector's space and the query vector was never\ncompared to a candidate's; two models at dim 768 produced a plausible cosine\nthat meant nothing. dense_pair() now refuses any pair whose spaces differ.\n\nSmaller: queryId is validated to 64 chars and a `search-` prefix so an agent-\nCLI caller cannot collide with a live job-{uuid} in the shared cancel\nregistry; the embedding write-back re-checks the cache generation under the\nsame lock so an in-flight search cannot resurrect vectors after\nprivacy_clear_data; ai_set_embedding_config now actually clears cached\nembeddings on a space change as its comment always claimed; PostingsCache::add\ninvalidates only when the text changed, so \"show more\" no longer wipes the\ncache for byte-identical postings; a Drop guard releases the cancel-registry\nslot on panic; the operator-safety test now asserts `-golang` finds the doc\ncontaining golang instead of discarding its results; and two constant docs\nstop claiming recall properties they do not measure.\n\nThe dense candidate pool is unchanged, but its doc now says plainly that the\ndense arm re-ranks lexical hits and only retrieves when lexical found nothing.\ndense.rs drops a 116 ms JSON-parse justification that was measured on a\ndifferent store.\n\nTwo script inventories re-anchored: the log-error-leak allowlist keys that the\npostings edit shifted, and the JobsPage event-subscription hash that the\nrenderer edit shifted. Neither note's content changed.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: point adr-039 at the file that actually owns the candidate cap\n\nTwo pointers in ADR-039 named `retrieval/mod.rs` as the home of\nDENSE_CANDIDATE_MAX. The constant and its doc comment live in\ncommands/hybrid_search.rs; retrieval/mod.rs does not mention it. A rule-17\npointer that points at the wrong file is worse than no pointer, because the\nreader stops looking.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: split the rerank timeout by provider class and pin the search-id prefix on the wire\n\nRe-review of the rerank fixes found one number that could make the feature\npermanently degraded for the offline-first audience, plus six consistency\ngaps. All are addressed; nothing here changes a gate or a charge path.\n\nThe 45s rerank timeout was a single flat tier. The rerank prompt is\nRERANK_TOP_K x RERANK_ITEM_CHAR_BUDGET, roughly 3,000 input tokens, and CPU-\nonly Ollama commonly evaluates prompts at 20-60 tok/s -- 50 to 150 seconds\nbefore the first output token. On that fully-supported configuration the\nrerank would have reported unavailable on every search, permanently, with no\nway for the user to tell a stalled provider from a slow one. It now follows\nthe file's existing local/cloud split for completions: 45s for cloud\nproviders, 180s for Ollama. The local bound is DERIVED from that token\narithmetic, not measured, and its doc says so and names the measurement that\nwould replace it. Relational tests pin both tiers against their neighbours,\nthe way every other constant in that file is pinned; dropping the local tier\nbelow its own derived floor fails a test that names the broken derivation.\n\nThe wire contract now states the search-id prefix the backend hard-rejects.\njobs/cancel.rs names that prefix as the safety net against colliding with a\nlive job id, yet the Zod schema said only min(1).max(64), so an agent-CLI\ncaller following the published contract would have been rejected with no\ndocumented reason. An existing test fixture was minting an unprefixed id and\nwould have failed its own new constraint; it is corrected and a rejection\ntest added. Generated docs are unaffected.\n\nSmaller: the id length check now counts chars like its sibling three lines\nup instead of bytes; a security-reasoning comment claimed resume_pipeline_run\nregisters run-{uuid} when it registers job-{uuid}, fixed in both places it\nwas stated; the embed_with_config doc no longer claims ai_embed is its only\ncaller; and clear_embeddings now bumps the cache generation so an in-flight\ndense arm holding an older config snapshot cannot re-seed stale-space vectors\nafter a settings-driven clear -- belt-and-braces, since every read was\nalready space-guarded, and stated as such.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: revert the clear_embeddings generation bump and anchor the timeout floor to its inputs\n\nThe previous commit made clear_embeddings bump the cache generation as a\nbelt-and-braces guard on the dense arm's write-back. That counter has a\nsecond reader: the search's stale-corpus return check. ai_reembed_all calls\nclear_embeddings unconditionally on every \"Re-index now\", so a re-index\nlanding mid-search discarded a fully-computed, still-correct ranking,\nreturned no hits, and told the user \"your scraped jobs changed\" -- which was\nfalse, since clear_embeddings leaves the job list intact. One field serving\ntwo consumers with opposite safe directions, for the third time on this\nbranch. The bump is reverted; the write guard covers clear_all only, by\ndesign, and a stale-space vector re-seeded after a settings-driven clear is\ndead weight, never scored, because every read is space-guarded. The existing\ngeneration test now asserts clear_embeddings does NOT bump, as a direct guard\nagainst reintroducing this.\n\nThe local rerank timeout's test compared against a re-typed 150 seconds. Its\ndocumented derivation reads RERANK_TOP_K and RERANK_ITEM_CHAR_BUDGET; doubling\nthe former makes the real floor 300s and the doc false, and the old test\nstayed green. The floor is now computed from those constants in the test, so\nit moves with its inputs -- verified by doubling RERANK_TOP_K and watching the\ntest fail with the recomputed floor, then reverting.\n\nTwo doc clauses: the local tier's margin over its derived floor is stated as\nthin, and named as having to absorb output tokens and a cold model load; and\nthe provider-class split is stated as Ollama-only -- a self-hosted\nOpenAiCompatible endpoint and the CLI agents sit on the cloud tier, which\nfails safe and is identical to the flat bound it replaced, with the follow-up\nnamed.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: fix a dangling link in the search contract and stop calling f32 ranking lossless\n\nTwo documentation findings from external review of #1090.\n\nThe contract's TSDoc linked to a `PostingsHybridSearchResult` type that does\nnot exist; the result type is `HybridSearchResult`. The link resolved to\nnothing in editors and in the generated API page, which is regenerated here.\n\nADR-039 described f32 vectors as \"lossless for cosine-distance ranking\". They\nare not: rounding perturbs cosine values in the last few bits and can reorder\nnear-tied postings. The honest claim is that f32 is sufficiently precise for\nthis bounded ranking, because no tie-tolerance guarantee is made or needed --\nthe rerank is the ordering authority when it runs, and the fused list is a\ncandidate pool rather than a scored ranking.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: retry after the semantic-scoring write lands, and keep degradation copy true per arm\n\nFour renderer findings from external review of #1090, each verified before\nfixing.\n\nThe one-click \"enable semantic ranking\" path flipped the local store, fired\nthe async preference write, and retried the search immediately. The retried\nsearch reads the preference from the Rust store, which the write had not yet\nreached, so it ran with the dense arm still skipped and the user who had just\nclicked the button got keyword results again. The retry now runs in the\nwrite's onSuccess; on failure the toast explains and nothing retries. A test\nwith a deferred promise proves no search is issued before the write resolves.\n\nThe mock client's hybrid-search stub resolved undefined, and the hook's\nsuccess handler reads outcome and hits from it, so a mock-backed search --\nthe client Playwright and dev mode use -- threw after a successful request.\nIt now returns a typed empty result with every arm skipped.\n\nThe degradation notices made claims about the combined order that were false\nin reachable combinations: with the dense arm unavailable and the rerank\nhaving run, \"showing keyword results\" and \"showing the fused keyword/semantic\norder\" rendered together. Each notice now describes only its own arm; the\n\"Ranked by\" line already states what actually ran. Copy-only change in both\nlocales, key counts unchanged at 2544 each.\n\nA test that called a pure predicate twice with identical arguments proved\nonly that it was deterministic, not that it takes no corpus input. It now\nasserts the arity directly, with a note on why a corpus-aware version would\nbelong in the page, not the predicate.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: report a failed lexical query as unavailable, and bound the dense arm's wall clock\n\nTwo backend findings from external review of #1090, both verified before\nfixing -- and the first one's stated trigger turned out to be wrong.\n\nThe review claimed a punctuation-only query such as \"-\" makes FTS5 reject the\nMATCH and the lexical arm reports a successful zero-hit search. Twenty-seven\nadversarial inputs, including that one, all succeeded with zero rows: quoting\nevery token genuinely defeats the operator grammar. What DOES reproduce is an\nembedded NUL byte, and it fails at a third swallow point the review did not\nname -- prepare and query_map both succeed, the error surfaces only when the\nrow iterator is stepped, and filter_map(Result::ok) was quietly discarding\nit. So the concern was real and the specifics were not. LexicalIndex::search\nnow returns a Result and propagates all three error paths; a per-row error\naborts the result, since it reflects the query expression failing rather than\none row's data. The degrade decision moves to L3, where a new run_lexical_arm\nmaps Err to ArmStatus::Unavailable, keeping the swallow-to-empty policy out of\nthe L1 module. Tests at both levels use the NUL-byte trigger.\n\nThe dense arm had no wall-clock bound: one query embed plus up to forty\ncandidate embeds, sequentially, each bounded only by the provider's 30s\nper-attempt timeout, with user cancel as the only exit. The loop now also\nbreaks on elapsed time -- bounding by elapsed rather than wrapping in a\ntimeout, because a timeout would drop the future and discard every pair\nalready embedded, whereas the loop can exit and rank what it collected, the\nsame partial-results posture the existing cancellation check already had.\nDENSE_ARM_TIMEOUT is derived (three worst-case embed round-trips plus a\nconnection margin), says so, and names the measurement that would replace it.\nIt is deliberately not split by local and cloud: the rerank split exists\nbecause generation is an order of magnitude slower on CPU, and embedding is a\nsingle forward pass with no such asymmetry -- the per-call ceiling it is built\nfrom is already identical for both. A relational test pins it against three\nembed round-trips and against COMPLETION; dropping it to 60s fails the test.\n\nNot covered: the elapsed-time break inside run_dense_arm itself has no test,\nfor the same reason the adjacent cancellation check never has -- it needs a\nmanaged-state harness this crate does not have.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n---------\n\nCo-authored-by: Claude Opus 5 <noreply@anthropic.com>",
          "timestamp": "2026-09-02T01:25:45+02:00",
          "tree_id": "ccb5de190e24a3583176245668f0524f3500e8e8",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/a27d89da76e2edf8921b3eac28448cda636ba173"
        },
        "date": 1788305850483,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2263958,
            "range": "± 39507",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2691112,
            "range": "± 40712",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 305715,
            "range": "± 7606",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "c01e7eaf7a81cbc801498ee40ce1480f6e56924d",
          "message": "docs: make the ai rigour visible where a reviewer actually reads (#1091)\n\n* docs: make the ai rigour visible where a reviewer actually reads\n\nThe repo already contained a deterministic validator taxonomy with a measured\nfalse-positive budget, evidence grounding that drops any quote not literally\nin the source, a fabrication gate that refuses to save rather than save with\na flag, untrusted-text fencing, and an offline eval harness in CI. None of it\nwas visible on the surfaces a reviewer reads first: the README had zero\noccurrences of recall, precision, false-positive or eval harness, and\ndocs/knowledge covered the provider layer but not the generation pipeline.\n\nAdds a README section that lets a reader answer, without installing, how the\noutput is known to be true and how the checks are known to work -- the\nharness stated as properties (every planted defect recalled at its claimed\nseverity, zero Criticals on truthful documents, Warning false positives held\nto a named budget, CI-enforced) rather than as numbers, and a candour\nparagraph on what is not measured. The fencing bullet is written around the\nprinciple -- anything the user did not type is fenced, including the model's\nown earlier output -- with scraped job ads as the example, since that is\nwhere an attacker has a foothold.\n\nAdds docs/knowledge/generation-domain.md, the missing domain page, and\nregisters it. Every number that would drift is a pointer to its owning\nsymbol; a first draft pasted a stage count, a repair-round cap, a call count\nand a candidate cap, and each was replaced with the constant that owns it.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: say on the landing what the app does, not just how the repo is built\n\nThe landing's only AI-shaped page documents the .claude development agent\nfleet -- how this repo is built -- and a reader takes it for the product's\nAI. A note at the top of /agent-system now says which it is and points at\n/how-it-works for the product's own pipeline. It reuses the page's existing\naccent tokens and the same callout shape /how-it-works already has, rather\nthan a new style.\n\nThe /how-it-works interview cheat-sheet gains entries a reviewer would ask,\neach answered from the source with file paths and no numbers that drift: how\nthe model is stopped from inventing experience, how the checks are known to\nwork (the harness, stated as properties), what happens when it is not sure,\nhow prompt injection from scraped ads is handled, what is deliberately not\nmeasured, and whether the job search is actually semantic -- answered exactly\nas ADR-039 does. The landing is not localized, so these are plain strings.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* test: measure which synonym pairs the lexical arm misses, and that the pool does not bridge them\n\nA deterministic table over a frozen copy of the keyword kernel's synonym\npairs: for each, one posting containing only the alias, queried with the\ncanonical, against a real FTS5 index. Under rusqlite's bundled build, 22 of\n24 pairs are lexical misses; the two hits are react.js and vue.js, because\nunicode61 splits on the dot and a bare-word query already matches. The count\nand the exact hit set are asserted as literals, not derived from each other.\n\nA second row per pair adds a distractor containing the canonical term and\nasserts the alias posting is absent from the dense candidate pool, so the\nmiss is not bridged once keyword search finds anything -- the policy ADR-039\nstates.\n\nThis measures two things and its header says so: the lexical arm's\nno-synonym behaviour, and the pool policy. It does not measure retrieval\nquality and it does not measure the embedding model; a proposal to do that\nwas rejected as unsound and the header records why. The pairs are a frozen\ncopy with a drift guard against the live table rather than an import of it,\nbecause that table is scoring data pinned to the match-formula version and\nan eval-motivated edit must never silently change ATS scoring.\n\nOne honest limit: dense_candidate_pool is private to its module, so the\nsecond row mirrors its lexical-hits branch rather than calling it, with the\nmirror cited to the source line and the in-crate unit test that pins the\nbranch named. Mutation-checked by making one alias resolve in the sanitiser:\nthe count dropped to 21 and the assertion failed.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: keep the adr filename on one line in the cheat-sheet\n\nA template literal broke the ADR-039 filename across a line inside a\n`.path` span. `white-space: nowrap` stops wrapping but still collapses the\nembedded newline to a space, so the page rendered `adr-039-hybrid-\npostings-…` with a spurious space inside the identifier -- not the\ncopy-pasteable path it is meant to be. Review caught it from the CSS spec\nrather than a render; the fix is the filename on one line.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: point the grounding link at the right file, and scope the critical claim honestly\n\nTwo external-review findings on the README and one on the landing.\n\nThe README linked `evidence::ground` to documents/evidence/mod.rs, which is\nthe scoring kernel's evidence ranking and defines no such function. The\ngrounding step lives at pipeline/resume/stages/evidence.rs; the link now\npoints there. Also fixes a typo the typos check did not catch.\n\nThe cheat-sheet said every Critical the content validator raises is a\ncomparison against the résumé text. That is the shape of the factual\nCriticals, but not of language_mismatch, header_in_body or\ntemplate_placeholder, which are structural rules. The property the code\nactually guarantees -- and the one worth claiming -- is that every Critical\nis deterministic: a comparison or a structural rule the code runs with no\nmodel in the loop. It now says that.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: strip the last drift-prone literals from the generation-domain page\n\nThird pass on a page that is meant to be a thin pointer. External review\nfound what two internal passes had reported clean: the company-research\nfence cap as a number, module-doc references by line number, an eight-entry\nnumbered stage list with per-stage prose, a per-module validator enumeration,\nand the eval harness's assertions restated in full. Each is now the symbol\nthat owns it -- buildCompanyResearchBlock, the module doc by name,\nQUALITY_STAGES with the stages as a plain ordered list, CONTENT_ISSUE_CODES,\nand tests/eval.rs for its own assertions.\n\nAlso repoints the grounding section at pipeline/resume/stages/evidence.rs,\nwhere ground() actually lives -- documents/evidence/ is the scoring kernel's\nevidence ranking, a different thing -- and shortens the Eval heading so the\nin-page fragment link resolves.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* test: assert the pool policy against the real function, not a mirror\n\nExternal review graded the mirrored candidate-pool logic in the lexical-gap\ntable as Major, and the in-repo test audit reached the same conclusion with\nthe fix: the in-crate test module already has access to the private\ndense_candidate_pool, so the pool half of the measurement belongs there.\n\nThe mixed-corpus assertion -- an alias-only posting is absent from the pool\nonce a distractor contains the canonical term -- now lives in\ncommands/hybrid_search/test.rs and calls the real function with real rows.\nMutation-checked by making the pool return the whole eligible set: the new\ntest failed on the first pair, and so did the existing ordering test, which\nis how a load-bearing mutation should look. The mirror is deleted.\n\nThe integration test keeps only the lexical half and its header now says\nso: it measures which pairs BM25 misses on this corpus, still 22 of 24 with\nthe same two dot-split hits, and points at the in-crate test for the pool.\nPer-row results are collected before any assertion, so the diagnostic table\nprints even when a row errors -- the audit's one advisory.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n---------\n\nCo-authored-by: Claude Fable 5.1 <noreply@anthropic.com>",
          "timestamp": "2026-09-02T02:27:24+02:00",
          "tree_id": "1aee121471b198b0a096268dcfbf17f50bfdc360",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/c01e7eaf7a81cbc801498ee40ce1480f6e56924d"
        },
        "date": 1788309544552,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2207096,
            "range": "± 19950",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2659041,
            "range": "± 27429",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 256917,
            "range": "± 6651",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "44fd17dde4ad2dbe8e5719db52b84fda02f51475",
          "message": "feat: mcp stdio server mode for the agent cli (#1092)\n\n* chore: route the agent-cli module directory to its own reviewer\n\nThe review routes sent only extension_bridge/agent_cli.rs to\nagent-cli-reviewer and the whole extension_bridge tree to\nextension-reviewer, so a new child module under agent_cli/ would be reviewed\nby the wrong critic and the agent-system drift check could not see it. The\ndirectory glob now sits beside the file glob in both the route list and the\nagent-cli scope. This also closes the gap that left policy.rs unrouted.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: stop pointing callers at a command table adr-038 does not contain\n\nTwo refusal strings sent a caller to ADR-038 \"for the full command table\".\nThat ADR contains no command table -- zero hits for any command name -- and\nthe MCP surface is about to put these strings in front of a model that will\nfollow them. They now point at `agent schema`, the MCP `commands` tool, and\npolicy.rs. String-only edits; no test compares the detail text.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: record adr-040 and document the mcp server mode for users and reviewers\n\nADR-040 records the MCP stdio server as a mode of the existing agent CLI,\nwith each decision's rejected alternative: the legacy 2025-11-25 wire on\npurpose, because Claude Code 2.1.258 and Codex 0.144.6 both open stdio\nservers with the initialize handshake that the current 2026-07-28 revision\nremoved, and legacy clients have no fall-forward; hand-rolled JSON-RPC over\nserde_json rather than an SDK crate; a mode of the binary rather than a\nTauri command, so the policy table and its exactness test are untouched;\ntools split along the Effect boundary with MCP annotations rather than one\ngeneric call tool; the confirm ceremony passed through verbatim, never\nresolved server-side; results rather than protocol errors for app refusals;\nand one writeln! site as code shape. It extends ADR-038's egress amendment\nto this surface -- tool results leave the machine via the client -- and\nrecords why the pairing-token row is reclassified NotExposed.\n\nThe knowledge base gains an MCP-mode section on the agent-cli page and the\nindex row the drift check requires. The README gains client config for\nClaude Code and Codex; a first draft had the Codex config at the wrong path\nin the wrong format with a nonsense env line, corrected to\n~/.codex/config.toml with an [mcp_servers.<name>] table. Three pointers\nthat sent callers to ADR-038 \"for the full command table\" -- which it does\nnot contain -- now point at the commands tool, agent schema, and policy.rs.\n\nThe agent-cli-standards skill gains an MCP-mode block amending its\none-document-per-invocation rule for this mode only.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* feat: add an mcp stdio server mode to the agent cli\n\n`ajh-tauri agent mcp [--allow-irreversible]` is a new mode of the agent\nCLI (agent_cli/mcp.rs), never a second binary or Tauri command. It speaks\nthe legacy 2025-11-25 JSON-RPC stdio lifecycle Claude Code and Codex\nactually open, answering server/discover with plain -32601 so a\nMCP_PROTOCOL_NEGOTIATION=auto client falls back to initialize (verified\nlive against both the raw wire and the installed Claude Code binary).\n\nEight tools along the Effect boundary: five curated (best-matches, job,\nprofile, automations) plus a local commands tool that enumerates the\npolicy table by effect with no bridge call, and three generic dispatch\ntiers (call-read/call-reversible/call-irreversible) instead of one\nmonolithic call tool, so a client can auto-approve reads while still\nprompting on deletes. call-irreversible is omitted from tools/list unless\nlaunched with --allow-irreversible and carries\n_meta.anthropic/requiresUserInteraction, since Codex never reads that\nhint and cannot be the only gate. call-* tools refuse locally (never\nforward) a namespace/command the bundled policy table doesn't know, and\nrefuse a known row dispatched through the wrong tool, naming the right\none. tools/call arguments become this CLI's own argv and run through the\nexisting parse_verb, so this layer owns no second validator. confirm\nreaches call-irreversible verbatim and is absent from the other two\ntools' schemas by construction.\n\nTwo security fixes bundled in because the MCP surface is what exposes\nthem: extension_bridge_status is reclassified NotExposed (it returned the\nplaintext pairing token verbatim, which a generic-tier or MCP caller must\nnever receive), and best-matches now fences title/company/location the\nsame way job.description already does, since MCP is the first surface\nthat reads those fields with no surrounding prompt.\n\nstdout carries only compact JSON-RPC lines through one writeln! site; a\nsource guard fails if a later edit adds println!/print!/to_string_pretty.\nMutation-checked live: removing either local refusal (unknown-command,\nwrong-tool) turns a dedicated test red immediately.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* chore: split the duplicated glob key into two review-route objects\n\na95f79d5 added the agent_cli directory glob by inserting a second \"glob\"\nline into the same JSON object as the agent_cli.rs glob. JSON parsers apply\nlast-key-wins, so the file glob was silently dropped and the most-touched\nfile in the MCP change had no reviewer route -- while the drift check passed,\nbecause the surviving glob was valid. Caught by the author building the\nmodule, not by any check. The two globs are now two objects.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: describe the mcp write tiers by effect class and correct adr-040\n\nThe security review found the privilege boundary's own description wrong: the README\nand the knowledge page called --allow-irreversible the \"mutations\" switch while the\nreversible write tier shipped ungated, and both README examples named commands the flag\ncannot enable (applications_track is Reversible; boards_login_with_browser is NotExposed).\nThe tiers are now described by Effect class, with the new read-only default and the\n--allow-reversible flag the rework adds.\n\nADR-040 carried three claims the code in the same branch contradicts: a single held\nWebSocket connection with exit on pairing_rejected (it is a fresh handshake per call and\nthe refusal is a result), best-matches fields \"unfenced\" (fenced in the same commit), and\na \"Verified behaviors\" section asserting Codex is written in Go (it embeds rmcp, Rust) and\npresenting unobserved client versions as observed. Section 6 now records why both write\ntiers get the same gate; the verified section separates scripted runs from source reading.\n\nThe knowledge page also loses the rule-17 literals (tool count, throttle numbers) for\npointers at tools() and BridgeState, and scopes its \"safe for transcripts\" claim to the\nerror envelope, since a successful reply's data is the command's real output.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: gate the mcp reversible tier and close three refusal leaks\n\nA scripted stdio run of the real binary (unit tests never drove tools/list\nor tools/call end to end) surfaced what the review rounds predicted from\nsource alone: launched with no flags, tools/list still listed call-reversible\nunder only destructiveHint:false, a hint Codex ignores; commands with\neffect=bogus answered {\"commands\":[]} isError:false exit 0 instead of a\nusage error; and a NotExposed row (extension_bridge_status, the plaintext\npairing token) was let through call-read locally and relied on the running\napp's own gate to refuse it, which a cross-version peer might not do.\n\n- Gate call-reversible behind --allow-reversible, mirroring the existing\n  --allow-irreversible gate; --allow-irreversible now implies it (three\n  strict-superset launch tiers, each pinned by a hand-written name list).\n- commands validates its effect filter against the same array it builds\n  the schema enum from, and marks a gated row \"unavailable\" instead of\n  naming a tool it cannot dispatch to.\n- One tool_for(Effect) replaces two independently-hand-written mappings\n  (commands_value and local_call_refusal), and local_call_refusal now\n  refuses a NotExposed row on every call-* tool by itself.\n- Added a 256 KiB MCP_RESULT_MAX_BYTES cap (refuses as result_too_large,\n  never truncates JSON) and dropped structuredContent everywhere (a live\n  `commands` frame measured 50,695 bytes with it, 26,821 without).\n- `agent mcp --help` now prints usage and exits 0 instead of exiting 2;\n  INSTRUCTIONS names app_not_located separately from app_not_running and\n  gains one sentence per enabled flag, built once at startup.\n\nEvery fix carries a test that fails when reverted; each was mutation-checked\nby hand (see the handoff) and restored before the next.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* refactor: share the not_exposed sentinel with the mcp local refusal\n\nThe rework's local NotExposed refusal in mcp.rs carried a second hand-typed copy of\nagent_call's private sentinel because that file was out of the author's scope. The\nclosed sentinel vocabulary is the contract every caller matches on, so a duplicate literal\nis a drift point: widen the constant to pub(super) exactly as ERR_UNKNOWN_COMMAND and\nERR_CONFIRMATION_REQUIRED already are, and import it.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: repair a duplicated clause in adr-040 and record the local-refusal trade\n\nThe earlier docs pass replaced \"an mcp verb\" inside a sentence that already explained the\nintercept, leaving two copies of the same clause. Section 10 now also records the trade the\nsecurity re-review named: refusing NotExposed rows before the bridge removes the app-side\nspan a probe used to leave, while adding no disclosure the commands tool did not already make.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: amend the mcp skill block and the knowledge page for the reworked gates\n\nThe binding standard still described a single --allow-irreversible gate and \"exactly one\nwriteln! site\" after the rework added the reversible gate, the read-only default and a\n--help write that precedes any frame; the invariant the source guard now enforces is one\nstdout() acquisition. The knowledge page claimed every error field is a closed-set sentinel,\nwhich one live call refutes (the curated tier's throttle refusal is prose), and called three\ncall-read targets \"tools\".\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: state the fence per surface in the mcp skill block and fix an adr line pointer\n\nThe skill block claimed third-party text is fenced \"unchanged from the CLI\", which the\npre-PR gate refuted for the job tool; a blanket claim in the file every future reviewer loads\nis what would have kept that hole hidden. The bullet now names the fence call on each\nsurface and makes a missing one a finding. ADR-040 cited a line range that pointed at the\nunconfirmed dispatch path instead of the confirmation logic; it names the functions now.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: gate the reversible tier by type, fence job like best-matches, cap every refusal\n\nThree more review rounds each found a shape a scripted run reproduced live: a 300 KB\nnamespace on an unknown-command refusal produced a 300 KB reply because the result\ncap only checked a dispatched command's own payload, never a local refusal that echoes\nthe caller's own oversized input; that refusal's detail told the model to retry through\n\"agent call ns:cmd\", a working bypass recipe addressed to the exact agent the cap exists\nto bound; {\"effect\":5} skipped the string-only validator and returned every row; and the\njob tool fenced only description while best-matches fenced title/company/location too,\nleaving one curated resource open on the same threat.\n\n- Move the MCP_RESULT_MAX_BYTES check into tool_result so it covers every payload the\n  tool wraps, not just a dispatched reply; reword the refusal for the human reading the\n  transcript, never naming a CLI invocation.\n- Validate `effect` by type, not just by value, so a non-string bogus filter is a usage\n  error instead of an unfiltered success.\n- Replace the raw allow_reversible/allow_irreversible bool pair with a Tier enum\n  (Read/Reversible/Irreversible) so `--allow-irreversible` alone implying the reversible\n  tier is enforced by the type, not a comment; deleting the implication now fails six\n  tests instead of zero.\n- Add fence_posting_display_fields and call it from resolve_job the same way\n  fence_best_match_fields already does per row, so `job` and `best-matches` fence\n  title/company/location identically.\n- Word the launch-tier instructions notice by tier, not by the literal flag typed\n  (--allow-irreversible alone was claiming the caller passed --allow-reversible too);\n  indent the mcp row in --help like every verb row; drop the trailing blank line\n  writeln! left after --help; and restore a dropped clause explaining why the\n  regenerate-token proof is vacuous even for `connected`, not just `port`/`token`.\n\nEvery fix here carries a test that fails when reverted; each was mutation-checked by\nhand (see the handoff) and restored before the next.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* refactor: forward instead of panic when the mcp tool lookup has no arm\n\nThe rework replaced an unreachable match arm with an expect. The invariant holds today\n(NotExposed returns earlier, so every remaining Effect maps to a tool), but this path runs\nunder panic = \"abort\" above crash reporting, where a panic is a silent server death. The\nlookup now forwards to the app on None, which refuses on its own.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: answer the coderabbit round on the mcp pages\n\nScope the method-not-found claim to request methods (a notification gets no frame), scope\nthe README requirement to bridge-backed calls (initialize, --help and the local commands\ntool run with the app stopped), rewrite the knowledge page's MCP section as pointers into\nthe owning symbols instead of copied versions, flags, sentinels and PII targets, and record\nsingle-flight dispatch in ADR-040 as a decision with its ceiling and its named follow-up.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* ui: restore contrast on the agent-system hero link\n\nThe how-it-works link added to the agent-system note in #1091 inherits the page's accent\nred on the note's tinted background and measures under the 4.5:1 minimum, so the landing\na11y gate fails on every run that reaches it (the reveal fade makes the measured blend vary,\nwhich is why #1091's own gate happened to pass). In-copy links inside the note now inherit\nthe note's text colour with an underline. Verified locally: build + check:a11y, 13 pages,\n0 violations. Unrelated to the MCP change, but it blocks this PR's required check.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* test: widen the mcp sentinel scanner and pin single-flight dispatch\n\nFrom the CodeRabbit round on the MCP mode. The sentinel drift scanner matched one exact\nspelling (\"error\": \" with a single space), so a compact literal or a rustfmt line split\nescaped it while the non-empty sanity check stayed green; it now walks the source and\ntolerates any whitespace around the key, the colon and the opening quote, mutation-checked\nwith a compact and an unregistered literal. A new test drives serve() with a tools/call, a\nping and a second tools/call and asserts replies come back in input order with the ping\nanswered only after the in-flight call, pinning the single-flight contract that ADR-040\nsection 12 now records; the module doc names the reader-task/serialized-writer follow-up.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n---------\n\nCo-authored-by: Claude Fable 5.1 <noreply@anthropic.com>",
          "timestamp": "2026-09-02T07:05:15+02:00",
          "tree_id": "c04ab1061b4b9e4368437c6cb258ae3534da9990",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/44fd17dde4ad2dbe8e5719db52b84fda02f51475"
        },
        "date": 1788326224117,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2284982,
            "range": "± 15482",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2747888,
            "range": "± 45601",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 310918,
            "range": "± 7932",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "49699333+dependabot[bot]@users.noreply.github.com",
            "name": "dependabot[bot]",
            "username": "dependabot[bot]"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "84cbcc6d5edb79ae245ddc494e8fc7ad678d986f",
          "message": "chore: bump the desktop-rust group (#1097)\n\nBumps the desktop-rust group in /apps/desktop/src-tauri with 15 updates:\n\n| Package | From | To |\n| --- | --- | --- |\n| [tauri-plugin-opener](https://github.com/tauri-apps/plugins-workspace) | `2.5.4` | `2.5.5` |\n| [tauri-plugin-dialog](https://github.com/tauri-apps/plugins-workspace) | `2.7.2` | `2.7.3` |\n| [tauri-plugin-updater](https://github.com/tauri-apps/plugins-workspace) | `2.10.1` | `2.11.0` |\n| [tauri-plugin-clipboard-manager](https://github.com/tauri-apps/plugins-workspace) | `2.3.2` | `2.3.3` |\n| [tauri-plugin-shell](https://github.com/tauri-apps/plugins-workspace) | `2.3.5` | `2.3.6` |\n| [tauri-plugin-log](https://github.com/tauri-apps/plugins-workspace) | `2.9.0` | `2.9.1` |\n| [sentry](https://github.com/getsentry/sentry-rust) | `0.49.1` | `0.49.2` |\n| [tauri-plugin-single-instance](https://github.com/tauri-apps/plugins-workspace) | `2.4.3` | `2.4.4` |\n| [tauri-plugin-notification](https://github.com/tauri-apps/plugins-workspace) | `2.3.3` | `2.4.0` |\n| [tauri-plugin-deep-link](https://github.com/tauri-apps/plugins-workspace) | `2.4.9` | `2.4.10` |\n| [tauri-plugin-positioner](https://github.com/tauri-apps/plugins-workspace) | `2.3.3` | `2.3.4` |\n| [tauri-plugin-websocket](https://github.com/tauri-apps/plugins-workspace) | `2.4.2` | `2.4.3` |\n| [uuid](https://github.com/uuid-rs/uuid) | `1.25.0` | `1.26.0` |\n| [flate2](https://github.com/rust-lang/flate2-rs) | `1.1.9` | `1.1.10` |\n| [aes](https://github.com/RustCrypto/block-ciphers) | `0.9.2` | `0.9.3` |\n\n\nUpdates `tauri-plugin-opener` from 2.5.4 to 2.5.5\n- [Release notes](https://github.com/tauri-apps/plugins-workspace/releases)\n- [Commits](https://github.com/tauri-apps/plugins-workspace/compare/http-v2.5.4...http-v2.5.5)\n\nUpdates `tauri-plugin-dialog` from 2.7.2 to 2.7.3\n- [Release notes](https://github.com/tauri-apps/plugins-workspace/releases)\n- [Commits](https://github.com/tauri-apps/plugins-workspace/compare/dialog-v2.7.2...dialog-v2.7.3)\n\nUpdates `tauri-plugin-updater` from 2.10.1 to 2.11.0\n- [Release notes](https://github.com/tauri-apps/plugins-workspace/releases)\n- [Commits](https://github.com/tauri-apps/plugins-workspace/compare/updater-v2.10.1...updater-v2.11.0)\n\nUpdates `tauri-plugin-clipboard-manager` from 2.3.2 to 2.3.3\n- [Release notes](https://github.com/tauri-apps/plugins-workspace/releases)\n- [Commits](https://github.com/tauri-apps/plugins-workspace/compare/os-v2.3.2...nfc-v2.3.3)\n\nUpdates `tauri-plugin-shell` from 2.3.5 to 2.3.6\n- [Release notes](https://github.com/tauri-apps/plugins-workspace/releases)\n- [Commits](https://github.com/tauri-apps/plugins-workspace/compare/nfc-v2.3.5...nfc-v2.3.6)\n\nUpdates `tauri-plugin-log` from 2.9.0 to 2.9.1\n- [Release notes](https://github.com/tauri-apps/plugins-workspace/releases)\n- [Commits](https://github.com/tauri-apps/plugins-workspace/compare/log-v2.9.0...log-v2.9.1)\n\nUpdates `sentry` from 0.49.1 to 0.49.2\n- [Release notes](https://github.com/getsentry/sentry-rust/releases)\n- [Changelog](https://github.com/getsentry/sentry-rust/blob/master/CHANGELOG.md)\n- [Commits](https://github.com/getsentry/sentry-rust/compare/0.49.1...0.49.2)\n\nUpdates `tauri-plugin-single-instance` from 2.4.3 to 2.4.4\n- [Release notes](https://github.com/tauri-apps/plugins-workspace/releases)\n- [Commits](https://github.com/tauri-apps/plugins-workspace/compare/fs-v2.4.3...fs-v2.4.4)\n\nUpdates `tauri-plugin-notification` from 2.3.3 to 2.4.0\n- [Release notes](https://github.com/tauri-apps/plugins-workspace/releases)\n- [Commits](https://github.com/tauri-apps/plugins-workspace/compare/nfc-v2.3.3...fs-v2.4.0)\n\nUpdates `tauri-plugin-deep-link` from 2.4.9 to 2.4.10\n- [Release notes](https://github.com/tauri-apps/plugins-workspace/releases)\n- [Commits](https://github.com/tauri-apps/plugins-workspace/compare/deep-link-v2.4.9...deep-link-v2.4.10)\n\nUpdates `tauri-plugin-positioner` from 2.3.3 to 2.3.4\n- [Release notes](https://github.com/tauri-apps/plugins-workspace/releases)\n- [Commits](https://github.com/tauri-apps/plugins-workspace/compare/nfc-v2.3.3...nfc-v2.3.4)\n\nUpdates `tauri-plugin-websocket` from 2.4.2 to 2.4.3\n- [Release notes](https://github.com/tauri-apps/plugins-workspace/releases)\n- [Commits](https://github.com/tauri-apps/plugins-workspace/compare/fs-v2.4.2...fs-v2.4.3)\n\nUpdates `uuid` from 1.25.0 to 1.26.0\n- [Release notes](https://github.com/uuid-rs/uuid/releases)\n- [Commits](https://github.com/uuid-rs/uuid/compare/1.25.0...v1.26.0)\n\nUpdates `flate2` from 1.1.9 to 1.1.10\n- [Release notes](https://github.com/rust-lang/flate2-rs/releases)\n- [Commits](https://github.com/rust-lang/flate2-rs/compare/1.1.9...1.1.10)\n\nUpdates `aes` from 0.9.2 to 0.9.3\n- [Commits](https://github.com/RustCrypto/block-ciphers/compare/aes-v0.9.2...aes-v0.9.3)\n\n---\nupdated-dependencies:\n- dependency-name: tauri-plugin-opener\n  dependency-version: 2.5.5\n  dependency-type: direct:production\n  update-type: version-update:semver-patch\n  dependency-group: desktop-rust\n- dependency-name: tauri-plugin-dialog\n  dependency-version: 2.7.3\n  dependency-type: direct:production\n  update-type: version-update:semver-patch\n  dependency-group: desktop-rust\n- dependency-name: tauri-plugin-updater\n  dependency-version: 2.11.0\n  dependency-type: direct:production\n  update-type: version-update:semver-minor\n  dependency-group: desktop-rust\n- dependency-name: tauri-plugin-clipboard-manager\n  dependency-version: 2.3.3\n  dependency-type: direct:production\n  update-type: version-update:semver-patch\n  dependency-group: desktop-rust\n- dependency-name: tauri-plugin-shell\n  dependency-version: 2.3.6\n  dependency-type: direct:production\n  update-type: version-update:semver-patch\n  dependency-group: desktop-rust\n- dependency-name: tauri-plugin-log\n  dependency-version: 2.9.1\n  dependency-type: direct:production\n  update-type: version-update:semver-patch\n  dependency-group: desktop-rust\n- dependency-name: sentry\n  dependency-version: 0.49.2\n  dependency-type: direct:production\n  update-type: version-update:semver-patch\n  dependency-group: desktop-rust\n- dependency-name: tauri-plugin-single-instance\n  dependency-version: 2.4.4\n  dependency-type: direct:production\n  update-type: version-update:semver-patch\n  dependency-group: desktop-rust\n- dependency-name: tauri-plugin-notification\n  dependency-version: 2.4.0\n  dependency-type: direct:production\n  update-type: version-update:semver-minor\n  dependency-group: desktop-rust\n- dependency-name: tauri-plugin-deep-link\n  dependency-version: 2.4.10\n  dependency-type: direct:production\n  update-type: version-update:semver-patch\n  dependency-group: desktop-rust\n- dependency-name: tauri-plugin-positioner\n  dependency-version: 2.3.4\n  dependency-type: direct:production\n  update-type: version-update:semver-patch\n  dependency-group: desktop-rust\n- dependency-name: tauri-plugin-websocket\n  dependency-version: 2.4.3\n  dependency-type: direct:production\n  update-type: version-update:semver-patch\n  dependency-group: desktop-rust\n- dependency-name: uuid\n  dependency-version: 1.26.0\n  dependency-type: direct:production\n  update-type: version-update:semver-minor\n  dependency-group: desktop-rust\n- dependency-name: flate2\n  dependency-version: 1.1.10\n  dependency-type: direct:production\n  update-type: version-update:semver-patch\n  dependency-group: desktop-rust\n- dependency-name: aes\n  dependency-version: 0.9.3\n  dependency-type: direct:production\n  update-type: version-update:semver-patch\n  dependency-group: desktop-rust\n...\n\nSigned-off-by: dependabot[bot] <support@github.com>\nCo-authored-by: dependabot[bot] <49699333+dependabot[bot]@users.noreply.github.com>",
          "timestamp": "2026-09-02T20:21:05+02:00",
          "tree_id": "0316ff2ac2fc008a3a0f79911ba17a6b5159b9db",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/84cbcc6d5edb79ae245ddc494e8fc7ad678d986f"
        },
        "date": 1788374712831,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2299437,
            "range": "± 25327",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2737169,
            "range": "± 35080",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 310167,
            "range": "± 9877",
            "unit": "ns/iter"
          }
        ]
      },
      {
        "commit": {
          "author": {
            "email": "51081940+saeedkolivand@users.noreply.github.com",
            "name": "Saeed Kolivand",
            "username": "saeedkolivand"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "0c14e8f15a38f4ced0a5f3bf5a9f5c96cfcb3702",
          "message": "feat: help chat, a11y lint over the design system, orphan copy cleanup and the mcp residuals (#1101)\n\n* feat: hybrid help search command with a text-hash vector cache\n\nhelp_search retrieves the best help entries for a question with the shipped\nretrieval stack (ephemeral FTS5 lexical arm, dense cosine arm, reciprocal-rank\nfusion). The renderer sends the active locale's entries per request, so no\ntranslation bundle is baked into the binary. The dense arm runs only under the\nsemantic-scoring opt-in and degrades honestly; one embedding-config snapshot\nfeeds both the daily-ceiling charge and the embed call; entry vectors are\ncached by text hash in a new help_vectors table that factory reset and an\nembedding-space change both clear. The policy row is Irreversible for the same\nreason the postings search's is. A lexical eval over the real corpus asserts\nten contested phrasings reach their entry in the top three.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* feat: answer mcp pings and local tools while a bridge call is in flight\n\nThe serve loop becomes one reader, one dispatcher and one writer over a\nchannel: bridge-backed tool calls still run single-flight in input order\nthrough one worker, while ping, initialize, tools/list and every local tool are\nanswered immediately from the writer thread. EOF drains queued calls; a write\nerror still ends the server with exit 0. The irreversible confirm path gains a\npure core so match, mismatch and unavailable proof are tested, including that\na mismatch never dispatches.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* feat: help chat on the support page over the shipped corpus\n\nAsk a question and get a grounded, streamed answer: the page's own entries are\nsent for retrieval, the top hits are rendered as trusted app copy, and the\nquestion, chat history and a capped glance of the user's data are fenced as\nuntrusted text with every fence tag neutralised. The keyword-only mode is\nsurfaced when semantic scoring is off. Also removes the thirteen orphaned\ndashboard translation leaves and adds the clear-location label the hoisted\nLocationInput control needs.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: keyboard-reachable ui primitives and the a11y lint over packages/ui\n\nThe advisory jsx-a11y job linted only the renderer. It now covers packages/ui\nwith two scoped, reasoned overrides, and the real defects it surfaced are\nfixed: a clear control nested inside another button, a clickable tag with no\nrole or key handling, an hrefless anchor, composite containers in the tab\norder, and mouse-only image panning.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: describe the dashboard the landing architecture map actually ships\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: record adr-043 and resync adr-040/041/042 for the help chat and mcp changes\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* refactor: move the agent-cli policy types out of the table module\n\npolicy.rs sat one line under the R8 cap after the help_search row. The\ntypes and the classification rules move to policy/types.rs, re-exported so\nevery import path is unchanged; the POLICY table itself is byte-identical.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: pin sentry log and metric pipelines off without the deprecated options\n\nMain's sentry patch bump deprecated enable_logs and enable_metrics; the\nfirst only governs integrations this build never installs and the second is\na no-op. Both calls go, and the privacy property moves to three static legs:\nthe feature gate in the manifest, an empty integrations list, and an egress\nscan for any capture call site.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: rank help questions by term overlap and bound the dense arm and its cache\n\nThe help arm inherited the postings search's implicit AND, so a full-sentence\nquestion matched nothing on a default install; it now ORs its terms so BM25\nranks by overlap, and the eval carries chat-shaped sentences that fail under\nAND. The dense arm stops on the dense-arm deadline and reports unavailable\nwhen it could not pair every entry; cache-miss embeds are capped per request\nand help_vectors joins the prune sweep, so an agent-tier caller cannot grow\nthe table without bound. Both spend-charging policy rows now carry the\nweak-proof flag the table's doctrine asks for.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: bound the mcp call queue and the drain after eof\n\nThe reader/dispatcher split had replaced the pipe's backpressure with\nunbounded channels. The bridge-call queue is now bounded and a call that\narrives while it is full is refused with a server_busy sentinel; after EOF the\nloop drains under one absolute deadline and exits even if calls remain.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: help chat states, prompt markers and primitive targets from review\n\nThe chat card's source chips ellipsize on a block label, the streamed answer\nno longer scroll-hijacks the page, one always-mounted live region announces\nthinking, errors and answers without re-reading the previous reply, muted text\nmeets the small-text contrast floor, a failed question keeps its text and\noffers retry, and the component now has a render test per state. The prompt\ndefuses forged section markers and sizes cloud models correctly; the recent\napplication list leaves the machine only for application questions and the\nglance is fetched inside send. Tag, MarkdownMessage, LocationInput and\nImagePreview take the reviewers' target-size, inline-flow and focus fixes.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: resync adr-040 and adr-043 for the review-round changes\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: bound the mcp reader queue and index the help vector sweep\n\nThe reader-to-writer event channel was still unbounded, so a client that\nstopped draining stdout could grow the process; it is now a bounded channel\nthe reader blocks on. The help_vectors migration gains the created_at index\nthe row-cap sweep is written for, the cache docs carry the concurrency\ncaveat, and the retrieval eval records its top-two rate against the smallest\nproduction limit.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: help chat single-flight, lightbox focus restore and primitive nits\n\nStop no longer re-enables Ask while retrieval is still running, a new run\naborts the controller it replaces, and both are pinned by tests along with\nthe shared-query-key claim of the glance fetch. The image lightbox restores\nfocus to its opener on close, the markdown anchor carries noreferrer, and the\ncomposite containers that gained a programmatic tab stop do not paint a ring\non a mousedown in their gaps.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: name the three help-vector bounds and the concurrency caveat in adr-043\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: guard markdown link schemes in the shape static analysis recognises\n\nCodeQL flagged the anchor href as DOM text reinterpreted as HTML: the user's\nown editor text flows into a markdown link and a regex on a trimmed copy is\nnot a barrier its taint model tracks. The same allowlist is now a startsWith\ncheck on the exact string that reaches the attribute.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: help dense-arm completeness after ranking and bounded mcp shutdown replies\n\nThe completeness check now compares the ranked count, so a degenerate vector\ncan no longer yield a partial ranking reported as hybrid; the per-request\nembed cap is asserted against the real corpus; the semantic-scoring gate has\na source guard; single-character CJK tokens survive the OR query. In the MCP\nloop a call the worker would start after EOF without budget to finish is\nanswered with a shutting_down sentinel instead of being abandoned, every\nearly exit sets the abandoned flag, and the drain-deadline test gains an\norder of magnitude of scheduling slack.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* fix: markdown links safe on any click, help glance tolerant of one failed read\n\nThe scheme allowlist is a startsWith check on the string that reaches href,\nthe form static analysis recognises, and a middle-click is cancelled too.\nChat history keeps its newest turns when trimmed and indented forged markers\nare defused. Text typed while an answer streams survives; the glance reads\nsettle independently and an unavailable source is omitted rather than read\nas zero; the lightbox traps focus with the house hook and dialog shells keep\ntheir focus indicator; the API page's TSDoc is pointers only.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n* docs: record the rewritten mcp loop's real-binary smoke in adr-040\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_012EQX4DcucKiSdacnJHHxgz\n\n---------\n\nCo-authored-by: Claude Fable 5.1 <noreply@anthropic.com>",
          "timestamp": "2026-09-03T01:27:51+02:00",
          "tree_id": "2ade35a4059dc96a7cd540b300d115b37ba9218c",
          "url": "https://github.com/saeedkolivand/ai-job-hunter-app/commit/0c14e8f15a38f4ced0a5f3bf5a9f5c96cfcb3702"
        },
        "date": 1788393130588,
        "tool": "cargo",
        "benches": [
          {
            "name": "pdf/classic",
            "value": 2377360,
            "range": "± 19475",
            "unit": "ns/iter"
          },
          {
            "name": "pdf/atelier_two_column",
            "value": 2888603,
            "range": "± 25843",
            "unit": "ns/iter"
          },
          {
            "name": "docx_classic",
            "value": 259993,
            "range": "± 4617",
            "unit": "ns/iter"
          }
        ]
      }
    ]
  }
}