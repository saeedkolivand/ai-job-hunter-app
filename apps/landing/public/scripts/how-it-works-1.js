/* console easter egg — devtools hello, in the pipeline's voice */
      (function () { try {
        var head = 'color:#0a0e17;background:#6ea8fe;font:700 18px/1.4 monospace;padding:6px 12px';
        var teal = 'color:#37d39b;font:13px/1.7 monospace';
        var soft = 'color:#93a0b8;font:13px/1.7 monospace';
        console.log('%c scrape → match → write → you press send ', head);
        console.log('%cthat\'s the whole pipeline, and it runs on your machine. read it 👉 https://github.com/saeedkolivand/ai-job-hunter-app', teal);
        console.log('%c(the one step left to you is the one I keep getting rejected at.)', soft);
      } catch (e) {} })();
