// Marketing-tier "back to the chaos" link — the `.top-back` chrome repeated at
// the top of privacy/download/creature body.html. Server component, no props.
export function BackLink() {
  return (
    <a className="top-back" href="/">
      ← back to the chaos
    </a>
  );
}
