//! `validate`'s own test suite — split by topic under the R8 LOC cap. `support` holds the one
//! shared fixture (`has_command`) more than one topic module needs.

mod basic;
mod nested;
mod ordering;
mod support;
