//! `proof`'s own test suite — split by topic under the R8 LOC cap, mirroring the section
//! dividers the single file used to carry (`build_input`, `extract`, fencing-parity, `hint`,
//! the grace window).

mod build_input;
mod extract_list_match;
mod extract_scalar;
mod fencing_a;
mod fencing_b;
mod grace_window;
mod hint;
mod support;
