{
  "name": "@ajh/extension",
  "version": "0.140.0",
  "private": true,
  "type": "module",
  "description": "AI Job Hunter — browser extension (MV3, Chrome + Firefox) bridging job pages to the local desktop app",
  "scripts": {
    "build": "pnpm build:chrome && pnpm build:firefox",
    "build:chrome": "cross-env BROWSER=chrome vite build",
    "build:firefox": "cross-env BROWSER=firefox vite build",
    "package": "pnpm build && node scripts/package.mjs",
    "dev": "cross-env BROWSER=chrome vite build --watch --mode development",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@ajh/shared": "workspace:*",
    "@wxt-dev/browser": "^0.2.6"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.6",
    "cross-env": "^10.1.0",
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
