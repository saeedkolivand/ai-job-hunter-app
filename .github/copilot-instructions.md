# AI Job Hunter — Copilot Instructions

**Read `AGENTS.md` in the repository root and follow it in full — it is the canonical, single source of truth for all coding rules.**

Copilot reads `AGENTS.md` natively, but not in every feature, and repository-wide instructions outrank agent instructions in precedence — so this pointer stays to cover the gap. Never duplicate rules here; edit `AGENTS.md` instead. Claude Code's extra orchestration lives in `CLAUDE.md` and does not apply to Copilot.
