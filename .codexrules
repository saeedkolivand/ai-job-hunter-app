# AI Job Hunter — Codex Rules

**Read `AGENTS.md` in the repository root and follow it in full — it is the canonical, single source of truth for all coding rules.** Codex reads `AGENTS.md` natively; this file exists only so the pointer is unmissable.

Never duplicate rules here — edit `AGENTS.md` instead. Claude Code's extra orchestration lives in `CLAUDE.md` and does not apply to you.
