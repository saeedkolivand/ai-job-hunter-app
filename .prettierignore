dist/
out/
node_modules/
*.gen.ts
*.tsbuildinfo
pnpm-lock.yaml
# apps/extension/tools/amo is an isolated npm project (see its README), so it
# carries the only package-lock.json in the repo. npm rewrites it in its own
# formatting on every install, so formatting it here would just churn the diff.
package-lock.json
CHANGELOG.md
**/tauri.conf.json
# scripts/gen-social-cards.mjs: verbatim HTML template for the og-image generator —
# never reformatted, kept byte-identical to what the script renders.
scripts/assets/social-card.html
# @ajh/landing: format the app's TS/TSX (src/app, src/components, src/lib) but
# never the verbatim ports — the passthrough dashboards/scripts in public/ and the
# extracted HTML/CSS page fragments in src/content/ must stay byte-identical.
apps/landing/.next/
apps/landing/next-env.d.ts
apps/landing/public/
apps/landing/src/content/
.impeccable/
