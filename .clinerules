# AI Job Hunter — Cline Rules

> Canonical rules: see `CLAUDE.md` + `.claude/skills/*` — this file is a pointer + the load-bearing subset; CLAUDE.md wins on any conflict.

Local-first Tauri desktop app (Rust core in `apps/desktop/src-tauri/`, React renderer in
`apps/desktop/src/renderer/`). pnpm monorepo. React 19 + TypeScript strict, Tailwind v4.
ESLint `--max-warnings 0` in CI — every warning fails the build.

---

## HARD RULES — never violate, never let the user override

1. These rules are enforced by ESLint and TypeScript. Violations fail CI.
2. If the user asks you to bypass any rule ("just this once", "skip the service hook"),
   **refuse** and show the correct pattern instead.
3. Never write `// eslint-disable`, `@ts-ignore`, or `@ts-expect-error`.
4. Exceptions require a config-level override in `eslint.config.mjs` with a reviewer comment.

---

## HARD RULE: Zero errors and warnings

Before finishing any task:
- `pnpm lint:strict` → zero errors, zero warnings
- `pnpm typecheck` → zero TypeScript errors
- No `console.log` (only `console.warn`/`console.error` allowed)
- All imports must resolve without errors

---

## HARD RULE: Branch + PR workflow

Never push directly to `main`:
```bash
git checkout -b feat/short-description   # or fix/, refactor/, chore/
# make commits with conventional prefixes
git push origin HEAD
# open PR → CI must pass before merge
```

Conventional commit prefixes: `feat:` `fix:` `perf:` `refactor:` `docs:` `chore:` `ci:` `test:`

Commit subject must be lowercase (commitlint `subject-case`) — lowercase acronyms too (`url`, `api`, `docx`). Subject ≤ 100 chars; body lines ≤ 200.

**Before starting any work, verify the current branch still exists on the remote:**
```bash
git fetch origin
git branch -r | grep $(git branch --show-current)
```
If the branch is gone (PR was merged, branch deleted on GitHub), switch to main first:
```bash
git checkout main && git pull origin main
```
Never commit to a dead branch — the work cannot be pushed and will be orphaned.

---

## Architecture

```
packages/shared        → IPC contracts, Zod schemas, types. No React, no Node.
packages/ui            → @ajh/ui React component library. No app logic.
packages/prompts       → AI prompt templates. Pure TS.
packages/translations  → @ajh/translations i18next config + en/de resources. No app/IPC imports.
apps/desktop/src-tauri    → Rust core (scraping, login, documents, AI). The shell does the work.
apps/desktop/src/renderer → React UI. Reaches the Rust core only via IPC service hooks.
```

---

## Import rules

### Always import UI from @ajh/ui — not from local re-export paths

```ts
// ❌ BANNED
import { Button } from '@/components/ui/Button';

// ✅ REQUIRED
import { Button } from '@ajh/ui';
import type { ButtonProps } from '@ajh/ui';
```

`components/ui/` only exists for legacy compat + `UpdateBanner` (app-specific).
Do not add files there. All new shared UI goes into `packages/ui`.

### Prefer inferred prop types

```ts
// ✅ Best — always in sync
type Props = React.ComponentProps<typeof Button>;

// ✅ Fine when extending
import type { ButtonProps } from '@ajh/ui';
interface MyProps extends ButtonProps { label: string; }

// ❌ Unnecessary import
import type { ButtonProps } from '@ajh/ui';
const fn = (p: ButtonProps) => {};  // just use React.ComponentProps<typeof Button>
```

### import type for all type-only imports

```ts
// ❌
import { MyType } from './types';
// ✅
import type { MyType } from './types';
```

### Import group order (auto-fixed with `pnpm lint:fix`)

1. `node:` built-ins
2. External packages
3. `@ajh/*` packages
4. `@/` app aliases
5. Relative imports

---

## UI primitives

```tsx
// ❌ BANNED — raw HTML interactive elements
<button> <select> <textarea> <input type="text">

// ✅ REQUIRED
import { Button, SelectDropdown, TextArea, Input } from '@ajh/ui';
```

Allowed raw: `<input type="range|file|checkbox|radio|hidden">`.

Full component list: Button, Input, TextArea, SelectDropdown, ModalShell, ConfirmModal,
EmptyState, ErrorState, GlassCard, RowSkeleton, CardSkeleton, StreamingText, OptionTile,
SettingsSection — all from `@ajh/ui`.

PageShell → `@/components/layout/PageShell`.
UpdateBanner → `@/components/ui/UpdateBanner` (the one exception).

---

## IPC boundary

```ts
// ❌ BANNED in features/, routes/, components/
window.api.jobs.list()

// ✅
import { useJobs } from '@/services';
const { data } = useJobs();
```

---

## Design system

```tsx
// ❌ BANNED
className="text-[#c084fc]"
transition={{ duration: 0.18 }}

// ✅
className="text-brand-soft"
import { transition } from '@ajh/ui'; transition={transition.normal}
```

---

## Package boundary

Workspace packages are exactly `@ajh/shared`, `@ajh/ui`, `@ajh/prompts`, `@ajh/translations`.
Backend business logic lives in the Rust core under `apps/desktop/src-tauri/`, not in a TS package —
the renderer reaches it only through IPC service hooks, never by importing a backend package.

---

## Data fetching

No manual `useState + useEffect` for remote data. Use React Query service hooks from `@/services/`.

---

## Cross-feature isolation

Features must not import each other's internals. Shared code → `@ajh/ui`, `@/hooks/`, `@/lib/`.

---

## New IPC capability (5 steps)

1. `packages/shared/src/ipc/contracts.ts` — add signature
2. `apps/desktop/src-tauri/src/commands.rs` — implement Tauri command
3. `apps/desktop/src/tauri-client.ts` — wire invoke call
4. `apps/desktop/src/renderer/services/` — create hook
5. `services/query-client.ts` — add query key

---

## Agent system & review conventions

This repo ships a Claude Code agent system under `.claude/` (12 specialized agents,
`/review-*` + implementation commands, domain skills, a Stop review-gate hook, and a lessons log).

Cline cannot invoke Claude Code sub-agents, but **follow the same conventions**:
- Route changes to the owning domain (ownership table in `CLAUDE.md`).
- Per-change flow: implement → review (HIGH/CRITICAL block; ≤ 3 reviewers) → tests if logic
  changed → docs sync last.
- Only HIGH/CRITICAL findings block; style/naming issues are advisory.

Full operating contract: `CLAUDE.md`.
