# AI Job Hunter — Roo Rules

**Read `AGENTS.md` in the repository root and follow it in full — it is the canonical, single source of truth for all coding rules.**

The rules there are enforced by ESLint, TypeScript, and CI (`--max-warnings 0`). Never duplicate them here — edit `AGENTS.md` instead. Claude Code's extra orchestration lives in `CLAUDE.md` and does not apply to you.
